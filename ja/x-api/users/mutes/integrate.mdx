---
title: 連携ガイド
sidebarTitle: 連携ガイド
description: ミュート関連エンドポイントを統合する際の主要な概念とベストプラクティス
keywords: ["ミュート連携", "ユーザーのミュート統合", "ミュートガイド"]
---

import { Button } from '/snippets/ja/button.mdx';

このガイドでは、アプリケーションにミュート関連のエンドポイントを組み込むために必要な主要な概念を説明します。

***

<div id="authentication">
  ## 認証
</div>

ミュートのエンドポイントで非公開のミュートリストにアクセスするには、ユーザー認証が必要です。

| メソッド | 説明 |
|:-------|:------------|
| [OAuth 2.0 Authorization Code with PKCE](/ja/resources/fundamentals/authentication#oauth-2-0-authorization-code-flow-with-pkce-2) | 新しいアプリケーション向けに推奨されます |
| [OAuth 1.0a User Context](/ja/resources/fundamentals/authentication) | レガシーサポート |

<Warning>
  App-only 認証はサポートされていません。ユーザーの代理として認証する必要があります。
</Warning>

<div id="required-scopes-oauth-20">
  ### 必要なスコープ（OAuth 2.0）
</div>

| Scope | 必須となる操作 |
|:------|:-------------|
| `mute.read` | ミュートしているアカウントの取得 |
| `mute.write` | アカウントのミュートおよびミュート解除 |
| `users.read` | mute スコープと併用する場合に必須 |

***

<div id="endpoints-overview">
  ## エンドポイント概要
</div>

| Method | Endpoint | 説明 |
|:-------|:---------|:------------|
| GET | `/2/users/:id/muting` | ミュートしているアカウントの一覧を取得 |
| POST | `/2/users/:id/muting` | アカウントをミュート |
| DELETE | `/2/users/:source_user_id/muting/:target_user_id` | アカウントのミュートを解除 |

***

<div id="fields-and-expansions">
  ## フィールドと展開
</div>

<div id="default-response">
  ### デフォルトのレスポンス
</div>

```json
{
  "data": [
    {
      "id": "1234567890",
      "name": "Example User",
      "username": "example"
    }
  ]
}
```

<div id="available-fields">
  ### 利用可能なフィールド
</div>

<Accordion title="user.fields">
  | フィールド | 説明 |
  |:------|:------------|
  | `created_at` | アカウント作成日時 |
  | `description` | ユーザーの自己紹介 |
  | `profile_image_url` | プロフィール画像のURL |
  | `public_metrics` | フォロー／フォロワー数 |
  | `verified` | 認証ステータス |
</Accordion>

<Accordion title="expansions">
  | 展開 | 説明 |
  |:----------|:------------|
  | `pinned_tweet_id` | ユーザーの固定された投稿 |
</Accordion>

<div id="example-with-fields">
  ### フィールドを指定した例
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/123456789/muting?\
  user.fields=username,verified,created_at&\
  max_results=100" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

  # 追加のフィールド情報付きでミュート中のユーザーを取得
  for page in client.users.get_muting(
      user_id="123456789",
      user_fields=["username", "verified", "created_at"],
      max_results=100
  ):
      for user in page.data:
          print(f"{user.username} - Verified: {user.verified}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

  const paginator = client.users.getMuting("123456789", {
    userFields: ["username", "verified", "created_at"],
    maxResults: 100,
  });

  for await (const page of paginator) {
    page.data?.forEach((user) => {
      console.log(`${user.username} - Verified: ${user.verified}`);
    });
  }
  ```
</CodeGroup>

***

<div id="pagination">
  ## ページネーション
</div>

ミュートリストが大きいユーザーの場合、結果はページ分割されて返されます。

<CodeGroup dropdown>
  ```bash cURL
  # 最初のリクエスト
  curl "https://api.x.com/2/users/123/muting?max_results=100" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"

  # ページネーション用トークンを指定した後続リクエスト
  curl "https://api.x.com/2/users/123/muting?max_results=100&pagination_token=NEXT_TOKEN" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

  # SDK がページネーションを自動的に処理します
  all_muted = []

  for page in client.users.get_muting(user_id="123", max_results=100):
      if page.data:
          all_muted.extend(page.data)

  print(f"Muted {len(all_muted)} users")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

  async function getAllMutedUsers(userId) {
    const allMuted = [];

    // SDK がページネーションを自動的に処理します
    const paginator = client.users.getMuting(userId, { maxResults: 100 });

    for await (const page of paginator) {
      if (page.data) {
        allMuted.push(...page.data);
      }
    }

    return allMuted;
  }

  // 使用例
  const muted = await getAllMutedUsers("123");
  console.log(`Muted ${muted.length} users`);
  ```
</CodeGroup>

<Card title="ページネーションガイド" icon="arrow-right" href="/ja/x-api/fundamentals/pagination">
  ページネーションの詳細は、こちらのガイドを参照してください
</Card>

***

<div id="behavior-differences">
  ## 挙動の違い
</div>

<div id="muting-vs-blocking">
  ### ミュートとブロックの違い
</div>

| 機能 | ミュート | ブロック |
|:--------|:-----|:------|
| 相手の投稿が表示されるか | いいえ（非表示） | いいえ |
| 相手に自分の投稿が表示されるか | はい | いいえ |
| 相手が自分をフォローできるか | はい（フォロー可能） | いいえ（フォロー解除） |
| 相手が自分にDMを送れるか | はい | いいえ |
| 通知が送信されるか | いいえ | いいえ |

<Tip>
  ミュートは非公開の操作です。ミュートされたユーザーには通知されず、自分がミュートされたことを知ることもできません。
</Tip>

***

<div id="error-handling">
  ## エラー処理
</div>

| ステータス | エラー | 解決方法 |
|:-------|:------|:---------|
| 400 | 無効なリクエスト | ユーザーIDの形式を確認する |
| 401 | 認証されていません | アクセストークンを確認する |
| 403 | アクセス拒否 | スコープと権限を確認する |
| 404 | 見つかりません | ユーザーが存在しません |
| 429 | リクエストが多すぎます | しばらく待ってから再試行する |

***

<div id="next-steps">
  ## 次のステップ
</div>

<CardGroup cols={2}>
  <Card title="クイックスタート" icon="rocket" href="/ja/x-api/users/mutes/quickstart/manage-mutes-quickstart">
    最初のミュートリクエストを送信する
  </Card>

  <Card title="ブロック" icon="ban" href="/ja/x-api/users/blocks/introduction">
    ミュートの代わりにユーザーをブロックする
  </Card>

  <Card title="APIリファレンス" icon="code" href="/ja/x-api/users/get-muting">
    エンドポイントの詳細ドキュメント
  </Card>

  <Card title="サンプルコード" icon="github" href="https://github.com/xdevplatform/Twitter-API-v2-sample-code">
    動作するコードサンプル
  </Card>
</CardGroup>