---
title: ミュート中のユーザーの取得
sidebarTitle: ミュート中のユーザーの取得
description: ミュートしているユーザーの一覧を取得する
keywords: ["ミュート中のユーザー 取得 クイックスタート", "ミュートユーザー 取得 クイックスタート", "ミュート中のユーザー 取得 チュートリアル", "ミュート中のユーザー 取得 ガイド", "ミュート クイックスタート"]
---

import { Button } from '/snippets/ja/button.mdx';

このガイドでは、X API を使用してミュート中のユーザー一覧を取得する方法を説明します。

<Note>
  **前提条件**

  作業を始める前に、次のものを用意してください。

  * 承認済みの App を含む [開発者アカウント](https://developer.x.com/en/portal/petition/essential/basic-info)
  * ユーザーアクセストークン（OAuth 1.0a または OAuth 2.0 PKCE）
</Note>

***

<div id="get-your-muted-users">
  ## ミュートしているユーザーを取得する
</div>

<Steps>
  <Step title="自分のユーザーIDを取得する">
    認証済みユーザーのIDが必要です。[user lookup endpoint](/ja/x-api/users/lookup/introduction) を使うか、Access Token から確認できます（数値部分があなたのユーザーIDです）。
  </Step>

  <Step title="ミュートしているユーザーを取得するリクエストを送信する">
    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/users/123456789/muting?\
      user.fields=created_at,username,verified&\
      max_results=100" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

      # ページネーションを使ってミュートしているユーザーを取得する
      for page in client.users.get_muting(
          "123456789",
          user_fields=["created_at", "username", "verified"],
          max_results=100
      ):
          for user in page.data:
              print(f"{user.username} - Muted")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

      // ページネーションを使ってミュートしているユーザーを取得する
      const paginator = client.users.getMuting("123456789", {
        userFields: ["created_at", "username", "verified"],
        maxResults: 100,
      });

      for await (const page of paginator) {
        page.data?.forEach((user) => {
          console.log(`${user.username} - Muted`);
        });
      }
      ```
    </CodeGroup>
  </Step>

  <Step title="レスポンスを確認する">
    ```json
    {
      "data": [
        {
          "id": "2244994945",
          "name": "X Developers",
          "username": "XDevelopers",
          "created_at": "2013-12-14T04:35:55.000Z",
          "verified": true
        }
      ],
      "meta": {
        "result_count": 1,
        "next_token": "1710819323648428707"
      }
    }
    ```
  </Step>
</Steps>

***

<div id="include-additional-data">
  ## 追加データを含める
</div>

ピン留めされた投稿などの関連データを取得するには、expansions パラメーターを使用します。

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/123456789/muting?\
  user.fields=created_at&\
  expansions=pinned_tweet_id&\
  tweet.fields=created_at" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

  # expansions を使用してミュートしているユーザーを取得
  for page in client.users.get_muting(
      "123456789",
      user_fields=["created_at"],
      expansions=["pinned_tweet_id"],
      tweet_fields=["created_at"]
  ):
      for user in page.data:
          print(f"{user.username}")
      # ピン留めされた投稿は page.includes.tweets に含まれます
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

  // expansions を使用してミュートしているユーザーを取得
  const paginator = client.users.getMuting("123456789", {
    userFields: ["created_at"],
    expansions: ["pinned_tweet_id"],
    tweetFields: ["created_at"],
  });

  for await (const page of paginator) {
    page.data?.forEach((user) => {
      console.log(user.username);
    });
    // ピン留めされた投稿は page.includes?.tweets に含まれます
  }
  ```
</CodeGroup>

<div id="response-with-expansion">
  ### 拡張付きレスポンス
</div>

```json
{
  "data": [
    {
      "username": "XDevelopers",
      "created_at": "2013-12-14T04:35:55.000Z",
      "id": "2244994945",
      "name": "X Developers",
      "pinned_tweet_id": "1430984356139470849"
    }
  ],
  "includes": {
    "tweets": [
      {
        "created_at": "2021-08-26T20:03:51.000Z",
        "id": "1430984356139470849",
        "text": "Help us build a better X Developer Platform!..."
      }
    ]
  },
  "meta": {
    "result_count": 1
  }
}
```

***

<div id="paginate-through-results">
  ## 結果をページング処理する
</div>

SDK はページネーションを自動で処理します。cURL を使用する場合は、レスポンスの `next_token` を使用します。

```bash
curl "https://api.x.com/2/users/123456789/muting?\
max_results=100&\
pagination_token=1710819323648428707" \
  -H "Authorization: Bearer $USER_ACCESS_TOKEN"
```

***

<div id="next-steps">
  ## 次のステップ
</div>

<CardGroup cols={2}>
  <Card title="ミュートを管理" icon="volume-xmark" href="/ja/x-api/users/mutes/quickstart/manage-mutes-quickstart">
    ユーザーをミュート／ミュート解除する
  </Card>

  <Card title="APIリファレンス" icon="code" href="/ja/x-api/users/get-muted-users">
    エンドポイントの完全なドキュメント
  </Card>
</CardGroup>