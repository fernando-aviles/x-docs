---
title: はじめに
sidebarTitle: はじめに
keywords: ["DM blocks", "direct message blocks", "block DMs", "DM blocking", "direct message blocking", "block messages"]
---

DM ブロック管理エンドポイントを使用すると、認証済みユーザーに代わって指定したアカウントをブロックまたはブロック解除できます。これらのエンドポイントでは、2 つの POST メソッドを利用できます。

* **/2/users/:id/dm/block**: アカウントをブロックします
* **/2/users/:id/dm/unblock**: アカウントのブロックを解除します

<div id="getting-started">
  ### はじめに
</div>

<div id="authentication">
  ### 認証
</div>

ユーザーの代理としてリクエストを送信するため、これらのエンドポイントは [OAuth 1.0a ユーザーコンテキスト](/ja/resources/fundamentals/authentication#obtaining-access-tokens-using-3-legged-oauth-flow) または [OAuth 2.0 Authorization Code with PKCE](/ja/resources/fundamentals/authentication#oauth-2-0-authorization-code-flow-with-pkce-2) で認証し、リクエスト対象のユーザーに紐づくユーザーアクセストークンを使用する必要があります。ユーザーアクセストークンは、[3-legged OAuth flow](https://developer.x.com/resources/fundamentals/authentication/obtaining-user-access-tokens)（OAuth 1.0a）または [Authorization Code with PKCE grant flow](/ja/resources/fundamentals/authentication#oauth-2-0-authorization-code-flow-with-pkce-2)（OAuth 2.0）を使用して生成できます。

<div id="making-a-request">
  ### リクエストの送信
</div>

ブロック

ユーザーがアプリで認証を完了すると、次のようにそのユーザーに代わって Block エンドポイントを呼び出すことができます。

```
      curl --request POST 'https://api.x.com/2/users/:id/dm/block' --header 'Authorization: ••••••'
```

リクエストが成功すると、次のような JSON レスポンスが表示されます。

```
{
   "data": {
       "blocked": true
   }
}
```

**ブロック解除**

ユーザーがあなたのアプリで認証を済ませたら、以下のようにユーザーに代わって Unblock エンドポイントを呼び出せます。

```
      curl --request POST 'https://api.x.com/2/users/:id/dm/unblock' --header 'Authorization: ••••••'
```

リクエストが成功した場合、次のような JSON レスポンスが表示されます。

```
{
   "data": {
       "blocked": false
   }
}
```
