---
title: クイックスタート
sidebarTitle: クイックスタート
description: リストタイムラインから投稿を取得する
keywords: ["リスト 投稿 クイックスタート", "リストタイムライン クイックスタート", "リストの投稿を取得するチュートリアル"]
---

import { Button } from '/snippets/ja/button.mdx';

このガイドでは、リストのタイムラインから投稿を取得する手順を説明します。

<Note>
  **前提条件**

  始める前に、次のものが必要です。

  * 承認済みのアプリを持つ[開発者アカウント](https://developer.x.com/en/portal/petition/essential/basic-info)
  * アプリのBearer Token
</Note>

***

<Steps>
  <Step title="リストIDを見つける" icon="list">
    x.comでリストを表示しているときのURLからリストIDを確認できます。

    ```
    https://x.com/i/lists/84839422
                          └── ここがリストIDです
    ```
  </Step>

  <Step title="リストのタイムラインをリクエストする" icon="terminal">
    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/lists/84839422/tweets?\
      tweet.fields=created_at,public_metrics,author_id&\
      expansions=author_id&\
      user.fields=username,verified&\
      max_results=10" \
        -H "Authorization: Bearer $BEARER_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # ページネーション付きでリストから投稿を取得する
      for page in client.lists.get_tweets(
          "84839422",
          tweet_fields=["created_at", "public_metrics", "author_id"],
          expansions=["author_id"],
          user_fields=["username", "verified"],
          max_results=10
      ):
          for post in page.data:
              print(f"{post.text[:50]}... - Likes: {post.public_metrics.like_count}")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      // ページネーション付きでリストから投稿を取得する
      const paginator = client.lists.getTweets("84839422", {
        tweetFields: ["created_at", "public_metrics", "author_id"],
        expansions: ["author_id"],
        userFields: ["username", "verified"],
        maxResults: 10,
      });

      for await (const page of paginator) {
        page.data?.forEach((post) => {
          console.log(`${post.text?.slice(0, 50)}... - Likes: ${post.public_metrics?.like_count}`);
        });
      }
      ```
    </CodeGroup>
  </Step>

  <Step title="レスポンスを確認する" icon="eye">
    ```json
    {
      "data": [
        {
          "id": "1458172421115101189",
          "text": "Check out our latest announcement...",
          "author_id": "4172587277",
          "created_at": "2024-01-15T10:30:00.000Z",
          "public_metrics": {
            "retweet_count": 42,
            "reply_count": 5,
            "like_count": 156,
            "quote_count": 3
          },
          "edit_history_tweet_ids": ["1458172421115101189"]
        }
      ],
      "includes": {
        "users": [
          {
            "id": "4172587277",
            "username": "TechNews",
            "verified": true
          }
        ]
      },
      "meta": {
        "result_count": 1,
        "next_token": "7140dibdnow9c7btw3z2vwioavpvutgzrzm9icis4ndix"
      }
    }
    ```
  </Step>

  <Step title="結果をページングする" icon="arrow-right">
    SDKではページネーションが自動的に処理されます。cURLを使用する場合は、さらに投稿を取得するためにレスポンス内の`next_token`を使用します。

    ```bash
    curl "https://api.x.com/2/lists/84839422/tweets?\
    max_results=10&\
    pagination_token=7140dibdnow9c7btw3z2vwioavpvutgzrzm9icis4ndix" \
      -H "Authorization: Bearer $BEARER_TOKEN"
    ```
  </Step>
</Steps>

<Note>
  このエンドポイントは、指定したリストから最新の投稿を最大800件まで返します。
</Note>

***

<div id="next-steps">
  ## 次のステップ
</div>

<CardGroup cols={2}>
  <Card title="リスト検索" icon="list" href="/ja/x-api/lists/list-lookup/quickstart">
    リストの詳細を取得
  </Card>

  <Card title="リストメンバー" icon="users" href="/ja/x-api/lists/list-members/introduction">
    リストメンバーを取得
  </Card>

  <Card title="連携ガイド" icon="book" href="/ja/x-api/lists/list-tweets/integrate">
    重要な概念とベストプラクティス
  </Card>

  <Card title="APIリファレンス" icon="code" href="/ja/x-api/lists/get-list-tweets">
    エンドポイントの詳細なドキュメント
  </Card>
</CardGroup>