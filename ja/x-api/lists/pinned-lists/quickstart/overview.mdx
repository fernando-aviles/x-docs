---
title: ピン留めリストの概要
sidebarTitle: 概要
description: ピン留めリスト用エンドポイントの利用を開始する
keywords: ["ピン留めリスト 概要", "ピン留めリスト クイックスタート", "リストをピン留め 概要", "リストのピン留め 概要"]
---

import { Button } from '/snippets/ja/button.mdx';

ピン留めされた Lists エンドポイントを使用すると、ユーザーがピン留めしている Lists を取得し、どの Lists をピン留めするかを管理できます。

<Note>
  **前提条件**

  開始する前に、次のものが必要です。

  * 承認済みの App を含む[開発者アカウント](https://developer.x.com/en/portal/petition/essential/basic-info)
  * User Access Token（OAuth 1.0a または OAuth 2.0 PKCE）
</Note>

***

<div id="available-endpoints">
  ## 利用可能なエンドポイント
</div>

<CardGroup cols={2}>
  <Card title="ピン留め済みリストの取得" icon="thumbtack" href="/ja/x-api/lists/pinned-lists/quickstart/pinned-list-lookup">
    ピン留め済みリストを取得します
  </Card>

  <Card title="ピン留め済みリストの管理" icon="pin" href="/ja/x-api/lists/pinned-lists/quickstart/manage-pinned-lists">
    リストをピン留め／ピン留め解除します
  </Card>
</CardGroup>

***

<div id="authentication">
  ## 認証
</div>

| 操作 | 認証方式 |
|:----------|:---------------|
| ピン留めされたListの取得 | OAuth 1.0a または OAuth 2.0 PKCE |
| Listのピン留め/解除 | OAuth 1.0a または OAuth 2.0 PKCE |

<Warning>
  取得と管理の両方の操作には、ユーザーコンテキストでの認証が必要です。アプリケーション単体（Bearer Token）による認証はサポートされていません。
</Warning>

***

<div id="quick-example">
  ## 簡単な例
</div>

<CodeGroup dropdown>
  ```bash cURL
  # 固定されたリストを取得
  curl "https://api.x.com/2/users/2244994945/pinned_lists" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

  # 固定されたリストを取得
  response = client.lists.get_pinned("2244994945")

  for lst in response.data:
      print(f"{lst.name}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

  // 固定されたリストを取得
  const response = await client.lists.getPinned("2244994945");

  response.data?.forEach((lst) => {
    console.log(lst.name);
  });
  ```
</CodeGroup>

***

<div id="next-steps">
  ## 次のステップ
</div>

<CardGroup cols={2}>
  <Card title="リストの検索" icon="list" href="/ja/x-api/lists/list-lookup/quickstart">
    リストの詳細を取得
  </Card>

  <Card title="リストの管理" icon="pen" href="/ja/x-api/lists/manage-lists/quickstart">
    リストを作成と更新
  </Card>
</CardGroup>