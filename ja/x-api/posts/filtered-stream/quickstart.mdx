---
title: クイックスタート
sidebarTitle: クイックスタート
description: フィルタ済みストリームに接続し、投稿をリアルタイムで受信します
keywords: ["フィルタ済みストリーム クイックスタート", "ストリーミング クイックスタート", "フィルタ済みストリーム チュートリアル", "ストリームガイド", "ストリーミング例"]
---

import { Button } from '/snippets/ja/button.mdx';

このガイドでは、フィルタールールに一致するリアルタイムの投稿を受信するために、filtered stream に接続する手順を説明します。

<Note>
  **前提条件**

  開始する前に、以下を用意してください:

  * [開発者アカウント](https://developer.x.com/en/portal/petition/essential/basic-info)
  * App の Bearer Token（Developer Console の &quot;Keys and tokens&quot; で確認できます）
</Note>

***

<Steps>
  <Step title="フィルタールールを作成する" icon="filter">
    ルールは、どの投稿を受け取るかを定義します。演算子を使用して、キーワード、ハッシュタグ、ユーザーなどに一致させます。

    **ルールの例:** 「cat」という語を含み、画像が付いた投稿に一致させる：

    ```
    cat has:images
    ```

    <Card title="ルールを作成" icon="filter" href="/ja/x-api/posts/filtered-stream/integrate/build-a-rule">
      ルール構文と演算子について学ぶ
    </Card>
  </Step>

  <Step title="ストリームにルールを追加" icon="plus">
    rules エンドポイントを使用してルールを追加します。どのルールが各投稿にマッチしたかを識別できるように、`tag` を含めてください。

    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/tweets/search/stream/rules" \
        -H "Authorization: Bearer $BEARER_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{
          "add": [
            {"value": "cat has:images", "tag": "cats with images"}
          ]
        }'
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # フィルタリング済みストリームにルールを追加する
      response = client.filtered_stream.add_rules(
          add=[{"value": "cat has:images", "tag": "cats with images"}]
      )

      for rule in response.data:
          print(f"Rule added: {rule.id} - {rule.value}")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      // フィルタリング済みストリームにルールを追加する
      const response = await client.filteredStream.addRules({
        add: [{ value: "cat has:images", tag: "cats with images" }],
      });

      response.data?.forEach((rule) => {
        console.log(`Rule added: ${rule.id} - ${rule.value}`);
      });
      ```
    </CodeGroup>

    **レスポンス:**

    ```json
    {
      "data": [
        {
          "id": "1273026480692322304",
          "value": "cat has:images",
          "tag": "cats with images"
        }
      ],
      "meta": {
        "sent": "2024-01-15T10:30:00.000Z",
        "summary": {
          "created": 1,
          "not_created": 0,
          "valid": 1,
          "invalid": 0
        }
      }
    }
    ```
  </Step>

  <Step title="ルールを検証する" icon="check">
    アクティブなルールをすべて一覧表示して、自分のルールが追加されたことを確認します:

    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/tweets/search/stream/rules" \
        -H "Authorization: Bearer $BEARER_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # すべてのアクティブなルールを取得
      response = client.filtered_stream.get_rules()

      for rule in response.data:
          print(f"Active rule: {rule.id} - {rule.value}")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      // すべてのアクティブなルールを取得
      const response = await client.filteredStream.getRules();

      response.data?.forEach((rule) => {
        console.log(`Active rule: ${rule.id} - ${rule.value}`);
      });
      ```
    </CodeGroup>
  </Step>

  <Step title="ストリームに接続する" icon="plug">
    条件に一致する投稿を受信するための永続接続を開きます。

    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/tweets/search/stream?\
      tweet.fields=created_at,author_id&\
      expansions=author_id&\
      user.fields=username" \
        -H "Authorization: Bearer $BEARER_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # フィルタ済みストリームに接続
      for post in client.filtered_stream.stream(
          tweet_fields=["created_at", "author_id"],
          expansions=["author_id"],
          user_fields=["username"]
      ):
          print(f"New post: {post.data.text}")
          print(f"Matching rules: {post.matching_rules}")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      // フィルタ済みストリームに接続
      const stream = client.filteredStream.stream({
        tweetFields: ["created_at", "author_id"],
        expansions: ["author_id"],
        userFields: ["username"],
      });

      for await (const post of stream) {
        console.log(`New post: ${post.data?.text}`);
        console.log(`Matching rules: ${post.matching_rules}`);
      }
      ```
    </CodeGroup>
  </Step>

  <Step title="受信した投稿を処理する" icon="message">
    一致する投稿ストリーム（JSONオブジェクト）:

    ```json
    {
      "data": {
        "id": "1234567890",
        "text": "Look at this cute cat! 🐱",
        "author_id": "9876543210",
        "created_at": "2024-01-15T10:35:00.000Z",
        "edit_history_tweet_ids": ["1234567890"]
      },
      "includes": {
        "users": [
          {
            "id": "9876543210",
            "username": "catperson"
          }
        ]
      },
      "matching_rules": [
        {
          "id": "1273026480692322304",
          "tag": "cats with images"
        }
      ]
    }
    ```

    <Tip>
      `matching_rules` 配列には、定義したタグに基づいてどのルールが投稿にマッチしたかが示されます。
    </Tip>
  </Step>

  <Step title="ルールを削除（任意）" icon="trash">
    ID でルールを削除する:

    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/tweets/search/stream/rules" \
        -H "Authorization: Bearer $BEARER_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{
          "delete": {
            "ids": ["1273026480692322304"]
          }
        }'
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # ID でルールを削除
      response = client.filtered_stream.delete_rules(
          delete={"ids": ["1273026480692322304"]}
      )
      print(f"Deleted: {response.meta.summary.deleted}")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      // ID でルールを削除
      const response = await client.filteredStream.deleteRules({
        delete: { ids: ["1273026480692322304"] },
      });
      console.log(`Deleted: ${response.meta?.summary?.deleted}`);
      ```
    </CodeGroup>
  </Step>
</Steps>

***

<div id="managing-your-connection">
  ## 接続の管理
</div>

<AccordionGroup>
  <Accordion title="キープアライブ信号">
    ストリームは 20 秒ごとに空行（`\r\n`）を送信します。20 秒間 `data` もキープアライブも受信しない場合は、再接続してください。
  </Accordion>

  <Accordion title="切断">
    接続を閉じるには `Ctrl+C` を押すか、ターミナルウィンドウを閉じてください。
  </Accordion>

  <Accordion title="接続数の制限">
    1 つの App につき確立できる接続は 1 つだけです。新しい接続を確立すると、既存の接続は閉じられます。
  </Accordion>
</AccordionGroup>

***

<div id="next-steps">
  ## 次のステップ
</div>

<CardGroup cols={2}>
  <Card title="ルールを作成する" icon="filter" href="/ja/x-api/posts/filtered-stream/integrate/build-a-rule">
    ルール構文について学ぶ
  </Card>

  <Card title="演算子リファレンス" icon="list-check" href="/ja/x-api/posts/filtered-stream/integrate/operators">
    利用可能なすべての演算子
  </Card>

  <Card title="切断時の処理" icon="plug" href="/ja/x-api/posts/filtered-stream/integrate/handling-disconnections">
    スムーズに再接続する
  </Card>

  <Card title="APIリファレンス" icon="code" href="/ja/x-api/posts/filtered-stream">
    エンドポイントの詳細なドキュメント
  </Card>
</CardGroup>