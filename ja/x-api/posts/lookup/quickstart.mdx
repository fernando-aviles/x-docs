---
title: クイックスタート
sidebarTitle: クイックスタート
description: 数分で初めての投稿取得リクエストを実行できるようになります
keywords: ["投稿取得クイックスタート", "ツイート検索クイックスタート", "取得チュートリアル", "取得ガイド", "ツイート取得クイックスタート", "取得の例"]
---

import { Button } from "/snippets/ja/button.mdx"

このガイドでは、X API v2 を使って初めての投稿取得リクエストを行う手順を説明します。

<Note>
  **前提条件**

  開始する前に、次のものが必要です。

  * 承認済みの App を持つ [開発者アカウント](https://developer.x.com/en/portal/petition/essential/basic-info)
  * App のベアラートークン（開発者コンソールの「Keys and tokens」で確認できます）
</Note>

***

<Steps>
  <Step title="投稿IDを取得する">
    すべての投稿には一意のIDがあります。投稿のURLに含まれています。

    ```
    https://x.com/XDevelopers/status/1228393702244134912
                                    └── これが投稿 ID です
    ```
  </Step>

  <Step title="リクエストを実行">
    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/tweets/1228393702244134912" \
        -H "Authorization: Bearer $BEARER_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # ID で 1 件の投稿を取得する
      response = client.posts.get("1228393702244134912")
      print(response.data)
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      const response = await client.posts.get("1228393702244134912");
      console.log(response.data);
      ```
    </CodeGroup>
  </Step>

  <Step title="レスポンスを確認する">
    デフォルトのレスポンスには、投稿の `id`、`text`、`edit_history_tweet_ids` が含まれます。

    ```json
    {
      "data": {
        "id": "1228393702244134912",
        "text": "What did the developer write in their Valentine's card?\n\nwhile(true) {\n    I = Love(You);\n}",
        "edit_history_tweet_ids": ["1228393702244134912"]
      }
    }
    ```
  </Step>

  <Step title="追加フィールドをリクエストする">
    クエリパラメータを指定して、より多くのデータを取得します：

    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/tweets/1228393702244134912?\
      tweet.fields=created_at,public_metrics,author_id&\
      expansions=author_id&\
      user.fields=username,verified" \
        -H "Authorization: Bearer $BEARER_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # 追加のフィールドとexpansionsを指定して投稿を取得します
      response = client.posts.get(
          "1228393702244134912",
          tweet_fields=["created_at", "public_metrics", "author_id"],
          expansions=["author_id"],
          user_fields=["username", "verified"]
      )

      print(response.data)
      print(response.includes)  # 投稿者ユーザーオブジェクトを含みます
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      const response = await client.posts.get("1228393702244134912", {
        tweetFields: ["created_at", "public_metrics", "author_id"],
        expansions: ["author_id"],
        userFields: ["username", "verified"],
      });

      console.log(response.data);
      console.log(response.includes); // 投稿者ユーザーオブジェクトを含みます
      ```
    </CodeGroup>

    **レスポンス:**

    ```json
    {
      "data": {
        "id": "1228393702244134912",
        "text": "What did the developer write in their Valentine's card?...",
        "created_at": "2020-02-14T19:00:55.000Z",
        "author_id": "2244994945",
        "public_metrics": {
          "retweet_count": 156,
          "reply_count": 23,
          "like_count": 892,
          "quote_count": 12
        },
        "edit_history_tweet_ids": ["1228393702244134912"]
      },
      "includes": {
        "users": [
          {
            "id": "2244994945",
            "username": "XDevelopers",
            "verified": true
          }
        ]
      }
    }
    ```
  </Step>

  <Step title="複数の投稿を取得する">
    1 回のリクエストで最大 100 件の投稿を取得する:

    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/tweets?\
      ids=1228393702244134912,1227640996038684673,1199786642791452673&\
      tweet.fields=created_at,author_id" \
        -H "Authorization: Bearer $BEARER_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # ID を指定して複数の投稿を取得
      response = client.posts.get_posts(
          ids=["1228393702244134912", "1227640996038684673", "1199786642791452673"],
          tweet_fields=["created_at", "author_id"]
      )

      for post in response.data:
          print(f"{post.id}: {post.text[:50]}...")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      const response = await client.posts.getPosts({
        ids: ["1228393702244134912", "1227640996038684673", "1199786642791452673"],
        tweetFields: ["created_at", "author_id"],
      });

      response.data?.forEach((post) => {
        console.log(`${post.id}: ${post.text?.slice(0, 50)}...`);
      });
      ```
    </CodeGroup>
  </Step>
</Steps>

***

<div id="next-steps">
  ## 次のステップ
</div>

<CardGroup cols={2}>
  <Card title="統合ガイド" icon="book" href="/ja/x-api/posts/lookup/integrate">
    認証、レート制限、ベストプラクティスを学ぶ
  </Card>

  <Card title="フィールドとexpansions" icon="sliders" href="/ja/x-api/fundamentals/fields">
    フィールドとexpansionsシステムをマスターする
  </Card>

  <Card title="APIリファレンス" icon="code" href="/ja/x-api/posts/get-post-by-id">
    利用可能なすべてのパラメータを確認する
  </Card>

  <Card title="サンプルコード" icon="github" href="https://github.com/xdevplatform/Twitter-API-v2-sample-code">
    さらに多くのサンプルコードを確認する
  </Card>
</CardGroup>