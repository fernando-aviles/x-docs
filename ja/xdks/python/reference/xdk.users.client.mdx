---
title: "UsersClient"
sidebarTitle: "UsersClient"
---

<div id="usersclient">
  ## UsersClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">基底クラス: object</Badge>

ユーザー関連の操作を行うクライアント

<div id="constructors">
  ## コンストラクタ
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメータ

<ParamField path="path.client" type="Client"></ParamField>

<div id="block_dms">
  ### `block_dms`
</div>

DM をブロックする
認証済みユーザーに対して、特定のユーザーIDを指定し、そのユーザーとのダイレクトメッセージの送受信をブロックします。

#### パラメーター

<ParamField path="path.id" type="Any">
認証済みユーザーが、そのユーザーとのDMをブロックしようとしている対象ユーザーのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`BlockDmsResponse` - レスポンスデータ

<div id="create_bookmark">
  ### `create_bookmark`
</div>

ブックマークを作成する
認証済みユーザーのブックマークに投稿を追加します。

#### パラメーター

<ParamField path="path.id" type="Any">
ブックマークを追加する認証済み送信元ユーザーの ID。
</ParamField>

<ParamField path="body.body" type="CreateBookmarkRequest">
リクエスト本文
</ParamField>

<div id="delete_bookmark">
  ### `delete_bookmark`
</div>

ブックマークの削除
認証済みユーザーのブックマークから、指定したIDの投稿を削除します。

#### パラメータ

<ParamField path="path.id" type="Any">
ブックマークを削除する認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.tweet_id" type="Any">
ソースユーザーがブックマークから削除する投稿のID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`DeleteBookmarkResponse` - レスポンスデータ

<div id="follow_list">
  ### `follow_list`
</div>

リストをフォロー
認証済みユーザーが、指定したリストのIDを用いてそのリストをフォローします。

#### パラメータ

<ParamField path="path.id" type="Any">
List をフォローする認証済みソース User の ID。
</ParamField>

<ParamField path="body.body" type="FollowListRequest">
リクエストボディ
</ParamField>

<div id="follow_user">
  ### `follow_user`
</div>

ユーザーをフォローする
認証済みユーザーが、指定したユーザー id のアカウントをフォローします。

#### パラメータ

<ParamField path="path.id" type="Any">
ターゲットUserをフォローするようリクエストしている認証済み送信元UserのID。
</ParamField>

<ParamField path="body.body" type="FollowUserRequest">
リクエストボディ
</ParamField>

<div id="get_blocking">
  ### `get_blocking`
</div>

ブロック中ユーザーの取得
指定したユーザーidがブロックしているユーザーの一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
結果を返す対象となる認証済みソース User の ID です。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大件数を指定します。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
結果の特定の「ページ」を取得するために使用するパラメーターです。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示する Tweet フィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetBlockingResponse`

<div id="get_bookmark_folders">
  ### `get_bookmark_folders`
</div>

ブックマークフォルダーの取得
認証済みユーザーが作成したブックマークフォルダーの一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
結果を返す対象となる認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用されるパラメーター。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetBookmarkFoldersResponse`

<div id="get_bookmarks">
  ### `get_bookmarks`
</div>

ブックマークの取得
認証済みユーザーがブックマークした投稿の一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
結果を返す対象となる、認証済みソースユーザーのid。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大件数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用するパラメーター。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドを、カンマ区切りのリストで指定。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドを、カンマ区切りのリストで指定。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するメディアのフィールドを、カンマ区切りのリストで指定。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する投票のフィールドを、カンマ区切りのリストで指定。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドを、カンマ区切りのリストで指定。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する場所のフィールドを、カンマ区切りのリストで指定。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetBookmarksResponse`

<div id="get_bookmarks_by_folder_id">
  ### `get_bookmarks_by_folder_id`
</div>

フォルダーIDでブックマークを取得する
認証済みユーザーの特定のブックマークフォルダー内にある投稿を、そのフォルダーIDを指定して取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
結果を返す対象となる認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.folder_id" type="Any">
認証済みユーザーが投稿を取得しようとしているブックマークフォルダーのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetBookmarksByFolderIdResponse` - レスポンスデータ

<div id="get_by_id">
  ### `get_by_id`
</div>

ID でユーザーを取得
指定したユーザー ID の詳細情報を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
取得するユーザーのid。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByIdResponse` - レスポンスデータ

<div id="get_by_ids">
  ### `get_by_ids`
</div>

id でユーザーを取得
指定した複数のユーザーの詳細を、その id を指定して取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.ids" type="List">
カンマ区切りのユーザーIDのリスト。最大100個まで指定できます。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByIdsResponse` - レスポンスデータ

<div id="get_by_username">
  ### `get_by_username`
</div>

ユーザー名からユーザーを取得
指定したユーザー名に対応するユーザーの詳細を取得します。

#### パラメーター

<ParamField path="path.username" type="str">
ユーザー名。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するTweetフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByUsernameResponse` - レスポンスデータ

<div id="get_by_usernames">
  ### `get_by_usernames`
</div>

ユーザー名でユーザーを取得する
複数のユーザーの詳細を、そのユーザー名から取得します。

#### パラメータ

<ParamField path="path.usernames" type="List">
カンマ区切りのusernameのリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByUsernamesResponse` - レスポンスデータ

<div id="get_followed_lists">
  ### `get_followed_lists`
</div>

フォローしているリストを取得
特定のユーザーのidを指定して、そのユーザーがフォローしているリストを一覧で取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
取得するユーザーのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返される結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
指定した「ページ」の結果を取得するために使用するパラメーターです。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
表示するListフィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserフィールドをカンマ区切りで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetFollowedListsResponse`

<div id="get_followers">
  ### `get_followers`
</div>

フォロワーを取得する
指定したUserのidを基に、そのUserをフォローしているUserの一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
取得対象のユーザーのid。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
取得する結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
結果の特定の「ページ」を取得するために使用するパラメーターです。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetFollowersResponse`

<div id="get_following">
  ### `get_following`
</div>

フォロー中ユーザーの取得
指定したユーザーIDを基に、そのユーザーがフォローしているユーザーの一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
取得するユーザーのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
取得する結果数の上限。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
結果の特定の「ページ」を取得するために使用するパラメーターです。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetFollowingResponse`

<div id="get_liked_posts">
  ### `get_liked_posts`
</div>

いいねした投稿を取得
特定のユーザーのIDを指定して、そのユーザーがいいねした投稿の一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
取得するユーザーのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
結果の次の「ページ」を取得するために使用されるパラメーターです。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するMediaフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示するPollフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示するPlaceフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetLikedPostsResponse`

<div id="get_list_memberships">
  ### `get_list_memberships`
</div>

List メンバーシップを取得する
指定した User の id を使って、そのユーザーがメンバーになっている List の一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
照会するユーザーのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
結果の特定の「ページ」を取得するために使用されるパラメーターです。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
表示するListフィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーフィールドをカンマ区切りで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetListMembershipsResponse`

<div id="get_me">
  ### `get_me`
</div>

自分のユーザー情報を取得
認証されたユーザーの詳細を取得します。

#### パラメータ

<ParamField path="path.user_fields" type="List または None" default="None">
表示する User フィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.expansions" type="List または None" default="None">
展開するフィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List または None" default="None">
表示する Tweet フィールドのカンマ区切りリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetMeResponse` - レスポンスデータ

<div id="get_mentions">
  ### `get_mentions`
</div>

メンションを取得
特定のユーザーIDを指定して、そのユーザーがメンションされている投稿の一覧を取得します。

#### パラメータ

<ParamField path="path.id" type="Any">
照会するユーザーの ID。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
結果セットに含める投稿の最小 ID。このパラメータは、start_time が同時に指定されている場合は start_time より優先されます。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
結果セットに含める投稿の最大 ID。このパラメータは、end_time が同時に指定されている場合は end_time より優先されます。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用するパラメータ。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。投稿が取得される最も早い UTC のタイムスタンプ。since_id パラメータが同時に指定されている場合は、since_id が優先されます。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。投稿が取得される最も遅い UTC のタイムスタンプ。until_id パラメータが同時に指定されている場合は、until_id が優先されます。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示する Tweet のフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示する Media のフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する Poll のフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User のフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する Place のフィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetMentionsResponse`

<div id="get_muting">
  ### `get_muting`
</div>

ミュートの取得
認証済みユーザーがミュート中のユーザーの一覧を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
結果を返す対象となる認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返す結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
結果の次の「ページ」を取得するために使用するパラメーター。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserフィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するTweetフィールドのカンマ区切りリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetMutingResponse`

<div id="get_owned_lists">
  ### `get_owned_lists`
</div>

所有しているリストを取得する
特定のユーザーのidを指定して、そのユーザーが所有しているリストの一覧を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
照会する User の ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
指定した「ページ」の結果を取得するために使用されるパラメーター。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
表示する List フィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetOwnedListsResponse`

<div id="get_pinned_lists">
  ### `get_pinned_lists`
</div>

ピン留めされたリストを取得
認証済みユーザーがピン留めしたリストの一覧を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
結果を返す対象となる認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
表示するListのフィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserのフィールドをカンマ区切りで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetPinnedListsResponse` - レスポンスデータ

<div id="get_posts">
  ### `get_posts`
</div>

投稿を取得
特定のユーザーのIDを指定して、そのユーザーが作成した投稿の一覧を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
照会するユーザーのID。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
結果セットに含める投稿IDの最小値。このパラメーターとstart_timeが両方指定された場合は、このパラメーターが優先されます。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
結果セットに含める投稿IDの最大値。このパラメーターとend_timeが両方指定された場合は、このパラメーターが優先されます。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大件数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用するパラメーターです。
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
除外するエンティティのセット（例: 「replies」や「retweets」）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ形式。投稿が返される最も早いUTCタイムスタンプです。since_idパラメーターも指定されている場合は、since_idが優先されます。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ形式。投稿が返される最も遅いUTCタイムスタンプです。until_idパラメーターも指定されている場合は、until_idが優先されます。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するTweetオブジェクトのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するMediaオブジェクトのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示するPollオブジェクトのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserオブジェクトのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示するPlaceオブジェクトのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetPostsResponse`

<div id="get_reposts_of_me">
  ### `get_reposts_of_me`
</div>

自分の投稿のRepostを取得する
認証済みユーザーの投稿をRepostしている投稿の一覧を取得します。

#### パラメーター

<ParamField path="path.max_results" type="int or None" default="None">
取得する結果の最大数です。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用するパラメーターです。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示する Tweet フィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示する Media フィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する Poll フィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する Place フィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetRepostsOfMeResponse`

<div id="get_timeline">
  ### `get_timeline`
</div>

タイムラインを取得

認証済みユーザーのタイムラインに含まれる投稿を、最新のものが先頭になるよう逆時系列で並べたリストとして取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
逆時系列タイムラインの投稿を取得する対象となる、認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
結果セットに含める最小の投稿ID。このパラメーターは、start_time と両方が指定された場合、start_time より優先されます。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
結果セットに含める最大の投稿ID。このパラメーターは、end_time と両方が指定された場合、end_time より優先されます。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
取得する結果の最大件数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用されるパラメーターです。
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
除外するエンティティの集合（例: 「replies」や「retweets」）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。投稿を取得する最も早い UTC タイムスタンプ。since_id パラメーターも指定されている場合は、since_id が優先されます。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。投稿を取得する最も遅い UTC タイムスタンプ。until_id パラメーターも指定されている場合は、until_id が優先されます。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示する Media フィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する Poll フィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する Place フィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetTimelineResponse`

<div id="like_post">
  ### `like_post`
</div>

投稿をいいねする
認証済みユーザーが、指定した投稿IDの投稿にいいねを付けます。

#### パラメータ

<ParamField path="path.id" type="Any">
投稿に「いいね」を付けるようリクエストしている、認証済みの送信元ユーザーのID。
</ParamField>

<ParamField path="body.body" type="LikePostRequest">
リクエストボディ
</ParamField>

<div id="mute_user">
  ### `mute_user`
</div>

ユーザーをミュートする
認証済みのユーザーが、特定のユーザーをそのidを指定してミュートします。

#### パラメーター

<ParamField path="path.id" type="Any">
ミュート対象のUserをミュートするようリクエストしている、認証済みソースUserのID。
</ParamField>

<ParamField path="body.body" type="MuteUserRequest">
リクエストボディ
</ParamField>

<div id="pin_list">
  ### `pin_list`
</div>

List をピン留めする

認証済みユーザーが、指定した id の List をピン留めします。

#### パラメーター

<ParamField path="path.id" type="Any">
リストをピン留めする認証済みソースユーザーのID。
</ParamField>

<ParamField path="body.body" type="PinListRequest">
リクエストボディ
</ParamField>

<div id="repost_post">
  ### `repost_post`
</div>

投稿をリポストする
認証済みユーザーが指定したIDの投稿をリポストします。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
投稿のRepostをリクエストしている、認証済みの発信元ユーザーのID。
</ParamField>

<ParamField path="body.body" type="RepostPostRequest">
リクエストボディ
</ParamField>

<div id="search">
  ### `search`
</div>

ユーザー検索
検索クエリに一致するユーザーのリストを取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="Any">
ユーザーを検索するためのクエリ文字列。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
取得する結果の最大数。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
このパラメータは、次の「ページ」の結果を取得するために使用されます。パラメータで使用する値は、API が返すレスポンスから直接取得されるものであり、変更してはいけません。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーフィールドを、カンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドを、カンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートフィールドを、カンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorSearchResponse`

<div id="unblock_dms">
  ### `unblock_dms`
</div>

DM のブロック解除
認証済みユーザーに対して、指定したユーザー id の相手とのダイレクトメッセージの送受信ブロックを解除します。

#### パラメーター

<ParamField path="path.id" type="Any">
認証済みユーザーがDMのブロック解除を行おうとしている対象ユーザーのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnblockDmsResponse` - レスポンスデータ

<div id="unfollow_list">
  ### `unfollow_list`
</div>

リストのフォロー解除
認証済みユーザーが、指定した id の特定のリストのフォローを解除します。

#### パラメータ

<ParamField path="path.id" type="Any">
リストのフォローを解除する認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.list_id" type="Any">
フォローを解除するリストのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnfollowListResponse` - レスポンスデータ

<div id="unfollow_user">
  ### `unfollow_user`
</div>

ユーザーのフォロー解除
認証済みユーザーが、指定したidのユーザーのフォローを解除します。

#### パラメーター

<ParamField path="path.source_user_id" type="Any">
フォロー解除をリクエストしている認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.target_user_id" type="Any">
ソースユーザーがフォロー解除をリクエストしているターゲットユーザーのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnfollowUserResponse` - レスポンスデータ

<div id="unlike_post">
  ### `unlike_post`
</div>

投稿の「いいね」を取り消す
認証済みユーザーが、指定したIDの投稿に付けた「いいね」を取り消します。

#### パラメーター

<ParamField path="path.id" type="Any">
投稿のいいね取り消しをリクエストしている、認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.tweet_id" type="Any">
ユーザーがいいねの取り消しをリクエストしている投稿のID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnlikePostResponse` - レスポンスデータ

<div id="unmute_user">
  ### `unmute_user`
</div>

ユーザーのミュート解除
認証済みユーザーが、指定したユーザーのミュートを、そのIDを指定して解除します。

#### パラメーター

<ParamField path="path.source_user_id" type="Any">
ミュート解除をリクエストしている認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.target_user_id" type="Any">
ソースユーザーがミュート解除をリクエストしている対象ユーザーのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnmuteUserResponse` - レスポンスデータ

<div id="unpin_list">
  ### `unpin_list`
</div>

リストのピン留め解除
認証されたユーザーが、指定したIDの特定のリストのピン留めを解除します。

#### パラメーター

<ParamField path="path.id" type="Any">
結果を返す対象となる、認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.list_id" type="Any">
ピン留めを解除するListのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnpinListResponse` - レスポンスデータ

<div id="unrepost_post">
  ### `unrepost_post`
</div>

投稿のリポストを解除する
認証済みユーザーが、指定したIDの特定の投稿のリポストを解除します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
投稿のRepostを要求している認証済みの元ユーザーのID。
</ParamField>

<ParamField path="path.source_tweet_id" type="Any">
ユーザーがRepostの取り消しを要求している投稿のID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnrepostPostResponse` - レスポンスデータ