---
title: "UsersClient"
sidebarTitle: "UsersClient"
---

<div id="usersclient">
  ## UsersClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">基底クラス: object</Badge>

ユーザー関連の操作を行うクライアント

<div id="constructors">
  ## コンストラクタ
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメーター

<ParamField path="path.client" type="Client"></ParamField>

<div id="block_dms">
  ### `block_dms`
</div>

DM をブロック
認証済みユーザーに対し、特定のユーザーの id を指定して、そのユーザーとのダイレクトメッセージの送受信をブロックします。

#### パラメーター

<ParamField path="path.id" type="Any">
認証済みユーザーがDMをブロックしようとしている相手ユーザーのid。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`BlockDmsResponse` - レスポンスデータ

<div id="create_bookmark">
  ### `create_bookmark`
</div>

ブックマークを作成
認証済みユーザーのブックマークに投稿を追加します。

#### パラメーター

<ParamField path="path.id" type="Any">
ブックマークを追加する対象となる、認証済みソースユーザーのID。
</ParamField>

<ParamField path="body.body" type="CreateBookmarkRequest">
リクエストボディ
</ParamField>

<div id="delete_bookmark">
  ### `delete_bookmark`
</div>

ブックマークの削除
認証済みユーザーのブックマークから、ID を指定して投稿を削除します。

#### パラメータ

<ParamField path="path.id" type="Any">
ブックマークを削除する認証済みのソースユーザーのID。
</ParamField>

<ParamField path="path.tweet_id" type="Any">
ソースユーザーがブックマークから削除する投稿のID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`DeleteBookmarkResponse` - レスポンスデータ

<div id="follow_list">
  ### `follow_list`
</div>

リストをフォローする
認証済みユーザーが、指定されたIDの特定のリストをフォローするようにします。

#### パラメーター

<ParamField path="path.id" type="Any">
List をフォローする認証済みユーザーの id。
</ParamField>

<ParamField path="body.body" type="FollowListRequest">
リクエスト本文
</ParamField>

<div id="follow_user">
  ### `follow_user`
</div>

ユーザーをフォローする

認証済みユーザーが、指定したユーザーのidを用いてそのユーザーをフォローします。

#### パラメーター

<ParamField path="path.id" type="Any">
対象Userのフォローをリクエストしている認証済みソースUserのid。
</ParamField>

<ParamField path="body.body" type="FollowUserRequest">
リクエストボディ
</ParamField>

<div id="get_blocking">
  ### `get_blocking`
</div>

ブロック中ユーザーの取得
指定したユーザーidがブロックしているユーザーの一覧を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
結果を返す対象となる認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
特定の「ページ」に対応する結果を取得するために使用するパラメーター。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetBlockingResponse`

<div id="get_bookmark_folders">
  ### `get_bookmark_folders`
</div>

ブックマークフォルダーの取得
認証済みユーザーが作成したブックマークフォルダーの一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
結果を返す対象となる認証済みソースユーザーのid。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大取得件数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用するパラメーター。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetBookmarkFoldersResponse`

<div id="get_bookmarks">
  ### `get_bookmarks`
</div>

ブックマークを取得
認証済みユーザーがブックマークした投稿のリストを取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
結果を返す対象となる、認証済みのソースユーザーのIDです。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数です。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用されるパラメーターです。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドを指定する、カンマ区切りのリストです。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドを指定する、カンマ区切りのリストです。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するメディアのフィールドを指定する、カンマ区切りのリストです。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する投票のフィールドを指定する、カンマ区切りのリストです。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドを指定する、カンマ区切りのリストです。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する場所のフィールドを指定する、カンマ区切りのリストです。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetBookmarksResponse`

<div id="get_bookmarks_by_folder_id">
  ### `get_bookmarks_by_folder_id`
</div>

フォルダーIDによるブックマークの取得
認証済みユーザーについて、指定したIDのブックマークフォルダーに含まれる投稿を取得します。

#### パラメータ

<ParamField path="path.id" type="Any">
結果を返す対象の認証済み送信元ユーザーのID。
</ParamField>

<ParamField path="path.folder_id" type="Any">
認証済みユーザーが投稿を取得しようとしているブックマークフォルダのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetBookmarksByFolderIdResponse` - レスポンスデータ

<div id="get_by_id">
  ### `get_by_id`
</div>

id で User を取得
指定した User の詳細をその id で取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
照会するユーザーのID。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByIdResponse` - レスポンスデータ

<div id="get_by_ids">
  ### `get_by_ids`
</div>

複数の id でユーザーを取得
複数の id を指定して複数ユーザーの詳細を取得します。

#### パラメーター

<ParamField path="path.ids" type="List">
User ID のカンマ区切りリストです。最大 100 個まで指定できます。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドのカンマ区切りリストです。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドのカンマ区切りリストです。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートフィールドのカンマ区切りリストです。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByIdsResponse` - レスポンスデータ

<div id="get_by_username">
  ### `get_by_username`
</div>

ユーザー名でユーザーを取得
指定したユーザー名のユーザーの詳細を取得します。

#### パラメーター

<ParamField path="path.username" type="str">
ユーザー名を指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートフィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByUsernameResponse` - レスポンスデータ

<div id="get_by_usernames">
  ### `get_by_usernames`
</div>

ユーザー名からユーザーを取得する
複数のユーザーについて、ユーザー名を指定して詳細を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.usernames" type="List">
カンマ区切りの username のリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByUsernamesResponse` - レスポンスデータ

<div id="get_followed_lists">
  ### `get_followed_lists`
</div>

フォロー中のリストを取得
ユーザー ID を指定して、そのユーザーがフォローしているリストの一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
検索対象のユーザーのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
取得する結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
特定の「ページ」に対応する結果を取得するために使用されるパラメーターです。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
表示するListのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetFollowedListsResponse`

<div id="get_followers">
  ### `get_followers`
</div>

フォロワーを取得する
指定したユーザーのIDを用いて、そのユーザーをフォローしているユーザーの一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
照会対象ユーザーのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
取得する結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
このパラメーターは、指定した「ページ」の結果を取得するために使用されます。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetFollowersResponse`

<div id="get_following">
  ### `get_following`
</div>

フォロー取得
指定したUserのidを基に、そのUserがフォローしているUserの一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
取得対象の User の ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返す結果の最大数を指定します。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
指定した「ページ」の結果を取得するために使用するパラメーターです。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User のフィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドをカンマ区切りで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetFollowingResponse`

<div id="get_liked_posts">
  ### `get_liked_posts`
</div>

いいねした投稿を取得
特定のユーザーのIDを指定して、そのユーザーがいいねした投稿の一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
取得するユーザーの id。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
結果の次の「ページ」を取得するために使用するパラメーター。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示する Tweet フィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示する Media フィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する Poll フィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する Place フィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetLikedPostsResponse`

<div id="get_list_memberships">
  ### `get_list_memberships`
</div>

List のメンバーシップを取得
指定した User の id を基に、そのユーザーがメンバーになっている List の一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
照会するユーザーの ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大件数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
特定の「ページ」の結果を取得するために使用されるパラメーターです。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
表示する List のフィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User のフィールドをカンマ区切りで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetListMembershipsResponse`

<div id="get_me">
  ### `get_me`
</div>

自分のユーザー情報を取得
認証済みユーザーの詳細を取得します。

#### パラメータ

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示する Tweet フィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetMeResponse` - レスポンスデータ

<div id="get_mentions">
  ### `get_mentions`
</div>

メンションを取得
特定のユーザーIDを指定して、そのユーザーに言及している投稿の一覧を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
取得対象のユーザーのID。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
結果セットに含める投稿IDの最小値。このパラメーターとstart_timeの両方が指定されている場合は、このパラメーターが優先されます。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
結果セットに含める投稿IDの最大値。このパラメーターとend_timeの両方が指定されている場合は、このパラメーターが優先されます。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大件数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用されるパラメーターです。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。投稿が取得される最も早いUTCタイムスタンプ。since_idパラメーターも指定されている場合は、そちらが優先されます。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。投稿が取得される最も遅いUTCタイムスタンプ。until_idパラメーターも指定されている場合は、そちらが優先されます。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するTweetフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するMediaフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示するPollフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示するPlaceフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetMentionsResponse`

<div id="get_muting">
  ### `get_muting`
</div>

ミュート中ユーザーの取得
認証済みユーザーがミュートしているユーザーの一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
結果を返す認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用されるパラメーター。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserフィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートフィールドのカンマ区切りリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetMutingResponse`

<div id="get_owned_lists">
  ### `get_owned_lists`
</div>

所有している List を取得
ユーザーの id を指定して、そのユーザーが所有する List の一覧を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
照会対象ユーザーのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
取得する結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
結果の特定の「ページ」を取得するために使用されるパラメーターです。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
表示する List フィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドのカンマ区切りリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetOwnedListsResponse`

<div id="get_pinned_lists">
  ### `get_pinned_lists`
</div>

ピン留めされたリストを取得する
認証済みユーザーによってピン留めされているリストの一覧を取得します。

#### パラメータ

<ParamField path="path.id" type="Any">
結果を取得する対象の認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
表示する List フィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetPinnedListsResponse` - レスポンスの data

<div id="get_posts">
  ### `get_posts`
</div>

投稿を取得する
特定のユーザーのIDを指定して、そのユーザーが作成した投稿のリストを取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
取得対象ユーザーのID。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
結果セットに含める投稿IDの最小値。このパラメーターは、start_time と両方指定された場合には start_time より優先されます。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
結果セットに含める投稿IDの最大値。このパラメーターは、end_time と両方指定された場合には end_time より優先されます。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大件数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
結果の次の「ページ」を取得するために使用するパラメーターです。
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
除外するエンティティの集合（例: 「replies」や「retweets」）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。投稿が返される最も早いUTCタイムスタンプ。since_id パラメーターも指定されている場合は、since_id が優先されます。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。投稿が返される最も遅いUTCタイムスタンプ。until_id パラメーターも指定されている場合は、until_id が優先されます。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示する Tweet のフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するメディアのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する投票のフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する場所のフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetPostsResponse`

<div id="get_reposts_of_me">
  ### `get_reposts_of_me`
</div>

自分へのRepostを取得する
認証済みユーザーのコンテンツをRepostした投稿の一覧を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数です。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用されるパラメータです。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示する Tweet フィールドのカンマ区切りリストです。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドのカンマ区切りリストです。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示する Media フィールドのカンマ区切りリストです。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する Poll フィールドのカンマ区切りリストです。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドのカンマ区切りリストです。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する Place フィールドのカンマ区切りリストです。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetRepostsOfMeResponse`

<div id="get_timeline">
  ### `get_timeline`
</div>

タイムラインの取得
認証済みユーザーのタイムラインに含まれる投稿を、新しいものから古いものへ並べた一覧として取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
逆時系列タイムラインの投稿を取得する認証済みソースユーザーのID。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
結果セットに含める最小の投稿ID。このパラメータは、start_time と両方が指定された場合、start_time より優先されます。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
結果セットに含める最大の投稿ID。このパラメータは、end_time と両方が指定された場合、end_time より優先されます。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大件数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用するパラメータです。
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
除外するエンティティの集合（例: 「replies」や「retweets」）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。投稿が返される最も早いUTCタイムスタンプ。since_id パラメータが同時に指定されている場合は、since_id が優先されます。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。投稿が返される最も遅いUTCタイムスタンプ。until_id パラメータが同時に指定されている場合は、until_id が優先されます。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するメディアのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する投票のフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する場所のフィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetTimelineResponse`

<div id="like_post">
  ### `like_post`
</div>

投稿をいいねする
認証済みユーザーが、指定したIDの投稿をいいねします。

#### パラメーター

<ParamField path="path.id" type="Any">
投稿にいいねを付けるようリクエストしている認証済みソースユーザーの ID。
</ParamField>

<ParamField path="body.body" type="LikePostRequest">
リクエストボディ
</ParamField>

<div id="mute_user">
  ### `mute_user`
</div>

ユーザーをミュートする
認証済みユーザーが、指定したユーザー id のアカウントをミュートします。

#### パラメータ

<ParamField path="path.id" type="Any">
対象のUserのミュートをリクエストしている認証済み発信元Userのid。
</ParamField>

<ParamField path="body.body" type="MuteUserRequest">
リクエストボディ
</ParamField>

<div id="pin_list">
  ### `pin_list`
</div>

リストをピン留めする
認証済みユーザーが、指定したIDのリストをピン留めします。

#### パラメーター

<ParamField path="path.id" type="Any">
List をピン留めする認証済みソース User の ID。
</ParamField>

<ParamField path="body.body" type="PinListRequest">
リクエストボディ
</ParamField>

<div id="repost_post">
  ### `repost_post`
</div>

投稿をリポストする
認証済みユーザーが、指定したIDの投稿をリポストします。

#### パラメーター

<ParamField path="path.id" type="Any">
投稿のリポストをリクエストしている認証済みの送信元ユーザーのID。
</ParamField>

<ParamField path="body.body" type="RepostPostRequest">
リクエストボディ
</ParamField>

<div id="search">
  ### `search`
</div>

ユーザーを検索
検索クエリに一致するユーザーのリストを取得します。

#### パラメータ

<ParamField path="path.query" type="Any">
ユーザーを検索するためのクエリ文字列。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
取得する結果の最大数。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
このパラメータは、結果の次の「ページ」を取得するために使用されます。このパラメータに指定する値は、API が返すレスポンスからそのまま取得するものであり、変更しないでください。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートフィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorSearchResponse`

<div id="unblock_dms">
  ### `unblock_dms`
</div>

DM のブロック解除
認証ユーザーに対して、指定したユーザーの id を用いて、そのユーザーとのダイレクトメッセージの送受信に対するブロックを解除します。

#### パラメーター

<ParamField path="path.id" type="Any">
認証済みのユーザーがDMブロックの解除をリクエストしている対象ユーザーのIDです。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnblockDmsResponse` - レスポンスデータ

<div id="unfollow_list">
  ### `unfollow_list`
</div>

リストのフォロー解除
認証済みユーザーが、指定したidのリストのフォローを解除します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
List のフォローを解除する認証済みの発信元ユーザーの ID。
</ParamField>

<ParamField path="path.list_id" type="Any">
フォローを解除する List の ID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnfollowListResponse` - レスポンスデータ

<div id="unfollow_user">
  ### `unfollow_user`
</div>

ユーザーのフォローを解除する
認証済みユーザーが、指定したユーザーの id を使ってそのユーザーのフォローを解除します。

#### パラメータ

<ParamField path="path.source_user_id" type="Any">
対象Userのフォロー解除を要求している、認証済みソースUserのID。
</ParamField>

<ParamField path="path.target_user_id" type="Any">
ソースUserがフォロー解除を要求しているターゲットUserのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnfollowUserResponse` - レスポンス data

<div id="unlike_post">
  ### `unlike_post`
</div>

投稿の「いいね」を取り消す
認証済みユーザーが、ID を指定して特定の投稿の「いいね」を取り消します。

#### パラメーター

<ParamField path="path.id" type="Any">
いいねを取り消すよう要求している認証済みソースユーザーの ID。
</ParamField>

<ParamField path="path.tweet_id" type="Any">
ユーザーがいいねの取り消しを要求している投稿の ID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnlikePostResponse` - レスポンスの data

<div id="unmute_user">
  ### `unmute_user`
</div>

ユーザーのミュート解除
認証済みユーザーが、指定したIDのユーザーのミュートを解除します。

#### パラメータ

<ParamField path="path.source_user_id" type="Any">
ミュート解除をリクエストしている認証済みの発信元ユーザーのID。
</ParamField>

<ParamField path="path.target_user_id" type="Any">
発信元ユーザーがミュート解除をリクエストしている対象ユーザーのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnmuteUserResponse` - レスポンスデータ

<div id="unpin_list">
  ### `unpin_list`
</div>

リストのピン留め解除
認証済みユーザーが、指定した id を持つ特定のリストのピン留めを解除します。

#### パラメーター

<ParamField path="path.id" type="Any">
結果を返す対象となる認証済みソースユーザーの ID。
</ParamField>

<ParamField path="path.list_id" type="Any">
ピン留めを解除するリストの ID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnpinListResponse` - レスポンスデータ

<div id="unrepost_post">
  ### `unrepost_post`
</div>

投稿のRepostを解除
認証済みユーザーが、指定されたIDの特定の投稿のRepostを解除します。

#### パラメーター

<ParamField path="path.id" type="Any">
認証済みのソースユーザーで、投稿のリポストをリクエストしているユーザーのID。
</ParamField>

<ParamField path="path.source_tweet_id" type="Any">
ユーザーがリツイート解除をリクエストしている投稿のID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`UnrepostPostResponse` - レスポンスデータ