---
title: "CommunitiesClient"
sidebarTitle: "CommunitiesClient"
---

<div id="communitiesclient">
  ## CommunitiesClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">基底クラス: object</Badge>

コミュニティ機能を操作するクライアント

<div id="constructors">
  ## コンストラクター
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメータ

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_by_id">
  ### `get_by_id`
</div>

ID を指定して Community を取得
指定した ID の Community の詳細を取得します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.id" type="Any">
Community の ID。
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
表示する Community のフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByIdResponse` - レスポンスデータ

<div id="search">
  ### `search`
</div>

コミュニティを検索
指定した検索クエリに一致するコミュニティのリストを取得します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.query" type="str">
コミュニティを検索するためのクエリ。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
1 回のリクエストで返される検索結果の最大数。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用されるパラメーターです。このパラメーターで使用する値は、API が提供するレスポンスからそのまま取得されるもので、変更してはいけません。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用されるパラメーターです。このパラメーターで使用する値は、API が提供するレスポンスからそのまま取得されるもので、変更してはいけません。
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
表示する Community のフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorSearchResponse`