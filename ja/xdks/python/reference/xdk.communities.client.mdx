---
title: "CommunitiesClient"
sidebarTitle: "CommunitiesClient"
---

<div id="communitiesclient">
  ## CommunitiesClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">基底クラス: object</Badge>

コミュニティに関する操作を行う Client

<div id="constructors">
  ## コンストラクタ
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメータ

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_by_id">
  ### `get_by_id`
</div>

ID で Community を取得
指定した Community の詳細を、その ID を用いて取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
コミュニティのID。
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
表示するコミュニティのフィールドをカンマ区切りで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByIdResponse` - レスポンスデータ

<div id="search">
  ### `search`
</div>

コミュニティの検索
指定した検索クエリに一致するコミュニティのリストを取得します。

#### パラメータ

<ParamField path="path.query" type="str">
コミュニティを検索するためのクエリ。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
1 回のリクエストで返される検索結果の最大数。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
このパラメータは、次の「ページ」の結果を取得するために使用されます。このパラメータで使用する値は、API から返されるレスポンスから直接取得されたものであり、変更してはいけません。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
このパラメータは、次の「ページ」の結果を取得するために使用されます。このパラメータで使用する値は、API から返されるレスポンスから直接取得されたものであり、変更してはいけません。
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
表示するコミュニティのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorSearchResponse`