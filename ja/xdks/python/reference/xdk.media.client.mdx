---
title: "MediaClient"
sidebarTitle: "MediaClient"
---

<div id="mediaclient">
  ## MediaClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">継承元: object</Badge>

メディア操作用のクライアント

<div id="constructors">
  ## コンストラクタ
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメーター

<ParamField path="path.client" type="Client"></ParamField>

<div id="append_upload">
  ### `append_upload`
</div>

メディアアップロードへの追記
Media アップロードリクエストに data を追記します。

#### パラメータ

<ParamField path="path.id" type="Any">
append 操作を実行する対象メディアの識別子。
</ParamField>

<ParamField path="body.body" type="AppendUploadRequest">
リクエストボディ
</ParamField>

<div id="create_metadata">
  ### `create_metadata`
</div>

メディアのメタデータを作成
メディアファイルのメタデータを作成します。
body: リクエストボディ
:returns: レスポンスデータ
:rtype: CreateMetadataResponse

#### パラメータ

<ParamField path="body.body" type="CreateMetadataRequest"></ParamField>

<div id="create_subtitles">
  ### `create_subtitles`
</div>

Media の字幕を作成します
指定した Media ファイルに字幕を作成します。
body: リクエストボディ
:returns: レスポンスデータ
:rtype: CreateSubtitlesResponse

#### パラメータ

<ParamField path="body.body" type="CreateSubtitlesRequest"></ParamField>

<div id="delete_subtitles">
  ### `delete_subtitles`
</div>

Media の字幕を削除します。
特定の Media ファイルの字幕を削除します。
body: リクエストボディ
:returns: レスポンスの data
:rtype: DeleteSubtitlesResponse

#### パラメーター

<ParamField path="body.body" type="DeleteSubtitlesRequest"></ParamField>

<div id="finalize_upload">
  ### `finalize_upload`
</div>

メディアのアップロードの完了
メディアのアップロードリクエストを完了します。

#### パラメータ

<ParamField path="path.id" type="Any">
ファイナライズ対象のメディアのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`FinalizeUploadResponse` - レスポンスデータ

<div id="get_analytics">
  ### `get_analytics`
</div>

メディアのアナリティクスを取得
メディアに関するアナリティクスデータを取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.media_keys" type="List">
Media Key のカンマ区切りリスト。1つのリクエストにつき最大100件まで指定できます。
</ParamField>

<ParamField path="path.end_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。時間範囲の終了を表す UTC タイムスタンプ。
</ParamField>

<ParamField path="path.start_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。時間範囲の開始を表す UTC タイムスタンプ。
</ParamField>

<ParamField path="path.granularity" type="str">
検索件数結果の集計粒度。
</ParamField>

<ParamField path="path.media_analytics_fields" type="List or None" default="None">
表示する MediaAnalytics フィールドのカンマ区切りリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetAnalyticsResponse` - レスポンスデータ

<div id="get_by_key">
  ### `get_by_key`
</div>

メディアキーで Media を取得します  
メディアキーを指定して、特定の Media オブジェクトの詳細を取得します。

#### パラメーター

<ParamField path="path.media_key" type="Any">
1つの Media Key。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示する Media フィールドのカンマ区切りリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByKeyResponse` - レスポンスの data

<div id="get_by_keys">
  ### `get_by_keys`
</div>

Media をメディアキーで取得します
メディアキーを指定して Media ファイルの詳細を取得します。

#### パラメーター

<ParamField path="path.media_keys" type="List">
カンマ区切りの Media Keys のリストです。1つのリクエストにつき最大100個まで指定できます。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示する Media フィールドをカンマ区切りで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByKeysResponse` - レスポンスのdata

<div id="get_upload_status">
  ### `get_upload_status`
</div>

メディアアップロードのステータスを取得
メディアアップロードのステータスを、その id を指定して取得します。

#### パラメーター

<ParamField path="path.media_id" type="Any">
取得対象のメディアアップロードステータスに対応するメディア id。
</ParamField>

<ParamField path="path.command" type="str or None" default="None">
メディアアップロードリクエストに対するコマンド。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetUploadStatusResponse` - レスポンスデータ

<div id="initialize_upload">
  ### `initialize_upload`
</div>

メディアアップロードの初期化
メディアアップロードを初期化します。
body: リクエストボディ
:returns: レスポンスデータ
:rtype: InitializeUploadResponse

#### パラメータ

<ParamField path="body.body" type="InitializeUploadRequest"></ParamField>

<div id="upload">
  ### `upload`
</div>

メディアのアップロード
投稿またはその他のコンテンツで使用するためのメディアファイルをアップロードします。
body: リクエストボディ
:returns: レスポンスの data
:rtype: UploadResponse

#### パラメーター

<ParamField path="body.body" type="UploadRequest"></ParamField>