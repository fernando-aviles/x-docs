---
title: "WebhooksClient"
sidebarTitle: "WebhooksClient"
---

<div id="webhooksclient">
  ## WebhooksClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">基底クラス: object</Badge>

webhooks 操作を行うためのクライアント

<div id="constructors">
  ## コンストラクタ
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメータ

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

Webhook の作成
新しい Webhook 設定を作成します。
body: リクエストボディ
:returns: レスポンスデータ
:rtype: CreateResponse

#### パラメータ

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="create_stream_link">
  ### `create_stream_link`
</div>

ストリームリンクを作成
指定された webhook に FilteredStream イベントを配信するためのリンクを作成します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.webhook_id" type="Any">
FilteredStream のルールセットに関連付ける webhook の ID。
</ParamField>

<ParamField path="path.tweet_fields" type="str or None" default="None">
表示するツイートのフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="str or None" default="None">
展開対象のフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.media_fields" type="str or None" default="None">
表示するメディアフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.poll_fields" type="str or None" default="None">
表示する投票フィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.user_fields" type="str or None" default="None">
表示するユーザーフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.place_fields" type="str or None" default="None">
表示する場所フィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`CreateStreamLinkResponse` - レスポンスデータ

<div id="create_webhook_replay_job">
  ### `create_webhook_replay_job`
</div>

Webhook のリプレイジョブを作成します
Webhook に対して配信済み、または配信が試行されたすべてのイベントについて、過去最大 24 時間分のイベントを取得するリプレイジョブを作成します。
body: リクエストボディ
:returns: レスポンスの data
:rtype: CreateWebhookReplayJobResponse

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="body.body" type="CreateWebhookReplayJobRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

Webhook の削除
既存の webhook 構成を削除します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.webhook_id" type="Any">
削除対象の Webhook の ID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`DeleteResponse` - レスポンスデータ

<div id="delete_stream_link">
  ### `delete_stream_link`
</div>

ストリームリンクを削除
指定した webhook への FilteredStream イベントからのリンクを削除します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.webhook_id" type="Any">
FilteredStream のルールセットにリンクする webhook の ID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`DeleteStreamLinkResponse` - レスポンスデータ

<div id="get">
  ### `get`
</div>

webhook を取得
クライアントアプリに関連付けられている webhook 設定の一覧を取得します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.webhook_config_fields" type="List or None" default="None">
表示する WebhookConfig のフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetResponse` - レスポンスデータ

<div id="get_stream_links">
  ### `get_stream_links`
</div>

ストリームリンクを取得
フィルタ済みストリームのルールセットに関連付けられている webhook リンクの一覧を取得します。
:returns: レスポンスのdata
:rtype: GetStreamLinksResponse

<div id="returns">
  #### 戻り値
</div>

`GetStreamLinksResponse`

<div id="validate">
  ### `validate`
</div>

Webhook を検証します  
指定した webhook に対して CRC チェックをトリガーします。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.webhook_id" type="Any">
検証対象の webhook の ID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`ValidateResponse` - レスポンスデータ