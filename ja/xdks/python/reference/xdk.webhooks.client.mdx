---
title: "WebhooksClient"
sidebarTitle: "WebhooksClient"
---

<div id="webhooksclient">
  ## WebhooksClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">基底クラス: object</Badge>

Webhooks 操作用のクライアント

<div id="constructors">
  ## コンストラクタ
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメータ

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

Webhook を作成する
新しい webhook 構成を作成します。
body: リクエストボディ
:returns: レスポンスの data
:rtype: CreateResponse

#### パラメータ

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="create_stream_link">
  ### `create_stream_link`
</div>

ストリームリンクを作成  
指定した webhook に Filtered Stream イベントを配信するためのリンクを作成します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.webhook_id" type="Any">
FilteredStream のルールセットにリンクする webhook の ID。
</ParamField>

<ParamField path="path.tweet_fields" type="str or None" default="None">
表示するツイートフィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.expansions" type="str or None" default="None">
展開するフィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.media_fields" type="str or None" default="None">
表示する Media フィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.poll_fields" type="str or None" default="None">
表示する Poll フィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.user_fields" type="str or None" default="None">
表示する User フィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.place_fields" type="str or None" default="None">
表示する Place フィールドをカンマ区切りで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`CreateStreamLinkResponse` - レスポンスデータ

<div id="create_webhook_replay_job">
  ### `create_webhook_replay_job`
</div>

Webhook のリプレイジョブを作成
Webhook に対して配信された、または配信が試行されたすべてのイベントについて、過去 24 時間以内のイベントを取得するリプレイジョブを作成します。
body: リクエストボディ
:returns: レスポンス data
:rtype: CreateWebhookReplayJobResponse

#### パラメータ

<ParamField path="body.body" type="CreateWebhookReplayJobRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

webhook を削除
既存の webhook 構成を削除します。

#### パラメータ

<ParamField path="path.webhook_id" type="Any">
削除する webhook の ID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`DeleteResponse` - レスポンス data

<div id="delete_stream_link">
  ### `delete_stream_link`
</div>

ストリームリンクを削除
FilteredStream イベントと指定した webhook とのリンクを削除します。

#### パラメーター

<ParamField path="path.webhook_id" type="Any">
FilteredStream のルールセットに関連付ける webhook の id。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`DeleteStreamLinkResponse` - レスポンス データ

<div id="get">
  ### `get`
</div>

Webhook の取得
クライアントアプリケーションに関連付けられている webhook 設定の一覧を取得します。

#### パラメーター

<ParamField path="path.webhook_config_fields" type="List or None" default="None">
表示する WebhookConfig のフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetResponse` - レスポンスの data

<div id="get_stream_links">
  ### `get_stream_links`
</div>

ストリームリンクを取得する
フィルター済みストリームルールセットに関連付けられている webhook リンクの一覧を取得します。
:returns: レスポンスデータ
:rtype: GetStreamLinksResponse

<div id="returns">
  #### 戻り値
</div>

`GetStreamLinksResponse`

<div id="validate">
  ### `validate`
</div>

Webhook を検証します
指定した Webhook に対して CRC チェックをトリガーします。

#### パラメーター

<ParamField path="path.webhook_id" type="Any">
検証対象のWebhookのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`ValidateResponse` - レスポンスデータ