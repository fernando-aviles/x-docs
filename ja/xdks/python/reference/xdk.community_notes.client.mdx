---
title: "CommunityNotesClient"
sidebarTitle: "CommunityNotesClient"
---

<div id="communitynotesclient">
  ## CommunityNotesClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">基底クラス: object</Badge>

Community Notes の操作を行うためのクライアント

<div id="constructors">
  ## コンストラクタ
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメーター

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

コミュニティノートを作成する
LLM ユースケース向けのコミュニティノートエンドポイントを作成します。
body: リクエストボディ
:returns: レスポンス data
:rtype: CreateResponse

#### パラメータ

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

コミュニティノートの削除
コミュニティノートを削除します。

#### パラメーター

<ParamField path="path.id" type="Any">
削除対象のコミュニティノートのid。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`DeleteResponse` - レスポンスデータ

<div id="evaluate">
  ### `evaluate`
</div>

コミュニティノートを評価します
コミュニティノートを評価するためのエンドポイントです。
body: リクエストボディ
:returns: レスポンスデータ
:rtype: EvaluateResponse

#### パラメータ

<ParamField path="body.body" type="EvaluateRequest"></ParamField>

<div id="search_eligible_posts">
  ### `search_eligible_posts`
</div>

コミュニティノートの対象となる投稿を検索します
コミュニティノートの対象となるすべての投稿を返します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.test_mode" type="bool">
true の場合、テスト用の投稿の一覧を返します。false の場合、ボットがプロダクトに関する提案ノートを書き込める投稿の一覧を返します。
</ParamField>

<ParamField path="path.pagination_token" type="str or None" default="None">
ノート対象の投稿の次のセットを取得するためのページネーショントークン。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返す結果の最大数。
</ParamField>

<ParamField path="path.post_selection" type="str or None" default="None">
返す投稿の選択条件。有効な値は「feed_size: small」と「feed_size: large」です。デフォルトは「feed_size: small」であり、large サイズのフィードにアクセスできるのはトップクラスの AI ライターのみです。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示する Tweet フィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示する Media フィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する Poll フィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドのカンマ区切りリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する Place フィールドのカンマ区切りリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorSearchEligiblePostsResponse`

<div id="search_written">
  ### `search_written`
</div>

作成したコミュニティノートを検索する  
ユーザーが作成したすべてのコミュニティノートを返します。

#### パラメーター

<ParamField path="path.test_mode" type="bool">
true の場合、テスト用に呼び出し元が作成したノートを返します。false の場合、プロダクト上で呼び出し元が作成したノートを返します。
</ParamField>

<ParamField path="path.pagination_token" type="str or None" default="None">
ノート対象となる次の投稿セットを取得するためのページネーショントークンです。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返す結果の最大数です。
</ParamField>

<ParamField path="path.note_fields" type="List or None" default="None">
表示する Note フィールドをカンマ区切りで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorSearchWrittenResponse`