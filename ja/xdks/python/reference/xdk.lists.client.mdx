---
title: "ListsClient"
sidebarTitle: "ListsClient"
---

<div id="listsclient">
  ## ListsClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">基底クラス: object</Badge>

リスト関連の操作を行うクライアント

<div id="constructors">
  ## コンストラクター
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメーター

<ParamField path="path.client" type="Client"></ParamField>

<div id="add_member">
  ### `add_member`
</div>

List にメンバーを追加
指定した ID の List に User を追加します。

#### パラメータ

<ParamField path="path.id" type="Any">
メンバーを追加する対象のリストのID。
</ParamField>

<ParamField path="body.body" type="AddMemberRequest">
リクエストボディ
</ParamField>

<div id="create">
  ### `create`
</div>

List の作成
認証済みユーザーの新しい List を作成します。
body: リクエストボディ
:returns: レスポンスデータ
:rtype: CreateResponse

#### パラメーター

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

リストの削除
認証済みユーザーが所有する特定のリストを、指定した id によって削除します。

#### パラメーター

<ParamField path="path.id" type="Any">
削除する List の id です。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`DeleteResponse` - レスポンス データ

<div id="get_by_id">
  ### `get_by_id`
</div>

ID で List を取得する
指定した ID の List の詳細を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
リストのid。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
表示するListフィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserフィールドをカンマ区切りで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByIdResponse` - レスポンスデータ

<div id="get_followers">
  ### `get_followers`
</div>

List のフォロワーを取得する
指定した List の id をもとに、その List をフォローしている User のリストを取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
List のID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返される結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
結果の特定の「ページ」を取得するために使用されるパラメーターです。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetFollowersResponse`

<div id="get_members">
  ### `get_members`
</div>

List のメンバーを取得する
指定された id を持つ List に含まれる User の一覧を取得します。

#### パラメータ

<ParamField path="path.id" type="Any">
List の ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
特定の「ページ」の結果を取得するために使用するパラメータです。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートフィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetMembersResponse`

<div id="get_posts">
  ### `get_posts`
</div>

リストの投稿一覧を取得
特定のリストのIDを指定して、そのリストに関連付けられた投稿の一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
List の ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次のページの結果を取得するために使用するパラメーターです。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドを指定する、カンマ区切りのリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドを指定する、カンマ区切りのリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するメディアのフィールドを指定する、カンマ区切りのリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する投票のフィールドを指定する、カンマ区切りのリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドを指定する、カンマ区切りのリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する場所のフィールドを指定する、カンマ区切りのリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetPostsResponse`

<div id="remove_member_by_user_id">
  ### `remove_member_by_user_id`
</div>

リストメンバーを削除
指定したリストから、そのリストのIDとユーザーのIDを指定してユーザーを削除します。

#### パラメータ

<ParamField path="path.id" type="Any">
メンバーを削除する対象のListのID。
</ParamField>

<ParamField path="path.user_id" type="Any">
Listから削除するUserのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`RemoveMemberByUserIdResponse` - レスポンスデータ

<div id="update">
  ### `update`
</div>

リストの更新

ID を指定して、認証済みユーザーが所有する特定のリストの詳細を更新します。

#### パラメータ

<ParamField path="path.id" type="Any">
更新対象のListのID。
</ParamField>

<ParamField path="body.body" type="UpdateRequest">
リクエスト本文
</ParamField>