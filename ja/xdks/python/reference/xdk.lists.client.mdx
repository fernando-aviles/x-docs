---
title: "ListsClient"
sidebarTitle: "ListsClient"
---

<div id="listsclient">
  ## ListsClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">基底クラス: object</Badge>

Lists の操作を行うクライアント

<div id="constructors">
  ## コンストラクター
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメータ

<ParamField path="path.client" type="Client"></ParamField>

<div id="add_member">
  ### `add_member`
</div>

リストのメンバーを追加
指定したidを持つリストにユーザーを追加します。

#### パラメーター

<ParamField path="path.id" type="Any">
メンバーを追加する List の ID。
</ParamField>

<ParamField path="body.body" type="AddMemberRequest">
リクエストボディ
</ParamField>

<div id="create">
  ### `create`
</div>

List の作成
認証済みユーザー用の新しい List を作成します。
body: リクエストボディ
:returns: レスポンスデータ
:rtype: CreateResponse

#### パラメータ

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

List の削除
認証済みユーザーが所有する特定の List を、その ID を指定して削除します。

#### パラメーター

<ParamField path="path.id" type="Any">
削除対象の List の ID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`DeleteResponse` - レスポンスデータ

<div id="get_by_id">
  ### `get_by_id`
</div>

ID で List を取得
指定した ID の List の詳細を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
List の ID。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
表示する List フィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
expansions で展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByIdResponse` - レスポンスデータ

<div id="get_followers">
  ### `get_followers`
</div>

List のフォロワーを取得
特定の List をフォローしている User の一覧を、その List の id を指定して取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
リストの ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大件数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
指定した「ページ」の結果を取得するために使用するパラメーターです。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドをカンマ区切りで指定するリストです。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定するリストです。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートフィールドをカンマ区切りで指定するリストです。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetFollowersResponse`

<div id="get_members">
  ### `get_members`
</div>

List のメンバーを取得します
指定された List の id を基に、その List のメンバーである User の一覧を取得します。

#### パラメーター

<ParamField path="path.id" type="Any">
List の ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
取得する結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
結果の特定の「ページ」を取得するために使用するパラメーターです。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示する Tweet フィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetMembersResponse`

<div id="get_posts">
  ### `get_posts`
</div>

List の投稿を取得
特定の List の ID を指定して、その List に関連付けられた投稿の一覧を取得します。

#### パラメータ

<ParamField path="path.id" type="Any">
List の ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
次の「ページ」の結果を取得するために使用されるパラメータです。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドを指定する、カンマ区切りのリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドを指定する、カンマ区切りのリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示する Media フィールドを指定する、カンマ区切りのリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示する Poll フィールドを指定する、カンマ区切りのリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドを指定する、カンマ区切りのリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示する Place フィールドを指定する、カンマ区切りのリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetPostsResponse`

<div id="remove_member_by_user_id">
  ### `remove_member_by_user_id`
</div>

リストメンバーを削除
リストのidとユーザーのidを指定して、そのリストからユーザーを削除します。

#### パラメーター

<ParamField path="path.id" type="Any">
メンバーを削除するリストのID。
</ParamField>

<ParamField path="path.user_id" type="Any">
リストから削除されるユーザーのID。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`RemoveMemberByUserIdResponse` - レスポンスデータ

<div id="update">
  ### `update`
</div>

List を更新
認証済みユーザーが所有する特定の List の詳細を、その ID を指定して更新します。

#### パラメータ

<ParamField path="path.id" type="Any">
更新対象の List の ID。
</ParamField>

<ParamField path="body.body" type="UpdateRequest">
リクエストボディ
</ParamField>