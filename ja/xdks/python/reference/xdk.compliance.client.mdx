---
title: "ComplianceClient"
sidebarTitle: "ComplianceClient"
---

<div id="complianceclient">
  ## ComplianceClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">継承元: object</Badge>

コンプライアンス関連の操作を行うためのクライアント

<div id="constructors">
  ## コンストラクター
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメータ

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_jobs">
  ### `create_jobs`
</div>

Compliance Job の作成
指定されたジョブタイプの Compliance Job を新規作成します。
body: リクエストボディ
:returns: レスポンスデータ
:rtype: CreateJobsResponse

#### パラメータ

<ParamField path="body.body" type="CreateJobsRequest"></ParamField>

<div id="get_jobs">
  ### `get_jobs`
</div>

コンプライアンスジョブを取得します
ジョブタイプおよび任意のステータスでフィルタリングされたコンプライアンスジョブの一覧を取得します。

#### パラメーター

<ParamField path="path.type" type="str">
一覧表示する Compliance Job のタイプ。
</ParamField>

<ParamField path="path.status" type="str or None" default="None">
一覧表示する Compliance Job のステータス。
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
表示する ComplianceJob のフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetJobsResponse` - レスポンスデータ

<div id="get_jobs_by_id">
  ### `get_jobs_by_id`
</div>

ID でコンプライアンスジョブを取得
特定のコンプライアンスジョブの ID を指定して、その詳細を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
取得する Compliance Job のid。
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
表示する ComplianceJob のフィールドをカンマ区切りのリストとして指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetJobsByIdResponse` - レスポンスデータ