---
title: "SpacesClient"
sidebarTitle: "SpacesClient"
---

<div id="spacesclient">
  ## SpacesClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">基底クラス: object</Badge>

Spaces 操作用のクライアント

<div id="constructors">
  ## コンストラクタ
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメータ

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_buyers">
  ### `get_buyers`
</div>

Space のチケット購入者を取得します
特定の Space のチケットを購入した User の一覧を、Space ID を指定して取得します。

#### パラメーター

<ParamField path="path.id" type="str">
取得するSpaceのIDです。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
結果の特定の「ページ」を取得するために使用するパラメーターです。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大件数です。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserのフィールドをコンマ区切りで指定するリストです。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをコンマ区切りで指定するリストです。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートのフィールドをコンマ区切りで指定するリストです。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetBuyersResponse`

<div id="get_by_creator_ids">
  ### `get_by_creator_ids`
</div>

作成者IDでスペースを取得します
指定したユーザーIDが作成したスペースの詳細を取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.user_ids" type="List">
検索対象とするユーザーのIDの一覧。
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
表示するSpaceフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
表示するTopicフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByCreatorIdsResponse` - レスポンスデータ

<div id="get_by_id">
  ### `get_by_id`
</div>

ID でスペースを取得する
指定した ID のスペースの詳細を取得します。

#### パラメーター

<ParamField path="path.id" type="str">
取得するSpaceのID。
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
表示するSpaceフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
表示するTopicフィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByIdResponse` - レスポンスデータ

<div id="get_by_ids">
  ### `get_by_ids`
</div>

id で Space を取得する
複数の Space の id を指定して、その詳細を取得します。

#### パラメータ

<ParamField path="path.ids" type="List">
返却する Space ID のリストです。
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
表示する Space のフィールドを、カンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドを、カンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドを、カンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
表示するトピックのフィールドを、カンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByIdsResponse` - レスポンスデータ

<div id="get_posts">
  ### `get_posts`
</div>

Space 内の投稿を取得する
特定の Space 内で共有された投稿の一覧を、その Space の ID を指定して取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="str">
取得するSpaceのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
指定されたSpaceから取得する投稿数。指定しない場合は、最大値である100が使用されます。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するTweetのフィールドを、カンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドを、カンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するMediaのフィールドを、カンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示するPollのフィールドを、カンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserのフィールドを、カンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示するPlaceのフィールドを、カンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetPostsResponse` - レスポンスデータ

<div id="search">
  ### `search`
</div>

スペースを検索
指定された検索クエリに一致するスペースのリストを取得します。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="str">
検索に使用するクエリ。
</ParamField>

<ParamField path="path.state" type="str or None" default="None">
検索対象とする Spaces の状態。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返す結果の数。
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
表示する Space のフィールドを、カンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドを、カンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User のフィールドを、カンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
表示する Topic のフィールドを、カンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`SearchResponse` - レスポンスデータ