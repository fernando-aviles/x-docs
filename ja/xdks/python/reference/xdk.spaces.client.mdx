---
title: "SpacesClient"
sidebarTitle: "SpacesClient"
---

<div id="spacesclient">
  ## SpacesClient
</div>

<Badge color="blue">クラス</Badge>

<Badge color="gray">基底クラス: object</Badge>

Spaces の操作を行うための Client

<div id="constructors">
  ## コンストラクター
</div>

<div id="__init__">
  ### `__init__`
</div>

#### パラメータ

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_buyers">
  ### `get_buyers`
</div>

スペースのチケット購入者を取得
指定したスペースIDに対して、そのチケットを購入したユーザーのリストを取得します。

#### パラメータ

<ParamField path="path.id" type="str">
取得するSpaceのID。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
指定した「ページ」の結果を取得するためのパラメータ。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
結果の最大件数。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定したリスト。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するTweetフィールドをカンマ区切りで指定したリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`IteratorGetBuyersResponse`

<div id="get_by_creator_ids">
  ### `get_by_creator_ids`
</div>

作成者IDを指定してSpacesを取得します
指定したユーザーIDが作成したSpacesの詳細を取得します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.user_ids" type="List">
検索対象とするユーザーのID。
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
表示するスペースのフィールドをコンマ区切りで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをコンマ区切りで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するユーザーのフィールドをコンマ区切りで指定します。
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
表示するトピックのフィールドをコンマ区切りで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByCreatorIdsResponse` - レスポンスデータ

<div id="get_by_id">
  ### `get_by_id`
</div>

ID でスペースを取得
指定した ID のスペースの詳細を取得します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.id" type="str">
取得する Space の ID。
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
レスポンスに含める Space フィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
レスポンスに含める User フィールドをカンマ区切りのリストで指定します。
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
レスポンスに含める Topic フィールドをカンマ区切りのリストで指定します。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByIdResponse` - レスポンスデータ

<div id="get_by_ids">
  ### `get_by_ids`
</div>

ID を指定して Space を取得する
複数の Space を ID で指定して詳細情報を取得します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.ids" type="List">
返す Space ID のリスト。
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
表示する Space フィールドのカンマ区切りのリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドのカンマ区切りのリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示する User フィールドのカンマ区切りのリスト。
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
表示する Topic フィールドのカンマ区切りのリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetByIdsResponse` - レスポンスデータ

<div id="get_posts">
  ### `get_posts`
</div>

Space の投稿を取得
特定の Space 内で共有された投稿の一覧を、その ID を指定して取得します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.id" type="str">
取得するスペースのID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
指定されたスペースから取得する投稿数。指定しない場合は、最大値である100が使用されます。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
表示するツイートフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
表示するMediaフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
表示するPollフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserフィールドをカンマ区切りで指定するリスト。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
表示するPlaceフィールドをカンマ区切りで指定するリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`GetPostsResponse` - レスポンスデータ

<div id="search">
  ### `search`
</div>

スペースを検索する
指定したクエリに一致するスペースのリストを取得します。

<div id="parameters">
  #### パラメーター
</div>

<ParamField path="path.query" type="str">
検索クエリ。
</ParamField>

<ParamField path="path.state" type="str or None" default="None">
検索対象とするSpaceの状態。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返す結果の数。
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
表示するSpaceのフィールドを指定するカンマ区切りのリスト。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
展開するフィールドを指定するカンマ区切りのリスト。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
表示するUserのフィールドを指定するカンマ区切りのリスト。
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
表示するTopicのフィールドを指定するカンマ区切りのリスト。
</ParamField>

<div id="returns">
  #### 戻り値
</div>

`SearchResponse` 型のレスポンスデータ