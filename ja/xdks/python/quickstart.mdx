---
title: クイックスタート
sidebarTitle: クイックスタート
---

この例では、Bearer Token 認証を使用して XDK で投稿をすばやく検索する方法を紹介します。

<div id="step-1-install-the-sdk">
  ## ステップ 1: SDK をインストール
</div>

```bash
pip install xdk
```

<div id="step-2-get-your-bearer-token">
  ## ステップ 2: Bearer Token を取得する
</div>

1. [X 開発者コンソール](https://developer.x.com/en/portal/dashboard) にログインします。
2. アプリを新規作成するか、既存のアプリを選択します。
3. 「Keys and Tokens」セクションで、Bearer Token（アプリ専用認証）を発行します。

<div id="step-3-write-and-run-your-first-script">
  ## ステップ 3: 最初のスクリプトを作成して実行する
</div>

`quickstart.py` というファイルを作成します:

```python
# クライアントをインポート
from xdk import Client
# 実際のBearer Tokenに置き換えてください
client = Client(bearer_token="YOUR_BEARER_TOKEN_HERE")
# "api"に言及している最近の投稿を取得
# search_recentはIteratorを返すので、それを反復処理します
for page in client.posts.search_recent(query="api", max_results=10):
    if page.data and len(page.data) > 0:
        # 最初の投稿にアクセス - Pydanticモデルは属性とdict両方のアクセスをサポートしています
        first_post = page.data[0]
        post_text = first_post.text if hasattr(first_post, 'text') else first_post.get('text', '')
        print(f"Latest Post: {post_text}")
        break
    else:
        print("No Posts found.")
        break
```

実行してみましょう：

```bash
python quickstart.py
```

**想定される出力**:

```
最新の投稿: XDK Python SDKに関するエキサイティングなアップデート!
```

**トラブルシューティング**: 401 エラーが発生した場合は、Bearer Token を再確認してください。レート制限 (429) の場合は、時間をおいてから再試行してください。

<div id="next-steps">
  ## 次のステップ
</div>

* [Authentication](/ja/xdks/python/authentication) を参照して、Bearer Token（アプリ専用）認証、PKCE を用いた OAuth 2.0（ユーザーコンテキスト）、および OAuth 1.0a（レガシーなユーザーコンテキスト）の使い方を理解しましょう。
* [Pagination](/ja/xdks/python/pagination) について学び、大量の結果を取得しつつ、複数回の API 呼び出しを意識せずに済むユースケースでの使い方を把握しましょう。
* [Streaming](/ja/xdks/python/streaming) をさらに掘り下げて、リアルタイムデータを扱う方法を学びましょう。
  Python XDK を使った詳細なコード例については、[コードサンプルの GitHub リポジトリ](https://github.com/xdevplatform/samples/tree/main/python) を参照してください。