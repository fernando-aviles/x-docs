---
title: "TypeScript XDK"
sidebarTitle: "개요"
---

X API(이전 Twitter API)를 위한 포괄적인 TypeScript SDK로, 스마트 페이지네이션, 다양한 인증 방식, 실시간 스트리밍, 완전한 유형 안전성 등 고급 기능을 제공합니다.

<div id="key-features">
  ## 주요 기능
</div>

* **🔐 인증**: 사용자 컨텍스트(OAuth1.0a, OAuth2.0) 및 앱 전용(Bearer Token) 인증
* **🔄 페이지네이션**: 비동기 반복을 지원하는 자동 페이지네이션
* **📡 스트리밍**: 자동 재연결 기능이 있는 이벤트 기반 스트리밍
* **📚 타입 안전성**: 모든 엔드포인트와 매개변수에 대한 완전한 TypeScript 정의
* **🎯 X API 전 기능 지원**: 사용자, 포스트, 리스트, 북마크, 커뮤니티 등

<div id="quick-start">
  ## 빠른 시작
</div>

<CodeGroup dropdown>
  ```typescript quickstart.ts theme={null}
  import { 
      Client, 
      type ClientConfig,
      type Users
  } from '@xdevplatform/xdk';

  const config: ClientConfig = { bearerToken: 'your-bearer-token' };

  const client: Client = new Client(config);

  async function main(): Promise<void> {
    const userResponse: Users.GetByUsernameResponse = await client.users.getByUsername('XDevelopers');
    const username: string = userResponse.data?.username!;
    console.log(username);
  }

  main();
  ```

  ```javascript quickstart.js theme={null}
  import { Client } from '@xdevplatform/xdk';

  const client = new Client({ bearerToken: 'your-bearer-token' });

  const userResponse = await client.users.getByUsername('XDevelopers');
  const username = userResponse.data.username;
  console.log(username);
  ```
</CodeGroup>

<Info>
  JavaScript/TypeScript용 XDK를 사용하는 자세한 코드 예제는 [code samples GitHub repo](https://github.com/xdevplatform/samples/tree/main/javascript)에서 확인하세요.
</Info>

<div id="whats-next">
  ## 다음 단계
</div>

* [설치 가이드](/ko/xdks/typescript/install) - 프로젝트에 SDK를 설정하는 방법을 알아봅니다
* [인증](/ko/xdks/typescript/authentication) - 다양한 인증 방법에 대해 알아봅니다
* [페이지네이션](/ko/xdks/typescript/pagination) - 데이터 페이지네이션에 대해 알아봅니다
* [스트리밍](/ko/xdks/typescript/streaming) - 실시간 데이터 스트리밍에 대해 알아봅니다
* [API 레퍼런스](/ko/xdks/typescript/reference/Client) - 전체 API 문서를 확인합니다