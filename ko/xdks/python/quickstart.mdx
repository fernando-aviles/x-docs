---
title: 빠른 시작
sidebarTitle: 빠른 시작
---

이 예제에서는 XDK를 사용해 Bearer Token 인증으로 포스트를 빠르게 검색하는 방법을 보여줍니다.

<div id="step-1-install-the-sdk">
  ## 1단계: SDK 설치
</div>

```bash
pip install xdk
```

<div id="step-2-get-your-bearer-token">
  ## 2단계: Bearer Token 받기
</div>

1. [X 개발자 콘솔](https://developer.x.com/en/portal/dashboard)에 로그인합니다.
2. 앱을 생성하거나 선택합니다.
3. &quot;Keys and Tokens&quot;에서 Bearer Token(앱 전용 인증)을 발급받습니다.

<div id="step-3-write-and-run-your-first-script">
  ## 3단계: 첫 번째 스크립트 작성 및 실행
</div>

`quickstart.py`라는 파일을 생성합니다:

```python
# Import the client
from xdk import Client
# Replace with your actual Bearer Token
client = Client(bearer_token="YOUR_BEARER_TOKEN_HERE")
# Fetch recent Posts mentioning "api"
# search_recent returns an Iterator, so iterate over it
for page in client.posts.search_recent(query="api", max_results=10):
    if page.data and len(page.data) > 0:
        # 첫 번째 포스트 접근 - Pydantic 모델은 속성 및 딕셔너리 접근 모두 지원
        first_post = page.data[0]
        post_text = first_post.text if hasattr(first_post, 'text') else first_post.get('text', '')
        print(f"Latest Post: {post_text}")
        break
    else:
        print("No Posts found.")
        break
```

실행해 보세요:

```bash
python quickstart.py
```

**예상 출력 결과**:

```
Latest Post: XDK Python SDK의 흥미로운 업데이트!
```

**문제 해결**: 401 오류가 발생하면 Bearer Token을 다시 확인하세요. 요청 한도 초과(429) 오류가 발생하면 잠시 기다렸다가 다시 시도하세요.

<div id="next-steps">
  ## 다음 단계
</div>

* Bearer Token(앱 전용) 인증, PKCE를 사용하는 OAuth 2.0(사용자 컨텍스트), OAuth 1.0a(레거시 사용자 컨텍스트)를 사용하는 방법을 이해하려면 [Authentication](/ko/xdks/python/authentication)을 살펴보세요.
* 여러 차례 API 호출을 신경 쓰지 않고도 많은 결과를 받고 싶을 때를 위한 사용 사례를 알아보려면 [Pagination](/ko/xdks/python/pagination)을 학습하세요.
* 실시간 데이터를 다루는 방법을 배우려면 [Streaming](/ko/xdks/python/streaming)을 자세히 살펴보세요.
  Python XDK를 사용하는 자세한 코드 예시는 [code samples GitHub repo](https://github.com/xdevplatform/samples/tree/main/python)를 확인하세요.