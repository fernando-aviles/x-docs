---
title: "UsersClient"
sidebarTitle: "UsersClient"
---

<div id="usersclient">
  ## UsersClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">기반 클래스: object</Badge>

사용자 관련 작업을 수행하기 위한 클라이언트

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 매개변수

<ParamField path="path.client" type="Client"></ParamField>

<div id="block_dms">
  ### `block_dms`
</div>

DM 차단
인증된 사용자를 대신해, 지정한 사용자 id를 사용하여 해당 사용자와 주고받는 다이렉트 메시지를 차단합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
인증된 사용자가 DM을 차단하려는 대상 사용자의 id입니다.
</ParamField>

#### 반환

`BlockDmsResponse` - 응답 data

<div id="create_bookmark">
  ### `create_bookmark`
</div>

북마크 생성
인증된 사용자의 북마크에 포스트를 추가합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
북마크를 추가할 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="body.body" type="CreateBookmarkRequest">
요청 본문
</ParamField>

<div id="delete_bookmark">
  ### `delete_bookmark`
</div>

북마크 삭제
인증된 사용자의 북마크에서 해당 ID를 가진 포스트를 제거합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
북마크에서 제거를 수행하는 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
소스 사용자가 북마크에서 제거하려는 포스트의 ID입니다.
</ParamField>

#### 반환값

`DeleteBookmarkResponse` - 응답 데이터

<div id="follow_list">
  ### `follow_list`
</div>

리스트 팔로우
인증된 사용자가 지정된 id의 특정 리스트를 팔로우하도록 합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
List를 팔로우할 인증된 소스 User의 id입니다.
</ParamField>

<ParamField path="body.body" type="FollowListRequest">
요청 본문입니다.
</ParamField>

<div id="follow_user">
  ### `follow_user`
</div>

사용자 팔로우
인증된 사용자가 지정된 id를 가진 사용자를 팔로우합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
대상 사용자를 팔로우하도록 요청하는 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="body.body" type="FollowUserRequest">
요청 본문
</ParamField>

<div id="get_blocking">
  ### `get_blocking`
</div>

차단 대상 조회
지정된 User ID가 차단한 User 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
결과를 반환할 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환

`IteratorGetBlockingResponse`

<div id="get_bookmark_folders">
  ### `get_bookmark_folders`
</div>

북마크 폴더 가져오기
인증된 사용자가 생성한 북마크 폴더 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
결과를 반환할 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과 수의 최댓값입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 '페이지'의 결과를 가져오기 위해 사용하는 매개변수입니다.
</ParamField>

#### 반환값

`IteratorGetBookmarkFoldersResponse`

<div id="get_bookmarks">
  ### `get_bookmarks`
</div>

북마크 가져오기
인증된 사용자가 북마크한 포스트 목록을 가져옵니다.

#### 매개변수

<ParamField path="path.id" type="Any">
결과를 반환할 인증된 소스 User의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
결과의 다음 "페이지"를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetBookmarksResponse`

<div id="get_bookmarks_by_folder_id">
  ### `get_bookmarks_by_folder_id`
</div>

폴더 ID로 북마크 가져오기
인증된 사용자의 특정 북마크 폴더 ID를 사용해 해당 폴더에 있는 포스트를 가져옵니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
결과를 반환할 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.folder_id" type="Any">
인증된 사용자가 포스트를 가져오려는 북마크 폴더의 ID입니다.
</ParamField>

#### 반환

`GetBookmarksByFolderIdResponse` - 응답 데이터

<div id="get_by_id">
  ### `get_by_id`
</div>

id로 사용자 조회
특정 사용자의 id를 통해 세부 정보를 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
조회할 User의 ID입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`GetByIdResponse` - 응답 data

<div id="get_by_ids">
  ### `get_by_ids`
</div>

ID로 사용자 가져오기
여러 사용자의 ID를 기준으로 세부 정보를 조회합니다.

#### 매개변수

<ParamField path="path.ids" type="List">
쉼표로 구분된 User ID 목록입니다. 최대 100개의 ID를 지정할 수 있습니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환값

`GetByIdsResponse` - 응답 데이터

<div id="get_by_username">
  ### `get_by_username`
</div>

username으로 User 조회
지정한 username의 특정 User 상세 정보를 조회합니다.

#### 매개변수

<ParamField path="path.username" type="str">
사용자 이름입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 목록으로, 쉼표로 구분됩니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드 목록으로, 쉼표로 구분됩니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드 목록으로, 쉼표로 구분됩니다.
</ParamField>

#### 반환값

`GetByUsernameResponse` - 응답 data

<div id="get_by_usernames">
  ### `get_by_usernames`
</div>

username으로 사용자 가져오기
여러 사용자에 대한 세부 정보를 username을 기준으로 조회합니다.

#### 매개변수

<ParamField path="path.usernames" type="List">
쉼표로 구분된 username 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환

`GetByUsernamesResponse` - 응답 data

<div id="get_followed_lists">
  ### `get_followed_lists`
</div>

팔로우 중인 리스트 조회
특정 사용자의 id를 사용하여, 해당 사용자가 팔로우 중인 리스트들을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
조회할 사용자의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 ‘페이지’의 결과를 가져오는 데 사용하는 매개변수입니다.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
표시할 리스트 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환

`IteratorGetFollowedListsResponse`

<div id="get_followers">
  ### `get_followers`
</div>

팔로워 가져오기
특정 사용자의 ID를 기준으로 해당 사용자를 팔로우하는 User 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
조회할 User의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환값

`IteratorGetFollowersResponse`

<div id="get_following">
  ### `get_following`
</div>

팔로잉 조회
특정 사용자의 id를 사용해 해당 사용자가 팔로우하는 사용자 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
조회할 사용자의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetFollowingResponse`

<div id="get_liked_posts">
  ### `get_liked_posts`
</div>

좋아요한 포스트 조회
특정 사용자의 id를 사용해, 해당 사용자가 좋아요한 포스트 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
조회할 User의 id입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetLikedPostsResponse`

<div id="get_list_memberships">
  ### `get_list_memberships`
</div>

리스트 멤버십 조회
특정 사용자의 id를 기준으로, 해당 사용자가 멤버로 속한 리스트 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
조회할 사용자 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
표시할 List 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetListMembershipsResponse`

<div id="get_me">
  ### `get_me`
</div>

내 사용자 정보 가져오기
인증된 사용자의 상세 정보를 조회합니다.

#### 매개변수

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`GetMeResponse` - 응답 데이터

<div id="get_mentions">
  ### `get_mentions`
</div>

멘션 가져오기
특정 사용자의 id를 사용해 해당 사용자를 멘션한 포스트 목록을 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
조회할 사용자 ID입니다.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
결과 집합에 포함될 최소 포스트 ID입니다. 이 매개변수와 start_time이 모두 지정된 경우 이 매개변수가 우선합니다.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
결과 집합에 포함될 최대 포스트 ID입니다. 이 매개변수와 end_time이 모두 지정된 경우 이 매개변수가 우선합니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 페이지의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ 형식입니다. 포스트가 제공될 수 있는 가장 이른 UTC 타임스탬프입니다. since_id 매개변수가 함께 지정된 경우 해당 매개변수가 우선합니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ 형식입니다. 포스트가 제공될 수 있는 가장 늦은 UTC 타임스탬프입니다. until_id 매개변수가 함께 지정된 경우 해당 매개변수가 우선합니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 객체 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetMentionsResponse`

<div id="get_muting">
  ### `get_muting`
</div>

뮤트 대상 가져오기
인증된 사용자가 뮤트한 사용자 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
결과를 반환할 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 "페이지"의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환값

`IteratorGetMutingResponse`

<div id="get_owned_lists">
  ### `get_owned_lists`
</div>

소유한 리스트 조회
특정 사용자의 id로 해당 사용자가 소유한 리스트 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
조회할 User의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 ‘페이지’ 결과를 가져오는 데 사용되는 파라미터입니다.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
표시할 List 필드를 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetOwnedListsResponse`

<div id="get_pinned_lists">
  ### `get_pinned_lists`
</div>

고정된 리스트 가져오기
인증된 사용자가 고정한 리스트 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
결과를 반환할 인증된 소스 사용자 ID입니다.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
표시할 List 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`GetPinnedListsResponse` - 응답 데이터

<div id="get_posts">
  ### `get_posts`
</div>

포스트 가져오기
특정 사용자의 ID를 사용해 해당 사용자가 작성한 포스트 목록을 가져옵니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
조회할 사용자의 ID입니다.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
결과 집합에 포함될 최소 포스트 ID입니다. 이 파라미터와 start_time이 모두 지정된 경우, 이 파라미터가 우선합니다.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
결과 집합에 포함될 최대 포스트 ID입니다. 이 파라미터와 end_time이 모두 지정된 경우, 이 파라미터가 우선합니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 ‘페이지’의 결과를 가져오는 데 사용되는 파라미터입니다.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
결과에서 제외할 엔티티 집합입니다(예: ‘replies’ 또는 ‘retweets’).
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 포스트가 반환될 가장 이른 UTC 타임스탬프입니다. since_id 파라미터도 지정된 경우, since_id가 우선합니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 포스트가 반환될 가장 늦은 UTC 타임스탬프입니다. until_id 파라미터도 지정된 경우, until_id가 우선합니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환

`IteratorGetPostsResponse`

<div id="get_reposts_of_me">
  ### `get_reposts_of_me`
</div>

나에 대한 재게시 목록 가져오기
인증된 사용자의 콘텐츠를 재게시한 포스트 목록을 가져옵니다.

#### 매개변수

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 페이지의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetRepostsOfMeResponse`

<div id="get_timeline">
  ### `get_timeline`
</div>

타임라인 가져오기
인증된 사용자의 타임라인에서 포스트를 최신순으로 정렬한 목록을 가져옵니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
인증된 소스 사용자(User)의 최신순 타임라인 포스트를 나열할 사용자 ID입니다.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
결과 집합에 포함될 최소 포스트 ID입니다. 이 매개변수와 start_time이 모두 지정된 경우, 이 매개변수가 start_time보다 우선 적용됩니다.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
결과 집합에 포함될 최대 포스트 ID입니다. 이 매개변수와 end_time이 모두 지정된 경우, 이 매개변수가 end_time보다 우선 적용됩니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
제외할 엔티티 집합입니다(예: ‘replies’ 또는 ‘retweets’).
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 포스트를 제공할 때 사용할 가장 이른 UTC 타임스탬프입니다. since_id 매개변수가 함께 지정된 경우, since_id 매개변수가 우선 적용됩니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 포스트를 제공할 때 사용할 가장 늦은 UTC 타임스탬프입니다. until_id 매개변수가 함께 지정된 경우, until_id 매개변수가 우선 적용됩니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 포스트 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetTimelineResponse`

<div id="like_post">
  ### `like_post`
</div>

포스트 좋아요
인증된 사용자가 ID로 지정된 특정 포스트에 좋아요를 추가합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
포스트에 좋아요를 요청하는 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="body.body" type="LikePostRequest">
요청 본문
</ParamField>

<div id="mute_user">
  ### `mute_user`
</div>

사용자 음소거
인증된 사용자가 지정한 id에 해당하는 특정 사용자를 음소거합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
대상 User를 뮤트하도록 요청하는 인증된 소스 User의 ID입니다.
</ParamField>

<ParamField path="body.body" type="MuteUserRequest">
요청 본문
</ParamField>

<div id="pin_list">
  ### `pin_list`
</div>

리스트 고정
인증된 사용자가 id로 지정된 특정 리스트를 고정하도록 합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
목록을 고정하는 인증된 원본 사용자의 id입니다.
</ParamField>

<ParamField path="body.body" type="PinListRequest">
요청 본문
</ParamField>

<div id="repost_post">
  ### `repost_post`
</div>

포스트 리포스트
인증된 사용자가 특정 포스트의 ID로 해당 포스트를 리포스트합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
포스트의 리포스트를 요청하는 인증된 소스 사용자(source User)의 id입니다.
</ParamField>

<ParamField path="body.body" type="RepostPostRequest">
요청 본문
</ParamField>

<div id="search">
  ### `search`
</div>

사용자 검색
검색 쿼리와 일치하는 사용자 목록을 반환합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="Any">
사용자를 검색하기 위한 쿼리 문자열입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
다음 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다. 이 매개변수에 사용하는 값은 API가 제공하는 응답에서 직접 가져오며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환

`IteratorSearchResponse`

<div id="unblock_dms">
  ### `unblock_dms`
</div>

DM 차단 해제
인증된 사용자에 대해, 특정 사용자 id를 지정하여 해당 사용자와 주고받는 다이렉트 메시지 차단을 해제합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
인증된 사용자가 DM 차단 해제를 요청하는 대상 사용자를 나타내는 id입니다.
</ParamField>

#### 반환값

`UnblockDmsResponse` - 응답 데이터

<div id="unfollow_list">
  ### `unfollow_list`
</div>

리스트 언팔로우
인증된 사용자가 특정 리스트 id를 기준으로 해당 리스트를 언팔로우하도록 합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
언팔로우를 수행할 인증된 소스 User의 id입니다.
</ParamField>

<ParamField path="path.list_id" type="Any">
언팔로우할 List의 id입니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`UnfollowListResponse` - 응답 데이터

<div id="unfollow_user">
  ### `unfollow_user`
</div>

사용자 팔로우 해제
인증된 사용자가 특정 id를 가진 사용자를 팔로우 해제합니다.

#### 매개변수

<ParamField path="path.source_user_id" type="Any">
언팔로우를 요청하는 인증된 소스 User의 id입니다.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
소스 User가 언팔로우를 요청하는 대상 User의 id입니다.
</ParamField>

#### 반환

`UnfollowUserResponse` - 응답 데이터

<div id="unlike_post">
  ### `unlike_post`
</div>

포스트 좋아요 취소
인증된 사용자가 ID로 지정한 특정 포스트의 좋아요를 취소합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
좋아요를 취소하려는 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
사용자가 좋아요를 취소하려는 포스트의 ID입니다.
</ParamField>

#### 반환

`UnlikePostResponse` - 응답 데이터

<div id="unmute_user">
  ### `unmute_user`
</div>

사용자 음소거 해제
인증된 사용자가 id로 지정된 특정 사용자의 음소거를 해제합니다.

#### 매개변수

<ParamField path="path.source_user_id" type="Any">
대상 사용자의 음소거 해제를 요청하는 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
소스 사용자가 음소거 해제를 요청하는 대상 사용자의 ID입니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`UnmuteUserResponse` - 응답 데이터

<div id="unpin_list">
  ### `unpin_list`
</div>

목록 고정 해제
인증된 사용자가 id로 지정된 특정 목록의 고정을 해제합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
결과를 반환할 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.list_id" type="Any">
고정 해제할 리스트의 ID입니다.
</ParamField>

#### 반환값

`UnpinListResponse` - 응답 데이터

<div id="unrepost_post">
  ### `unrepost_post`
</div>

포스트 리포스트 취소
인증된 사용자가 ID로 지정한 특정 포스트에 대한 리포스트를 취소합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
포스트의 리포스트를 요청하는 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.source_tweet_id" type="Any">
해당 사용자가 리포스트 취소를 요청하는 포스트의 ID입니다.
</ParamField>

#### 반환값

`UnrepostPostResponse` - 응답 데이터