---
title: "UsersClient"
sidebarTitle: "UsersClient"
---

<div id="usersclient">
  ## UsersClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">상속: object</Badge>

사용자 관련 작업을 위한 Client

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 매개변수

<ParamField path="path.client" type="Client"></ParamField>

<div id="block_dms">
  ### `block_dms`
</div>

DM 차단
인증된 사용자에 대해 특정 사용자의 id를 지정해, 해당 사용자와 주고받는 다이렉트 메시지를 차단합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
인증된 사용자가 해당 사용자로부터 DM 수신을 차단하려는 대상 사용자의 ID입니다.
</ParamField>

#### 반환값

`BlockDmsResponse` - 응답 데이터

<div id="create_bookmark">
  ### `create_bookmark`
</div>

북마크 생성
인증된 사용자의 북마크에 게시물을 추가합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
북마크를 추가할 인증된 소스 사용자(User)의 ID입니다.
</ParamField>

<ParamField path="body.body" type="CreateBookmarkRequest">
요청 본문
</ParamField>

<div id="delete_bookmark">
  ### `delete_bookmark`
</div>

북마크 삭제
인증된 사용자의 북마크에서 해당 ID의 게시물을 제거합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
북마크에서 제거를 수행하는 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
소스 사용자가 북마크에서 제거하려는 게시물의 ID입니다.
</ParamField>

#### 반환값

`DeleteBookmarkResponse` - 응답 데이터

<div id="follow_list">
  ### `follow_list`
</div>

리스트 팔로우
인증된 사용자가 특정 ID로 지정된 리스트를 팔로우하게 합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
리스트를 팔로우하게 될 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="body.body" type="FollowListRequest">
요청 본문
</ParamField>

<div id="follow_user">
  ### `follow_user`
</div>

사용자 팔로우
인증된 사용자가 지정된 id를 가진 특정 사용자를 팔로우하게 합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
대상 User를 팔로우하도록 요청하는 인증된 소스 User의 id입니다.
</ParamField>

<ParamField path="body.body" type="FollowUserRequest">
요청 본문
</ParamField>

<div id="get_blocking">
  ### `get_blocking`
</div>

차단 목록 가져오기
지정된 사용자 ID가 차단한 사용자 목록을 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
결과를 반환받을 인증된 소스 User의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
반환할 결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 ‘페이지’의 결과를 조회하는 데 사용하는 매개변수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환값

`IteratorGetBlockingResponse`

<div id="get_bookmark_folders">
  ### `get_bookmark_folders`
</div>

북마크 폴더 가져오기
인증된 사용자가 생성한 북마크 폴더 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
결과를 반환할 인증된 소스 User의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

#### 반환

`IteratorGetBookmarkFoldersResponse`

<div id="get_bookmarks">
  ### `get_bookmarks`
</div>

북마크 가져오기
인증된 사용자가 북마크한 포스트 목록을 반환합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
결과를 반환할 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
반환할 최대 결과 수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 '페이지'의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

#### 반환값

`IteratorGetBookmarksResponse`

<div id="get_bookmarks_by_folder_id">
  ### `get_bookmarks_by_folder_id`
</div>

폴더 ID로 북마크 조회
인증된 사용자의 특정 북마크 폴더에서 해당 ID를 가진 포스트를 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
결과를 반환할 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.folder_id" type="Any">
인증된 사용자가 포스트를 가져오려는 북마크 폴더의 ID입니다.
</ParamField>

#### 반환

`GetBookmarksByFolderIdResponse` - 응답 데이터

<div id="get_by_id">
  ### `get_by_id`
</div>

id로 User 가져오기
특정 User를 id로 조회하여 해당 User의 세부 정보를 반환합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
조회할 User의 ID입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None" >
표시할 Tweet 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

#### 반환

`GetByIdResponse` - 응답 데이터

<div id="get_by_ids">
  ### `get_by_ids`
</div>

id로 사용자 가져오기
여러 사용자의 세부 정보를 해당 id를 통해 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.ids" type="List">
쉼표로 구분된 사용자 ID 목록입니다. 최대 100개의 ID를 지정할 수 있습니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`GetByIdsResponse` - 응답 데이터입니다.

<div id="get_by_username">
  ### `get_by_username`
</div>

username으로 사용자 가져오기
지정한 username에 해당하는 특정 사용자의 상세 정보를 조회합니다.

#### 매개변수

<ParamField path="path.username" type="str">
사용자 이름입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드를 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`GetByUsernameResponse` - 응답 데이터

<div id="get_by_usernames">
  ### `get_by_usernames`
</div>

username으로 사용자 가져오기
여러 사용자의 세부 정보를 username으로 조회합니다.

#### 매개변수

<ParamField path="path.usernames" type="List">
쉼표로 구분된 username 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`GetByUsernamesResponse` - 응답 데이터

<div id="get_followed_lists">
  ### `get_followed_lists`
</div>

팔로우 중인 리스트 가져오기
특정 사용자의 `id`를 사용해 해당 사용자가 팔로우 중인 리스트를 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
조회할 사용자 id입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
표시할 리스트 필드를 쉼표로 구분된 목록으로 지정합니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분된 목록으로 지정합니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드를 쉼표로 구분된 목록으로 지정합니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`IteratorGetFollowedListsResponse`

<div id="get_followers">
  ### `get_followers`
</div>

팔로워 가져오기
지정한 User의 id를 사용해, 해당 User를 팔로우하는 User 목록을 반환합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
조회할 User의 id입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
expansions에 사용할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환

`IteratorGetFollowersResponse`

<div id="get_following">
  ### `get_following`
</div>

팔로잉 조회
특정 사용자의 ID를 받아, 그 사용자가 팔로우하는 사용자 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
조회할 사용자의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 '페이지'의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

#### 반환

`IteratorGetFollowingResponse`

<div id="get_liked_posts">
  ### `get_liked_posts`
</div>

좋아요한 포스트 조회
특정 사용자의 id를 사용하여 해당 사용자가 좋아요한 포스트 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
조회할 User의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 페이지의 결과를 가져오는 데 사용하는 매개변수입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetLikedPostsResponse`

<div id="get_list_memberships">
  ### `get_list_memberships`
</div>

리스트 멤버십 조회
특정 사용자가 멤버로 속한 리스트 목록을 해당 사용자의 id로 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
조회할 사용자의 id입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
이 매개변수는 지정된 ‘페이지’의 결과를 가져오는 데 사용됩니다.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
표시할 리스트 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`IteratorGetListMembershipsResponse`

<div id="get_me">
  ### `get_me`
</div>

내 사용자 정보 가져오기
인증된 사용자의 상세 정보를 조회합니다.

#### 매개변수

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환

`GetMeResponse` - 응답 데이터

<div id="get_mentions">
  ### `get_mentions`
</div>

멘션 조회
특정 사용자의 ID를 기준으로 해당 사용자를 멘션한 포스트 목록을 반환합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
조회할 사용자의 ID입니다.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
결과 집합에 포함될 최소 게시물 ID입니다. 이 파라미터와 start_time이 모두 지정된 경우, 이 파라미터가 우선합니다.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
결과 집합에 포함될 최대 게시물 ID입니다. 이 파라미터와 end_time이 모두 지정된 경우, 이 파라미터가 우선합니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 "페이지"의 결과를 가져올 때 사용하는 파라미터입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 게시물을 반환하기 시작하는 가장 이른 UTC 타임스탬프입니다. since_id 파라미터도 지정된 경우, since_id가 우선합니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 게시물을 반환하는 가장 늦은 UTC 타임스탬프입니다. until_id 파라미터도 지정된 경우, until_id가 우선합니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 미디어 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 투표 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 장소 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetMentionsResponse`

<div id="get_muting">
  ### `get_muting`
</div>

뮤트 대상 가져오기
인증된 사용자가 뮤트한 사용자 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
결과를 반환할 인증된 소스 User의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
이 매개변수는 다음 페이지의 결과를 가져오는 데 사용됩니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

#### 반환

`IteratorGetMutingResponse`

<div id="get_owned_lists">
  ### `get_owned_lists`
</div>

소유한 리스트 가져오기
특정 사용자의 id를 사용해 해당 사용자가 소유한 리스트 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
조회할 User의 id입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
반환할 결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
표시할 리스트 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

#### 반환값

`IteratorGetOwnedListsResponse`

<div id="get_pinned_lists">
  ### `get_pinned_lists`
</div>

고정된 리스트 가져오기
인증된 사용자가 고정한 리스트를 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
결과를 반환할 인증된 소스 User의 ID입니다.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
표시할 리스트 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환

`GetPinnedListsResponse` - 응답 데이터

<div id="get_posts">
  ### `get_posts`
</div>

포스트 가져오기
특정 사용자의 id로 해당 사용자가 작성한 포스트 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
조회할 User의 ID입니다.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
결과 집합에 포함될 최소 게시물 ID입니다. 이 매개변수와 start_time이 모두 지정된 경우, 이 매개변수가 우선합니다.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
결과 집합에 포함될 최대 게시물 ID입니다. 이 매개변수와 end_time이 모두 지정된 경우, 이 매개변수가 우선합니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
제외할 엔티티 집합입니다(예: ‘replies’ 또는 ‘retweets’).
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 포스트가 반환될 가장 이른 UTC 타임스탬프입니다. since_id 매개변수가 함께 지정된 경우, since_id가 우선합니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 포스트가 반환될 가장 늦은 UTC 타임스탬프입니다. until_id 매개변수가 함께 지정된 경우, until_id가 우선합니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetPostsResponse`

<div id="get_reposts_of_me">
  ### `get_reposts_of_me`
</div>

나를 리포스트한 포스트 가져오기
인증된 사용자의 콘텐츠를 리포스트한 포스트 리스트를 가져옵니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.max_results" type="int or None" default="None">
최대 결과 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
결과의 다음 '페이지'를 가져오는 데 사용하는 매개변수입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 미디어 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 투표 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 장소 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetRepostsOfMeResponse`

<div id="get_timeline">
  ### `get_timeline`
</div>

타임라인 가져오기
인증된 사용자의 타임라인에 있는 포스트 리스트를 최신순(시간 역순)으로 반환합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
최신순 타임라인 포스트를 나열할 인증된 소스 User의 ID입니다.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
결과 집합에 포함될 최소 게시물 ID입니다. 이 매개변수와 start_time이 모두 지정된 경우, 이 매개변수가 우선합니다.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
결과 집합에 포함될 최대 게시물 ID입니다. 이 매개변수와 end_time이 모두 지정된 경우, 이 매개변수가 우선합니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
제외할 엔터티 집합입니다(예: ‘replies’ 또는 ‘retweets’).
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 포스트가 제공될 가장 이른 UTC 타임스탬프입니다. since_id 매개변수도 지정된 경우, since_id가 우선합니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 포스트가 제공될 가장 늦은 UTC 타임스탬프입니다. until_id 매개변수도 지정된 경우, until_id가 우선합니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetTimelineResponse`

<div id="like_post">
  ### `like_post`
</div>

게시물 좋아요
인증된 사용자가 특정 게시물 id를 사용해 해당 게시물에 좋아요를 표시하도록 합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
게시물에 좋아요를 요청하는 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="body.body" type="LikePostRequest">
요청 본문
</ParamField>

<div id="mute_user">
  ### `mute_user`
</div>

사용자 음소거
인증된 사용자가 id로 지정된 특정 사용자를 음소거합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
대상 User를 음소거하도록 요청하는 인증된 소스 User의 id입니다.
</ParamField>

<ParamField path="body.body" type="MuteUserRequest">
요청 본문
</ParamField>

<div id="pin_list">
  ### `pin_list`
</div>

리스트 고정
인증된 사용자가 ID로 지정한 특정 리스트를 고정합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
리스트를 고정할 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="body.body" type="PinListRequest">
요청 본문
</ParamField>

<div id="repost_post">
  ### `repost_post`
</div>

게시물 리포스트
인증된 사용자가 지정한 게시물 ID에 해당하는 특정 게시물을 리포스트합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
게시물 리포스트를 요청하는 인증된 소스 사용자 ID입니다.
</ParamField>

<ParamField path="body.body" type="RepostPostRequest">
요청 본문
</ParamField>

<div id="search">
  ### `search`
</div>

사용자 검색
검색어와 일치하는 사용자 목록을 가져옵니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="Any">
사용자를 검색할 때 사용할 쿼리 문자열입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
이 매개변수는 다음 ‘페이지’의 결과를 가져오는 데 사용됩니다. 이 매개변수에 전달하는 값은 API가 제공한 응답에서 그대로 가져온 것이어야 하며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드 목록을 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드 목록을 쉼표로 구분한 리스트입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드 목록을 쉼표로 구분한 리스트입니다.
</ParamField>

#### 반환값

`IteratorSearchResponse`

<div id="unblock_dms">
  ### `unblock_dms`
</div>

DM 차단 해제
인증된 사용자에 대해 특정 사용자 ID를 기준으로 해당 사용자와 주고받는 다이렉트 메시지 차단을 해제합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
인증된 사용자가 DM 차단을 해제하려는 대상 사용자의 id입니다.
</ParamField>

#### 반환값

`UnblockDmsResponse` - 응답 데이터

<div id="unfollow_list">
  ### `unfollow_list`
</div>

리스트 팔로우 해제
인증된 사용자가 ID로 지정된 특정 리스트의 팔로우를 해제합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
리스트 언팔로우를 수행하는 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.list_id" type="Any">
언팔로우할 리스트의 ID입니다.
</ParamField>

#### 반환값

`UnfollowListResponse` - 응답 데이터

<div id="unfollow_user">
  ### `unfollow_user`
</div>

사용자 언팔로우
인증된 사용자가 특정 사용자의 id로 해당 사용자를 언팔로우합니다.

#### 매개변수

<ParamField path="path.source_user_id" type="Any">
언팔로우를 요청하는 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
소스 사용자가 언팔로우를 요청하는 대상 사용자의 ID입니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`UnfollowUserResponse` - 응답 데이터

<div id="unlike_post">
  ### `unlike_post`
</div>

게시물 좋아요 취소
인증된 사용자가 특정 게시물 ID를 사용해 좋아요를 취소합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
게시물의 좋아요를 취소하려는 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
사용자가 좋아요를 취소하려는 게시물의 ID입니다.
</ParamField>

#### 반환값

`UnlikePostResponse` - 응답 데이터

<div id="unmute_user">
  ### `unmute_user`
</div>

사용자 음소거 해제
인증된 사용자가 지정한 id를 가진 특정 사용자의 음소거를 해제합니다.

#### 매개변수

<ParamField path="path.source_user_id" type="Any">
대상 사용자의 음소거 해제를 요청하는 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
소스 사용자가 음소거 해제를 요청하는 대상 사용자의 ID입니다.
</ParamField>

#### 반환

`UnmuteUserResponse` - 응답 데이터

<div id="unpin_list">
  ### `unpin_list`
</div>

리스트 고정 해제
인증된 사용자가 ID로 지정된 특정 리스트의 고정을 해제합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
결과를 반환할 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.list_id" type="Any">
고정 해제할 리스트의 ID입니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`UnpinListResponse` - 응답 데이터

<div id="unrepost_post">
  ### `unrepost_post`
</div>

게시물 재게시 취소
인증된 사용자가 지정한 ID를 가진 특정 게시물의 재게시를 취소합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
게시물을 리포스트하도록 요청하는 인증된 소스 사용자의 ID입니다.
</ParamField>

<ParamField path="path.source_tweet_id" type="Any">
사용자가 리포스트 취소를 요청하는 게시물의 ID입니다.
</ParamField>

#### 반환값

`UnrepostPostResponse` - 응답 데이터