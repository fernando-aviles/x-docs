---
title: "NewsClient"
sidebarTitle: "NewsClient"
---

<div id="newsclient">
  ## NewsClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">기반 클래스: object</Badge>

뉴스 관련 작업을 위한 Client

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get">
  ### `get`
</div>

ID로 뉴스 기사 가져오기
해당 ID의 뉴스 기사를 가져옵니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.id" type="Any">
뉴스 기사의 식별자(id)입니다.
</ParamField>

<ParamField path="path.news_fields" type="List or None" default="None">
표시할 뉴스 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

#### 반환

`GetResponse` - 응답 데이터

<div id="search">
  ### `search`
</div>

뉴스 검색
지정된 검색 쿼리와 일치하는 뉴스 기사의 목록을 반환합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.query" type="str">
검색 쿼리입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
반환할 결과 개수입니다.
</ParamField>

<ParamField path="path.max_age_hours" type="int or None" default="None">
검색할 뉴스 기사의 최대 경과 시간(시간 단위)입니다.
</ParamField>

<ParamField path="path.news_fields" type="List or None" default="None">
표시할 뉴스 필드를 쉼표로 구분한 리스트입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`SearchResponse` - 응답 데이터