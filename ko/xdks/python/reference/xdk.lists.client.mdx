---
title: "ListsClient"
sidebarTitle: "ListsClient"
---

<div id="listsclient">
  ## ListsClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">기반 클래스: object</Badge>

리스트 관련 작업을 위한 클라이언트

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 매개변수

<ParamField path="path.client" type="Client"></ParamField>

<div id="add_member">
  ### `add_member`
</div>

리스트 멤버 추가
지정된 id의 특정 리스트에 사용자를 추가합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
멤버를 추가할 List의 ID입니다.
</ParamField>

<ParamField path="body.body" type="AddMemberRequest">
요청 본문
</ParamField>

<div id="create">
  ### `create`
</div>

리스트 생성
인증된 사용자에 대해 새 리스트를 생성합니다.
body: 요청 본문
:returns: 응답 data
:rtype: CreateResponse

#### 매개변수

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

리스트 삭제
id로 식별되는, 인증된 사용자가 소유한 특정 리스트를 삭제합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
삭제할 List의 id입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`DeleteResponse` - 응답 데이터

<div id="get_by_id">
  ### `get_by_id`
</div>

id로 리스트 가져오기
지정한 id를 가진 특정 리스트의 세부 정보를 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
List의 ID입니다.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
표시할 List 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`GetByIdResponse` - 응답 데이터

<div id="get_followers">
  ### `get_followers`
</div>

List 팔로워 목록 가져오기
특정 List의 ID를 사용해 해당 List를 팔로우하는 User 목록을 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
리스트의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
반환할 결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
이 매개변수는 지정된 ‘페이지’에 해당하는 결과를 가져오는 데 사용됩니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`IteratorGetFollowersResponse`

<div id="get_members">
  ### `get_members`
</div>

List 멤버 가져오기
특정 List의 id를 지정하여 해당 List에 속한 사용자 목록을 가져옵니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
List의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
최대 결과 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
결과의 특정 "페이지"를 가져오기 위한 매개변수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환

`IteratorGetMembersResponse`

<div id="get_posts">
  ### `get_posts`
</div>

리스트 포스트 가져오기
특정 리스트 ID에 속한 포스트 목록을 가져옵니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
리스트의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 '페이지' 결과를 가져오기 위해 사용하는 매개변수입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 미디어 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 투표 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 장소 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환

`IteratorGetPostsResponse`

<div id="remove_member_by_user_id">
  ### `remove_member_by_user_id`
</div>

리스트 멤버 제거
특정 리스트에서 해당 리스트의 id와 사용자의 id를 사용해 사용자를 제거합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
멤버를 제거할 List의 ID입니다.
</ParamField>

<ParamField path="path.user_id" type="Any">
List에서 제거할 User의 ID입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`RemoveMemberByUserIdResponse` - 응답 데이터

<div id="update">
  ### `update`
</div>

리스트 업데이트
인증된 사용자가 소유한 특정 리스트의 세부 정보를 해당 ID를 사용해 업데이트합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
수정할 List의 ID입니다.
</ParamField>

<ParamField path="body.body" type="UpdateRequest">
요청 본문
</ParamField>