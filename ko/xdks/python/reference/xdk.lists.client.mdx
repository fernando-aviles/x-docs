---
title: "ListsClient"
sidebarTitle: "ListsClient"
---

<div id="listsclient">
  ## ListsClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">기반 클래스: object</Badge>

Lists 관련 작업을 위한 클라이언트

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 매개변수

<ParamField path="path.client" type="Client"></ParamField>

<div id="add_member">
  ### `add_member`
</div>

List 멤버 추가
특정 List의 ID를 사용해 해당 List에 User를 추가합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
멤버를 추가할 List의 ID입니다.
</ParamField>

<ParamField path="body.body" type="AddMemberRequest">
요청 본문
</ParamField>

<div id="create">
  ### `create`
</div>

리스트 생성
인증된 사용자용 새 리스트를 생성합니다.
body: 요청 본문
:returns: 응답 data
:rtype: CreateResponse

#### 매개변수

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

리스트 삭제
인증된 사용자가 소유한 특정 리스트를 해당 id로 삭제합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
삭제할 List의 id입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`DeleteResponse` - 응답 data

<div id="get_by_id">
  ### `get_by_id`
</div>

id로 List 조회
특정 List의 id를 사용해 세부 정보를 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
리스트의 ID입니다.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
표시할 List 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`GetByIdResponse` - 응답 데이터

<div id="get_followers">
  ### `get_followers`
</div>

List 팔로워 조회
특정 List의 ID를 사용해 해당 List를 팔로우하는 User 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
List의 id입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 "페이지"의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`IteratorGetFollowersResponse`

<div id="get_members">
  ### `get_members`
</div>

List 멤버 가져오기
특정 List의 ID를 사용해 해당 List에 속한 User 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
리스트의 id입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
최대 결과 수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
특정 "페이지"의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환

`IteratorGetMembersResponse`

<div id="get_posts">
  ### `get_posts`
</div>

리스트 포스트 조회
지정된 리스트의 ID를 사용해 해당 리스트에 연결된 포스트 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
리스트의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 "페이지"의 결과를 가져오기 위한 매개변수입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`IteratorGetPostsResponse`

<div id="remove_member_by_user_id">
  ### `remove_member_by_user_id`
</div>

리스트 멤버 제거
리스트 id와 사용자 id를 사용해 지정된 리스트에서 사용자를 제거합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
멤버를 제거할 리스트의 ID입니다.
</ParamField>

<ParamField path="path.user_id" type="Any">
리스트에서 제거할 사용자의 ID입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`RemoveMemberByUserIdResponse` - 응답 data

<div id="update">
  ### `update`
</div>

리스트 업데이트
인증된 사용자가 소유한 특정 리스트의 id를 사용해 해당 리스트의 세부 정보를 업데이트합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
수정할 List의 ID입니다.
</ParamField>

<ParamField path="body.body" type="UpdateRequest">
요청 본문
</ParamField>