---
title: "StreamClient"
sidebarTitle: "StreamClient"
---

<div id="submodules">
  ## 하위 모듈
</div>

* [xdk.stream.client 모듈](/ko/xdks/python/reference/xdk.stream.client)
  * [`StreamClient`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient)
    * [`StreamClient.__init__()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.__init__)
    * [`StreamClient.get_rule_counts()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.get_rule_counts)
    * [`StreamClient.get_rules()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.get_rules)
    * [`StreamClient.labels_compliance()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.labels_compliance)
    * [`StreamClient.likes_compliance()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.likes_compliance)
    * [`StreamClient.likes_firehose()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.likes_firehose)
    * [`StreamClient.likes_sample10()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.likes_sample10)
    * [`StreamClient.posts()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts)
    * [`StreamClient.posts_compliance()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_compliance)
    * [`StreamClient.posts_firehose()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_firehose)
    * [`StreamClient.posts_firehose_en()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_firehose_en)
    * [`StreamClient.posts_firehose_ja()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_firehose_ja)
    * [`StreamClient.posts_firehose_ko()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_firehose_ko)
    * [`StreamClient.posts_firehose_pt()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_firehose_pt)
    * [`StreamClient.posts_sample()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_sample)
    * [`StreamClient.posts_sample10()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.posts_sample10)
    * [`StreamClient.update_rules()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.update_rules)
    * [`StreamClient.users_compliance()`](/ko/xdks/python/reference/xdk.stream.client#xdk.stream.client.StreamClient.users_compliance)
* [xdk.stream.models 모듈](/ko/xdks/python/reference/xdk.stream.models)
  * [`GetRuleCountsResponse`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.GetRuleCountsResponse)
    * [`GetRuleCountsResponse.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.GetRuleCountsResponse.model_config)
  * [`GetRulesResponse`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.GetRulesResponse)
    * [`GetRulesResponse.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.GetRulesResponse.model_config)
  * [`LabelsComplianceResponse`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.LabelsComplianceResponse)
    * [`LabelsComplianceResponse.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.LabelsComplianceResponse.model_config)
  * [`LikesComplianceResponse`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.LikesComplianceResponse)
    * [`LikesComplianceResponse.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.LikesComplianceResponse.model_config)
  * [`LikesFirehoseResponse`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.LikesFirehoseResponse)
    * [`LikesFirehoseResponse.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.LikesFirehoseResponse.model_config)
  * [`LikesSample10Response`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.LikesSample10Response)
    * [`LikesSample10Response.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.LikesSample10Response.model_config)
  * [`PostsComplianceResponse`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsComplianceResponse)
    * [`PostsComplianceResponse.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsComplianceResponse.model_config)
  * [`PostsFirehoseEnResponse`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseEnResponse)
    * [`PostsFirehoseEnResponse.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseEnResponse.model_config)
  * [`PostsFirehoseJaResponse`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseJaResponse)
    * [`PostsFirehoseJaResponse.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseJaResponse.model_config)
  * [`PostsFirehoseKoResponse`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseKoResponse)
    * [`PostsFirehoseKoResponse.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseKoResponse.model_config)
  * [`PostsFirehosePtResponse`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehosePtResponse)
    * [`PostsFirehosePtResponse.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehosePtResponse.model_config)
  * [`PostsFirehoseResponse`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseResponse)
    * [`PostsFirehoseResponse.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsFirehoseResponse.model_config)
  * [`PostsResponse`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsResponse)
    * [`PostsResponse.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsResponse.model_config)
  * [`PostsSample10Response`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsSample10Response)
    * [`PostsSample10Response.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsSample10Response.model_config)
  * [`PostsSampleResponse`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsSampleResponse)
    * [`PostsSampleResponse.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.PostsSampleResponse.model_config)
  * [`UpdateRulesRequest`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.UpdateRulesRequest)
    * [`UpdateRulesRequest.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.UpdateRulesRequest.model_config)
  * [`UpdateRulesResponse`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.UpdateRulesResponse)
    * [`UpdateRulesResponse.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.UpdateRulesResponse.model_config)
  * [`UsersComplianceResponse`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.UsersComplianceResponse)
    * [`UsersComplianceResponse.model_config`](/ko/xdks/python/reference/xdk.stream.models#xdk.stream.models.UsersComplianceResponse.model_config)

<div id="module-contents">
  ## 모듈 내용
</div>

이 모듈은 X API의 스트림 엔드포인트에 대한 액세스를 제공하고
스트림 관련 모든 기능에 대한 기본 진입점 역할을 합니다.

<div id="class-xdkstreamstreamclient">
  ### `class xdk.stream.StreamClient`
</div>

스트림 작업을 위한 스트리밍용 Client

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.client" type="Client" />

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.client" type="Client" />

<div id="get_rule_counts">
  ### `get_rule_counts`
</div>

스트림 규칙 수 가져오기
필터 스트림에서 활성 규칙 세트에 포함된 규칙 수를 조회합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.rules_count_fields" type="List or None" default="None">
  표시할 RulesCount 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`GetRuleCountsResponse` - 응답 데이터

<div id="get_rules">
  ### `get_rules`
</div>

스트림 규칙 가져오기
필터링된 스트림에 대해 활성화된 규칙 집합 전체 또는 일부를 조회합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.ids" type="List or None" default="None">
  쉼표로 구분된 규칙 ID 목록입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  최대 결과 수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="str or None" default="None">
  결과를 페이지네이션하기 위해, 요청에서 반환된 ‘next&#95;token’ 값을 전달하면 채워지는 값입니다.
</ParamField>

#### 반환

`IteratorGetRulesResponse`

<div id="labels_compliance">
  ### `labels_compliance`
</div>

게시물 라벨 스트림(Streaming)
게시물에 적용되는 모든 라벨링 이벤트를 스트리밍합니다.
이 엔드포인트는 데이터가 사용 가능해지는 즉시 실시간으로 전송하는 스트리밍 엔드포인트입니다.
스트림에서 수신되는 각 항목은 단일 데이터 포인트를 나타냅니다.
연결은 지수 백오프 재시도 로직으로 자동 관리됩니다.
스트림이 끊어지면 SDK가 클라이언트 측 개입 없이 자동으로 다시 연결합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  요청할 백필(backfill) 기간(분)입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 게시물 레이블이 제공될 가장 이른 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 게시물 레이블이 제공될 가장 나중의 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  재시도 동작, 타임아웃, 콜백을 커스터마이징하기 위한 선택적 StreamConfig입니다.
</ParamField>

<div id="likes_compliance">
  ### `likes_compliance`
</div>

좋아요 컴플라이언스 데이터 스트리밍
사용자의 좋아요와 관련된 모든 컴플라이언스 데이터를 스트리밍합니다.
이 엔드포인트는 데이터가 생성되는 즉시 실시간으로 전달하는 스트리밍 엔드포인트입니다.
각 항목은 스트림에서 나온 하나의 데이터 포인트를 나타냅니다.
연결은 지수 백오프를 사용하는 재시도 로직으로 자동 관리됩니다.
스트림이 끊기면 SDK가 클라이언트 측 개입 없이 자동으로 재연결합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  요청할 백필(backfill) 분 수입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ 형식입니다. Likes 컴플라이언스 이벤트가 제공되기 시작하는 가장 이른 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ 형식입니다. Likes 컴플라이언스 이벤트가 제공되는 가장 늦은 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  재시도 동작, 타임아웃, 콜백을 사용자 지정하기 위한 선택적 StreamConfig입니다.
</ParamField>

<div id="likes_firehose">
  ### `likes_firehose`
</div>

모든 Like 스트림 (스트리밍)
실시간으로 모든 공개 Like를 스트리밍합니다.
이 엔드포인트는 데이터가 생성되는 대로 실시간으로 스트리밍합니다.
각 항목은 스트림에서 나온 단일 데이터 포인트를 나타냅니다.
연결은 지수 백오프 재시도 로직으로 자동 관리됩니다.
스트림이 끊어지면 SDK가 클라이언트 개입 없이 자동으로 다시 연결합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.partition" type="int">
  파티션 번호입니다.
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  요청한 백필(backfill) 분 수입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 좋아요가 제공될 가장 이른 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 게시물이 제공될 가장 늦은 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.like_with_tweet_author_fields" type="List or None" default="None">
  표시할 LikeWithTweetAuthor 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  재시도 동작, 타임아웃 및 콜백을 사용자 정의하기 위한 선택적 StreamConfig입니다.
</ParamField>

<div id="likes_sample10">
  ### `likes_sample10`
</div>

Likes 샘플 스트림 (스트리밍)
공개 Likes의 10% 샘플을 실시간으로 스트리밍합니다.
이 엔드포인트는 데이터가 생성되는 대로 실시간으로 전달하는 스트리밍 엔드포인트입니다.
수신되는 각 항목은 스트림에서 나온 단일 데이터 포인트를 나타냅니다.
연결은 지수 백오프(exponential backoff) 재시도 로직을 사용해 자동으로 관리됩니다.
스트림 연결이 끊어지면 SDK가 별도의 Client 개입 없이 자동으로 재연결합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.partition" type="int">
  파티션 번호입니다.
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  요청한 백필 기간(분)입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 좋아요가 제공될 수 있는 가장 이른 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 포스트가 제공될 수 있는 가장 늦은 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.like_with_tweet_author_fields" type="List or None" default="None">
  표시할 LikeWithTweetAuthor 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  재시도 동작, 타임아웃, 콜백을 사용자 지정하기 위한 선택적 StreamConfig입니다.
</ParamField>

<div id="posts">
  ### `posts`
</div>

필터링된 포스트 스트림 (Streaming)
활성 규칙 집합과 일치하는 포스트를 실시간으로 스트리밍합니다.
이 엔드포인트는 데이터가 사용 가능해지는 대로 실시간으로 제공하는 스트리밍 엔드포인트입니다.
각 반환 항목은 스트림에서 나온 단일 데이터 포인트를 나타냅니다.
연결은 지수 백오프 기반 재시도 로직으로 자동으로 관리됩니다.
스트림이 끊기면 SDK가 Client의 개입 없이 자동으로 다시 연결합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  요청할 백필 기간(분)입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ 형식. 포스트가 제공될 가장 이른 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ 형식. 포스트가 제공될 가장 늦은 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  재시도 동작, 타임아웃 및 콜백을 커스터마이징하기 위한 선택적 StreamConfig입니다.
</ParamField>

<div id="posts_compliance">
  ### `posts_compliance`
</div>

포스트 컴플라이언스 데이터를 스트리밍합니다 (스트리밍)
포스트와 관련된 모든 컴플라이언스 데이터를 스트리밍합니다.
이 엔드포인트는 데이터가 사용 가능해지는 즉시 실시간으로 전송하는 스트리밍 엔드포인트입니다.
각 항목은 스트림에서 나온 단일 데이터 포인트를 나타냅니다.
연결은 지수적 백오프 재시도 로직으로 자동으로 관리됩니다.
스트림 연결이 끊기면 SDK가 별도 개입 없이 자동으로 다시 연결합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.partition" type="int">
  파티션 번호입니다.
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  요청한 백필 기간(분)입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 게시물 규정 준수 이벤트가 제공되기 시작하는 가장 이른 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 게시물 규정 준수 이벤트가 제공되는 범위의 가장 늦은 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  재시도 동작, 타임아웃, 콜백을 사용자 지정할 수 있는 선택적 StreamConfig입니다.
</ParamField>

<div id="posts_firehose">
  ### `posts_firehose`
</div>

모든 포스트 스트리밍 (스트리밍)
실시간으로 모든 공개 포스트를 스트리밍합니다.
이 엔드포인트는 데이터가 생성되는 대로 실시간으로 전달하는 스트리밍 엔드포인트입니다.
각 항목은 스트림에서 나온 단일 데이터 포인트를 나타냅니다.
연결은 지수적 백오프 재시도 로직으로 자동으로 관리됩니다.
스트림이 끊어지면 SDK가 사용자 개입 없이 자동으로 재연결합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.partition" type="int">
  파티션 번호입니다.
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  요청한 백필(backfill) 시간(분)입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 포스트가 제공되는 가장 이른 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 포스트가 제공되는 가장 늦은 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 Tweet 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  표시할 Media 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  표시할 Poll 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 User 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  표시할 Place 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  재시도 동작, 타임아웃, 콜백을 사용자 지정하기 위한 선택적 StreamConfig입니다.
</ParamField>

<div id="posts_firehose_en">
  ### `posts_firehose_en`
</div>

영어 포스트 스트림 (스트리밍)
실시간으로 모든 공개 영어 포스트를 스트리밍합니다.
이 엔드포인트는 데이터가 생성되는 즉시 실시간으로 전달하는 스트리밍 엔드포인트입니다.
각 전달 항목은 스트림의 단일 데이터 포인트를 나타냅니다.
연결은 지수 백오프가 적용된 재시도 로직으로 자동 관리됩니다.
스트림이 끊기면 SDK가 Client의 개입 없이 자동으로 재연결합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.partition" type="int">
  파티션 번호입니다.
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  요청된 백필 분 수입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 포스트가 제공될 가장 이른 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 포스트가 제공될 가장 늦은 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 Tweet 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  표시할 Media 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  표시할 Poll 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 User 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  표시할 Place 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  재시도 동작, 타임아웃, 콜백을 사용자 지정하기 위한 선택적 StreamConfig입니다.
</ParamField>

<div id="posts_firehose_ja">
  ### `posts_firehose_ja`
</div>

일본어 포스트 스트림(Streaming)
모든 공개 일본어 포스트를 실시간으로 스트리밍합니다.
이 엔드포인트는 데이터가 생성되는 대로 실시간으로 전달하는 스트리밍 엔드포인트입니다.
스트림에서 전달되는 각 항목은 단일 데이터 포인트를 나타냅니다.
연결은 지수 백오프 재시도 로직으로 자동으로 관리됩니다.
스트림이 끊어지면 SDK가 클라이언트 개입 없이 자동으로 재연결합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.partition" type="int">
  파티션 번호입니다.
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  요청한 백필 분(minute) 수입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 포스트가 제공될 가장 이른 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 포스트가 제공될 가장 늦은 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 Tweet 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  표시할 Media 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  표시할 Poll 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 User 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  표시할 Place 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  재시도 동작, 타임아웃, 콜백을 사용자 지정하기 위한 옵션인 StreamConfig입니다.
</ParamField>

<div id="posts_firehose_ko">
  ### `posts_firehose_ko`
</div>

한국어 포스트 스트리밍 (Streaming)
실시간으로 모든 공개 한국어 포스트를 스트리밍합니다.
이 엔드포인트는 데이터가 생성되는 대로 실시간으로 전달하는 스트리밍 엔드포인트입니다.
각 수신 항목은 스트림에서 나온 단일 데이터 포인트를 나타냅니다.
연결은 지수 백오프 기반 재시도 로직으로 자동 관리됩니다.
스트림 연결이 끊어지면 SDK가 별도의 클라이언트 개입 없이 자동으로 재연결합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.partition" type="int">
  파티션 번호입니다.
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  요청한 백필 기간(분)입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 포스트가 제공될 가장 이른 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. 포스트가 제공될 가장 늦은 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  재시도 동작, 타임아웃, 콜백을 사용자 지정하기 위한 선택적 StreamConfig입니다.
</ParamField>

<div id="posts_firehose_pt">
  ### `posts_firehose_pt`
</div>

포르투갈어 포스트 스트리밍 (Streaming)
실시간으로 모든 공개 포르투갈어 포스트를 스트리밍합니다.
이 엔드포인트는 데이터가 사용 가능해지는 대로 실시간으로 데이터를 제공하는 스트리밍 엔드포인트입니다.
제공되는 각 항목은 스트림에서 나온 단일 데이터 포인트를 나타냅니다.
연결은 지수적 백오프 재시도 로직으로 자동 관리됩니다.
스트림 연결이 끊어지면 SDK가 클라이언트 개입 없이 자동으로 재연결합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.partition" type="int">
  파티션 번호입니다.
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  요청된 백필(backfill) 기간(분 단위)입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ 형식. 포스트가 제공될 가장 이른 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ 형식. 포스트가 제공될 가장 늦은 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  재시도 동작, 타임아웃 및 콜백을 사용자 지정하기 위한 선택적 StreamConfig입니다.
</ParamField>

<div id="posts_sample">
  ### `posts_sample`
</div>

샘플링된 포스트 스트림 (스트리밍)
실시간으로 공개 포스트의 1% 샘플을 스트리밍합니다.
이는 데이터가 사용 가능해지는 즉시 실시간으로 데이터를 제공하는 스트리밍 엔드포인트입니다.
각 항목은 스트림에서 나온 하나의 데이터 포인트를 나타냅니다.
연결은 지수 백오프 기반 재시도 로직으로 자동 관리됩니다.
스트림 연결이 끊어지면 SDK가 별도의 클라이언트 개입 없이 자동으로 재연결합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  요청한 백필(backfill) 기간(분 단위)입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 Tweet 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  표시할 Media 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  표시할 Poll 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 User 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  표시할 Place 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  재시도 동작, 타임아웃 및 콜백을 커스터마이즈하기 위한 선택적 StreamConfig입니다.
</ParamField>

<div id="posts_sample10">
  ### `posts_sample10`
</div>

10% 샘플 포스트 스트리밍 (Streaming)
실시간으로 전체 공개 포스트의 10% 샘플을 스트리밍합니다.
이것은 데이터가 사용 가능해지는 즉시 실시간으로 전송하는 스트리밍 엔드포인트입니다.
각 항목은 스트림에서 나온 단일 데이터 포인트를 나타냅니다.
연결은 지수 백오프 재시도 로직으로 자동 관리됩니다.
스트림이 끊어지면 SDK가 클라이언트 개입 없이 자동으로 재연결합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.partition" type="int">
  파티션 번호입니다.
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  요청된 백필(backfill) 분 수입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ 형식입니다. 게시물이 제공될 가장 이른 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ 형식입니다. 게시물이 제공될 가장 늦은 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  재시도 동작, 타임아웃 및 콜백을 사용자 지정하기 위한 선택적 StreamConfig입니다.
</ParamField>

<div id="update_rules">
  ### `update_rules`
</div>

스트림 규칙 업데이트
필터링된 스트림의 활성 규칙 집합에 규칙을 추가하거나 삭제합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="body.body" type="UpdateRulesRequest">
  요청 본문
</ParamField>

<div id="users_compliance">
  ### `users_compliance`
</div>

Users 규정 준수 데이터를 스트리밍합니다 (스트리밍)
Users와 관련된 모든 규정 준수 데이터를 스트리밍합니다.
이 엔드포인트는 데이터가 사용 가능해지는 즉시 실시간으로 전달하는 스트리밍 엔드포인트입니다.
스트림에서 전달되는 각 항목은 단일 데이터 포인트를 나타냅니다.
연결은 지수 백오프 재시도 로직으로 자동 관리됩니다.
스트림 연결이 끊어지면 SDK가 Client의 개입 없이 자동으로 재연결합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.partition" type="int">
  파티션 번호입니다.
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
  요청할 백필 기간(분)입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. User Compliance 이벤트를 제공하기 시작할 가장 이른 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. User Compliance 이벤트를 제공하는 데 사용될 가장 늦은 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
  재시도 동작, 타임아웃 및 콜백을 사용자 정의하기 위한 선택적 StreamConfig입니다.
</ParamField>