---
title: "SpacesClient"
sidebarTitle: "SpacesClient"
---

<div id="spacesclient">
  ## SpacesClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">기반 클래스: object</Badge>

Spaces 작업을 수행하기 위한 Client

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 매개변수

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_buyers">
  ### `get_buyers`
</div>

Space 티켓 구매자 조회
지정된 Space의 id를 기준으로 해당 Space 티켓을 구매한 사용자 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="str">
조회할 Space의 id입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 ‘페이지’의 결과를 조회하는 데 사용됩니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`IteratorGetBuyersResponse`

<div id="get_by_creator_ids">
  ### `get_by_creator_ids`
</div>

creator ID로 Spaces 조회
지정한 User ID가 생성한 Spaces의 세부 정보를 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.user_ids" type="List">
검색 대상 사용자들의 ID입니다.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
표시할 Space 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
expansions에 사용할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
표시할 토픽 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`GetByCreatorIdsResponse` - 응답 데이터

<div id="get_by_id">
  ### `get_by_id`
</div>

ID로 스페이스 가져오기
해당 ID를 사용해 특정 스페이스의 세부 정보를 가져옵니다.

#### 매개변수

<ParamField path="path.id" type="str">
가져올 Space의 ID입니다.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
표시할 Space 필드를 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분된 리스트입니다.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
표시할 Topic 필드를 쉼표로 구분된 리스트입니다.
</ParamField>

#### 반환

`GetByIdResponse` - 응답 데이터

<div id="get_by_ids">
  ### `get_by_ids`
</div>

id로 Spaces 가져오기
여러 Space의 세부 정보를 각 id로 가져옵니다.

#### 매개변수

<ParamField path="path.ids" type="List">
반환할 Space ID 목록입니다.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
표시할 Space 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
표시할 Topic 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`GetByIdsResponse` - 응답 데이터

<div id="get_posts">
  ### `get_posts`
</div>

Space 게시물 가져오기
특정 Space에서 공유된 게시물 목록을 해당 Space의 ID를 사용해 조회합니다.

#### 매개변수

<ParamField path="path.id" type="str">
조회할 Space의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
지정한 Space에서 가져올 게시물의 개수입니다. 값을 지정하지 않으면 최대값인 100으로 기본 설정됩니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 미디어 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 투표 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 장소 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환

`GetPostsResponse` - 응답 데이터

<div id="search">
  ### `search`
</div>

Space 검색
지정된 검색 쿼리에 맞는 Space 리스트를 가져옵니다.

#### 매개변수

<ParamField path="path.query" type="str">
검색 쿼리입니다.
</ParamField>

<ParamField path="path.state" type="str or None" default="None">
검색할 Space의 상태입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
반환할 결과의 개수입니다.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
표시할 Space 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
표시할 Topic 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환

`SearchResponse` - 응답 데이터