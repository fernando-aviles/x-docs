---
title: "SpacesClient"
sidebarTitle: "SpacesClient"
---

<div id="spacesclient">
  ## SpacesClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">기반 클래스: object</Badge>

Spaces 관련 작업을 위한 클라이언트

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_buyers">
  ### `get_buyers`
</div>

Space 티켓 구매자 조회
특정 Space의 ID를 사용해 해당 Space의 티켓을 구매한 사용자 목록을 조회합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.id" type="str">
조회할 Space의 ID입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
결과의 특정 ‘페이지’를 가져오는 데 사용하는 매개변수입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드를 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환

`IteratorGetBuyersResponse`

<div id="get_by_creator_ids">
  ### `get_by_creator_ids`
</div>

creator id로 Spaces 조회
지정된 사용자 id가 생성한 Spaces의 세부 정보를 조회합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.user_ids" type="List">
검색할 User의 id 목록입니다.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
표시할 Space 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
표시할 Topic 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환

`GetByCreatorIdsResponse` - 응답 데이터

<div id="get_by_id">
  ### `get_by_id`
</div>

ID로 스페이스 가져오기
지정된 ID의 특정 스페이스 상세 정보를 조회합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.id" type="str">
가져올 Space의 ID입니다.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
표시할 Space 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
표시할 Topic 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`GetByIdResponse` - 응답 데이터

<div id="get_by_ids">
  ### `get_by_ids`
</div>

ID로 여러 Space 가져오기
여러 Space의 id를 사용해 해당 Space의 세부 정보를 조회합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.ids" type="List">
반환할 Space ID의 목록입니다.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
표시할 Space 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
표시할 Topic 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`GetByIdsResponse` - 응답 데이터

<div id="get_posts">
  ### `get_posts`
</div>

스페이스 포스트 가져오기
지정된 ID를 사용하여 특정 스페이스에서 공유된 포스트 목록을 가져옵니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.id" type="str">
가져올 Space의 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
지정된 Space에서 가져올 포스트 개수입니다. 값이 제공되지 않으면 최대값인 100으로 기본 설정됩니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환 값

`GetPostsResponse` - 응답 data 객체

<div id="search">
  ### `search`
</div>

스페이스 검색
지정된 검색 쿼리와 일치하는 스페이스 목록을 반환합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.query" type="str">
검색 쿼리입니다.
</ParamField>

<ParamField path="path.state" type="str or None" default="None">
검색할 Spaces의 상태입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
반환할 결과 수입니다.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
표시할 Space 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
표시할 Topic 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### Returns
</div>

`SearchResponse` - 응답 데이터