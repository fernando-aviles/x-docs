---
title: "ActivityClient"
sidebarTitle: "ActivityClient"
---

이 모듈은 X API의 activity 엔드포인트와 상호작용하기 위한 클라이언트를 제공합니다.
실시간 스트리밍 작업은 도착하는 대로 data를 내보내는 제너레이터를 반환합니다.
스트리밍 연결은 안정적인 처리를 위해 지수 백오프 재시도 로직으로 자동 관리됩니다.

<div id="activityclient">
  ## ActivityClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">상속: object</Badge>

Activity 작업용 스트리밍 클라이언트

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 매개변수

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_subscription">
  ### `create_subscription`
</div>

X 활동 구독 생성
X 활동 이벤트에 대한 구독을 생성합니다
body: 요청 본문
:returns: 응답 데이터
:rtype: CreateSubscriptionResponse

#### 매개변수

<ParamField path="body.body" type="CreateSubscriptionRequest"></ParamField>

<div id="delete_subscription">
  ### `delete_subscription`
</div>

X 활동 구독을 삭제합니다
X 활동 이벤트에 대한 구독을 삭제합니다

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.subscription_id" type="Any">
삭제할 구독의 ID입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`DeleteSubscriptionResponse` - 응답 데이터

<div id="get_subscriptions">
  ### `get_subscriptions`
</div>

X 활동 구독을 가져옵니다
XAA에 대한 활성 구독 목록을 가져옵니다
:returns: 응답 데이터
:rtype: GetSubscriptionsResponse

<div id="returns">
  #### 반환값
</div>

`GetSubscriptionsResponse`

<div id="stream">
  ### `stream`
</div>

Activity Stream (스트리밍)
X 활동 스트림
이 엔드포인트는 data가 사용 가능해지는 대로 실시간으로 제공하는 스트리밍 엔드포인트입니다.
각 항목은 스트림에서 나온 단일 data 포인트를 나타냅니다.
연결은 지수 백오프 기반 재시도 로직으로 자동 관리됩니다.
스트림이 끊기면 SDK가 클라이언트 개입 없이 자동으로 다시 연결합니다.

#### 매개변수

<ParamField path="path.backfill_minutes" type="int or None" default="None">
요청한 백필 기간(분)입니다.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 포스트 레이블이 제공될 가장 이른 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 포스트 레이블이 제공될 가장 늦은 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
재시도 동작, 타임아웃, 콜백을 커스터마이즈하기 위한 선택적 StreamConfig입니다.
</ParamField>

<div id="update_subscription">
  ### `update_subscription`
</div>

X activity 구독 업데이트
X activity 이벤트 구독을 업데이트합니다

#### 매개변수

<ParamField path="path.subscription_id" type="Any">
업데이트할 구독의 ID입니다.
</ParamField>

<ParamField path="body.body" type="UpdateSubscriptionRequest">
요청 본문
</ParamField>