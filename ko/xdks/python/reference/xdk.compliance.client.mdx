---
title: "ComplianceClient"
sidebarTitle: "ComplianceClient"
---

<div id="complianceclient">
  ## ComplianceClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">기반 클래스: object</Badge>

컴플라이언스 작업용 Client

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_jobs">
  ### `create_jobs`
</div>

컴플라이언스 작업 생성
지정된 작업 type에 대한 새 컴플라이언스 작업을 생성합니다.
body: 요청 본문
:returns: 응답 데이터
:rtype: CreateJobsResponse

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="body.body" type="CreateJobsRequest"></ParamField>

<div id="get_jobs">
  ### `get_jobs`
</div>

컴플라이언스 작업 가져오기
작업 type 및 선택적 상태로 필터링된 컴플라이언스 작업 리스트를 가져옵니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.type" type="str">
조회할 Compliance Job의 type입니다.
</ParamField>

<ParamField path="path.status" type="str or None" default="None">
조회할 Compliance Job의 상태입니다.
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
표시할 ComplianceJob 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환값

`GetJobsResponse` - 응답 데이터

<div id="get_jobs_by_id">
  ### `get_jobs_by_id`
</div>

ID로 Compliance Job 조회
지정한 ID에 해당하는 특정 Compliance Job의 세부 정보를 조회합니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.id" type="Any">
조회할 컴플라이언스 작업의 ID입니다.
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
표시할 ComplianceJob 필드의 쉼표로 구분된 리스트입니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`GetJobsByIdResponse` - 응답 데이터