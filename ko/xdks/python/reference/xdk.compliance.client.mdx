---
title: "ComplianceClient"
sidebarTitle: "ComplianceClient"
---

<div id="complianceclient">
  ## ComplianceClient
</div>

<Badge color="blue">Class</Badge>

<Badge color="gray">Bases: object</Badge>

컴플라이언스 작업용 클라이언트

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_jobs">
  ### `create_jobs`
</div>

컴플라이언스 작업 생성
지정된 작업 유형의 새 컴플라이언스 작업을 생성합니다.
body: 요청 본문
:returns: 응답 data
:rtype: CreateJobsResponse

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="body.body" type="CreateJobsRequest"></ParamField>

<div id="get_jobs">
  ### `get_jobs`
</div>

규정 준수 작업 가져오기
작업 유형과 선택적인 상태를 기준으로 필터링된 규정 준수 작업 목록을 가져옵니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.type" type="str">
나열할 Compliance Job의 유형.
</ParamField>

<ParamField path="path.status" type="str or None" default="None">
나열할 Compliance Job의 상태.
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
표시할 ComplianceJob 필드의 쉼표로 구분된 목록.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`GetJobsResponse` - 응답 데이터

<div id="get_jobs_by_id">
  ### `get_jobs_by_id`
</div>

ID로 Compliance Job 가져오기
특정 Compliance Job을 ID로 조회하여 세부 정보를 가져옵니다.

<div id="parameters">
  #### 매개변수
</div>

<ParamField path="path.id" type="Any">
가져올 Compliance Job의 id입니다.
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
표시할 ComplianceJob 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`GetJobsByIdResponse` - 응답 데이터