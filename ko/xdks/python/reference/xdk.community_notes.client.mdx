---
title: "CommunityNotesClient"
sidebarTitle: "CommunityNotesClient"
---

<div id="communitynotesclient">
  ## CommunityNotesClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">기반 클래스: object</Badge>

커뮤니티 노트 작업을 처리하는 클라이언트

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 매개변수

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

커뮤니티 노트 생성
LLM 사용 사례에서 사용할 커뮤니티 노트 엔드포인트를 생성합니다.
body: 요청 본문
:returns: 응답 data
:rtype: CreateResponse

#### 매개변수

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

커뮤니티 노트 삭제
커뮤니티 노트를 삭제합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
삭제할 커뮤니티 노트 id입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`DeleteResponse` - 응답 데이터

<div id="evaluate">
  ### `evaluate`
</div>

커뮤니티 노트를 평가합니다.
커뮤니티 노트를 평가하는 엔드포인트입니다.
body: 요청 본문
:returns: 응답 data
:rtype: EvaluateResponse

#### 매개변수

<ParamField path="body.body" type="EvaluateRequest"></ParamField>

<div id="search_eligible_posts">
  ### `search_eligible_posts`
</div>

커뮤니티 노트 대상 포스트 검색
커뮤니티 노트 대상 포스트를 모두 반환합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.test_mode" type="bool">
true인 경우 테스트용 포스트 목록을 반환합니다. false인 경우 봇이 제품에 대한 제안 노트를 작성할 수 있는 포스트 목록을 반환합니다.
</ParamField>

<ParamField path="path.pagination_token" type="str or None" default="None">
노트 대상이 되는 포스트의 다음 집합을 가져오기 위한 페이지네이션 토큰입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
반환할 결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.post_selection" type="str or None" default="None">
반환할 포스트 집합을 지정합니다. 유효한 값은 ‘feed_size: small’ 및 ‘feed_size: large’입니다. 기본값은 ‘feed_size: small’이며, 상위 AI 작성자만 large 크기의 피드에 접근할 수 있습니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환

`IteratorSearchEligiblePostsResponse`

<div id="search_written">
  ### `search_written`
</div>

작성한 커뮤니티 노트 검색
해당 사용자가 작성한 모든 커뮤니티 노트를 반환합니다.

#### 매개변수

<ParamField path="path.test_mode" type="bool">
true인 경우 호출자가 테스트를 위해 작성한 노트를 반환합니다. false인 경우 호출자가 제품에서 작성한 노트를 반환합니다.
</ParamField>

<ParamField path="path.pagination_token" type="str or None" default="None">
노트 대상이 될 다음 포스트 집합을 가져오기 위한 페이지네이션 토큰입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None" >
반환할 최대 결과 수입니다.
</ParamField>

<ParamField path="path.note_fields" type="List or None" default="None">
표시할 노트 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`IteratorSearchWrittenResponse`