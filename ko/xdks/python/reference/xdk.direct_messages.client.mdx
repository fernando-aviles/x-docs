---
title: "DirectMessagesClient"
sidebarTitle: "DirectMessagesClient"
---

<div id="directmessagesclient">
  ## DirectMessagesClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">기반 클래스: object</Badge>

다이렉트 메시지 관련 작업을 위한 클라이언트

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 매개변수

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_by_conversation_id">
  ### `create_by_conversation_id`
</div>

대화 id로 DM 메시지 생성
특정 대화의 id를 지정해 해당 대화로 새 다이렉트 메시지를 전송합니다.

#### 매개변수

<ParamField path="path.dm_conversation_id" type="str">
DM 대화 ID.
</ParamField>

<ParamField path="body.body" type="CreateByConversationIdRequest">
요청 본문
</ParamField>

<div id="create_by_participant_id">
  ### `create_by_participant_id`
</div>

participant id를 사용해 DM 메시지 생성
특정 참여자의 id를 사용해 새 다이렉트 메시지를 보냅니다.

#### 매개변수

<ParamField path="path.participant_id" type="Any">
DM을 받는 수신 사용자 ID입니다.
</ParamField>

<ParamField path="body.body" type="CreateByParticipantIdRequest">
요청 본문
</ParamField>

<div id="create_conversation">
  ### `create_conversation`
</div>

DM 대화 생성
지정된 참여자들과 새로운 다이렉트 메시지 대화를 시작합니다.
body: 요청 본문
:returns: 응답 데이터
:rtype: CreateConversationResponse

#### 매개변수

<ParamField path="body.body" type="CreateConversationRequest"></ParamField>

<div id="delete_events">
  ### `delete_events`
</div>

DM 이벤트 삭제
인증된 사용자가 소유한 경우, 지정된 id를 가진 특정 다이렉트 메시지 이벤트를 삭제합니다.

#### 매개변수

<ParamField path="path.event_id" type="Any">
삭제할 다이렉트 메시지 이벤트의 ID입니다.
</ParamField>

#### 반환

`DeleteEventsResponse` - 응답 데이터

<div id="get_events">
  ### `get_events`
</div>

DM 이벤트 가져오기
모든 대화에서 발생한 최신 다이렉트 메시지 이벤트 목록을 검색합니다.

#### 매개변수

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
이 매개변수는 지정된 ‘페이지’의 결과를 가져오는 데 사용됩니다.
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
결과에 포함할 event_types 집합입니다.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
표시할 DmEvent 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`IteratorGetEventsResponse`

<div id="get_events_by_conversation_id">
  ### `get_events_by_conversation_id`
</div>

DM 대화의 DM 이벤트 가져오기
특정 DM 대화에 대한 DM 이벤트를 가져옵니다.

#### 매개변수

<ParamField path="path.id" type="Any">
DM 대화 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
결과의 특정 ‘페이지’를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
결과에 포함할 event_types의 집합입니다.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
표시할 DmEvent 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`IteratorGetEventsByConversationIdResponse`

<div id="get_events_by_id">
  ### `get_events_by_id`
</div>

ID로 DM 이벤트 가져오기
특정 다이렉트 메시지(DM) 이벤트의 세부 정보를 해당 ID로 조회합니다.

#### 매개변수

<ParamField path="path.event_id" type="Any">
DM 이벤트 id.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
표시할 DmEvent 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환

`GetEventsByIdResponse` - 응답 data

<div id="get_events_by_participant_id">
  ### `get_events_by_participant_id`
</div>

특정 DM 대화의 DM 이벤트 가져오기
특정 대화의 다이렉트 메시지 이벤트를 가져옵니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.participant_id" type="Any">
일대일 DM 대화의 참가자 사용자 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 ‘페이지’의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
결과에 포함할 event_types의 집합입니다.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
표시할 DmEvent 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환값
</div>

`IteratorGetEventsByParticipantIdResponse`