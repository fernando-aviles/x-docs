---
title: "PostsClient"
sidebarTitle: "PostsClient"
---

<div id="postsclient">
  ## PostsClient
</div>

<Badge color="blue">클래스</Badge>

<Badge color="gray">상속: object</Badge>

포스트 관련 작업을 위한 클라이언트

<div id="constructors">
  ## 생성자
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 매개변수

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

포스트 생성 또는 수정
인증된 사용자를 위해 새 포스트를 생성하거나, `edit_options`가 제공된 경우 기존 포스트를 수정합니다.
body: 요청 본문
:returns: 응답 data
:rtype: CreateResponse

#### 매개변수

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

포스트 삭제
인증된 사용자가 소유한 경우에 한해, 지정한 ID의 특정 포스트를 삭제합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
삭제할 포스트의 ID입니다.
</ParamField>

#### 반환

`DeleteResponse` - 응답 데이터

<div id="get_analytics">
  ### `get_analytics`
</div>

포스트 분석 가져오기
지정된 기간 내 특정 포스트에 대한 분석 데이터를 가져옵니다.

#### 매개변수

<ParamField path="path.ids" type="List">
쉼표로 구분된 포스트 id 목록입니다. 하나의 요청당 최대 100개까지 허용됩니다.
</ParamField>

<ParamField path="path.end_time" type="str">
YYYY-MM-DDTHH:mm:ssZ 형식입니다. 시간 범위의 종료 시점을 나타내는 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.start_time" type="str">
YYYY-MM-DDTHH:mm:ssZ 형식입니다. 시간 범위의 시작 시점을 나타내는 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.granularity" type="str">
검색 결과 수에 사용할 세분화 수준입니다.
</ParamField>

<ParamField path="path.analytics_fields" type="List or None" default="None">
표시할 애널리틱스 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`GetAnalyticsResponse` - 응답 데이터

<div id="get_by_id">
  ### `get_by_id`
</div>

ID로 포스트 조회
ID를 사용하여 특정 포스트의 세부 정보를 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
단일 포스트 ID입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환

`GetByIdResponse` - 응답 데이터

<div id="get_by_ids">
  ### `get_by_ids`
</div>

id로 포스트 가져오기
여러 포스트의 세부 정보를 id로 가져옵니다.

#### 매개변수

<ParamField path="path.ids" type="List">
쉼표로 구분된 포스트 ID 목록입니다. 단일 요청에는 최대 100개까지 허용됩니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`GetByIdsResponse` - 응답 data

<div id="get_counts_all">
  ### `get_counts_all`
</div>

모든 포스트 개수 가져오기
전체 아카이브에서 검색 쿼리와 일치하는 포스트의 개수를 조회합니다.

#### 매개변수

<ParamField path="path.query" type="str">
포스트를 검색하기 위한 단일 쿼리/규칙/필터입니다. 최대 쿼리 길이를 확인하려면 [https://t.co/rulelength](https://t.co/rulelength)를 참고하세요.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ 형식입니다. 포스트가 제공될 수 있는 가장 오래된 UTC 타임스탬프(최근 7일 이내)입니다. 타임스탬프는 초 단위의 세분화 수준을 가지며, 포함 범위입니다(예: 12:00:01은 해당 분의 첫 번째 초를 포함합니다).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ 형식입니다. 포스트가 제공될 수 있는 가장 최신 UTC 타임스탬프입니다. 타임스탬프는 초 단위의 세분화 수준을 가지며, 제외 범위입니다(예: 12:00:01은 해당 분의 첫 번째 초를 제외합니다).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
지정된 ID보다 큰(즉, 더 최신인) 포스트 ID를 가진 결과를 반환합니다.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
지정된 ID보다 작은(즉, 더 오래된) 포스트 ID를 가진 결과를 반환합니다.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
이 매개변수는 다음 ‘페이지’의 결과를 가져오는 데 사용됩니다. 이 매개변수에 사용하는 값은 API가 제공한 응답에서 직접 가져와야 하며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
이 매개변수는 다음 ‘페이지’의 결과를 가져오는 데 사용됩니다. 이 매개변수에 사용하는 값은 API가 제공한 응답에서 직접 가져와야 하며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
검색 카운트 결과의 세분화 수준입니다.
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
표시할 SearchCount 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetCountsAllResponse`

<div id="get_counts_recent">
  ### `get_counts_recent`
</div>

최근 포스트 수 가져오기
검색 쿼리에 일치하는 최근 7일간의 포스트 수를 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="str">
포스트를 매칭하기 위한 하나의 쿼리/규칙/필터입니다. 최대 쿼리 길이를 확인하려면 [https://t.co/rulelength](https://t.co/rulelength)를 참조하세요.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 포스트가 제공될 기준이 되는 가장 오래된 UTC 타임스탬프입니다(가장 최근 7일 이내). 타임스탬프는 초 단위 정밀도를 가지며, 포함 범위입니다(예: 12:00:01은 해당 분의 첫 번째 초를 포함합니다).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 포스트가 제공될 기준이 되는 가장 최신 UTC 타임스탬프입니다. 타임스탬프는 초 단위 정밀도를 가지며, 제외 범위입니다(예: 12:00:01은 해당 분의 첫 번째 초를 제외합니다).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
지정된 id보다 큰(즉, 더 최근의) 포스트 id를 가진 결과를 반환합니다.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
지정된 id보다 작은(즉, 더 이전의) 포스트 id를 가진 결과를 반환합니다.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
이 매개변수는 다음 ‘페이지’의 결과를 가져오는 데 사용됩니다. 이 매개변수에 사용되는 값은 API가 제공한 응답에서 직접 가져오며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
이 매개변수는 다음 ‘페이지’의 결과를 가져오는 데 사용됩니다. 이 매개변수에 사용되는 값은 API가 제공한 응답에서 직접 가져오며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
검색 집계 결과에 대한 세분화 수준입니다.
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
표시할 SearchCount 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`IteratorGetCountsRecentResponse`

<div id="get_insights28hr">
  ### `get_insights28hr`
</div>

28시간 포스트 인사이트 가져오기
지정한 포스트에 대해 지난 28시간 동안의 참여 지표를 조회합니다.

#### 매개변수

<ParamField path="path.tweet_ids" type="List">
28시간 메트릭용 포스트 id 목록입니다.
</ParamField>

<ParamField path="path.granularity" type="str">
메트릭 응답의 세분화 수준입니다.
</ParamField>

<ParamField path="path.requested_metrics" type="List">
히스토리컬 요청에 대해 조회할 메트릭입니다.
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
표시할 참여도 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<div id="returns">
  #### 반환 값
</div>

`GetInsights28hrResponse` - 응답 데이터

<div id="get_insights_historical">
  ### `get_insights_historical`
</div>

과거 포스트 인사이트 조회
지정된 포스트에 대해 설정된 기간 내의 과거 참여 지표를 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.tweet_ids" type="List">
과거 메트릭용 포스트 id 목록입니다.
</ParamField>

<ParamField path="path.end_time" type="str">
YYYY-MM-DDTHH:mm:ssZ. 기간의 종료 시점을 나타내는 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.start_time" type="str">
YYYY-MM-DDTHH:mm:ssZ. 기간의 시작 시점을 나타내는 UTC 타임스탬프입니다.
</ParamField>

<ParamField path="path.granularity" type="str">
메트릭 응답의 세분화 수준입니다.
</ParamField>

<ParamField path="path.requested_metrics" type="List">
과거 조회에 대해 요청할 메트릭입니다.
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
표시할 Engagement 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`GetInsightsHistoricalResponse` - 응답 데이터

<div id="get_liking_users">
  ### `get_liking_users`
</div>

좋아요를 누른 사용자 가져오기
지정된 id를 가진 특정 포스트에 좋아요를 누른 사용자 목록을 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
단일 포스트 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
결과의 다음 ‘페이지’를 가져오는 데 사용하는 매개변수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환값

`IteratorGetLikingUsersResponse`

<div id="get_quoted">
  ### `get_quoted`
</div>

인용된 포스트 가져오기
특정 포스트 ID를 기준으로, 해당 포스트를 인용한 포스트 목록을 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
단일 포스트 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
반환할 최대 결과 수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
지정된 ‘페이지’의 결과를 가져오는 데 사용하는 파라미터입니다.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
제외할 엔티티 집합입니다(예: ‘replies’ 또는 ‘retweets’).
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 트윗 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 미디어 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 투표 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 사용자 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 장소 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetQuotedResponse`

<div id="get_reposted_by">
  ### `get_reposted_by`
</div>

리포스트한 사용자 조회
특정 포스트 ID를 기준으로 해당 포스트를 리포스트한 사용자 목록을 조회합니다.

#### 매개변수

<ParamField path="path.id" type="Any">
단일 포스트 ID입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 ‘페이지’의 결과를 가져오는 데 사용하는 매개변수입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드를 쉼표로 구분한 목록입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드를 쉼표로 구분한 목록입니다.
</ParamField>

#### 반환값

`IteratorGetRepostedByResponse`

<div id="get_reposts">
  ### `get_reposts`
</div>

리포스트 가져오기
지정된 ID를 가진 특정 포스트를 리포스트한 포스트 목록을 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
단일 포스트 id입니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
최대 결과 수입니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 페이지의 결과를 가져오는 데 사용되는 매개변수입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorGetRepostsResponse`

<div id="hide_reply">
  ### `hide_reply`
</div>

답글 숨기기
인증된 사용자가 소유한 대화의 답글을 숨기거나 숨김을 해제합니다.

#### 매개변수

<ParamField path="path.tweet_id" type="Any">
숨기거나 숨김 해제하려는 답글의 ID입니다.
</ParamField>

<ParamField path="body.body" type="HideReplyRequest">
요청 본문
</ParamField>

<div id="search_all">
  ### `search_all`
</div>

모든 포스트 검색
검색어와 일치하는 포스트를 전체 아카이브에서 가져옵니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="str">
포스트를 매칭하기 위한 하나의 쿼리/규칙/필터입니다. 최대 쿼리 길이는 [https://t.co/rulelength](https://t.co/rulelength)를 참고하세요.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 포스트를 제공하기 시작할 가장 오래된 UTC 타임스탬프입니다. 타임스탬프는 초 단위까지 표시되며, 포함입니다(예: 12:00:01은 해당 분의 첫 번째 초를 포함합니다).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. 포스트를 제공할 가장 최신 UTC 타임스탬프입니다. 타임스탬프는 초 단위까지 표시되며, 제외입니다(예: 12:00:01은 해당 분의 첫 번째 초를 제외합니다).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
지정된 ID보다 큰(즉, 더 최신) 포스트 ID를 가진 결과를 반환합니다.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
지정된 ID보다 작은(즉, 더 오래된) 포스트 ID를 가진 결과를 반환합니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
요청에서 반환할 검색 결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
이 파라미터는 다음 "페이지"의 결과를 가져오는 데 사용됩니다. 이 파라미터에 사용할 값은 API가 제공하는 응답에서 직접 가져오며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
이 파라미터는 다음 "페이지"의 결과를 가져오는 데 사용됩니다. 이 파라미터에 사용할 값은 API가 제공하는 응답에서 직접 가져오며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
결과를 반환할 정렬 순서입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환값

`IteratorSearchAllResponse`

<div id="search_recent">
  ### `search_recent`
</div>

최근 포스트 검색
검색 쿼리와 일치하는 최근 7일 이내의 포스트를 조회합니다.

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="str">
포스트와 매칭하기 위한 하나의 쿼리/규칙/필터입니다. 최대 쿼리 길이를 확인하려면 [https://t.co/rulelength](https://t.co/rulelength)를 참고하세요.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ 형식입니다. 포스트가 제공되는 가장 오래된 UTC 타임스탬프입니다. 타임스탬프는 초 단위 정밀도를 가지며, 포함 범위입니다(예: 12:00:01은 해당 분의 첫 번째 초를 포함합니다).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ 형식입니다. 포스트가 제공되는 가장 최신 UTC 타임스탬프입니다. 타임스탬프는 초 단위 정밀도를 가지며, 제외 범위입니다(예: 12:00:01은 해당 분의 첫 번째 초를 제외합니다).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
지정된 id보다 큰(즉, 더 최근인) 포스트 id가 있는 결과를 반환합니다.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
지정된 id보다 작은(즉, 더 이전인) 포스트 id가 있는 결과를 반환합니다.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
요청에서 반환할 검색 결과의 최대 개수입니다.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
다음 '페이지'의 결과를 가져오는 데 사용되는 파라미터입니다. 이 파라미터에 사용되는 값은 API가 제공한 응답에서 직접 가져오며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
다음 '페이지'의 결과를 가져오는 데 사용되는 파라미터입니다. 이 파라미터에 사용되는 값은 API가 제공한 응답에서 직접 가져오며, 수정해서는 안 됩니다.
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
결과를 반환할 정렬 순서입니다.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
표시할 Tweet 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
확장할 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
표시할 Media 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
표시할 Poll 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
표시할 User 필드의 쉼표로 구분된 목록입니다.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
표시할 Place 필드의 쉼표로 구분된 목록입니다.
</ParamField>

#### 반환

`IteratorSearchRecentResponse`