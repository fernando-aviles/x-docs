---
title: 리스트 멤버 조회
sidebarTitle: 리스트 멤버 조회
description: 리스트 멤버 가져오기
keywords: ["리스트 멤버 조회 빠른 시작", "리스트 멤버 조회", "리스트 멤버 가져오기 빠른 시작", "리스트 멤버십 조회"]
---

import { Button } from '/snippets/ko/button.mdx';

이 가이드에서는 리스트 멤버를 조회하는 방법을 설명합니다.

<Note>
  **사전 준비 사항**

  시작하기 전에 다음이 필요합니다.

  * 승인된 App이 있는 [개발자 계정](https://developer.x.com/en/portal/petition/essential/basic-info)
  * App의 Bearer 토큰
</Note>

***

<div id="get-list-members">
  ## 리스트 멤버 가져오기
</div>

<Steps>
  <Step title="리스트 ID 찾기">
    리스트를 볼 때 URL에서 리스트 ID를 확인할 수 있습니다:

    ```
    https://x.com/i/lists/84839422
                          └── 이것이 리스트 ID입니다
    ```
  </Step>

  <Step title="리스트 멤버 조회 요청 보내기">
    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/lists/84839422/members?\
      user.fields=created_at,username,verified&\
      max_results=100" \
        -H "Authorization: Bearer $BEARER_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # 페이지네이션을 사용해 리스트 멤버를 가져옵니다
      for page in client.lists.get_members(
          "84839422",
          user_fields=["created_at", "username", "verified"],
          max_results=100
      ):
          for user in page.data:
              print(f"{user.username} - 가입일: {user.created_at}")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      // 페이지네이션을 사용해 리스트 멤버를 가져옵니다
      const paginator = client.lists.getMembers("84839422", {
        userFields: ["created_at", "username", "verified"],
        maxResults: 100,
      });

      for await (const page of paginator) {
        page.data?.forEach((user) => {
          console.log(`${user.username} - 가입일: ${user.created_at}`);
        });
      }
      ```
    </CodeGroup>
  </Step>

  <Step title="응답 확인하기">
    ```json
    {
      "data": [
        {
          "id": "1319036828964454402",
          "name": "Birdwatch",
          "username": "birdwatch",
          "created_at": "2020-10-21T22:04:47.000Z",
          "verified": true
        },
        {
          "id": "1065249714214457345",
          "name": "Spaces",
          "username": "TwitterSpaces",
          "created_at": "2018-11-21T14:24:58.000Z",
          "verified": true
        }
      ],
      "meta": {
        "result_count": 2,
        "next_token": "5349804505549807616"
      }
    }
    ```
  </Step>
</Steps>

***

<div id="include-additional-data">
  ## 추가 데이터 포함하기
</div>

expansions를 사용해 고정된 게시물과 같은 연관 데이터를 함께 가져올 수 있습니다:

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/lists/84839422/members?\
  user.fields=created_at&\
  expansions=pinned_tweet_id&\
  tweet.fields=created_at" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # expansions를 사용해 리스트 멤버 가져오기
  for page in client.lists.get_members(
      "84839422",
      user_fields=["created_at"],
      expansions=["pinned_tweet_id"],
      tweet_fields=["created_at"]
  ):
      for user in page.data:
          print(f"{user.username}")
      # 고정된 게시물은 page.includes.tweets에 포함됩니다
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // expansions를 사용해 리스트 멤버 가져오기
  const paginator = client.lists.getMembers("84839422", {
    userFields: ["created_at"],
    expansions: ["pinned_tweet_id"],
    tweetFields: ["created_at"],
  });

  for await (const page of paginator) {
    page.data?.forEach((user) => {
      console.log(user.username);
    });
    // 고정된 게시물은 page.includes?.tweets에 포함됩니다
  }
  ```
</CodeGroup>

***

<div id="next-steps">
  ## 다음 단계
</div>

<CardGroup cols={2}>
  <Card title="리스트 멤버 관리" icon="user-plus" href="/ko/x-api/lists/list-members/quickstart/manage-list-members">
    멤버 추가 및 제거
  </Card>

  <Card title="리스트 조회" icon="list" href="/ko/x-api/lists/list-lookup/quickstart">
    리스트 세부 정보 조회
  </Card>

  <Card title="API 참조 문서" icon="code" href="/ko/x-api/lists/get-list-members">
    엔드포인트 전체 문서
  </Card>
</CardGroup>