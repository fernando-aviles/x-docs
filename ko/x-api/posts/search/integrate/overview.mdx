---
title: 통합 가이드
sidebarTitle: 개요
description: Search Posts 통합을 위한 핵심 개념과 모범 사례
keywords: ["검색 통합", "검색 개요", "검색 가이드", "검색 구현", "포스트 검색 통합"]
---

이 가이드는 애플리케이션에 Search Posts 엔드포인트를 통합하는 데 필요한 핵심 개념을 설명합니다.

***

<div id="authentication">
  ## 인증
</div>

<div id="recent-search">
  ### 최근 검색
</div>

최근 검색은 여러 인증 방식을 지원합니다:

| Method | Use case |
|:-------|:---------|
| [OAuth 2.0 App-Only](/ko/resources/fundamentals/authentication#oauth-2-0) | 공개 포스트 데이터 |
| [OAuth 2.0 Authorization Code with PKCE](/ko/resources/fundamentals/authentication#oauth-2-0-authorization-code-flow-with-pkce-2) | 비공개 지표 |
| [OAuth 1.0a User Context](/ko/resources/fundamentals/authentication) | 비공개 지표 |

<div id="full-archive-search">
  ### 전체 아카이브 검색
</div>

전체 아카이브 검색은 [OAuth 2.0 앱 전용(App-Only)](/ko/resources/fundamentals/authentication#oauth-2-0) 인증만 지원합니다.

<Warning>
  `non_public_metrics`, `organic_metrics`, `promoted_metrics`와 같은 비공개 지표는 전체 아카이브 검색에서 사용할 수 없습니다. 이는 앱 전용 인증만 지원하기 때문입니다.
</Warning>

***

<div id="building-queries">
  ## 쿼리 작성
</div>

쿼리는 연산자를 사용해 포스트를 검색합니다. 연산자를 불리언 논리와 결합해 사용하세요:

```
(AI OR "machine learning") lang:en -is:retweet has:links
```

<div id="query-length-limits">
  ### 쿼리 길이 제한
</div>

| 액세스 수준 | 최근 검색 | 전체 아카이브 검색 |
|:-------------|:--------------|:--------------------|
| Self-serve | 512자 | 1,024자 |
| Enterprise | 4,096자 | 4,096자 |

<div id="operator-types">
  ### 연산자 유형
</div>

| 유형 | 설명 | 예시 |
|:-----|:------------|:--------|
| **단독** | 단독으로 사용 가능합니다 | `#python`, `from:user` |
| **연결 필수** | 단독 연산자와 함께 사용해야 합니다 | `has:media`, `is:retweet` |

<Card title="쿼리 만들기" icon="magnifying-glass" href="/ko/x-api/posts/search/integrate/build-a-query">
  쿼리 구문을 자세히 알아보기
</Card>

<Card title="연산자 참조" icon="list-check" href="/ko/x-api/posts/search/integrate/operators">
  사용 가능한 모든 연산자 보기
</Card>

***

<div id="fields-and-expansions">
  ## 필드와 expansions
</div>

기본적으로 응답에는 `id`, `text`, `edit_history_tweet_ids`만 포함됩니다. 추가 데이터를 요청하려면 매개변수를 사용하세요.

<div id="example-request">
  ### 요청 예시
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/tweets/search/recent?\
  query=python&\
  tweet.fields=created_at,public_metrics,author_id&\
  expansions=author_id,attachments.media_keys&\
  user.fields=username,verified&\
  media.fields=url,type" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # 최신 포스트 검색
  for page in client.posts.search_recent(
      query="python",
      tweet_fields=["created_at", "public_metrics", "author_id"],
      expansions=["author_id", "attachments.media_keys"],
      user_fields=["username", "verified"],
      media_fields=["url", "type"],
      max_results=100
  ):
      for post in page.data:
          print(f"{post.text} - Likes: {post.public_metrics.like_count}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // 최신 포스트 검색
  const paginator = client.posts.searchRecent("python", {
    tweetFields: ["created_at", "public_metrics", "author_id"],
    expansions: ["author_id", "attachments.media_keys"],
    userFields: ["username", "verified"],
    mediaFields: ["url", "type"],
    maxResults: 100,
  });

  for await (const page of paginator) {
    page.data?.forEach((post) => {
      console.log(`${post.text} - Likes: ${post.public_metrics?.like_count}`);
    });
  }
  ```
</CodeGroup>

<div id="available-expansions">
  ### 사용 가능한 expansions
</div>

| Expansion | 반환 값 |
|:----------|:--------|
| `author_id` | 작성자의 사용자 객체 |
| `attachments.media_keys` | 첨부된 미디어 객체 |
| `attachments.poll_ids` | 첨부된 투표 객체 |
| `referenced_tweets.id` | 인용 또는 답글 대상 포스트 객체 |
| `geo.place_id` | 장소 객체 |
| `entities.mentions.username` | 멘션된 사용자 객체 |

<Card title="필드 및 expansions 가이드" icon="sliders" href="/ko/x-api/fundamentals/fields">
  응답을 사용자 지정하는 방법에 대해 더 알아보기
</Card>

***

<div id="pagination">
  ## 페이지네이션
</div>

검색 엔드포인트는 결과를 페이지별로 반환합니다. 추가 페이지를 가져오려면 응답의 `next_token`을 사용하세요.

<div id="how-it-works">
  ### 작동 방식
</div>

1. 초기 요청을 보낼 때 `max_results`를 포함합니다.
2. `meta` 객체에서 `next_token`을 확인합니다.
3. 이후 요청에 `next_token`을 포함합니다.
4. `next_token`이 더 이상 반환되지 않을 때까지 반복합니다.

<div id="example">
  ### 예제
</div>

<CodeGroup dropdown>
  ```bash cURL
  # 첫 번째 요청
  curl "https://api.x.com/2/tweets/search/recent?query=python&max_results=100" \
    -H "Authorization: Bearer $BEARER_TOKEN"

  # 페이지네이션 토큰을 포함한 다음 요청
  curl "https://api.x.com/2/tweets/search/recent?query=python&max_results=100&next_token=NEXT_TOKEN" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # SDK가 페이지네이션을 자동으로 처리합니다.
  all_posts = []

  for page in client.posts.search_recent(query="python", max_results=100):
      if page.data:
          all_posts.extend(page.data)

  print(f"{len(all_posts)}개의 포스트를 찾았습니다")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  async function getAllResults(query) {
    const allPosts = [];

    // SDK가 페이지네이션을 자동으로 처리합니다.
    const paginator = client.posts.searchRecent(query, { maxResults: 100 });

    for await (const page of paginator) {
      if (page.data) {
        allPosts.push(...page.data);
      }
    }

    return allPosts;
  }

  // 사용 예
  const posts = await getAllResults("python");
  console.log(`${posts.length}개의 포스트를 찾았습니다`);
  ```
</CodeGroup>

<Card title="페이지네이션 가이드" icon="arrow-right" href="/ko/x-api/posts/search/integrate/paginate">
  페이지네이션에 대해 자세히 알아보기
</Card>

***

<div id="post-edits">
  ## 포스트 수정
</div>

포스트는 30분 이내에 최대 5회까지 수정할 수 있습니다. 검색 엔드포인트는 항상 최신 버전을 반환합니다.

<div id="considerations">
  ### 고려사항
</div>

* `edit_history_tweet_ids`에는 모든 포스트 ID가 포함됩니다(가장 오래된 것부터).
* 30분 편집 가능 시간 이후에 가져온 포스트는 최종 버전을 나타냅니다.
* 실시간에 가까운 사용 사례에서는 최근에 게시된 포스트가 여전히 수정될 수 있습니다.

<Card title="포스트 편집 기본 사항" icon="clock-rotate-left" href="/ko/x-api/fundamentals/edit-posts">
  포스트 편집에 대해 더 알아보기
</Card>

***

<div id="best-practices">
  ## 모범 사례
</div>

<CardGroup cols={2}>
  <Card title="구체적으로 시작" icon="crosshairs">
    여러 연산자를 함께 사용해 결과 범위를 좁히고 불필요한 결과를 줄이세요.
  </Card>

  <Card title="반복적으로 테스트" icon="flask">
    처음에는 광범위하게 시작한 뒤, 결과를 바탕으로 점진적으로 정제하세요.
  </Card>

  <Card title="페이지네이션 처리" icon="arrow-right">
    대규모 결과 집합에 대해 적절한 페이지네이션을 구현하세요.
  </Card>

  <Card title="결과 캐싱" icon="database">
    반복 요청을 피하기 위해 결과를 로컬에 저장하세요.
  </Card>
</CardGroup>

***

<div id="next-steps">
  ## 다음 단계
</div>

<CardGroup cols={2}>
  <Card title="쿼리 작성" icon="magnifying-glass" href="/ko/x-api/posts/search/integrate/build-a-query">
    쿼리 구문 익히기
  </Card>

  <Card title="연산자 레퍼런스" icon="list-check" href="/ko/x-api/posts/search/integrate/operators">
    사용 가능한 모든 연산자
  </Card>

  <Card title="페이지네이션" icon="arrow-right" href="/ko/x-api/posts/search/integrate/paginate">
    대량 결과 처리
  </Card>

  <Card title="API 레퍼런스" icon="code" href="/ko/x-api/posts/recent-search">
    전체 엔드포인트 문서
  </Card>
</CardGroup>