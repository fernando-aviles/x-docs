---
title: 빠른 시작
sidebarTitle: 빠른 시작
description: 키워드로 Spaces 검색
keywords: ["Spaces 검색 빠른 시작", "Spaces 검색 튜토리얼", "Spaces 찾기 가이드"]
---

import { Button } from '/snippets/ko/button.mdx';

이 가이드는 키워드로 Spaces를 검색하는 방법을 설명합니다.

<Note>
  **사전 준비 사항**

  시작하기 전에 다음이 필요합니다.

  * 승인된 App이 있는 [개발자 계정](https://developer.x.com/en/portal/petition/essential/basic-info)
  * App의 Bearer Token
</Note>

***

<div id="search-for-spaces">
  ## 스페이스 검색
</div>

키워드와 일치하는 스페이스를 검색하려면:

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/spaces/search?\
  query=AI&\
  space.fields=title,host_ids,participant_count,state&\
  state=live" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # 스페이스 검색
  response = client.spaces.search(
      query="AI",
      space_fields=["title", "host_ids", "participant_count", "state"],
      state="live"
  )

  for space in response.data:
      print(f"{space.title} - {space.participant_count} participants")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // 스페이스 검색
  const response = await client.spaces.search({
    query: "AI",
    spaceFields: ["title", "host_ids", "participant_count", "state"],
    state: "live",
  });

  response.data?.forEach((space) => {
    console.log(`${space.title} - ${space.participant_count} participants`);
  });
  ```
</CodeGroup>

<div id="response">
  ### 응답
</div>

```json
{
  "data": [
    {
      "id": "1DXxyRYNejbKM",
      "state": "live",
      "title": "Discussing AI and the Future",
      "host_ids": ["2244994945"],
      "participant_count": 245
    },
    {
      "id": "1YqJDqWYNQDGW",
      "state": "live",
      "title": "AI in Healthcare",
      "host_ids": ["783214"],
      "participant_count": 89
    }
  ],
  "meta": {
    "result_count": 2
  }
}
```

***

<div id="filter-by-state">
  ## 상태로 필터링하기
</div>

라이브 중이거나 예정된 스페이스만 검색합니다:

<div id="live-spaces-only">
  ### 라이브 중인 Space만
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/spaces/search?query=tech&state=live" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # 라이브 중인 Space만 검색
  response = client.spaces.search(query="tech", state="live")

  for space in response.data:
      print(f"LIVE: {space.title}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // 라이브 중인 Space만 검색
  const response = await client.spaces.search({
    query: "tech",
    state: "live",
  });

  response.data?.forEach((space) => {
    console.log(`LIVE: ${space.title}`);
  });
  ```
</CodeGroup>

<div id="scheduled-spaces-only">
  ### 예약된 Spaces만
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/spaces/search?query=tech&state=scheduled" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # 예약된 Spaces만 검색
  response = client.spaces.search(query="tech", state="scheduled")

  for space in response.data:
      print(f"SCHEDULED: {space.title}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // 예약된 Spaces만 검색
  const response = await client.spaces.search({
    query: "tech",
    state: "scheduled",
  });

  response.data?.forEach((space) => {
    console.log(`SCHEDULED: ${space.title}`);
  });
  ```
</CodeGroup>

***

<div id="include-host-information">
  ## 호스트 정보 포함
</div>

호스트 사용자 데이터를 확장합니다:

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/spaces/search?\
  query=AI&\
  space.fields=title,host_ids,state&\
  expansions=host_ids&\
  user.fields=username,verified" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # 호스트 정보와 함께 검색
  response = client.spaces.search(
      query="AI",
      space_fields=["title", "host_ids", "state"],
      expansions=["host_ids"],
      user_fields=["username", "verified"]
  )

  for space in response.data:
      print(f"{space.title}")
  # 호스트 정보는 response.includes.users에 포함됩니다
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // 호스트 정보와 함께 검색
  const response = await client.spaces.search({
    query: "AI",
    spaceFields: ["title", "host_ids", "state"],
    expansions: ["host_ids"],
    userFields: ["username", "verified"],
  });

  response.data?.forEach((space) => {
    console.log(space.title);
  });
  // 호스트 정보는 response.includes?.users에 포함됩니다
  ```
</CodeGroup>

***

<div id="common-parameters">
  ## 공통 파라미터
</div>

| Parameter | Description |
|:----------|:------------|
| `query` | 검색 쿼리(필수) |
| `state` | 필터 값: `live`, `scheduled`, 또는 `all` |
| `max_results` | 반환할 결과 수(1-100) |
| `space.fields` | 포함할 Space 필드 |
| `expansions` | 포함할 관련 객체 |
| `user.fields` | 포함할 User 필드 |

***

<div id="next-steps">
  ## 다음 단계
</div>

<CardGroup cols={2}>
  <Card title="Space 조회" icon="microphone" href="/ko/x-api/spaces/lookup/quickstart">
    ID로 Space 조회
  </Card>

  <Card title="API 레퍼런스" icon="code" href="/ko/x-api/spaces/search-spaces">
    전체 엔드포인트 문서
  </Card>
</CardGroup>