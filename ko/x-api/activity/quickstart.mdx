---
title: 빠른 시작
sidebarTitle: 빠른 시작
description: 활동 스트림 구독을 설정하고 이벤트를 수신합니다.
keywords: ["활동 빠른 시작", "활동 스트림 튜토리얼", "구독 가이드"]
---

import { Button } from '/snippets/ko/button.mdx';

이 가이드는 실시간 계정 활동 이벤트를 수신할 수 있도록 액티비티 스트림 구독을 설정하는 과정을 단계별로 안내합니다.

<Note>
  **필수 조건**

  시작하기 전에 다음이 필요합니다.

  * 앱 승인이 완료된 [개발자 계정](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 앱의 Bearer Token
</Note>

***

<Steps>
  <Step title="구독 생성하기" icon="bell">
    사용자의 활동 이벤트를 구독합니다.

    <Tabs>
      <Tab title="cURL">
        ```bash
        curl -X POST "https://api.x.com/2/activity/subscriptions" \
          -H "Authorization: Bearer $BEARER_TOKEN" \
          -H "Content-Type: application/json" \
          -d '{
            "user_id": "2244994945",
            "event_types": ["tweet_create_events", "favorite_events", "follow_events"]
          }'
        ```
      </Tab>

      <Tab title="Python">
        ```python
        import requests

        bearer_token = "YOUR_BEARER_TOKEN"

        url = "https://api.x.com/2/activity/subscriptions"
        headers = {
            "Authorization": f"Bearer {bearer_token}",
            "Content-Type": "application/json"
        }
        payload = {
            "user_id": "2244994945",
            "event_types": ["tweet_create_events", "favorite_events", "follow_events"]
        }

        response = requests.post(url, headers=headers, json=payload)
        print(response.json())
        ```
      </Tab>
    </Tabs>

    **응답:**

    ```json
    {
      "data": {
        "id": "1234567890",
        "user_id": "2244994945",
        "event_types": ["tweet_create_events", "favorite_events", "follow_events"],
        "created_at": "2024-01-15T10:00:00.000Z"
      }
    }
    ```
  </Step>

  <Step title="스트림에 연결하기" icon="plug">
    이벤트를 수신하기 위한 지속 연결을 엽니다.

    <Tabs>
      <Tab title="cURL">
        ```bash
        curl "https://api.x.com/2/activity/stream" \
          -H "Authorization: Bearer $BEARER_TOKEN"
        ```
      </Tab>

      <Tab title="Python">
        ```python
        import requests

        bearer_token = "YOUR_BEARER_TOKEN"

        url = "https://api.x.com/2/activity/stream"
        headers = {"Authorization": f"Bearer {bearer_token}"}

        response = requests.get(url, headers=headers, stream=True)

        for line in response.iter_lines():
            if line:
                print(line.decode("utf-8"))
        ```
      </Tab>
    </Tabs>
  </Step>

  <Step title="수신된 이벤트 처리하기" icon="message">
    이벤트는 JSON 객체로 스트리밍됩니다.

    ```json
    {
      "for_user_id": "2244994945",
      "event_type": "tweet_create_events",
      "created_at": "2024-01-15T10:30:00.000Z",
      "tweet_create_events": [
        {
          "id": "1234567890",
          "text": "Hello from the stream!",
          "author_id": "2244994945"
        }
      ]
    }
    ```
  </Step>
</Steps>

***

<div id="available-event-types">
  ## 사용 가능한 이벤트 유형
</div>

| Event | Description |
|:------|:------------|
| `tweet_create_events` | 사용자가 새 포스트를 작성합니다 |
| `favorite_events` | 사용자가 포스트를 좋아합니다 |
| `follow_events` | 사용자가 다른 사용자를 팔로우하거나 팔로우됩니다 |
| `direct_message_events` | 사용자가 DM을 보내거나 받습니다 |
| `block_events` | 사용자가 사용자를 차단하거나 차단 해제합니다 |
| `mute_events` | 사용자가 사용자를 뮤트하거나 뮤트 해제합니다 |

***

<div id="manage-subscriptions">
  ## 구독 관리
</div>

<AccordionGroup>
  <Accordion title="구독 목록 조회">
    모든 활성 구독을 조회합니다:

    ```bash
    curl "https://api.x.com/2/activity/subscriptions" \
      -H "Authorization: Bearer $BEARER_TOKEN"
    ```
  </Accordion>

  <Accordion title="구독 업데이트">
    구독의 이벤트 유형을 수정합니다:

    ```bash
    curl -X PUT "https://api.x.com/2/activity/subscriptions/1234567890" \
      -H "Authorization: Bearer $BEARER_TOKEN" \
      -H "Content-Type: application/json" \
      -d '{
        "event_types": ["tweet_create_events", "favorite_events"]
      }'
    ```
  </Accordion>

  <Accordion title="구독 삭제">
    구독을 삭제합니다:

    ```bash
    curl -X DELETE "https://api.x.com/2/activity/subscriptions/1234567890" \
      -H "Authorization: Bearer $BEARER_TOKEN"
    ```
  </Accordion>
</AccordionGroup>

***

<div id="next-steps">
  ## 다음 단계
</div>

<CardGroup cols={2}>
  <Card title="Account Activity API" icon="bell" href="/ko/x-api/account-activity/introduction">
    Webhook 기반 대안
  </Card>

  <Card title="API 레퍼런스" icon="code" href="/ko/x-api/activity/activity-stream">
    전체 엔드포인트 문서
  </Card>
</CardGroup>