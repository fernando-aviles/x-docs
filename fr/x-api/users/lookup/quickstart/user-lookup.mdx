---
title: "Démarrage rapide : recherche d’utilisateurs"
sidebarTitle: "Recherche d’utilisateurs"
description: "Rechercher des utilisateurs par ID ou par nom d’utilisateur"
keywords: ["démarrage rapide recherche d’utilisateurs", "tutoriel récupération d’utilisateur", "guide utilisateur par ID", "utilisateur par nom d’utilisateur"]
---

import { Button } from '/snippets/fr/button.mdx';

Ce guide vous explique comment rechercher des utilisateurs par leur identifiant ou leur nom d’utilisateur.

<Note>
  **Prérequis**

  Avant de commencer, vous aurez besoin de :

  * Un [compte développeur](https://developer.x.com/en/portal/petition/essential/basic-info) avec une application approuvée
  * Le Bearer Token de votre application
</Note>

***

<div id="look-up-by-id">
  ## Recherche par identifiant
</div>

### Utilisateur unique

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/2244994945?\
  user.fields=created_at,description,verified,public_metrics" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Récupérer un utilisateur par identifiant
  response = client.users.get(
      "2244994945",
      user_fields=["created_at", "description", "verified", "public_metrics"]
  )

  print(f"Nom : {response.data.name}")
  print(f"Nom d'utilisateur : {response.data.username}")
  print(f"Abonnés : {response.data.public_metrics.followers_count}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Récupérer un utilisateur par identifiant
  const response = await client.users.get("2244994945", {
    userFields: ["created_at", "description", "verified", "public_metrics"],
  });

  console.log(`Nom : ${response.data?.name}`);
  console.log(`Nom d'utilisateur : ${response.data?.username}`);
  console.log(`Abonnés : ${response.data?.public_metrics?.followers_count}`);
  ```
</CodeGroup>

<div id="response">
  ### Réponse
</div>

```json
{
  "data": {
    "id": "2244994945",
    "name": "X Developers",
    "username": "XDevelopers",
    "created_at": "2013-12-14T04:35:55.000Z",
    "description": "The voice of the X developer community",
    "verified": true,
    "public_metrics": {
      "followers_count": 583423,
      "following_count": 2048,
      "tweet_count": 14052,
      "listed_count": 1672
    }
  }
}
```

<div id="multiple-users">
  ### Plusieurs utilisateurs
</div>

Recherchez jusqu’à 100 utilisateurs à la fois :

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users?\
  ids=2244994945,783214,6253282&\
  user.fields=username,verified" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Récupérer plusieurs utilisateurs par ID
  response = client.users.get_users(
      ids=["2244994945", "783214", "6253282"],
      user_fields=["username", "verified"]
  )

  for user in response.data:
      print(f"{user.username} - Verified: {user.verified}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Récupérer plusieurs utilisateurs par ID
  const response = await client.users.getUsers({
    ids: ["2244994945", "783214", "6253282"],
    userFields: ["username", "verified"],
  });

  response.data?.forEach((user) => {
    console.log(`${user.username} - Verified: ${user.verified}`);
  });
  ```
</CodeGroup>

***

<div id="look-up-by-username">
  ## Rechercher par nom d’utilisateur
</div>

<div id="single-user">
  ### Un seul utilisateur
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/by/username/XDevelopers?\
  user.fields=created_at,description,verified" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Récupérer un utilisateur par nom d'utilisateur
  response = client.users.get_by_username(
      "XDevelopers",
      user_fields=["created_at", "description", "verified"]
  )

  print(f"ID: {response.data.id}")
  print(f"Nom : {response.data.name}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Récupérer un utilisateur par nom d'utilisateur
  const response = await client.users.getByUsername("XDevelopers", {
    userFields: ["created_at", "description", "verified"],
  });

  console.log(`ID: ${response.data?.id}`);
  console.log(`Nom : ${response.data?.name}`);
  ```
</CodeGroup>

<div id="multiple-users">
  ### Plusieurs utilisateurs
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/by?\
  usernames=XDevelopers,X,elonmusk&\
  user.fields=created_at,verified" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Récupérer plusieurs utilisateurs par leurs noms d’utilisateur
  response = client.users.get_users_by_usernames(
      usernames=["XDevelopers", "X", "elonmusk"],
      user_fields=["created_at", "verified"]
  )

  for user in response.data:
      print(f"{user.username} - {user.created_at}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Récupérer plusieurs utilisateurs par leurs noms d’utilisateur
  const response = await client.users.getUsersByUsernames({
    usernames: ["XDevelopers", "X", "elonmusk"],
    userFields: ["created_at", "verified"],
  });

  response.data?.forEach((user) => {
    console.log(`${user.username} - ${user.created_at}`);
  });
  ```
</CodeGroup>

***

<div id="available-fields">
  ## Champs disponibles
</div>

| Champ | Description |
|:------|:------------|
| `created_at` | Date de création du compte |
| `description` | Biographie de l&#39;utilisateur |
| `profile_image_url` | URL de l&#39;avatar |
| `verified` | Statut de vérification du compte |
| `public_metrics` | Nombre d&#39;abonnés/abonnements |
| `location` | Emplacement défini par l&#39;utilisateur |
| `url` | Site web de l&#39;utilisateur |
| `protected` | Statut de compte protégé |
| `pinned_tweet_id` | ID de la Publication épinglée |

***

<div id="handle-errors">
  ## Gérer les erreurs
</div>

<div id="user-not-found">
  ### Utilisateur introuvable
</div>

```json
{
  "errors": [
    {
      "resource_type": "user",
      "title": "Not Found Error",
      "detail": "Could not find user with username: [nonexistent_user]."
    }
  ]
}
```

<div id="protected-user">
  ### Utilisateur protégé
</div>

Les données des utilisateurs protégés sont toujours renvoyées, mais vous ne pourrez pas accéder à leurs publications sauf si vous les suivez.

***

<div id="next-steps">
  ## Prochaines étapes
</div>

<CardGroup cols={2}>
  <Card title="Utilisateur authentifié" icon="user-check" href="/fr/x-api/users/lookup/quickstart/authenticated-lookup">
    Obtenir l’utilisateur actuel
  </Card>

  <Card title="Guide d’intégration" icon="book" href="/fr/x-api/users/lookup/integrate">
    Concepts clés et bonnes pratiques
  </Card>

  <Card title="Référence de l’API" icon="code" href="/fr/x-api/users/single-user-lookup">
    Documentation complète de l’endpoint
  </Card>
</CardGroup>