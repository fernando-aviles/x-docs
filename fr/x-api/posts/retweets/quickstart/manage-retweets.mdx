---
title: Gérer les Retweets
sidebarTitle: Gérer les Retweets
description: Retweeter et annuler des Retweets avec l’API X
keywords: ["gérer les retweets", "retweeter des publications", "annuler un retweet", "guide de démarrage rapide des retweets", "tutoriel retweet", "guide retweet"]
---

import { Button } from '/snippets/fr/button.mdx';

Ce guide vous explique comment effectuer et annuler des Retweets à l’aide de l’API X.

<Note>
  **Prérequis**

  Avant de commencer, vous aurez besoin :

  * D’un [compte développeur](https://developer.x.com/en/portal/petition/essential/basic-info) avec une application approuvée
  * D’un User Access Token (OAuth 1.0a ou OAuth 2.0 PKCE)
</Note>

***

<div id="retweet-a-post">
  ## Retweeter une publication
</div>

<Steps>
  <Step title="Obtenir votre identifiant utilisateur">
    Vous avez besoin de l’identifiant de votre utilisateur authentifié. Vous pouvez le trouver à l’aide du [user lookup endpoint](/fr/x-api/users/lookup/introduction) ou dans votre Access Token (la partie numérique correspond à votre identifiant utilisateur).
  </Step>

  <Step title="Obtenir l’identifiant de la publication">
    Recherchez l’identifiant de la publication dans l’URL lorsque vous affichez une publication :

    ```
    https://x.com/XDevelopers/status/1228393702244134912
                                    └── C’est l’identifiant de la publication
    ```
  </Step>

  <Step title="Envoyer la requête de retweet">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/users/123456789/retweets" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"tweet_id": "1228393702244134912"}'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # Retweet a Post
      response = client.posts.retweet(
          user_id="123456789",
          tweet_id="1228393702244134912"
      )

      print(f"Retweeted: {response.data.retweeted}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // Retweet a Post
      const response = await client.posts.retweet("123456789", {
        tweetId: "1228393702244134912",
      });

      console.log(`Retweeted: ${response.data?.retweeted}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="Examiner la réponse">
    ```json
    {
      "data": {
        "retweeted": true
      }
    }
    ```
  </Step>
</Steps>

***

<div id="undo-a-retweet">
  ## Annuler un Retweet
</div>

Annuler un Retweet :

<CodeGroup dropdown>
  ```bash cURL
  curl -X DELETE "https://api.x.com/2/users/123456789/retweets/1228393702244134912" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client
  from xdk.oauth1_auth import OAuth1

  oauth1 = OAuth1(
      api_key="YOUR_API_KEY",
      api_secret="YOUR_API_SECRET",
      access_token="YOUR_ACCESS_TOKEN",
      access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
  )

  client = Client(auth=oauth1)

  # Annuler un Retweet
  response = client.posts.unretweet(
      user_id="123456789",
      tweet_id="1228393702244134912"
  )

  print(f"Retweeted: {response.data.retweeted}")
  ```

  ```javascript JavaScript SDK
  import { Client, OAuth1 } from "@xdevplatform/xdk";

  const oauth1 = new OAuth1({
    apiKey: "YOUR_API_KEY",
    apiSecret: "YOUR_API_SECRET",
    accessToken: "YOUR_ACCESS_TOKEN",
    accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
  });

  const client = new Client({ oauth1 });

  // Annuler un Retweet
  const response = await client.posts.unretweet("123456789", "1228393702244134912");

  console.log(`Retweeted: ${response.data?.retweeted}`);
  ```
</CodeGroup>

**Réponse :**

```json
{
  "data": {
    "retweeted": false
  }
}
```

***

<div id="next-steps">
  ## Étapes suivantes
</div>

<CardGroup cols={2}>
  <Card title="Recherche de Retweets" icon="retweet" href="/fr/x-api/posts/retweets/quickstart/retweets-lookup">
    Obtenir la liste des utilisateurs qui ont retweeté une Publication
  </Card>

  <Card title="Publications citées" icon="quote-left" href="/fr/x-api/posts/quote-tweets/quickstart">
    Obtenir les Publications citées
  </Card>

  <Card title="Référence de l’API" icon="code" href="/fr/x-api/posts/retweet-a-post">
    Documentation complète de l’endpoint
  </Card>
</CardGroup>