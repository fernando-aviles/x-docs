---
title: Gérer les mentions J’aime
sidebarTitle: Gérer les mentions J’aime
description: Aimer et retirer des mentions J’aime sur des publications avec X API
keywords: ["gérer les mentions J’aime", "aimer des publications", "retirer une mention J’aime", "prise en main rapide des mentions J’aime", "tutoriel sur les mentions J’aime", "guide des mentions J’aime"]
---

import { Button } from '/snippets/fr/button.mdx';

Ce guide vous explique comment aimer et ne plus aimer des Publications à l&#39;aide de X API.

<Note>
  **Prérequis**

  Avant de commencer, vous aurez besoin :

  * d&#39;un [compte développeur](https://developer.x.com/en/portal/petition/essential/basic-info) avec une application approuvée
  * d&#39;un jeton d&#39;accès utilisateur (User Access Token) (OAuth 1.0a ou OAuth 2.0 PKCE)
</Note>

***

<div id="like-a-post">
  ## Aimer une Publication
</div>

<Steps>
  <Step title="Obtenir votre identifiant utilisateur">
    Vous avez besoin de l’identifiant de votre utilisateur authentifié. Vous pouvez le trouver à l’aide du [point de terminaison de recherche d’utilisateur](/fr/x-api/users/lookup/introduction) ou dans votre Access Token (la partie numérique correspond à votre identifiant utilisateur).
  </Step>

  <Step title="Obtenir l’identifiant de la Publication">
    Recherchez l’identifiant de la Publication dans l’URL lorsque vous affichez une Publication :

    ```
    https://x.com/XDevelopers/status/1228393702244134912
                                    └── Voici l’identifiant de la Publication
    ```
  </Step>

  <Step title="Envoyer la requête J’aime">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/users/123456789/likes" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"tweet_id": "1228393702244134912"}'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # Aimer une Publication
      response = client.posts.like(
          user_id="123456789",
          tweet_id="1228393702244134912"
      )

      print(f"Liked: {response.data.liked}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // Aimer une Publication
      const response = await client.posts.like("123456789", {
        tweetId: "1228393702244134912",
      });

      console.log(`Liked: ${response.data?.liked}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="Examiner la Réponse">
    ```json
    {
      "data": {
        "liked": true
      }
    }
    ```
  </Step>
</Steps>

***

<div id="unlike-a-post">
  ## Retirer un J’aime d’une publication
</div>

Supprimez un J’aime d’une publication :

<CodeGroup dropdown>
  ```bash cURL
  curl -X DELETE "https://api.x.com/2/users/123456789/likes/1228393702244134912" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client
  from xdk.oauth1_auth import OAuth1

  oauth1 = OAuth1(
      api_key="YOUR_API_KEY",
      api_secret="YOUR_API_SECRET",
      access_token="YOUR_ACCESS_TOKEN",
      access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
  )

  client = Client(auth=oauth1)

  # Retirer un J’aime d’une publication
  response = client.posts.unlike(
      user_id="123456789",
      tweet_id="1228393702244134912"
  )

  print(f"Liked: {response.data.liked}")
  ```

  ```javascript JavaScript SDK
  import { Client, OAuth1 } from "@xdevplatform/xdk";

  const oauth1 = new OAuth1({
    apiKey: "YOUR_API_KEY",
    apiSecret: "YOUR_API_SECRET",
    accessToken: "YOUR_ACCESS_TOKEN",
    accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
  });

  const client = new Client({ oauth1 });

  // Retirer un J’aime d’une publication
  const response = await client.posts.unlike("123456789", "1228393702244134912");

  console.log(`Liked: ${response.data?.liked}`);
  ```
</CodeGroup>

**Réponse :**

```json
{
  "data": {
    "liked": false
  }
}
```

***

<div id="next-steps">
  ## Prochaines étapes
</div>

<CardGroup cols={2}>
  <Card title="Recherche des mentions J’aime" icon="heart" href="/fr/x-api/posts/likes/quickstart/likes-lookup">
    Récupérer la liste des utilisateurs qui ont aimé une Publication
  </Card>

  <Card title="Référence de l’API" icon="code" href="/fr/x-api/posts/like-a-post">
    Documentation complète du point de terminaison
  </Card>
</CardGroup>