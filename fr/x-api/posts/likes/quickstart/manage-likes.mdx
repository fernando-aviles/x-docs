---
title: Gérer les mentions J’aime
sidebarTitle: Gérer les mentions J’aime
description: Ajouter et supprimer des mentions J’aime sur des Publications à l’aide de X API
keywords: ["gérer les mentions j’aime", "aimer des publications", "supprimer une mention j’aime", "démarrage rapide des mentions j’aime", "tutoriel sur les mentions j’aime", "guide des mentions j’aime"]
---

import { Button } from '/snippets/fr/button.mdx';

Ce guide vous explique comment ajouter et retirer un J’aime sur des Publications à l’aide de la X API.

<Note>
  **Prérequis**

  Avant de commencer, vous aurez besoin de :

  * d’un [compte développeur](https://developer.x.com/en/portal/petition/essential/basic-info) avec une App approuvée
  * d’un jeton d’accès utilisateur (User Access Token) (OAuth 1.0a ou OAuth 2.0 PKCE)
</Note>

***

<div id="like-a-post">
  ## Ajouter un J’aime à une Publication
</div>

<Steps>
  <Step title="Obtenir votre ID utilisateur">
    Vous avez besoin de l’ID de l’utilisateur authentifié. Vous pouvez l’obtenir en utilisant l’[endpoint de recherche d’utilisateurs](/fr/x-api/users/lookup/introduction) ou à partir de votre Access Token (la partie numérique correspond à votre ID utilisateur).
  </Step>

  <Step title="Obtenir l’ID de la Publication">
    Trouvez l’ID de la Publication dans l’URL lorsque vous affichez une Publication :

    ```
    https://x.com/XDevelopers/status/1228393702244134912
                                    └── Ceci est l’ID de la Publication
    ```
  </Step>

  <Step title="Envoyer la requête de like">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/users/123456789/likes" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"tweet_id": "1228393702244134912"}'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # Ajouter un J’aime à une Publication
      response = client.posts.like(
          user_id="123456789",
          tweet_id="1228393702244134912"
      )

      print(f"Liked: {response.data.liked}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // Ajouter un J’aime à une Publication
      const response = await client.posts.like("123456789", {
        tweetId: "1228393702244134912",
      });

      console.log(`Liked: ${response.data?.liked}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="Examiner la réponse">
    ```json
    {
      "data": {
        "liked": true
      }
    }
    ```
  </Step>
</Steps>

***

<div id="unlike-a-post">
  ## Retirer un J’aime d’une Publication
</div>

Supprimez un J’aime sur une Publication :

<CodeGroup dropdown>
  ```bash cURL
  curl -X DELETE "https://api.x.com/2/users/123456789/likes/1228393702244134912" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client
  from xdk.oauth1_auth import OAuth1

  oauth1 = OAuth1(
      api_key="YOUR_API_KEY",
      api_secret="YOUR_API_SECRET",
      access_token="YOUR_ACCESS_TOKEN",
      access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
  )

  client = Client(auth=oauth1)

  # Retirer un J’aime d’une Publication
  response = client.posts.unlike(
      user_id="123456789",
      tweet_id="1228393702244134912"
  )

  print(f"Liked: {response.data.liked}")
  ```

  ```javascript JavaScript SDK
  import { Client, OAuth1 } from "@xdevplatform/xdk";

  const oauth1 = new OAuth1({
    apiKey: "YOUR_API_KEY",
    apiSecret: "YOUR_API_SECRET",
    accessToken: "YOUR_ACCESS_TOKEN",
    accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
  });

  const client = new Client({ oauth1 });

  // Retirer un J’aime d’une Publication
  const response = await client.posts.unlike("123456789", "1228393702244134912");

  console.log(`Liked: ${response.data?.liked}`);
  ```
</CodeGroup>

**Réponse :**

```json
{
  "data": {
    "liked": false
  }
}
```

***

<div id="next-steps">
  ## Étapes suivantes
</div>

<CardGroup cols={2}>
  <Card title="Recherche des likes" icon="heart" href="/fr/x-api/posts/likes/quickstart/likes-lookup">
    Obtenir la liste des utilisateurs qui ont aimé une Publication
  </Card>

  <Card title="Référence de l’API" icon="code" href="/fr/x-api/posts/like-a-post">
    Documentation complète de l’endpoint
  </Card>
</CardGroup>