---
title: Recherche de likes
sidebarTitle: Recherche de likes
description: Récupérer les utilisateurs qui ont aimé une Publication et les Publications qu’un utilisateur a aimées
keywords: ["recherche de likes", "démarrage rapide likes", "obtenir les likes", "tutoriel likes", "guide likes", "exemple likes"]
---

import { Button } from '/snippets/fr/button.mdx';

Ce guide vous explique comment récupérer les données de mentions J’aime en utilisant X API.

<Note>
  **Prérequis**

  Avant de commencer, vous aurez besoin de :

  * Un [compte développeur](https://developer.x.com/en/portal/petition/essential/basic-info) avec une App approuvée
  * Le Bearer Token de votre App (pour les données publiques) ou un User Access Token (pour les métriques privées)
</Note>

***

<div id="get-users-who-liked-a-post">
  ## Récupérer les utilisateurs qui ont aimé une Publication
</div>

Récupérez la liste des utilisateurs qui ont aimé une Publication donnée :

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/tweets/1354143047324299264/liking_users?\
  user.fields=created_at,username,verified" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Obtenir les utilisateurs qui ont aimé une Publication avec pagination
  for page in client.posts.get_liking_users(
      "1354143047324299264",
      user_fields=["created_at", "username", "verified"]
  ):
      for user in page.data:
          print(f"{user.username} - Date d'inscription : {user.created_at}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Obtenir les utilisateurs qui ont aimé une Publication avec pagination
  const paginator = client.posts.getLikingUsers("1354143047324299264", {
    userFields: ["created_at", "username", "verified"],
  });

  for await (const page of paginator) {
    page.data?.forEach((user) => {
      console.log(`${user.username} - Date d'inscription : ${user.created_at}`);
    });
  }
  ```
</CodeGroup>

<div id="response">
  ### Réponse
</div>

```json
{
  "data": [
    {
      "created_at": "2008-12-04T18:51:57.000Z",
      "id": "17874544",
      "username": "TwitterSupport",
      "name": "Twitter Support",
      "verified": true
    },
    {
      "created_at": "2007-02-20T14:35:54.000Z",
      "id": "783214",
      "username": "Twitter",
      "name": "Twitter",
      "verified": true
    }
  ],
  "meta": {
    "result_count": 2,
    "next_token": "7140dibdnow9c7btw3z2vwioavpvutgzrzm9icis4ndix"
  }
}
```

***

<div id="get-a-users-liked-posts">
  ## Obtenir les Publications aimées d’un utilisateur
</div>

Récupérez les Publications aimées par un utilisateur donné :

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/2244994945/liked_tweets?\
  tweet.fields=created_at,public_metrics&\
  max_results=10" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Obtenir les Publications aimées d’un utilisateur avec pagination
  for page in client.users.get_liked_tweets(
      "2244994945",
      tweet_fields=["created_at", "public_metrics"],
      max_results=10
  ):
      for post in page.data:
          print(f"{post.text[:50]}... - Likes: {post.public_metrics.like_count}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Obtenir les Publications aimées d’un utilisateur avec pagination
  const paginator = client.users.getLikedTweets("2244994945", {
    tweetFields: ["created_at", "public_metrics"],
    maxResults: 10,
  });

  for await (const page of paginator) {
    page.data?.forEach((post) => {
      console.log(`${post.text?.slice(0, 50)}... - Likes: ${post.public_metrics?.like_count}`);
    });
  }
  ```
</CodeGroup>

<div id="response">
  ### Réponse
</div>

```json
{
  "data": [
    {
      "id": "1362449997430542337",
      "text": "Honored to be the first developer to be featured...",
      "created_at": "2021-02-18T17:45:00.000Z",
      "public_metrics": {
        "retweet_count": 5,
        "reply_count": 2,
        "like_count": 42,
        "quote_count": 1
      }
    }
  ],
  "meta": {
    "result_count": 1,
    "next_token": "7140dibdnow9c7btw4539n0vybdnx19ylpayqf16fjt4l"
  }
}
```

***

<div id="include-additional-data">
  ## Inclure des données supplémentaires
</div>

Utilisez le paramètre expansions pour obtenir des données associées, comme les Publications épinglées :

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/tweets/1354143047324299264/liking_users?\
  user.fields=created_at&\
  expansions=pinned_tweet_id&\
  tweet.fields=created_at" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Récupérer les utilisateurs ayant aimé avec l'expansion de Publication épinglée
  for page in client.posts.get_liking_users(
      "1354143047324299264",
      user_fields=["created_at"],
      expansions=["pinned_tweet_id"],
      tweet_fields=["created_at"]
  ):
      for user in page.data:
          print(f"{user.username}")
      # Les Publications épinglées se trouvent dans page.includes.tweets
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Récupérer les utilisateurs ayant aimé avec l'expansion de Publication épinglée
  const paginator = client.posts.getLikingUsers("1354143047324299264", {
    userFields: ["created_at"],
    expansions: ["pinned_tweet_id"],
    tweetFields: ["created_at"],
  });

  for await (const page of paginator) {
    page.data?.forEach((user) => {
      console.log(user.username);
    });
    // Les Publications épinglées se trouvent dans page.includes?.tweets
  }
  ```
</CodeGroup>

***

<div id="next-steps">
  ## Prochaines étapes
</div>

<CardGroup cols={2}>
  <Card title="Gérer les J’aime" icon="heart" href="/fr/x-api/posts/likes/quickstart/manage-likes">
    Aimer et retirer des J’aime sur des Publications
  </Card>

  <Card title="Référence de l’API" icon="code" href="/fr/x-api/posts/get-liking-users">
    Documentation complète du point de terminaison
  </Card>
</CardGroup>