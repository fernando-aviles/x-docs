---
title: Démarrage rapide
sidebarTitle: Démarrage rapide
description: Rechercher des listes par id ou par propriétaire
keywords: ["démarrage rapide pour la recherche de listes", "tutoriel pour obtenir une liste", "guide de recherche de listes"]
---

import { Button } from '/snippets/fr/button.mdx';

Ce guide vous explique comment récupérer les informations d’une List à l’aide des endpoints de recherche de List.

<Note>
  **Prérequis**

  Avant de commencer, vous aurez besoin de :

  * D’un [compte développeur](https://developer.x.com/en/portal/petition/essential/basic-info) avec une App approuvée
  * Du Bearer Token de votre App
</Note>

***

<div id="get-a-list-by-id">
  ## Obtenir une liste par ID
</div>

Récupérer les détails d&#39;une liste spécifique :

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/lists/1234567890?\
  list.fields=description,owner_id,member_count,follower_count,private,created_at" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python SDK Python
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Obtenir une liste par ID
  response = client.lists.get(
      "1234567890",
      list_fields=["description", "owner_id", "member_count", "follower_count", "private", "created_at"]
  )

  print(f"Liste: {response.data.name}")
  print(f"Membres: {response.data.member_count}")
  ```

  ```javascript SDK JavaScript
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Obtenir une liste par ID
  const response = await client.lists.get("1234567890", {
    listFields: ["description", "owner_id", "member_count", "follower_count", "private", "created_at"],
  });

  console.log(`Liste: ${response.data?.name}`);
  console.log(`Membres: ${response.data?.member_count}`);
  ```
</CodeGroup>

<div id="response">
  ### Réponse
</div>

```json
{
  "data": {
    "id": "1234567890",
    "name": "Tech News",
    "description": "Principaux journalistes et publications tech",
    "owner_id": "2244994945",
    "private": false,
    "member_count": 50,
    "follower_count": 1250,
    "created_at": "2023-01-15T10:00:00.000Z"
  }
}
```

***

<div id="get-lists-owned-by-a-user">
  ## Récupérer les Listes détenues par un utilisateur
</div>

Récupérez toutes les Listes détenues par un utilisateur spécifique :

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/2244994945/owned_lists?\
  list.fields=description,member_count,follower_count&\
  max_results=100" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Récupérer les Listes détenues par un utilisateur avec pagination
  for page in client.lists.get_owned_lists(
      "2244994945",
      list_fields=["description", "member_count", "follower_count"],
      max_results=100
  ):
      for lst in page.data:
          print(f"{lst.name} - {lst.member_count} members")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Récupérer les Listes détenues par un utilisateur avec pagination
  const paginator = client.lists.getOwnedLists("2244994945", {
    listFields: ["description", "member_count", "follower_count"],
    maxResults: 100,
  });

  for await (const page of paginator) {
    page.data?.forEach((lst) => {
      console.log(`${lst.name} - ${lst.member_count} members`);
    });
  }
  ```
</CodeGroup>

<div id="response">
  ### Réponse
</div>

```json
{
  "data": [
    {
      "id": "1234567890",
      "name": "Tech News",
      "description": "Top tech journalists",
      "member_count": 50,
      "follower_count": 1250
    },
    {
      "id": "9876543210",
      "name": "Developer Tools",
      "description": "Useful tools for developers",
      "member_count": 25,
      "follower_count": 500
    }
  ],
  "meta": {
    "result_count": 2
  }
}
```

***

<div id="include-owner-information">
  ## Inclure les informations sur le propriétaire
</div>

Étendez les données utilisateur du propriétaire :

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/lists/1234567890?\
  list.fields=description,owner_id&\
  expansions=owner_id&\
  user.fields=username,verified" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Récupérer la liste avec les informations sur le propriétaire
  response = client.lists.get(
      "1234567890",
      list_fields=["description", "owner_id"],
      expansions=["owner_id"],
      user_fields=["username", "verified"]
  )

  print(f"List: {response.data.name}")
  # Les informations sur le propriétaire se trouvent dans response.includes.users
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Récupérer la liste avec les informations sur le propriétaire
  const response = await client.lists.get("1234567890", {
    listFields: ["description", "owner_id"],
    expansions: ["owner_id"],
    userFields: ["username", "verified"],
  });

  console.log(`List: ${response.data?.name}`);
  // Les informations sur le propriétaire se trouvent dans response.includes?.users
  ```
</CodeGroup>

<div id="response-with-expansion">
  ### Réponse avec expansion
</div>

```json
{
  "data": {
    "id": "1234567890",
    "name": "Tech News",
    "description": "Top tech journalists",
    "owner_id": "2244994945"
  },
  "includes": {
    "users": [
      {
        "id": "2244994945",
        "username": "XDevelopers",
        "verified": true
      }
    ]
  }
}
```

***

<div id="available-fields">
  ## Champs disponibles
</div>

| Champ | Description |
|:------|:------------|
| `description` | Description de la List |
| `owner_id` | ID de l’utilisateur propriétaire |
| `private` | Indique si la List est privée |
| `member_count` | Nombre de membres |
| `follower_count` | Nombre d’abonnés |
| `created_at` | Date de création de la List |

***

<div id="next-steps">
  ## Prochaines étapes
</div>

<CardGroup cols={2}>
  <Card title="Publications d’une liste" icon="list" href="/fr/x-api/lists/list-tweets/quickstart">
    Récupérez les Publications d’une liste
  </Card>

  <Card title="Membres de la liste" icon="users" href="/fr/x-api/lists/list-members/quickstart/list-members-lookup">
    Récupérez les membres d’une liste
  </Card>

  <Card title="Gérer les listes" icon="pen" href="/fr/x-api/lists/manage-lists/quickstart">
    Créez et mettez à jour des listes
  </Card>

  <Card title="Référence de l’API" icon="code" href="/fr/x-api/lists/list-lookup-by-list-id">
    Documentation complète du point de terminaison
  </Card>
</CardGroup>