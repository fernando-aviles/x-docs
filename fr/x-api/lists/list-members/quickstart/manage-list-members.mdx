---
title: Gérer les membres d’une Liste
sidebarTitle: Gérer les membres d’une Liste
description: Ajouter et supprimer des membres d’une Liste
keywords: ["démarrage rapide gestion des membres d’une Liste", "démarrage rapide ajout de membres d’une Liste", "démarrage rapide suppression de membres d’une Liste", "gestion des membres d’une Liste"]
---

import { Button } from '/snippets/fr/button.mdx';

Ce guide vous explique comment ajouter et supprimer des membres d’une Liste.

<Note>
  **Prérequis**

  Avant de commencer, vous aurez besoin de :

  * d’un [compte développeur](https://developer.x.com/en/portal/petition/essential/basic-info) avec une application approuvée
  * d’un User Access Token (OAuth 1.0a ou OAuth 2.0 PKCE)
  * d’une Liste dont vous êtes propriétaire
</Note>

***

<div id="add-a-member-to-a-list">
  ## Ajouter un membre à une Liste
</div>

<Steps>
  <Step title="Obtenir l’identifiant de la Liste et l’identifiant utilisateur">
    Vous avez besoin de l’identifiant de votre Liste et de l’identifiant utilisateur de la personne que vous souhaitez ajouter. Trouvez les identifiants utilisateur à l’aide de l’[endpoint de recherche d’utilisateurs](/fr/x-api/users/lookup/introduction).
  </Step>

  <Step title="Ajouter le membre">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/lists/1441162269824405510/members" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"user_id": "2244994945"}'
      ```

      ```python SDK Python
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # Ajouter un membre à une Liste
      response = client.lists.add_member(
          list_id="1441162269824405510",
          user_id="2244994945"
      )

      print(f"Is member: {response.data.is_member}")
      ```

      ```javascript SDK JavaScript
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // Ajouter un membre à une Liste
      const response = await client.lists.addMember("1441162269824405510", {
        userId: "2244994945",
      });

      console.log(`Is member: ${response.data?.is_member}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="Examiner la réponse">
    ```json
    {
      "data": {
        "is_member": true
      }
    }
    ```
  </Step>
</Steps>

***

<div id="remove-a-member-from-a-list">
  ## Supprimer un membre d’une Liste
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl -X DELETE "https://api.x.com/2/lists/1441162269824405510/members/2244994945" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client
  from xdk.oauth1_auth import OAuth1

  oauth1 = OAuth1(
      api_key="YOUR_API_KEY",
      api_secret="YOUR_API_SECRET",
      access_token="YOUR_ACCESS_TOKEN",
      access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
  )

  client = Client(auth=oauth1)

  # Supprimer un membre d’une Liste
  response = client.lists.remove_member(
      list_id="1441162269824405510",
      user_id="2244994945"
  )

  print(f"Is member: {response.data.is_member}")
  ```

  ```javascript JavaScript SDK
  import { Client, OAuth1 } from "@xdevplatform/xdk";

  const oauth1 = new OAuth1({
    apiKey: "YOUR_API_KEY",
    apiSecret: "YOUR_API_SECRET",
    accessToken: "YOUR_ACCESS_TOKEN",
    accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
  });

  const client = new Client({ oauth1 });

  // Supprimer un membre d’une Liste
  const response = await client.lists.removeMember(
    "1441162269824405510",
    "2244994945"
  );

  console.log(`Is member: ${response.data?.is_member}`);
  ```
</CodeGroup>

**Réponse :**

```json
{
  "data": {
    "is_member": false
  }
}
```

***

<div id="important-notes">
  ## Remarques importantes
</div>

<Note>
  * Vous ne pouvez gérer que les membres des Listes dont vous êtes propriétaire
  * L’ajout d’un utilisateur à une Liste ne nécessite pas son autorisation
  * Les utilisateurs peuvent voir à quelles Listes publiques ils ont été ajoutés
</Note>

***

<div id="next-steps">
  ## Prochaines étapes
</div>

<CardGroup cols={2}>
  <Card title="Recherche des membres de Liste" icon="users" href="/fr/x-api/lists/list-members/quickstart/list-members-lookup">
    Obtenir les membres d’une Liste
  </Card>

  <Card title="Gérer les Listes" icon="pen" href="/fr/x-api/lists/manage-lists/quickstart">
    Créer et mettre à jour des Listes
  </Card>

  <Card title="Référence de l’API" icon="code" href="/fr/x-api/lists/add-list-member">
    Documentation complète du point de terminaison
  </Card>
</CardGroup>