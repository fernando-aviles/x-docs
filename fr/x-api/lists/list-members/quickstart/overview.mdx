---
title: Présentation des membres de liste
sidebarTitle: Présentation
description: Premiers pas avec les endpoints des membres de liste
keywords: ["guide de démarrage rapide des membres de liste", "présentation des membres de liste", "tutoriel sur les membres de liste", "guide de démarrage rapide de l’appartenance à une liste"]
---

import { Button } from '/snippets/fr/button.mdx';

Les endpoints List members vous permettent de consulter les membres d&#39;une Liste et de gérer les membres de la Liste.

<Note>
  **Prérequis**

  Avant de commencer, vous aurez besoin de :

  * D&#39;un [compte développeur](https://developer.x.com/en/portal/petition/essential/basic-info) avec une application approuvée
  * Du Bearer Token de votre application (pour les recherches)
  * D&#39;un User Access Token (pour la gestion des membres)
</Note>

***

<div id="available-endpoints">
  ## Points de terminaison disponibles
</div>

<CardGroup cols={2}>
  <Card title="Recherche des membres d’une Liste" icon="users" href="/fr/x-api/lists/list-members/quickstart/list-members-lookup">
    Obtenir les membres d’une Liste
  </Card>

  <Card title="Gérer les membres d’une Liste" icon="user-plus" href="/fr/x-api/lists/list-members/quickstart/manage-list-members">
    Ajouter et supprimer des membres
  </Card>
</CardGroup>

***

<div id="authentication">
  ## Authentification
</div>

| Opération | Authentification |
|:----------|:---------------|
| Rechercher des membres | Bearer Token, OAuth 1.0a ou OAuth 2.0 PKCE |
| Ajouter/supprimer des membres | OAuth 1.0a ou OAuth 2.0 PKCE |

***

<div id="quick-example">
  ## Exemple rapide
</div>

<CodeGroup dropdown>
  ```bash cURL
  # Récupérer les membres de la liste
  curl "https://api.x.com/2/lists/84839422/members" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Récupérer les membres de la liste
  for page in client.lists.get_members("84839422"):
      for user in page.data:
          print(f"{user.username}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Récupérer les membres de la liste
  const paginator = client.lists.getMembers("84839422");

  for await (const page of paginator) {
    page.data?.forEach((user) => {
      console.log(user.username);
    });
  }
  ```
</CodeGroup>

***

<div id="next-steps">
  ## Prochaines étapes
</div>

<CardGroup cols={2}>
  <Card title="Recherche de listes" icon="list" href="/fr/x-api/lists/list-lookup/quickstart">
    Obtenir les détails d&#39;une liste
  </Card>

  <Card title="Publications de la liste" icon="message" href="/fr/x-api/lists/list-tweets/quickstart">
    Obtenir les publications d&#39;une liste
  </Card>
</CardGroup>