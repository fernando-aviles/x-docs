---
title: Effectuer votre première requête
sidebarTitle: Effectuer votre première requête
description: Prenez en main X API en quelques minutes
keywords: ["première requête", "bien démarrer", "démarrage rapide de l’API", "effectuer une requête API", "cURL", "exemple d’API", "exemple de code"]
---

import { Button } from '/snippets/fr/button.mdx';

Ce guide vous explique comment effectuer votre première requête vers X API. Vous aurez besoin d&#39;un [compte développeur avec des identifiants d&#39;application](/fr/x-api/getting-started/getting-access) avant de commencer.

***

<div id="quick-start-with-curl">
  ## Démarrage rapide avec cURL
</div>

La manière la plus rapide de tester l’API est d’utiliser cURL. Voyons comment rechercher un utilisateur :

```bash
curl "https://api.x.com/2/users/by/username/xdevelopers" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

Remplacez `$BEARER_TOKEN` par votre Bearer Token. Vous obtiendrez une Réponse similaire :

```json
{
  "data": {
    "id": "2244994945",
    "name": "X Developers",
    "username": "xdevelopers"
  }
}
```

***

<div id="step-by-step-guide">
  ## Guide étape par étape
</div>

<Steps>
  <Step title="Obtenir votre Bearer Token">
    Dans le [Developer Console](https://console.x.com), accédez à votre application et copiez le Bearer Token.
  </Step>

  <Step title="Choisir un endpoint">
    Commencez avec l’un de ces endpoints adaptés aux débutants :

    | Endpoint | Ce qu’il fait |
    |:---------|:-------------|
    | [User lookup](/fr/x-api/users/lookup/introduction) | Obtenir le profil d’un utilisateur par nom d’utilisateur ou identifiant |
    | [Post lookup](/fr/x-api/posts/lookup/introduction) | Obtenir une publication par identifiant |
    | [Recent search](/fr/x-api/posts/search/introduction) | Rechercher des publications des 7 derniers jours |
  </Step>

  <Step title="Effectuer la requête">
    Utilisez cURL, Postman ou votre client HTTP préféré :

    ```bash
    # Rechercher un utilisateur par nom d’utilisateur
    curl "https://api.x.com/2/users/by/username/xdevelopers" \
      -H "Authorization: Bearer $BEARER_TOKEN"
    ```
  </Step>

  <Step title="Analyser la réponse">
    Les réponses sont au format JSON. Les données principales se trouvent dans le champ `data` :

    ```json
    {
      "data": {
        "id": "2244994945",
        "name": "X Developers",
        "username": "xdevelopers"
      }
    }
    ```
  </Step>
</Steps>

***

<div id="request-more-data-with-fields">
  ## Demander davantage de données avec les champs
</div>

Par défaut, les endpoints renvoient un nombre minimal de champs. Utilisez le paramètre `fields` pour demander des données supplémentaires :

```bash
curl "https://api.x.com/2/users/by/username/xdevelopers?user.fields=created_at,description,public_metrics" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

Réponse :

```json
{
  "data": {
    "id": "2244994945",
    "name": "X Developers",
    "username": "xdevelopers",
    "created_at": "2013-12-14T04:35:55.000Z",
    "description": "The voice of the X Developer Platform",
    "public_metrics": {
      "followers_count": 570842,
      "following_count": 2048,
      "tweet_count": 14052,
      "listed_count": 1672
    }
  }
}
```

[En savoir plus sur les champs →](/fr/x-api/fundamentals/fields)

***

<div id="more-examples">
  ## Autres exemples
</div>

<Tabs>
  <Tab title="Consulter une publication">
    ```bash
    curl "https://api.x.com/2/tweets/1460323737035677698?tweet.fields=created_at,public_metrics" \
      -H "Authorization: Bearer $BEARER_TOKEN"
    ```
  </Tab>

  <Tab title="Rechercher des publications récentes">
    ```bash
    curl "https://api.x.com/2/tweets/search/recent?query=from:xdevelopers&tweet.fields=created_at" \
      -H "Authorization: Bearer $BEARER_TOKEN"
    ```
  </Tab>

  <Tab title="Obtenir les publications d’un utilisateur">
    ```bash
    curl "https://api.x.com/2/users/2244994945/tweets?max_results=5" \
      -H "Authorization: Bearer $BEARER_TOKEN"
    ```
  </Tab>
</Tabs>

***

<div id="using-code-instead-of-curl">
  ## Utiliser du code plutôt que cURL
</div>

<Tabs>
  <Tab title="Python">
    ```python
    import requests

    bearer_token = "YOUR_BEARER_TOKEN"
    url = "https://api.x.com/2/users/by/username/xdevelopers"

    headers = {"Authorization": f"Bearer {bearer_token}"}
    response = requests.get(url, headers=headers)

    print(response.json())
    ```
  </Tab>

  <Tab title="JavaScript">
    ```javascript
    const bearerToken = "YOUR_BEARER_TOKEN";
    const url = "https://api.x.com/2/users/by/username/xdevelopers";

    fetch(url, {
      headers: { Authorization: `Bearer ${bearerToken}` }
    })
      .then(res => res.json())
      .then(data => console.log(data));
    ```
  </Tab>

  <Tab title="SDK officiels">
    Pour les environnements de production, nous vous recommandons d’utiliser les SDK officiels :

    * [SDK Python](/fr/xdks/python/overview)
    * [SDK TypeScript](/fr/xdks/typescript/overview)

    Ils prennent en charge automatiquement l’authentification, la pagination et la limitation de débit.
  </Tab>
</Tabs>

***

<div id="tools-for-testing">
  ## Outils de test
</div>

<CardGroup cols={3}>
  <Card title="Postman" icon="server" href="/fr/tutorials/postman-getting-started">
    Tests visuels de l’API à l’aide de notre collection.
  </Card>

  <Card title="Exemples de code" icon="github" href="https://github.com/xdevplatform/Twitter-API-v2-sample-code">
    Exemples dans plusieurs langues.
  </Card>

  <Card title="Référence de l’API" icon="code" href="/fr/x-api/posts/lookup/introduction">
    Documentation complète des endpoints.
  </Card>
</CardGroup>

***

<div id="troubleshooting">
  ## Dépannage
</div>

<Accordion title="401 Non autorisé">
  * Vérifiez que votre Bearer Token est correct
  * Assurez-vous que le jeton n’a pas été régénéré entre-temps
  * Vérifiez le format de l’en-tête `Authorization` : `Bearer YOUR_TOKEN`
</Accordion>

<Accordion title="403 Interdit">
  * Votre application n’a peut-être pas accès à ce point de terminaison
  * Certains points de terminaison requièrent une authentification en contexte utilisateur (OAuth 1.0a ou 2.0)
  * Vérifiez les autorisations de votre application dans le Developer Console
</Accordion>

<Accordion title="429 Trop de requêtes">
  * Vous avez atteint une limite de requêtes
  * Vérifiez l’en-tête `x-rate-limit-reset` pour savoir quand réessayer
  * Implémentez une stratégie de backoff exponentiel dans votre code
</Accordion>

[Référence complète des erreurs →](/fr/x-api/fundamentals/response-codes-and-errors)

***

<div id="next-steps">
  ## Prochaines étapes
</div>

<CardGroup cols={2}>
  <Card title="Découvrir l’authentification" icon="key" href="/fr/resources/fundamentals/authentication/overview">
    Comprenez OAuth pour les requêtes avec contexte utilisateur.
  </Card>

  <Card title="Explorer les endpoints" icon="compass" href="/fr/x-api/posts/search/introduction">
    Découvrez ce que vous pouvez créer.
  </Card>

  <Card title="Utiliser un SDK" icon="cube" href="/fr/xdks/overview">
    Accélérez votre développement avec les bibliothèques officielles.
  </Card>

  <Card title="Construire quelque chose" icon="hammer" href="/fr/x-api/what-to-build">
    Des idées de projets à réaliser.
  </Card>
</CardGroup>