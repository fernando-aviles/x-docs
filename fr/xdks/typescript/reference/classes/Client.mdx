---
title: "Client"
sidebarTitle: "Client"
---

Classe Client principale pour X API

Il s’agit du point d’entrée principal pour interagir avec X API. Elle fournit
l’accès à tous les endpoints de l’API via des modules Client spécialisés et gère
l’authentification, la configuration des requêtes et la gestion des erreurs.

**`Exemple`**

```typescript
import { Client } from '@xdevplatform/xdk';

const client = new Client({
  bearerToken: 'your-bearer-token'
});

// Récupérer les informations de l'utilisateur
const user = await client.users.getUser('783214');

// Récupérer les abonnés avec pagination
const followers = await client.users.getFollowers('783214', {
  maxResults: 10,
  userFields: ['id', 'name', 'username']
});

// Itérer sur les abonnés
for await (const follower of followers) {
  console.log(follower.username);
}
```

<div id="constructors">
  ## Constructeurs
</div>

<div id="constructor">
  ### constructor
</div>

• **new Client**(`config`): [`Client`](/fr/xdks/typescript/reference/classes/Client)

Crée une nouvelle instance de Client X API

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type | Description |
| :------ | :------ | :------ |
| `config` | `any` | Options de configuration du client |

<div id="returns">
  #### Renvoie
</div>

[`Client`](/fr/xdks/typescript/reference/classes/Client)

**`Exemple`**

```typescript
// Authentification par Bearer Token
const client = new Client({
  bearerToken: 'your-bearer-token'
});

// Authentification OAuth2
const client = new Client({
  accessToken: 'your-access-token'
});

// Authentification OAuth1
const client = new Client({
  oauth1: oauth1Instance
});
```

[client.ts:401](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/client.ts#L401)

<div id="properties">
  ## Propriétés
</div>

<ResponseField name="baseUrl" type="string" required>
  URL de base pour les requêtes API
</ResponseField>

<ResponseField name="bearerToken" type="string">
  Bearer Token pour l’authentification
</ResponseField>

<ResponseField name="accessToken" type="string">
  Jeton d’accès OAuth2
</ResponseField>

<ResponseField name="oauth1" type="any">
  Instance OAuth1 pour l’authentification
</ResponseField>

<ResponseField name="headers" type="Headers" required>
  En-têtes pour les requêtes
</ResponseField>

<ResponseField name="timeout" type="number" required>
  Délai d’expiration de la requête en millisecondes
</ResponseField>

<ResponseField name="retry" type="boolean" required>
  Indique s’il faut réessayer automatiquement les requêtes ayant échoué
</ResponseField>

<ResponseField name="maxRetries" type="number" required>
  Nombre maximal de tentatives de réessai
</ResponseField>

<ResponseField name="httpClient" type="HttpClient = httpClient" required>
  Client HTTP pour effectuer les requêtes
</ResponseField>

<ResponseField name="general" type="GeneralClient" required>
  Client général
</ResponseField>

<ResponseField name="accountActivity" type="AccountActivityClient" required>
  Client d’activité du compte
</ResponseField>

<ResponseField name="communityNotes" type="CommunityNotesClient" required>
  Client des notes de la communauté
</ResponseField>

<ResponseField name="compliance" type="ComplianceClient" required>
  Client de conformité
</ResponseField>

<ResponseField name="connections" type="ConnectionsClient" required>
  Client des connexions
</ResponseField>

<ResponseField name="users" type="UsersClient" required>
  Client des utilisateurs
</ResponseField>

<ResponseField name="news" type="NewsClient" required>
  Client des actualités
</ResponseField>

<ResponseField name="spaces" type="SpacesClient" required>
  Client Spaces
</ResponseField>

<ResponseField name="activity" type="ActivityClient" required>
  Client d’activité
</ResponseField>

<ResponseField name="usage" type="UsageClient" required>
  Client d’utilisation
</ResponseField>

<ResponseField name="trends" type="TrendsClient" required>
  Client des tendances
</ResponseField>

<ResponseField name="posts" type="PostsClient" required>
  Client des publications
</ResponseField>

<ResponseField name="directMessages" type="DirectMessagesClient" required>
  Client des messages directs
</ResponseField>

<ResponseField name="communities" type="CommunitiesClient" required>
  Client des communautés
</ResponseField>

<ResponseField name="media" type="MediaClient" required>
  Client média
</ResponseField>

<ResponseField name="webhooks" type="WebhooksClient" required>
  Client webhooks
</ResponseField>

<ResponseField name="stream" type="StreamClient" required>
  Client de flux
</ResponseField>

<ResponseField name="lists" type="ListsClient" required>
  Client des listes
</ResponseField>

<ResponseField name="request" type="Promise<T>" required>
  Effectuer une requête authentifiée vers X API

  Cette méthode gère l’authentification, le formatage de la requête et la gestion
  des erreurs pour toutes les requêtes API. Elle ajoute automatiquement les en-têtes
  d’authentification appropriés en fonction de la configuration du client.
</ResponseField>

<ResponseField name="isTokenExpired" type="boolean" required>
  Vérifier si le jeton OAuth2 a expiré
</ResponseField>

<ResponseField name="refreshToken" type="Promise<void>" required>
  Rafraîchir le jeton OAuth2
</ResponseField>

<ResponseField name="isAuthenticated" type="boolean" required>
  Obtenir l’état actuel de l’authentification
</ResponseField>

<ResponseField name="mapSecuritySchemeToAuthTypes" type="string[]" required>
  Faire correspondre les noms de schémas de sécurité OpenAPI aux types d’authentification internes
</ResponseField>

<ResponseField name="validateAuthentication" type="void" required>
  Valider que la méthode d’authentification requise est disponible
</ResponseField>

<ResponseField name="getAvailableAuthTypes" type="string[]" required>
  Obtenir les types d’authentification disponibles
</ResponseField>