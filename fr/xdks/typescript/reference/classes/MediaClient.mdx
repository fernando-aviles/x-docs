---
title: "MediaClient"
sidebarTitle: "MediaClient"
---

Client pour les opérations liées aux médias

Ce client fournit des méthodes pour interagir avec les endpoints média
de la X API. Il gère l’authentification, le formatage des requêtes et l’analyse
des réponses pour toutes les opérations liées aux médias.

zeUpload](/xdks/typescript/reference/classes/MediaClient#finalizeupload)

* [initializeUpload](/fr/xdks/typescript/reference/classes/MediaClient#initializeupload)
* [createSubtitles](/fr/xdks/typescript/reference/classes/MediaClient#createsubtitles)
* [deleteSubtitles](/fr/xdks/typescript/reference/classes/MediaClient#deletesubtitles)

<div id="constructors">
  ## Constructeurs
</div>

<div id="constructor">
  ### constructor
</div>

• **new MediaClient**(`client`): [`MediaClient`](/fr/xdks/typescript/reference/classes/MediaClient)

Crée une nouvelle instance de client média

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type | Description |
| :------ | :------ | :------ |
| `client` | [`Client`](/fr/xdks/typescript/reference/classes/Client) | L&#39;instance principale du Client X API |

<div id="returns">
  #### Retourne
</div>

[`MediaClient`](/fr/xdks/typescript/reference/classes/MediaClient)

#### Défini dans

[media/client.ts:250](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L250)

<div id="methods">
  ## Méthodes
</div>

<div id="getbykey">
  ### getByKey
</div>

▸ **getByKey**(`mediaKey`, `options`): `Promise`&lt;`Response`&gt;

Obtenir un média par clé média\
Récupère les détails d’un fichier média spécifique en utilisant sa clé média.

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type | Description |
| :------ | :------ | :------ |
| `mediaKey` | `string` | Une seule clé média. |
| `options` | `GetByKeyOptions` &amp; &#123; `requestOptions`: &#123; `raw`: `true`  &#125;  &#125; | - |

#### Renvoie

`Promise`&lt;`Response`&gt;

Promesse se résolvant en la Réponse de l&#39;API, ou en l&#39;objet Response brut si `requestOptions.raw` vaut `true`

#### Défini dans

[media/client.ts:295](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L295)

▸ **getByKey**(`mediaKey`, `options?`): `Promise`&lt;[`Get2MediaMediaKeyResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.Get2MediaMediaKeyResponse)&gt;

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `mediaKey` | `string` |
| `options?` | `GetByKeyOptions` |

#### Renvoie

`Promise`&lt;[`Get2MediaMediaKeyResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.Get2MediaMediaKeyResponse)&gt;

#### Défini dans

[media/client.ts:308](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L308)

***

<div id="getuploadstatus">
  ### getUploadStatus
</div>

▸ **getUploadStatus**(`mediaId`, `options`): `Promise`&lt;`Response`&gt;

Obtient l’état du téléversement d’un média\
Récupère l’état du téléversement d’un média à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type | Description |
| :------ | :------ | :------ |
| `mediaId` | `string` | ID du média pour lequel le statut de téléversement est demandé. |
| `options` | `GetUploadStatusOptions` &amp; &#123; `requestOptions`: &#123; `raw`: `true`  &#125;  &#125; | - |

#### Renvoie

`Promise`&lt;`Response`&gt;

Promise qui se résout avec la réponse de l’API, ou la réponse brute si `requestOptions.raw` vaut `true`

#### Défini dans

[media/client.ts:442](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L442)

▸ **getUploadStatus**(`mediaId`, `options?`): `Promise`&lt;[`MediaUploadResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.MediaUploadResponse)&gt;

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `mediaId` | `string` |
| `options?` | `GetUploadStatusOptions` |

#### Renvoie

`Promise`&lt;[`MediaUploadResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.MediaUploadResponse)&gt;

#### Défini dans

[media/client.ts:455](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L455)

***

<div id="upload">
  ### upload
</div>

▸ **upload**(`options`): `Promise`&lt;`Response`&gt;

Téléverser un média
Téléverse un fichier média à utiliser dans des publications ou d&#39;autres contenus.

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `options` | `UploadOptions` &amp; &#123; `requestOptions`: &#123; `raw`: `true`  &#125;  &#125; |

#### Renvoie

`Promise`&lt;`Response`&gt;

Promise qui se résout en la réponse de l&#39;API, ou en `Response` brute si `requestOptions.raw` est à `true`

#### Défini dans

[media/client.ts:582](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L582)

▸ **upload**(`options?`): `Promise`&lt;[`MediaUploadResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.MediaUploadResponse)&gt;

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `options?` | `UploadOptions` |

#### Renvoie

`Promise`&lt;[`MediaUploadResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.MediaUploadResponse)&gt;

#### Défini dans

[media/client.ts:591](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L591)

***

<div id="getanalytics">
  ### getAnalytics
</div>

▸ **getAnalytics**(`mediaKeys`, `endTime`, `startTime`, `granularity`, `options`): `Promise`&lt;`Response`&gt;

Obtenir les données d’analyse des médias
Récupère les données d’analyse pour les médias.

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type | Description |
| :------ | :------ | :------ |
| `mediaKeys` | `any`[] | Une liste de Media Keys séparées par des virgules. Jusqu&#39;à 100 éléments sont autorisés dans une seule requête. |
| `endTime` | `string` | YYYY-MM-DDTHH:mm:ssZ. L&#39;horodatage UTC représentant la fin de l&#39;intervalle de temps. |
| `startTime` | `string` | YYYY-MM-DDTHH:mm:ssZ. L&#39;horodatage UTC représentant le début de l&#39;intervalle de temps. |
| `granularity` | `string` | La granularité des résultats de comptage de la recherche. |
| `options` | `GetAnalyticsOptions` &amp; &#123; `requestOptions`: &#123; `raw`: `true`  &#125;  &#125; | - |

#### Renvoie

`Promise`&lt;`Response`&gt;

Promise se résolvant en la réponse de l’API, ou en la réponse brute si `requestOptions.raw` est à true

#### Défini dans

[media/client.ts:699](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L699)

▸ **getAnalytics**(`mediaKeys`, `endTime`, `startTime`, `granularity`, `options?`): `Promise`&lt;[`MediaAnalytics`](/fr/xdks/typescript/reference/interfaces/Schemas.MediaAnalytics)&gt;

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `mediaKeys` | `any`[] |
| `endTime` | `string` |
| `startTime` | `string` |
| `granularity` | `string` |
| `options?` | `GetAnalyticsOptions` |

#### Renvoie

`Promise`&lt;[`MediaAnalytics`](/fr/xdks/typescript/reference/interfaces/Schemas.MediaAnalytics)&gt;

#### Défini dans

[media/client.ts:724](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L724)

***

<div id="appendupload">
  ### appendUpload
</div>

▸ **appendUpload**(`id`, `options`): `Promise`&lt;`Response`&gt;

Ajout à un téléversement de média
Ajoute des données à une requête de téléversement de média.

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type | Description |
| :------ | :------ | :------ |
| `id` | `string` | Identifiant du média sur lequel effectuer l’opération d’ajout. |
| `options` | `AppendUploadOptions` &amp; &#123; `requestOptions`: &#123; `raw`: `true`  &#125;  &#125; | - |

#### Renvoie

`Promise`&lt;`Response`&gt;

Promise résolue avec la réponse de l’API, ou l’objet `Response` brut si `requestOptions.raw` est `true`

#### Défini dans

[media/client.ts:918](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L918)

▸ **appendUpload**(`id`, `options?`): `Promise`&lt;[`MediaUploadAppendResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.MediaUploadAppendResponse)&gt;

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `id` | `string` |
| `options?` | `AppendUploadOptions` |

#### Renvoie

`Promise`&lt;[`MediaUploadAppendResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.MediaUploadAppendResponse)&gt;

#### Défini dans

[media/client.ts:931](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L931)

***

<div id="getbykeys">
  ### getByKeys
</div>

▸ **getByKeys**(`mediaKeys`, `options`): `Promise`&lt;`Response`&gt;

Obtenir des médias par clés média\
Récupère les détails des fichiers média à partir de leurs clés média.

#### Parameters

| Nom | Type | Description |
| :------ | :------ | :------ |
| `mediaKeys` | `any`[] | Une liste de Media Keys séparées par des virgules. Jusqu&#39;à 100 valeurs sont autorisées par requête. |
| `options` | `GetByKeysOptions` &amp; &#123; `requestOptions`: &#123; `raw`: `true`  &#125;  &#125; | - |

#### Renvoie

`Promise`&lt;`Response`&gt;

Promise qui se résout avec la réponse de l&#39;API, ou l&#39;objet `Response` brut si `requestOptions.raw` est `true`

#### Défini dans

[media/client.ts:1039](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L1039)

▸ **getByKeys**(`mediaKeys`, `options?`): `Promise`&lt;[`Get2MediaResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.Get2MediaResponse)&gt;

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `mediaKeys` | `any`[] |
| `options?` | `GetByKeysOptions` |

#### Renvoie

`Promise`&lt;[`Get2MediaResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.Get2MediaResponse)&gt;

#### Défini dans

[media/client.ts:1052](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L1052)

***

<div id="createmetadata">
  ### createMetadata
</div>

▸ **createMetadata**(`options`): `Promise`&lt;`Response`&gt;

Créer des métadonnées Media
Crée des métadonnées pour un fichier Media.

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `options` | `CreateMetadataOptions` &amp; &#123; `requestOptions`: &#123; `raw`: `true`  &#125;  &#125; |

#### Renvoie

`Promise`&lt;`Response`&gt;

Promise qui se résout avec la réponse de l&#39;API, ou l&#39;objet Response brut si requestOptions.raw est à true

#### Défini dans

[media/client.ts:1191](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L1191)

▸ **createMetadata**(`options?`): `Promise`&lt;[`MetadataCreateResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.MetadataCreateResponse)&gt;

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `options?` | `CreateMetadataOptions` |

#### Renvoie

`Promise`&lt;[`MetadataCreateResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.MetadataCreateResponse)&gt;

#### Défini dans

[media/client.ts:1200](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L1200)

***

<div id="finalizeupload">
  ### finalizeUpload
</div>

▸ **finalizeUpload**(`id`, `options`): `Promise`&lt;`Response`&gt;

Finaliser le téléversement de médias\
Finalise une requête de téléversement de médias.

#### Parameters

| Nom | Type | Description |
| :------ | :------ | :------ |
| `id` | `string` | L’id du média à finaliser. |
| `options` | `Object` | - |
| `options.requestOptions` | `Object` | - |
| `options.requestOptions.raw` | `true` | - |

#### Renvoie

`Promise`&lt;`Response`&gt;

Promise résolue avec la réponse de l&#39;API, ou avec l&#39;objet `Response` brut si `requestOptions.raw` est à true

#### Défini dans

[media/client.ts:1296](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L1296)

▸ **finalizeUpload**(`id`): `Promise`&lt;[`MediaUploadResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.MediaUploadResponse)&gt;

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `id` | `string` |

#### Renvoie

`Promise`&lt;[`MediaUploadResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.MediaUploadResponse)&gt;

#### Défini dans

[media/client.ts:1309](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L1309)

***

<div id="initializeupload">
  ### initializeUpload
</div>

▸ **initializeUpload**(`options`): `Promise`&lt;`Response`&gt;

Initialiser le téléversement de médias
Initialise un téléversement de médias.

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `options` | `InitializeUploadOptions` &amp; &#123; `requestOptions`: &#123; `raw`: `true`  &#125;  &#125; |

#### Renvoie

`Promise`&lt;`Response`&gt;

Promise se résolvant en la réponse de l’API, ou en une Response brute si `requestOptions.raw` est `true`

#### Défini dans

[media/client.ts:1396](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L1396)

▸ **initializeUpload**(`options?`): `Promise`&lt;[`MediaUploadResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.MediaUploadResponse)&gt;

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `options?` | `InitializeUploadOptions` |

#### Renvoie

`Promise`&lt;[`MediaUploadResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.MediaUploadResponse)&gt;

#### Défini dans

[media/client.ts:1405](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L1405)

***

<div id="createsubtitles">
  ### createSubtitles
</div>

▸ **createSubtitles**(`options`): `Promise`&lt;`Response`&gt;

Créer des sous-titres pour Media
Crée des sous-titres pour un fichier média spécifique.

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `options` | `CreateSubtitlesOptions` &amp; &#123; `requestOptions`: &#123; `raw`: `true`  &#125;  &#125; |

#### Renvoie

`Promise`&lt;`Response`&gt;

Promise qui se résout en la réponse de l’API, ou en l’objet Response brut si requestOptions.raw est true

#### Défini dans

[media/client.ts:1497](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L1497)

▸ **createSubtitles**(`options?`): `Promise`&lt;[`SubtitlesCreateResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.SubtitlesCreateResponse)&gt;

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `options?` | `CreateSubtitlesOptions` |

#### Renvoie

`Promise`&lt;[`SubtitlesCreateResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.SubtitlesCreateResponse)&gt;

#### Défini dans

[media/client.ts:1506](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L1506)

***

<div id="deletesubtitles">
  ### deleteSubtitles
</div>

▸ **deleteSubtitles**(`options`): `Promise`&lt;`Response`&gt;

Supprimer les sous-titres d’un média
Supprime les sous-titres d’un fichier Media spécifique.

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `options` | `DeleteSubtitlesOptions` &amp; &#123; `requestOptions`: &#123; `raw`: `true`  &#125;  &#125; |

#### Renvoie

`Promise`&lt;`Response`&gt;

Promise qui se résout en la réponse de l&#39;API, ou en l&#39;objet `Response` brut si `requestOptions.raw` est `true`

#### Défini dans

[media/client.ts:1598](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L1598)

▸ **deleteSubtitles**(`options?`): `Promise`&lt;[`SubtitlesDeleteResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.SubtitlesDeleteResponse)&gt;

<div id="parameters">
  #### Paramètres
</div>

| Nom | Type |
| :------ | :------ |
| `options?` | `DeleteSubtitlesOptions` |

#### Renvoie

`Promise`&lt;[`SubtitlesDeleteResponse`](/fr/xdks/typescript/reference/interfaces/Schemas.SubtitlesDeleteResponse)&gt;

<div id="defined-in">
  #### Définie dans
</div>

[media/client.ts:1607](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/media/client.ts#L1607)