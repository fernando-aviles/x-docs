---
title: "UsersClient"
sidebarTitle: "UsersClient"
---

<div id="usersclient">
  ## UsersClient
</div>

<Badge color="blue">Classe</Badge>

<Badge color="gray">Bases : object</Badge>

Client pour les opérations liées aux utilisateurs

<div id="constructors">
  ## Constructeurs
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="block_dms">
  ### `block_dms`
</div>

Bloquer les messages privés
Bloque les messages privés envoyés à ou reçus d’un utilisateur spécifique identifié par son id pour l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur cible que l’utilisateur authentifié demande de bloquer pour les Messages privés.
</ParamField>

#### Renvoie

`BlockDmsResponse` - données de la réponse

<div id="create_bookmark">
  ### `create_bookmark`
</div>

Créer un signet
Ajoute une publication aux signets de l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur source authentifié pour lequel vous souhaitez ajouter des signets.
</ParamField>

<ParamField path="body.body" type="CreateBookmarkRequest">
Corps de la requête
</ParamField>

<div id="delete_bookmark">
  ### `delete_bookmark`
</div>

Supprimer un signet
Supprime une publication des Signets de l’utilisateur authentifié à l’aide de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur source authentifié dont il faut supprimer le signet.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
L’ID de la Publication que l’utilisateur source retire de ses signets.
</ParamField>

#### Renvoie

`DeleteBookmarkResponse` - données de la réponse

<div id="follow_list">
  ### `follow_list`
</div>

Suivre une liste
Permet à l’utilisateur authentifié de suivre une liste spécifique via son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur source authentifié qui suivra la liste.
</ParamField>

<ParamField path="body.body" type="FollowListRequest">
Corps de la requête
</ParamField>

<div id="follow_user">
  ### `follow_user`
</div>

Suivre un utilisateur
Fait en sorte que l’utilisateur authentifié commence à suivre un utilisateur spécifique à l’aide de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur source authentifié qui demande à suivre l’utilisateur cible.
</ParamField>

<ParamField path="body.body" type="FollowUserRequest">
Corps de la requête
</ParamField>

<div id="get_blocking">
  ### `get_blocking`
</div>

Récupérer les blocages
Renvoie la liste des utilisateurs bloqués par l’id d’utilisateur spécifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’id de l’utilisateur source authentifié pour lequel les résultats doivent être renvoyés.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir une « page » spécifique de résultats.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste séparée par des virgules de champs d’utilisateur à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste séparée par des virgules de champs à étendre.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste séparée par des virgules de champs de Tweet à afficher.
</ParamField>

#### Renvoie

`IteratorGetBlockingResponse`

<div id="get_bookmark_folders">
  ### `get_bookmark_folders`
</div>

Obtenir les dossiers de signets
Renvoie une liste de dossiers de signets créés par l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur source authentifié pour lequel les résultats seront renvoyés.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre permet d’obtenir la page de résultats suivante.
</ParamField>

#### Renvoie

`IteratorGetBookmarkFoldersResponse`

<div id="get_bookmarks">
  ### `get_bookmarks`
</div>

Récupérer les favoris
Renvoie la liste des Publications mises en favori par l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur source authentifié pour lequel retourner les résultats.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre permet d’obtenir la « page » suivante de résultats.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Tweet à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste, séparée par des virgules, de champs à étendre.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Media à afficher.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Poll à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs User à afficher.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Place à afficher.
</ParamField>

<div id="returns">
  #### Valeur de retour
</div>

`IteratorGetBookmarksResponse`

<div id="get_bookmarks_by_folder_id">
  ### `get_bookmarks_by_folder_id`
</div>

Récupérer les signets par ID de dossier
Récupère les Publications dans un dossier de signets spécifique à partir de son ID pour l’utilisateur authentifié.

#### Parameters

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur source authentifié pour lequel les résultats doivent être renvoyés.
</ParamField>

<ParamField path="path.folder_id" type="Any">
L’identifiant du dossier de favoris pour lequel l’utilisateur authentifié souhaite récupérer des Publications.
</ParamField>

#### Renvoie

`GetBookmarksByFolderIdResponse` - Données de la réponse

<div id="get_by_id">
  ### `get_by_id`
</div>

Obtenir un utilisateur par identifiant
Récupère les détails d’un utilisateur spécifique à partir de son identifiant.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur à rechercher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à développer, séparés par des virgules.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`GetByIdResponse` - Données de la réponse

<div id="get_by_ids">
  ### `get_by_ids`
</div>

Obtenir des Users par leurs id
Récupère les détails de plusieurs Users à partir de leurs id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.ids" type="List">
Une liste d’ID utilisateur, séparés par des virgules. Vous pouvez spécifier jusqu’à 100 ID.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste d’ID utilisateur, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à développer, séparés par des virgules.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`GetByIdsResponse` - Données de réponse

<div id="get_by_username">
  ### `get_by_username`
</div>

Obtenir un User par nom d’utilisateur
Récupère les détails d’un User spécifique à partir de son nom d’utilisateur.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.username" type="str">
Un nom d’utilisateur.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à étendre.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`GetByUsernameResponse` - données de la Réponse

<div id="get_by_usernames">
  ### `get_by_usernames`
</div>

Obtenir des utilisateurs par noms d’utilisateur
Récupère les détails de plusieurs utilisateurs à partir de leurs noms d’utilisateur.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.usernames" type="Liste">
Une liste de noms d’utilisateur séparés par des virgules.
</ParamField>

<ParamField path="path.user_fields" type="Liste ou None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="Liste ou None" default="None">
Une liste de champs, séparés par des virgules, à étendre.
</ParamField>

<ParamField path="path.tweet_fields" type="Liste ou None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`GetByUsernamesResponse` - Données de la réponse

<div id="get_followed_lists">
  ### `get_followed_lists`
</div>

Obtenir les listes suivies
Récupère les listes suivies par un utilisateur spécifique à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur à rechercher.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir une « page » donnée de résultats.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Une liste de champs List, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à étendre.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`IteratorGetFollowedListsResponse`

<div id="get_followers">
  ### `get_followers`
</div>

Obtenir les abonnés
Récupère la liste des utilisateurs qui suivent un utilisateur donné à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de l’Utilisateur à rechercher.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir une page spécifique de résultats.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs d’Utilisateur, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à étendre.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs de Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`IteratorGetFollowersResponse`

<div id="get_following">
  ### `get_following`
</div>

Obtenir les comptes suivis
Récupère la liste des utilisateurs qu’un utilisateur spécifique suit à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur à rechercher.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir une « page » spécifique de résultats.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à développer, séparés par des virgules.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`IteratorGetFollowingResponse`

<div id="get_liked_posts">
  ### `get_liked_posts`
</div>

Récupérer les Publications aimées
Récupère la liste des Publications aimées par un utilisateur spécifique, identifié par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur à rechercher.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre permet d’obtenir la « page » suivante de résultats.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs de Tweet séparés par des virgules à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à étendre, séparés par des virgules.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs Media séparés par des virgules à afficher.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Une liste de champs Poll séparés par des virgules à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User séparés par des virgules à afficher.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Une liste de champs Place séparés par des virgules à afficher.
</ParamField>

#### Renvoie

`IteratorGetLikedPostsResponse`

<div id="get_list_memberships">
  ### `get_list_memberships`
</div>

Obtenir les appartenances à des Listes
Récupère les Listes auxquelles un Utilisateur spécifique appartient à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’id de l’utilisateur à rechercher.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre permet d’obtenir une « page » spécifique de résultats.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Une liste de champs List séparés par des virgules à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à étendre, séparés par des virgules.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User séparés par des virgules à afficher.
</ParamField>

#### Renvoie

`IteratorGetListMembershipsResponse`

<div id="get_me">
  ### `get_me`
</div>

Obtenir mon utilisateur
Récupère les informations de l'utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à développer.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`GetMeResponse` - données de la réponse

<div id="get_mentions">
  ### `get_mentions`
</div>

Obtenir les mentions
Récupère une liste de publications qui mentionnent un utilisateur spécifique à partir de son id.

#### Parameters

<ParamField path="path.id" type="Any">
L'ID de l'utilisateur à rechercher.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
L'ID minimal de publication à inclure dans le jeu de résultats. Ce paramètre prime sur start_time si les deux sont spécifiés.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
L'ID maximal de publication à inclure dans le jeu de résultats. Ce paramètre prime sur end_time si les deux sont spécifiés.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir la « page » suivante de résultats.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. Le premier horodatage UTC à partir duquel les publications seront renvoyées. Le paramètre since_id prime s'il est également spécifié.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. Le dernier horodatage UTC jusqu'auquel les publications seront renvoyées. Le paramètre until_id prime s'il est également spécifié.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à étendre.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs Media, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Une liste de champs Poll, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Une liste de champs Place, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`IteratorGetMentionsResponse`

<div id="get_muting">
  ### `get_muting`
</div>

Récupérer les comptes mis en sourdine
Récupère la liste des comptes mis en sourdine par l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’id de l’utilisateur source authentifié pour lequel les résultats doivent être renvoyés.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre permet d’obtenir la « page » suivante de résultats.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs d’utilisateur, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à développer.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs de Tweet, séparés par des virgules, à afficher.
</ParamField>

<div id="returns">
  #### Valeur de retour
</div>

`IteratorGetMutingResponse`

<div id="get_owned_lists">
  ### `get_owned_lists`
</div>

Obtenir les listes appartenant à un utilisateur
Récupère les listes appartenant à un utilisateur spécifique à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur à rechercher.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour récupérer une « page » spécifique de résultats.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Une liste de champs List, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à développer, séparés par des virgules.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`IteratorGetOwnedListsResponse`

<div id="get_pinned_lists">
  ### `get_pinned_lists`
</div>

Obtenir les listes épinglées
Récupère les listes épinglées par l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’id de l’utilisateur source authentifié pour lequel renvoyer les résultats.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs List à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste, séparée par des virgules, de champs à développer.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs d’utilisateur à afficher.
</ParamField>

<div id="returns">
  #### Valeur de retour
</div>

`GetPinnedListsResponse` - données de la réponse

<div id="get_posts">
  ### `get_posts`
</div>

Obtenir des publications
Récupère la liste des publications d’un utilisateur donné à partir de son id.

#### Parameters

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur à rechercher.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
L’identifiant minimal de Publication à inclure dans le jeu de résultats. Ce paramètre a priorité sur start_time si les deux sont spécifiés.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
L’identifiant maximal de Publication à inclure dans le jeu de résultats. Ce paramètre a priorité sur end_time si les deux sont spécifiés.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir la page suivante de résultats.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
L’ensemble des entités à exclure (par exemple « replies » ou « retweets »).
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. L’horodatage UTC le plus ancien à partir duquel des Publications seront renvoyées. Le paramètre since_id a priorité s’il est également spécifié.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. L’horodatage UTC le plus récent jusqu’auquel des Publications seront renvoyées. Le paramètre until_id a priorité s’il est également spécifié.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à étendre.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs Media, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Une liste de champs Poll, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Une liste de champs Place, séparés par des virgules, à afficher.
</ParamField>

#### Retourne

`IteratorGetPostsResponse`

<div id="get_reposts_of_me">
  ### `get_reposts_of_me`
</div>

Obtenir mes Reposts
Récupère une liste de Publications qui repartagent le contenu de l'utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre permet d’obtenir la « page » suivante de résultats.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à étendre.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs Media, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Une liste de champs Poll, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Une liste de champs Place, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`IteratorGetRepostsOfMeResponse`

<div id="get_timeline">
  ### `get_timeline`
</div>

Obtenir la Timeline
Récupère une liste de Publications classées par ordre chronologique inverse dans la Timeline de l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de l’Utilisateur source authentifié pour lister les Publications de sa timeline en ordre antéchronologique.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
L’identifiant minimal de Publication à inclure dans le jeu de résultats. Ce paramètre a priorité sur start_time s’ils sont tous les deux spécifiés.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
L’identifiant maximal de Publication à inclure dans le jeu de résultats. Ce paramètre a priorité sur end_time s’ils sont tous les deux spécifiés.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir la « page » de résultats suivante.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
L’ensemble des entités à exclure (par exemple « replies » ou « retweets »).
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. L’horodatage UTC le plus ancien à partir duquel les Publications seront fournies. Le paramètre since_id a priorité s’il est également spécifié.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. L’horodatage UTC le plus récent jusqu’auquel les Publications seront fournies. Le paramètre until_id a priorité s’il est également spécifié.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs de Tweet, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à étendre, séparés par des virgules.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs Media, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Une liste de champs Poll, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Une liste de champs Place, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`IteratorGetTimelineResponse`

<div id="like_post">
  ### `like_post`
</div>

Aimer une publication
Permet à l’utilisateur authentifié d’aimer une publication spécifique à partir de son identifiant.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L'ID de l'utilisateur source authentifié qui demande d'aimer la Publication.
</ParamField>

<ParamField path="body.body" type="LikePostRequest">
Corps de la requête
</ParamField>

<div id="mute_user">
  ### `mute_user`
</div>

Mettre un utilisateur en sourdine
Fait en sorte que l’utilisateur authentifié mette en sourdine un utilisateur spécifique à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur source authentifié qui demande de mettre en sourdine l’utilisateur cible.
</ParamField>

<ParamField path="body.body" type="MuteUserRequest">
Corps de la requête
</ParamField>

<div id="pin_list">
  ### `pin_list`
</div>

Épingler une liste
Fait en sorte que l’utilisateur authentifié épingle une liste spécifique identifiée par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur source authentifié qui épinglera la Liste.
</ParamField>

<ParamField path="body.body" type="PinListRequest">
Corps de la requête
</ParamField>

<div id="repost_post">
  ### `repost_post`
</div>

Reposter une Publication
Permet à l'utilisateur authentifié de republier une Publication spécifique à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur source authentifié qui demande la republication de la Publication.
</ParamField>

<ParamField path="body.body" type="RepostPostRequest">
Corps de la requête
</ParamField>

<div id="search">
  ### `search`
</div>

Recherche d’utilisateurs
Renvoie une liste d’utilisateurs correspondant à une requête de recherche.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.query" type="Any">
La chaîne de requête utilisée pour rechercher des utilisateurs.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir la « page » suivante de résultats. La valeur utilisée avec ce paramètre est directement extraite de la Réponse fournie par l’API et ne doit pas être modifiée.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs User à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste, séparée par des virgules, de champs à développer.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Tweet à afficher.
</ParamField>

#### Renvoie

`IteratorSearchResponse`

<div id="unblock_dms">
  ### `unblock_dms`
</div>

Débloquer les messages privés
Débloque les messages privés vers ou depuis un utilisateur spécifique à l’aide de son id pour l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’id de l’utilisateur cible pour lequel l’utilisateur authentifié demande la levée du blocage des messages privés.
</ParamField>

#### Renvoie

`UnblockDmsResponse` - données de la réponse

<div id="unfollow_list">
  ### `unfollow_list`
</div>

Ne plus suivre une liste
Permet à l’utilisateur authentifié d’arrêter de suivre une liste spécifique par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur source authentifié qui se désabonnera de la Liste.
</ParamField>

<ParamField path="path.list_id" type="Any">
L’ID de la Liste à ne plus suivre.
</ParamField>

#### Renvoie

`UnfollowListResponse` - Données de la réponse

<div id="unfollow_user">
  ### `unfollow_user`
</div>

Ne plus suivre un utilisateur
Fait que l’utilisateur authentifié ne suive plus un utilisateur spécifique, identifié par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.source_user_id" type="Any">
L'identifiant de l'utilisateur source authentifié qui demande à ne plus suivre l'utilisateur cible.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
L'identifiant de l'utilisateur que l'utilisateur source demande à ne plus suivre.
</ParamField>

#### Renvoie

`UnfollowUserResponse` - données de la Réponse

<div id="unlike_post">
  ### `unlike_post`
</div>

Retirer la mention J’aime de la Publication
Fait en sorte que l’utilisateur authentifié retire la mention J’aime d’une Publication spécifique à partir de son ID.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L'identifiant de l'Utilisateur source authentifié qui demande de retirer son like de la Publication.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
L'identifiant de la Publication pour laquelle l'Utilisateur demande de retirer son like.
</ParamField>

#### Renvoie

`UnlikePostResponse` - Données de la réponse

<div id="unmute_user">
  ### `unmute_user`
</div>

Annuler la mise en sourdine d’un utilisateur
Amène l’utilisateur authentifié à annuler la mise en sourdine d’un utilisateur spécifique via son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.source_user_id" type="Any">
L’ID de l’utilisateur source authentifié qui demande de désactiver la mise en sourdine de l’utilisateur cible.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
L’ID de l’utilisateur pour lequel l’utilisateur source demande de désactiver la mise en sourdine.
</ParamField>

#### Renvoie

`UnmuteUserResponse` - données de la réponse

<div id="unpin_list">
  ### `unpin_list`
</div>

Désépingler une List
Permet à l’utilisateur authentifié de désépingler une List spécifique par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur source authentifié pour lequel renvoyer les résultats.
</ParamField>

<ParamField path="path.list_id" type="Any">
L’ID de la liste à désépingler.
</ParamField>

#### Renvoie

`UnpinListResponse` - données de la réponse

<div id="unrepost_post">
  ### `unrepost_post`
</div>

Annuler la republication d’une Publication
Permet à l’utilisateur authentifié d’annuler la republication d’une Publication spécifique à partir de son identifiant.

#### Parameters

<ParamField path="path.id" type="Any">
L’identifiant de l’Utilisateur source authentifié qui demande de republier la Publication.
</ParamField>

<ParamField path="path.source_tweet_id" type="Any">
L’identifiant de la Publication dont l’Utilisateur demande l’annulation du repost.
</ParamField>

#### Renvoie

`UnrepostPostResponse` - données de réponse