---
title: "UsersClient"
sidebarTitle: "UsersClient"
---

<div id="usersclient">
  ## UsersClient
</div>

<Badge color="blue">Classe</Badge>

<Badge color="gray">Bases : object</Badge>

Client pour les opérations relatives aux utilisateurs

<div id="constructors">
  ## Constructeurs
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="block_dms">
  ### `block_dms`
</div>

Bloquer les DM
Bloque les messages directs envoyés ou reçus par un utilisateur spécifique, identifié par son id, pour l'utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’id de l’utilisateur cible dont l’utilisateur authentifié demande le blocage des Messages privés.
</ParamField>

<div id="returns">
  #### Valeur de retour
</div>

`BlockDmsResponse` - données de la réponse

<div id="create_bookmark">
  ### `create_bookmark`
</div>

Créer un signet
Ajoute une publication aux signets de l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur source authentifié pour lequel ajouter des signets.
</ParamField>

<ParamField path="body.body" type="CreateBookmarkRequest">
Corps de la requête
</ParamField>

<div id="delete_bookmark">
  ### `delete_bookmark`
</div>

Supprimer un signet
Supprime une Publication des signets de l’utilisateur authentifié à partir de son identifiant.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur source authentifié dont le signet doit être supprimé.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
L’identifiant de la Publication que l’utilisateur source supprime de ses signets.
</ParamField>

#### Renvoie

`DeleteBookmarkResponse` - données de la réponse

<div id="follow_list">
  ### `follow_list`
</div>

Suivre une Liste
Permet à l’utilisateur authentifié de s’abonner à une Liste spécifique à partir de son identifiant.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’id de l’utilisateur source authentifié qui suivra la Liste.
</ParamField>

<ParamField path="body.body" type="FollowListRequest">
Corps de la requête
</ParamField>

<div id="follow_user">
  ### `follow_user`
</div>

Suivre un utilisateur
Permet à l’utilisateur authentifié de suivre un utilisateur spécifique à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L'id de l'utilisateur source authentifié qui souhaite suivre l'utilisateur cible.
</ParamField>

<ParamField path="body.body" type="FollowUserRequest">
Corps de la requête
</ParamField>

<div id="get_blocking">
  ### `get_blocking`
</div>

Obtenir les utilisateurs bloqués
Renvoie une liste d’utilisateurs bloqués par l’id d’utilisateur spécifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur source authentifié pour lequel les résultats doivent être renvoyés.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir une « page » spécifique de résultats.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs d’utilisateur, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à étendre, séparés par des virgules.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs de Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`IteratorGetBlockingResponse`

<div id="get_bookmark_folders">
  ### `get_bookmark_folders`
</div>

Récupérer les dossiers de Signets
Récupère la liste des dossiers de Signets créés par l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur source authentifié pour lequel renvoyer les résultats.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre permet d’obtenir la « page » suivante de résultats.
</ParamField>

#### Renvoie

`IteratorGetBookmarkFoldersResponse`

<div id="get_bookmarks">
  ### `get_bookmarks`
</div>

Récupérer les signets
Récupère la liste des Publications mises en signet par l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur source authentifié pour lequel renvoyer les résultats.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir la « page » suivante de résultats.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste séparée par des virgules de champs Tweet à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste séparée par des virgules de champs à étendre.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste séparée par des virgules de champs média à afficher.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Une liste séparée par des virgules de champs de sondage à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste séparée par des virgules de champs d’utilisateur à afficher.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Une liste séparée par des virgules de champs de lieu à afficher.
</ParamField>

#### Renvoie

`IteratorGetBookmarksResponse`

<div id="get_bookmarks_by_folder_id">
  ### `get_bookmarks_by_folder_id`
</div>

Obtenir les signets par ID de dossier
Récupère les Publications d'un dossier de signets spécifique, identifié par son ID, pour l'utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L'id de l'Utilisateur source authentifié pour lequel renvoyer les résultats.
</ParamField>

<ParamField path="path.folder_id" type="Any">
L'id du Dossier de signets pour lequel l'Utilisateur authentifié tente de récupérer des Publications.
</ParamField>

#### Renvoie

`GetBookmarksByFolderIdResponse` - Données de la réponse

<div id="get_by_id">
  ### `get_by_id`
</div>

Récupérer un utilisateur par id
Récupère les détails d’un utilisateur spécifique à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur à rechercher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs User à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste, séparée par des virgules, de champs à étendre.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Tweet à afficher.
</ParamField>

#### Renvoie

`GetByIdResponse` - Données de réponse

<div id="get_by_ids">
  ### `get_by_ids`
</div>

Obtenir des utilisateurs par id
Récupère les détails de plusieurs utilisateurs en fonction de leurs id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.ids" type="List">
Une liste d’identifiants d’utilisateur séparés par des virgules. Vous pouvez spécifier jusqu’à 100 identifiants.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs d’utilisateur à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste, séparée par des virgules, de champs à développer.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs de Tweet à afficher.
</ParamField>

#### Renvoie

`GetByIdsResponse` - Données de la réponse

<div id="get_by_username">
  ### `get_by_username`
</div>

Obtenir un utilisateur par nom d’utilisateur
Récupère les détails d’un utilisateur spécifique à partir de son nom d’utilisateur.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.username" type="str">
Un nom d’utilisateur.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à développer.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`GetByUsernameResponse` - Données de la réponse

<div id="get_by_usernames">
  ### `get_by_usernames`
</div>

Obtenir des utilisateurs par noms d’utilisateur
Récupère les détails de plusieurs utilisateurs en fonction de leurs noms d’utilisateur.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.usernames" type="List">
Une liste de noms d’utilisateur séparés par des virgules.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User séparés par des virgules à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs séparés par des virgules à développer.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet séparés par des virgules à afficher.
</ParamField>

#### Renvoie

`GetByUsernamesResponse` - données de la réponse

<div id="get_followed_lists">
  ### `get_followed_lists`
</div>

Obtenir les Listes suivies
Récupère la liste des Listes suivies par un Utilisateur donné, à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L&#39;identifiant de l&#39;Utilisateur à consulter.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir une « page » spécifique de résultats.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Une liste de champs List à afficher, séparés par des virgules.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à développer, séparés par des virgules.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User à afficher, séparés par des virgules.
</ParamField>

#### Renvoie

`IteratorGetFollowedListsResponse`

<div id="get_followers">
  ### `get_followers`
</div>

Obtenir les abonnés
Récupère la liste des utilisateurs qui suivent un utilisateur donné à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’id de l’utilisateur à récupérer.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir une « page » spécifique de résultats.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs d’utilisateur, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à étendre.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`IteratorGetFollowersResponse`

<div id="get_following">
  ### `get_following`
</div>

Obtenir les comptes suivis
Récupère la liste des utilisateurs suivis par un utilisateur spécifique, identifié par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’id de l’utilisateur à rechercher.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir une page spécifique de résultats.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs utilisateur, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à développer.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs de Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`IteratorGetFollowingResponse`

<div id="get_liked_posts">
  ### `get_liked_posts`
</div>

Récupérer les Publications aimées
Récupère la liste des Publications aimées par un utilisateur spécifique à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’id de l’utilisateur à rechercher.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir la « page » suivante de résultats.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs de Tweet à afficher, séparés par des virgules.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à étendre, séparés par des virgules.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs Media à afficher, séparés par des virgules.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Une liste de champs de Poll à afficher, séparés par des virgules.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User à afficher, séparés par des virgules.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Une liste de champs Place à afficher, séparés par des virgules.
</ParamField>

#### Renvoie

`IteratorGetLikedPostsResponse`

<div id="get_list_memberships">
  ### `get_list_memberships`
</div>

Récupérer les appartenances aux Listes
Récupère l’ensemble des Listes dont un utilisateur donné est membre, à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur à récupérer.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir une « page » spécifique de résultats.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Une liste, séparée par des virgules, des champs List à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste, séparée par des virgules, des champs à étendre.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste, séparée par des virgules, des champs User à afficher.
</ParamField>

#### Renvoie

`IteratorGetListMembershipsResponse`

<div id="get_me">
  ### `get_me`
</div>

Récupérer mon utilisateur
Récupère les détails de l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs d’utilisateur, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à étendre.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs de Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`GetMeResponse` - données de la réponse

<div id="get_mentions">
  ### `get_mentions`
</div>

Obtenir les mentions
Récupère la liste des Publications qui mentionnent un utilisateur spécifique à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur à rechercher.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
L’identifiant minimal de Publication à inclure dans l’ensemble de résultats. Ce paramètre a priorité sur start_time si les deux sont spécifiés.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
L’identifiant maximal de Publication à inclure dans l’ensemble de résultats. Ce paramètre a priorité sur end_time si les deux sont spécifiés.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir la « page » suivante de résultats.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. Le premier horodatage UTC à partir duquel les Publications seront renvoyées. Le paramètre since_id a priorité s’il est également spécifié.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. Le dernier horodatage UTC jusqu’auquel les Publications seront renvoyées. Le paramètre until_id a priorité s’il est également spécifié.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet à afficher, séparés par des virgules.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à développer, séparés par des virgules.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs Media à afficher, séparés par des virgules.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Une liste de champs Poll à afficher, séparés par des virgules.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User à afficher, séparés par des virgules.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Une liste de champs Place à afficher, séparés par des virgules.
</ParamField>

#### Renvoie

`IteratorGetMentionsResponse`

<div id="get_muting">
  ### `get_muting`
</div>

Obtenir la liste des comptes mis en sourdine
Renvoie la liste des comptes mis en sourdine par l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’id de l’utilisateur source authentifié pour lequel renvoyer les résultats.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir la « page » suivante de résultats.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à étendre, séparés par des virgules.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`IteratorGetMutingResponse`

<div id="get_owned_lists">
  ### `get_owned_lists`
</div>

Obtenir les Listes possédées
Récupère la liste des Listes appartenant à un utilisateur spécifique à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur à rechercher.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir une « page » donnée de résultats.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Une liste de champs List, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à développer.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<div id="returns">
  #### Valeur de retour
</div>

`IteratorGetOwnedListsResponse`

<div id="get_pinned_lists">
  ### `get_pinned_lists`
</div>

Obtenir les listes épinglées
Récupère la liste des listes épinglées de l'utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur source authentifié pour lequel les résultats seront renvoyés.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Une liste de champs List, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à étendre.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`GetPinnedListsResponse` - Données de la réponse

<div id="get_posts">
  ### `get_posts`
</div>

Récupérer les publications
Récupère une liste de publications créées par un utilisateur spécifique à l’aide de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur à rechercher.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
L’ID minimal de Publication à inclure dans le jeu de résultats. Ce paramètre a priorité sur start_time si les deux sont spécifiés.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
L’ID maximal de Publication à inclure dans le jeu de résultats. Ce paramètre a priorité sur end_time si les deux sont spécifiés.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir la page de résultats suivante.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
L’ensemble des entités à exclure (par exemple « replies » ou « retweets »).
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. L’horodatage UTC le plus ancien à partir duquel les Publications seront renvoyées. Le paramètre since_id a priorité s’il est également spécifié.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. L’horodatage UTC le plus récent jusqu’auquel les Publications seront renvoyées. Le paramètre until_id a priorité s’il est également spécifié.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs de Tweet séparés par des virgules à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à étendre, séparés par des virgules.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs de média séparés par des virgules à afficher.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Une liste de champs de sondage séparés par des virgules à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs d’utilisateur séparés par des virgules à afficher.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Une liste de champs de lieu séparés par des virgules à afficher.
</ParamField>

#### Renvoie

`IteratorGetPostsResponse`

<div id="get_reposts_of_me">
  ### `get_reposts_of_me`
</div>

Obtenir mes republications
Renvoie une liste de Publications qui republient le contenu de l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre permet d’obtenir la « page » suivante de résultats.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Liste de champs à étendre, séparés par des virgules.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Liste de champs Media, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Liste de champs Poll, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Liste de champs Place, séparés par des virgules, à afficher.
</ParamField>

<div id="returns">
  #### Valeur de retour
</div>

`IteratorGetRepostsOfMeResponse`

<div id="get_timeline">
  ### `get_timeline`
</div>

Obtenir la Timeline
Récupère une liste antéchronologique de Publications dans la Timeline de l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’Utilisateur source authentifié dont on souhaite lister les Publications du fil chronologique inversé.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
L’ID minimal de Publication à inclure dans le jeu de résultats. Ce paramètre prévaut sur start_time si les deux sont spécifiés.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
L’ID maximal de Publication à inclure dans le jeu de résultats. Ce paramètre prévaut sur end_time si les deux sont spécifiés.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir la « page » suivante de résultats.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
L’ensemble des entités à exclure (par exemple « replies » ou « retweets »).
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. L’horodatage UTC le plus ancien à partir duquel les Publications seront fournies. Le paramètre since_id prévaut s’il est également spécifié.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. L’horodatage UTC le plus récent jusqu’auquel les Publications seront fournies. Le paramètre until_id prévaut s’il est également spécifié.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs de Tweet, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à étendre, séparés par des virgules.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs de média, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Une liste de champs de sondage, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs d’utilisateur, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Une liste de champs de lieu, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`IteratorGetTimelineResponse`

<div id="like_post">
  ### `like_post`
</div>

Aimer une publication
Permet à l’utilisateur authentifié d’aimer une publication spécifique identifiée par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur source authentifié qui demande à aimer la Publication.
</ParamField>

<ParamField path="body.body" type="LikePostRequest">
Corps de la requête
</ParamField>

<div id="mute_user">
  ### `mute_user`
</div>

Mettre un utilisateur en sourdine
Permet à l’utilisateur authentifié de mettre en sourdine un utilisateur spécifique à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’id de l’utilisateur source authentifié qui demande de mettre en sourdine l’utilisateur cible.
</ParamField>

<ParamField path="body.body" type="MuteUserRequest">
Corps de la requête
</ParamField>

<div id="pin_list">
  ### `pin_list`
</div>

Épingler une Liste
Permet à l’utilisateur authentifié d’épingler une Liste spécifique à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’id de l’utilisateur source authentifié qui épinglera la Liste.
</ParamField>

<ParamField path="body.body" type="PinListRequest">
Corps de la requête
</ParamField>

<div id="repost_post">
  ### `repost_post`
</div>

Reposter une publication
Permet à l’utilisateur authentifié de reposter une publication spécifique à l’aide de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur source authentifié qui demande à republier la Publication.
</ParamField>

<ParamField path="body.body" type="RepostPostRequest">
Corps de la requête
</ParamField>

<div id="search">
  ### `search`
</div>

Recherche d’utilisateurs
Renvoie une liste d’utilisateurs correspondant à une requête de recherche.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.query" type="Any">
La chaîne de requête à utiliser pour rechercher des utilisateurs.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir la « page » suivante de résultats. La valeur utilisée avec ce paramètre est extraite directement de la réponse fournie par l’API et ne doit pas être modifiée.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User séparés par des virgules à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à développer, séparés par des virgules.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet séparés par des virgules à afficher.
</ParamField>

#### Renvoie

`IteratorSearchResponse`

<div id="unblock_dms">
  ### `unblock_dms`
</div>

Débloquer les messages privés
Débloque les messages privés vers ou en provenance d’un utilisateur spécifique, identifié par son id, pour l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L'id de l'utilisateur cible dont l'utilisateur authentifié demande le déblocage des messages privés.
</ParamField>

#### Renvoie

`UnblockDmsResponse` - données de la réponse

<div id="unfollow_list">
  ### `unfollow_list`
</div>

Se désabonner d’une Liste
Fait en sorte que l’utilisateur authentifié se désabonne d’une Liste spécifique par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur source authentifié qui se désabonnera de la liste.
</ParamField>

<ParamField path="path.list_id" type="Any">
L’ID de la liste dont il faut se désabonner.
</ParamField>

<div id="returns">
  #### Valeur de retour
</div>

`UnfollowListResponse` - données de réponse

<div id="unfollow_user">
  ### `unfollow_user`
</div>

Se désabonner d’un utilisateur
Fait en sorte que l’utilisateur authentifié cesse de suivre un utilisateur spécifique identifié par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.source_user_id" type="Any">
L’identifiant de l’Utilisateur source authentifié qui demande à ne plus suivre l’Utilisateur cible.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
L’identifiant de l’Utilisateur que l’Utilisateur source demande à ne plus suivre.
</ParamField>

#### Renvoie

`UnfollowUserResponse` - Données de la réponse

<div id="unlike_post">
  ### `unlike_post`
</div>

Retirer un J’aime d’une Publication
Permet à l’utilisateur authentifié de retirer son J’aime d’une Publication spécifique identifiée par son ID.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’Utilisateur source authentifié qui demande à retirer son « J’aime » de la publication.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
L’ID de la publication pour laquelle l’Utilisateur demande à retirer son « J’aime ».
</ParamField>

#### Renvoie

`UnlikePostResponse` - données de la réponse

<div id="unmute_user">
  ### `unmute_user`
</div>

Ne plus masquer un utilisateur
Fait en sorte que l’utilisateur authentifié ne masque plus un utilisateur spécifique par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.source_user_id" type="Any">
L’identifiant de l’utilisateur source authentifié qui demande d’annuler la mise en sourdine de l’utilisateur cible.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
L’identifiant de l’utilisateur pour lequel l’utilisateur source demande l’annulation de la mise en sourdine.
</ParamField>

#### Renvoie

`UnmuteUserResponse` - Données de la réponse

<div id="unpin_list">
  ### `unpin_list`
</div>

Retirer l’épinglage d’une List
Permet à l’utilisateur authentifié de retirer l’épinglage d’une List spécifique à partir de son id.

#### Parameters

<ParamField path="path.id" type="Any">
L’identifiant de l’utilisateur source authentifié pour lequel renvoyer les résultats.
</ParamField>

<ParamField path="path.list_id" type="Any">
L’identifiant de la liste à détacher.
</ParamField>

#### Renvoie

`UnpinListResponse` - Données de la réponse

<div id="unrepost_post">
  ### `unrepost_post`
</div>

Annuler la republication d’une Publication
Annule la republication, par l’utilisateur authentifié, d’une Publication spécifique par son ID.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de l’utilisateur source authentifié qui demande à repartager la Publication.
</ParamField>

<ParamField path="path.source_tweet_id" type="Any">
L’ID de la Publication pour laquelle l’utilisateur demande l’annulation du retweet.
</ParamField>

#### Renvoie

`UnrepostPostResponse` - données de réponse