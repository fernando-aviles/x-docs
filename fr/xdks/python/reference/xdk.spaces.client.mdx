---
title: "SpacesClient"
sidebarTitle: "SpacesClient"
---

<div id="spacesclient">
  ## SpacesClient
</div>

<Badge color="blue">Classe</Badge>

<Badge color="gray">Bases : object</Badge>

Client pour les opérations sur les Spaces

<div id="constructors">
  ## Constructeurs
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_buyers">
  ### `get_buyers`
</div>

Récupérer les acheteurs de billets d’un Space
Renvoie la liste des Utilisateurs qui ont acheté des billets pour un Space spécifique, identifié par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="str">
L’identifiant du Space à récupérer.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre permet d’obtenir une « page » de résultats donnée.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs d’utilisateur à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste, séparée par des virgules, de champs à développer.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs de Tweet à afficher.
</ParamField>

#### Renvoie

`IteratorGetBuyersResponse`

<div id="get_by_creator_ids">
  ### `get_by_creator_ids`
</div>

Obtenir des Spaces par identifiants de créateur
Récupère les détails des Spaces créés par les utilisateurs dont les identifiants sont spécifiés.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.user_ids" type="List">
Les identifiants des utilisateurs à parcourir.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Une liste de champs Space, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à étendre.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Une liste de champs Topic, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`GetByCreatorIdsResponse` - Données de la réponse

<div id="get_by_id">
  ### `get_by_id`
</div>

Récupérer un Space par id
Récupère les détails d’un Space spécifique à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="str">
L'identifiant de l'Espace à récupérer.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Une liste de champs Space, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à étendre (expansions), séparés par des virgules.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Une liste de champs Topic, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`GetByIdResponse` - Données de la réponse

<div id="get_by_ids">
  ### `get_by_ids`
</div>

Obtenir des Spaces par leurs id
Récupère les détails de plusieurs Spaces à partir de leurs id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.ids" type="List">
La liste des id de Space à retourner.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Une liste, séparée par des virgules, des champs de Space à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste, séparée par des virgules, des champs à étendre (expansions).
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste, séparée par des virgules, des champs d’utilisateur à afficher.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Une liste, séparée par des virgules, des champs de Topic à afficher.
</ParamField>

<div id="returns">
  #### Valeur de retour
</div>

`GetByIdsResponse` - données de la réponse

<div id="get_posts">
  ### `get_posts`
</div>

Obtenir les publications d’un Space
Récupère une liste de publications partagées dans un Space spécifique à partir de son ID.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="str">
L'identifiant de l'Espace à récupérer.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre de Publications à récupérer à partir de l'Espace fourni. Si ce paramètre n'est pas renseigné, la valeur par défaut sera 100 (maximum).
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet à afficher, séparés par des virgules.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à étendre, séparés par des virgules.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs Media à afficher, séparés par des virgules.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Une liste de champs Poll à afficher, séparés par des virgules.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User à afficher, séparés par des virgules.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Une liste de champs Place à afficher, séparés par des virgules.
</ParamField>

<div id="returns">
  #### Valeur de retour
</div>

`GetPostsResponse` - données de la réponse

<div id="search">
  ### `search`
</div>

Recherche de Spaces
Renvoie une liste de Spaces correspondant à la requête de recherche spécifiée.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.query" type="str">
La requête de recherche.
</ParamField>

<ParamField path="path.state" type="str or None" default="None">
L’état des Spaces à rechercher.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre de résultats à renvoyer.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Une liste de champs Space, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à étendre.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Une liste de champs Topic, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`SearchResponse` - données de la réponse