---
title: "SpacesClient"
sidebarTitle: "SpacesClient"
---

<div id="spacesclient">
  ## SpacesClient
</div>

<Badge color="blue">Classe</Badge>

<Badge color="gray">Bases : object</Badge>

Client pour les opérations relatives aux Spaces

<div id="constructors">
  ## Constructeurs
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_buyers">
  ### `get_buyers`
</div>

Récupérer les acheteurs de billets d’un Space
Récupère la liste des utilisateurs qui ont acheté des billets pour un Space donné, à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="str">
L’identifiant de l’Espace à récupérer.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir une « page » spécifique de résultats.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs d’utilisateur séparés par des virgules à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à étendre, séparés par des virgules.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs de Tweet séparés par des virgules à afficher.
</ParamField>

<div id="returns">
  #### Retourne
</div>

`IteratorGetBuyersResponse`

<div id="get_by_creator_ids">
  ### `get_by_creator_ids`
</div>

Obtenir des Spaces par identifiants de créateur
Récupère les détails des Spaces créés par les utilisateurs dont les identifiants sont spécifiés.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.user_ids" type="List">
Les identifiants des utilisateurs à utiliser pour la recherche.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Une liste de champs Space, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à développer.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Une liste de champs Topic, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`GetByCreatorIdsResponse` - Données de la réponse

<div id="get_by_id">
  ### `get_by_id`
</div>

Récupérer un Space par ID
Récupère les détails d’un Space spécifique à partir de son ID.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="str">
L’identifiant du Space à récupérer.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Space à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste, séparée par des virgules, de champs à développer.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs User à afficher.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Topic à afficher.
</ParamField>

#### Renvoie

`GetByIdResponse` - Données de la réponse

<div id="get_by_ids">
  ### `get_by_ids`
</div>

Récupérer des Spaces par id
Récupère les détails de plusieurs Spaces à partir de leurs id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.ids" type="List">
La liste des ID de Space à renvoyer.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Space à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste, séparée par des virgules, de champs à développer.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs User à afficher.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Topic à afficher.
</ParamField>

#### Renvoie

`GetByIdsResponse` - Données de la réponse

<div id="get_posts">
  ### `get_posts`
</div>

Obtenir les Publications d’un Space
Récupère la liste des Publications partagées dans un Space donné à partir de son identifiant.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="str">
L’identifiant du Space à récupérer.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre de Publications à récupérer à partir du Space fourni. S’il n’est pas fourni, la valeur prendra par défaut la valeur maximale de 100.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs de Tweet, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à développer, séparés par des virgules.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs de média, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Une liste de champs de sondage, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs d’utilisateur, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Une liste de champs de lieu, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`GetPostsResponse` - Données de la réponse

<div id="search">
  ### `search`
</div>

Rechercher des Spaces
Récupère une liste de Spaces correspondant à la requête de recherche spécifiée.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.query" type="str">
La requête de recherche.
</ParamField>

<ParamField path="path.state" type="str or None" default="None">
L’état des Spaces à rechercher.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre de résultats à retourner.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Space à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste, séparée par des virgules, de champs à étendre.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs User à afficher.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Topic à afficher.
</ParamField>

#### Renvoie

`SearchResponse` - Données de la réponse