---
title: "SpacesClient"
sidebarTitle: "SpacesClient"
---

<div id="spacesclient">
  ## SpacesClient
</div>

<Badge color="blue">Classe</Badge>

<Badge color="gray">Bases : object</Badge>

Client pour les opérations relatives aux Spaces

<div id="constructors">
  ## Constructeurs
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_buyers">
  ### `get_buyers`
</div>

Obtenir les acheteurs de billets d’un Space
Récupère la liste des utilisateurs qui ont acheté des billets pour un Space donné, identifié par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="str">
L’identifiant du Space à récupérer.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre permet d’obtenir une « page » précise de résultats.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à développer.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

<div id="returns">
  #### Renvoie
</div>

`IteratorGetBuyersResponse`

<div id="get_by_creator_ids">
  ### `get_by_creator_ids`
</div>

Obtenir les Spaces par id de créateur
Récupère les détails des Spaces créés par les id des utilisateurs spécifiés.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.user_ids" type="List">
Les ID des utilisateurs dans lesquels effectuer la recherche.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Une liste de champs Space séparés par des virgules à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs séparés par des virgules à étendre (expansions).
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User séparés par des virgules à afficher.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Une liste de champs Topic séparés par des virgules à afficher.
</ParamField>

<div id="returns">
  #### Renvoie
</div>

`GetByCreatorIdsResponse` - Données de la réponse

<div id="get_by_id">
  ### `get_by_id`
</div>

Récupérer un Space par id
Récupère les détails d'un Space spécifique à partir de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="str">
L’identifiant du Space à récupérer.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Une liste de champs Space à afficher, séparés par des virgules.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à étendre, séparés par des virgules.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User à afficher, séparés par des virgules.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Une liste de champs Topic à afficher, séparés par des virgules.
</ParamField>

<div id="returns">
  #### Renvoie
</div>

`GetByIdResponse` - Données de la réponse

<div id="get_by_ids">
  ### `get_by_ids`
</div>

Récupérer des Spaces par id
Récupère les détails de plusieurs Spaces à partir de leurs id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.ids" type="List">
La liste des ID de Space à renvoyer.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Une liste de champs Space, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs à étendre, séparés par des virgules.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Une liste de champs Topic, séparés par des virgules, à afficher.
</ParamField>

<div id="returns">
  #### Renvoie
</div>

`GetByIdsResponse` - Données de la réponse

<div id="get_posts">
  ### `get_posts`
</div>

Obtenir les Publications d’un Space
Récupère une liste de Publications partagées dans un Space spécifique, identifié par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="str">
L’identifiant du Space à récupérer.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre de Publications à récupérer à partir du Space fourni. S’il n’est pas fourni, la valeur sera par défaut fixée à 100 (maximum).
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs de Tweet à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste, séparée par des virgules, de champs à étendre.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Media à afficher.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Poll à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs User à afficher.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Place à afficher.
</ParamField>

<div id="returns">
  #### Renvoie
</div>

`GetPostsResponse` - données de la réponse

<div id="search">
  ### `search`
</div>

Recherche de Spaces
Renvoie une liste de Spaces correspondant à la requête de recherche fournie.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.query" type="str">
La requête de recherche.
</ParamField>

<ParamField path="path.state" type="str or None" default="None">
L&apos;état des Spaces à rechercher.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre de résultats à renvoyer.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Une liste de champs Space, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à étendre.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Une liste de champs Topic, séparés par des virgules, à afficher.
</ParamField>

<div id="returns">
  #### Renvoie
</div>

`SearchResponse` - Données de la réponse