---
title: "DirectMessagesClient"
sidebarTitle: "DirectMessagesClient"
---

<div id="directmessagesclient">
  ## DirectMessagesClient
</div>

<Badge color="blue">Classe</Badge>

<Badge color="gray">Bases : object</Badge>

Client pour les opérations sur les messages privés

<div id="constructors">
  ## Constructeurs
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_by_conversation_id">
  ### `create_by_conversation_id`
</div>

Créer un message privé à partir de l’id de conversation
Envoie un nouveau message privé dans une conversation spécifique identifiée par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.dm_conversation_id" type="str">
L'id de la conversation de DM.
</ParamField>

<ParamField path="body.body" type="CreateByConversationIdRequest">
Corps de la requête
</ParamField>

<div id="create_by_participant_id">
  ### `create_by_participant_id`
</div>

Créer un message privé par id de participant
Envoie un nouveau message privé à un participant spécifique, identifié par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.participant_id" type="Any">
L’id de l’utilisateur destinataire du DM.
</ParamField>

<ParamField path="body.body" type="CreateByParticipantIdRequest">
Corps de la requête
</ParamField>

<div id="create_conversation">
  ### `create_conversation`
</div>

Créer une conversation de messages privés
Lance une nouvelle conversation de messages privés avec les participants spécifiés.
body: Corps de la requête
:returns: Données de la réponse
:rtype: CreateConversationResponse

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="body.body" type="CreateConversationRequest"></ParamField>

<div id="delete_events">
  ### `delete_events`
</div>

Supprimer un événement de message privé
Supprime un événement de message privé spécifique en fonction de son id, s’il appartient à l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.event_id" type="Any">
L'identifiant de l'événement de message privé à supprimer.
</ParamField>

<div id="returns">
  #### Valeur de retour
</div>

`DeleteEventsResponse` - données de la réponse

<div id="get_events">
  ### `get_events`
</div>

Obtenir les événements de messages privés
Renvoie une liste des événements de messages privés récents pour l’ensemble des conversations.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre permet d’obtenir une « page » spécifique de résultats.
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
L’ensemble des event_types à inclure dans les résultats.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Une liste de champs DmEvent, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à étendre.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs Media, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`IteratorGetEventsResponse`

<div id="get_events_by_conversation_id">
  ### `get_events_by_conversation_id`
</div>

Obtenir les événements de messages directs pour une conversation de messages directs
Récupère les événements de messages directs pour une conversation donnée.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’ID de la conversation de DM.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir une « page » spécifique de résultats.
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
L’ensemble des event_types à inclure dans les résultats.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs DmEvent à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste, séparée par des virgules, de champs à étendre.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Media à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs User à afficher.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Tweet à afficher.
</ParamField>

#### Renvoie

`IteratorGetEventsByConversationIdResponse`

<div id="get_events_by_id">
  ### `get_events_by_id`
</div>

Récupérer un événement de message privé par id
Récupère les détails d’un événement de message privé spécifique identifié par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.event_id" type="Any">
ID de l’événement de DM.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs DmEvent à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste, séparée par des virgules, de champs à étendre.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Media à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs User à afficher.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Tweet à afficher.
</ParamField>

#### Renvoie

`GetEventsByIdResponse` - Données de la réponse

<div id="get_events_by_participant_id">
  ### `get_events_by_participant_id`
</div>

Récupère les événements de messages directs pour une conversation de messages directs.
Récupère les événements de messages directs pour une conversation spécifique.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.participant_id" type="Any">
L’identifiant de l’utilisateur participant pour la conversation de messages privés (DM) en tête-à-tête.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir une « page » spécifique de résultats.
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
L’ensemble des <code>event_types</code> à inclure dans les résultats.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Une liste séparée par des virgules des champs <code>DmEvent</code> à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste séparée par des virgules des champs à étendre.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste séparée par des virgules des champs <code>Media</code> à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste séparée par des virgules des champs <code>User</code> à afficher.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste séparée par des virgules des champs <code>Tweet</code> à afficher.
</ParamField>

#### Renvoie

`IteratorGetEventsByParticipantIdResponse`