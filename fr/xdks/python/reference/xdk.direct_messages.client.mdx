---
title: "DirectMessagesClient"
sidebarTitle: "DirectMessagesClient"
---

<div id="directmessagesclient">
  ## DirectMessagesClient
</div>

<Badge color="blue">Classe</Badge>

<Badge color="gray">Base : object</Badge>

Client pour la gestion des messages privés

<div id="constructors">
  ## Constructeurs
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_by_conversation_id">
  ### `create_by_conversation_id`
</div>

Créer un message privé par id de conversation
Envoie un nouveau message privé dans une conversation spécifique en se servant de son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.dm_conversation_id" type="str">
L’ID de la conversation de DM.
</ParamField>

<ParamField path="body.body" type="CreateByConversationIdRequest">
Corps de la requête
</ParamField>

<div id="create_by_participant_id">
  ### `create_by_participant_id`
</div>

Créer un message privé par id de participant
Envoie un nouveau message privé à un participant spécifique en utilisant son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.participant_id" type="Any">
L’identifiant de l’utilisateur qui recevra le DM.
</ParamField>

<ParamField path="body.body" type="CreateByParticipantIdRequest">
Corps de la requête
</ParamField>

<div id="create_conversation">
  ### `create_conversation`
</div>

Créer une conversation de messages directs
Crée une nouvelle conversation de messages directs avec les participants spécifiés.
body: Corps de la requête
:returns: Données de la réponse
:rtype: CreateConversationResponse

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="body.body" type="CreateConversationRequest"></ParamField>

<div id="delete_events">
  ### `delete_events`
</div>

Supprimer un événement de MD
Supprime un événement de message direct spécifique par son id, s’il appartient à l’utilisateur authentifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.event_id" type="Any">
L’identifiant de l’événement de message privé à supprimer.
</ParamField>

<div id="returns">
  #### Valeur de retour
</div>

`DeleteEventsResponse` - Données de réponse

<div id="get_events">
  ### `get_events`
</div>

Récupérer les événements de messages directs
Récupère la liste des événements récents de messages directs pour l'ensemble des conversations.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre permet d’obtenir une « page » spécifique de résultats.
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
L’ensemble des valeurs event_types à inclure dans les résultats.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs DmEvent à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste, séparée par des virgules, de champs à étendre.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Media à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs User à afficher.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste, séparée par des virgules, de champs Tweet à afficher.
</ParamField>

#### Renvoie

`IteratorGetEventsResponse`

<div id="get_events_by_conversation_id">
  ### `get_events_by_conversation_id`
</div>

Obtenir les événements de messages privés pour une conversation de messages privés
Récupère les événements de messages privés d’une conversation spécifique.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L&#39;identifiant de la conversation DM.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir une « page » spécifique de résultats.
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
L&#39;ensemble des event_types à inclure dans les résultats.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Une liste de champs DmEvent, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à développer.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs Media, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`IteratorGetEventsByConversationIdResponse`

<div id="get_events_by_id">
  ### `get_events_by_id`
</div>

Récupérer un événement de message direct par identifiant
Récupère les détails d’un événement de message direct spécifique à partir de son identifiant.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.event_id" type="Any">
ID de l’événement DM.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Une liste de champs DmEvent, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à développer.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs Media, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`GetEventsByIdResponse` - Données de la réponse

<div id="get_events_by_participant_id">
  ### `get_events_by_participant_id`
</div>

Obtenir les événements de messages privés (DM) pour une conversation donnée
Récupère les événements de messages privés pour une conversation spécifique.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.participant_id" type="Any">
L&#39;id de l&#39;utilisateur participant à la conversation de DM en tête-à-tête.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Le nombre maximal de résultats.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir une « page » spécifique de résultats.
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
L&#39;ensemble des event_types à inclure dans les résultats.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Une liste de champs DmEvent, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Une liste de champs, séparés par des virgules, à développer.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs Media, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Une liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Une liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`IteratorGetEventsByParticipantIdResponse`