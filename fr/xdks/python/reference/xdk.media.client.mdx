---
title: "MediaClient"
sidebarTitle: "MediaClient"
---

<div id="mediaclient">
  ## MediaClient
</div>

<Badge color="blue">Classe</Badge>

<Badge color="gray">Bases : object</Badge>

Client pour les opérations média

<div id="constructors">
  ## Constructeurs
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="append_upload">
  ### `append_upload`
</div>

Ajouter à un envoi Media
Ajoute des data à la requête d'envoi Media.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
Identifiant du média pour l’opération d’ajout.
</ParamField>

<ParamField path="body.body" type="AppendUploadRequest">
Corps de la requête
</ParamField>

<div id="create_metadata">
  ### `create_metadata`
</div>

Créer les métadonnées Media
Crée les métadonnées d’un fichier Media.
body: Corps de la requête
:returns: données de la réponse
:rtype: CreateMetadataResponse

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="body.body" type="CreateMetadataRequest"></ParamField>

<div id="create_subtitles">
  ### `create_subtitles`
</div>

Créer des sous-titres pour des Media
Crée des sous-titres pour un fichier Media spécifique.
body: Corps de la requête
:returns: données de la réponse
:rtype: CreateSubtitlesResponse

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="body.body" type="CreateSubtitlesRequest"></ParamField>

<div id="delete_subtitles">
  ### `delete_subtitles`
</div>

Supprimer les sous-titres Media
Supprime les sous-titres d’un Media spécifique.
body: Corps de la requête
:returns: data de la Réponse
:rtype: DeleteSubtitlesResponse

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="body.body" type="DeleteSubtitlesRequest"></ParamField>

<div id="finalize_upload">
  ### `finalize_upload`
</div>

Finaliser le téléversement de médias
Finalise une requête de téléversement de médias.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L'identifiant du média ciblé à finaliser.
</ParamField>

<div id="returns">
  #### Renvoie
</div>

`FinalizeUploadResponse` - données de la réponse

<div id="get_analytics">
  ### `get_analytics`
</div>

Obtenir les statistiques des médias
Récupère les données d’analyse pour les médias.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.media_keys" type="List">
Une liste de Media Keys séparés par des virgules. Jusqu’à 100 sont autorisés dans une seule requête.
</ParamField>

<ParamField path="path.end_time" type="str">
YYYY-MM-DDTHH:mm:ssZ. L’horodatage UTC qui représente la fin de l’intervalle de temps.
</ParamField>

<ParamField path="path.start_time" type="str">
YYYY-MM-DDTHH:mm:ssZ. L’horodatage UTC qui représente le début de l’intervalle de temps.
</ParamField>

<ParamField path="path.granularity" type="str">
La granularité des résultats de décompte de recherche.
</ParamField>

<ParamField path="path.media_analytics_fields" type="List or None" default="None">
Une liste de champs MediaAnalytics séparés par des virgules à afficher.
</ParamField>

<div id="returns">
  #### Renvoie
</div>

`GetAnalyticsResponse` - données de la réponse

<div id="get_by_key">
  ### `get_by_key`
</div>

Obtenir un objet Media par clé de média
Récupère les détails d’un fichier Media spécifique à partir de sa clé de média.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.media_key" type="Any">
Une seule Media Key.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs Media séparés par des virgules à afficher.
</ParamField>

<div id="returns">
  #### Renvoie
</div>

`GetByKeyResponse` - données de la réponse

<div id="get_by_keys">
  ### `get_by_keys`
</div>

Obtenir des médias par clés média
Récupère les détails des fichiers média à partir de leurs clés média.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.media_keys" type="List">
Une liste de Media Keys séparées par des virgules. Jusqu'à 100 sont autorisées dans une seule requête.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Une liste de champs Media séparés par des virgules à afficher.
</ParamField>

<div id="returns">
  #### Renvoie
</div>

`GetByKeysResponse` - données de la réponse

<div id="get_upload_status">
  ### `get_upload_status`
</div>

Obtenir le statut du téléversement de média
Récupère le statut d’un téléversement de média par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.media_id" type="Any">
id du média pour le statut de chargement du média demandé.
</ParamField>

<ParamField path="path.command" type="str or None" default="None">
Commande associée à la requête de chargement de média.
</ParamField>

<div id="returns">
  #### Renvoie
</div>

`GetUploadStatusResponse` - données de la réponse

<div id="initialize_upload">
  ### `initialize_upload`
</div>

Initialiser le téléversement de média
Initialise un téléversement de média.
body: Corps de la requête
:returns: Données de la réponse
:rtype: InitializeUploadResponse

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="body.body" type="InitializeUploadRequest"></ParamField>

<div id="upload">
  ### `upload`
</div>

Téléverser un média
Téléverse un fichier média à utiliser dans des publications ou d'autres contenus.
body: Corps de la requête
:returns: données de la réponse
:rtype: UploadResponse

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="body.body" type="UploadRequest"></ParamField>