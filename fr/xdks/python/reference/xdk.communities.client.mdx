---
title: "CommunitiesClient"
sidebarTitle: "CommunitiesClient"
---

<div id="communitiesclient">
  ## CommunitiesClient
</div>

<Badge color="blue">Classe</Badge>

<Badge color="gray">Bases : object</Badge>

Client pour les opérations sur les communautés

<div id="constructors">
  ## Constructeurs
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_by_id">
  ### `get_by_id`
</div>

Récupérer une Community par ID
Récupère les détails d'une Community donnée à partir de son ID.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L'identifiant de la Community.
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
Une liste de champs Community séparés par des virgules à afficher.
</ParamField>

#### Renvoie

`GetByIdResponse` - Données de la réponse

<div id="search">
  ### `search`
</div>

Rechercher des communautés
Renvoie une liste de communautés qui correspondent à la requête de recherche spécifiée.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.query" type="str">
Chaîne de requête pour rechercher des communautés.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Nombre maximal de résultats de recherche à renvoyer dans une requête.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir la « page » suivante de résultats. La valeur utilisée avec ce paramètre est extraite directement de la Réponse fournie par l’API et ne doit pas être modifiée.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir la « page » suivante de résultats. La valeur utilisée avec ce paramètre est extraite directement de la Réponse fournie par l’API et ne doit pas être modifiée.
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
Une liste de champs Community, séparés par des virgules, à afficher.
</ParamField>

<div id="returns">
  #### Valeur de retour
</div>

`IteratorSearchResponse`