---
title: "CommunitiesClient"
sidebarTitle: "CommunitiesClient"
---

<div id="communitiesclient">
  ## CommunitiesClient
</div>

<Badge color="blue">Classe</Badge>

<Badge color="gray">Bases : object</Badge>

Client pour les opérations relatives aux communautés

<div id="constructors">
  ## Constructeurs
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_by_id">
  ### `get_by_id`
</div>

Récupérer une communauté par id
Récupère les détails d'une communauté spécifique par son id.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.id" type="Any">
L’identifiant de la Community.
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
Une liste de champs Community, séparés par des virgules, à afficher.
</ParamField>

<div id="returns">
  #### Renvoie
</div>

`GetByIdResponse` - données de la réponse

<div id="search">
  ### `search`
</div>

Recherche de communautés
Renvoie une liste de communautés correspondant à la requête de recherche spécifiée.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.query" type="str">
Requête de recherche de communautés.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Nombre maximal de résultats de recherche à retourner par une requête.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir la « page » suivante de résultats. La valeur utilisée avec le paramètre est directement extraite de la réponse fournie par l’API et ne doit pas être modifiée.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Ce paramètre est utilisé pour obtenir la « page » suivante de résultats. La valeur utilisée avec le paramètre est directement extraite de la réponse fournie par l’API et ne doit pas être modifiée.
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
Liste de champs Community, séparée par des virgules, à afficher.
</ParamField>

<div id="returns">
  #### Renvoie
</div>

`IteratorSearchResponse`