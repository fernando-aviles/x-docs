---
title: "WebhooksClient"
sidebarTitle: "WebhooksClient"
---

<div id="webhooksclient">
  ## WebhooksClient
</div>

<Badge color="blue">Classe</Badge>

<Badge color="gray">Bases : object</Badge>

Client pour les opérations liées aux webhooks

<div id="constructors">
  ## Constructeurs
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

Créer un webhook
Crée une nouvelle configuration de webhook.
body: Corps de la requête
:returns: Données de la réponse
:rtype: CreateResponse

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="create_stream_link">
  ### `create_stream_link`
</div>

Créer un lien de flux
Crée un lien pour acheminer les événements FilteredStream vers le webhook spécifié.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.webhook_id" type="Any">
L’ID du webhook à associer à votre jeu de règles FilteredStream.
</ParamField>

<ParamField path="path.tweet_fields" type="str or None" default="None">
Liste de champs Tweet, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.expansions" type="str or None" default="None">
Liste de champs, séparés par des virgules, à étendre.
</ParamField>

<ParamField path="path.media_fields" type="str or None" default="None">
Liste de champs Media, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.poll_fields" type="str or None" default="None">
Liste de champs Poll, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.user_fields" type="str or None" default="None">
Liste de champs User, séparés par des virgules, à afficher.
</ParamField>

<ParamField path="path.place_fields" type="str or None" default="None">
Liste de champs Place, séparés par des virgules, à afficher.
</ParamField>

#### Renvoie

`CreateStreamLinkResponse` - Données de la réponse

<div id="create_webhook_replay_job">
  ### `create_webhook_replay_job`
</div>

Créer une tâche de relecture pour le webhook
Crée une tâche de relecture pour récupérer les événements des 24 dernières heures pour tous les événements envoyés ou dont l’envoi a été tenté vers le webhook.
body: Corps de la requête
:returns: données de la Réponse
:rtype: CreateWebhookReplayJobResponse

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="body.body" type="CreateWebhookReplayJobRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

Supprimer un webhook
Supprime la configuration d’un webhook existant.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.webhook_id" type="Any">
L’ID du webhook à supprimer.
</ParamField>

#### Renvoie

`DeleteResponse` - données de réponse

<div id="delete_stream_link">
  ### `delete_stream_link`
</div>

Supprimer le lien de flux
Supprime le lien entre les événements FilteredStream et le webhook indiqué.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.webhook_id" type="Any">
L’ID du webhook à associer à votre jeu de règles FilteredStream.
</ParamField>

<div id="returns">
  #### Valeur de retour
</div>

`DeleteStreamLinkResponse` - Données de la réponse

<div id="get">
  ### `get`
</div>

Récupérer un webhook
Récupérer une liste de configurations de webhook associées à une application cliente.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.webhook_config_fields" type="List or None" default="None">
Une liste de champs WebhookConfig séparés par des virgules à afficher.
</ParamField>

#### Renvoie

`GetResponse` - données de la réponse

<div id="get_stream_links">
  ### `get_stream_links`
</div>

Obtenir les liens de flux
Récupère une liste de liens webhook associés à un jeu de règles de flux filtré.
:returns: Données de la Réponse
:rtype: GetStreamLinksResponse

#### Renvoie

`GetStreamLinksResponse`

<div id="validate">
  ### `validate`
</div>

Valide le webhook
Déclenche un contrôle CRC pour un webhook donné.

<div id="parameters">
  #### Paramètres
</div>

<ParamField path="path.webhook_id" type="Any">
L’identifiant du webhook à vérifier.
</ParamField>

#### Valeur renvoyée

`ValidateResponse` - Données de la réponse