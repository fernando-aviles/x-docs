---
title: "PostsClient"
sidebarTitle: "PostsClient"
---

<div id="submodules">
  ## उप-मॉड्यूल
</div>

* [xdk.posts.client मॉड्यूल](/hi/xdks/python/reference/xdk.posts.client)
  * [`PostsClient`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient)
    * [`PostsClient.__init__()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.__init__)
    * [`PostsClient.create()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.create)
    * [`PostsClient.delete()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.delete)
    * [`PostsClient.get_analytics()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_analytics)
    * [`PostsClient.get_by_id()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_by_id)
    * [`PostsClient.get_by_ids()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_by_ids)
    * [`PostsClient.get_counts_all()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_counts_all)
    * [`PostsClient.get_counts_recent()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_counts_recent)
    * [`PostsClient.get_insights28hr()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_insights28hr)
    * [`PostsClient.get_insights_historical()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_insights_historical)
    * [`PostsClient.get_liking_users()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_liking_users)
    * [`PostsClient.get_quoted()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_quoted)
    * [`PostsClient.get_reposted_by()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_reposted_by)
    * [`PostsClient.get_reposts()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_reposts)
    * [`PostsClient.hide_reply()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.hide_reply)
    * [`PostsClient.search_all()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.search_all)
    * [`PostsClient.search_recent()`](/hi/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.search_recent)
* [xdk.posts.models मॉड्यूल](/hi/xdks/python/reference/xdk.posts.models)
  * [`CreateRequest`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateRequest)
    * [`CreateRequest.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateRequest.model_config)
  * [`CreateResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateResponse)
    * [`CreateResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateResponse.model_config)
  * [`DeleteResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.DeleteResponse)
    * [`DeleteResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.DeleteResponse.model_config)
  * [`GetAnalyticsResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetAnalyticsResponse)
    * [`GetAnalyticsResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetAnalyticsResponse.model_config)
  * [`GetByIdResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdResponse)
    * [`GetByIdResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdResponse.model_config)
  * [`GetByIdsResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdsResponse)
    * [`GetByIdsResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdsResponse.model_config)
  * [`GetCountsAllResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsAllResponse)
    * [`GetCountsAllResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsAllResponse.model_config)
  * [`GetCountsRecentResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsRecentResponse)
    * [`GetCountsRecentResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsRecentResponse.model_config)
  * [`GetInsights28hrResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsights28hrResponse)
    * [`GetInsights28hrResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsights28hrResponse.model_config)
  * [`GetInsightsHistoricalResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsightsHistoricalResponse)
    * [`GetInsightsHistoricalResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsightsHistoricalResponse.model_config)
  * [`GetLikingUsersResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetLikingUsersResponse)
    * [`GetLikingUsersResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetLikingUsersResponse.model_config)
  * [`GetQuotedResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetQuotedResponse)
    * [`GetQuotedResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetQuotedResponse.model_config)
  * [`GetRepostedByResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostedByResponse)
    * [`GetRepostedByResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostedByResponse.model_config)
  * [`GetRepostsResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostsResponse)
    * [`GetRepostsResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostsResponse.model_config)
  * [`HideReplyRequest`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyRequest)
    * [`HideReplyRequest.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyRequest.model_config)
  * [`HideReplyResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyResponse)
    * [`HideReplyResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyResponse.model_config)
  * [`SearchAllResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchAllResponse)
    * [`SearchAllResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchAllResponse.model_config)
  * [`SearchRecentResponse`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchRecentResponse)
    * [`SearchRecentResponse.model_config`](/hi/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchRecentResponse.model_config)

<div id="module-contents">
  ## मॉड्यूल की सामग्री
</div>

यह मॉड्यूल X API के पोस्ट एंडपॉइंट्स तक पहुँच प्रदान करता है
और पोस्ट से संबंधित सभी कार्यक्षमताओं के लिए मुख्य प्रवेश बिंदु के रूप में कार्य करता है।

<div id="class-xdkpostspostsclient">
  ### `class xdk.posts.PostsClient`
</div>

पोस्ट संचालन के लिए Client

<div id="parameters">
  #### पैरामीटर्स
</div>

<ParamField path="path.client" type="Client" />

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### पैरामीटर्स
</div>

<ParamField path="path.client" type="Client" />

<div id="create">
  ### `create`
</div>

पोस्ट बनाएँ या संपादित करें
प्रमाणीकृत उपयोगकर्ता के लिए नई पोस्ट बनाता है, या edit&#95;options दिए जाने पर किसी मौजूदा पोस्ट को संपादित करता है।
body: अनुरोध बॉडी
:returns: रिस्पॉन्स डेटा
:rtype: CreateResponse

<div id="parameters">
  #### पैरामीटर्स
</div>

<ParamField path="body.body" type="CreateRequest" />

<div id="delete">
  ### `delete`
</div>

पोस्ट हटाएँ
यदि प्रमाणित उपयोगकर्ता उसका स्वामी है, तो यह उसकी ID द्वारा किसी विशिष्ट पोस्ट को हटाता है।

#### पैरामीटर

<ParamField path="path.id" type="Any">
  हटाई जाने वाली पोस्ट की id.
</ParamField>

#### रिटर्न

`DeleteResponse` - रिस्पॉन्स डेटा

<div id="get_analytics">
  ### `get_analytics`
</div>

पोस्ट विश्लेषिकी प्राप्त करें
निर्धारित समयावधि के भीतर निर्दिष्ट पोस्ट्स के लिए विश्लेषिकी डेटा प्राप्त करता है।

#### पैरामीटर

<ParamField path="path.ids" type="List">
  पोस्ट ID की अल्पविराम-से-पृथक सूची। एक अनुरोध में अधिकतम 100 ID की अनुमति है।
</ParamField>

<ParamField path="path.end_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ. समय सीमा के अंत को दर्शाने वाला UTC टाइमस्टैम्प।
</ParamField>

<ParamField path="path.start_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ. समय सीमा की शुरुआत को दर्शाने वाला UTC टाइमस्टैम्प।
</ParamField>

<ParamField path="path.granularity" type="str">
  खोज गणना परिणामों के लिए ग्रैन्युलैरिटी।
</ParamField>

<ParamField path="path.analytics_fields" type="List or None" default="None">
  दिखाए जाने वाले Analytics फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

#### रिटर्न

`GetAnalyticsResponse` - रिस्पॉन्स डेटा

<div id="get_by_id">
  ### `get_by_id`
</div>

ID के आधार पर पोस्ट प्राप्त करें
किसी विशिष्ट पोस्ट का विवरण उसकी ID के आधार पर प्राप्त करता है।

#### पैरामीटर

<ParamField path="path.id" type="Any">
  एकल पोस्ट ID।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Tweet फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Media फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Poll फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले User फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Place फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

<div id="returns">
  #### लौटाता है
</div>

`GetByIdResponse` - रिस्पॉन्स डेटा

<div id="get_by_ids">
  ### `get_by_ids`
</div>

IDs के आधार पर पोस्ट्स प्राप्त करें
कई पोस्ट्स के IDs के आधार पर उनका विवरण प्राप्त करता है।

#### पैरामीटर

<ParamField path="path.ids" type="List">
  अल्पविराम से अलग की गई पोस्ट ID की सूची। एक अनुरोध में अधिकतम 100 की अनुमति है।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Media फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Poll फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Place फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

#### रिटर्न

`GetByIdsResponse` - रिस्पॉन्स डेटा

<div id="get_counts_all">
  ### `get_counts_all`
</div>

सभी पोस्ट्स की संख्या प्राप्त करें
पूर्ण आर्काइव में किसी खोज क्वेरी से मेल खाने वाले पोस्ट्स की संख्या प्राप्त करता है।

#### पैरामीटर

<ParamField path="path.query" type="str">
  पोस्ट्स से मिलान करने के लिए एक query/rule/filter। अधिकतम query लंबाई जानने के लिए [https://t.co/rulelength](https://t.co/rulelength) देखें।
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. सबसे पुराना UTC टाइमस्टैम्प (पिछले 7 दिनों के भीतर) जिससे पोस्ट्स लौटाए जाएँगे। टाइमस्टैम्प सेकंड-स्तर की सटीकता में है और समावेशी है (अर्थात 12:00:01 में मिनट का पहला सेकंड शामिल होता है)।
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. सबसे नया, यानी सबसे हाल का UTC टाइमस्टैम्प, तक जिसके लिए पोस्ट्स लौटाए जाएँगे। टाइमस्टैम्प सेकंड-स्तर की सटीकता में है और अपवर्जी है (अर्थात 12:00:01 मिनट के पहले सेकंड को शामिल नहीं करता)।
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  निर्दिष्ट ID से बड़ी (अर्थात उससे अधिक हाल की) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  निर्दिष्ट ID से छोटी (अर्थात उससे पुरानी) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पेज’ पाने के लिए किया जाता है। इस पैरामीटर के साथ इस्तेमाल किया जाने वाला मान सीधे API द्वारा दिए गए रिस्पॉन्स से लिया जाता है और इसे बदला नहीं जाना चाहिए।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पेज’ पाने के लिए किया जाता है। इस पैरामीटर के साथ इस्तेमाल किया जाने वाला मान सीधे API द्वारा दिए गए रिस्पॉन्स से लिया जाता है और इसे बदला नहीं जाना चाहिए।
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
  खोज गणना परिणामों की ग्रैन्युलैरिटी।
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
  दिखाने के लिए SearchCount फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

#### रिटर्न

`IteratorGetCountsAllResponse`

<div id="get_counts_recent">
  ### `get_counts_recent`
</div>

हाल के पोस्ट्स की संख्या प्राप्त करें
यह खोज क्वेरी से मेल खाने वाले पिछले 7 दिनों के पोस्ट्स की संख्या प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर्स
</div>

<ParamField path="path.query" type="str">
  पोस्ट्स का मिलान करने के लिए एक query/rule/filter। अधिकतम query लंबाई जानने के लिए [https://t.co/rulelength](https://t.co/rulelength) देखें।
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. सबसे पुराना UTC टाइमस्टैम्प (पिछले 7 दिनों में से), जिसके बाद के पोस्ट्स दिए जाएँगे। टाइमस्टैम्प सेकंड-स्तर की ग्रैन्युलैरिटी में है और समावेशी है (यानी 12:00:01 में मिनट का पहला सेकंड शामिल है)।
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. सबसे नया, यानी सबसे हाल का UTC टाइमस्टैम्प, जिसके पहले तक के पोस्ट्स दिए जाएँगे। टाइमस्टैम्प सेकंड-स्तर की ग्रैन्युलैरिटी में है और बहिष्कारी है (यानी 12:00:01 में मिनट का पहला सेकंड शामिल नहीं है)।
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  निर्दिष्ट ID से बड़ी (यानी उससे अधिक हाल की) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  निर्दिष्ट ID से छोटी (यानी उससे पुरानी) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  यह पैरामीटर परिणामों का अगला ‘page’ पाने के लिए इस्तेमाल किया जाता है। इस पैरामीटर के साथ इस्तेमाल किया जाने वाला मान सीधे API द्वारा दिए गए रिस्पॉन्स से लिया जाता है, और इसमें कोई बदलाव नहीं किया जाना चाहिए।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  यह पैरामीटर परिणामों का अगला ‘page’ पाने के लिए इस्तेमाल किया जाता है। इस पैरामीटर के साथ इस्तेमाल किया जाने वाला मान सीधे API द्वारा दिए गए रिस्पॉन्स से लिया जाता है, और इसमें कोई बदलाव नहीं किया जाना चाहिए।
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
  खोज गणना परिणामों की ग्रैन्युलैरिटी।
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
  दिखाने के लिए SearchCount फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

#### रिटर्न

`IteratorGetCountsRecentResponse`

<div id="get_insights28hr">
  ### `get_insights28hr`
</div>

28 घंटे के पोस्ट इनसाइट्स प्राप्त करें
पिछले 28 घंटों में निर्दिष्ट पोस्ट्स के लिए एंगेजमेंट मेट्रिक्स प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर्स
</div>

<ParamField path="path.tweet_ids" type="List">
  28hr मेट्रिक्स के लिए PostIds की सूची।
</ParamField>

<ParamField path="path.granularity" type="str">
  मेट्रिक्स रिस्पॉन्स की ग्रैन्युलैरिटी।
</ParamField>

<ParamField path="path.requested_metrics" type="List">
  ऐतिहासिक अनुरोध के लिए अनुरोध किए गए मेट्रिक्स।
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
  प्रदर्शित की जाने वाली Engagement फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

#### रिटर्न

`GetInsights28hrResponse` - रिस्पॉन्स डेटा

<div id="get_insights_historical">
  ### `get_insights_historical`
</div>

ऐतिहासिक पोस्ट इनसाइट्स प्राप्त करें
यह निर्धारित समय-सीमा के भीतर निर्दिष्ट पोस्ट्स के लिए ऐतिहासिक एंगेजमेंट मेट्रिक्स प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर्स
</div>

<ParamField path="path.tweet_ids" type="List">
  ऐतिहासिक मेट्रिक्स के लिए PostIds की सूची।
</ParamField>

<ParamField path="path.end_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ. समय-सीमा के अंत को दर्शाने वाला UTC टाइमस्टैम्प।
</ParamField>

<ParamField path="path.start_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ. समय-सीमा की शुरुआत को दर्शाने वाला UTC टाइमस्टैम्प।
</ParamField>

<ParamField path="path.granularity" type="str">
  मेट्रिक्स रिस्पॉन्स का ग्रैन्युलैरिटी स्तर।
</ParamField>

<ParamField path="path.requested_metrics" type="List">
  ऐतिहासिक अनुरोध के लिए मांगे गए मेट्रिक्स।
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
  दिखाने के लिए Engagement फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

#### रिटर्न्स

`GetInsightsHistoricalResponse` - रिस्पॉन्स डेटा

<div id="get_liking_users">
  ### `get_liking_users`
</div>

पसंद करने वाले उपयोगकर्ता प्राप्त करें
यह किसी विशिष्ट पोस्ट को उसकी ID के आधार पर पसंद करने वाले उपयोगकर्ताओं की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर्स
</div>

<ParamField path="path.id" type="Any">
  किसी एक पोस्ट की ID।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित करने के लिए फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

#### रिटर्न

`IteratorGetLikingUsersResponse`

<div id="get_quoted">
  ### `get_quoted`
</div>

उद्धृत करने वाली पोस्ट्स प्राप्त करें
यह किसी विशिष्ट पोस्ट को उसके ID के आधार पर उद्धृत करने वाली पोस्ट्स की सूची प्राप्त करता है।

#### पैरामीटर

<ParamField path="path.id" type="Any">
  एक पोस्ट ID।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  लौटाए जाने वाले परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का निर्दिष्ट ‘पृष्ठ’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
  बाहर रखी जाने वाली एंटिटीज़ का सेट (उदा. ‘replies’ या ‘retweets’)।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Media फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Poll फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Place फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<div id="returns">
  #### लौटाता है
</div>

`IteratorGetQuotedResponse`

<div id="get_reposted_by">
  ### `get_reposted_by`
</div>

रीपोस्ट करने वाले उपयोगकर्ता प्राप्त करें
किसी विशिष्ट पोस्ट की ID के आधार पर उसे रीपोस्ट करने वाले उपयोगकर्ताओं की सूची प्राप्त करता है।

#### पैरामीटर

<ParamField path="path.id" type="Any">
  एक पोस्ट ID।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  एक्सपैंड किए जाने वाले फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

#### रिटर्न

`IteratorGetRepostedByResponse`

<div id="get_reposts">
  ### `get_reposts`
</div>

रीपोस्ट्स प्राप्त करें
किसी विशिष्ट पोस्ट की ID के आधार पर, उसे रीपोस्ट करने वाले पोस्ट्स की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर्स
</div>

<ParamField path="path.id" type="Any">
  एक पोस्ट ID।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पृष्ठ’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  दिखाने के लिए Media फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  दिखाने के लिए Poll फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  दिखाने के लिए Place फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

#### रिटर्न

`IteratorGetRepostsResponse`

<div id="hide_reply">
  ### `hide_reply`
</div>

रिप्लाई छिपाएँ
प्रमाणित उपयोगकर्ता की अपनी बातचीत में किसी रिप्लाई को छिपाता है या फिर से दिखाता है।

#### पैरामीटर

<ParamField path="path.tweet_id" type="Any">
  उस रिप्लाई की ID जिसे आप छिपाना या फिर से दिखाना चाहते हैं।
</ParamField>

<ParamField path="body.body" type="HideReplyRequest">
  अनुरोध बॉडी
</ParamField>

<div id="search_all">
  ### `search_all`
</div>

सभी पोस्ट्स खोजें
खोज क्वेरी से मेल खाने वाले पूरे आर्काइव से पोस्ट्स प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर्स
</div>

<ParamField path="path.query" type="str">
  पोस्ट्स का मिलान करने के लिए एक query/rule/filter। अधिकतम query लंबाई जानने के लिए [https://t.co/rulelength](https://t.co/rulelength) देखें।
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. वह सबसे पुराना UTC टाइमस्टैम्प, जिससे पोस्ट्स उपलब्ध कराए जाएंगे। टाइमस्टैम्प सेकंड-स्तर की सटीकता में है और समावेशी है (यानी 12:00:01 में मिनट का पहला सेकंड शामिल है)।
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. वह नवीनतम UTC टाइमस्टैम्प, तक जिसके लिए पोस्ट्स उपलब्ध कराए जाएंगे। टाइमस्टैम्प सेकंड-स्तर की सटीकता में है और बहिष्करणीय है (यानी 12:00:01 में मिनट का पहला सेकंड शामिल नहीं है)।
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  निर्दिष्ट पोस्ट ID से बड़े (अर्थात उससे अधिक नए) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  निर्दिष्ट पोस्ट ID से छोटे (अर्थात उससे पुराने) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  किसी रिक्वेस्ट द्वारा लौटाए जाने वाले खोज परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘page’ प्राप्त करने के लिए किया जाता है। इस पैरामीटर के साथ उपयोग किया जाने वाला मान सीधे API द्वारा दिए गए रिस्पॉन्स से लिया जाता है और इसमें बदलाव नहीं किया जाना चाहिए।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘page’ प्राप्त करने के लिए किया जाता है। इस पैरामीटर के साथ उपयोग किया जाने वाला मान सीधे API द्वारा दिए गए रिस्पॉन्स से लिया जाता है और इसमें बदलाव नहीं किया जाना चाहिए।
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
  परिणाम किस क्रम में लौटाए जाएं।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  दिखाने के लिए Media फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  दिखाने के लिए Poll फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  दिखाने के लिए Place फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

#### रिटर्न

`IteratorSearchAllResponse`

<div id="search_recent">
  ### `search_recent`
</div>

हाल के पोस्ट्स खोजें
यह पिछले 7 दिनों के उन पोस्ट्स को प्राप्त करता है जो किसी खोज क्वेरी से मेल खाते हैं।

<div id="parameters">
  #### पैरामीटर्स
</div>

<ParamField path="path.query" type="str">
  पोस्ट्स से मिलान करने के लिए एक query/rule/filter। अधिकतम query लंबाई जानने के लिए [https://t.co/rulelength](https://t.co/rulelength) देखें।
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. सबसे पुराना UTC टाइमस्टैम्प, जिससे पोस्ट्स उपलब्ध कराए जाएँगे। टाइमस्टैम्प की ग्रैन्युलैरिटी सेकंड स्तर की है और यह समावेशी है (अर्थात 12:00:01 में मिनट का पहला सेकंड शामिल है)।
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. सबसे नया, यानी सबसे हाल का UTC टाइमस्टैम्प, तक जिसके लिए पोस्ट्स उपलब्ध कराए जाएँगे। टाइमस्टैम्प की ग्रैन्युलैरिटी सेकंड स्तर की है और यह बहिष्कारी है (अर्थात 12:00:01 में मिनट का पहला सेकंड शामिल नहीं है)।
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  निर्दिष्ट ID से बड़ी (अर्थात उससे अधिक हाल की) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  निर्दिष्ट ID से छोटी (अर्थात उससे पुरानी) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  किसी अनुरोध द्वारा लौटाए जाने वाले खोज परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘page’ प्राप्त करने के लिए किया जाता है। इस पैरामीटर के साथ उपयोग किया जाने वाला मान सीधे API द्वारा दिए गए रिस्पॉन्स से लिया जाता है और इसे बदला नहीं जाना चाहिए।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘page’ प्राप्त करने के लिए किया जाता है। इस पैरामीटर के साथ उपयोग किया जाने वाला मान सीधे API द्वारा दिए गए रिस्पॉन्स से लिया जाता है और इसे बदला नहीं जाना चाहिए।
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
  वह क्रम जिसमें परिणाम लौटाए जाएँ।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  दिखाने के लिए Media फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  दिखाने के लिए Poll फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  दिखाने के लिए Place फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

#### रिटर्न

`IteratorSearchRecentResponse`