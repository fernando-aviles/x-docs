---
title: "PostsClient"
sidebarTitle: "PostsClient"
---

<div id="postsclient">
  ## PostsClient
</div>

<Badge color="blue">क्लास</Badge>

<Badge color="gray">आधार: object</Badge>

पोस्ट ऑपरेशन्स के लिए Client

<div id="constructors">
  ## कंस्ट्रक्टर
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### पैरामीटर्स
</div>

<ParamField path="path.client" type="Client" />

<div id="create">
  ### `create`
</div>

पोस्ट बनाएँ या संपादित करें
प्रमाणीकृत उपयोगकर्ता के लिए एक नई पोस्ट बनाता है, या यदि edit&#95;options दिए गए हों, तो किसी मौजूदा पोस्ट को संपादित करता है।
body: अनुरोध बॉडी
:returns: रिस्पॉन्स डेटा
:rtype: CreateResponse

#### पैरामीटर

<ParamField path="body.body" type="CreateRequest" />

<div id="delete">
  ### `delete`
</div>

पोस्ट हटाएँ
यदि प्रमाणीकृत उपयोगकर्ता उसका स्वामी है, तो उसकी ID के आधार पर किसी विशिष्ट पोस्ट को हटाता है।

#### पैरामीटर

<ParamField path="path.id" type="Any">
  हटाए जाने वाली पोस्ट ID।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`DeleteResponse` - रिस्पॉन्स डेटा

<div id="get_analytics">
  ### `get_analytics`
</div>

पोस्ट विश्लेषिकी प्राप्त करें
यह निर्धारित समय-सीमा के भीतर निर्दिष्ट पोस्ट्स के लिए विश्लेषिकी डेटा प्राप्त करता है।

#### पैरामीटर

<ParamField path="path.ids" type="List">
  पोस्ट ID की कॉमा-सेपरेटेड सूची। एक ही अनुरोध में अधिकतम 100 की अनुमति है।
</ParamField>

<ParamField path="path.end_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ. समय सीमा के अंत को दर्शाने वाला UTC टाइमस्टैम्प।
</ParamField>

<ParamField path="path.start_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ. समय सीमा की शुरुआत को दर्शाने वाला UTC टाइमस्टैम्प।
</ParamField>

<ParamField path="path.granularity" type="str">
  सर्च काउंट परिणामों के लिए ग्रैन्युलैरिटी।
</ParamField>

<ParamField path="path.analytics_fields" type="List or None" default="None">
  दिखाने के लिए Analytics फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`GetAnalyticsResponse` - रिस्पॉन्स डेटा

<div id="get_by_id">
  ### `get_by_id`
</div>

ID से पोस्ट प्राप्त करें
किसी विशिष्ट पोस्ट का विवरण उसकी ID से प्राप्त करता है।

#### पैरामीटर

<ParamField path="path.id" type="Any">
  एक पोस्ट ID.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तार के लिए फ़ील्ड्स की अल्पविराम से अलग की गई सूची.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  दिखाने के लिए Media फ़ील्ड्स की अल्पविराम से अलग की गई सूची.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  दिखाने के लिए Poll फ़ील्ड्स की अल्पविराम से अलग की गई सूची.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की अल्पविराम से अलग की गई सूची.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  दिखाने के लिए Place फ़ील्ड्स की अल्पविराम से अलग की गई सूची.
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`GetByIdResponse` - रिस्पॉन्स डेटा

<div id="get_by_ids">
  ### `get_by_ids`
</div>

IDs द्वारा पोस्ट्स प्राप्त करें
उनकी IDs के आधार पर कई पोस्ट्स का विवरण प्राप्त करता है।

#### पैरामीटर

<ParamField path="path.ids" type="List">
  पोस्ट IDs की कॉमा से अलग की गई सूची। एक अनुरोध में अधिकतम 100 IDs की अनुमति है।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाए जाने वाले Tweet फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  दिखाए जाने वाले Media फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  दिखाए जाने वाले Poll फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाए जाने वाले User फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  दिखाए जाने वाले Place फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`GetByIdsResponse` - रिस्पॉन्स डेटा

<div id="get_counts_all">
  ### `get_counts_all`
</div>

सभी पोस्ट्स की संख्या प्राप्त करें
पूर्ण अभिलेखागार से खोज क्वेरी से मेल खाने वाली पोस्ट्स की संख्या प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर्स
</div>

<ParamField path="path.query" type="str">
  पोस्ट्स के मिलान के लिए एक query/rule/filter। अधिकतम query लंबाई जानने के लिए [https://t.co/rulelength](https://t.co/rulelength) देखें।
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. सबसे पुराना UTC टाइमस्टैम्प (पिछले 7 दिनों में से), जिससे पोस्ट्स प्रदान किए जाएंगे। टाइमस्टैम्प सेकंड-स्तर की सटीकता में है और समावेशी है (यानी 12:00:01 में मिनट का पहला सेकंड शामिल होता है)।
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. सबसे नया, यानी सबसे हाल का UTC टाइमस्टैम्प, जिस तक पोस्ट्स प्रदान किए जाएंगे। टाइमस्टैम्प सेकंड-स्तर की सटीकता में है और अपवर्जी है (यानी 12:00:01 में मिनट का पहला सेकंड शामिल नहीं होता)।
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  निर्दिष्ट ID से बड़ी (अर्थात उससे अधिक हाल की) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  निर्दिष्ट ID से छोटी (अर्थात उससे पुरानी) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  यह पैरामीटर परिणामों का अगला ‘page’ प्राप्त करने के लिए उपयोग किया जाता है। इस पैरामीटर के साथ इस्तेमाल किया जाने वाला मान सीधे API द्वारा दिए गए response से लिया जाता है और इसे बदला नहीं जाना चाहिए।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  यह पैरामीटर परिणामों का अगला ‘page’ प्राप्त करने के लिए उपयोग किया जाता है। इस पैरामीटर के साथ इस्तेमाल किया जाने वाला मान सीधे API द्वारा दिए गए response से लिया जाता है और इसे बदला नहीं जाना चाहिए।
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
  खोज गणना परिणामों की ग्रैन्युलैरिटी।
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
  दिखाए जाने वाले SearchCount फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`IteratorGetCountsAllResponse`

<div id="get_counts_recent">
  ### `get_counts_recent`
</div>

हाल की पोस्ट्स की संख्या प्राप्त करें
खोज क्वेरी से मेल खाने वाली पिछले 7 दिनों की पोस्ट्स की संख्या प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर्स
</div>

<ParamField path="path.query" type="str">
  पोस्ट्स के मिलान के लिए एक query/rule/filter। अधिकतम query लंबाई जानने के लिए [https://t.co/rulelength](https://t.co/rulelength) देखें।
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. वह सबसे पुराना UTC timestamp (पिछले 7 दिनों में से) जिससे पोस्ट्स प्रदान किए जाएंगे। Timestamp सेकंड-स्तर की सटीकता में है और समावेशी है (अर्थात 12:00:01 में मिनट का पहला सेकंड शामिल होता है)।
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. वह सबसे नया, सबसे हालिया UTC timestamp तक जिसके लिए पोस्ट्स प्रदान किए जाएंगे। Timestamp सेकंड-स्तर की सटीकता में है और अपवर्जी है (अर्थात 12:00:01 में मिनट का पहला सेकंड शामिल नहीं होता)।
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  निर्दिष्ट ID से बड़ी (अर्थात उससे अधिक हालिया) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  निर्दिष्ट ID से छोटी (अर्थात उससे पुरानी) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  यह पैरामीटर परिणामों का अगला ‘page’ प्राप्त करने के लिए उपयोग किया जाता है। इस पैरामीटर के साथ इस्तेमाल किया जाने वाला मान सीधे API द्वारा प्रदान किए गए response से लिया जाता है, और इसमें कोई बदलाव नहीं किया जाना चाहिए।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  यह पैरामीटर परिणामों का अगला ‘page’ प्राप्त करने के लिए उपयोग किया जाता है। इस पैरामीटर के साथ इस्तेमाल किया जाने वाला मान सीधे API द्वारा प्रदान किए गए response से लिया जाता है, और इसमें कोई बदलाव नहीं किया जाना चाहिए।
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
  search counts परिणामों की ग्रैन्युलैरिटी।
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
  दिखाने के लिए SearchCount फ़ील्ड्स की comma-separated सूची।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`IteratorGetCountsRecentResponse`

<div id="get_insights28hr">
  ### `get_insights28hr`
</div>

28 घंटे की पोस्ट अंतर्दृष्टियाँ प्राप्त करें
पिछले 28 घंटों में निर्दिष्ट पोस्ट्स के लिए एंगेजमेंट मेट्रिक्स प्राप्त करता है।

#### पैरामीटर

<ParamField path="path.tweet_ids" type="List">
  28hr मेट्रिक्स के लिए पोस्ट IDs की सूची।
</ParamField>

<ParamField path="path.granularity" type="str">
  मेट्रिक्स रिस्पॉन्स की ग्रैन्युलैरिटी।
</ParamField>

<ParamField path="path.requested_metrics" type="List">
  ऐतिहासिक अनुरोध के लिए अनुरोध किए गए मेट्रिक्स।
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
  दिखाने के लिए Engagement फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`GetInsights28hrResponse` - रिस्पॉन्स डेटा

<div id="get_insights_historical">
  ### `get_insights_historical`
</div>

ऐतिहासिक पोस्ट इनसाइट्स प्राप्त करें
यह निर्धारित समयावधि के भीतर निर्दिष्ट पोस्ट्स के लिए ऐतिहासिक एंगेजमेंट मेट्रिक्स पुनर्प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर्स
</div>

<ParamField path="path.tweet_ids" type="List">
  ऐतिहासिक मेट्रिक्स के लिए पोस्ट IDs की सूची।
</ParamField>

<ParamField path="path.end_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ. समय सीमा के अंत को दर्शाने वाला UTC टाइमस्टैम्प।
</ParamField>

<ParamField path="path.start_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ. समय सीमा की शुरुआत को दर्शाने वाला UTC टाइमस्टैम्प।
</ParamField>

<ParamField path="path.granularity" type="str">
  मेट्रिक्स रिस्पॉन्स की ग्रैन्युलैरिटी।
</ParamField>

<ParamField path="path.requested_metrics" type="List">
  ऐतिहासिक अनुरोध के लिए अनुरोध किए गए मेट्रिक्स।
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
  दिखाने के लिए Engagement फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`GetInsightsHistoricalResponse` - रिस्पॉन्स डेटा

<div id="get_liking_users">
  ### `get_liking_users`
</div>

पसंद करने वाले उपयोगकर्ता प्राप्त करें
यह पोस्ट ID के आधार पर किसी विशिष्ट पोस्ट को पसंद करने वाले उपयोगकर्ताओं की सूची प्राप्त करता है।

#### पैरामीटर

<ParamField path="path.id" type="Any">
  एक पोस्ट id।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले User फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Tweet फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`IteratorGetLikingUsersResponse`

<div id="get_quoted">
  ### `get_quoted`
</div>

उद्धृत पोस्ट्स प्राप्त करें
किसी विशिष्ट पोस्ट को उद्धृत करने वाली पोस्ट्स की एक सूची उसकी ID के आधार पर प्राप्त करता है।

#### पैरामीटर

<ParamField path="path.id" type="Any">
  किसी एक पोस्ट की ID।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  लौटाए जाने वाले परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का एक निर्दिष्ट ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
  शामिल न की जाने वाली इकाइयों का सेट (उदा. ‘replies’ या ‘retweets’)।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाए जाने वाले Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  दिखाए जाने वाले Media फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  दिखाए जाने वाले Poll फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाए जाने वाले User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  दिखाए जाने वाले Place फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`IteratorGetQuotedResponse`

<div id="get_reposted_by">
  ### `get_reposted_by`
</div>

रीपोस्ट करने वाले उपयोगकर्ताओं को प्राप्त करें
यह उसकी ID के आधार पर किसी विशिष्ट पोस्ट को रीपोस्ट करने वाले उपयोगकर्ताओं की सूची प्राप्त करता है।

#### पैरामीटर

<ParamField path="path.id" type="Any">
  किसी एक पोस्ट की ID।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए User फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Tweet फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`IteratorGetRepostedByResponse`

<div id="get_reposts">
  ### `get_reposts`
</div>

रीपोस्ट प्राप्त करें
यह किसी विशेष पोस्ट को उसके id के आधार पर रीपोस्ट करने वाले पोस्ट्स की सूची प्राप्त करता है।

#### पैरामीटर

<ParamField path="path.id" type="Any">
  एक पोस्ट ID।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  यह पैरामीटर परिणामों का अगला ‘पेज’ प्राप्त करने के लिए उपयोग किया जाता है।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Media फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Poll फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Place फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`IteratorGetRepostsResponse`

<div id="hide_reply">
  ### `hide_reply`
</div>

उत्तर छिपाएँ
प्रमाणीकृत उपयोगकर्ता के स्वामित्व वाली बातचीत में किसी उत्तर को छिपाता है या उसका छिपाव हटाता है।

#### पैरामीटर

<ParamField path="path.tweet_id" type="Any">
  उस जवाब का ID जिसे आप छिपाना या फिर दिखाना चाहते हैं।
</ParamField>

<ParamField path="body.body" type="HideReplyRequest">
  अनुरोध का मुख्य भाग
</ParamField>

<div id="search_all">
  ### `search_all`
</div>

सभी पोस्ट्स खोजें
यह खोज क्वेरी से मेल खाने वाले पूरे आर्काइव के पोस्ट्स प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर्स
</div>

<ParamField path="path.query" type="str">
  पोस्ट्स का मिलान करने के लिए एक query/rule/filter। अधिकतम query लंबाई जानने के लिए [https://t.co/rulelength](https://t.co/rulelength) देखें।
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. वह सबसे पुराना UTC टाइमस्टैम्प, जिससे पोस्ट्स उपलब्ध कराए जाएँगे। टाइमस्टैम्प सेकंड-स्तर की सटीकता में है और समावेशी है (अर्थात 12:00:01 में मिनट का पहला सेकंड शामिल होता है)।
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. वह सबसे नया, यानी सबसे हाल का UTC टाइमस्टैम्प, तक जिसके लिए पोस्ट्स उपलब्ध कराए जाएँगे। टाइमस्टैम्प सेकंड-स्तर की सटीकता में है और अपवर्जी है (अर्थात 12:00:01 में मिनट का पहला सेकंड शामिल नहीं होता)।
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  निर्दिष्ट ID से बड़ी (अर्थात उससे अधिक हाल की) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  निर्दिष्ट ID से छोटी (अर्थात उससे पुरानी) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  किसी अनुरोध द्वारा लौटाए जाने वाले खोज परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पेज’ प्राप्त करने के लिए किया जाता है। इस पैरामीटर के साथ उपयोग किया जाने वाला मान सीधे API द्वारा प्रदान किए गए response से लिया जाता है और इसे संशोधित नहीं किया जाना चाहिए।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पेज’ प्राप्त करने के लिए किया जाता है। इस पैरामीटर के साथ उपयोग किया जाने वाला मान सीधे API द्वारा प्रदान किए गए response से लिया जाता है और इसे संशोधित नहीं किया जाना चाहिए।
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
  वह क्रम जिसमें परिणाम लौटाए जाएँगे।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाए जाने वाले Tweet फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  दिखाए जाने वाले Media फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  दिखाए जाने वाले Poll फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाए जाने वाले User फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  दिखाए जाने वाले Place फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`IteratorSearchAllResponse`

<div id="search_recent">
  ### `search_recent`
</div>

हाल के पोस्ट्स खोजें
खोज क्वेरी से मेल खाने वाले पिछले 7 दिनों के पोस्ट्स को पुनर्प्राप्त करता है।

#### पैरामीटर

<ParamField path="path.query" type="str">
  पोस्ट्स से मिलान करने के लिए एक query/rule/filter। अधिकतम query लंबाई जानने के लिए [https://t.co/rulelength](https://t.co/rulelength) देखें।
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. वह सबसे पुराना UTC टाइमस्टैम्प, जिसके बाद के पोस्ट्स प्रदान किए जाएँगे। टाइमस्टैम्प सेकंड-स्तर की सटीकता में है और समावेशी है (यानी 12:00:01 में मिनट का पहला सेकंड शामिल होता है)।
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. वह नवीनतम UTC टाइमस्टैम्प, जिसके पहले तक के पोस्ट्स प्रदान किए जाएँगे। टाइमस्टैम्प सेकंड-स्तर की सटीकता में है और अपवर्जी है (यानी 12:00:01 में मिनट का पहला सेकंड शामिल नहीं होता)।
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  निर्दिष्ट ID से बड़ी (यानी उससे अधिक हाल की) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  निर्दिष्ट ID से छोटी (यानी उससे पुरानी) पोस्ट ID वाले परिणाम लौटाता है।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  किसी अनुरोध द्वारा लौटाए जाने वाले खोज परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘page’ प्राप्त करने के लिए किया जाता है। इस पैरामीटर के साथ उपयोग किया जाने वाला मान सीधे API द्वारा दिए गए response से लिया जाता है और इसमें बदलाव नहीं किया जाना चाहिए।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘page’ प्राप्त करने के लिए किया जाता है। इस पैरामीटर के साथ उपयोग किया जाने वाला मान सीधे API द्वारा दिए गए response से लिया जाता है और इसमें बदलाव नहीं किया जाना चाहिए।
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
  वह क्रम जिसमें परिणाम लौटाए जाएँगे।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Tweet फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तार के लिए फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Media फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Poll फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए User फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Place फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`IteratorSearchRecentResponse`