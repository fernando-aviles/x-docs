---
title: "ListsClient"
sidebarTitle: "ListsClient"
---

<div id="listsclient">
  ## ListsClient
</div>

<Badge color="blue">क्लास</Badge>

<Badge color="gray">बेस: object</Badge>

सूची संचालन के लिए Client

<div id="constructors">
  ## कंस्ट्रक्टर्स
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.client" type="Client" />

<div id="add_member">
  ### `add_member`
</div>

सूची में सदस्य जोड़ें
किसी विशिष्ट सूची में उसकी ID के आधार पर एक उपयोगकर्ता को जोड़ता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस सूची का id जिसमें एक सदस्य जोड़ा जाना है।
</ParamField>

<ParamField path="body.body" type="AddMemberRequest">
  अनुरोध बॉडी
</ParamField>

<div id="create">
  ### `create`
</div>

सूची बनाएँ
प्रमाणीकृत उपयोगकर्ता के लिए नई सूची बनाता है।
body: अनुरोध बॉडी
:returns: प्रतिक्रिया डेटा
:rtype: CreateResponse

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="body.body" type="CreateRequest" />

<div id="delete">
  ### `delete`
</div>

सूची हटाएँ
प्रमाणीकृत उपयोगकर्ता के स्वामित्व वाली किसी विशिष्ट सूची को उसकी id के आधार पर हटाता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस सूची की id जिसे हटाना है।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`DeleteResponse` - प्रतिक्रिया डेटा

<div id="get_by_id">
  ### `get_by_id`
</div>

ID से सूची प्राप्त करें
किसी विशिष्ट सूची का विवरण उसकी ID से प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  सूची की ID।
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
  प्रदर्शित की जाने वाली सूची फ़ील्ड्स की अल्पविराम-से-अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम-से-अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले उपयोगकर्ता फ़ील्ड्स की अल्पविराम-से-अलग की गई सूची।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`GetByIdResponse` - प्रतिक्रिया डेटा

<div id="get_followers">
  ### `get_followers`
</div>

सूची के फ़ॉलोअर्स प्राप्त करें
उस विशिष्ट सूची को उसके id के आधार पर फ़ॉलो करने वाले उपयोगकर्ताओं की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  सूची की id.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का एक निर्दिष्ट ‘पेज’ प्राप्त करने के लिए किया जाता है.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की अल्पविराम-से-अलग की गई सूची.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तार करने के लिए फ़ील्ड्स की अल्पविराम-से-अलग की गई सूची.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की अल्पविराम-से-अलग की गई सूची.
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`IteratorGetFollowersResponse`

<div id="get_members">
  ### `get_members`
</div>

सूची के सदस्य प्राप्त करें
यह किसी विशिष्ट सूची की ID के आधार पर उसके सदस्य Users की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  सूची की ID।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का एक निर्दिष्ट ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित करने के लिए फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`IteratorGetMembersResponse`

<div id="get_posts">
  ### `get_posts`
</div>

सूची के पोस्ट्स प्राप्त करें
किसी विशिष्ट सूची की id के आधार पर उससे संबद्ध पोस्ट्स की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  सूची की id.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तार के लिए फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  दिखाने के लिए Media फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  दिखाने के लिए Poll फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  दिखाने के लिए Place फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`IteratorGetPostsResponse`

<div id="remove_member_by_user_id">
  ### `remove_member_by_user_id`
</div>

सूची से सदस्य हटाएँ
किसी विशिष्ट सूची से उसकी ID और उपयोगकर्ता की ID के आधार पर उपयोगकर्ता को हटाता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस सूची की id, जिससे सदस्य को हटाना है।
</ParamField>

<ParamField path="path.user_id" type="Any">
  उस उपयोगकर्ता की id, जिसे सूची से हटाया जाएगा।
</ParamField>

<div id="returns">
  #### रिटर्न
</div>

`RemoveMemberByUserIdResponse` - प्रतिक्रिया डेटा

<div id="update">
  ### `update`
</div>

सूची अपडेट करें
प्रमाणीकृत उपयोगकर्ता के स्वामित्व वाली किसी विशिष्ट सूची के विवरण को उसकी id के आधार पर अपडेट करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  संशोधित की जाने वाली सूची की ID।
</ParamField>

<ParamField path="body.body" type="UpdateRequest">
  अनुरोध बॉडी
</ParamField>