---
title: "UsersClient"
sidebarTitle: "UsersClient"
---

<div id="usersclient">
  ## UsersClient
</div>

<Badge color="blue">क्लास</Badge>

<Badge color="gray">बेस: object</Badge>

उपयोगकर्ता कार्रवाइयों के लिए Client

<div id="constructors">
  ## कंस्ट्रक्टर्स
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.client" type="Client" />

<div id="block_dms">
  ### `block_dms`
</div>

DMs ब्लॉक करें
प्रमाणीकृत उपयोगकर्ता के लिए, किसी विशिष्ट User की ID के आधार पर, उस उपयोगकर्ता से आने वाले या उसे भेजे जाने वाले direct messages को ब्लॉक करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस लक्षित उपयोगकर्ता की ID, जिसके DMs को ब्लॉक करने का अनुरोध प्रमाणित उपयोगकर्ता कर रहा है।
</ParamField>

#### रिटर्न

`BlockDmsResponse` - प्रतिक्रिया डेटा

<div id="create_bookmark">
  ### `create_bookmark`
</div>

बुकमार्क बनाएँ
प्रमाणीकृत उपयोगकर्ता के बुकमार्क में एक पोस्ट जोड़ें।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणीकृत स्रोत उपयोगकर्ता की id, जिसके लिए बुकमार्क जोड़े जाएंगे।
</ParamField>

<ParamField path="body.body" type="CreateBookmarkRequest">
  अनुरोध बॉडी
</ParamField>

<div id="delete_bookmark">
  ### `delete_bookmark`
</div>

बुकमार्क हटाएँ
प्रमाणित उपयोगकर्ता के बुकमार्क्स से उसकी id के आधार पर एक पोस्ट हटाता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणीकृत स्रोत उपयोगकर्ता का पहचानकर्ता, जिसका बुकमार्क हटाया जाना है।
</ParamField>

<ParamField path="path.tweet_id" type="Any">
  उस पोस्ट का पहचानकर्ता, जिसे स्रोत उपयोगकर्ता बुकमार्क से हटा रहा है।
</ParamField>

#### रिटर्न

`DeleteBookmarkResponse` - प्रतिक्रिया डेटा

<div id="follow_list">
  ### `follow_list`
</div>

सूची को फ़ॉलो करें
प्रमाणित उपयोगकर्ता को उसकी id के आधार पर किसी विशिष्ट सूची को फ़ॉलो करने देता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  प्रमाणित स्रोत उपयोगकर्ता की id, जो इस सूची को फ़ॉलो करेगा।
</ParamField>

<ParamField path="body.body" type="FollowListRequest">
  अनुरोध बॉडी
</ParamField>

<div id="follow_user">
  ### `follow_user`
</div>

उपयोगकर्ता को फ़ॉलो करें
प्रमाणित उपयोगकर्ता को id के आधार पर किसी विशिष्ट उपयोगकर्ता को फ़ॉलो करने देता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणीकृत स्रोत User की id, जो लक्ष्य User को फ़ॉलो करने का अनुरोध कर रहा है।
</ParamField>

<ParamField path="body.body" type="FollowUserRequest">
  अनुरोध बॉडी
</ParamField>

<div id="get_blocking">
  ### `get_blocking`
</div>

ब्लॉक किए गए उपयोगकर्ता प्राप्त करें
निर्दिष्ट उपयोगकर्ता ID द्वारा ब्लॉक किए गए उपयोगकर्ताओं की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणित स्रोत User की ID, जिसके लिए परिणाम लौटाए जाने हैं।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का निर्दिष्ट ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले User फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Tweet फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

#### रिटर्न

`IteratorGetBlockingResponse`

<div id="get_bookmark_folders">
  ### `get_bookmark_folders`
</div>

बुकमार्क फ़ोल्डर प्राप्त करें
प्रमाणीकृत उपयोगकर्ता द्वारा बनाए गए बुकमार्क फ़ोल्डरों की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणीकृत स्रोत उपयोगकर्ता की id, जिसके लिए परिणाम लौटाए जाने हैं।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

#### रिटर्न

`IteratorGetBookmarkFoldersResponse`

<div id="get_bookmarks">
  ### `get_bookmarks`
</div>

बुकमार्क प्राप्त करें
प्रमाणीकृत उपयोगकर्ता द्वारा बुकमार्क की गई पोस्ट्स की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणित स्रोत उपयोगकर्ता की id, जिसके लिए परिणाम लौटाए जाने हैं।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Media फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Poll फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Place फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

#### रिटर्न

`IteratorGetBookmarksResponse`

<div id="get_bookmarks_by_folder_id">
  ### `get_bookmarks_by_folder_id`
</div>

फ़ोल्डर ID के आधार पर बुकमार्क प्राप्त करें
प्रमाणित उपयोगकर्ता के लिए किसी विशिष्ट बुकमार्क फ़ोल्डर की ID से उसमें मौजूद पोस्ट्स प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणीकृत स्रोत उपयोगकर्ता की ID, जिसके लिए परिणाम लौटाने हैं।
</ParamField>

<ParamField path="path.folder_id" type="Any">
  उस बुकमार्क फ़ोल्डर की ID, जिसके लिए प्रमाणीकृत उपयोगकर्ता पोस्ट्स प्राप्त करना चाहता है।
</ParamField>

#### रिटर्न

`GetBookmarksByFolderIdResponse` - प्रतिक्रिया डेटा

<div id="get_by_id">
  ### `get_by_id`
</div>

ID द्वारा उपयोगकर्ता प्राप्त करें
किसी विशिष्ट उपयोगकर्ता का विवरण उसकी ID के आधार पर प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  लुकअप किए जाने वाले User का ID।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

#### रिटर्न

`GetByIdResponse` - प्रतिक्रिया डेटा

<div id="get_by_ids">
  ### `get_by_ids`
</div>

ID के आधार पर उपयोगकर्ताओं को प्राप्त करें
एकाधिक उपयोगकर्ताओं के ID के आधार पर उनका विवरण प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.ids" type="List">
  User IDs की कॉमा से अलग की गई सूची। आप अधिकतम 100 IDs निर्दिष्ट कर सकते हैं।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित करने के लिए फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

#### रिटर्न

`GetByIdsResponse` - प्रतिक्रिया डेटा

<div id="get_by_username">
  ### `get_by_username`
</div>

उपयोगकर्ता नाम से उपयोगकर्ता प्राप्त करें
किसी विशिष्ट उपयोगकर्ता का विवरण उसके उपयोगकर्ता नाम से प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.username" type="str">
  एक उपयोगकर्ता नाम।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए User फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित करने के लिए फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Tweet फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

#### रिटर्न

`GetByUsernameResponse` - प्रतिक्रिया डेटा

<div id="get_by_usernames">
  ### `get_by_usernames`
</div>

उपयोगकर्ता नामों द्वारा उपयोगकर्ताओं को प्राप्त करें
उनके उपयोगकर्ता नामों के आधार पर कई उपयोगकर्ताओं का विवरण प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.usernames" type="List">
  कॉमा से अलग की गई उपयोगकर्ता नामों की एक सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले User फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Tweet फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

#### रिटर्न

`GetByUsernamesResponse` - प्रतिक्रिया डेटा

<div id="get_followed_lists">
  ### `get_followed_lists`
</div>

फ़ॉलो की गई सूचियाँ प्राप्त करें
किसी विशिष्ट उपयोगकर्ता की id के आधार पर, उसके द्वारा फ़ॉलो की गई सूचियों की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  जिस उपयोगकर्ता को देखना है, उसकी id।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का निर्दिष्ट ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए सूची फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित करने के लिए फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए उपयोगकर्ता फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

<div id="returns">
  #### रिटर्न करता है
</div>

`IteratorGetFollowedListsResponse`

<div id="get_followers">
  ### `get_followers`
</div>

फ़ॉलोअर्स प्राप्त करें
किसी विशिष्ट उपयोगकर्ता को फ़ॉलो करने वाले उपयोगकर्ताओं की सूची उसके id के आधार पर प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  खोजे जाने वाले उपयोगकर्ता की ID।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का एक निर्दिष्ट ‘पृष्ठ’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

#### लौटाता है

`IteratorGetFollowersResponse`

<div id="get_following">
  ### `get_following`
</div>

फ़ॉलो किए गए उपयोगकर्ता प्राप्त करें
किसी विशिष्ट उपयोगकर्ता की id के आधार पर, उसके द्वारा फ़ॉलो किए गए उपयोगकर्ताओं की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  जिस User को lookup करना है, उसकी ID.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का कोई निर्दिष्ट ‘पेज’ प्राप्त करने के लिए किया जाता है.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की कॉमा से अलग की गई सूची.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित करने के लिए फ़ील्ड्स की कॉमा से अलग की गई सूची.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की कॉमा से अलग की गई सूची.
</ParamField>

<div id="returns">
  #### रिटर्न करता है
</div>

`IteratorGetFollowingResponse`

<div id="get_liked_posts">
  ### `get_liked_posts`
</div>

पसंद किए गए पोस्ट्स प्राप्त करें
किसी विशिष्ट उपयोगकर्ता की ID के आधार पर उसके द्वारा पसंद किए गए पोस्ट्स की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  जिस उपयोगकर्ता को lookup करना है, उसका पहचानकर्ता।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  दिखाने के लिए Media फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  दिखाने के लिए Poll फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  दिखाने के लिए Place फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

#### लौटाता है

`IteratorGetLikedPostsResponse`

<div id="get_list_memberships">
  ### `get_list_memberships`
</div>

सूची सदस्यताएँ प्राप्त करें
किसी विशिष्ट उपयोगकर्ता के ID के आधार पर उन सूचियों की सूची प्राप्त करता है, जिनका वह सदस्य है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  जिस उपयोगकर्ता को खोजना है उसकी ID।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का एक निर्दिष्ट ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले सूची फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले उपयोगकर्ता फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

#### रिटर्न

`IteratorGetListMembershipsResponse`

<div id="get_me">
  ### `get_me`
</div>

मेरे उपयोगकर्ता की जानकारी प्राप्त करें
प्रमाणित उपयोगकर्ता का विवरण प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित करने के लिए फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

#### रिटर्न

`GetMeResponse` - प्रतिक्रिया डेटा

<div id="get_mentions">
  ### `get_mentions`
</div>

उल्लेख प्राप्त करें
किसी विशिष्ट उपयोगकर्ता की id का उल्लेख करने वाली पोस्ट्स की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  लुकअप किए जाने वाले User की ID.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  परिणाम सेट में शामिल की जाने वाली न्यूनतम पोस्ट ID. यदि दोनों निर्दिष्ट हों, तो यह पैरामीटर start&#95;time पर प्राथमिकता लेता है.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  परिणाम सेट में शामिल की जाने वाली अधिकतम पोस्ट ID. यदि दोनों निर्दिष्ट हों, तो यह पैरामीटर end&#95;time पर प्राथमिकता लेता है.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पेज’ प्राप्त करने के लिए किया जाता है.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. वह सबसे प्रारंभिक UTC टाइमस्टैम्प, जिससे पोस्ट्स उपलब्ध कराई जाएंगी. यदि since&#95;id पैरामीटर भी निर्दिष्ट है, तो उसे प्राथमिकता दी जाती है.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. वह नवीनतम UTC टाइमस्टैम्प, जिसके तक पोस्ट्स उपलब्ध कराई जाएंगी. यदि until&#95;id पैरामीटर भी निर्दिष्ट है, तो उसे प्राथमिकता दी जाती है.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाए जाने वाले Tweet फ़ील्ड्स की कॉमा-सेपरेटेड सूची.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की कॉमा-सेपरेटेड सूची.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  दिखाए जाने वाले Media फ़ील्ड्स की कॉमा-सेपरेटेड सूची.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  दिखाए जाने वाले Poll फ़ील्ड्स की कॉमा-सेपरेटेड सूची.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाए जाने वाले User फ़ील्ड्स की कॉमा-सेपरेटेड सूची.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  दिखाए जाने वाले Place फ़ील्ड्स की कॉमा-सेपरेटेड सूची.
</ParamField>

#### लौटाता है

`IteratorGetMentionsResponse`

<div id="get_muting">
  ### `get_muting`
</div>

म्यूट किए गए उपयोगकर्ता प्राप्त करें
प्रमाणीकृत उपयोगकर्ता द्वारा म्यूट किए गए उपयोगकर्ताओं की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणीकृत स्रोत User की id, जिसके लिए परिणाम लौटाए जाने हैं।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पृष्ठ’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

#### लौटाता है

`IteratorGetMutingResponse`

<div id="get_owned_lists">
  ### `get_owned_lists`
</div>

स्वामित्व वाली सूचियाँ प्राप्त करें
किसी विशिष्ट उपयोगकर्ता की ID के आधार पर उसके स्वामित्व वाली सूचियों की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  जिस User को lookup करना है उसकी ID।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का एक निर्दिष्ट ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
  दिखाने के लिए List फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तार के लिए फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

#### लौटाता है

`IteratorGetOwnedListsResponse`

<div id="get_pinned_lists">
  ### `get_pinned_lists`
</div>

पिन की गई सूचियाँ प्राप्त करें
यह प्रमाणीकृत उपयोगकर्ता द्वारा पिन की गई सूचियाँ प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणीकृत स्रोत उपयोगकर्ता का id, जिसके लिए परिणाम लौटाए जाने हैं।
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
  दिखाने के लिए सूची फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की अल्पविराम-से-पृथक सूची।
</ParamField>

#### रिटर्न

`GetPinnedListsResponse` - प्रतिक्रिया डेटा

<div id="get_posts">
  ### `get_posts`
</div>

पोस्ट्स प्राप्त करें
किसी विशिष्ट उपयोगकर्ता की id के आधार पर उसके द्वारा लिखी गई पोस्ट्स की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस उपयोगकर्ता की ID, जिसे लुकअप करना है।
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  परिणाम सेट में शामिल की जाने वाली न्यूनतम पोस्ट ID। यदि दोनों निर्दिष्ट हैं, तो यह पैरामीटर start&#95;time पर प्राथमिकता लेता है।
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  परिणाम सेट में शामिल की जाने वाली अधिकतम पोस्ट ID। यदि दोनों निर्दिष्ट हैं, तो यह पैरामीटर end&#95;time पर प्राथमिकता लेता है।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पृष्ठ’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
  बाहर रखी जाने वाली इकाइयों का सेट (उदा. ‘replies’ या ‘retweets’)।
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. सबसे प्रारंभिक UTC टाइमस्टैम्प, जिसके बाद की पोस्ट्स लौटाई जाएंगी। यदि since&#95;id पैरामीटर भी निर्दिष्ट है, तो उसे प्राथमिकता मिलेगी।
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. सबसे नवीनतम UTC टाइमस्टैम्प, जिसके पहले तक की पोस्ट्स लौटाई जाएंगी। यदि until&#95;id पैरामीटर भी निर्दिष्ट है, तो उसे प्राथमिकता मिलेगी।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Media फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Poll फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Place फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

#### रिटर्न

`IteratorGetPostsResponse`

<div id="get_reposts_of_me">
  ### `get_reposts_of_me`
</div>

मेरे रीपोस्ट्स प्राप्त करें
प्रमाणीकृत उपयोगकर्ता की सामग्री को रीपोस्ट करने वाली पोस्ट्स की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाने के लिए Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित करने के लिए फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  दिखाने के लिए Media फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  दिखाने के लिए Poll फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाने के लिए User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  दिखाने के लिए Place फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

#### रिटर्न

`IteratorGetRepostsOfMeResponse`

<div id="get_timeline">
  ### `get_timeline`
</div>

टाइमलाइन प्राप्त करें
प्रमाणीकृत उपयोगकर्ता की टाइमलाइन में पोस्ट्स की उल्टे कालानुक्रमिक क्रम वाली सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणीकृत स्रोत User की id, जिसकी रिवर्स क्रोनोलॉजिकल टाइमलाइन के पोस्ट्स सूचीबद्ध करने हैं।
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  परिणाम सेट में शामिल की जाने वाली न्यूनतम पोस्ट id। यदि दोनों निर्दिष्ट हैं, तो यह पैरामीटर start&#95;time पर प्राथमिकता लेता है।
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  परिणाम सेट में शामिल की जाने वाली अधिकतम पोस्ट id। यदि दोनों निर्दिष्ट हैं, तो यह पैरामीटर end&#95;time पर प्राथमिकता लेता है।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का अगला ‘पृष्ठ’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
  बाहर रखने वाली इकाइयों का सेट (उदाहरण के लिए, ‘replies’ या ‘retweets’)।
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. वह सबसे प्रारंभिक UTC टाइमस्टैम्प, जिससे पोस्ट्स प्रदान किए जाएँगे। यदि since&#95;id पैरामीटर भी निर्दिष्ट है, तो उसे प्राथमिकता मिलेगी।
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. वह नवीनतम UTC टाइमस्टैम्प, तक जिसके पोस्ट्स प्रदान किए जाएँगे। यदि until&#95;id पैरामीटर भी निर्दिष्ट है, to उसे प्राथमिकता मिलेगी।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Tweet फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तार करने के लिए फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Media फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Poll फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए User फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  प्रदर्शित करने के लिए Place फ़ील्ड्स की अल्पविराम-से-अलग सूची।
</ParamField>

#### रिटर्न

`IteratorGetTimelineResponse`

<div id="like_post">
  ### `like_post`
</div>

पोस्ट को पसंद करें
प्रमाणीकृत उपयोगकर्ता को उसकी ID के आधार पर किसी विशिष्ट पोस्ट को पसंद करने देता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणित स्रोत उपयोगकर्ता की ID, जो पोस्ट को लाइक करने का अनुरोध कर रहा है।
</ParamField>

<ParamField path="body.body" type="LikePostRequest">
  अनुरोध बॉडी
</ParamField>

<div id="mute_user">
  ### `mute_user`
</div>

उपयोगकर्ता को म्यूट करें
प्रमाणीकृत उपयोगकर्ता को किसी विशिष्ट उपयोगकर्ता को उसकी ID के आधार पर म्यूट करने देता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  प्रमाणीकृत स्रोत उपयोगकर्ता का ID, जो लक्षित उपयोगकर्ता को म्यूट करने का अनुरोध कर रहा है।
</ParamField>

<ParamField path="body.body" type="MuteUserRequest">
  अनुरोध बॉडी
</ParamField>

<div id="pin_list">
  ### `pin_list`
</div>

सूची पिन करें
प्रमाणित उपयोगकर्ता को id के आधार पर किसी विशिष्ट सूची को पिन करने देता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  प्रमाणित स्रोत उपयोगकर्ता की id, जो सूची को पिन करेगा।
</ParamField>

<ParamField path="body.body" type="PinListRequest">
  अनुरोध बॉडी
</ParamField>

<div id="repost_post">
  ### `repost_post`
</div>

पोस्ट को रीपोस्ट करें
यह प्रमाणीकृत उपयोगकर्ता को किसी विशिष्ट पोस्ट को उसकी ID के आधार पर रीपोस्ट करने की अनुमति देता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणित स्रोत उपयोगकर्ता की ID जो पोस्ट को रीपोस्ट करने का अनुरोध कर रहा है।
</ParamField>

<ParamField path="body.body" type="RepostPostRequest">
  अनुरोध बॉडी
</ParamField>

<div id="search">
  ### `search`
</div>

उपयोगकर्ता खोजें
खोज क्वेरी से मेल खाने वाले उपयोगकर्ताओं की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.query" type="Any">
  उपयोगकर्ताओं को क्वेरी करने के लिए उपयोग की जाने वाली क्वेरी स्ट्रिंग।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  रिज़ल्ट्स की अधिकतम संख्या।
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग रिज़ल्ट्स का अगला ‘पेज’ प्राप्त करने के लिए किया जाता है। इस पैरामीटर के साथ उपयोग किया जाने वाला मान API द्वारा प्रदान किए गए रिस्पॉन्स से सीधे लिया जाता है, और इसमें बदलाव नहीं किया जाना चाहिए।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाए जाने वाले User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाए जाने वाले Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

#### रिटर्न

`IteratorSearchResponse`

<div id="unblock_dms">
  ### `unblock_dms`
</div>

DMs को अनब्लॉक करें
प्रमाणीकृत उपयोगकर्ता के लिए, किसी विशिष्ट User की ID के आधार पर उससे आने वाले या उसे भेजे जाने वाले डायरेक्ट मैसेज को अनब्लॉक करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस लक्षित User की ID जिसके लिए प्रमाणीकृत उपयोगकर्ता DMs को अनब्लॉक करने का अनुरोध कर रहा है।
</ParamField>

#### रिटर्न

`UnblockDmsResponse` - प्रतिक्रिया डेटा

<div id="unfollow_list">
  ### `unfollow_list`
</div>

सूची को अनफ़ॉलो करें
इससे प्रमाणित उपयोगकर्ता, id के आधार पर, किसी विशिष्ट सूची को अनफ़ॉलो कर सकता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणीकृत स्रोत उपयोगकर्ता की ID, जो सूची को unfollow करेगा।
</ParamField>

<ParamField path="path.list_id" type="Any">
  उस सूची की ID जिसे unfollow किया जाना है।
</ParamField>

#### रिटर्न

`UnfollowListResponse` - प्रतिक्रिया डेटा

<div id="unfollow_user">
  ### `unfollow_user`
</div>

उपयोगकर्ता को अनफ़ॉलो करें
यह प्रमाणीकृत उपयोगकर्ता को किसी विशिष्ट उपयोगकर्ता को उसकी ID के आधार पर अनफ़ॉलो करने देता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.source_user_id" type="Any">
  उस प्रमाणित स्रोत उपयोगकर्ता की ID, जो लक्ष्य उपयोगकर्ता को unfollow करने का अनुरोध कर रहा है।
</ParamField>

<ParamField path="path.target_user_id" type="Any">
  उस उपयोगकर्ता की ID, जिसे स्रोत उपयोगकर्ता unfollow करने का अनुरोध कर रहा है।
</ParamField>

#### रिटर्न

`UnfollowUserResponse` - प्रतिक्रिया डेटा

<div id="unlike_post">
  ### `unlike_post`
</div>

पोस्ट को अनलाइक करें
यह प्रमाणित उपयोगकर्ता को उसकी `id` के आधार पर किसी विशिष्ट पोस्ट को अनलाइक करने देता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणीकृत स्रोत उपयोगकर्ता की ID, जो पोस्ट से पसंद हटाने का अनुरोध कर रहा है।
</ParamField>

<ParamField path="path.tweet_id" type="Any">
  उस पोस्ट की ID, जिससे उपयोगकर्ता पसंद हटाने का अनुरोध कर रहा है।
</ParamField>

#### रिटर्न

`UnlikePostResponse` - प्रतिक्रिया डेटा

<div id="unmute_user">
  ### `unmute_user`
</div>

उपयोगकर्ता का म्यूट हटाएँ
प्रमाणित उपयोगकर्ता को id के आधार पर किसी विशिष्ट उपयोगकर्ता का म्यूट हटाने देता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.source_user_id" type="Any">
  प्रमाणीकृत स्रोत उपयोगकर्ता की ID, जो लक्षित उपयोगकर्ता को अनम्यूट करने का अनुरोध कर रहा है।
</ParamField>

<ParamField path="path.target_user_id" type="Any">
  उस उपयोगकर्ता की ID, जिसे स्रोत उपयोगकर्ता अनम्यूट करने का अनुरोध कर रहा है।
</ParamField>

#### रिटर्न

`UnmuteUserResponse` - प्रतिक्रिया डेटा

<div id="unpin_list">
  ### `unpin_list`
</div>

सूची को अनपिन करें
प्रमाणीकृत उपयोगकर्ता को उसकी ID के आधार पर किसी विशिष्ट सूची को अनपिन करने देता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणीकृत स्रोत उपयोगकर्ता की ID, जिसके लिए परिणाम लौटाए जाने हैं।
</ParamField>

<ParamField path="path.list_id" type="Any">
  अनपिन की जाने वाली सूची की ID।
</ParamField>

#### रिटर्न

`UnpinListResponse` - प्रतिक्रिया डेटा

<div id="unrepost_post">
  ### `unrepost_post`
</div>

पोस्ट का रीपोस्ट हटाएँ
यह प्रमाणित उपयोगकर्ता को किसी विशिष्ट पोस्ट की ID के आधार पर उसका रीपोस्ट हटाने देता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  उस प्रमाणित स्रोत उपयोगकर्ता की ID, जो पोस्ट को रीपोस्ट करने का अनुरोध कर रहा है।
</ParamField>

<ParamField path="path.source_tweet_id" type="Any">
  उस पोस्ट की ID, जिसका रीट्वीट हटाने का अनुरोध उपयोगकर्ता कर रहा है।
</ParamField>

#### रिटर्न

`UnrepostPostResponse` - प्रतिक्रिया डेटा