---
title: "DirectMessagesClient"
sidebarTitle: "DirectMessagesClient"
---

<div id="directmessagesclient">
  ## DirectMessagesClient
</div>

<Badge color="blue">क्लास</Badge>

<Badge color="gray">बेस: object</Badge>

डायरेक्ट मैसेज ऑपरेशनों के लिए Client

<div id="constructors">
  ## कंस्ट्रक्टर्स
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.client" type="Client" />

<div id="create_by_conversation_id">
  ### `create_by_conversation_id`
</div>

वार्तालाप ID द्वारा DM संदेश बनाएँ
यह किसी विशिष्ट वार्तालाप को उसकी ID के आधार पर एक नया डायरेक्ट मैसेज भेजता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.dm_conversation_id" type="str">
  DM वार्तालाप की ID।
</ParamField>

<ParamField path="body.body" type="CreateByConversationIdRequest">
  अनुरोध बॉडी
</ParamField>

<div id="create_by_participant_id">
  ### `create_by_participant_id`
</div>

प्रतिभागी ID द्वारा DM संदेश बनाएँ
किसी विशिष्ट प्रतिभागी को उसकी ID के आधार पर एक नया डायरेक्ट मैसेज भेजता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.participant_id" type="Any">
  उस प्राप्तकर्ता उपयोगकर्ता की ID जिसे DM प्राप्त होगा।
</ParamField>

<ParamField path="body.body" type="CreateByParticipantIdRequest">
  अनुरोध बॉडी
</ParamField>

<div id="create_conversation">
  ### `create_conversation`
</div>

DM वार्तालाप बनाएँ
निर्दिष्ट प्रतिभागियों के साथ एक नया डायरेक्ट मैसेज वार्तालाप शुरू करता है।
body: अनुरोध बॉडी
:returns: प्रतिक्रिया डेटा
:rtype: CreateConversationResponse

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="body.body" type="CreateConversationRequest" />

<div id="delete_events">
  ### `delete_events`
</div>

DM इवेंट हटाएँ
अगर प्रमाणित उपयोगकर्ता उसका स्वामी है, तो यह किसी विशिष्ट डायरेक्ट मैसेज इवेंट को उसकी ID से हटाता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.event_id" type="Any">
  हटाए जाने वाले direct-message इवेंट की ID।
</ParamField>

#### रिटर्न

`DeleteEventsResponse` - प्रतिक्रिया डेटा

<div id="get_events">
  ### `get_events`
</div>

DM ईवेंट प्राप्त करें
सभी वार्तालापों में हाल के डायरेक्ट मैसेज ईवेंट्स की सूची प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  यह पैरामीटर परिणामों का कोई निर्दिष्ट ‘पृष्ठ’ प्राप्त करने के लिए उपयोग किया जाता है।
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
  परिणामों में शामिल किए जाने वाले event&#95;types का सेट।
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले DmEvent फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Media फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<div id="returns">
  #### रिटर्न करता है
</div>

`IteratorGetEventsResponse`

<div id="get_events_by_conversation_id">
  ### `get_events_by_conversation_id`
</div>

किसी DM वार्तालाप के लिए DM इवेंट प्राप्त करें
यह किसी विशिष्ट बातचीत के लिए डायरेक्ट मैसेज इवेंट प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.id" type="Any">
  DM वार्तालाप की id.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का निर्दिष्ट ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
  परिणामों में शामिल किए जाने वाले event&#95;types का सेट।
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले DmEvent फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Media फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले User फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Tweet फ़ील्ड्स की अल्पविराम से अलग की गई सूची।
</ParamField>

#### लौटाता है

`IteratorGetEventsByConversationIdResponse`

<div id="get_events_by_id">
  ### `get_events_by_id`
</div>

ID से DM ईवेंट प्राप्त करें
यह किसी विशिष्ट डायरेक्ट मैसेज ईवेंट का विवरण उसकी ID के आधार पर प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.event_id" type="Any">
  DM ईवेंट id।
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले DmEvent फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तारित किए जाने वाले फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Media फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले User फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  प्रदर्शित किए जाने वाले Tweet फ़ील्ड्स की कॉमा से अलग की गई सूची।
</ParamField>

#### रिटर्न

`GetEventsByIdResponse` - प्रतिक्रिया डेटा

<div id="get_events_by_participant_id">
  ### `get_events_by_participant_id`
</div>

किसी DM वार्तालाप के लिए DM इवेंट प्राप्त करें
किसी विशिष्ट बातचीत के लिए डायरेक्ट मैसेज इवेंट प्राप्त करता है।

<div id="parameters">
  #### पैरामीटर
</div>

<ParamField path="path.participant_id" type="Any">
  One to One DM वार्तालाप में प्रतिभागी उपयोगकर्ता की ID।
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  परिणामों की अधिकतम संख्या।
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  इस पैरामीटर का उपयोग परिणामों का कोई निर्दिष्ट ‘पेज’ प्राप्त करने के लिए किया जाता है।
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
  परिणामों में शामिल किए जाने वाले event&#95;types का सेट।
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
  दिखाए जाने वाले DmEvent फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  विस्तार के लिए फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  दिखाए जाने वाले Media फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  दिखाए जाने वाले User फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  दिखाए जाने वाले Tweet फ़ील्ड्स की कॉमा-सेपरेटेड सूची।
</ParamField>

#### लौटाया गया मान

`IteratorGetEventsByParticipantIdResponse`