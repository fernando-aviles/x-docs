---
title: त्वरित शुरुआत
sidebarTitle: त्वरित शुरुआत
description: उपयोगकर्ताओं को ब्लॉक और अनब्लॉक करें और अपनी ब्लॉक सूची प्राप्त करें
keywords: ["ब्लॉक त्वरित शुरुआत", "उपयोगकर्ताओं को ब्लॉक करने की त्वरित शुरुआत", "ब्लॉक ट्यूटोरियल", "ब्लॉक गाइड"]
---

import { Button } from '/snippets/hi/button.mdx';

यह मार्गदर्शिका आपको उपयोगकर्ताओं को ब्लॉक और अनब्लॉक करने, और अपनी ब्लॉक सूची प्राप्त करने की प्रक्रिया समझाती है।

<Note>
  **पूर्वापेक्षाएँ**

  शुरू करने से पहले, आपको इनकी आवश्यकता होगी:

  * स्वीकृत ऐप के साथ एक [डेवलपर खाता](https://developer.x.com/en/portal/petition/essential/basic-info)
  * User Access Token (OAuth 1.0a or OAuth 2.0 PKCE)
</Note>

***

<div id="get-blocked-users">
  ## ब्लॉक किए गए उपयोगकर्ताओं को प्राप्त करें
</div>

<Steps>
  <Step title="अपनी उपयोगकर्ता ID प्राप्त करें">
    अपनी ब्लॉक सूची प्राप्त करने के लिए आपको अपने ऑथेंटिकेट किए गए उपयोगकर्ता की ID चाहिए। आप इसे `/2/users/me` एंडपॉइंट से प्राप्त कर सकते हैं या अपने टोकन की ID का उपयोग कर सकते हैं।
  </Step>

  <Step title="अपनी ब्लॉक सूची का अनुरोध करें">
    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/users/123456789/blocking?\
      user.fields=username,verified,created_at&\
      max_results=100" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

      # पेजिनेशन के साथ ब्लॉक किए गए उपयोगकर्ताओं को प्राप्त करें
      for page in client.users.get_blocking(
          "123456789",
          user_fields=["username", "verified", "created_at"],
          max_results=100
      ):
          for user in page.data:
              print(f"{user.username} - Created: {user.created_at}")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

      // पेजिनेशन के साथ ब्लॉक किए गए उपयोगकर्ताओं को प्राप्त करें
      const paginator = client.users.getBlocking("123456789", {
        userFields: ["username", "verified", "created_at"],
        maxResults: 100,
      });

      for await (const page of paginator) {
        page.data?.forEach((user) => {
          console.log(`${user.username} - Created: ${user.created_at}`);
        });
      }
      ```
    </CodeGroup>
  </Step>

  <Step title="रिस्पॉन्स की समीक्षा करें">
    ```json
    {
      "data": [
        {
          "id": "17874544",
          "name": "Example User",
          "username": "example_user",
          "verified": false,
          "created_at": "2008-12-04T18:51:57.000Z"
        }
      ],
      "meta": {
        "result_count": 1,
        "next_token": "abc123"
      }
    }
    ```
  </Step>
</Steps>

***

<div id="block-a-user">
  ## किसी उपयोगकर्ता को ब्लॉक करें
</div>

<Steps>
  <Step title="लक्षित उपयोगकर्ता की पहचान करें">
    जिस खाते को आप ब्लॉक करना चाहते हैं, उसकी उपयोगकर्ता ID प्राप्त करें।
  </Step>

  <Step title="ब्लॉक अनुरोध भेजें">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/users/123456789/blocking" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"target_user_id": "9876543210"}'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # किसी उपयोगकर्ता को ब्लॉक करें
      response = client.users.block(
          source_user_id="123456789",
          target_user_id="9876543210"
      )
      print(f"Blocking: {response.data.blocking}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // किसी उपयोगकर्ता को ब्लॉक करें
      const response = await client.users.block("123456789", {
        targetUserId: "9876543210",
      });
      console.log(`Blocking: ${response.data?.blocking}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="ब्लॉक की पुष्टि करें">
    ```json
    {
      "data": {
        "blocking": true
      }
    }
    ```
  </Step>
</Steps>

***

<div id="unblock-a-user">
  ## किसी उपयोगकर्ता को अनब्लॉक करें
</div>

<Steps>
  <Step title="अनब्लॉक करने का अनुरोध भेजें">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X DELETE "https://api.x.com/2/users/123456789/blocking/9876543210" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # किसी उपयोगकर्ता को अनब्लॉक करें
      response = client.users.unblock(
          source_user_id="123456789",
          target_user_id="9876543210"
      )
      print(f"Blocking: {response.data.blocking}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // किसी उपयोगकर्ता को अनब्लॉक करें
      const response = await client.users.unblock("123456789", "9876543210");
      console.log(`Blocking: ${response.data?.blocking}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="अनब्लॉक होने की पुष्टि करें">
    ```json
    {
      "data": {
        "blocking": false
      }
    }
    ```
  </Step>
</Steps>

***

<div id="next-steps">
  ## अगले चरण
</div>

<CardGroup cols={2}>
  <Card title="म्यूट" icon="volume-xmark" href="/hi/x-api/users/mutes/introduction">
    ब्लॉक करने के बजाय उपयोगकर्ताओं को म्यूट करें
  </Card>

  <Card title="फ़ॉलो" icon="user-plus" href="/hi/x-api/users/follows/introduction">
    फ़ॉलो प्रबंधित करें
  </Card>

  <Card title="एकीकरण मार्गदर्शिका" icon="book" href="/hi/x-api/users/blocks/integrate">
    मुख्य अवधारणाएँ और सर्वोत्तम प्रथाएँ
  </Card>

  <Card title="API संदर्भ" icon="code" href="/hi/x-api/users/get-blocking">
    एंडपॉइंट का पूरा दस्तावेज़ीकरण
  </Card>
</CardGroup>