---
title: म्यूट प्रबंधित करें
sidebarTitle: म्यूट प्रबंधित करें
description: X API का उपयोग करके उपयोगकर्ताओं को म्यूट और अनम्यूट करें
keywords: ["म्यूट प्रबंधित करें त्वरित शुरुआत", "उपयोगकर्ताओं को म्यूट करें त्वरित शुरुआत", "म्यूट प्रबंधित करें ट्यूटोरियल", "म्यूट मार्गदर्शिका", "म्यूट त्वरित शुरुआत"]
---

import { Button } from '/snippets/hi/button.mdx';

यह मार्गदर्शिका आपको X API का उपयोग करके उपयोगकर्ताओं को म्यूट और अनम्यूट करने का तरीका बताती है।

<Note>
  **पूर्वापेक्षाएँ**

  शुरू करने से पहले, आपके पास ये होने चाहिए:

  * स्वीकृत ऐप के साथ एक [डेवलपर खाता](https://developer.x.com/en/portal/petition/essential/basic-info)
  * उपयोगकर्ता एक्सेस टोकन (OAuth 1.0a या OAuth 2.0 PKCE)
</Note>

***

<div id="mute-a-user">
  ## किसी उपयोगकर्ता को म्यूट करें
</div>

<Steps>
  <Step title="अपनी उपयोगकर्ता ID प्राप्त करें">
    आपको अपने प्रमाणीकृत उपयोगकर्ता की ID चाहिए। आप इसे [उपयोगकर्ता लुकअप एंडपॉइंट](/hi/x-api/users/lookup/introduction) की मदद से या अपने Access Token से प्राप्त कर सकते हैं (उसका संख्यात्मक भाग आपकी उपयोगकर्ता ID है)।
  </Step>

  <Step title="लक्षित उपयोगकर्ता की ID प्राप्त करें">
    जिस खाते को आप म्यूट करना चाहते हैं, उसकी उपयोगकर्ता ID [उपयोगकर्ता लुकअप एंडपॉइंट](/hi/x-api/users/lookup/introduction) की मदद से पता करें।
  </Step>

  <Step title="म्यूट अनुरोध भेजें">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/users/123456789/muting" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"target_user_id": "9876543210"}'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # किसी उपयोगकर्ता को म्यूट करें
      response = client.users.mute(
          source_user_id="123456789",
          target_user_id="9876543210"
      )

      print(f"Muting: {response.data.muting}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // किसी उपयोगकर्ता को म्यूट करें
      const response = await client.users.mute("123456789", {
        targetUserId: "9876543210",
      });

      console.log(`Muting: ${response.data?.muting}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="रिस्पॉन्स की समीक्षा करें">
    ```json
    {
      "data": {
        "muting": true
      }
    }
    ```
  </Step>
</Steps>

***

<div id="unmute-a-user">
  ## किसी उपयोगकर्ता को अनम्यूट करें
</div>

किसी उपयोगकर्ता का म्यूट हटाएँ:

<CodeGroup dropdown>
  ```bash cURL
  curl -X DELETE "https://api.x.com/2/users/123456789/muting/9876543210" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client
  from xdk.oauth1_auth import OAuth1

  oauth1 = OAuth1(
      api_key="YOUR_API_KEY",
      api_secret="YOUR_API_SECRET",
      access_token="YOUR_ACCESS_TOKEN",
      access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
  )

  client = Client(auth=oauth1)

  # किसी उपयोगकर्ता को अनम्यूट करें
  response = client.users.unmute(
      source_user_id="123456789",
      target_user_id="9876543210"
  )

  print(f"Muting: {response.data.muting}")
  ```

  ```javascript JavaScript SDK
  import { Client, OAuth1 } from "@xdevplatform/xdk";

  const oauth1 = new OAuth1({
    apiKey: "YOUR_API_KEY",
    apiSecret: "YOUR_API_SECRET",
    accessToken: "YOUR_ACCESS_TOKEN",
    accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
  });

  const client = new Client({ oauth1 });

  // किसी उपयोगकर्ता को अनम्यूट करें
  const response = await client.users.unmute("123456789", "9876543210");

  console.log(`Muting: ${response.data?.muting}`);
  ```
</CodeGroup>

**रिस्पॉन्स:**

```json
{
  "data": {
    "muting": false
  }
}
```

***

<div id="mute-vs-block">
  ## म्यूट बनाम ब्लॉक
</div>

| सुविधा                       | म्यूट | ब्लॉक |
| :--------------------------- | :---- | :---- |
| उनके पोस्ट्स देख सकते हैं    | नहीं  | नहीं  |
| वे आपके पोस्ट्स देख सकते हैं | हाँ   | नहीं  |
| वे आपको फ़ॉलो कर सकते हैं    | हाँ   | नहीं  |
| वे आपको DM कर सकते हैं       | हाँ   | नहीं  |
| उन्हें इसका पता चलता है      | नहीं  | हाँ   |

***

<div id="next-steps">
  ## अगले चरण
</div>

<CardGroup cols={2}>
  <Card title="म्यूट किए गए उपयोगकर्ताओं की जानकारी" icon="volume-xmark" href="/hi/x-api/users/mutes/quickstart/mutes-lookup">
    अपने म्यूट किए गए उपयोगकर्ताओं को देखें
  </Card>

  <Card title="ब्लॉक" icon="ban" href="/hi/x-api/users/blocks/quickstart">
    इसके बजाय उपयोगकर्ताओं को ब्लॉक करें
  </Card>

  <Card title="API संदर्भ" icon="code" href="/hi/x-api/users/mute-user-by-user-id">
    एंडपॉइंट का पूरा दस्तावेज़ीकरण
  </Card>
</CardGroup>