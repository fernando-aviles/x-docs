---
title: उपयोगकर्ता खोज की त्वरित शुरुआत
sidebarTitle: उपयोगकर्ता खोज
description: ID या उपयोगकर्ता नाम से उपयोगकर्ता खोजें
keywords: ["उपयोगकर्ता खोज की त्वरित शुरुआत", "उपयोगकर्ता प्राप्त करने का ट्यूटोरियल", "ID द्वारा उपयोगकर्ता गाइड", "उपयोगकर्ता नाम से उपयोगकर्ता"]
---

import { Button } from '/snippets/hi/button.mdx';

यह मार्गदर्शिका आपको उपयोगकर्ताओं को उनकी ID या उपयोगकर्ता नाम से खोजने की प्रक्रिया बताती है।

<Note>
  **पूर्वापेक्षाएँ**

  शुरू करने से पहले, आपको इनकी आवश्यकता होगी:

  * एक स्वीकृत ऐप के साथ [डेवलपर खाता](https://developer.x.com/en/portal/petition/essential/basic-info)
  * आपके ऐप का बेयरर टोकन
</Note>

***

<div id="look-up-by-id">
  ## ID के आधार पर खोजें
</div>

<div id="single-user">
  ### एक उपयोगकर्ता
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/2244994945?\
  user.fields=created_at,description,verified,public_metrics" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # ID से उपयोगकर्ता प्राप्त करें
  response = client.users.get(
      "2244994945",
      user_fields=["created_at", "description", "verified", "public_metrics"]
  )

  print(f"Name: {response.data.name}")
  print(f"Username: {response.data.username}")
  print(f"Followers: {response.data.public_metrics.followers_count}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // ID से उपयोगकर्ता प्राप्त करें
  const response = await client.users.get("2244994945", {
    userFields: ["created_at", "description", "verified", "public_metrics"],
  });

  console.log(`Name: ${response.data?.name}`);
  console.log(`Username: ${response.data?.username}`);
  console.log(`Followers: ${response.data?.public_metrics?.followers_count}`);
  ```
</CodeGroup>

<div id="response">
  ### प्रतिक्रिया
</div>

```json
{
  "data": {
    "id": "2244994945",
    "name": "X Developers",
    "username": "XDevelopers",
    "created_at": "2013-12-14T04:35:55.000Z",
    "description": "The voice of the X developer community",
    "verified": true,
    "public_metrics": {
      "followers_count": 583423,
      "following_count": 2048,
      "tweet_count": 14052,
      "listed_count": 1672
    }
  }
}
```

<div id="multiple-users">
  ### कई उपयोगकर्ता
</div>

एक बार में अधिकतम 100 उपयोगकर्ताओं को खोजें:

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users?\
  ids=2244994945,783214,6253282&\
  user.fields=username,verified" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # ID द्वारा कई उपयोगकर्ता प्राप्त करें
  response = client.users.get_users(
      ids=["2244994945", "783214", "6253282"],
      user_fields=["username", "verified"]
  )

  for user in response.data:
      print(f"{user.username} - Verified: {user.verified}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // ID द्वारा कई उपयोगकर्ता प्राप्त करें
  const response = await client.users.getUsers({
    ids: ["2244994945", "783214", "6253282"],
    userFields: ["username", "verified"],
  });

  response.data?.forEach((user) => {
    console.log(`${user.username} - Verified: ${user.verified}`);
  });
  ```
</CodeGroup>

***

<div id="look-up-by-username">
  ## उपयोगकर्ता नाम से खोजें
</div>

<div id="single-user">
  ### एक उपयोगकर्ता
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/by/username/XDevelopers?\
  user.fields=created_at,description,verified" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # उपयोगकर्ता नाम से उपयोगकर्ता की जानकारी प्राप्त करें
  response = client.users.get_by_username(
      "XDevelopers",
      user_fields=["created_at", "description", "verified"]
  )

  print(f"ID: {response.data.id}")
  print(f"नाम: {response.data.name}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // उपयोगकर्ता नाम से उपयोगकर्ता की जानकारी प्राप्त करें
  const response = await client.users.getByUsername("XDevelopers", {
    userFields: ["created_at", "description", "verified"],
  });

  console.log(`ID: ${response.data?.id}`);
  console.log(`नाम: ${response.data?.name}`);
  ```
</CodeGroup>

<div id="multiple-users">
  ### कई उपयोगकर्ता
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/by?\
  usernames=XDevelopers,X,elonmusk&\
  user.fields=created_at,verified" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # उपयोगकर्ता नाम के आधार पर कई उपयोगकर्ता प्राप्त करें
  response = client.users.get_users_by_usernames(
      usernames=["XDevelopers", "X", "elonmusk"],
      user_fields=["created_at", "verified"]
  )

  for user in response.data:
      print(f"{user.username} - {user.created_at}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // उपयोगकर्ता नाम के आधार पर कई उपयोगकर्ता प्राप्त करें
  const response = await client.users.getUsersByUsernames({
    usernames: ["XDevelopers", "X", "elonmusk"],
    userFields: ["created_at", "verified"],
  });

  response.data?.forEach((user) => {
    console.log(`${user.username} - ${user.created_at}`);
  });
  ```
</CodeGroup>

***

<div id="available-fields">
  ## उपलब्ध फ़ील्ड्स
</div>

| फ़ील्ड              | विवरण                                |
| :------------------ | :----------------------------------- |
| `created_at`        | खाता बनाने की तारीख                  |
| `description`       | उपयोगकर्ता का बायो                   |
| `profile_image_url` | अवतार URL                            |
| `verified`          | सत्यापन की स्थिति                    |
| `public_metrics`    | फ़ॉलोअर/फ़ॉलोइंग की संख्या           |
| `location`          | उपयोगकर्ता द्वारा सेट किया गया स्थान |
| `url`               | उपयोगकर्ता की वेबसाइट                |
| `protected`         | संरक्षित खाते की स्थिति              |
| `pinned_tweet_id`   | पिन की गई पोस्ट ID                   |

***

<div id="handle-errors">
  ## त्रुटियों को संभालें
</div>

<div id="user-not-found">
  ### उपयोगकर्ता नहीं मिला
</div>

```json
{
  "errors": [
    {
      "resource_type": "user",
      "title": "Not Found Error",
      "detail": "Could not find user with username: [nonexistent_user]."
    }
  ]
}
```

<div id="protected-user">
  ### संरक्षित उपयोगकर्ता
</div>

संरक्षित उपयोगकर्ताओं का डेटा अब भी लौटाया जाता है, लेकिन जब तक आप उन्हें फ़ॉलो नहीं करते, आप उनकी पोस्ट्स नहीं देख पाएंगे।

***

<div id="next-steps">
  ## अगले चरण
</div>

<CardGroup cols={2}>
  <Card title="प्रमाणीकृत उपयोगकर्ता" icon="user-check" href="/hi/x-api/users/lookup/quickstart/authenticated-lookup">
    वर्तमान उपयोगकर्ता की जानकारी प्राप्त करें
  </Card>

  <Card title="एकीकरण मार्गदर्शिका" icon="book" href="/hi/x-api/users/lookup/integrate">
    मुख्य अवधारणाएँ और सर्वोत्तम पद्धतियाँ
  </Card>

  <Card title="API संदर्भ" icon="code" href="/hi/x-api/users/single-user-lookup">
    एंडपॉइंट का पूरा दस्तावेज़ीकरण
  </Card>
</CardGroup>