---
title: "Cliente"
sidebarTitle: "Cliente"
---

Este módulo proporciona la clase principal Client para interactuar con la X API.
Coordina todos los subclientes y gestiona la autenticación, la gestión de sesiones
y los flujos OAuth2 PKCE. Toda la funcionalidad se genera a partir de la especificación OpenAPI.

<div id="client">
  ## Client
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: object</Badge>

| None = None)

Cliente para interactuar con X API.

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

Inicializa el cliente de X API.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.base_url" type="str" default="'https://api.x.com'">
La URL base de X API (de manera predeterminada [https://api.x.com](https://api.x.com)).
</ParamField>

<ParamField path="path.bearer_token" type="str or None" default="None">
El Bearer Token para X API (autenticación solo de aplicación).
</ParamField>

<ParamField path="path.access_token" type="str or None" default="None">
El token de acceso OAuth2 para contexto de usuario (puede usarse directamente como Bearer Token).
</ParamField>

<ParamField path="path.client_id" type="str or None" default="None">
El id de cliente para X API (obligatorio para el flujo OAuth2 PKCE).
</ParamField>

<ParamField path="path.client_secret" type="str or None" default="None">
El secreto de cliente para X API.
</ParamField>

<ParamField path="path.redirect_uri" type="str or None" default="None">
La URI de redirección para la autorización OAuth2.
</ParamField>

<ParamField path="path.token" type="Dict[str, Any] or None" default="None">
Un diccionario de token OAuth2 existente (si está disponible). Si se proporciona, se extraerá access_token.
</ParamField>

<ParamField path="path.scope" type="str or List[str] or None" default="None">
Cadena de texto con espacios como separadores o lista de cadenas de texto para los scopes de autorización OAuth2.
</ParamField>

<ParamField path="path.authorization_base_url" type="str" default="'https://x.com/i'">
La URL base para la autorización OAuth2 (de manera predeterminada [https://x.com/i](https://x.com/i)).
</ParamField>

<ParamField path="path.auth" type="OAuth1">
Instancia de OAuth1 para la autenticación OAuth1.0a.
</ParamField>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.base_url" type="str" default="'https://api.x.com'"></ParamField>

<ParamField path="path.bearer_token" type="str or None" default="None"></ParamField>

<ParamField path="path.access_token" type="str or None" default="None"></ParamField>

<ParamField path="path.client_id" type="str or None" default="None"></ParamField>

<ParamField path="path.client_secret" type="str or None" default="None"></ParamField>

<ParamField path="path.redirect_uri" type="str or None" default="None"></ParamField>

<ParamField path="path.token" type="Dict[str, Any] or None" default="None"></ParamField>

<ParamField path="path.scope" type="str or List[str] or None" default="None"></ParamField>

<ParamField path="path.authorization_base_url" type="str" default="'https://x.com/i'"></ParamField>

<ParamField path="path.auth" type="OAuth1"></ParamField>

<div id="exchange_code">
  ### `exchange_code`
</div>

Intercambia un código de autorización por tokens (coincide con la API de TypeScript).

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.code" type="Any">
El código de autorización proporcionado en el callback.
</ParamField>

<ParamField path="path.code_verifier" type="Any" default="None">
Verificador de código opcional (usa el verificador almacenado si no se indica).
</ParamField>

<div id="fetch_token">
  ### `fetch_token`
</div>

Obtiene un token utilizando la URL de respuesta de autorización (método heredado).

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.authorization_response" type="Any">
La URL de callback completa que se recibe tras la autorización.
</ParamField>

<div id="get_authorization_url">
  ### `get_authorization_url`
</div>

Obtiene la URL de autorización para el flujo OAuth2 PKCE.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.state" type="Any" default="None">
Parámetro de estado opcional para mayor seguridad.
</ParamField>

<div id="is_token_expired">
  ### `is_token_expired`
</div>

Comprueba si el token OAuth2 ha caducado.

<div id="refresh_token">
  ### `refresh_token`
</div>

Renueva el token OAuth2.