---
title: "PostsClient"
sidebarTitle: "PostsClient"
---

<div id="submodules">
  ## Submódulos
</div>

* [Módulo xdk.posts.client](/es/xdks/python/reference/xdk.posts.client)
  * [`PostsClient`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient)
    * [`PostsClient.__init__()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.__init__)
    * [`PostsClient.create()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.create)
    * [`PostsClient.delete()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.delete)
    * [`PostsClient.get_analytics()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_analytics)
    * [`PostsClient.get_by_id()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_by_id)
    * [`PostsClient.get_by_ids()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_by_ids)
    * [`PostsClient.get_counts_all()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_counts_all)
    * [`PostsClient.get_counts_recent()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_counts_recent)
    * [`PostsClient.get_insights28hr()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_insights28hr)
    * [`PostsClient.get_insights_historical()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_insights_historical)
    * [`PostsClient.get_liking_users()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_liking_users)
    * [`PostsClient.get_quoted()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_quoted)
    * [`PostsClient.get_reposted_by()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_reposted_by)
    * [`PostsClient.get_reposts()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_reposts)
    * [`PostsClient.hide_reply()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.hide_reply)
    * [`PostsClient.search_all()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.search_all)
    * [`PostsClient.search_recent()`](/es/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.search_recent)
* [Módulo xdk.posts.models](/es/xdks/python/reference/xdk.posts.models)
  * [`CreateRequest`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateRequest)
    * [`CreateRequest.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateRequest.model_config)
  * [`CreateResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateResponse)
    * [`CreateResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateResponse.model_config)
  * [`DeleteResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.DeleteResponse)
    * [`DeleteResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.DeleteResponse.model_config)
  * [`GetAnalyticsResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetAnalyticsResponse)
    * [`GetAnalyticsResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetAnalyticsResponse.model_config)
  * [`GetByIdResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdResponse)
    * [`GetByIdResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdResponse.model_config)
  * [`GetByIdsResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdsResponse)
    * [`GetByIdsResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdsResponse.model_config)
  * [`GetCountsAllResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsAllResponse)
    * [`GetCountsAllResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsAllResponse.model_config)
  * [`GetCountsRecentResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsRecentResponse)
    * [`GetCountsRecentResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsRecentResponse.model_config)
  * [`GetInsights28hrResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsights28hrResponse)
    * [`GetInsights28hrResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsights28hrResponse.model_config)
  * [`GetInsightsHistoricalResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsightsHistoricalResponse)
    * [`GetInsightsHistoricalResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsightsHistoricalResponse.model_config)
  * [`GetLikingUsersResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetLikingUsersResponse)
    * [`GetLikingUsersResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetLikingUsersResponse.model_config)
  * [`GetQuotedResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetQuotedResponse)
    * [`GetQuotedResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetQuotedResponse.model_config)
  * [`GetRepostedByResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostedByResponse)
    * [`GetRepostedByResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostedByResponse.model_config)
  * [`GetRepostsResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostsResponse)
    * [`GetRepostsResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostsResponse.model_config)
  * [`HideReplyRequest`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyRequest)
    * [`HideReplyRequest.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyRequest.model_config)
  * [`HideReplyResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyResponse)
    * [`HideReplyResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyResponse.model_config)
  * [`SearchAllResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchAllResponse)
    * [`SearchAllResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchAllResponse.model_config)
  * [`SearchRecentResponse`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchRecentResponse)
    * [`SearchRecentResponse.model_config`](/es/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchRecentResponse.model_config)

<div id="module-contents">
  ## Contenido del módulo
</div>

Este módulo proporciona acceso a los endpoints de publicaciones de la X API
y sirve como el punto de entrada principal para toda la funcionalidad relacionada con las publicaciones.

<div id="class-xdkpostspostsclient">
  ### `class xdk.posts.PostsClient`
</div>

Cliente para operaciones con publicaciones

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client" />

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client" />

<div id="create">
  ### `create`
</div>

Crear o editar una publicación
Crea una nueva publicación para el usuario autenticado, o edita una publicación existente si se proporcionan edit&#95;options.
body: Cuerpo de la solicitud
:returns: datos de la respuesta
:rtype: CreateResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateRequest" />

<div id="delete">
  ### `delete`
</div>

Eliminar publicación
Elimina una publicación específica por su id si pertenece al usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
  El ID de la Publicación que se va a eliminar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`DeleteResponse` - datos de respuesta

<div id="get_analytics">
  ### `get_analytics`
</div>

Obtener estadísticas de Publicaciones
Recupera datos analíticos de las Publicaciones especificadas en un intervalo de tiempo definido.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.ids" type="List">
  Una lista separada por comas de ids de Publicaciones. Se permiten hasta 100 en una sola solicitud.
</ParamField>

<ParamField path="path.end_time" type="str">
  AAAA-MM-DDTHH:mm:ssZ. La marca de tiempo en UTC que representa el final del intervalo de tiempo.
</ParamField>

<ParamField path="path.start_time" type="str">
  AAAA-MM-DDTHH:mm:ssZ. La marca de tiempo en UTC que representa el inicio del intervalo de tiempo.
</ParamField>

<ParamField path="path.granularity" type="str">
  La granularidad de los resultados de recuentos de búsqueda.
</ParamField>

<ParamField path="path.analytics_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Analytics que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetAnalyticsResponse` - Datos de respuesta

<div id="get_by_id">
  ### `get_by_id`
</div>

Obtener una Publicación por ID
Obtiene los detalles de una Publicación específica por su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
  Un único id de la Publicación.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  Una lista de campos de Tweet separados por comas para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  Una lista de campos separados por comas para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  Una lista de campos de Media separados por comas para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  Una lista de campos de Poll separados por comas para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  Una lista de campos de User separados por comas para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  Una lista de campos de Place separados por comas para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByIdResponse` - datos de la respuesta

<div id="get_by_ids">
  ### `get_by_ids`
</div>

Obtener Publicaciones por id
Recupera los detalles de múltiples Publicaciones utilizando sus id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.ids" type="List">
  Una lista separada por comas de ids de Publicaciones. Se permite incluir hasta 100 en una sola solicitud.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Tuit para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  Una lista separada por comas de campos de medios para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  Una lista separada por comas de campos de encuestas para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  Una lista separada por comas de campos de lugar para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByIdsResponse` - Datos de la respuesta

<div id="get_counts_all">
  ### `get_counts_all`
</div>

Obtener el recuento total de todas las publicaciones
Obtiene el número de publicaciones que coinciden con una consulta de búsqueda en todo el archivo.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="str">
  Una consulta/regla/filtro para hacer coincidir Publicaciones. Consulta [https://t.co/rulelength](https://t.co/rulelength) para identificar la longitud máxima de la consulta.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más antigua (de los últimos 7 días) a partir de la cual se proporcionarán las Publicaciones. La marca de tiempo tiene granularidad de segundos y es inclusiva (es decir, 12:00:01 incluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las Publicaciones. La marca de tiempo tiene granularidad de segundos y es exclusiva (es decir, 12:00:01 excluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  Devuelve resultados con un id de Publicación mayor que (es decir, más reciente que) el id especificado.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  Devuelve resultados con un id de Publicación menor que (es decir, más antigua que) el id especificado.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  Este parámetro se utiliza para obtener la siguiente «página» de resultados. El valor utilizado con el parámetro se extrae directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  Este parámetro se utiliza para obtener la siguiente «página» de resultados. El valor utilizado con el parámetro se extrae directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
  La granularidad para los resultados de recuento de búsqueda.
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
  Una lista separada por comas de campos SearchCount que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetCountsAllResponse`

<div id="get_counts_recent">
  ### `get_counts_recent`
</div>

Obtiene el número de Publicaciones recientes
Devuelve el número de Publicaciones de los últimos 7 días que coinciden con una consulta de búsqueda.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="str">
  Una consulta/regla/filtro para hacer coincidir las Publicaciones. Consulta [https://t.co/rulelength](https://t.co/rulelength) para identificar la longitud máxima de la consulta.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más antigua (de los últimos 7 días) a partir de la cual se proporcionarán las Publicaciones. La marca de tiempo tiene granularidad de segundos y es inclusiva (es decir, 12:00:01 incluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las Publicaciones. La marca de tiempo tiene granularidad de segundos y es exclusiva (es decir, 12:00:01 excluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  Devuelve resultados con un id de Publicación mayor (es decir, más reciente) que el id especificado.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  Devuelve resultados con un id de Publicación menor (es decir, más antiguo) que el id especificado.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  Este parámetro se usa para obtener la siguiente ‘página’ de resultados. El valor utilizado con este parámetro se extrae directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  Este parámetro se usa para obtener la siguiente ‘página’ de resultados. El valor utilizado con este parámetro se extrae directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
  La granularidad de los resultados de recuentos de búsqueda.
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
  Una lista separada por comas de campos SearchCount que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetCountsRecentResponse`

<div id="get_insights28hr">
  ### `get_insights28hr`
</div>

Obtener estadísticas de Publicaciones de las últimas 28 horas
Obtiene métricas de interacción para las Publicaciones especificadas durante las últimas 28 horas.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.tweet_ids" type="List">
  Lista de PostIds para métricas de 28 horas.
</ParamField>

<ParamField path="path.granularity" type="str">
  Granularidad de la respuesta de métricas.
</ParamField>

<ParamField path="path.requested_metrics" type="List">
  Métricas solicitadas para la consulta histórica.
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
  Una lista separada por comas de campos de engagement que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetInsights28hrResponse` - datos de la respuesta

<div id="get_insights_historical">
  ### `get_insights_historical`
</div>

Obtener estadísticas históricas de Publicaciones
Obtiene métricas históricas de interacción para las Publicaciones especificadas dentro de un intervalo de tiempo determinado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.tweet_ids" type="List">
  Lista de ids de Publicaciones para métricas históricas.
</ParamField>

<ParamField path="path.end_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo en UTC que representa el final del intervalo de tiempo.
</ParamField>

<ParamField path="path.start_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo en UTC que representa el inicio del intervalo de tiempo.
</ParamField>

<ParamField path="path.granularity" type="str">
  Granularidad de la respuesta de métricas.
</ParamField>

<ParamField path="path.requested_metrics" type="List">
  Métricas solicitadas para la consulta histórica.
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Engagement que se van a mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetInsightsHistoricalResponse` - datos de la respuesta

<div id="get_liking_users">
  ### `get_liking_users`
</div>

Obtener usuarios que dieron Me gusta
Obtiene una lista de usuarios que dieron Me gusta a una Publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
  Un único identificador de la Publicación.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  Este parámetro se usa para obtener la siguiente &quot;página&quot; de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  Una lista separada por comas de campos de tuit que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetLikingUsersResponse`

<div id="get_quoted">
  ### `get_quoted`
</div>

Obtener publicaciones citadas
Devuelve una lista de publicaciones que citan una publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
  Un único id de la Publicación.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  El número máximo de resultados que se deben devolver.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  Este parámetro se utiliza para obtener una «página» específica de resultados.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
  El conjunto de entidades que se van a excluir (por ejemplo, «replies» o «retweets»).
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  Una lista de campos de Tuit, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  Una lista de campos, separados por comas, que se expandirán.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  Una lista de campos de medios, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  Una lista de campos de encuesta, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  Una lista de campos de usuario, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  Una lista de campos de lugar, separados por comas, que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetQuotedResponse`

<div id="get_reposted_by">
  ### `get_reposted_by`
</div>

Obtener usuarios que hicieron repost
Recupera una lista de usuarios que hicieron repost de una Publicación específica mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
  Un único id de la Publicación.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  Este parámetro se utiliza para obtener la siguiente «página» de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  Una lista de campos de usuario, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  Una lista de campos, separados por comas, que se expandirán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  Una lista de campos de Tuit, separados por comas, que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetRepostedByResponse`

<div id="get_reposts">
  ### `get_reposts`
</div>

Obtener reposts
Recupera una lista de Publicaciones que republican una Publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
  El id de una única Publicación.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  La cantidad máxima de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  Este parámetro se usa para obtener la siguiente «página» de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Tuit que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Media que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  Una lista separada por comas de campos de encuesta (Poll) que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Usuario que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  Una lista separada por comas de campos de lugar (Place) que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetRepostsResponse`

<div id="hide_reply">
  ### `hide_reply`
</div>

Ocultar respuesta
Oculta o deja de ocultar una respuesta en una conversación que pertenece al usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.tweet_id" type="Any">
  El ID de la respuesta que deseas ocultar o volver a mostrar.
</ParamField>

<ParamField path="body.body" type="HideReplyRequest">
  Cuerpo de la solicitud
</ParamField>

<div id="search_all">
  ### `search_all`
</div>

Buscar en todas las Publicaciones
Recupera Publicaciones del archivo completo que coinciden con una consulta de búsqueda.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="str">
  Una consulta/regla/filtro para hacer coincidir Publicaciones. Consulta [https://t.co/rulelength](https://t.co/rulelength) para identificar la longitud máxima de la consulta.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más antigua a partir de la cual se proporcionarán las Publicaciones. La marca de tiempo tiene granularidad de segundos y es inclusiva (es decir, 12:00:01 incluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las Publicaciones. La marca de tiempo tiene granularidad de segundos y es exclusiva (es decir, 12:00:01 excluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  Devuelve resultados con un ID de Publicación mayor que (es decir, más reciente que) el ID especificado.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  Devuelve resultados con un ID de Publicación menor que (es decir, más antiguo que) el ID especificado.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  El número máximo de resultados de búsqueda que se devolverán en una solicitud.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  Este parámetro se usa para obtener la siguiente &quot;página&quot; de resultados. El valor utilizado con el parámetro se obtiene directamente de la Respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  Este parámetro se usa para obtener la siguiente &quot;página&quot; de resultados. El valor utilizado con el parámetro se obtiene directamente de la Respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
  El orden en el que se devolverán los resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Tweet que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Media que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Poll que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  Una lista separada por comas de campos de User que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Place que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorSearchAllResponse`

<div id="search_recent">
  ### `search_recent`
</div>

Buscar publicaciones recientes
Obtiene publicaciones de los últimos 7 días que coinciden con una consulta de búsqueda.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="str">
  Una consulta/regla/filtro para hacer coincidir con Publicaciones. Consulta [https://t.co/rulelength](https://t.co/rulelength) para identificar la longitud máxima de la consulta.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más antigua desde la cual se proporcionarán las Publicaciones. La marca de tiempo tiene granularidad de segundos y es inclusiva (es decir, 12:00:01 incluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente (más nueva) hasta la cual se proporcionarán las Publicaciones. La marca de tiempo tiene granularidad de segundos y es exclusiva (es decir, 12:00:01 excluye el primer segundo del minuto).
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  Devuelve resultados con un ID de Publicación mayor (es decir, más reciente) que el ID especificado.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  Devuelve resultados con un ID de Publicación menor (es decir, más antiguo) que el ID especificado.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  El número máximo de resultados de búsqueda que se devolverán en una solicitud.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  Este parámetro se usa para obtener la siguiente ‘página’ de resultados. El valor que se pasa a este parámetro se obtiene directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  Este parámetro se usa para obtener la siguiente ‘página’ de resultados. El valor que se pasa a este parámetro se obtiene directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
  El orden en el que se devolverán los resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Tweet para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Media para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Poll para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  Una lista separada por comas de campos de User para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Place para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorSearchRecentResponse`