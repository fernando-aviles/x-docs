---
title: "UsersClient"
sidebarTitle: "UsersClient"
---

<div id="usersclient">
  ## UsersClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: object</Badge>

Cliente para operaciones con usuarios

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="block_dms">
  ### `block_dms`
</div>

Bloquear MDs
Bloquea los mensajes directos entrantes o salientes de un usuario específico mediante su id para el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de destino para el que el usuario autenticado solicita bloquear los mensajes directos.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`BlockDmsResponse` - datos de la respuesta

<div id="create_bookmark">
  ### `create_bookmark`
</div>

Crear marcador
Añade una publicación a los marcadores del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado para el que se agregarán marcadores.
</ParamField>

<ParamField path="body.body" type="CreateBookmarkRequest">
Cuerpo de la solicitud
</ParamField>

<div id="delete_bookmark">
  ### `delete_bookmark`
</div>

Eliminar marcador
Elimina una Publicación de los marcadores del usuario autenticado mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado cuyo marcador se va a eliminar.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
El id de la Publicación que el usuario de origen está quitando de sus marcadores.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`DeleteBookmarkResponse` - Datos de la respuesta

<div id="follow_list">
  ### `follow_list`
</div>

Seguir una lista
Hace que el usuario autenticado siga una lista específica mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado que seguirá la lista.
</ParamField>

<ParamField path="body.body" type="FollowListRequest">
Cuerpo de la solicitud
</ParamField>

<div id="follow_user">
  ### `follow_user`
</div>

Seguir a un usuario
Permite que el usuario autenticado siga a un usuario específico por su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado que solicita seguir al usuario de destino.
</ParamField>

<ParamField path="body.body" type="FollowUserRequest">
Cuerpo de la solicitud
</ParamField>

<div id="get_blocking">
  ### `get_blocking`
</div>

Obtener bloqueos
Recupera una lista de usuarios bloqueados por el usuario cuyo id se especifica.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado para el que se devolverán los resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una «página» específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se deben expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetBlockingResponse`

<div id="get_bookmark_folders">
  ### `get_bookmark_folders`
</div>

Obtener las carpetas de marcadores
Obtiene una lista de carpetas de marcadores creadas por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario de origen autenticado para el que se devolverán resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente «página» de resultados.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetBookmarkFoldersResponse`

<div id="get_bookmarks">
  ### `get_bookmarks`
</div>

Obtener marcadores
Obtiene una lista de Publicaciones añadidas a marcadores por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario de origen autenticado para el cual se devolverán los resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente «página» de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de Media para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de Poll para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de Usuario para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de Place para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetBookmarksResponse`

<div id="get_bookmarks_by_folder_id">
  ### `get_bookmarks_by_folder_id`
</div>

Obtener marcadores por ID de carpeta
Recupera publicaciones en una carpeta de marcadores específica por su ID para el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
La id del Usuario de origen autenticado para el cual se devolverán los resultados.
</ParamField>

<ParamField path="path.folder_id" type="Any">
La id de la carpeta de marcadores para la cual el Usuario autenticado intenta obtener Publicaciones.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetBookmarksByFolderIdResponse` - Datos de la respuesta

<div id="get_by_id">
  ### `get_by_id`
</div>

Obtener usuario por ID
Obtiene los detalles de un usuario específico por su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
La ID del usuario que se va a consultar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByIdResponse` - Datos de respuesta

<div id="get_by_ids">
  ### `get_by_ids`
</div>

Obtener usuarios por id
Recupera los detalles de varios usuarios por sus id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.ids" type="List">
Una lista de id de usuario separados por comas. Puedes especificar hasta 100 id.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByIdsResponse` - datos de la respuesta

<div id="get_by_username">
  ### `get_by_username`
</div>

Obtener un User por nombre de usuario
Recupera los detalles de un User específico a partir de su nombre de usuario.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.username" type="str">
Un nombre de usuario.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separada por comas, para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separada por comas, para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Tuit, separada por comas, para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByUsernameResponse` - datos de la respuesta

<div id="get_by_usernames">
  ### `get_by_usernames`
</div>

Obtener usuarios por nombre de usuario
Recupera los detalles de varios usuarios a partir de sus nombres de usuario.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.usernames" type="List">
Una lista de nombres de usuario, separados por comas.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Tuit, separados por comas, para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByUsernamesResponse` - datos de la respuesta

<div id="get_followed_lists">
  ### `get_followed_lists`
</div>

Obtener las Listas seguidas
Recupera una colección de Listas seguidas por un usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una “página” específica de resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista separada por comas de campos de List que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de User que se van a mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetFollowedListsResponse`

<div id="get_followers">
  ### `get_followers`
</div>

Obtener seguidores
Obtiene una lista de Usuarios que siguen a un Usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario que se va a buscar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una ‘página’ específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit que se van a mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetFollowersResponse`

<div id="get_following">
  ### `get_following`
</div>

Obtener seguidos
Devuelve una lista de usuarios a los que sigue un usuario específico, identificado por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una «página» específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de Usuario que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetFollowingResponse`

<div id="get_liked_posts">
  ### `get_liked_posts`
</div>

Obtener Publicaciones a las que se ha dado Me gusta
Recupera una lista de Publicaciones a las que un Usuario específico ha dado Me gusta mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente "página" de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de medios para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuesta para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetLikedPostsResponse`

<div id="get_list_memberships">
  ### `get_list_memberships`
</div>

Obtener membresías de Listas
Obtiene una lista de Listas de las que un usuario específico es miembro, usando su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario que se desea consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una “página” específica de resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista separada por comas de campos de List que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos a expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de User que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetListMembershipsResponse`

<div id="get_me">
  ### `get_me`
</div>

Obtener mi usuario
Obtiene los detalles del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Tuit, separados por comas, para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetMeResponse` - datos de la respuesta

<div id="get_mentions">
  ### `get_mentions`
</div>

Obtener menciones
Obtiene una lista de Publicaciones que mencionan a un usuario específico por su id.

#### Parameters

<ParamField path="path.id" type="Any">
El id del usuario que se va a consultar.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
El id mínimo de la Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre start_time si ambos se especifican.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
El id máximo de la Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre end_time si ambos se especifican.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente «página» de resultados.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más antigua a partir de la cual se proporcionarán las Publicaciones. El parámetro since_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las Publicaciones. El parámetro until_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Tweet, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se expandirán.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista de campos de Media, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista de campos de Poll, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de User, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista de campos de Place, separados por comas, que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetMentionsResponse`

<div id="get_muting">
  ### `get_muting`
</div>

Obtener usuarios silenciados
Obtiene una lista de usuarios silenciados por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El identificador del Usuario de origen autenticado para el que se devolverán resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente "página" de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de Usuario que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetMutingResponse`

<div id="get_owned_lists">
  ### `get_owned_lists`
</div>

Obtiene las Listas propias
Obtiene una lista de Listas que son propiedad de un usuario específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a buscar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una “página” específica de resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista separada por comas de campos de List para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetOwnedListsResponse`

<div id="get_pinned_lists">
  ### `get_pinned_lists`
</div>

Obtener listas fijadas
Devuelve una lista de listas fijadas por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado para el que se devolverán resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista separada por comas de campos de List que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de User que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetPinnedListsResponse` - Datos de la respuesta

<div id="get_posts">
  ### `get_posts`
</div>

Obtener publicaciones
Obtiene una lista de publicaciones creadas por un Usuario específico a partir de su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario que se va a consultar.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
El id mínimo de Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre start_time si ambos se especifican.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
El id máximo de Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre end_time si ambos se especifican.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente “página” de resultados.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
El conjunto de entidades que se deben excluir (por ejemplo, “replies” o “retweets”).
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más temprana desde la cual se proporcionarán las Publicaciones. El parámetro since_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las Publicaciones. El parámetro until_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de Media que se van a mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de Poll que se van a mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de User que se van a mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de Place que se van a mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetPostsResponse`

<div id="get_reposts_of_me">
  ### `get_reposts_of_me`
</div>

Obtener los reposts de mi cuenta
Obtiene una lista de publicaciones que hacen repost del contenido del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente "página" de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de Media que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de Poll que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de User que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de Place que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetRepostsOfMeResponse`

<div id="get_timeline">
  ### `get_timeline`
</div>

Obtener Timeline
Recupera una lista de Publicaciones en orden cronológico inverso en el Timeline del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario de origen autenticado para listar las Publicaciones de la cronología inversa.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
El ID mínimo de la Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre start_time si ambos se especifican.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
El ID máximo de la Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre end_time si ambos se especifican.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente “página” de resultados.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
El conjunto de entidades que se van a excluir (por ejemplo, “replies” o “retweets”).
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más temprana desde la cual se proporcionarán las Publicaciones. El parámetro since_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las Publicaciones. El parámetro until_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos a ampliar.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de medios para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuesta para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetTimelineResponse`

<div id="like_post">
  ### `like_post`
</div>

Marcar como me gusta una Publicación
Hace que el usuario autenticado marque con “Me gusta” una Publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario de origen autenticado que solicita marcar que le gusta la Publicación.
</ParamField>

<ParamField path="body.body" type="LikePostRequest">
Cuerpo de la solicitud
</ParamField>

<div id="mute_user">
  ### `mute_user`
</div>

Silenciar usuario
Permite que el usuario autenticado silencie a un usuario específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado que solicita silenciar al Usuario de destino.
</ParamField>

<ParamField path="body.body" type="MuteUserRequest">
Cuerpo de la solicitud
</ParamField>

<div id="pin_list">
  ### `pin_list`
</div>

Fijar Lista
Permite que el usuario autenticado fije una Lista específica mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario de origen autenticado que fijará la Lista.
</ParamField>

<ParamField path="body.body" type="PinListRequest">
Cuerpo de la solicitud
</ParamField>

<div id="repost_post">
  ### `repost_post`
</div>

Republicar publicación
Hace que el usuario autenticado vuelva a publicar una Publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario de origen autenticado que solicita volver a publicar la Publicación.
</ParamField>

<ParamField path="body.body" type="RepostPostRequest">
Cuerpo de la solicitud
</ParamField>

<div id="search">
  ### `search`
</div>

Buscar usuarios
Obtiene una lista de usuarios que coinciden con una consulta de búsqueda.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="Any">
La cadena de consulta con la que se buscan usuarios.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente "página" de resultados. El valor utilizado con el parámetro se toma directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se deben expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorSearchResponse`

<div id="unblock_dms">
  ### `unblock_dms`
</div>

Desbloquear MD
Desbloquea los mensajes directos de o hacia un Usuario específico, identificado por su id, para el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del User de destino para el cual el usuario autenticado solicita desbloquear los DMs.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`UnblockDmsResponse` - datos de la respuesta

<div id="unfollow_list">
  ### `unfollow_list`
</div>

Dejar de seguir una Lista
Hace que el usuario autenticado deje de seguir una Lista específica por su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado que dejará de seguir la Lista.
</ParamField>

<ParamField path="path.list_id" type="Any">
El id de la Lista que se va a dejar de seguir.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`UnfollowListResponse` - Datos de la respuesta

<div id="unfollow_user">
  ### `unfollow_user`
</div>

Dejar de seguir a un usuario
Hace que el usuario autenticado deje de seguir a un usuario específico identificado por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.source_user_id" type="Any">
El id del usuario de origen autenticado que solicita dejar de seguir al usuario de destino.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
El id del usuario de destino al que el usuario de origen solicita dejar de seguir.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`UnfollowUserResponse` - datos de la respuesta

<div id="unlike_post">
  ### `unlike_post`
</div>

Quitar el Me gusta de la Publicación
Hace que el usuario autenticado quite el Me gusta de una Publicación específica por su id.

#### Parameters

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado que solicita quitar el Me gusta de la publicación.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
El id de la publicación a la que el usuario solicita quitar el Me gusta.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`UnlikePostResponse` - datos de la respuesta

<div id="unmute_user">
  ### `unmute_user`
</div>

Quitar silencio de usuario
Hace que el usuario autenticado deje de silenciar a un usuario específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.source_user_id" type="Any">
El ID del Usuario de origen autenticado que solicita dejar de silenciar al Usuario de destino.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
El ID del Usuario que el Usuario de origen solicita dejar de silenciar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`UnmuteUserResponse` - datos de la Respuesta

<div id="unpin_list">
  ### `unpin_list`
</div>

Desanclar lista
Hace que el usuario autenticado desancle una lista específica mediante su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado para el cual se devolverán los resultados.
</ParamField>

<ParamField path="path.list_id" type="Any">
El id de la Lista que se va a desanclar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`UnpinListResponse` - data de la respuesta

<div id="unrepost_post">
  ### `unrepost_post`
</div>

Quitar republicación de Publicación
Hace que el usuario autenticado deje de republicar una Publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario de origen autenticado que solicita republicar la Publicación.
</ParamField>

<ParamField path="path.source_tweet_id" type="Any">
El ID de la Publicación cuya republicación el Usuario solicita deshacer.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`UnrepostPostResponse` - Datos de la Respuesta