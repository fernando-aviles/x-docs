---
title: "UsersClient"
sidebarTitle: "UsersClient"
---

<div id="usersclient">
  ## UsersClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Base: objeto</Badge>

Cliente para operaciones con usuarios

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="block_dms">
  ### `block_dms`
</div>

Bloquear MD

Bloquea los mensajes directos de o hacia un usuario específico mediante su id para el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de destino para el que el usuario autenticado solicita bloquear los DMs.
</ParamField>

#### Devuelve

`BlockDmsResponse` - datos de la respuesta

<div id="create_bookmark">
  ### `create_bookmark`
</div>

Crear marcador
Añade una publicación a los marcadores del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado al que se le añadirán marcadores.
</ParamField>

<ParamField path="body.body" type="CreateBookmarkRequest">
Cuerpo de la solicitud
</ParamField>

<div id="delete_bookmark">
  ### `delete_bookmark`
</div>

Eliminar marcador
Elimina una Publicación de los Marcadores del usuario autenticado mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario de origen autenticado cuyo marcador se va a eliminar.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
El ID de la Publicación que el Usuario de origen está eliminando de sus marcadores.
</ParamField>

#### Devuelve

`DeleteBookmarkResponse` - datos de la respuesta

<div id="follow_list">
  ### `follow_list`
</div>

Seguir lista
Hace que el usuario autenticado siga una lista específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado que seguirá la Lista.
</ParamField>

<ParamField path="body.body" type="FollowListRequest">
Cuerpo de la solicitud
</ParamField>

<div id="follow_user">
  ### `follow_user`
</div>

Seguir a un usuario
Hace que el usuario autenticado comience a seguir a un usuario específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario autenticado de origen que solicita seguir al usuario de destino.
</ParamField>

<ParamField path="body.body" type="FollowUserRequest">
Cuerpo de la solicitud
</ParamField>

<div id="get_blocking">
  ### `get_blocking`
</div>

Obtener usuarios bloqueados
Obtiene una lista de usuarios bloqueados por el ID de usuario especificado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado para el cual se devolverán los resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una “página” específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetBlockingResponse`

<div id="get_bookmark_folders">
  ### `get_bookmark_folders`
</div>

Obtener carpetas de Marcadores
Devuelve una lista de carpetas de Marcadores creadas por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario autenticado de origen para el que se devolverán los resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente página de resultados.
</ParamField>

#### Devuelve

`IteratorGetBookmarkFoldersResponse`

<div id="get_bookmarks">
  ### `get_bookmarks`
</div>

Obtener marcadores
Obtiene una lista de Publicaciones añadidas a marcadores por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado para el que se devolverán los resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente «página» de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Tuit, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se expandirán.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista de campos de Media, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista de campos de Poll, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de Usuario, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista de campos de Place, separados por comas, que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetBookmarksResponse`

<div id="get_bookmarks_by_folder_id">
  ### `get_bookmarks_by_folder_id`
</div>

Obtener marcadores por ID de carpeta
Recupera las Publicaciones en una carpeta de marcadores específica por su ID del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario de origen autenticado para el que se devolverán resultados.
</ParamField>

<ParamField path="path.folder_id" type="Any">
El ID de la carpeta de marcadores para la que el Usuario autenticado está intentando recuperar Publicaciones.
</ParamField>

<div id="returns">
  #### Valores devueltos
</div>

`GetBookmarksByFolderIdResponse` - datos de la respuesta

<div id="get_by_id">
  ### `get_by_id`
</div>

Obtener usuario por ID
Obtiene los detalles de un usuario específico por su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario que se va a buscar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit que se van a mostrar.
</ParamField>

#### Devuelve

`GetByIdResponse` - datos de la respuesta

<div id="get_by_ids">
  ### `get_by_ids`
</div>

Obtener usuarios por id
Obtiene los detalles de varios usuarios a partir de sus id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.ids" type="List">
Una lista de identificadores de usuario separados por comas. Puedes especificar hasta 100 identificadores.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit para mostrar.
</ParamField>

#### Devuelve

`GetByIdsResponse` - Datos de la respuesta

<div id="get_by_username">
  ### `get_by_username`
</div>

Obtener usuario por nombre de usuario  
Recupera los detalles de un usuario específico a partir de su nombre de usuario.

#### Parameters

<ParamField path="path.username" type="str">
Un nombre de usuario.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit para mostrar.
</ParamField>

#### Devuelve

`GetByUsernameResponse` - Datos de la respuesta

<div id="get_by_usernames">
  ### `get_by_usernames`
</div>

Obtener usuarios por nombres de usuario
Recupera los detalles de varios usuarios por sus nombres de usuario.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.usernames" type="List">
Una lista de nombres de usuario separados por comas.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se ampliarán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Tuit, separados por comas, que se mostrarán.
</ParamField>

#### Devuelve

`GetByUsernamesResponse` - Datos de la respuesta

<div id="get_followed_lists">
  ### `get_followed_lists`
</div>

Obtener listas seguidas
Obtiene una lista de Listas seguidas por un Usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una «página» específica de resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista separada por comas de campos de List para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

#### Devuelve

`IteratorGetFollowedListsResponse`

<div id="get_followers">
  ### `get_followers`
</div>

Obtener seguidores
Obtiene una lista de usuarios que siguen a un usuario específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una página específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit que se mostrarán.
</ParamField>

#### Valor devuelto

`IteratorGetFollowersResponse`

<div id="get_following">
  ### `get_following`
</div>

Obtener seguidos
Obtiene una lista de usuarios a los que sigue un usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una "página" específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetFollowingResponse`

<div id="get_liked_posts">
  ### `get_liked_posts`
</div>

Obtener Publicaciones con Me gusta
Obtiene una lista de Publicaciones a las que un usuario específico ha dado Me gusta, por su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente ‘página’ de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se deben expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de Media para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuesta para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar para mostrar.
</ParamField>

#### Devuelve

`IteratorGetLikedPostsResponse`

<div id="get_list_memberships">
  ### `get_list_memberships`
</div>

Obtener membresías en Listas
Recupera una lista de Listas de las que un Usuario específico es miembro, mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una "página" específica de resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista de campos de List separados por comas para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos separados por comas para expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de User separados por comas para mostrar.
</ParamField>

#### Devuelve

`IteratorGetListMembershipsResponse`

<div id="get_me">
  ### `get_me`
</div>

Obtener mi usuario
Obtiene los detalles del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Tuit, separados por comas, para mostrar.
</ParamField>

#### Devuelve

`GetMeResponse` - datos de la respuesta

<div id="get_mentions">
  ### `get_mentions`
</div>

Obtener menciones
Obtiene una lista de Publicaciones que mencionan a un Usuario específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El identificador del usuario que se va a buscar.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
El identificador mínimo de publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre start_time si se especifican ambos.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
El identificador máximo de publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre end_time si se especifican ambos.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente "página" de resultados.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más antigua a partir de la cual se proporcionarán las publicaciones. El parámetro since_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las publicaciones. El parámetro until_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Tweet, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos a expandir, separados por comas.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista de campos de medios, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista de campos de encuesta, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista de campos de lugar, separados por comas, para mostrar.
</ParamField>

#### Devuelve

`IteratorGetMentionsResponse`

<div id="get_muting">
  ### `get_muting`
</div>

Obtener silenciamientos
Obtiene una lista de usuarios silenciados por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario de origen autenticado para el cual se devolverán los resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente "página" de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None" >
Una lista separada por comas de campos de Tuit que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetMutingResponse`

<div id="get_owned_lists">
  ### `get_owned_lists`
</div>

Obtener listas propias
Obtiene una lista de listas propiedad de un usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a buscar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una «página» específica de resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista de campos de List, separados por comas, que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se van a expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de User, separados por comas, que se van a mostrar.
</ParamField>

#### Devuelve

`IteratorGetOwnedListsResponse`

<div id="get_pinned_lists">
  ### `get_pinned_lists`
</div>

Obtener Listas fijadas
Devuelve un listado de Listas fijadas por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado para el que se devolverán los resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista separada por comas de campos de List que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de User que se mostrarán.
</ParamField>

#### Devuelve

`GetPinnedListsResponse` - Datos de la Respuesta

<div id="get_posts">
  ### `get_posts`
</div>

Obtener Publicaciones
Obtiene una lista de publicaciones creadas por un usuario específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a buscar.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
El ID mínimo de la Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre start_time si se especifican ambos.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
El ID máximo de la Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre end_time si se especifican ambos.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente “página” de resultados.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
El conjunto de entidades que se van a excluir (por ejemplo, “replies” o “retweets”).
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más antigua a partir de la cual se proporcionarán las publicaciones. El parámetro since_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las publicaciones. El parámetro until_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Tweet separados por comas que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos que se van a expandir, separados por comas.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista de campos de Media separados por comas que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista de campos de Poll separados por comas que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de User separados por comas que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista de campos de Place separados por comas que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetPostsResponse`

<div id="get_reposts_of_me">
  ### `get_reposts_of_me`
</div>

Obtener mis Reposts
Obtiene una lista de Publicaciones que son Reposts del contenido del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente “página” de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de medios que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuestas que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuarios que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugares que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetRepostsOfMeResponse`

<div id="get_timeline">
  ### `get_timeline`
</div>

Obtener cronología
Obtiene una lista de Publicaciones en orden cronológico inverso en la cronología del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del User autenticado cuya cronología inversa se usará para listar las Publicaciones.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
El ID mínimo de Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre start_time si ambos se especifican.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
El ID máximo de Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre end_time si ambos se especifican.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente «página» de resultados.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
El conjunto de entidades que se deben excluir (por ejemplo, «replies» o «retweets»).
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más temprana a partir de la cual se proporcionarán las Publicaciones. El parámetro since_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las Publicaciones. El parámetro until_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista, separada por comas, de campos de Tweet que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista, separada por comas, de campos que se deben expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista, separada por comas, de campos de Media que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista, separada por comas, de campos de Poll que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista, separada por comas, de campos de User que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista, separada por comas, de campos de Place que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetTimelineResponse`

<div id="like_post">
  ### `like_post`
</div>

Indicar Me gusta en la publicación
Hace que el usuario autenticado indique Me gusta en una publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario de origen autenticado que solicita indicar "Me gusta" en la Publicación.
</ParamField>

<ParamField path="body.body" type="LikePostRequest">
Cuerpo de la solicitud
</ParamField>

<div id="mute_user">
  ### `mute_user`
</div>

Silenciar usuario
Permite que el usuario autenticado silencie a un usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado que solicita silenciar al Usuario de destino.
</ParamField>

<ParamField path="body.body" type="MuteUserRequest">
Cuerpo de la solicitud
</ParamField>

<div id="pin_list">
  ### `pin_list`
</div>

Fijar lista
Permite que el usuario autenticado fije una lista específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario de origen autenticado que fijará la lista.
</ParamField>

<ParamField path="body.body" type="PinListRequest">
Cuerpo de la solicitud
</ParamField>

<div id="repost_post">
  ### `repost_post`
</div>

Republicar Publicación
Hace que el usuario autenticado vuelva a publicar una Publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
La id del usuario de origen autenticado que solicita republicar la Publicación.
</ParamField>

<ParamField path="body.body" type="RepostPostRequest">
Cuerpo de la solicitud
</ParamField>

<div id="search">
  ### `search`
</div>

Buscar usuarios
Obtiene una lista de usuarios que coinciden con una consulta de búsqueda.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="Any">
La cadena de consulta con la que se buscará a los usuarios.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente "página" de resultados. El valor usado con el parámetro se extrae directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit que se van a mostrar.
</ParamField>

#### Devuelve

`IteratorSearchResponse`

<div id="unblock_dms">
  ### `unblock_dms`
</div>

Desbloquear MD
Desbloquea los mensajes directos de o hacia un usuario específico mediante su id para el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de destino cuyos mensajes directos el usuario autenticado solicita desbloquear.
</ParamField>

#### Devuelve

`UnblockDmsResponse` - datos de la respuesta

<div id="unfollow_list">
  ### `unfollow_list`
</div>

Dejar de seguir una lista
Hace que el usuario autenticado deje de seguir una lista específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
La ID del usuario de origen autenticado que dejará de seguir la Lista.
</ParamField>

<ParamField path="path.list_id" type="Any">
La ID de la Lista a la que se dejará de seguir.
</ParamField>

#### Devuelve

`UnfollowListResponse` - Datos de la respuesta

<div id="unfollow_user">
  ### `unfollow_user`
</div>

Dejar de seguir a un usuario
Hace que el usuario autenticado deje de seguir a un usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.source_user_id" type="Any">
El id del Usuario de origen autenticado que solicita dejar de seguir al Usuario de destino.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
El id del Usuario al que el Usuario de origen solicita dejar de seguir.
</ParamField>

#### Devuelve

`UnfollowUserResponse` - datos de la respuesta

<div id="unlike_post">
  ### `unlike_post`
</div>

Quitar "Me gusta" de una Publicación
Hace que el usuario autenticado quite el "Me gusta" de una publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado que solicita quitar el me gusta de la Publicación.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
El id de la Publicación de la que el Usuario solicita quitar el me gusta.
</ParamField>

#### Devuelve

`UnlikePostResponse` - datos de la respuesta

<div id="unmute_user">
  ### `unmute_user`
</div>

Dejar de silenciar a un usuario
Hace que el usuario autenticado deje de silenciar a un usuario específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.source_user_id" type="Any">
El id del usuario de origen autenticado que solicita dejar de silenciar al usuario de destino.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
El id del usuario al que el usuario de origen solicita dejar de silenciar.
</ParamField>

#### Devuelve

`UnmuteUserResponse` - datos de la respuesta

<div id="unpin_list">
  ### `unpin_list`
</div>

Desfijar Lista
Hace que el usuario autenticado deje de fijar una Lista específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado para el cual se devolverán los resultados.
</ParamField>

<ParamField path="path.list_id" type="Any">
El id de la Lista que se va a dejar de fijar.
</ParamField>

#### Devuelve

`UnpinListResponse` - Datos de la respuesta

<div id="unrepost_post">
  ### `unrepost_post`
</div>

Quitar republicación de una Publicación
Hace que el usuario autenticado deje de republicar una Publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado que solicita republicar la Publicación.
</ParamField>

<ParamField path="path.source_tweet_id" type="Any">
El id de la Publicación cuya republicación el Usuario solicita anular.
</ParamField>

#### Devuelve

`UnrepostPostResponse` - datos de la respuesta