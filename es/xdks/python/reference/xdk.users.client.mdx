---
title: "UsersClient"
sidebarTitle: "UsersClient"
---

<div id="usersclient">
  ## UsersClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: object</Badge>

Client para operaciones con usuarios

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="block_dms">
  ### `block_dms`
</div>

Bloquear MD

Bloquea los mensajes directos hacia o desde un usuario específico, identificado por su id, para el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El identificador del Usuario de destino para el cual el usuario autenticado solicita bloquear los mensajes directos.
</ParamField>

#### Devuelve

`BlockDmsResponse` - Datos de la respuesta

<div id="create_bookmark">
  ### `create_bookmark`
</div>

Crear marcador
Agrega una Publicación a los marcadores del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado al que se le añadirán marcadores.
</ParamField>

<ParamField path="body.body" type="CreateBookmarkRequest">
Cuerpo de la solicitud
</ParamField>

<div id="delete_bookmark">
  ### `delete_bookmark`
</div>

Eliminar marcador
Elimina una Publicación de los marcadores del usuario autenticado por su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El identificador del Usuario de origen autenticado cuyo marcador se va a eliminar.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
El identificador de la Publicación que el Usuario de origen elimina de sus marcadores.
</ParamField>

#### Valores devueltos

`DeleteBookmarkResponse` - Datos de la respuesta

<div id="follow_list">
  ### `follow_list`
</div>

Seguir una lista
Hace que el usuario autenticado siga una Lista específica identificada por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado que seguirá la Lista.
</ParamField>

<ParamField path="body.body" type="FollowListRequest">
Cuerpo de la solicitud
</ParamField>

<div id="follow_user">
  ### `follow_user`
</div>

Seguir a un usuario
Hace que el usuario autenticado siga a un usuario específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El identificador del usuario de origen autenticado que solicita seguir al usuario de destino.
</ParamField>

<ParamField path="body.body" type="FollowUserRequest">
Cuerpo de la solicitud
</ParamField>

<div id="get_blocking">
  ### `get_blocking`
</div>

Obtener usuarios bloqueados
Recupera una lista de usuarios bloqueados por el id de usuario especificado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario de origen autenticado para el cual se devolverán los resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una "página" específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetBlockingResponse`

<div id="get_bookmark_folders">
  ### `get_bookmark_folders`
</div>

Obtener carpetas de marcadores
Obtiene una lista de carpetas de marcadores creadas por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario de origen autenticado para el que se devolverán los resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente página de resultados.
</ParamField>

#### Devuelve

`IteratorGetBookmarkFoldersResponse`

<div id="get_bookmarks">
  ### `get_bookmarks`
</div>

Obtener marcadores
Obtiene una lista de Publicaciones agregadas a marcadores por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado para el cual se devolverán los resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente "página" de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de medios que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuestas que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetBookmarksResponse`

<div id="get_bookmarks_by_folder_id">
  ### `get_bookmarks_by_folder_id`
</div>

Obtener marcadores por ID de carpeta
Recupera Publicaciones en una carpeta de marcadores específica por su ID del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El identificador del Usuario de origen autenticado para el cual se devolverán los resultados.
</ParamField>

<ParamField path="path.folder_id" type="Any">
El identificador de la Carpeta de Marcadores para la que el Usuario autenticado está intentando obtener Publicaciones.
</ParamField>

#### Devuelve

`GetBookmarksByFolderIdResponse` - Datos de respuesta

<div id="get_by_id">
  ### `get_by_id`
</div>

Obtener usuario por id
Recupera los detalles de un usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario que se va a consultar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de Usuario que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet que se van a mostrar.
</ParamField>

#### Devuelve

`GetByIdResponse` - Datos de la respuesta

<div id="get_by_ids">
  ### `get_by_ids`
</div>

Obtener usuarios por id
Recupera detalles de varios usuarios por sus id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.ids" type="List">
Una lista de identificadores de usuario, separados por comas. Puedes especificar hasta 100 identificadores.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se ampliarán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet que se mostrarán.
</ParamField>

#### Devuelve

`GetByIdsResponse` - Datos de respuesta

<div id="get_by_username">
  ### `get_by_username`
</div>

Obtener User por nombre de usuario
Obtiene los detalles de un User específico a partir de su nombre de usuario.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.username" type="str">
Un nombre de usuario.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Lista separada por comas de campos de Tweet para mostrar.
</ParamField>

#### Devuelve

`GetByUsernameResponse` - Datos de la respuesta

<div id="get_by_usernames">
  ### `get_by_usernames`
</div>

Obtener usuarios por nombre de usuario
Recupera los detalles de varios usuarios a partir de sus nombres de usuario.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.usernames" type="List">
Una lista de nombres de usuario separados por comas.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet para mostrar.
</ParamField>

#### Devuelve

`GetByUsernamesResponse` - Datos de respuesta

<div id="get_followed_lists">
  ### `get_followed_lists`
</div>

Obtener listas seguidas
Recupera una lista de Listas seguidas por un usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario que se va a buscar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una «página» específica de resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista de campos de Lista separados por comas para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos separados por comas para expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de Usuario separados por comas para mostrar.
</ParamField>

#### Devuelve

`IteratorGetFollowedListsResponse`

<div id="get_followers">
  ### `get_followers`
</div>

Obtener seguidores
Obtiene una lista de usuarios que siguen a un usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una «página» específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se van a expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Tweet, separados por comas, que se van a mostrar.
</ParamField>

#### Devuelve

`IteratorGetFollowersResponse`

<div id="get_following">
  ### `get_following`
</div>

Obtener las cuentas que sigue
Recupera una lista de Usuarios que un Usuario específico sigue mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario que se va a buscar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una «página» específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de Usuario, separados por comas, que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se van a expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Tweet, separados por comas, que se van a mostrar.
</ParamField>

#### Devuelve

`IteratorGetFollowingResponse`

<div id="get_liked_posts">
  ### `get_liked_posts`
</div>

Obtener Publicaciones con Me gusta  
Obtiene una lista de Publicaciones a las que un Usuario específico ha dado Me gusta por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente «página» de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de Media que se van a mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de Poll que se van a mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de User que se van a mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de Place que se van a mostrar.
</ParamField>

#### Devuelve

`IteratorGetLikedPostsResponse`

<div id="get_list_memberships">
  ### `get_list_memberships`
</div>

Obtener membresías de listas
Recupera una lista de listas a las que pertenece un usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una «página» específica de resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista separada por comas de campos de Lista que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de Usuario que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetListMembershipsResponse`

<div id="get_me">
  ### `get_me`
</div>

Obtener mi usuario
Obtiene los detalles del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para ampliar.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet para mostrar.
</ParamField>

#### Devuelve

`GetMeResponse` - Datos de respuesta

<div id="get_mentions">
  ### `get_mentions`
</div>

Obtener menciones
Devuelve una lista de Publicaciones que mencionan a un Usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a consultar.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
El ID mínimo de la Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre start_time si ambos se especifican.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
El ID máximo de la Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre end_time si ambos se especifican.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente 'página' de resultados.
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más temprana a partir de la cual se proporcionarán las Publicaciones. El parámetro since_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se proporcionarán las Publicaciones. El parámetro until_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de medios que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuestas que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar que se mostrarán.
</ParamField>

#### Valor devuelto

`IteratorGetMentionsResponse`

<div id="get_muting">
  ### `get_muting`
</div>

Obtener usuarios silenciados
Devuelve una lista de usuarios silenciados por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario de origen autenticado para el que se devolverán los resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente «página» de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de Usuario, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se deben expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Tweet, separados por comas, que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetMutingResponse`

<div id="get_owned_lists">
  ### `get_owned_lists`
</div>

Obtener Listas propias
Devuelve una lista de Listas propiedad de un usuario específico, identificado por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario que se va a consultar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una página específica de resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista separada por comas de campos de Lista que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se van a mostrar.
</ParamField>

#### Devuelve

`IteratorGetOwnedListsResponse`

<div id="get_pinned_lists">
  ### `get_pinned_lists`
</div>

Obtiene las Listas fijadas
Recupera las Listas fijadas por el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El identificador del Usuario de origen autenticado para el cual se devolverán los resultados.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista separada por comas de campos de Lista que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de Usuario que se mostrarán.
</ParamField>

#### Devuelve

`GetPinnedListsResponse` - Datos de la respuesta

<div id="get_posts">
  ### `get_posts`
</div>

Obtener publicaciones
Obtiene una lista de publicaciones creadas por un usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario que se va a buscar.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
El ID mínimo de la Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre start_time si ambos se especifican.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
El ID máximo de la Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre end_time si ambos se especifican.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente "página" de resultados.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
El conjunto de entidades que se van a excluir (por ejemplo, "respuestas" o "retweets").
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más antigua a partir de la cual se devolverán las Publicaciones. El parámetro since_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo UTC más reciente hasta la cual se devolverán las Publicaciones. El parámetro until_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de medios que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuestas que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de Usuario que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugares que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetPostsResponse`

<div id="get_reposts_of_me">
  ### `get_reposts_of_me`
</div>

Obtener Reposts de mi cuenta  
Obtiene una lista de Publicaciones que son Reposts del contenido del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.max_results" type="int or None" default="None">
Número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente "página" de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Tweet separados por comas para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos separados por comas que se deben expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista de campos de medios separados por comas para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista de campos de encuestas separados por comas para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario separados por comas para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista de campos de lugar separados por comas para mostrar.
</ParamField>

#### Devuelve

`IteratorGetRepostsOfMeResponse`

<div id="get_timeline">
  ### `get_timeline`
</div>

Obtener la cronología
Recupera una lista de publicaciones en orden cronológico inverso en la cronología del usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario de origen autenticado para listar sus Publicaciones de la línea de tiempo en orden cronológico inverso.
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
El ID mínimo de Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre start_time si se especifican ambos.
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
El ID máximo de Publicación que se incluirá en el conjunto de resultados. Este parámetro tiene prioridad sobre end_time si se especifican ambos.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente "página" de resultados.
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
El conjunto de entidades que se deben excluir (por ejemplo, "replies" o "retweets").
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La primera marca de tiempo UTC a partir de la cual se proporcionarán las Publicaciones. El parámetro since_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ. La última marca de tiempo UTC hasta la cual se proporcionarán las Publicaciones. El parámetro until_id tiene prioridad si también se especifica.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de medios que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuestas que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar que se mostrarán.
</ParamField>

#### Devuelve

`IteratorGetTimelineResponse`

<div id="like_post">
  ### `like_post`
</div>

Indicar Me gusta en una Publicación
Hace que el usuario autenticado indique Me gusta en una Publicación específica por su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Usuario de origen autenticado que solicita indicar que le gusta la Publicación.
</ParamField>

<ParamField path="body.body" type="LikePostRequest">
Cuerpo de la solicitud
</ParamField>

<div id="mute_user">
  ### `mute_user`
</div>

Silenciar usuario
Hace que el usuario autenticado silencie a un usuario específico mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del usuario de origen autenticado que solicita silenciar al usuario de destino.
</ParamField>

<ParamField path="body.body" type="MuteUserRequest">
Cuerpo de la solicitud
</ParamField>

<div id="pin_list">
  ### `pin_list`
</div>

Anclar una Lista
Hace que el usuario autenticado ancle una Lista específica mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del usuario de origen autenticado que fijará la Lista.
</ParamField>

<ParamField path="body.body" type="PinListRequest">
Cuerpo de la solicitud
</ParamField>

<div id="repost_post">
  ### `repost_post`
</div>

Republicar Publicación
Hace que el usuario autenticado vuelva a publicar una Publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El identificador del Usuario de origen autenticado que solicita volver a publicar la Publicación.
</ParamField>

<ParamField path="body.body" type="RepostPostRequest">
Cuerpo de la solicitud
</ParamField>

<div id="search">
  ### `search`
</div>

Buscar usuarios
Obtiene una lista de usuarios que coinciden con una consulta de búsqueda.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="Any">
La cadena de consulta para buscar usuarios.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente «página» de resultados. El valor que se use con el parámetro se toma directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de Usuario que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se ampliarán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet que se mostrarán.
</ParamField>

#### Valor devuelto

`IteratorSearchResponse`

<div id="unblock_dms">
  ### `unblock_dms`
</div>

Desbloquear MDs
Desbloquea los mensajes directos de o hacia un usuario específico mediante su id para el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El identificador del usuario de destino para el cual el usuario autenticado solicita desbloquear los mensajes directos (DMs).
</ParamField>

#### Valores devueltos

`UnblockDmsResponse` - Datos de la respuesta

<div id="unfollow_list">
  ### `unfollow_list`
</div>

Dejar de seguir una Lista
Hace que el usuario autenticado deje de seguir una Lista específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El identificador del usuario de origen autenticado que dejará de seguir la lista.
</ParamField>

<ParamField path="path.list_id" type="Any">
El identificador de la lista que se dejará de seguir.
</ParamField>

#### Devuelve

`UnfollowListResponse` - Datos de respuesta

<div id="unfollow_user">
  ### `unfollow_user`
</div>

Dejar de seguir a un usuario
Hace que el usuario autenticado deje de seguir a un usuario específico según su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.source_user_id" type="Any">
El id del Usuario de origen autenticado que solicita dejar de seguir al Usuario de destino.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
El id del Usuario al que el Usuario de origen solicita dejar de seguir.
</ParamField>

#### Valores devueltos

`UnfollowUserResponse` - Datos de la respuesta

<div id="unlike_post">
  ### `unlike_post`
</div>

Quitar el Me gusta de una Publicación
Permite que el usuario autenticado quite el Me gusta de una Publicación específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Usuario autenticado de origen que solicita quitar el Me gusta de la Publicación.
</ParamField>

<ParamField path="path.tweet_id" type="Any">
El ID de la Publicación de la que el Usuario solicita quitar el Me gusta.
</ParamField>

#### Devuelve

`UnlikePostResponse` - Datos de respuesta

<div id="unmute_user">
  ### `unmute_user`
</div>

Quitar silencio a un usuario
Hace que el usuario autenticado deje de silenciar a un usuario específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.source_user_id" type="Any">
El id del Usuario de origen autenticado que solicita dejar de silenciar al Usuario de destino.
</ParamField>

<ParamField path="path.target_user_id" type="Any">
El id del Usuario de destino al que el Usuario de origen solicita dejar de silenciar.
</ParamField>

<div id="returns">
  #### Valores de retorno
</div>

`UnmuteUserResponse` - Datos de la respuesta

<div id="unpin_list">
  ### `unpin_list`
</div>

Desanclar Lista
Hace que el usuario autenticado desancle una Lista específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El identificador del Usuario de origen autenticado para el cual se devolverán los resultados.
</ParamField>

<ParamField path="path.list_id" type="Any">
El identificador de la Lista que se debe desanclar.
</ParamField>

#### Devuelve

`UnpinListResponse` - Datos de la respuesta

<div id="unrepost_post">
  ### `unrepost_post`
</div>

Anular republicación de Publicación
Hace que el usuario autenticado anule la republicación de una Publicación específica por su ID.

#### Parameters

<ParamField path="path.id" type="Any">
El identificador del Usuario de origen autenticado que solicita volver a publicar la Publicación.
</ParamField>

<ParamField path="path.source_tweet_id" type="Any">
El identificador de la Publicación de la que el Usuario solicita deshacer el Retweet.
</ParamField>

#### Devuelve

`UnrepostPostResponse` - Datos de respuesta