---
title: "TrendsClient"
sidebarTitle: "TrendsClient"
---

<div id="trendsclient">
  ## TrendsClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Base: object</Badge>

Cliente para operaciones de tendencias

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_ai">
  ### `get_ai`
</div>

Obtiene tendencias de IA por id
Recupera una tendencia de IA por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El identificador de la tendencia de IA.
</ParamField>

<ParamField path="path.news_fields" type="List or None" default="None">
Una lista de campos de News separados por comas para mostrar.
</ParamField>

#### Devuelve

`GetAiResponse` - Datos de respuesta

<div id="get_by_woeid">
  ### `get_by_woeid`
</div>

Obtener tendencias por WOEID
Obtiene los temas que son tendencia para una ubicación específica identificada por su WOEID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.woeid" type="int">
El WOEID del lugar para el que se quiere obtener una tendencia.
</ParamField>

<ParamField path="path.max_trends" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.trend_fields" type="List or None" default="None">
Una lista separada por comas de campos de tendencias que se mostrarán.
</ParamField>

#### Devuelve

`GetByWoeidResponse` - Datos de respuesta

<div id="get_personalized">
  ### `get_personalized`
</div>

Obtener tendencias personalizadas
Obtiene temas de tendencia personalizados para el usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.personalized_trend_fields" type="List or None" default="None">
Una lista de campos PersonalizedTrend separados por comas que se van a mostrar.
</ParamField>

<div id="returns">
  #### Valores devueltos
</div>

`GetPersonalizedResponse` - Datos de la respuesta