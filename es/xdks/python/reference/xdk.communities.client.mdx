---
title: "CommunitiesClient"
sidebarTitle: "CommunitiesClient"
---

<div id="communitiesclient">
  ## CommunitiesClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Clases base: object</Badge>

Cliente para operaciones con comunidades

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_by_id">
  ### `get_by_id`
</div>

Obtener comunidad por id
Obtiene los detalles de una comunidad específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El identificador de la Comunidad.
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
Una lista de campos de Comunidad, separados por comas, que se mostrarán.
</ParamField>

<div id="returns">
  #### Valores devueltos
</div>

`GetByIdResponse` - Datos de respuesta

<div id="search">
  ### `search`
</div>

Buscar Communities
Obtiene una lista de Communities que coinciden con la consulta de búsqueda especificada.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="str">
Consulta para buscar comunidades.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados de búsqueda que se devolverán en una solicitud.
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente «página» de resultados. El valor usado con el parámetro se extrae directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener la siguiente «página» de resultados. El valor usado con el parámetro se extrae directamente de la respuesta proporcionada por la API y no debe modificarse.
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
Una lista de campos de comunidad separados por comas que se mostrarán.
</ParamField>

#### Devuelve

`IteratorSearchResponse`