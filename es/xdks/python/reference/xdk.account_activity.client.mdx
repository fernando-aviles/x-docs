---
title: "AccountActivityClient"
sidebarTitle: "AccountActivityClient"
---

<div id="accountactivityclient">
  ## AccountActivityClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Base: object</Badge>

Cliente para operaciones de actividad de la cuenta

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_replay_job">
  ### `create_replay_job`
</div>

Crear trabajo de reproducción
Crea un trabajo de reproducción para recuperar actividades de hasta los últimos 5 días de todas las suscripciones asociadas con un webhook determinado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El identificador único de la configuración del webhook.
</ParamField>

<ParamField path="path.from_date" type="str">
La marca de tiempo UTC más antigua (inicial, inclusiva) a partir de la cual se proporcionarán los eventos, en el formato yyyymmddhhmm.
</ParamField>

<ParamField path="path.to_date" type="str">
La marca de tiempo UTC más reciente (final, exclusiva) hasta la cual se proporcionarán los eventos, en el formato yyyymmddhhmm.
</ParamField>

<div id="returns">
  #### Valores de retorno
</div>

`CreateReplayJobResponse` - Datos de la respuesta

<div id="create_subscription">
  ### `create_subscription`
</div>

Crear suscripción
Crea una suscripción de Account Activity para el usuario y el webhook especificados.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El id del webhook para comprobar la suscripción asociada.
</ParamField>

<ParamField path="body.body" type="CreateSubscriptionRequest">
Cuerpo de la solicitud
</ParamField>

<div id="delete_subscription">
  ### `delete_subscription`
</div>

Eliminar suscripción
Elimina una suscripción de Account Activity para el webhook y el id de usuario indicados.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El ID de webhook frente al cual verificar la suscripción.
</ParamField>

<ParamField path="path.user_id" type="Any">
ID de usuario del que darse de baja.
</ParamField>

#### Valor de retorno

`DeleteSubscriptionResponse` - Datos de la respuesta

<div id="get_subscription_count">
  ### `get_subscription_count`
</div>

Obtener el número de suscripciones
Obtiene el número de suscripciones de Account Activity que están activas actualmente.
:returns: Datos de la respuesta
:rtype: GetSubscriptionCountResponse

#### Devuelve

`GetSubscriptionCountResponse`

<div id="get_subscriptions">
  ### `get_subscriptions`
</div>

Obtener suscripciones
Devuelve una lista de todas las suscripciones activas para un webhook determinado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El id del webhook para obtener las suscripciones.
</ParamField>

#### Valor de retorno

`GetSubscriptionsResponse` - Datos de la respuesta

<div id="validate_subscription">
  ### `validate_subscription`
</div>

Validar suscripción
Comprueba la suscripción de Account Activity de un usuario para un webhook determinado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El identificador del webhook para comprobar la suscripción.
</ParamField>

#### Devuelve

`ValidateSubscriptionResponse` - Datos de respuesta