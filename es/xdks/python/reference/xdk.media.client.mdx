---
title: "MediaClient"
sidebarTitle: "MediaClient"
---

<div id="mediaclient">
  ## MediaClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: objeto</Badge>

Cliente para operaciones con medios

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="append_upload">
  ### `append_upload`
</div>

Agregar a una carga de medios
Agrega datos a una solicitud de carga de medios.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El identificador del contenido multimedia para el que se realizará la operación de anexión.
</ParamField>

<ParamField path="body.body" type="AppendUploadRequest">
Cuerpo de la solicitud
</ParamField>

<div id="create_metadata">
  ### `create_metadata`
</div>

Crear metadatos de Media
Crea metadatos para un archivo de Media.
body: Cuerpo de la solicitud
:returns: Datos de la respuesta
:rtype: CreateMetadataResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateMetadataRequest"></ParamField>

<div id="create_subtitles">
  ### `create_subtitles`
</div>

Crear subtítulos de Media
Crea subtítulos para un archivo de Media específico.
body: Cuerpo de la solicitud
:returns: data de la respuesta
:rtype: CreateSubtitlesResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateSubtitlesRequest"></ParamField>

<div id="delete_subtitles">
  ### `delete_subtitles`
</div>

Eliminar subtítulos de Media
Elimina los subtítulos de un archivo Media específico.
body: Cuerpo de la solicitud
:returns: data de la Respuesta
:rtype: DeleteSubtitlesResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="DeleteSubtitlesRequest"></ParamField>

<div id="finalize_upload">
  ### `finalize_upload`
</div>

Finalizar subida de medios
Finaliza una solicitud de subida de medios.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del contenido multimedia de destino que se va a finalizar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`FinalizeUploadResponse` - datos de la respuesta

<div id="get_analytics">
  ### `get_analytics`
</div>

Obtener analíticas de Media
Recupera datos de analíticas para Media.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.media_keys" type="List">
Una lista de Media Keys, separadas por comas. Se permiten hasta 100 por solicitud.
</ParamField>

<ParamField path="path.end_time" type="str">
AAAA-MM-DDTHH:mm:ssZ. La marca de tiempo UTC que representa el final del intervalo de tiempo.
</ParamField>

<ParamField path="path.start_time" type="str">
AAAA-MM-DDTHH:mm:ssZ. La marca de tiempo UTC que representa el inicio del intervalo de tiempo.
</ParamField>

<ParamField path="path.granularity" type="str">
La granularidad de los resultados de conteo de búsqueda.
</ParamField>

<ParamField path="path.media_analytics_fields" type="List or None" default="None">
Una lista separada por comas de campos de MediaAnalytics para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetAnalyticsResponse` - datos de la Respuesta

<div id="get_by_key">
  ### `get_by_key`
</div>

Obtener contenido multimedia por clave de medio
Recupera los detalles de un archivo multimedia específico a partir de su clave de medio.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.media_key" type="Any">
Una sola Media Key.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de Media para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByKeyResponse` - datos de la respuesta

<div id="get_by_keys">
  ### `get_by_keys`
</div>

Obtener contenido multimedia por claves de medios
Recupera los detalles de archivos multimedia a partir de sus claves de medios.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.media_keys" type="List">
Una lista separada por comas de claves de medios. Se permiten hasta 100 por solicitud.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de Media para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByKeysResponse` - Datos de la respuesta

<div id="get_upload_status">
  ### `get_upload_status`
</div>

Obtener el estado de carga de Media  
Obtiene el estado de una carga de Media por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.media_id" type="Any">
ID de medio para el estado de la carga de medio solicitada.
</ParamField>

<ParamField path="path.command" type="str or None" default="None">
El comando para la solicitud de carga de medio.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetUploadStatusResponse` - datos de la respuesta

<div id="initialize_upload">
  ### `initialize_upload`
</div>

Iniciar la carga de medios
Inicializa la carga de medios.
body: Cuerpo de la solicitud
:returns: datos de la respuesta
:rtype: InitializeUploadResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="InitializeUploadRequest"></ParamField>

<div id="upload">
  ### `upload`
</div>

Cargar contenido multimedia
Carga un archivo multimedia para usarlo en publicaciones u otro tipo de contenido.
body: Cuerpo de la solicitud
:returns: data de la respuesta
:rtype: UploadResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="UploadRequest"></ParamField>