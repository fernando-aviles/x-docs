---
title: "MediaClient"
sidebarTitle: "MediaClient"
---

<div id="mediaclient">
  ## MediaClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Base: object</Badge>

Cliente para operaciones con contenido multimedia

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="append_upload">
  ### `append_upload`
</div>

Anexar a la carga de medios
Anexa datos a una solicitud de carga de medios.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
Identificador del contenido multimedia sobre el que se va a realizar la operación de append.
</ParamField>

<ParamField path="body.body" type="AppendUploadRequest">
Cuerpo de la solicitud
</ParamField>

<div id="create_metadata">
  ### `create_metadata`
</div>

Crear metadatos de Media
Crea metadatos para un archivo de Media.
body: Cuerpo de la solicitud
:returns: data de la respuesta
:rtype: CreateMetadataResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateMetadataRequest"></ParamField>

<div id="create_subtitles">
  ### `create_subtitles`
</div>

Crear subtítulos de Media
Crea subtítulos para un archivo Media específico.
body: Cuerpo de la solicitud
:returns: Datos de la respuesta
:rtype: CreateSubtitlesResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateSubtitlesRequest"></ParamField>

<div id="delete_subtitles">
  ### `delete_subtitles`
</div>

Eliminar subtítulos de Media
Elimina los subtítulos de un archivo específico de Media.
body: Cuerpo de la solicitud
:returns: data de la respuesta
:rtype: DeleteSubtitlesResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="DeleteSubtitlesRequest"></ParamField>

<div id="finalize_upload">
  ### `finalize_upload`
</div>

Finalizar la carga de medios
Completa una solicitud de carga de medios.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del contenido multimedia de destino que se va a finalizar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`FinalizeUploadResponse` - data de la respuesta

<div id="get_analytics">
  ### `get_analytics`
</div>

Obtener estadísticas de Media
Recupera datos analíticos de Media.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.media_keys" type="List">
Una lista separada por comas de Media Keys. Se permiten hasta 100 en una sola solicitud.
</ParamField>

<ParamField path="path.end_time" type="str">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo en UTC que representa el final del intervalo de tiempo.
</ParamField>

<ParamField path="path.start_time" type="str">
YYYY-MM-DDTHH:mm:ssZ. La marca de tiempo en UTC que representa el inicio del intervalo de tiempo.
</ParamField>

<ParamField path="path.granularity" type="str">
La granularidad para los resultados de conteos de búsqueda.
</ParamField>

<ParamField path="path.media_analytics_fields" type="List or None" default="None">
Una lista separada por comas de campos de MediaAnalytics que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetAnalyticsResponse` - datos de la respuesta

<div id="get_by_key">
  ### `get_by_key`
</div>

Obtener Media por media key
Recupera los detalles de un archivo de Media específico por su media key.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.media_key" type="Any">
Una sola Media Key.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de Media que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByKeyResponse` - Datos de la respuesta

<div id="get_by_keys">
  ### `get_by_keys`
</div>

Obtener objetos Media por media keys  
Recupera los detalles de los objetos Media mediante sus media keys.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.media_keys" type="List">
Una lista separada por comas de Media Keys. Se permiten hasta 100 en una sola solicitud.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de Media que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByKeysResponse` - datos de la respuesta

<div id="get_upload_status">
  ### `get_upload_status`
</div>

Obtiene el estado de la subida de Media
Obtiene el estado de una subida de Media por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.media_id" type="Any">
id de contenido multimedia para el estado de la carga de contenido multimedia solicitada.
</ParamField>

<ParamField path="path.command" type="str or None" default="None">
El comando de la solicitud de carga de contenido multimedia.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetUploadStatusResponse` - datos de la respuesta

<div id="initialize_upload">
  ### `initialize_upload`
</div>

Inicializar la carga de medios
Inicializa una carga de medios.
body: Cuerpo de la solicitud
:returns: data de la respuesta
:rtype: InitializeUploadResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="InitializeUploadRequest"></ParamField>

<div id="upload">
  ### `upload`
</div>

Cargar contenido multimedia
Carga un archivo multimedia para usarlo en publicaciones u otro contenido.
body: Cuerpo de la solicitud
:returns: data de la respuesta
:rtype: UploadResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="UploadRequest"></ParamField>