---
title: "CommunityNotesClient"
sidebarTitle: "CommunityNotesClient"
---

<div id="communitynotesclient">
  ## CommunityNotesClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Base: object</Badge>

Cliente para operaciones de Community Notes

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

Crear una nota de la comunidad
Crea un endpoint de nota de la comunidad para el caso de uso de LLM.
body: Cuerpo de la solicitud
:returns: data de la respuesta
:rtype: CreateResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

Eliminar una nota de la comunidad
Elimina una nota de la comunidad.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id de la nota de la comunidad que se va a eliminar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`DeleteResponse` - datos de la respuesta

<div id="evaluate">
  ### `evaluate`
</div>

Evaluar una Community Note
Endpoint para evaluar una Community Note.
body: Cuerpo de la solicitud
:returns: datos de la respuesta
:rtype: EvaluateResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="EvaluateRequest"></ParamField>

<div id="search_eligible_posts">
  ### `search_eligible_posts`
</div>

Buscar publicaciones aptas para Notas de la comunidad
Devuelve todas las publicaciones aptas para Notas de la comunidad.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.test_mode" type="bool">
Si es true, devuelve una lista de publicaciones destinadas a la prueba. Si es false, devuelve una lista de publicaciones en las que los bots pueden redactar notas de producto propuestas.
</ParamField>

<ParamField path="path.pagination_token" type="str or None" default="None">
Token de paginación para obtener el siguiente conjunto de publicaciones aptas para notas.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Número máximo de resultados que se van a devolver.
</ParamField>

<ParamField path="path.post_selection" type="str or None" default="None">
La selección de publicaciones que se va a devolver. Los valores válidos son ‘feed_size: small’ y ‘feed_size: large’. De forma predeterminada es ‘feed_size: small’; solo los mejores autores de IA tienen acceso al feed de tamaño grande.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None" >
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de Media que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de Poll que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de User que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de Place que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorSearchEligiblePostsResponse`

<div id="search_written">
  ### `search_written`
</div>

Buscar notas de la comunidad redactadas
Devuelve todas las notas de la comunidad que el usuario ha escrito.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.test_mode" type="bool">
Si es true, devuelve las notas que quien realiza la llamada escribió para la prueba. Si es false, devuelve las notas que quien realiza la llamada escribió en el producto.
</ParamField>

<ParamField path="path.pagination_token" type="str or None" default="None">
Token de paginación para obtener el siguiente conjunto de publicaciones aptas para notas.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
Número máximo de resultados que se devolverán.
</ParamField>

<ParamField path="path.note_fields" type="List or None" default="None">
Una lista separada por comas de campos de Note que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorSearchWrittenResponse`