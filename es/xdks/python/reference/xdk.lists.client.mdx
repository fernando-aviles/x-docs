---
title: "ListsClient"
sidebarTitle: "ListsClient"
---

<div id="listsclient">
  ## ListsClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Clase base: object</Badge>

Cliente para operaciones de listas

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="add_member">
  ### `add_member`
</div>

Agregar miembro a la lista
Agrega un usuario a una lista específica por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID de la Lista a la que se va a añadir un miembro.
</ParamField>

<ParamField path="body.body" type="AddMemberRequest">
Cuerpo de la solicitud
</ParamField>

<div id="create">
  ### `create`
</div>

Crear Lista
Crea una nueva Lista para el usuario autenticado.
body: Cuerpo de la solicitud
:returns: datos de la respuesta
:rtype: CreateResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

Eliminar lista
Elimina una lista específica perteneciente al usuario autenticado mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
La id de la lista que se va a eliminar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`DeleteResponse` - datos de la respuesta

<div id="get_by_id">
  ### `get_by_id`
</div>

Obtener lista por id
Recupera los detalles de una lista específica mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID de la List.
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
Una lista de campos de List, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, para expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de User, separados por comas, para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByIdResponse` - datos de la respuesta

<div id="get_followers">
  ### `get_followers`
</div>

Obtiene los seguidores de la Lista
Recupera una lista de usuarios que siguen una Lista específica a partir de su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id de la lista.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una «página» específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetFollowersResponse`

<div id="get_members">
  ### `get_members`
</div>

Obtener miembros de la Lista
Obtiene una lista de usuarios que son miembros de una Lista específica a partir de su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID de la lista.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una «página» específica de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de usuario, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, para expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Tuit, separados por comas, para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetMembersResponse`

<div id="get_posts">
  ### `get_posts`
</div>

Obtener Publicaciones de una Lista
Recupera una lista de Publicaciones asociadas a una Lista específica por su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id de la lista.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener la siguiente “página” de resultados.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de medios para mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de encuesta para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario para mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de lugar para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetPostsResponse`

<div id="remove_member_by_user_id">
  ### `remove_member_by_user_id`
</div>

Eliminar miembro de la lista
Elimina a un usuario de una lista específica mediante el id de la lista y el id del usuario.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id de la lista de la que se eliminará un miembro.
</ParamField>

<ParamField path="path.user_id" type="Any">
El id del usuario que se eliminará de la lista.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`RemoveMemberByUserIdResponse` - Datos de la respuesta

<div id="update">
  ### `update`
</div>

Actualizar lista
Actualiza los detalles de una lista específica perteneciente al usuario autenticado mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id de la lista que se va a modificar.
</ParamField>

<ParamField path="body.body" type="UpdateRequest">
Cuerpo de la solicitud
</ParamField>