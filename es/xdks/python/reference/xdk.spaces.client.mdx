---
title: "SpacesClient"
sidebarTitle: "SpacesClient"
---

<div id="spacesclient">
  ## SpacesClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: object</Badge>

Cliente para operaciones con Spaces

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_buyers">
  ### `get_buyers`
</div>

Obtiene los compradores de entradas de un Space
Devuelve una lista de usuarios que compraron entradas para un Space específico a partir de su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="str">
El id del Space que se va a recuperar.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una "página" específica de resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit que se van a mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetBuyersResponse`

<div id="get_by_creator_ids">
  ### `get_by_creator_ids`
</div>

Obtener Spaces por id de creadores
Obtiene los detalles de los Spaces creados por las id de usuario especificadas.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.user_ids" type="List">
Los id de usuario en los que realizar la búsqueda.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Una lista de campos de Space, separada por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separada por comas, que se expandirán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de User, separada por comas, que se mostrarán.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Una lista de campos de Topic, separada por comas, que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByCreatorIdsResponse` - datos de la respuesta

<div id="get_by_id">
  ### `get_by_id`
</div>

Obtener un Space por identificador
Recupera los detalles de un Space específico a partir de su identificador.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="str">
El ID del Space que se va a recuperar.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Una lista de campos de Space, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se expandirán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de User, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Una lista de campos de Topic, separados por comas, que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByIdResponse` - datos de la respuesta

<div id="get_by_ids">
  ### `get_by_ids`
</div>

Obtener Spaces por id
Recupera los detalles de varios Spaces por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.ids" type="List">
La lista de IDs de Spaces que se devolverán.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Una lista separada por comas de campos de Spaces que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de usuario que se mostrarán.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Una lista separada por comas de campos de tema que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByIdsResponse` - Datos de la respuesta

<div id="get_posts">
  ### `get_posts`
</div>

Obtener publicaciones de un espacio
Obtiene una lista de publicaciones compartidas en un espacio específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="str">
El id del Space que se va a recuperar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número de Publicaciones que se obtendrán del Space proporcionado. Si no se especifica, el valor predeterminado será el máximo de 100.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tuit que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de Media que se van a mostrar.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
Una lista separada por comas de campos de Poll que se van a mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de User que se van a mostrar.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
Una lista separada por comas de campos de Place que se van a mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetPostsResponse` - Datos de la respuesta

<div id="search">
  ### `search`
</div>

Buscar Spaces
Obtiene una lista de Spaces que coinciden con la consulta de búsqueda especificada.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="str">
La consulta de búsqueda.
</ParamField>

<ParamField path="path.state" type="str or None" default="None">
El estado de los Spaces que se van a buscar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número de resultados que se devolverán.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
Una lista de campos de Space separados por comas para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos separados por comas que se van a expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de User separados por comas para mostrar.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
Una lista de campos de Topic separados por comas para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`SearchResponse` - datos de la respuesta