---
title: "WebhooksClient"
sidebarTitle: "WebhooksClient"
---

<div id="webhooksclient">
  ## WebhooksClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: object</Badge>

Cliente para la gestión de webhooks

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

Crear un webhook
Crea una nueva configuración de webhook.
body: Cuerpo de la solicitud
:returns: Datos de la respuesta
:rtype: CreateResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="create_stream_link">
  ### `create_stream_link`
</div>

Crear enlace de stream
Crea un enlace para enviar eventos de FilteredStream al webhook especificado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El ID del webhook que se vinculará a tu conjunto de reglas de FilteredStream.
</ParamField>

<ParamField path="path.tweet_fields" type="str or None" default="None">
Una lista separada por comas de campos de Tuit que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="str or None" default="None">
Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.media_fields" type="str or None" default="None">
Una lista separada por comas de campos de Media que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="str or None" default="None">
Una lista separada por comas de campos de Poll que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="str or None" default="None">
Una lista separada por comas de campos de User que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="str or None" default="None">
Una lista separada por comas de campos de Place que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`CreateStreamLinkResponse` - datos de la respuesta

<div id="create_webhook_replay_job">
  ### `create_webhook_replay_job`
</div>

Crear trabajo de reproducción para un webhook
Crea un trabajo de reproducción para recuperar eventos de hasta las últimas 24 horas para todos los eventos que se entregaron o se intentó entregar al webhook.
body: Cuerpo de la solicitud
:returns: Datos de la respuesta
:rtype: CreateWebhookReplayJobResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateWebhookReplayJobRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

Eliminar webhook
Elimina una configuración de webhook existente.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El id del webhook que se debe eliminar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`DeleteResponse` - datos de la respuesta

<div id="delete_stream_link">
  ### `delete_stream_link`
</div>

Eliminar enlace de stream
Elimina el enlace entre los eventos de FilteredStream y el webhook proporcionado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El ID del webhook que se vinculará a tu conjunto de reglas de FilteredStream.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`DeleteStreamLinkResponse` - datos de la respuesta

<div id="get">
  ### `get`
</div>

Obtiene el webhook
Obtiene una lista de configuraciones de webhooks asociadas a una aplicación cliente.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_config_fields" type="List or None" default="None">
Lista separada por comas de campos de WebhookConfig que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetResponse` - datos de la respuesta

<div id="get_stream_links">
  ### `get_stream_links`
</div>

Obtener enlaces de flujo
Obtén una lista de enlaces de webhook asociados con un conjunto de reglas de filtrado del flujo.
:returns: data de la respuesta
:rtype: GetStreamLinksResponse

<div id="returns">
  #### Devuelve
</div>

`GetStreamLinksResponse`

<div id="validate">
  ### `validate`
</div>

Validar el webhook
Inicia una comprobación CRC para un webhook específico.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El ID del webhook que se va a comprobar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`ValidateResponse` - datos de la respuesta