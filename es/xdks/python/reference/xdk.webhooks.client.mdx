---
title: "WebhooksClient"
sidebarTitle: "WebhooksClient"
---

<div id="webhooksclient">
  ## WebhooksClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: object</Badge>

Cliente para operaciones con webhooks

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

Crear un webhook
Crea una nueva configuración de webhook.
body: Cuerpo de la solicitud
:returns: datos de la Respuesta
:rtype: CreateResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="create_stream_link">
  ### `create_stream_link`
</div>

Crear enlace de stream
Crea un enlace para enviar eventos de FilteredStream al webhook indicado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El ID del webhook que se vinculará a tu conjunto de reglas de FilteredStream.
</ParamField>

<ParamField path="path.tweet_fields" type="str or None" default="None">
Una lista separada por comas de campos de Tweet que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="str or None" default="None">
Una lista separada por comas de campos que se deben expandir.
</ParamField>

<ParamField path="path.media_fields" type="str or None" default="None">
Una lista separada por comas de campos de Media que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="str or None" default="None">
Una lista separada por comas de campos de Poll que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="str or None" default="None">
Una lista separada por comas de campos de User que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="str or None" default="None">
Una lista separada por comas de campos de Place que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`CreateStreamLinkResponse` - Datos de la respuesta

<div id="create_webhook_replay_job">
  ### `create_webhook_replay_job`
</div>

Crear trabajo de repetición para webhook
Crea un trabajo de repetición para recuperar eventos de hasta las últimas 24 horas para todos los eventos entregados al webhook o para los que se haya intentado su entrega.
body: Cuerpo de la solicitud
:returns: Datos de la Respuesta
:rtype: CreateWebhookReplayJobResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateWebhookReplayJobRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

Eliminar webhook
Elimina una configuración de webhook existente.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El ID del webhook que se va a eliminar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`DeleteResponse` - datos de la Respuesta

<div id="delete_stream_link">
  ### `delete_stream_link`
</div>

Eliminar enlace de stream
Elimina el enlace de los eventos de FilteredStream al webhook especificado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El id del webhook que se vinculará a tu conjunto de reglas de FilteredStream.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`DeleteStreamLinkResponse` - datos de la respuesta

<div id="get">
  ### `get`
</div>

Obtener webhook
Obtener una lista de configuraciones de webhooks asociadas con una aplicación cliente.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_config_fields" type="Lista o None" default="None">
Una lista de campos de WebhookConfig separados por comas que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetResponse` - Datos de la respuesta

<div id="get_stream_links">
  ### `get_stream_links`
</div>

Obtener enlaces de stream
Obtiene una lista de enlaces de webhook asociados con un conjunto de reglas de stream filtrado.
:returns: Datos de la respuesta
:rtype: GetStreamLinksResponse

<div id="returns">
  #### Devuelve
</div>

`GetStreamLinksResponse`

<div id="validate">
  ### `validate`
</div>

Validar el webhook
Activa una comprobación CRC para un webhook determinado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.webhook_id" type="Any">
El ID del webhook que se va a verificar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`ValidateResponse` - Datos de la respuesta