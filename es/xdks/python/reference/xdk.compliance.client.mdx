---
title: "ComplianceClient"
sidebarTitle: "ComplianceClient"
---

<div id="complianceclient">
  ## ComplianceClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: object</Badge>

Cliente para operaciones de cumplimiento normativo

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_jobs">
  ### `create_jobs`
</div>

Crear Compliance Job
Crea un nuevo Compliance Job para el tipo de job especificado.
body: Cuerpo de la solicitud
:returns: datos de la respuesta
:rtype: CreateJobsResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateJobsRequest"></ParamField>

<div id="get_jobs">
  ### `get_jobs`
</div>

Obtener trabajos de cumplimiento
Obtiene una lista de trabajos de cumplimiento filtrados por tipo de trabajo y, de forma opcional, por estado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.type" type="str">
Tipo de Compliance Job que se va a listar.
</ParamField>

<ParamField path="path.status" type="str or None" default="None">
Estado de Compliance Job que se va a listar.
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
Lista separada por comas de los campos de ComplianceJob que se van a mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetJobsResponse` - datos de la respuesta

<div id="get_jobs_by_id">
  ### `get_jobs_by_id`
</div>

Obtener un Compliance Job por identificador
Recupera los detalles de un Compliance Job específico por su identificador.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El id del Compliance Job que se debe recuperar.
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
Una lista de campos de ComplianceJob, separados por comas, que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetJobsByIdResponse` - datos de la respuesta