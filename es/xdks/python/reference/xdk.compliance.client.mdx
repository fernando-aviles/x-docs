---
title: "ComplianceClient"
sidebarTitle: "ComplianceClient"
---

<div id="complianceclient">
  ## ComplianceClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: object</Badge>

Cliente para operaciones de cumplimiento normativo

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_jobs">
  ### `create_jobs`
</div>

Crear trabajo de cumplimiento
Crea un nuevo trabajo de cumplimiento para el tipo de trabajo especificado.
body: Cuerpo de la solicitud
:returns: datos de la respuesta
:rtype: CreateJobsResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateJobsRequest"></ParamField>

<div id="get_jobs">
  ### `get_jobs`
</div>

Obtener trabajos de cumplimiento
Obtiene una lista de trabajos de cumplimiento filtrados por tipo de trabajo y, opcionalmente, por estado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.type" type="str">
Tipo de Compliance Job que se desea listar.
</ParamField>

<ParamField path="path.status" type="str or None" default="None">
Estado del Compliance Job que se desea listar.
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
Lista separada por comas de campos de ComplianceJob que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetJobsResponse` - Datos de la respuesta

<div id="get_jobs_by_id">
  ### `get_jobs_by_id`
</div>

Obtener job de cumplimiento por id
Recupera los detalles de un job de cumplimiento específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID del Compliance Job que se va a recuperar.
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
Una lista separada por comas de los campos de ComplianceJob que se van a mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetJobsByIdResponse` - datos de la Respuesta