---
title: "ComplianceClient"
sidebarTitle: "ComplianceClient"
---

<div id="complianceclient">
  ## ComplianceClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: object</Badge>

Cliente para operaciones de cumplimiento

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_jobs">
  ### `create_jobs`
</div>

Crear un Compliance Job
Crea un nuevo Compliance Job para el tipo de job especificado.
body: Cuerpo de la solicitud
:returns: Datos de la respuesta
:rtype: CreateJobsResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateJobsRequest"></ParamField>

<div id="get_jobs">
  ### `get_jobs`
</div>

Obtener jobs de cumplimiento
Obtiene una lista de jobs de cumplimiento filtrados por tipo de job y, opcionalmente, por estado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.type" type="str">
Tipo de Compliance Job que se va a listar.
</ParamField>

<ParamField path="path.status" type="str or None" default="None">
Estado de Compliance Job que se va a listar.
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
Una lista separada por comas de los campos de ComplianceJob que se van a mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetJobsResponse` - Datos de respuesta

<div id="get_jobs_by_id">
  ### `get_jobs_by_id`
</div>

Obtener Compliance Job por ID
Recupera los detalles de un Compliance Job específico por su ID.

#### Parameters

<ParamField path="path.id" type="Any">
El ID del Compliance Job que se desea recuperar.
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
Una lista separada por comas de campos de ComplianceJob que se deben mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetJobsByIdResponse` - Datos de respuesta