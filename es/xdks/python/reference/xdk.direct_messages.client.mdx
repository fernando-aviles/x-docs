---
title: "DirectMessagesClient"
sidebarTitle: "DirectMessagesClient"
---

<div id="directmessagesclient">
  ## DirectMessagesClient
</div>

<Badge color="blue">Clase</Badge>

<Badge color="gray">Bases: object</Badge>

Cliente para operaciones con mensajes directos

<div id="constructors">
  ## Constructores
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_by_conversation_id">
  ### `create_by_conversation_id`
</div>

Crear mensaje directo por id de conversación
Envía un nuevo mensaje directo a una conversación específica mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.dm_conversation_id" type="str">
ID de la conversación de MD.
</ParamField>

<ParamField path="body.body" type="CreateByConversationIdRequest">
Cuerpo de la solicitud
</ParamField>

<div id="create_by_participant_id">
  ### `create_by_participant_id`
</div>

Crear un mensaje directo por ID de participante
Envía un nuevo mensaje directo a un participante específico mediante su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.participant_id" type="Any">
El ID del usuario destinatario que recibirá el mensaje directo.
</ParamField>

<ParamField path="body.body" type="CreateByParticipantIdRequest">
Cuerpo de la solicitud
</ParamField>

<div id="create_conversation">
  ### `create_conversation`
</div>

Crear una conversación de mensaje directo
Inicia una nueva conversación de mensaje directo con los participantes especificados.
body: Cuerpo de la solicitud
:returns: Datos de la respuesta
:rtype: CreateConversationResponse

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="body.body" type="CreateConversationRequest"></ParamField>

<div id="delete_events">
  ### `delete_events`
</div>

Eliminar evento de MD
Elimina un evento específico de mensaje directo por su id, siempre que pertenezca al usuario autenticado.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.event_id" type="Any">
El id del evento de mensaje directo que se eliminará.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`DeleteEventsResponse` - datos de la respuesta

<div id="get_events">
  ### `get_events`
</div>

Obtiene eventos de MD
Obtiene una lista de eventos recientes de mensajes directos en todas las conversaciones.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una “página” específica de resultados.
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
El conjunto de event_types que se incluirán en los resultados.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Una lista de campos de DmEvent, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista de campos, separados por comas, que se deben expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista de campos de Media, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista de campos de User, separados por comas, que se mostrarán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista de campos de Tweet, separados por comas, que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetEventsResponse`

<div id="get_events_by_conversation_id">
  ### `get_events_by_conversation_id`
</div>

Obtiene eventos de MD de una conversación de MD
Recupera eventos de mensajes directos para una conversación específica.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="Any">
El ID de la conversación de MD.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se usa para obtener una «página» específica de resultados.
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
El conjunto de event_types que se van a incluir en los resultados.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Una lista separada por comas de campos de DmEvent que se van a mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos que se van a expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de Media que se van a mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de User que se van a mostrar.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet que se van a mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetEventsByConversationIdResponse`

<div id="get_events_by_id">
  ### `get_events_by_id`
</div>

Obtener evento de mensaje directo por id
Recupera los detalles de un evento específico de mensaje directo mediante su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.event_id" type="Any">
id del evento de DM.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Una lista separada por comas de campos de DmEvent para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos de Media para mostrar.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos de User para mostrar.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos de Tweet para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetEventsByIdResponse` - datos de la respuesta

<div id="get_events_by_participant_id">
  ### `get_events_by_participant_id`
</div>

Obtener eventos de MD de una conversación de MD
Recupera los eventos de mensajes directos de una conversación específica.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.participant_id" type="Any">
El ID del usuario participante en la conversación de MD uno a uno.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
El número máximo de resultados.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
Este parámetro se utiliza para obtener una "página" específica de resultados.
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
El conjunto de event_types que se incluirán en los resultados.
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
Una lista separada por comas de campos DmEvent que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
Una lista separada por comas de campos Media que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
Una lista separada por comas de campos User que se mostrarán.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
Una lista separada por comas de campos Tweet que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetEventsByParticipantIdResponse`