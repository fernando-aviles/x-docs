---
title: "SpacesClient"
sidebarTitle: "SpacesClient"
---

<div id="submodules">
  ## Submódulos
</div>

* [módulo xdk.spaces.client](/es/xdks/python/reference/xdk.spaces.client)
  * [`SpacesClient`](/es/xdks/python/reference/xdk.spaces.client#xdk.spaces.client.SpacesClient)
    * [`SpacesClient.__init__()`](/es/xdks/python/reference/xdk.spaces.client#xdk.spaces.client.SpacesClient.__init__)
    * [`SpacesClient.get_buyers()`](/es/xdks/python/reference/xdk.spaces.client#xdk.spaces.client.SpacesClient.get_buyers)
    * [`SpacesClient.get_by_creator_ids()`](/es/xdks/python/reference/xdk.spaces.client#xdk.spaces.client.SpacesClient.get_by_creator_ids)
    * [`SpacesClient.get_by_id()`](/es/xdks/python/reference/xdk.spaces.client#xdk.spaces.client.SpacesClient.get_by_id)
    * [`SpacesClient.get_by_ids()`](/es/xdks/python/reference/xdk.spaces.client#xdk.spaces.client.SpacesClient.get_by_ids)
    * [`SpacesClient.get_posts()`](/es/xdks/python/reference/xdk.spaces.client#xdk.spaces.client.SpacesClient.get_posts)
    * [`SpacesClient.search()`](/es/xdks/python/reference/xdk.spaces.client#xdk.spaces.client.SpacesClient.search)
* [módulo xdk.spaces.models](/es/xdks/python/reference/xdk.spaces.models)
  * [`GetBuyersResponse`](/es/xdks/python/reference/xdk.spaces.models#xdk.spaces.models.GetBuyersResponse)
    * [`GetBuyersResponse.model_config`](/es/xdks/python/reference/xdk.spaces.models#xdk.spaces.models.GetBuyersResponse.model_config)
  * [`GetByCreatorIdsResponse`](/es/xdks/python/reference/xdk.spaces.models#xdk.spaces.models.GetByCreatorIdsResponse)
    * [`GetByCreatorIdsResponse.model_config`](/es/xdks/python/reference/xdk.spaces.models#xdk.spaces.models.GetByCreatorIdsResponse.model_config)
  * [`GetByIdResponse`](/es/xdks/python/reference/xdk.spaces.models#xdk.spaces.models.GetByIdResponse)
    * [`GetByIdResponse.model_config`](/es/xdks/python/reference/xdk.spaces.models#xdk.spaces.models.GetByIdResponse.model_config)
  * [`GetByIdsResponse`](/es/xdks/python/reference/xdk.spaces.models#xdk.spaces.models.GetByIdsResponse)
    * [`GetByIdsResponse.model_config`](/es/xdks/python/reference/xdk.spaces.models#xdk.spaces.models.GetByIdsResponse.model_config)
  * [`GetPostsResponse`](/es/xdks/python/reference/xdk.spaces.models#xdk.spaces.models.GetPostsResponse)
    * [`GetPostsResponse.model_config`](/es/xdks/python/reference/xdk.spaces.models#xdk.spaces.models.GetPostsResponse.model_config)
  * [`SearchResponse`](/es/xdks/python/reference/xdk.spaces.models#xdk.spaces.models.SearchResponse)
    * [`SearchResponse.model_config`](/es/xdks/python/reference/xdk.spaces.models#xdk.spaces.models.SearchResponse.model_config)

<div id="module-contents">
  ## Contenido del módulo
</div>

Este módulo proporciona acceso a los endpoints de Spaces de la X API
y actúa como punto de entrada principal para toda la funcionalidad relacionada con Spaces.

<div id="class-xdkspacesspacesclient">
  ### `class xdk.spaces.SpacesClient`
</div>

Cliente para operaciones con spaces

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client" />

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.client" type="Client" />

<div id="get_buyers">
  ### `get_buyers`
</div>

Obtener compradores de entradas de un Space
Recupera una lista de usuarios que compraron entradas para un Space específico mediante su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="str">
  El ID del Space que se va a recuperar.
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  Este parámetro se utiliza para obtener una &quot;página&quot; específica de resultados.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  El número máximo de resultados.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  Una lista de campos de usuario separados por comas para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  Una lista de campos a expandir, separados por comas.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  Una lista de campos de Tuit separados por comas para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`IteratorGetBuyersResponse`

<div id="get_by_creator_ids">
  ### `get_by_creator_ids`
</div>

Obtener Spaces por identificadores de creador
Recupera detalles de los Spaces creados por los identificadores de usuario especificados.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.user_ids" type="List">
  Los id de los usuarios entre los que se va a buscar.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
  Una lista de campos de Space, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  Una lista de campos, separados por comas, para expandir.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  Una lista de campos de usuario, separados por comas, para mostrar.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
  Una lista de campos de Topic, separados por comas, para mostrar.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByCreatorIdsResponse` - Datos de la respuesta

<div id="get_by_id">
  ### `get_by_id`
</div>

Obtener un Space por ID
Recupera los detalles de un Space específico por su ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="str">
  El id del Space que se va a obtener.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Space que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  Una lista separada por comas de campos de User que se mostrarán.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Topic que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByIdResponse` - datos de la respuesta

<div id="get_by_ids">
  ### `get_by_ids`
</div>

Obtener Spaces por ID
Recupera los detalles de varios Spaces por sus ID.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.ids" type="List">
  La lista de ID de Space que se devolverán.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Space que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  Una lista separada por comas de campos de User que se mostrarán.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Topic que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetByIdsResponse` - Datos de la respuesta

<div id="get_posts">
  ### `get_posts`
</div>

Obtener publicaciones de un Space
Recupera una lista de publicaciones compartidas en un Space específico por su id.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.id" type="str">
  El id del Space que se va a obtener.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  La cantidad de Publicaciones que se obtendrán del Space proporcionado. Si no se proporciona, el valor tomará el máximo de 100.
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Tweet que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  Una lista separada por comas de campos para expandir.
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Media que se mostrarán.
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Poll que se mostrarán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  Una lista separada por comas de campos de User que se mostrarán.
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Place que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`GetPostsResponse` - Datos de respuesta

<div id="search">
  ### `search`
</div>

Buscar Spaces
Obtiene una lista de Spaces que coinciden con la consulta de búsqueda indicada.

<div id="parameters">
  #### Parámetros
</div>

<ParamField path="path.query" type="str">
  La consulta de búsqueda.
</ParamField>

<ParamField path="path.state" type="str or None" default="None">
  El estado de los Spaces que se desea buscar.
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  El número de resultados que se devolverán.
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Space que se mostrarán.
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  Una lista separada por comas de campos que se expandirán.
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  Una lista separada por comas de campos de User que se mostrarán.
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
  Una lista separada por comas de campos de Topic que se mostrarán.
</ParamField>

<div id="returns">
  #### Devuelve
</div>

`SearchResponse` - datos de la respuesta