---
title: "Client"
sidebarTitle: "Client"
---

Clase de cliente principal para X API

Este es el punto de entrada principal para interactuar con X API. Proporciona
acceso a todos los endpoints de la API mediante módulos de cliente especializados y gestiona
la autenticación, la configuración de las solicitudes y el manejo de errores.

**`Ejemplo`**

```typescript
import { Client } from '@xdevplatform/xdk';

const client = new Client({
  bearerToken: 'your-bearer-token'
});

// Obtener información del usuario
const user = await client.users.getUser('783214');

// Obtener seguidores con paginación
const followers = await client.users.getFollowers('783214', {
  maxResults: 10,
  userFields: ['id', 'name', 'username']
});

// Iterar por los seguidores
for await (const follower of followers) {
  console.log(follower.username);
}
```

<div id="constructors">
  ## Constructores
</div>

<div id="constructor">
  ### constructor
</div>

• **new Client**(`config`): [`Client`](/es/xdks/typescript/reference/classes/Client)

Crea una nueva instancia de cliente de X API

<div id="parameters">
  #### Parámetros
</div>

| Nombre | Tipo | Descripción |
| :------ | :------ | :------ |
| `config` | `any` | Opciones de configuración para el cliente |

<div id="returns">
  #### Valor devuelto
</div>

[`Client`](/es/xdks/typescript/reference/classes/Client)

**`Ejemplo`**

```typescript
// Autenticación con Bearer Token
const client = new Client({
  bearerToken: 'your-bearer-token'
});

// Autenticación con OAuth2
const client = new Client({
  accessToken: 'your-access-token'
});

// Autenticación con OAuth1
const client = new Client({
  oauth1: oauth1Instance
});
```

[client.ts:401](https://github.com/xdevplatform/xdk-typescript/blob/81aacb165e0802e188f608bdf462b60fc4e713a2/src/client.ts#L401)

<div id="properties">
  ## Propiedades
</div>

<ResponseField name="baseUrl" type="string" required>
  URL base para las solicitudes a la API
</ResponseField>

<ResponseField name="bearerToken" type="string">
  Bearer Token para la autenticación
</ResponseField>

<ResponseField name="accessToken" type="string">
  Token de acceso OAuth2
</ResponseField>

<ResponseField name="oauth1" type="any">
  Instancia de OAuth1 para la autenticación
</ResponseField>

<ResponseField name="headers" type="Headers" required>
  Encabezados para las solicitudes
</ResponseField>

<ResponseField name="timeout" type="number" required>
  Tiempo de espera de las solicitudes en milisegundos
</ResponseField>

<ResponseField name="retry" type="boolean" required>
  Indica si se deben reintentar automáticamente las solicitudes fallidas
</ResponseField>

<ResponseField name="maxRetries" type="number" required>
  Número máximo de intentos de reintento
</ResponseField>

<ResponseField name="httpClient" type="HttpClient = httpClient" required>
  Cliente HTTP para realizar solicitudes
</ResponseField>

<ResponseField name="general" type="GeneralClient" required>
  Cliente general
</ResponseField>

<ResponseField name="accountActivity" type="AccountActivityClient" required>
  Cliente de actividad de la cuenta
</ResponseField>

<ResponseField name="communityNotes" type="CommunityNotesClient" required>
  Cliente de notas de la comunidad
</ResponseField>

<ResponseField name="compliance" type="ComplianceClient" required>
  Cliente de cumplimiento
</ResponseField>

<ResponseField name="connections" type="ConnectionsClient" required>
  Cliente de conexiones
</ResponseField>

<ResponseField name="users" type="UsersClient" required>
  Cliente de usuarios
</ResponseField>

<ResponseField name="news" type="NewsClient" required>
  Cliente de noticias
</ResponseField>

<ResponseField name="spaces" type="SpacesClient" required>
  Cliente de Spaces
</ResponseField>

<ResponseField name="activity" type="ActivityClient" required>
  Cliente de actividad
</ResponseField>

<ResponseField name="usage" type="UsageClient" required>
  Cliente de uso
</ResponseField>

<ResponseField name="trends" type="TrendsClient" required>
  Cliente de tendencias
</ResponseField>

<ResponseField name="posts" type="PostsClient" required>
  Cliente de publicaciones
</ResponseField>

<ResponseField name="directMessages" type="DirectMessagesClient" required>
  Cliente de mensajes directos
</ResponseField>

<ResponseField name="communities" type="CommunitiesClient" required>
  Cliente de comunidades
</ResponseField>

<ResponseField name="media" type="MediaClient" required>
  Cliente de medios
</ResponseField>

<ResponseField name="webhooks" type="WebhooksClient" required>
  Cliente de webhooks
</ResponseField>

<ResponseField name="stream" type="StreamClient" required>
  Cliente de streaming
</ResponseField>

<ResponseField name="lists" type="ListsClient" required>
  Cliente de listas
</ResponseField>

<ResponseField name="request" type="Promise<T>" required>
  Realiza una solicitud autenticada a X API

  Este método gestiona la autenticación, el formateo de la solicitud y el manejo
  de errores para todas las solicitudes de API. Agrega automáticamente los
  encabezados de autenticación apropiados según la configuración del cliente.
</ResponseField>

<ResponseField name="isTokenExpired" type="boolean" required>
  Comprueba si el token de OAuth2 ha expirado
</ResponseField>

<ResponseField name="refreshToken" type="Promise<void>" required>
  Renueva el token de OAuth2
</ResponseField>

<ResponseField name="isAuthenticated" type="boolean" required>
  Obtiene el estado de autenticación actual
</ResponseField>

<ResponseField name="mapSecuritySchemeToAuthTypes" type="string[]" required>
  Asigna los nombres de esquemas de seguridad de OpenAPI a los tipos internos de autenticación
</ResponseField>

<ResponseField name="validateAuthentication" type="void" required>
  Valida que el método de autenticación requerido esté disponible
</ResponseField>

<ResponseField name="getAvailableAuthTypes" type="string[]" required>
  Obtiene los tipos de autenticación disponibles
</ResponseField>