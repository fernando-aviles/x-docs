---
title: "XDK de TypeScript"
sidebarTitle: "Descripción general"
---

Un SDK completo de TypeScript para la X API (antes Twitter API) con funciones avanzadas como paginación inteligente, múltiples métodos de autenticación, streaming en tiempo real y seguridad de tipos total.

<div id="key-features">
  ## Características clave
</div>

* **🔐 Autenticación**: Autenticación en contexto de usuario (OAuth1.0a, OAuth2.0) y solo de aplicación (Bearer Token)
* **🔄 Paginación**: Paginación automática con compatibilidad con iteración asíncrona
* **📡 Streaming**: Streaming controlado por eventos con reconexión automática
* **📚 Tipado seguro**: Definiciones completas de TypeScript para todos los endpoints y parámetros
* **🎯 Compatibilidad completa con X API**: Usuarios, Publicaciones, Listas, Marcadores, Comunidades y más

<div id="quick-start">
  ## Inicio rápido
</div>

<CodeGroup dropdown>
  ```typescript quickstart.ts theme={null}
  import { 
      Client, 
      type ClientConfig,
      type Users
  } from '@xdevplatform/xdk';

  const config: ClientConfig = { bearerToken: 'your-bearer-token' };

  const client: Client = new Client(config);

  async function main(): Promise<void> {
    const userResponse: Users.GetByUsernameResponse = await client.users.getByUsername('XDevelopers');
    const username: string = userResponse.data?.username!;
    console.log(username);
  }

  main();
  ```

  ```javascript quickstart.js theme={null}
  import { Client } from '@xdevplatform/xdk';

  const client = new Client({ bearerToken: 'your-bearer-token' });

  const userResponse = await client.users.getByUsername('XDevelopers');
  const username = userResponse.data.username;
  console.log(username);
  ```
</CodeGroup>

<Info>
  Para ver ejemplos de código detallados que usan el XDK de JavaScript/TypeScript, consulta nuestro [repositorio de ejemplos de código en GitHub](https://github.com/xdevplatform/samples/tree/main/javascript).
</Info>

<div id="whats-next">
  ## Próximos pasos
</div>

* [Guía de instalación](/es/xdks/typescript/install) - Configura el SDK en tu proyecto
* [Autenticación](/es/xdks/typescript/authentication) - Aprende sobre los diferentes métodos de autenticación
* [Paginación](/es/xdks/typescript/pagination) - Aprende sobre la paginación de datos
* [Streaming](/es/xdks/typescript/streaming) - Aprende sobre la transmisión de datos en tiempo real
* [Referencia de la API](/es/xdks/typescript/reference/Client) - Consulta la documentación completa de la API