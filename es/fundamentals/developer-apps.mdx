---
title: Aplicaciones
sidebarTitle: Aplicaciones
icon: cube
description: Crea y configura aplicaciones para acceder a la X API
keywords: ["apps de X", "Apps de desarrollador", "claves de API", "OAuth", "autenticación", "gestión de apps", "credenciales de API", "tokens de acceso", "OAuth 1.0a", "OAuth 2.0", "permisos de la app", "URL de callback"]
---

Las aplicaciones actúan como contenedores para tus credenciales de API. Cada aplicación tiene sus propias claves, tokens y configuración.

---

<div id="app-credentials">
  ## Credenciales de la aplicación
</div>

Cuando creas una aplicación, puedes generar estas credenciales:

| Credencial | Caso de uso |
|:-----------|:------------|
| **API Key & Secret** | Autenticación con OAuth 1.0a. Se utilizan para firmar solicitudes o generar tokens de usuario. |
| **Access Token & Secret** | Realizar solicitudes en nombre de tu propia cuenta (OAuth 1.0a). |
| **Client ID & Secret** | Autenticación con OAuth 2.0. Se utilizan para el flujo de código de autorización. |
| **Bearer Token** | Autenticación solo de aplicación (app-only) para endpoints de datos públicos. |

<Tip>
Elige **OAuth 2.0** para proyectos nuevos. Ofrece permisos granulares y es obligatorio para los endpoints de contexto de usuario de X API v2.
</Tip>

---

<div id="creating-an-app">
  ## Crear una app
</div>

<Steps>
  <Step title="Abre Developer Console">
    Ve a [console.x.com](https://console.x.com) e inicia sesión.
  </Step>
  <Step title="Haz clic en Create App">
    Introduce un nombre, una descripción y el caso de uso de tu app.
  </Step>
  <Step title="Genera credenciales">
    Una vez creada, genera las claves y tokens que necesites.
  </Step>
  <Step title="Almacena de forma segura">
    Guarda las credenciales inmediatamente; solo se mostrarán una vez.
  </Step>
</Steps>

---

<div id="app-permissions-oauth-10a">
  ## Permisos de la aplicación (OAuth 1.0a)
</div>

Las aplicaciones de OAuth 1.0a tienen tres niveles de permisos:

<Tabs>
  <Tab title="Solo lectura">
    - Ver publicaciones, usuarios y datos públicos
    - No pueden publicar, indicar Me gusta ni modificar nada
    - No pueden acceder a los Mensajes directos
  </Tab>
  <Tab title="Lectura y escritura">
    - Todos los permisos de lectura
    - Publicar y eliminar publicaciones
    - Seguir/dejar de seguir usuarios
    - Indicar Me gusta y volver a publicar
    - No pueden acceder a los Mensajes directos
  </Tab>
  <Tab title="Lectura, escritura y MD">
    - Todos los permisos de lectura y escritura
    - Enviar y leer Mensajes directos
  </Tab>
</Tabs>

<Note>
Cambiar los permisos requiere que los usuarios vuelvan a autorizar tu aplicación para obtener nuevos tokens con el nuevo alcance.
</Note>

---

<div id="oauth-20-app-types">
  ## Tipos de apps OAuth 2.0
</div>

Al configurar OAuth 2.0, selecciona el tipo de app:

| Tipo | Cliente | Caso de uso |
|:-----|:--------|:------------|
| **Web App** | Confidencial | Aplicaciones del lado del servidor que pueden almacenar secretos de forma segura |
| **Automated App / Bot** | Confidencial | Bots y servicios automatizados que se ejecutan en servidores |
| **Native App** | Público | Apps móviles o de escritorio que no pueden proteger secretos |
| **Single Page App** | Público | Aplicaciones JavaScript basadas en el navegador |

Los **clientes confidenciales** reciben un Client Secret. Los **clientes públicos** usan únicamente PKCE.

---

<div id="callback-urls">
  ## URL de callback
</div>

Las URL de callback (URI de redirección) son obligatorias en los flujos de OAuth. Después de que un usuario autoriza tu aplicación, se le redirige a tu URL de callback con un código de autorización.

<div id="requirements">
  ### Requisitos
</div>

- Añade las URL de callback a la lista de permitidos de tu aplicación en el Developer Console
- Las URL deben coincidir exactamente (incluidas las barras diagonales finales)
- Máximo de **10 URL de callback** por aplicación
- Usa `https://` en producción
- Para desarrollo local, usa `http://127.0.0.1` (no `localhost`)

<div id="disallowed-protocols">
  ### Protocolos no permitidos
</div>

No se pueden utilizar los siguientes protocolos: `javascript`, `data`, `file`, `ftp`, `mailto`, `telnet` y otros esquemas no estándar.

<Accordion title="Lista completa de protocolos no permitidos">
`vbscript`, `javascript`, `vbs`, `data`, `mocha`, `keyword`, `livescript`, `ftp`, `file`, `gopher`, `acrobat`, `callto`, `daap`, `itpc`, `itms`, `firefoxurl`, `hcp`, `ldap`, `mailto`, `mmst`, `mmsu`, `msbd`, `rtsp`, `mso-offdap`, `snews`, `news`, `nntp`, `outlook`, `stssync`, `rlogin`, `telnet`, `tn3270`, `shell`, `sip`
</Accordion>

---

<div id="best-practices">
  ## Mejores prácticas
</div>

<CardGroup cols={2}>
  <Card title="Usa aplicaciones separadas" icon="layer-group">
    Crea distintas aplicaciones para desarrollo, preproducción y producción.
  </Card>
  <Card title="Rota las credenciales" icon="arrows-rotate">
    Regenera las claves periódicamente y cuando sospeches un posible compromiso.
  </Card>
  <Card title="Permisos mínimos" icon="shield-check">
    Solicita solo los permisos que tu aplicación realmente necesita.
  </Card>
  <Card title="Supervisa el uso" icon="chart-simple">
    Consulta la Developer Console con regularidad para supervisar el uso de la API.
  </Card>
</CardGroup>

---

<div id="automated-account-labels">
  ## Etiquetas de cuentas automatizadas
</div>

Si tu app ejecuta una cuenta bot, puedes etiquetarla como automatizada:

1. Ve a la **Configuración** de tu cuenta bot
2. Selecciona **Tu cuenta** → **Automatización**
3. Vincula la cuenta que la administra

Esto genera confianza entre los usuarios y distingue tu bot del spam.

---

<div id="troubleshooting">
  ## Solución de problemas
</div>

<Accordion title="Error: URL de callback no aprobada">
Asegúrate de que tu URL de callback coincida exactamente con la registrada en el Developer Console, incluido el protocolo y cualquier barra diagonal final. Codifica la URL en HTTP (URL-encode) cuando la pases como parámetro de consulta.
```json
{
  "errors": [{
    "code": 415,
    "message": "Callback URL not approved for this client application."
  }]
}
```
</Accordion>

<Accordion title="Aplicación suspendida">
Si tu aplicación aparece como suspendida, revisa tu correo electrónico para ver si hay una notificación del equipo de la plataforma de X. Usa el [Platform Help Form](https://help.x.com/forms/platform) para presentar una apelación.
</Accordion>