---
title: Descripción general de miembros de Listas
sidebarTitle: Descripción general
description: Comienza a usar los endpoints de miembros de Listas
keywords: ["inicio rápido de miembros de Listas", "descripción general de miembros de Listas", "tutorial de miembros de Listas", "inicio rápido de pertenencia a Listas"]
---

import { Button } from '/snippets/es/button.mdx';

Los endpoints de miembros de Lista te permiten consultar los miembros de una Lista y gestionar la membresía de la Lista.

<Note>
  **Requisitos previos**

  Antes de comenzar, necesitarás:

  * Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
  * El Bearer Token de tu App (para consultas)
  * Token de acceso de usuario (para administrar miembros)
</Note>

***

<div id="available-endpoints">
  ## Endpoints disponibles
</div>

<CardGroup cols={2}>
  <Card title="Consultar miembros de la Lista" icon="users" href="/es/x-api/lists/list-members/quickstart/list-members-lookup">
    Obtener miembros de una Lista
  </Card>

  <Card title="Gestionar miembros de la Lista" icon="user-plus" href="/es/x-api/lists/list-members/quickstart/manage-list-members">
    Agregar y eliminar miembros
  </Card>
</CardGroup>

***

<div id="authentication">
  ## Autenticación
</div>

| Operación | Autenticación |
|:----------|:---------------|
| Buscar miembros | Bearer Token, OAuth 1.0a o OAuth 2.0 PKCE |
| Añadir/eliminar miembros | OAuth 1.0a o OAuth 2.0 PKCE |

***

<div id="quick-example">
  ## Ejemplo rápido
</div>

<CodeGroup dropdown>
  ```bash cURL
  # Obtener miembros de la lista
  curl "https://api.x.com/2/lists/84839422/members" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Obtener miembros de la lista
  for page in client.lists.get_members("84839422"):
      for user in page.data:
          print(f"{user.username}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Obtener miembros de la lista
  const paginator = client.lists.getMembers("84839422");

  for await (const page of paginator) {
    page.data?.forEach((user) => {
      console.log(user.username);
    });
  }
  ```
</CodeGroup>

***

<div id="next-steps">
  ## Próximos pasos
</div>

<CardGroup cols={2}>
  <Card title="Consulta de listas" icon="list" href="/es/x-api/lists/list-lookup/quickstart">
    Obtener detalles de una lista
  </Card>

  <Card title="Publicaciones de una lista" icon="message" href="/es/x-api/lists/list-tweets/quickstart">
    Obtener publicaciones de una lista
  </Card>
</CardGroup>