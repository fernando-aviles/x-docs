---
title: Consulta de miembros de una lista
sidebarTitle: Consulta de miembros de una lista
description: Obtener miembros de una lista
keywords: ["guía de inicio rápido de consulta de miembros de la lista", "consulta de miembros de la lista", "guía de inicio rápido para obtener miembros de la lista", "consulta de pertenencia a la lista"]
---

import { Button } from '/snippets/es/button.mdx';

Esta guía te muestra cómo obtener los miembros de una Lista.

<Note>
  **Requisitos previos**

  Antes de comenzar, necesitas:

  * Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
  * El Bearer Token de tu App
</Note>

***

<div id="get-list-members">
  ## Obtener miembros de la Lista
</div>

<Steps>
  <Step title="Encontrar el ID de la Lista">
    Puedes encontrar el ID de la Lista en la URL cuando ves una Lista:

    ```
    https://x.com/i/lists/84839422
                          └── Este es el ID de la Lista
    ```
  </Step>

  <Step title="Solicitar los miembros de la Lista">
    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/lists/84839422/members?\
      user.fields=created_at,username,verified&\
      max_results=100" \
        -H "Authorization: Bearer $BEARER_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # Obtener miembros de la Lista con paginación
      for page in client.lists.get_members(
          "84839422",
          user_fields=["created_at", "username", "verified"],
          max_results=100
      ):
          for user in page.data:
              print(f"{user.username} - Se unió: {user.created_at}")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      // Obtener miembros de la Lista con paginación
      const paginator = client.lists.getMembers("84839422", {
        userFields: ["created_at", "username", "verified"],
        maxResults: 100,
      });

      for await (const page of paginator) {
        page.data?.forEach((user) => {
          console.log(`${user.username} - Se unió: ${user.created_at}`);
        });
      }
      ```
    </CodeGroup>
  </Step>

  <Step title="Revisar la respuesta">
    ```json
    {
      "data": [
        {
          "id": "1319036828964454402",
          "name": "Birdwatch",
          "username": "birdwatch",
          "created_at": "2020-10-21T22:04:47.000Z",
          "verified": true
        },
        {
          "id": "1065249714214457345",
          "name": "Spaces",
          "username": "TwitterSpaces",
          "created_at": "2018-11-21T14:24:58.000Z",
          "verified": true
        }
      ],
      "meta": {
        "result_count": 2,
        "next_token": "5349804505549807616"
      }
    }
    ```
  </Step>
</Steps>

***

<div id="include-additional-data">
  ## Incluir datos adicionales
</div>

Usa el parámetro expansions para obtener datos relacionados como publicaciones fijadas:

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/lists/84839422/members?\
  user.fields=created_at&\
  expansions=pinned_tweet_id&\
  tweet.fields=created_at" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Obtener miembros de la lista con expansions
  for page in client.lists.get_members(
      "84839422",
      user_fields=["created_at"],
      expansions=["pinned_tweet_id"],
      tweet_fields=["created_at"]
  ):
      for user in page.data:
          print(f"{user.username}")
      # Las publicaciones fijadas están en page.includes.tweets
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Obtener miembros de la lista con expansions
  const paginator = client.lists.getMembers("84839422", {
    userFields: ["created_at"],
    expansions: ["pinned_tweet_id"],
    tweetFields: ["created_at"],
  });

  for await (const page of paginator) {
    page.data?.forEach((user) => {
      console.log(user.username);
    });
    // Las publicaciones fijadas están en page.includes?.tweets
  }
  ```
</CodeGroup>

***

<div id="next-steps">
  ## Próximos pasos
</div>

<CardGroup cols={2}>
  <Card title="Administrar miembros de la lista" icon="user-plus" href="/es/x-api/lists/list-members/quickstart/manage-list-members">
    Agregar y eliminar miembros
  </Card>

  <Card title="Búsqueda de listas" icon="list" href="/es/x-api/lists/list-lookup/quickstart">
    Obtener detalles de la lista
  </Card>

  <Card title="Referencia de la API" icon="code" href="/es/x-api/lists/get-list-members">
    Documentación completa del endpoint
  </Card>
</CardGroup>