---
title: Gestionar Listas fijadas
sidebarTitle: Gestionar Listas fijadas
description: Fijar y desfijar Listas
keywords: ["inicio rápido para gestionar listas fijadas", "inicio rápido para fijar listas", "inicio rápido para desfijar listas", "gestión de listas fijadas"]
---

import { Button } from '/snippets/es/button.mdx';

Esta guía te muestra cómo fijar y desfijar Listas.

<Note>
  **Requisitos previos**

  Antes de comenzar, necesitas:

  * Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
  * Token de acceso de usuario (OAuth 1.0a o OAuth 2.0 PKCE)
</Note>

***

<div id="pin-a-list">
  ## Fijar una Lista
</div>

<Steps>
  <Step title="Obtén tu ID de usuario y el ID de la Lista">
    Necesitas el ID del usuario autenticado y el ID de la Lista que quieres fijar. Puedes encontrar el ID de una Lista en la URL cuando la estés viendo.
  </Step>

  <Step title="Fija la Lista">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/users/2244994945/pinned_lists" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"list_id": "1454155907651158017"}'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # Fijar una Lista
      response = client.lists.pin(
          user_id="2244994945",
          list_id="1454155907651158017"
      )

      print(f"Fijada: {response.data.pinned}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // Fijar una Lista
      const response = await client.lists.pin("2244994945", {
        listId: "1454155907651158017",
      });

      console.log(`Fijada: ${response.data?.pinned}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="Revisa la respuesta">
    ```json
    {
      "data": {
        "pinned": true
      }
    }
    ```
  </Step>
</Steps>

***

<div id="unpin-a-list">
  ## Desfijar una Lista
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl -X DELETE "https://api.x.com/2/users/2244994945/pinned_lists/1454155907651158017" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client
  from xdk.oauth1_auth import OAuth1

  oauth1 = OAuth1(
      api_key="YOUR_API_KEY",
      api_secret="YOUR_API_SECRET",
      access_token="YOUR_ACCESS_TOKEN",
      access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
  )

  client = Client(auth=oauth1)

  # Desfijar una Lista
  response = client.lists.unpin(
      user_id="2244994945",
      list_id="1454155907651158017"
  )

  print(f"Pinned: {response.data.pinned}")
  ```

  ```javascript JavaScript SDK
  import { Client, OAuth1 } from "@xdevplatform/xdk";

  const oauth1 = new OAuth1({
    apiKey: "YOUR_API_KEY",
    apiSecret: "YOUR_API_SECRET",
    accessToken: "YOUR_ACCESS_TOKEN",
    accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
  });

  const client = new Client({ oauth1 });

  // Desfijar una Lista
  const response = await client.lists.unpin("2244994945", "1454155907651158017");

  console.log(`Pinned: ${response.data?.pinned}`);
  ```
</CodeGroup>

**Respuesta:**

```json
{
  "data": {
    "pinned": false
  }
}
```

***

<div id="important-notes">
  ## Notas importantes
</div>

<Note>
  * Solo puedes anclar Listas que sigues o que te pertenecen
  * Las Listas ancladas aparecen en la parte superior de tus Listas en la app de X
  * Hay un límite en la cantidad de Listas que puedes anclar
</Note>

***

<div id="next-steps">
  ## Próximos pasos
</div>

<CardGroup cols={2}>
  <Card title="Consulta de listas fijadas" icon="thumbtack" href="/es/x-api/lists/pinned-lists/quickstart/pinned-list-lookup">
    Obtén tus listas fijadas
  </Card>

  <Card title="Consulta de listas" icon="list" href="/es/x-api/lists/list-lookup/quickstart">
    Obtén los detalles de la lista
  </Card>

  <Card title="Referencia de la API" icon="code" href="/es/x-api/lists/pin-list">
    Documentación completa del endpoint
  </Card>
</CardGroup>