---
title: Códigos de respuesta y errores
sidebarTitle: Códigos de respuesta
description: Códigos de estado HTTP y manejo de errores de X API
keywords: ["códigos de respuesta", "códigos de error", "códigos de estado HTTP", "errores de API", "manejo de errores", "depuración"]
---

import { Button } from '/snippets/es/button.mdx';

La X API usa códigos de estado HTTP estándar. Las solicitudes satisfactorias devuelven códigos 2xx; los errores devuelven códigos 4xx o 5xx con detalles en el cuerpo de la respuesta.

***

<div id="http-status-codes">
  ## Códigos de estado HTTP
</div>

<div id="success-codes">
  ### Códigos de éxito
</div>

| Código | Significado | Descripción |
|:-----|:--------|:------------|
| **200** | OK | Solicitud realizada correctamente |
| **201** | Creado | Recurso creado (solicitudes POST) |
| **204** | Sin contenido | Éxito sin cuerpo de respuesta (solicitudes DELETE) |

<div id="client-error-codes">
  ### Códigos de error del cliente
</div>

| Código | Significado | Causas comunes |
|:-----|:--------|:--------------|
| **400** | Solicitud incorrecta | JSON no válido, consulta mal formada, falta de parámetros obligatorios |
| **401** | No autorizado | Credenciales de autenticación no válidas o faltantes |
| **403** | Prohibido | Autenticación válida pero sin permisos para este recurso o acción |
| **404** | No encontrado | El recurso no existe o se ha eliminado |
| **409** | Conflicto | El stream no tiene reglas (solo para el stream filtrado) |
| **429** | Demasiadas solicitudes | Se superó el rate limit o el límite de uso |

<div id="server-error-codes">
  ### Códigos de error del servidor
</div>

| Código | Significado | Qué hacer |
|:-----|:--------|:-----------|
| **500** | Error interno del servidor | Espera y vuelve a intentarlo; consulta la [página de estado](/es/status) |
| **502** | Puerta de enlace no válida | Espera y vuelve a intentarlo |
| **503** | Servicio no disponible | X está sobrecargado; espera y vuelve a intentarlo |
| **504** | Tiempo de espera de la puerta de enlace | Espera y vuelve a intentarlo |

***

<div id="error-response-format">
  ## Formato de las respuestas de error
</div>

Las respuestas de error incluyen detalles estructurados:

```json
{
  "title": "Invalid Request",
  "detail": "The 'query' parameter is required.",
  "type": "https://api.x.com/2/problems/invalid-request"
}
```

| Campo    | Descripción                          |
| :------- | :----------------------------------- |
| `type`   | URI que identifica el tipo de error  |
| `title`  | Descripción breve del error          |
| `detail` | Explicación específica de este error |

Según el tipo de error, pueden estar presentes campos adicionales.

***

<div id="error-types">
  ## Tipos de errores
</div>

| Tipo | Descripción |
|:-----|:------------|
| `about:blank` | Error genérico (consulta el código de estado HTTP) |
| `.../invalid-request` | Solicitud con formato incorrecto o parámetros no válidos |
| `.../resource-not-found` | La Publicación, el usuario u otro recurso no existe |
| `.../not-authorized-for-resource` | Sin acceso a contenido privado/protegido |
| `.../client-forbidden` | La aplicación no está inscrita o no tiene el acceso requerido |
| `.../usage-capped` | Se superó el límite de uso |
| `.../rate-limit-exceeded` | Se superó el límite de solicitudes |
| `.../streaming-connection` | Problema en la conexión de streaming |
| `.../rule-cap` | Demasiadas reglas de stream filtrado |
| `.../invalid-rules` | Error de sintaxis en la regla |
| `.../duplicate-rules` | La regla ya existe |

***

<div id="partial-errors">
  ## Errores parciales
</div>

Algunas solicitudes pueden completarse solo parcialmente. Una respuesta 200 puede incluir tanto `data` como `errors`:

```json
{
  "data": [
    {"id": "123", "text": "Hello"}
  ],
  "errors": [
    {
      "resource_id": "456",
      "resource_type": "tweet",
      "title": "Error No Encontrado",
      "detail": "No se pudo encontrar el tuit con el id: [456].",
      "type": "https://api.x.com/2/problems/resource-not-found"
    }
  ]
}
```

Esto sucede cuando se solicitan varios recursos y algunos no están disponibles.

***

<div id="troubleshooting-common-errors">
  ## Solución de problemas de errores comunes
</div>

<Accordion title="401 No autorizado">
  **Verifica tu autenticación:**

  * Comprueba que estás usando el método de autenticación correcto para el endpoint
  * Asegúrate de que las credenciales no se hayan regenerado
  * Verifica el formato del encabezado `Authorization`
  * Para OAuth 1.0a, verifica el cálculo de la firma

  [Guía de autenticación →](/es/resources/fundamentals/authentication/overview)
</Accordion>

<Accordion title="403 Prohibido">
  **Verifica tu acceso:**

  * Comprueba que tu aplicación tenga acceso a este endpoint
  * Algunos endpoints requieren una inscripción o aprobación específica
  * Los endpoints con contexto de usuario necesitan los permisos (scopes) de OAuth apropiados
  * Es posible que el recurso sea privado o esté protegido
</Accordion>

<Accordion title="429 Demasiadas solicitudes">
  **Límite de frecuencia alcanzado:**

  * Consulta el encabezado `x-rate-limit-reset` para saber cuándo reintentar
  * Implementa backoff exponencial
  * Considera almacenar en caché las respuestas
  * Distribuye las solicitudes a lo largo de la ventana de tiempo

  [Guía de límites de frecuencia →](/es/x-api/fundamentals/rate-limits)
</Accordion>

<Accordion title="400 Solicitud incorrecta">
  **Corrige tu solicitud:**

  * Valida la sintaxis JSON
  * Verifica que no falten parámetros obligatorios
  * Comprueba los tipos de parámetros (cadenas de texto vs. números)
  * Escapa los caracteres especiales en las consultas
</Accordion>

<Accordion title="Faltan publicaciones esperadas">
  **Verifica estos factores:**

  * Las publicaciones de cuentas protegidas solo son visibles con autorización
  * Las publicaciones eliminadas devuelven 404
  * Algunas publicaciones se retienen en ciertas regiones
  * Verifica que la sintaxis de la consulta de búsqueda sea correcta
</Accordion>

<Accordion title="Desconexiones de la transmisión">
  **Gestiona la reconexión:**

  * Implementa la reconexión automática con backoff
  * Usa las funciones de recuperación para los datos perdidos
  * Comprueba si hay desconexiones por búfer lleno (el cliente no consume lo suficientemente rápido)
  * Verifica que exista al menos una regla de transmisión

  [Guía de transmisión →](/es/x-api/posts/filtered-stream/integrate/handling-disconnections)
</Accordion>

***

<div id="rate-limit-headers">
  ## Encabezados de límite de frecuencia
</div>

Cada respuesta incluye información sobre los límites de frecuencia:

```
x-rate-limit-limit: 900
x-rate-limit-remaining: 847
x-rate-limit-reset: 1705420800
```

| Encabezado               | Descripción                                             |
| :----------------------- | :------------------------------------------------------ |
| `x-rate-limit-limit`     | Límite máximo de solicitudes en la ventana actual       |
| `x-rate-limit-remaining` | Solicitudes restantes                                   |
| `x-rate-limit-reset`     | Marca de tiempo Unix en la que se restablece la ventana |

***

<div id="best-practices">
  ## Mejores prácticas
</div>

<CardGroup cols={2}>
  <Card title="Verifica los códigos de estado" icon="square-check">
    Verifica siempre el código de estado HTTP antes de analizar el cuerpo de la respuesta.
  </Card>

  <Card title="Gestiona errores parciales" icon="triangle-exclamation">
    Verifica la matriz `errors` incluso en respuestas con código 200.
  </Card>

  <Card title="Implementa lógica de reintentos" icon="arrows-rotate">
    Usa backoff exponencial para errores 429 y 5xx.
  </Card>

  <Card title="Registra detalles de la solicitud" icon="file-lines">
    Incluye el ID de la solicitud y la marca de tiempo para depuración.
  </Card>
</CardGroup>

***

<div id="getting-help">
  ## Obtener ayuda
</div>

Al publicar consultas sobre errores, incluye lo siguiente:

* La URL del endpoint de la API
* Los encabezados de la solicitud (elimina o sustituye las credenciales)
* La respuesta de error completa
* Lo que esperabas que sucediera
* Los pasos que ya intentaste

<CardGroup cols={2}>
  <Card title="Foro de desarrolladores" icon="comments" href="https://devcommunity.x.com">
    Haz preguntas y busca soluciones.
  </Card>

  <Card title="Estado de la API" icon="signal" href="/es/status">
    Revisa si hay incidencias conocidas.
  </Card>
</CardGroup>