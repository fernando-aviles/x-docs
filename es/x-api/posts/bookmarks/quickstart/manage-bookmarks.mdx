---
title: Administrar marcadores
sidebarTitle: Administrar marcadores
description: Añade y elimina marcadores con la X API
keywords: ["guía de inicio rápido para administrar marcadores", "guía de inicio rápido de marcadores", "tutorial de marcadores", "guía de marcadores", "inicio rápido de marcadores"]
---

import { Button } from '/snippets/es/button.mdx';

Esta guía explica cómo añadir y eliminar marcadores con la X API.

<Note>
  **Requisitos previos**

  Antes de comenzar, necesitas lo siguiente:

  * Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
  * Un token de acceso de usuario (User Access Token) con el alcance `bookmark.write` (OAuth 2.0 PKCE)
</Note>

***

<div id="add-a-bookmark">
  ## Añadir un marcador
</div>

<Steps>
  <Step title="Obtén tu ID de usuario">
    Necesitas el ID del usuario autenticado. Puedes encontrarlo usando el endpoint `/2/users/me` o el [endpoint de búsqueda de usuarios](/es/x-api/users/lookup/introduction).
  </Step>

  <Step title="Obtén el ID de la Publicación">
    Encuentra el ID de la publicación en la URL al ver una publicación:

    ```
    https://x.com/XDevelopers/status/1460323737035677698
                                    └── Este es el ID de la publicación
    ```
  </Step>

  <Step title="Añade el marcador">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/users/2244994945/bookmarks" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"tweet_id": "1460323737035677698"}'
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

      # Añade un marcador
      response = client.bookmarks.create(
          user_id="2244994945",
          tweet_id="1460323737035677698"
      )

      print(f"Marcado: {response.data.bookmarked}")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

      // Añade un marcador
      const response = await client.bookmarks.create("2244994945", {
        tweetId: "1460323737035677698",
      });

      console.log(`Marcado: ${response.data?.bookmarked}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="Revisa la respuesta">
    ```json
    {
      "data": {
        "bookmarked": true
      }
    }
    ```
  </Step>
</Steps>

***

<div id="remove-a-bookmark">
  ## Quitar un marcador
</div>

Elimina una Publicación de tus marcadores:

<CodeGroup dropdown>
  ```bash cURL
  curl -X DELETE "https://api.x.com/2/users/2244994945/bookmarks/1460323737035677698" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

  # Quitar un marcador
  response = client.bookmarks.delete(
      user_id="2244994945",
      tweet_id="1460323737035677698"
  )

  print(f"Bookmarked: {response.data.bookmarked}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

  // Quitar un marcador
  const response = await client.bookmarks.delete("2244994945", "1460323737035677698");

  console.log(`Bookmarked: ${response.data?.bookmarked}`);
  ```
</CodeGroup>

**Respuesta:**

```json
{
  "data": {
    "bookmarked": false
  }
}
```

***

<div id="required-scopes">
  ## Ámbitos obligatorios
</div>

Cuando uses OAuth 2.0 PKCE, tu token de acceso debe incluir los siguientes ámbitos:

| Ámbito | Descripción |
|:------|:------------|
| `bookmark.write` | Agregar y eliminar marcadores |
| `tweet.read` | Leer data de publicaciones |
| `users.read` | Leer data de usuarios |

***

<div id="next-steps">
  ## Próximos pasos
</div>

<CardGroup cols={2}>
  <Card title="Búsqueda de marcadores" icon="bookmark" href="/es/x-api/posts/bookmarks/quickstart/bookmarks-lookup">
    Obtén tus publicaciones guardadas en marcadores
  </Card>

  <Card title="Referencia de la API" icon="code" href="/es/x-api/posts/add-post-to-bookmarks">
    Documentación completa del endpoint
  </Card>
</CardGroup>