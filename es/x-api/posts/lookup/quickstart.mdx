---
title: Inicio rápido
sidebarTitle: Inicio rápido
description: Realiza tu primera solicitud de consulta de una Publicación en cuestión de minutos
keywords: ["inicio rápido de consulta de Publicación", "inicio rápido de consulta de Tuit", "tutorial de consulta", "guía de consulta", "inicio rápido para obtener un Tuit", "ejemplo de consulta"]
---

import { Button } from "/snippets/es/button.mdx"

Esta guía te explicará cómo realizar tu primera solicitud de consulta de una Publicación con la X API v2.

<Note>
  **Requisitos previos**

  Antes de comenzar, necesitarás:

  * Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
  * El Bearer Token de tu App (que se encuentra en la Consola de Desarrollador en «Keys and tokens»)
</Note>

***

<Steps>
  <Step title="Buscar el id de una Publicación">
    Cada Publicación tiene un id único. Puedes encontrarlo en la URL de la Publicación:

    ```
    https://x.com/XDevelopers/status/1228393702244134912
                                    └── Este es el ID de la Publicación
    ```
  </Step>

  <Step title="Realizar una solicitud">
    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/tweets/1228393702244134912" \
        -H "Authorization: Bearer $BEARER_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # Obtener una sola Publicación por id
      response = client.posts.get("1228393702244134912")
      print(response.data)
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      const response = await client.posts.get("1228393702244134912");
      console.log(response.data);
      ```
    </CodeGroup>
  </Step>

  <Step title="Revisa la respuesta">
    La respuesta predeterminada incluye el `id`, el `text` y `edit_history_tweet_ids` de la Publicación:

    ```json
    {
      "data": {
        "id": "1228393702244134912",
        "text": "What did the developer write in their Valentine's card?\n\nwhile(true) {\n    I = Love(You);\n}",
        "edit_history_tweet_ids": ["1228393702244134912"]
      }
    }
    ```
  </Step>

  <Step title="Solicita campos adicionales">
    Utiliza parámetros de consulta para obtener más datos:

    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/tweets/1228393702244134912?\
      tweet.fields=created_at,public_metrics,author_id&\
      expansions=author_id&\
      user.fields=username,verified" \
        -H "Authorization: Bearer $BEARER_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # Obtener una Publicación con campos adicionales y expansiones
      response = client.posts.get(
          "1228393702244134912",
          tweet_fields=["created_at", "public_metrics", "author_id"],
          expansions=["author_id"],
          user_fields=["username", "verified"]
      )

      print(response.data)
      print(response.includes)  # Contiene el objeto de usuario del autor
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      const response = await client.posts.get("1228393702244134912", {
        tweetFields: ["created_at", "public_metrics", "author_id"],
        expansions: ["author_id"],
        userFields: ["username", "verified"],
      });

      console.log(response.data);
      console.log(response.includes); // Contiene el objeto de usuario del autor
      ```
    </CodeGroup>

    **Respuesta:**

    ```json
    {
      "data": {
        "id": "1228393702244134912",
        "text": "What did the developer write in their Valentine's card?...",
        "created_at": "2020-02-14T19:00:55.000Z",
        "author_id": "2244994945",
        "public_metrics": {
          "retweet_count": 156,
          "reply_count": 23,
          "like_count": 892,
          "quote_count": 12
        },
        "edit_history_tweet_ids": ["1228393702244134912"]
      },
      "includes": {
        "users": [
          {
            "id": "2244994945",
            "username": "XDevelopers",
            "verified": true
          }
        ]
      }
    }
    ```
  </Step>

  <Step title="Consultar varias publicaciones">
    Recupera hasta 100 publicaciones en una sola solicitud:

    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/tweets?\
      ids=1228393702244134912,1227640996038684673,1199786642791452673&\
      tweet.fields=created_at,author_id" \
        -H "Authorization: Bearer $BEARER_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # Obtener varias publicaciones por ID
      response = client.posts.get_posts(
          ids=["1228393702244134912", "1227640996038684673", "1199786642791452673"],
          tweet_fields=["created_at", "author_id"]
      )

      for post in response.data:
          print(f"{post.id}: {post.text[:50]}...")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      const response = await client.posts.getPosts({
        ids: ["1228393702244134912", "1227640996038684673", "1199786642791452673"],
        tweetFields: ["created_at", "author_id"],
      });

      response.data?.forEach((post) => {
        console.log(`${post.id}: ${post.text?.slice(0, 50)}...`);
      });
      ```
    </CodeGroup>
  </Step>
</Steps>

***

<div id="next-steps">
  ## Próximos pasos
</div>

<CardGroup cols={2}>
  <Card title="Guía de integración" icon="book" href="/es/x-api/posts/lookup/integrate">
    Aprende sobre la autenticación, los límites de frecuencia y las buenas prácticas
  </Card>

  <Card title="Campos y Expansions" icon="sliders" href="/es/x-api/fundamentals/fields">
    Domina el sistema de campos y Expansions
  </Card>

  <Card title="Referencia de la API" icon="code" href="/es/x-api/posts/get-post-by-id">
    Consulta todos los parámetros disponibles
  </Card>

  <Card title="Código de ejemplo" icon="github" href="https://github.com/xdevplatform/Twitter-API-v2-sample-code">
    Explora más ejemplos
  </Card>
</CardGroup>