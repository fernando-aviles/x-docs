---
title: Haz tu primera solicitud
sidebarTitle: Haz tu primera solicitud
description: Empieza a usar X API en cuestión de minutos
keywords: ["primera solicitud", "primeros pasos", "inicio rápido de la API", "realizar solicitud de API", "cURL", "ejemplo de API", "código de ejemplo"]
---

import { Button } from '/snippets/es/button.mdx';

En esta guía aprenderás a hacer tu primera solicitud a la X API. Antes de empezar, necesitarás una [cuenta de desarrollador con credenciales de App](/es/x-api/getting-started/getting-access).

***

<div id="quick-start-with-curl">
  ## Inicio rápido con cURL
</div>

La forma más rápida de probar la API es con cURL. Vamos a buscar un usuario:

```bash
curl "https://api.x.com/2/users/by/username/xdevelopers" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

Reemplaza `$BEARER_TOKEN` con tu Bearer Token real. Obtendrás una respuesta como:

```json
{
  "data": {
    "id": "2244994945",
    "name": "X Developers",
    "username": "xdevelopers"
  }
}
```

***

<div id="step-by-step-guide">
  ## Guía paso a paso
</div>

<Steps>
  <Step title="Obtén tu Bearer Token">
    En la [Consola de desarrollador](https://console.x.com), dirígete a tu App y copia el Bearer Token.
  </Step>

  <Step title="Elige un endpoint">
    Empieza con uno de estos endpoints aptos para principiantes:

    | Endpoint | Qué hace |
    |:---------|:-------------|
    | [User lookup](/es/x-api/users/lookup/introduction) | Obtiene el perfil de usuario por nombre de usuario o ID |
    | [Post lookup](/es/x-api/posts/lookup/introduction) | Obtiene una publicación por ID |
    | [Recent search](/es/x-api/posts/search/introduction) | Busca publicaciones de los últimos 7 días |
  </Step>

  <Step title="Realiza la solicitud">
    Usa cURL, Postman o tu cliente HTTP preferido:

    ```bash
    # Buscar un usuario por nombre de usuario
    curl "https://api.x.com/2/users/by/username/xdevelopers" \
      -H "Authorization: Bearer $BEARER_TOKEN"
    ```
  </Step>

  <Step title="Analiza la respuesta">
    Las respuestas están en formato JSON. Los datos principales están en el campo `data`:

    ```json
    {
      "data": {
        "id": "2244994945",
        "name": "X Developers",
        "username": "xdevelopers"
      }
    }
    ```
  </Step>
</Steps>

***

<div id="request-more-data-with-fields">
  ## Solicita más datos con campos
</div>

De forma predeterminada, los endpoints devuelven un conjunto mínimo de campos. Usa el parámetro `fields` para solicitar datos adicionales:

```bash
curl "https://api.x.com/2/users/by/username/xdevelopers?user.fields=created_at,description,public_metrics" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

Respuesta:

```json
{
  "data": {
    "id": "2244994945",
    "name": "X Developers",
    "username": "xdevelopers",
    "created_at": "2013-12-14T04:35:55.000Z",
    "description": "La voz de la Plataforma de Desarrolladores de X",
    "public_metrics": {
      "followers_count": 570842,
      "following_count": 2048,
      "tweet_count": 14052,
      "listed_count": 1672
    }
  }
}
```

[Más información sobre campos →](/es/x-api/fundamentals/fields)

***

<div id="more-examples">
  ## Más ejemplos
</div>

<Tabs>
  <Tab title="Consultar una publicación">
    ```bash
    curl "https://api.x.com/2/tweets/1460323737035677698?tweet.fields=created_at,public_metrics" \
      -H "Authorization: Bearer $BEARER_TOKEN"
    ```
  </Tab>

  <Tab title="Buscar publicaciones recientes">
    ```bash
    curl "https://api.x.com/2/tweets/search/recent?query=from:xdevelopers&tweet.fields=created_at" \
      -H "Authorization: Bearer $BEARER_TOKEN"
    ```
  </Tab>

  <Tab title="Obtener las publicaciones de un usuario">
    ```bash
    curl "https://api.x.com/2/users/2244994945/tweets?max_results=5" \
      -H "Authorization: Bearer $BEARER_TOKEN"
    ```
  </Tab>
</Tabs>

***

<div id="using-code-instead-of-curl">
  ## Usar código en lugar de cURL
</div>

<Tabs>
  <Tab title="Python">
    ```python
    import requests

    bearer_token = "YOUR_BEARER_TOKEN"
    url = "https://api.x.com/2/users/by/username/xdevelopers"

    headers = {"Authorization": f"Bearer {bearer_token}"}
    response = requests.get(url, headers=headers)

    print(response.json())
    ```
  </Tab>

  <Tab title="JavaScript">
    ```javascript
    const bearerToken = "YOUR_BEARER_TOKEN";
    const url = "https://api.x.com/2/users/by/username/xdevelopers";

    fetch(url, {
      headers: { Authorization: `Bearer ${bearerToken}` }
    })
      .then(res => res.json())
      .then(data => console.log(data));
    ```
  </Tab>

  <Tab title="SDK oficiales">
    Para uso en producción, recomendamos los SDK oficiales:

    * [Python SDK](/es/xdks/python/overview)
    * [TypeScript SDK](/es/xdks/typescript/overview)

    Estos gestionan automáticamente la autenticación, la paginación y la limitación de frecuencia de solicitudes.
  </Tab>
</Tabs>

***

<div id="tools-for-testing">
  ## Herramientas para realizar pruebas
</div>

<CardGroup cols={3}>
  <Card title="Postman" icon="server" href="/es/tutorials/postman-getting-started">
    Pruebas visuales de la API con nuestra colección.
  </Card>

  <Card title="Código de ejemplo" icon="github" href="https://github.com/xdevplatform/Twitter-API-v2-sample-code">
    Ejemplos en varios lenguajes de programación.
  </Card>

  <Card title="Referencia de la API" icon="code" href="/es/x-api/posts/lookup/introduction">
    Documentación completa del endpoint.
  </Card>
</CardGroup>

***

<div id="troubleshooting">
  ## Solución de problemas
</div>

<Accordion title="401 Unauthorized">
  * Verifica que tu Bearer Token sea correcto
  * Asegúrate de que el token no se haya regenerado
  * Verifica el formato del encabezado `Authorization`: `Bearer YOUR_TOKEN`
</Accordion>

<Accordion title="403 Forbidden">
  * Es posible que tu app no tenga acceso a este endpoint
  * Algunos endpoints requieren autenticación con contexto de usuario (OAuth 1.0a o 2.0)
  * Revisa los permisos de tu app en la Consola de desarrollador
</Accordion>

<Accordion title="429 Too Many Requests">
  * Has alcanzado un límite de frecuencia de solicitudes
  * Revisa el encabezado `x-rate-limit-reset` para saber cuándo reintentar
  * Implementa backoff exponencial en tu código
</Accordion>

[Referencia completa de errores →](/es/x-api/fundamentals/response-codes-and-errors)

***

<div id="next-steps">
  ## Próximos pasos
</div>

<CardGroup cols={2}>
  <Card title="Aprende sobre autenticación" icon="key" href="/es/resources/fundamentals/authentication/overview">
    Comprende OAuth para solicitudes con contexto de usuario.
  </Card>

  <Card title="Explora los endpoints" icon="compass" href="/es/x-api/posts/search/introduction">
    Descubre lo que puedes crear.
  </Card>

  <Card title="Usa un SDK" icon="cube" href="/es/xdks/overview">
    Desarrolla más rápido con bibliotecas oficiales.
  </Card>

  <Card title="Crea algo" icon="hammer" href="/es/x-api/what-to-build">
    Ideas sobre qué crear.
  </Card>
</CardGroup>