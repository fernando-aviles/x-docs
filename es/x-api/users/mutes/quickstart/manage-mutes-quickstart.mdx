---
title: Gestionar cuentas silenciadas
sidebarTitle: Gestionar cuentas silenciadas
description: Silenciar y dejar de silenciar cuentas de usuario con X API
keywords: ["Inicio rápido para gestionar cuentas silenciadas", "Inicio rápido para silenciar usuarios", "Tutorial para gestionar cuentas silenciadas", "Guía para silenciar usuarios", "Inicio rápido sobre cuentas silenciadas"]
---

import { Button } from '/snippets/es/button.mdx';

Esta guía te explica cómo silenciar y dejar de silenciar usuarios con la X API.

<Note>
  **Requisitos previos**

  Antes de comenzar, necesitarás:

  * Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
  * Un token de acceso de usuario (User Access Token) (OAuth 1.0a o OAuth 2.0 PKCE)
</Note>

***

<div id="mute-a-user">
  ## Silenciar a un usuario
</div>

<Steps>
  <Step title="Obtén tu ID de usuario">
    Necesitas el ID del usuario autenticado. Puedes encontrarlo usando el [endpoint de búsqueda de usuarios](/es/x-api/users/lookup/introduction) o a partir de tu Access Token (la parte numérica es tu ID de usuario).
  </Step>

  <Step title="Obtén el ID del usuario de destino">
    Encuentra el ID de usuario de la cuenta que quieres silenciar usando el [endpoint de búsqueda de usuarios](/es/x-api/users/lookup/introduction).
  </Step>

  <Step title="Envía la solicitud para silenciar">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/users/123456789/muting" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"target_user_id": "9876543210"}'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # Silenciar a un usuario
      response = client.users.mute(
          source_user_id="123456789",
          target_user_id="9876543210"
      )

      print(f"Muting: {response.data.muting}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // Silenciar a un usuario
      const response = await client.users.mute("123456789", {
        targetUserId: "9876543210",
      });

      console.log(`Muting: ${response.data?.muting}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="Revisa la respuesta">
    ```json
    {
      "data": {
        "muting": true
      }
    }
    ```
  </Step>
</Steps>

***

<div id="unmute-a-user">
  ## Quitar el silencio a un usuario
</div>

Quita el silencio a un usuario:

<CodeGroup dropdown>
  ```bash cURL
  curl -X DELETE "https://api.x.com/2/users/123456789/muting/9876543210" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python SDK de Python
  from xdk import Client
  from xdk.oauth1_auth import OAuth1

  oauth1 = OAuth1(
      api_key="YOUR_API_KEY",
      api_secret="YOUR_API_SECRET",
      access_token="YOUR_ACCESS_TOKEN",
      access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
  )

  client = Client(auth=oauth1)

  # Quitar el silencio a un usuario
  response = client.users.unmute(
      source_user_id="123456789",
      target_user_id="9876543210"
  )

  print(f"Muting: {response.data.muting}")
  ```

  ```javascript SDK de JavaScript
  import { Client, OAuth1 } from "@xdevplatform/xdk";

  const oauth1 = new OAuth1({
    apiKey: "YOUR_API_KEY",
    apiSecret: "YOUR_API_SECRET",
    accessToken: "YOUR_ACCESS_TOKEN",
    accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
  });

  const client = new Client({ oauth1 });

  // Quitar el silencio a un usuario
  const response = await client.users.unmute("123456789", "9876543210");

  console.log(`Muting: ${response.data?.muting}`);
  ```
</CodeGroup>

**Respuesta:**

```json
{
  "data": {
    "muting": false
  }
}
```

***

<div id="mute-vs-block">
  ## Silenciar vs Bloquear
</div>

| Característica | Silenciar | Bloquear |
|:--------|:-----|:------|
| Ver sus publicaciones | No | No |
| Pueden ver tus publicaciones | Sí | No |
| Pueden seguirte | Sí | No |
| Pueden enviarte mensajes directos (MD) | Sí | No |
| Lo saben | No | Sí |

***

<div id="next-steps">
  ## Próximos pasos
</div>

<CardGroup cols={2}>
  <Card title="Búsqueda de cuentas silenciadas" icon="volume-xmark" href="/es/x-api/users/mutes/quickstart/mutes-lookup">
    Obtén la lista de tus usuarios silenciados
  </Card>

  <Card title="Bloqueos" icon="ban" href="/es/x-api/users/blocks/quickstart">
    En su lugar, bloquea usuarios
  </Card>

  <Card title="Referencia de la API" icon="code" href="/es/x-api/users/mute-user-by-user-id">
    Documentación completa del endpoint
  </Card>
</CardGroup>