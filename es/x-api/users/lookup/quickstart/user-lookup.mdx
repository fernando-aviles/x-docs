---
title: Guía rápida de búsqueda de usuarios
sidebarTitle: Búsqueda de usuarios
description: Buscar usuarios por ID o nombre de usuario
keywords: ["guía rápida de búsqueda de usuarios", "tutorial para obtener usuarios", "guía de usuario por ID", "usuario por nombre de usuario"]
---

import { Button } from '/snippets/es/button.mdx';

Esta guía te mostrará cómo buscar usuarios por su ID o nombre de usuario.

<Note>
  **Requisitos previos**

  Antes de comenzar, necesitarás:

  * Una [cuenta de desarrollador](https://developer.x.com/en/portal/petition/essential/basic-info) con una App aprobada
  * El Bearer Token de tu App
</Note>

***

<div id="look-up-by-id">
  ## Consultar por id
</div>

<div id="single-user">
  ### Usuario individual
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/2244994945?\
  user.fields=created_at,description,verified,public_metrics" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Obtener usuario por id
  response = client.users.get(
      "2244994945",
      user_fields=["created_at", "description", "verified", "public_metrics"]
  )

  print(f"Nombre: {response.data.name}")
  print(f"Nombre de usuario: {response.data.username}")
  print(f"Seguidores: {response.data.public_metrics.followers_count}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Obtener usuario por id
  const response = await client.users.get("2244994945", {
    userFields: ["created_at", "description", "verified", "public_metrics"],
  });

  console.log(`Nombre: ${response.data?.name}`);
  console.log(`Nombre de usuario: ${response.data?.username}`);
  console.log(`Seguidores: ${response.data?.public_metrics?.followers_count}`);
  ```
</CodeGroup>

<div id="response">
  ### Respuesta
</div>

```json
{
  "data": {
    "id": "2244994945",
    "name": "X Developers",
    "username": "XDevelopers",
    "created_at": "2013-12-14T04:35:55.000Z",
    "description": "The voice of the X developer community",
    "verified": true,
    "public_metrics": {
      "followers_count": 583423,
      "following_count": 2048,
      "tweet_count": 14052,
      "listed_count": 1672
    }
  }
}
```

<div id="multiple-users">
  ### Varios usuarios
</div>

Consulta hasta 100 usuarios a la vez:

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users?\
  ids=2244994945,783214,6253282&\
  user.fields=username,verified" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Obtener varios usuarios por ID
  response = client.users.get_users(
      ids=["2244994945", "783214", "6253282"],
      user_fields=["username", "verified"]
  )

  for user in response.data:
      print(f"{user.username} - Verified: {user.verified}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Obtener varios usuarios por ID
  const response = await client.users.getUsers({
    ids: ["2244994945", "783214", "6253282"],
    userFields: ["username", "verified"],
  });

  response.data?.forEach((user) => {
    console.log(`${user.username} - Verified: ${user.verified}`);
  });
  ```
</CodeGroup>

***

<div id="look-up-by-username">
  ## Consultar por nombre de usuario
</div>

<div id="single-user">
  ### Usuario individual
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/by/username/XDevelopers?\
  user.fields=created_at,description,verified" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Obtener un usuario por su nombre de usuario
  response = client.users.get_by_username(
      "XDevelopers",
      user_fields=["created_at", "description", "verified"]
  )

  print(f"ID: {response.data.id}")
  print(f"Nombre: {response.data.name}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Obtener un usuario por su nombre de usuario
  const response = await client.users.getByUsername("XDevelopers", {
    userFields: ["created_at", "description", "verified"],
  });

  console.log(`ID: ${response.data?.id}`);
  console.log(`Nombre: ${response.data?.name}`);
  ```
</CodeGroup>

<div id="multiple-users">
  ### Varios usuarios
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/by?\
  usernames=XDevelopers,X,elonmusk&\
  user.fields=created_at,verified" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # Obtener varios usuarios por sus nombres de usuario
  response = client.users.get_users_by_usernames(
      usernames=["XDevelopers", "X", "elonmusk"],
      user_fields=["created_at", "verified"]
  )

  for user in response.data:
      print(f"{user.username} - {user.created_at}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // Obtener varios usuarios por sus nombres de usuario
  const response = await client.users.getUsersByUsernames({
    usernames: ["XDevelopers", "X", "elonmusk"],
    userFields: ["created_at", "verified"],
  });

  response.data?.forEach((user) => {
    console.log(`${user.username} - ${user.created_at}`);
  });
  ```
</CodeGroup>

***

<div id="available-fields">
  ## Campos disponibles
</div>

| Campo | Descripción |
|:------|:------------|
| `created_at` | Fecha de creación de la cuenta |
| `description` | Biografía del usuario |
| `profile_image_url` | URL del avatar |
| `verified` | Estado de verificación |
| `public_metrics` | Cantidad de seguidores/seguidos |
| `location` | Ubicación definida por el usuario |
| `url` | Sitio web del usuario |
| `protected` | Estado de cuenta protegida |
| `pinned_tweet_id` | ID de Publicación fijada |

***

<div id="handle-errors">
  ## Manejo de errores
</div>

<div id="user-not-found">
  ### Usuario no encontrado
</div>

```json
{
  "errors": [
    {
      "resource_type": "user",
      "title": "Not Found Error",
      "detail": "Could not find user with username: [nonexistent_user]."
    }
  ]
}
```

<div id="protected-user">
  ### Usuario protegido
</div>

Los data de los usuarios protegidos continuarán devolviéndose, pero no podrás acceder a sus Publicaciones a menos que los sigas.

***

<div id="next-steps">
  ## Próximos pasos
</div>

<CardGroup cols={2}>
  <Card title="Usuario autenticado" icon="user-check" href="/es/x-api/users/lookup/quickstart/authenticated-lookup">
    Obtener el usuario actual
  </Card>

  <Card title="Guía de integración" icon="book" href="/es/x-api/users/lookup/integrate">
    Conceptos clave y mejores prácticas
  </Card>

  <Card title="Referencia de la API" icon="code" href="/es/x-api/users/single-user-lookup">
    Documentación completa del endpoint
  </Card>
</CardGroup>