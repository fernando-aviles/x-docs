---
title: "分页响应（PaginatedResponse）"
sidebarTitle: "分页响应（PaginatedResponse）"
---

分页响应接口

表示 X API 分页响应的结构。

<div id="type-parameters">
  ## 类型参数
</div>

| 名称 | 描述 |
| :------ | :------ |
| `T` | 响应中各项的类型 |

<div id="properties">
  ## 属性
</div>

<ResponseField name="data" type="T[]" required>
当前页中的项目数组

</ResponseField>

<ResponseField name="meta" type="Object">
分页元数据

<Expandable title="属性">
<ResponseField name="Name" type="Type | Description" required>
</ResponseField>

<ResponseField name="resultCount" type="number | 当前页中的结果数量">
</ResponseField>

<ResponseField name="nextToken" type="string | 用于获取下一页的令牌">
</ResponseField>

<ResponseField name="previousToken" type="string | 用于获取上一页的令牌">
</ResponseField>
</Expandable>
</ResponseField>

<ResponseField name="includes" type="Record&lt;string, any&gt;">
额外返回的对象（用户、推文等）

</ResponseField>

<ResponseField name="errors" type="any[]">
响应中的任何错误

</ResponseField>