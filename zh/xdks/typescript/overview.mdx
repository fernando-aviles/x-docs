---
title: "TypeScript XDK"
sidebarTitle: "概览"
---

一款面向 X API（前身为 Twitter API）的完整 TypeScript SDK，提供智能分页、多种身份验证方式、实时流式传输以及全面的类型安全等高级功能。

<div id="key-features">
  ## 关键功能
</div>

* **🔐 身份验证**：用户上下文（OAuth 1.0a、OAuth 2.0）以及应用专用（Bearer Token）身份验证
* **🔄 分页**：支持异步迭代的自动分页
* **📡 流式传输**：事件驱动的流式传输，并具备自动重连功能
* **📚 类型安全**：为所有端点和参数提供完整的 TypeScript 定义
* **🎯 完整的 X API 支持**：用户、帖子、列表、书签、社区等

<div id="quick-start">
  ## 快速开始
</div>

<CodeGroup dropdown>
  ```typescript quickstart.ts theme={null}
  import { 
      Client, 
      type ClientConfig,
      type Users
  } from '@xdevplatform/xdk';

  const config: ClientConfig = { bearerToken: 'your-bearer-token' };

  const client: Client = new Client(config);

  async function main(): Promise<void> {
    const userResponse: Users.GetByUsernameResponse = await client.users.getByUsername('XDevelopers');
    const username: string = userResponse.data?.username!;
    console.log(username);
  }

  main();
  ```

  ```javascript quickstart.js theme={null}
  import { Client } from '@xdevplatform/xdk';

  const client = new Client({ bearerToken: 'your-bearer-token' });

  const userResponse = await client.users.getByUsername('XDevelopers');
  const username = userResponse.data.username;
  console.log(username);
  ```
</CodeGroup>

<Info>
  要查看更多使用 JavaScript/TypeScript XDK 的详细代码示例，请访问我们的 [代码示例 GitHub 仓库](https://github.com/xdevplatform/samples/tree/main/javascript)。
</Info>

<div id="whats-next">
  ## 后续步骤
</div>

* [Installation Guide](/zh/xdks/typescript/install) - 在项目中设置 SDK
* [Authentication](/zh/xdks/typescript/authentication) - 了解不同的身份验证方法
* [Pagination](/zh/xdks/typescript/pagination) - 了解数据分页
* [Streaming](/zh/xdks/typescript/streaming) - 了解实时数据流
* [API Reference](/zh/xdks/typescript/reference/Client) - 查阅完整的 API 参考文档