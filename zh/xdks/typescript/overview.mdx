---
title: "TypeScript XDK"
sidebarTitle: "概览"
---

一个面向 X API（前称 Twitter API）的功能完备的 TypeScript SDK，提供智能分页、多种认证方式、实时流式传输以及完备的类型安全等高级特性。

<div id="key-features">
  ## 关键功能
</div>

* **🔐 身份验证**：用户上下文（OAuth1.0a、OAuth2.0）和仅限应用（Bearer Token）身份验证
* **🔄 分页**：支持异步迭代的自动分页
* **📡 流式**：事件驱动的流式处理，并支持自动重连
* **📚 类型安全**：为所有端点和参数提供完整的 TypeScript 定义
* **🎯 完整的 X API 支持**：用户、帖子、列表、书签、社区等

<div id="quick-start">
  ## 快速开始
</div>

<CodeGroup dropdown>
  ```typescript quickstart.ts theme={null}
  import { 
      Client, 
      type ClientConfig,
      type Users
  } from '@xdevplatform/xdk';

  const config: ClientConfig = { bearerToken: 'your-bearer-token' };

  const client: Client = new Client(config);

  async function main(): Promise<void> {
    const userResponse: Users.GetByUsernameResponse = await client.users.getByUsername('XDevelopers');
    const username: string = userResponse.data?.username!;
    console.log(username);
  }

  main();
  ```

  ```javascript quickstart.js theme={null}
  import { Client } from '@xdevplatform/xdk';

  const client = new Client({ bearerToken: 'your-bearer-token' });

  const userResponse = await client.users.getByUsername('XDevelopers');
  const username = userResponse.data.username;
  console.log(username);
  ```
</CodeGroup>

<Info>
  要查看使用 JavaScript/TypeScript XDK 的详细代码示例，请参阅我们的 [代码示例 GitHub 仓库](https://github.com/xdevplatform/samples/tree/main/javascript)。
</Info>

<div id="whats-next">
  ## 接下来可以做什么？
</div>

* [Installation Guide](/zh/xdks/typescript/install) - 在你的项目中安装并配置 SDK
* [Authentication](/zh/xdks/typescript/authentication) - 了解不同的认证方法
* [Pagination](/zh/xdks/typescript/pagination) - 了解如何对数据进行分页
* [Streaming](/zh/xdks/typescript/streaming) - 了解实时数据流式处理
* [API Reference](/zh/xdks/typescript/reference/Client) - 阅读完整的 API 参考文档