---
title: 快速入门
sidebarTitle: 快速入门
---

本示例演示如何使用 XDK，并通过 Bearer 令牌身份验证快速搜索帖子。

<div id="step-1-install-the-sdk">
  ## 步骤 1：安装 SDK
</div>

```bash
pip install xdk
```

<div id="step-2-get-your-bearer-token">
  ## 步骤 2：获取你的 Bearer 令牌
</div>

1. 登录 [X 开发者控制台](https://developer.x.com/en/portal/dashboard)。
2. 创建或选择一个 App。
3. 在 “Keys and Tokens” 下生成一个 Bearer 令牌（仅 App 身份验证）。

<div id="step-3-write-and-run-your-first-script">
  ## 步骤 3：编写并运行你的第一个脚本
</div>

创建一个名为 `quickstart.py` 的文件：

```python
# 导入 client
from xdk import Client
# 替换为您实际的 Bearer 令牌
client = Client(bearer_token="YOUR_BEARER_TOKEN_HERE")
# 获取提及 "api" 的最近帖子
# search_recent 返回一个迭代器,因此对其进行迭代
for page in client.posts.search_recent(query="api", max_results=10):
    if page.data and len(page.data) > 0:
        # 访问第一个帖子 - Pydantic 模型同时支持属性访问和字典访问
        first_post = page.data[0]
        post_text = first_post.text if hasattr(first_post, 'text') else first_post.get('text', '')
        print(f"Latest Post: {post_text}")
        break
    else:
        print("No Posts found.")
        break
```

运行代码：

```bash
python quickstart.py
```

**预期输出**：

```
最新帖子:XDK Python SDK 的重要更新!
```

**故障排查**：若遇到 401 错误，请仔细检查你的 Bearer 令牌。对于 429 速率限制错误，请等待一段时间后再重试。

<div id="next-steps">
  ## 后续步骤
</div>

* 查看 [Authentication](/zh/xdks/python/authentication)，了解如何使用 Bearer 令牌（仅 App）认证、带 PKCE 的 OAuth 2.0（用户上下文）以及 OAuth 1.0a（旧版用户上下文）。
* 学习 [Pagination](/zh/xdks/python/pagination)，适用于需要返回大量结果、又不想手动处理多次 API 调用的场景。
* 深入学习 [Streaming](/zh/xdks/python/streaming)，了解如何处理实时数据。
  如需查看使用 Python XDK 的详细代码示例，请访问我们的 [代码示例 GitHub 仓库](https://github.com/xdevplatform/samples/tree/main/python)。