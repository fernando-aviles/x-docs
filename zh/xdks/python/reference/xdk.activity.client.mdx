---
title: "ActivityClient"
sidebarTitle: "ActivityClient"
---

该模块提供用于与 X API 的 activity 端点交互的客户端。
实时流式操作会返回生成器，在数据到达时依次产出数据。
流式连接会自动管理，并使用指数退避重试逻辑，以实现健壮的错误处理。

<div id="activityclient">
  ## ActivityClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于处理活动操作的流式客户端

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_subscription">
  ### `create_subscription`
</div>

创建 X 活动订阅
为 X 活动事件创建订阅
body: 请求正文
:returns: 响应 data
:rtype: CreateSubscriptionResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="CreateSubscriptionRequest"></ParamField>

<div id="delete_subscription">
  ### `delete_subscription`
</div>

删除 X 活动订阅
删除针对 X 活动事件的订阅

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.subscription_id" type="Any">
要删除的订阅 id。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`DeleteSubscriptionResponse` - 响应数据

<div id="get_subscriptions">
  ### `get_subscriptions`
</div>

获取 X 活动的订阅
获取 XAA 的当前有效订阅列表
:returns: 响应 data
:rtype: GetSubscriptionsResponse

<div id="returns">
  #### 返回值
</div>

`GetSubscriptionsResponse`

<div id="stream">
  ### `stream`
</div>

活动流（流式传输）
X 活动流
这是一个流式端点，会在有数据可用时实时输出 data。
每个输出的项代表流中的一个数据点。
连接由指数退避重试逻辑自动管理。
如果流断开，SDK 会在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
请求回补的分钟数。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。从该最早的 UTC 时间戳开始提供帖子标签。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。截至该最晚的 UTC 时间戳提供帖子标签。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
可选的 StreamConfig，用于自定义重试行为、超时和回调。
</ParamField>

<div id="update_subscription">
  ### `update_subscription`
</div>

更新 X 活动订阅
更新针对 X 活动事件的订阅

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.subscription_id" type="Any">
要更新的订阅 ID。
</ParamField>

<ParamField path="body.body" type="UpdateSubscriptionRequest">
请求体
</ParamField>