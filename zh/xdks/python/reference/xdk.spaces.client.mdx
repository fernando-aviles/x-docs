---
title: "SpacesClient"
sidebarTitle: "SpacesClient"
---

<div id="spacesclient">
  ## SpacesClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于执行与 Spaces 相关操作的 Client

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 参数

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_buyers">
  ### `get_buyers`
</div>

获取 Space 门票购买用户
根据 Space 的 ID，获取购买该 Space 门票的用户列表。

#### 参数

<ParamField path="path.id" type="str">
要检索的 Space 的 ID。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定结果“页”的数据。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的用户字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetBuyersResponse`

<div id="get_by_creator_ids">
  ### `get_by_creator_ids`
</div>

根据创建者 id 获取 Spaces
检索由指定用户 id 创建的 Spaces 的详细信息。

#### 参数

<ParamField path="path.user_ids" type="List">
要搜索的用户 ID。
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
以逗号分隔的 Space 字段列表，用于显示。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的用户字段列表，用于显示。
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
以逗号分隔的 Topic 字段列表，用于显示。
</ParamField>

#### 返回

`GetByCreatorIdsResponse` - 响应数据

<div id="get_by_id">
  ### `get_by_id`
</div>

根据 ID 获取 Space
根据其 ID 获取指定 Space 的详细信息。

#### 参数

<ParamField path="path.id" type="str">
要检索的 Space 的 ID。
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
以逗号分隔的 Space 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的 User 字段列表。
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
以逗号分隔的 Topic 字段列表。
</ParamField>

#### 返回

`GetByIdResponse` - 响应数据

<div id="get_by_ids">
  ### `get_by_ids`
</div>

通过 id 获取多个 Space
根据多个 Space 的 id 检索其详细信息。

#### 参数

<ParamField path="path.ids" type="List">
要返回的 Space ID 列表。
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
以逗号分隔的要显示的 Space 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的 User 字段列表。
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
以逗号分隔的要显示的 Topic 字段列表。
</ParamField>

#### 返回

`GetByIdsResponse` - 响应数据

<div id="get_posts">
  ### `get_posts`
</div>

获取 Space 帖子
通过 Space 的 id，检索在特定 Space 中分享的帖子列表。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="str">
要检索的 Space 的 id。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
要从指定 Space 获取的帖子数。如果未提供，该值将默认为最大允许值 100。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要返回的 Tweet 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要返回的 Media 字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要返回的 Poll 字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要返回的 User 字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要返回的 Place 字段列表。
</ParamField>

#### 返回

`GetPostsResponse` - 响应数据

<div id="search">
  ### `search`
</div>

搜索 Spaces
获取与指定搜索查询匹配的 Spaces 列表。

#### 参数

<ParamField path="path.query" type="str">
搜索查询字符串。
</ParamField>

<ParamField path="path.state" type="str or None" default="None">
要搜索的 Space 的状态。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
要返回的结果数量。
</ParamField>

<ParamField path="path.space_fields" type="List or None" default="None">
以逗号分隔的 Space 字段列表，用于返回。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的 User 字段列表，用于返回。
</ParamField>

<ParamField path="path.topic_fields" type="List or None" default="None">
以逗号分隔的 Topic 字段列表，用于返回。
</ParamField>

#### 返回

`SearchResponse` - 响应数据