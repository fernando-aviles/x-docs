---
title: "CommunitiesClient"
sidebarTitle: "CommunitiesClient"
---

<div id="communitiesclient">
  ## CommunitiesClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于执行社区相关操作的客户端

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_by_id">
  ### `get_by_id`
</div>

按 ID 获取社区
检索具有指定 ID 的特定社区的详细信息。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
Community 的 ID。
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
用逗号分隔的要显示的 Community 字段列表。
</ParamField>

<div id="returns">
  #### 返回
</div>

`GetByIdResponse` - 响应数据

<div id="search">
  ### `search`
</div>

搜索社群
获取与指定搜索查询匹配的社群列表。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.query" type="str">
用于搜索社区的查询字符串。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
单个请求可返回的搜索结果最大数量。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一“页”的结果。与该参数一起使用的值直接来自 API 返回的响应，不应修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”的结果。与该参数一起使用的值直接来自 API 返回的响应，不应修改。
</ParamField>

<ParamField path="path.community_fields" type="List or None" default="None">
一个以逗号分隔的 Community 字段列表，用于指定要返回的 Community 字段。
</ParamField>

<div id="returns">
  #### 返回
</div>

`IteratorSearchResponse`