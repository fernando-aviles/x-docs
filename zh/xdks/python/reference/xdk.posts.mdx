---
title: "PostsClient"
sidebarTitle: "PostsClient"
---

<div id="submodules">
  ## 子模块
</div>

* [xdk.posts.client 模块](/zh/xdks/python/reference/xdk.posts.client)
  * [`PostsClient`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient)
    * [`PostsClient.__init__()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.__init__)
    * [`PostsClient.create()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.create)
    * [`PostsClient.delete()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.delete)
    * [`PostsClient.get_analytics()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_analytics)
    * [`PostsClient.get_by_id()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_by_id)
    * [`PostsClient.get_by_ids()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_by_ids)
    * [`PostsClient.get_counts_all()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_counts_all)
    * [`PostsClient.get_counts_recent()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_counts_recent)
    * [`PostsClient.get_insights28hr()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_insights28hr)
    * [`PostsClient.get_insights_historical()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_insights_historical)
    * [`PostsClient.get_liking_users()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_liking_users)
    * [`PostsClient.get_quoted()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_quoted)
    * [`PostsClient.get_reposted_by()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_reposted_by)
    * [`PostsClient.get_reposts()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.get_reposts)
    * [`PostsClient.hide_reply()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.hide_reply)
    * [`PostsClient.search_all()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.search_all)
    * [`PostsClient.search_recent()`](/zh/xdks/python/reference/xdk.posts.client#xdk.posts.client.PostsClient.search_recent)
* [xdk.posts.models 模块](/zh/xdks/python/reference/xdk.posts.models)
  * [`CreateRequest`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateRequest)
    * [`CreateRequest.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateRequest.model_config)
  * [`CreateResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateResponse)
    * [`CreateResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.CreateResponse.model_config)
  * [`DeleteResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.DeleteResponse)
    * [`DeleteResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.DeleteResponse.model_config)
  * [`GetAnalyticsResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetAnalyticsResponse)
    * [`GetAnalyticsResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetAnalyticsResponse.model_config)
  * [`GetByIdResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdResponse)
    * [`GetByIdResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdResponse.model_config)
  * [`GetByIdsResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdsResponse)
    * [`GetByIdsResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetByIdsResponse.model_config)
  * [`GetCountsAllResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsAllResponse)
    * [`GetCountsAllResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsAllResponse.model_config)
  * [`GetCountsRecentResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsRecentResponse)
    * [`GetCountsRecentResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetCountsRecentResponse.model_config)
  * [`GetInsights28hrResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsights28hrResponse)
    * [`GetInsights28hrResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsights28hrResponse.model_config)
  * [`GetInsightsHistoricalResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsightsHistoricalResponse)
    * [`GetInsightsHistoricalResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetInsightsHistoricalResponse.model_config)
  * [`GetLikingUsersResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetLikingUsersResponse)
    * [`GetLikingUsersResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetLikingUsersResponse.model_config)
  * [`GetQuotedResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetQuotedResponse)
    * [`GetQuotedResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetQuotedResponse.model_config)
  * [`GetRepostedByResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostedByResponse)
    * [`GetRepostedByResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostedByResponse.model_config)
  * [`GetRepostsResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostsResponse)
    * [`GetRepostsResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.GetRepostsResponse.model_config)
  * [`HideReplyRequest`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyRequest)
    * [`HideReplyRequest.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyRequest.model_config)
  * [`HideReplyResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyResponse)
    * [`HideReplyResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.HideReplyResponse.model_config)
  * [`SearchAllResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchAllResponse)
    * [`SearchAllResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchAllResponse.model_config)
  * [`SearchRecentResponse`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchRecentResponse)
    * [`SearchRecentResponse.model_config`](/zh/xdks/python/reference/xdk.posts.models#xdk.posts.models.SearchRecentResponse.model_config)

<div id="module-contents">
  ## 模块内容
</div>

本模块用于访问 X API 的帖子端点，是所有帖子相关功能的主要入口。

<div id="class-xdkpostspostsclient">
  ### `class xdk.posts.PostsClient`
</div>

用于帖子相关操作的客户端

#### 参数

<ParamField path="path.client" type="Client" />

<div id="__init__">
  ### `__init__`
</div>

#### 参数

<ParamField path="path.client" type="Client" />

<div id="create">
  ### `create`
</div>

创建或编辑帖子
为已通过认证的用户创建一个新的帖子，或者在提供 `edit_options` 时编辑现有帖子。
body: 请求体
:returns: 响应数据
:rtype: CreateResponse

#### 参数

<ParamField path="body.body" type="CreateRequest" />

<div id="delete">
  ### `delete`
</div>

删除帖子
如果帖子归当前已认证用户所有，则根据其 id 删除该帖子。

#### 参数

<ParamField path="path.id" type="Any">
  要删除的帖子 ID。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`DeleteResponse` - 响应数据

<div id="get_analytics">
  ### `get_analytics`
</div>

获取帖子分析数据
在指定时间范围内检索特定帖子的分析数据。

#### 参数

<ParamField path="path.ids" type="List">
  以逗号分隔的帖子 id 列表。单个请求中最多可指定 100 个。
</ParamField>

<ParamField path="path.end_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ。表示时间范围结束时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.start_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ。表示时间范围开始时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.granularity" type="str">
  搜索计数结果的粒度。
</ParamField>

<ParamField path="path.analytics_fields" type="List or None" default="None">
  以逗号分隔的要显示的分析字段列表。
</ParamField>

#### 返回

`GetAnalyticsResponse` - 响应数据

<div id="get_by_id">
  ### `get_by_id`
</div>

通过 ID 获取帖子
根据其 ID 检索特定帖子的详细信息。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
  单个帖子 ID。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  以逗号分隔的推文字段列表，要返回的字段。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  以逗号分隔的字段列表，要展开的字段。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  以逗号分隔的 Media 字段列表，要返回的字段。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  以逗号分隔的 Poll 字段列表，要返回的字段。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  以逗号分隔的 User 字段列表，要返回的字段。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  以逗号分隔的 Place 字段列表，要返回的字段。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetByIdResponse` - 响应数据

<div id="get_by_ids">
  ### `get_by_ids`
</div>

按 ID 获取帖子
通过多个帖子各自的 ID 检索详细信息。

#### 参数

<ParamField path="path.ids" type="List">
  以逗号分隔的帖子 ID 列表。单个请求最多允许 100 个。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  以逗号分隔的推文字段列表，用于指定要显示的字段。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  以逗号分隔的、要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  以逗号分隔的媒体字段列表，用于指定要显示的字段。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  以逗号分隔的投票字段列表，用于指定要显示的字段。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  以逗号分隔的用户字段列表，用于指定要显示的字段。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  以逗号分隔的地点字段列表，用于指定要显示的字段。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetByIdsResponse` - 响应数据

<div id="get_counts_all">
  ### `get_counts_all`
</div>

获取所有帖子计数
从完整归档中检索符合搜索查询条件的帖子数量。

#### 参数

<ParamField path="path.query" type="str">
  用于匹配帖子的单个查询/规则/过滤条件。请参考 [https://t.co/rulelength](https://t.co/rulelength) 了解最大查询长度。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。从该时间戳起（最近 7 天内的最早 UTC 时间戳）开始返回帖子。时间戳粒度为秒，且为包含关系（即设置为 12:00:01 时，会包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。返回帖子截至的最新 UTC 时间戳。时间戳粒度为秒，且为不包含关系（即设置为 12:00:01 时，不会包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  返回帖子 ID 大于（即比其更新于）指定 ID 的结果。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  返回帖子 ID 小于（即比其更早于）指定 ID 的结果。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  此参数用于获取下一“页”的结果。该参数所使用的值直接取自 API 提供的响应，不应进行修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  此参数用于获取下一“页”的结果。该参数所使用的值直接取自 API 提供的响应，不应进行修改。
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
  搜索计数结果的粒度。
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
  以逗号分隔的要显示的 SearchCount 字段列表。
</ParamField>

#### 返回

`IteratorGetCountsAllResponse`

<div id="get_counts_recent">
  ### `get_counts_recent`
</div>

获取近期帖子计数
检索过去 7 天内符合搜索查询的帖子数量。

#### 参数

<ParamField path="path.query" type="str">
  用于匹配帖子的单个查询/规则/过滤条件。请参阅 [https://t.co/rulelength](https://t.co/rulelength) 以了解最大查询长度。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子时所使用的最早 UTC 时间戳（从最近 7 天内选取）。时间戳精度为秒，并且是包含性的（即 12:00:01 包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子时所使用的最新（最近）UTC 时间戳。时间戳精度为秒，并且是排除性的（即 12:00:01 排除该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  返回帖子 ID 大于（即比其更新）指定 ID 的结果。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  返回帖子 ID 小于（即比其更早）指定 ID 的结果。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  此参数用于获取下一“页”的结果。与该参数一起使用的值直接取自 API 提供的响应，不应修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  此参数用于获取下一“页”的结果。与该参数一起使用的值直接取自 API 提供的响应，不应修改。
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
  搜索计数结果的粒度。
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
  以逗号分隔的要显示的 SearchCount 字段列表。
</ParamField>

#### 返回

`IteratorGetCountsRecentResponse`

<div id="get_insights28hr">
  ### `get_insights28hr`
</div>

获取过去 28 小时的帖子洞察
获取指定帖子在过去 28 小时内的互动指标。

#### 参数

<ParamField path="path.tweet_ids" type="List">
  用于 28 小时指标的帖子 ID 列表。
</ParamField>

<ParamField path="path.granularity" type="str">
  指标响应的粒度。
</ParamField>

<ParamField path="path.requested_metrics" type="List">
  用于历史请求的指标列表。
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
  要显示的、以逗号分隔的互动字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetInsights28hrResponse` - 响应数据

<div id="get_insights_historical">
  ### `get_insights_historical`
</div>

获取帖子历史洞察
在指定的时间范围内检索特定帖子在历史期间的互动指标。

#### 参数

<ParamField path="path.tweet_ids" type="List">
  用于历史指标的帖子 id 列表。
</ParamField>

<ParamField path="path.end_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ。表示时间范围结束时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.start_time" type="str">
  YYYY-MM-DDTHH:mm:ssZ。表示时间范围开始时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.granularity" type="str">
  指标响应的粒度级别。
</ParamField>

<ParamField path="path.requested_metrics" type="List">
  要请求的历史指标。
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
  要显示的参与度字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetInsightsHistoricalResponse` - 响应数据

<div id="get_liking_users">
  ### `get_liking_users`
</div>

获取点赞用户
根据帖子 ID 检索点赞该特定帖子的用户列表。

#### 参数

<ParamField path="path.id" type="Any">
  单个帖子 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  以逗号分隔的用户字段列表，用于展示。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  以逗号分隔的推文字段列表，用于展示。
</ParamField>

#### 返回

`IteratorGetLikingUsersResponse`

<div id="get_quoted">
  ### `get_quoted`
</div>

获取引用的帖子
检索引用某条指定 ID 帖子的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
  单个帖子 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  要返回的结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  此参数用于获取指定的一页结果。
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
  要排除的实体集合（例如 “replies” 或 “retweets”）。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  以逗号分隔的 Tweet 字段列表，用于指定要返回的字段。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  以逗号分隔的 Media 字段列表，用于指定要返回的字段。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  以逗号分隔的 Poll 字段列表，用于指定要返回的字段。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  以逗号分隔的 User 字段列表，用于指定要返回的字段。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  以逗号分隔的 Place 字段列表，用于指定要返回的字段。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetQuotedResponse`

<div id="get_reposted_by">
  ### `get_reposted_by`
</div>

获取转发用户
根据帖子 ID 获取转发了特定帖子的用户列表。

#### 参数

<ParamField path="path.id" type="Any">
  单个帖子 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  以逗号分隔的要显示的 User 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  以逗号分隔的要显示的 Tweet 字段列表。
</ParamField>

#### 返回

`IteratorGetRepostedByResponse`

<div id="get_reposts">
  ### `get_reposts`
</div>

获取转发帖子
检索转发指定 id 的某个帖子的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
  单个帖子 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  要显示的推文字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  要扩展的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  要显示的 Media 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  要显示的 Poll 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  要显示的 User 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  要显示的 Place 字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetRepostsResponse`

<div id="hide_reply">
  ### `hide_reply`
</div>

隐藏回复
隐藏或取消隐藏经过身份验证用户所拥有会话中的某条回复。

#### 参数

<ParamField path="path.tweet_id" type="Any">
  你要隐藏或取消隐藏的回复的 ID。
</ParamField>

<ParamField path="body.body" type="HideReplyRequest">
  请求正文
</ParamField>

<div id="search_all">
  ### `search_all`
</div>

搜索所有帖子
从全量存档中检索与搜索查询匹配的帖子。

#### 参数

<ParamField path="path.query" type="str">
  用于匹配帖子的单条查询、规则或过滤条件。请参阅 [https://t.co/rulelength](https://t.co/rulelength) 了解查询长度上限。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子时所使用的最早 UTC 时间戳。时间戳精度为秒，且为包含边界（即 12:00:01 包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。提供帖子时所使用的最新（最近）UTC 时间戳。时间戳精度为秒，且为不包含边界（即 12:00:01 不包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  返回帖子 ID 大于（即比其更新）指定 ID 的结果。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  返回帖子 ID 小于（即比其更早）指定 ID 的结果。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  单个请求可返回的最大搜索结果数。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  此参数用于获取下一“页”结果。该参数的值直接来自 API 提供的响应，不应进行修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  此参数用于获取下一“页”结果。该参数的值直接来自 API 提供的响应，不应进行修改。
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
  返回结果的排序方式。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  以逗号分隔的要展示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  以逗号分隔的要展示的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  以逗号分隔的要展示的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  以逗号分隔的要展示的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  以逗号分隔的要展示的地点字段列表。
</ParamField>

#### 返回

`IteratorSearchAllResponse`

<div id="search_recent">
  ### `search_recent`
</div>

搜索最近的帖子
检索过去 7 天内与搜索查询相匹配的帖子。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="str">
  用于匹配帖子的单个查询/规则/过滤条件。请参阅 [https://t.co/rulelength](https://t.co/rulelength) 了解查询长度上限。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。返回的帖子不会早于此最早的 UTC 时间戳。时间戳的粒度为秒，且为包含关系（即 12:00:01 包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
  YYYY-MM-DDTHH:mm:ssZ。返回的帖子不会晚于此最新（最近）的 UTC 时间戳。时间戳的粒度为秒，且为不包含关系（即 12:00:01 不包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
  返回帖子 ID 大于（即比其更新于）指定 ID 的结果。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
  返回帖子 ID 小于（即比其更早于）指定 ID 的结果。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
  单个请求可返回的搜索结果最大数量。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
  此参数用于获取下一“页”结果。与该参数一起使用的值直接来自 API 提供的响应，不应修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
  此参数用于获取下一“页”结果。与该参数一起使用的值直接来自 API 提供的响应，不应修改。
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
  返回结果的排列顺序。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
  以逗号分隔的要显示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
  以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
  以逗号分隔的要显示的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
  以逗号分隔的要显示的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
  以逗号分隔的要显示的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
  以逗号分隔的要显示的地点字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorSearchRecentResponse`