---
title: "AccountActivityClient"
sidebarTitle: "AccountActivityClient"
---

<div id="accountactivityclient">
  ## AccountActivityClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于帐户活动相关操作的客户端

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_replay_job">
  ### `create_replay_job`
</div>

创建重放作业
创建一个重放作业，用于检索与指定 webhook 关联的所有订阅在过去最多 5 天内的活动。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.webhook_id" type="Any">
webhook 配置的唯一标识符。
</ParamField>

<ParamField path="path.from_date" type="str">
要提供事件的起始（最早）UTC 时间戳（含），格式为 yyyymmddhhmm。
</ParamField>

<ParamField path="path.to_date" type="str">
要提供事件的结束（最晚）UTC 时间戳（不含），格式为 yyyymmddhhmm。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`CreateReplayJobResponse` - 响应数据

<div id="create_subscription">
  ### `create_subscription`
</div>

创建订阅
为指定的用户和 webhook 创建一个 Account Activity 订阅。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.webhook_id" type="Any">
要检查其订阅状态的 webhook ID。
</ParamField>

<ParamField path="body.body" type="CreateSubscriptionRequest">
请求正文
</ParamField>

<div id="delete_subscription">
  ### `delete_subscription`
</div>

删除订阅
删除指定 webhook 和用户 ID 的 Account Activity 订阅。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.webhook_id" type="Any">
要检查其订阅状态的 webhook ID。
</ParamField>

<ParamField path="path.user_id" type="Any">
要取消订阅的用户 ID。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`DeleteSubscriptionResponse` - 响应数据

<div id="get_subscription_count">
  ### `get_subscription_count`
</div>

获取订阅数量
检索当前处于活动状态的 Account Activity 订阅数量。
:returns: 响应数据
:rtype: GetSubscriptionCountResponse

<div id="returns">
  #### 返回值
</div>

`GetSubscriptionCountResponse`

<div id="get_subscriptions">
  ### `get_subscriptions`
</div>

获取订阅
检索指定 webhook 的所有活动订阅列表。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.webhook_id" type="Any">
用于获取订阅信息的 webhook id。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetSubscriptionsResponse` - 响应数据

<div id="validate_subscription">
  ### `validate_subscription`
</div>

验证订阅
检查指定 webhook 下某个用户的 Account Activity 订阅。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.webhook_id" type="Any">
用于检查订阅的 webhook id。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`ValidateSubscriptionResponse` - 响应数据