---
title: "MediaClient"
sidebarTitle: "MediaClient"
---

<div id="mediaclient">
  ## MediaClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

媒体操作客户端

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="append_upload">
  ### `append_upload`
</div>

追加媒体上传数据
将 data 追加到媒体上传请求中。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
用于执行追加操作的媒体标识符。
</ParamField>

<ParamField path="body.body" type="AppendUploadRequest">
请求体
</ParamField>

<div id="create_metadata">
  ### `create_metadata`
</div>

创建 Media 的元数据
为 Media 文件创建元数据。
body: 请求正文
:returns: 响应数据
:rtype: CreateMetadataResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="CreateMetadataRequest"></ParamField>

<div id="create_subtitles">
  ### `create_subtitles`
</div>

创建媒体字幕
为指定的媒体文件创建字幕。
body: 请求体
:returns: 响应数据
:rtype: CreateSubtitlesResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="CreateSubtitlesRequest"></ParamField>

<div id="delete_subtitles">
  ### `delete_subtitles`
</div>

删除 Media 的字幕
删除指定 Media 文件的字幕。
body: 请求体
:returns: 响应数据
:rtype: DeleteSubtitlesResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="DeleteSubtitlesRequest"></ParamField>

<div id="finalize_upload">
  ### `finalize_upload`
</div>

完成媒体上传
完成一条媒体上传请求。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
要完成上传操作的目标媒体的 media id。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`FinalizeUploadResponse` - 响应数据

<div id="get_analytics">
  ### `get_analytics`
</div>

获取媒体分析数据
检索媒体的分析数据。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.media_keys" type="List">
以逗号分隔的 Media Key 列表。单个请求中最多可提供 100 个。
</ParamField>

<ParamField path="path.end_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。表示时间范围结束时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.start_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。表示时间范围开始时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.granularity" type="str">
搜索计数结果的粒度。
</ParamField>

<ParamField path="path.media_analytics_fields" type="List or None" default="None">
以逗号分隔的 MediaAnalytics 字段列表，用于显示。
</ParamField>

#### 返回

`GetAnalyticsResponse` - 响应数据

<div id="get_by_key">
  ### `get_by_key`
</div>

通过 media key 获取 Media
检索指定 media key 对应的 Media 文件详情。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.media_key" type="Any">
单个 Media Key。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
要显示的、以逗号分隔的 Media 字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetByKeyResponse` - 响应数据

<div id="get_by_keys">
  ### `get_by_keys`
</div>

通过 media key 获取 Media
根据 media key 获取 Media 文件的详细信息。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.media_keys" type="List">
以逗号分隔的 Media Key 列表。单个请求最多可包含 100 个。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要返回的 Media 字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetByKeysResponse` - 响应数据

<div id="get_upload_status">
  ### `get_upload_status`
</div>

获取媒体上传的状态
根据媒体上传的 ID 检索其状态。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.media_id" type="Any">
用于查询所请求媒体上传状态的媒体 id。
</ParamField>

<ParamField path="path.command" type="str or None" default="None">
媒体上传请求所使用的命令。
</ParamField>

#### 返回

`GetUploadStatusResponse` - 响应数据

<div id="initialize_upload">
  ### `initialize_upload`
</div>

初始化媒体上传
初始化媒体上传。
body: 请求正文
:returns: 响应数据
:rtype: InitializeUploadResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="InitializeUploadRequest"></ParamField>

<div id="upload">
  ### `upload`
</div>

上传媒体
上传媒体文件，以便在帖子或其他内容中使用。
body: 请求体
:returns: 响应 data
:rtype: UploadResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="UploadRequest"></ParamField>