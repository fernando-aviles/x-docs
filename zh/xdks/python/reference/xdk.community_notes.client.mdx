---
title: "CommunityNotesClient"
sidebarTitle: "CommunityNotesClient"
---

<div id="communitynotesclient">
  ## CommunityNotesClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于处理 Community Notes 相关操作的客户端

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

创建 Community Note
为 LLM 用例创建 Community Note 端点。
body: 请求正文
:returns: 响应数据
:rtype: CreateResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

删除社区注释
删除一条社区注释。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
要删除的社区注释 ID。
</ParamField>

#### 返回

`DeleteResponse` - 响应 data

<div id="evaluate">
  ### `evaluate`
</div>

评估一条 Community Note（社区注释）
用于评估 Community Note 的端点。
body: 请求体
:returns: 响应 data
:rtype: EvaluateResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="EvaluateRequest"></ParamField>

<div id="search_eligible_posts">
  ### `search_eligible_posts`
</div>

搜索有资格获得社区注释的帖子  
返回所有有资格获得社区注释的帖子。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.test_mode" type="bool">
如果为 true，返回用于测试的帖子列表；如果为 false，返回机器人可以就该产品撰写建议注释的帖子列表。
</ParamField>

<ParamField path="path.pagination_token" type="str or None" default="None">
用于获取下一批符合注释条件帖子的分页令牌。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
要返回的最大结果数。
</ParamField>

<ParamField path="path.post_selection" type="str or None" default="None">
要返回的帖子集合。有效值为 ‘feed_size: small’ 和 ‘feed_size: large’。默认值为 ‘feed_size: small’，只有顶级 AI 写作者可以访问大尺寸信息流。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的 Tweet 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要显示的 Media 字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要显示的 Poll 字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的 User 字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要显示的 Place 字段列表。
</ParamField>

#### 返回

`IteratorSearchEligiblePostsResponse`

<div id="search_written">
  ### `search_written`
</div>

搜索用户撰写的 Community Notes
返回该用户撰写的所有 Community Notes。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.test_mode" type="bool">
如果为 true，则返回调用方为测试编写的备注；如果为 false，则返回调用方在产品上编写的备注。
</ParamField>

<ParamField path="path.pagination_token" type="str or None" default="None">
用于获取下一组可添加备注的帖子集合的分页令牌。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
要返回的最大结果数量。
</ParamField>

<ParamField path="path.note_fields" type="List or None" default="None">
以逗号分隔的要显示的 Note 字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorSearchWrittenResponse`