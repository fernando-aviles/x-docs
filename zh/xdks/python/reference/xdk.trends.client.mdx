---
title: "TrendsClient"
sidebarTitle: "TrendsClient"
---

<div id="trendsclient">
  ## TrendsClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于趋势操作的客户端

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_ai">
  ### `get_ai`
</div>

通过 id 获取 AI 趋势
根据其 id 检索一个 AI 趋势。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
AI 趋势的 id。
</ParamField>

<ParamField path="path.news_fields" type="List or None" default="None">
以逗号分隔的用于显示的 News 字段列表。
</ParamField>

#### 返回

`GetAiResponse` - 响应数据

<div id="get_by_woeid">
  ### `get_by_woeid`
</div>

通过 WOEID 获取趋势
获取由其 WOEID 标识的特定位置的趋势话题。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.woeid" type="int">
要查询趋势的地点的 WOEID。
</ParamField>

<ParamField path="path.max_trends" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.trend_fields" type="List or None" default="None">
以逗号分隔的 Trend 字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetByWoeidResponse` - 响应数据

<div id="get_personalized">
  ### `get_personalized`
</div>

获取个性化趋势
为经过身份验证的用户返回个性化的趋势话题。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.personalized_trend_fields" type="List or None" default="None">
以逗号分隔的 PersonalizedTrend 字段列表，用于指定要显示的字段。
</ParamField>

#### 返回

`GetPersonalizedResponse` - 响应数据