---
title: "PostsClient"
sidebarTitle: "PostsClient"
---

<div id="postsclient">
  ## PostsClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于帖子相关操作的客户端

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 参数

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

创建或编辑帖子
为已通过认证的用户创建新帖子，或者在提供 `edit_options` 时编辑现有帖子。
body: 请求体
:returns: 响应数据
:rtype: CreateResponse

#### 参数

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

删除帖子
删除由已认证用户拥有的指定帖子（通过其 ID）。

#### 参数

<ParamField path="path.id" type="Any">
要删除的帖子 ID。
</ParamField>

#### 返回

`DeleteResponse` - 响应数据

<div id="get_analytics">
  ### `get_analytics`
</div>

获取帖子分析数据
在指定的时间范围内检索指定帖子的分析数据。

#### 参数

<ParamField path="path.ids" type="List">
以逗号分隔的帖子 id 列表。单个请求最多允许 100 个 id。
</ParamField>

<ParamField path="path.end_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。表示时间范围结束时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.start_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。表示时间范围开始时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.granularity" type="str">
搜索计数结果的粒度。
</ParamField>

<ParamField path="path.analytics_fields" type="List or None" default="None">
要显示的 Analytics 字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetAnalyticsResponse` - 响应数据

<div id="get_by_id">
  ### `get_by_id`
</div>

通过 ID 获取帖子
检索具有指定帖子 ID 的帖子的详细信息。

#### 参数

<ParamField path="path.id" type="Any">
单个帖子 ID。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的推文字段的以逗号分隔的列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要扩展的字段的以逗号分隔的列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
要显示的媒体字段的以逗号分隔的列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
要显示的投票字段的以逗号分隔的列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的用户字段的以逗号分隔的列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
要显示的地点字段的以逗号分隔的列表。
</ParamField>

#### 返回

`GetByIdResponse` - 响应数据

<div id="get_by_ids">
  ### `get_by_ids`
</div>

根据 ID 获取帖子
根据多个 ID 获取多个帖子的详细信息。

#### 参数

<ParamField path="path.ids" type="List">
以逗号分隔的帖子 ID 列表。单个请求最多允许 100 个。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要显示的 Media 字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要显示的 Poll 字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的 User 字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要显示的 Place 字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetByIdsResponse` - 响应数据

<div id="get_counts_all">
  ### `get_counts_all`
</div>

获取所有帖子数量
从完整存档中检索与搜索查询匹配的帖子数量。

#### 参数

<ParamField path="path.query" type="str">
一个用于匹配帖子的查询/规则/过滤条件。请参阅 [https://t.co/rulelength](https://t.co/rulelength) 以了解查询长度上限。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子时所使用的最早 UTC 时间戳（从最近 7 天内计算）。时间戳以秒为粒度且为包含（即 12:00:01 包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子时所使用的最新 UTC 时间戳。时间戳以秒为粒度且为不包含（即 12:00:01 不包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
返回帖子 ID 大于（即比其更新）指定 ID 的结果。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
返回帖子 ID 小于（即比其更早）指定 ID 的结果。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。与该参数一起使用的值直接取自 API 提供的响应，不应修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。与该参数一起使用的值直接取自 API 提供的响应，不应修改。
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
搜索计数结果的粒度。
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
以逗号分隔的 SearchCount 字段列表，用于显示。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetCountsAllResponse`

<div id="get_counts_recent">
  ### `get_counts_recent`
</div>

获取近期帖子数量统计
检索过去 7 天内与搜索查询匹配的帖子数量。

#### 参数

<ParamField path="path.query" type="str">
用于匹配帖子的单个查询/规则/过滤条件。请参阅 [https://t.co/rulelength](https://t.co/rulelength) 以了解查询长度上限。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子数据时所使用的最早 UTC 时间戳（限最近 7 天内）。时间戳精度为秒，并且是包含性的（即 12:00:01 包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子数据时所使用的最新（最近）的 UTC 时间戳。时间戳精度为秒，并且是排他性的（即 12:00:01 不包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
返回帖子 ID 大于指定 ID 的结果（即，比该 ID 更新的帖子）。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
返回帖子 ID 小于指定 ID 的结果（即，比该 ID 更早的帖子）。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一“页”的结果。该参数所使用的值直接取自 API 提供的响应，不应进行修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”的结果。该参数所使用的值直接取自 API 提供的响应，不应进行修改。
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
搜索计数结果的粒度。
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
以逗号分隔的 SearchCount 字段列表，用于显示。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetCountsRecentResponse`

<div id="get_insights28hr">
  ### `get_insights28hr`
</div>

获取过去 28 小时内的帖子洞察  
检索指定帖子在过去 28 小时内的互动指标。

#### 参数

<ParamField path="path.tweet_ids" type="List">
用于 28 小时指标的 PostId 列表。
</ParamField>

<ParamField path="path.granularity" type="str">
指标响应的粒度。
</ParamField>

<ParamField path="path.requested_metrics" type="List">
用于历史请求的指标。
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
要显示的、以逗号分隔的参与度字段列表。
</ParamField>

#### 返回

`GetInsights28hrResponse` - 响应数据

<div id="get_insights_historical">
  ### `get_insights_historical`
</div>

获取历史帖子洞察数据
在指定的时间范围内检索特定帖子对应的历史互动指标。

#### 参数

<ParamField path="path.tweet_ids" type="List">
用于历史指标的帖子 id 列表。
</ParamField>

<ParamField path="path.end_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。表示时间范围结束时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.start_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。表示时间范围开始时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.granularity" type="str">
指标响应的粒度。
</ParamField>

<ParamField path="path.requested_metrics" type="List">
历史请求中要获取的指标。
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
要显示的以逗号分隔的互动字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetInsightsHistoricalResponse` - 响应数据

<div id="get_liking_users">
  ### `get_liking_users`
</div>

获取点赞用户
根据帖子的 id，检索点赞该帖子的用户列表。

#### 参数

<ParamField path="path.id" type="Any">
单个帖子 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
最大结果数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的用户字段列表，用于显示。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表，用于显示。
</ParamField>

#### 返回

`IteratorGetLikingUsersResponse`

<div id="get_quoted">
  ### `get_quoted`
</div>

获取引用帖子的帖子
检索引用指定帖子（通过其 ID 标识）的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
单个帖子 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
要返回的最大结果数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页”的结果。
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
要排除的实体集合（例如“replies”或“retweets”）。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的 Media 字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的 Poll 字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的 User 字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的 Place 字段列表。
</ParamField>

#### 返回

`IteratorGetQuotedResponse`

<div id="get_reposted_by">
  ### `get_reposted_by`
</div>

获取转发用户
根据其 id 检索转发了特定帖子的一组用户。

#### 参数

<ParamField path="path.id" type="Any">
单个帖子 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
用于获取下一“页”结果的参数。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要展示的 User 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要展示的 Tweet 字段列表。
</ParamField>

#### 返回

`IteratorGetRepostedByResponse`

<div id="get_reposts">
  ### `get_reposts`
</div>

获取转发
检索转发由其 id 指定的某条帖子的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
单个帖子 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的推文字段的以逗号分隔的列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要扩展的字段的以逗号分隔的列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
要显示的媒体字段的以逗号分隔的列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
要显示的投票字段的以逗号分隔的列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的用户字段的以逗号分隔的列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
要显示的位置字段的以逗号分隔的列表。
</ParamField>

#### 返回

`IteratorGetRepostsResponse`

<div id="hide_reply">
  ### `hide_reply`
</div>

隐藏回复
对经过身份验证用户所拥有的会话中的某条回复进行隐藏或取消隐藏。

#### 参数

<ParamField path="path.tweet_id" type="Any">
要隐藏或取消隐藏的回复的 ID。
</ParamField>

<ParamField path="body.body" type="HideReplyRequest">
请求正文
</ParamField>

<div id="search_all">
  ### `search_all`
</div>

搜索所有帖子
从完整存档中检索符合搜索查询条件的帖子。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="str">
用于匹配帖子的一个查询/规则/过滤器。请参阅 [https://t.co/rulelength](https://t.co/rulelength) 以了解查询长度上限。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。自该最早 UTC 时间戳起返回帖子。时间戳为秒级精度且为包含关系（即 12:00:01 包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。截至该最新、最近的 UTC 时间戳返回帖子。时间戳为秒级精度且为不包含关系（即 12:00:01 不包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
返回帖子 ID 大于（即比其更新）指定 ID 的结果。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
返回帖子 ID 小于（即比其更早）指定 ID 的结果。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
单个请求可以返回的最大搜索结果数量。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一页结果。与该参数一起使用的值直接取自 API 返回的响应，不应进行修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页结果。与该参数一起使用的值直接取自 API 返回的响应，不应进行修改。
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
返回结果的排序方式。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要显示的 Media 字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要显示的 Poll 字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的 User 字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要显示的 Place 字段列表。
</ParamField>

#### 返回

`IteratorSearchAllResponse`

<div id="search_recent">
  ### `search_recent`
</div>

搜索最近的帖子
检索过去 7 天内符合搜索查询的帖子。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="str">
一个用于匹配帖子的查询/规则/过滤条件。请参阅 [https://t.co/rulelength](https://t.co/rulelength) 了解最大查询长度。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子时所依据的最早 UTC 时间戳。时间戳精度为秒，且为包含关系（即 12:00:01 包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子时所依据的最新、最近的 UTC 时间戳。时间戳精度为秒，且为不包含关系（即 12:00:01 不包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
返回帖子 ID 大于（即比其更新）指定 ID 的结果。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
返回帖子 ID 小于（即比其更早）指定 ID 的结果。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
一次请求要返回的搜索结果的最大数量。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一“页”的结果。该参数的取值直接来自 API 提供的响应，不应被修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”的结果。该参数的取值直接来自 API 提供的响应，不应被修改。
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
返回结果的排序顺序。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的 Tweet 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要扩展的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
要显示的 Media 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
要显示的 Poll 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的 User 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
要显示的 Place 字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorSearchRecentResponse`