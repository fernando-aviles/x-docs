---
title: "PostsClient"
sidebarTitle: "PostsClient"
---

<div id="postsclient">
  ## PostsClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于帖子相关操作的 Client

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 参数

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

创建或编辑帖子
为已认证用户创建一个新的帖子，或者在提供 `edit_options` 时编辑一个已有的帖子。
body: 请求体
:returns: 响应数据
:rtype: CreateResponse

#### 参数

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

删除帖子
如果该帖子归当前已认证用户所有，则根据其 ID 删除指定的帖子。

#### 参数

<ParamField path="path.id" type="Any">
要删除的帖子 ID。
</ParamField>

#### 返回

`DeleteResponse` - 响应数据

<div id="get_analytics">
  ### `get_analytics`
</div>

获取帖子分析数据
在指定的时间范围内获取特定帖子的分析数据。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.ids" type="List">
以逗号分隔的帖子 ID 列表。单个请求最多允许 100 个。
</ParamField>

<ParamField path="path.end_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。表示时间范围结束时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.start_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。表示时间范围开始时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.granularity" type="str">
搜索结果计数的粒度。
</ParamField>

<ParamField path="path.analytics_fields" type="List or None" default="None">
以逗号分隔的要显示的 Analytics 字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetAnalyticsResponse` - 响应数据

<div id="get_by_id">
  ### `get_by_id`
</div>

通过 ID 获取帖子
根据其 ID 检索指定帖子的详细信息。

#### 参数

<ParamField path="path.id" type="Any">
单个帖子 ID。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表，用于显示。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的媒体字段列表，用于显示。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的投票字段列表，用于显示。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的用户字段列表，用于显示。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的地点字段列表，用于显示。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetByIdResponse` - 响应数据

<div id="get_by_ids">
  ### `get_by_ids`
</div>

通过 ID 批量获取帖子
根据多个帖子 ID 检索其详细信息。

#### 参数

<ParamField path="path.ids" type="List">
以逗号分隔的帖子 id 列表。每个请求最多允许 100 个。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要返回的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要返回的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要返回的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要返回的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要返回的地点字段列表。
</ParamField>

#### 返回

`GetByIdsResponse` - 响应数据

<div id="get_counts_all">
  ### `get_counts_all`
</div>

获取所有帖子的数量
从完整存档中检索与搜索查询匹配的帖子数量。

#### 参数

<ParamField path="path.query" type="str">
用于匹配帖子的单条查询/规则/过滤条件。请参考 [https://t.co/rulelength](https://t.co/rulelength) 以确定查询的最大长度。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子时使用的最早 UTC 时间戳（最近 7 天内）。时间戳精度为秒，且为包含关系（即 12:00:01 包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子时使用的最新（最近）UTC 时间戳。时间戳精度为秒，且为排他关系（即 12:00:01 不包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
返回帖子 ID 大于（即比其更新）指定 ID 的结果。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
返回帖子 ID 小于（即比其更早）指定 ID 的结果。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一页的结果。与该参数一起使用的值直接来自 API 提供的响应，不应修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。与该参数一起使用的值直接来自 API 提供的响应，不应修改。
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
搜索计数结果的粒度。
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
要展示的、以逗号分隔的 SearchCount 字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetCountsAllResponse`

<div id="get_counts_recent">
  ### `get_counts_recent`
</div>

获取最近的帖子数量
检索过去 7 天内符合搜索查询条件的帖子数量。

#### 参数

<ParamField path="path.query" type="str">
用于匹配帖子的单条查询 / 规则 / 过滤条件。请参阅 [https://t.co/rulelength](https://t.co/rulelength) 以确定查询的最大长度。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子时所使用的最早 UTC 时间戳（最近 7 天内）。时间戳的粒度为秒，且为包含边界（即 12:00:01 包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子时所使用的最新（最近） UTC 时间戳。时间戳的粒度为秒，且为不包含边界（即 12:00:01 不包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
返回帖子 ID 大于（即比其更新）指定 ID 的结果。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
返回帖子 ID 小于（即比其更早）指定 ID 的结果。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。此参数使用的值直接取自 API 返回的响应，不应修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。此参数使用的值直接取自 API 返回的响应，不应修改。
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
搜索结果计数的粒度。
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
以逗号分隔的要显示的 SearchCount 字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetCountsRecentResponse`

<div id="get_insights28hr">
  ### `get_insights28hr`
</div>

获取最近 28 小时的帖子洞察
获取指定帖子在过去 28 小时内的互动指标。

#### 参数

<ParamField path="path.tweet_ids" type="List">
用于 28 小时指标的 Post id 列表。
</ParamField>

<ParamField path="path.granularity" type="str">
指标响应的粒度。
</ParamField>

<ParamField path="path.requested_metrics" type="List">
历史请求中要获取的指标。
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
要显示的互动字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetInsights28hrResponse` - 响应数据

<div id="get_insights_historical">
  ### `get_insights_historical`
</div>

获取历史帖子洞察数据
在指定的时间范围内获取指定帖子对应的历史互动指标。

#### 参数

<ParamField path="path.tweet_ids" type="List">
历史指标所用的帖子 id 列表。
</ParamField>

<ParamField path="path.end_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。表示时间范围结束时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.start_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。表示时间范围开始时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.granularity" type="str">
指标响应的粒度。
</ParamField>

<ParamField path="path.requested_metrics" type="List">
历史请求中要查询的指标。
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
要显示的以逗号分隔的互动字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetInsightsHistoricalResponse` - 响应数据

<div id="get_liking_users">
  ### `get_liking_users`
</div>

获取点赞的用户
根据帖子 ID 获取对指定帖子点过赞的用户列表。

#### 参数

<ParamField path="path.id" type="Any">
单个帖子 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
用逗号分隔的用户字段列表，用于显示相应字段。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
用逗号分隔的字段列表，用于展开相关对象。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
用逗号分隔的推文字段列表，用于显示相应字段。
</ParamField>

#### 返回

`IteratorGetLikingUsersResponse`

<div id="get_quoted">
  ### `get_quoted`
</div>

获取引用特定帖子的帖子
检索引用特定帖子（通过其 ID 标识）的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
单个帖子 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
要返回的最大结果数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页面”的结果。
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
要排除的实体集合（例如“replies”或“retweets”）。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要显示的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要显示的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要显示的地点字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetQuotedResponse`

<div id="get_reposted_by">
  ### `get_reposted_by`
</div>

获取转发用户
根据帖子 ID 检索转发了指定帖子的用户列表。

#### 参数

<ParamField path="path.id" type="Any">
单个帖子 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的 User 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的 Tweet 字段列表。
</ParamField>

#### 返回

`IteratorGetRepostedByResponse`

<div id="get_reposts">
  ### `get_reposts`
</div>

获取转发
检索转发指定帖子（通过其 id 标识）的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
单个帖子 id。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要显示的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要显示的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要显示的地点字段列表。
</ParamField>

#### 返回

`IteratorGetRepostsResponse`

<div id="hide_reply">
  ### `hide_reply`
</div>

隐藏回复
隐藏或取消隐藏经认证用户所拥有的会话中的某条回复。

#### 参数

<ParamField path="path.tweet_id" type="Any">
你想要隐藏或取消隐藏的回复 ID。
</ParamField>

<ParamField path="body.body" type="HideReplyRequest">
请求正文
</ParamField>

<div id="search_all">
  ### `search_all`
</div>

搜索全部帖子
从完整存档中检索符合搜索查询的帖子。

#### 参数

<ParamField path="path.query" type="str">
用于匹配帖子的单条查询/规则/过滤条件。请参阅 [https://t.co/rulelength](https://t.co/rulelength) 以了解查询的最大长度。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子时可使用的最早 UTC 时间戳。时间戳精度为秒，且为包含端点（即 12:00:01 包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子时可使用的最新（最近）UTC 时间戳。时间戳精度为秒，且为不包含端点（即 12:00:01 不包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
返回帖子 ID 大于（即比其更新）指定 ID 的结果。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
返回帖子 ID 小于（即比其更早）指定 ID 的结果。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
单次请求可返回的搜索结果最大数量。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。与该参数一起使用的值直接取自 API 返回的响应，不应被修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。与该参数一起使用的值直接取自 API 返回的响应，不应被修改。
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
返回结果的排序方式。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的 Tweet 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要显示的 Media 字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要显示的 Poll 字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的 User 字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要显示的 Place 字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorSearchAllResponse`

<div id="search_recent">
  ### `search_recent`
</div>

搜索最近的帖子
获取过去 7 天内与搜索查询匹配的帖子。

#### 参数

<ParamField path="path.query" type="str">
用于匹配帖子的单个查询/规则/过滤条件。请参阅 [https://t.co/rulelength](https://t.co/rulelength) 了解最大查询长度。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子的最早 UTC 时间戳。时间戳精确到秒，为包含（inclusive）关系（即 12:00:01 包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子的最新（最近）UTC 时间戳。时间戳精确到秒，为不包含（exclusive）关系（即 12:00:01 不包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
返回 Post ID 大于（即比其更新）指定 ID 的结果。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
返回 Post ID 小于（即比其更早）指定 ID 的结果。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
单个请求可返回的搜索结果的最大数量。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。与该参数一起使用的值直接取自 API 返回的响应，不应修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。与该参数一起使用的值直接取自 API 返回的响应，不应修改。
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
返回结果的排序顺序。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要显示的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要显示的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要显示的地点字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorSearchRecentResponse`