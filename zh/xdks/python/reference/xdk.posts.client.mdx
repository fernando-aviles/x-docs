---
title: "PostsClient"
sidebarTitle: "PostsClient"
---

<div id="postsclient">
  ## PostsClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于处理帖子相关操作的客户端

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 参数

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

创建或编辑帖子
为已认证用户创建一个新的帖子，或者在提供 edit_options 时编辑现有帖子。
body: 请求正文
:returns: 响应数据
:rtype: CreateResponse

#### 参数

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

删除帖子
删除经过身份验证用户拥有的指定帖子（通过其 ID）。

#### 参数

<ParamField path="path.id" type="Any">
要删除的帖子的 ID。
</ParamField>

#### 返回

`DeleteResponse` - 响应数据

<div id="get_analytics">
  ### `get_analytics`
</div>

获取帖子分析
在指定的时间范围内获取特定帖子的分析数据。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.ids" type="List">
以英文逗号分隔的帖子 ID 列表。单个请求最多可包含 100 个。
</ParamField>

<ParamField path="path.end_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。表示时间范围结束时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.start_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。表示时间范围开始时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.granularity" type="str">
搜索计数结果的粒度。
</ParamField>

<ParamField path="path.analytics_fields" type="List or None" default="None">
以英文逗号分隔的 Analytics 字段列表，用于显示。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetAnalyticsResponse` - 响应数据

<div id="get_by_id">
  ### `get_by_id`
</div>

按 ID 获取帖子
获取指定 ID 帖子的详细信息。

#### 参数

<ParamField path="path.id" type="Any">
单个帖子 ID。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的推文字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要展开的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
要显示的媒体字段的逗号分隔列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
要显示的投票字段的逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的用户字段的逗号分隔列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
要显示的地点字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetByIdResponse` - 响应数据

<div id="get_by_ids">
  ### `get_by_ids`
</div>

根据 ID 获取帖子
通过帖子 ID 检索多个帖子的详细信息。

#### 参数

<ParamField path="path.ids" type="List">
以逗号分隔的帖子 ID 列表。单个请求中最多可指定 100 个。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的 Tweet 字段列表，用于显示。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的 Media 字段列表，用于显示。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的 Poll 字段列表，用于显示。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的 User 字段列表，用于显示。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的 Place 字段列表，用于显示。
</ParamField>

#### 返回

`GetByIdsResponse` - 响应数据

<div id="get_counts_all">
  ### `get_counts_all`
</div>

获取所有帖子计数
从完整归档中检索与搜索查询匹配的帖子数量。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="str">
用于匹配帖子的单个查询/规则/过滤条件。请参阅 [https://t.co/rulelength](https://t.co/rulelength) 了解查询长度上限。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最早 UTC 时间戳（最近 7 天内）。时间戳精确到秒且为包含关系（即 12:00:01 包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最新（最近）UTC 时间戳。时间戳精确到秒且为不包含关系（即 12:00:01 不包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
返回帖子 ID 大于（即更新于）指定 ID 的结果。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
返回帖子 ID 小于（即早于）指定 ID 的结果。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。该参数所使用的值直接取自 API 返回的响应，不应修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。该参数所使用的值直接取自 API 返回的响应，不应修改。
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
搜索计数结果的粒度。
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
以逗号分隔的要显示的 SearchCount 字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetCountsAllResponse`

<div id="get_counts_recent">
  ### `get_counts_recent`
</div>

获取最近帖子数量
检索符合搜索查询条件的过去 7 天内的帖子数量。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="str">
用于匹配帖子的单个查询/规则/过滤条件。请参阅 [https://t.co/rulelength](https://t.co/rulelength) 以确认最大查询长度。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子的最早 UTC 时间戳（限最近 7 天内）。时间戳的精度为秒，且为包含关系（即 12:00:01 包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子的最新（最近）UTC 时间戳。时间戳的精度为秒，且为不包含关系（即 12:00:01 不包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
返回帖子 ID 大于（即比其更新）指定的 ID 的结果。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
返回帖子 ID 小于（即比其更早）指定的 ID 的结果。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。与该参数一起使用的值直接取自 API 提供的响应，不应修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。与该参数一起使用的值直接取自 API 提供的响应，不应修改。
</ParamField>

<ParamField path="path.granularity" type="str or None" default="None">
搜索计数结果的粒度。
</ParamField>

<ParamField path="path.search_count_fields" type="List or None" default="None">
要显示的 SearchCount 字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetCountsRecentResponse`

<div id="get_insights28hr">
  ### `get_insights28hr`
</div>

获取过去 28 小时的帖子洞察
获取指定帖子在过去 28 小时内的互动指标。

#### 参数

<ParamField path="path.tweet_ids" type="List">
28 小时指标的帖子 id 列表。
</ParamField>

<ParamField path="path.granularity" type="str">
指标响应的粒度。
</ParamField>

<ParamField path="path.requested_metrics" type="List">
历史请求所需的指标。
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
要显示的互动字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetInsights28hrResponse` - 响应数据

<div id="get_insights_historical">
  ### `get_insights_historical`
</div>

获取历史帖子洞察数据  
在指定时间范围内检索特定帖子的历史互动指标。

#### 参数

<ParamField path="path.tweet_ids" type="List">
用于历史指标的帖子 id 列表。
</ParamField>

<ParamField path="path.end_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。表示时间范围结束时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.start_time" type="str">
YYYY-MM-DDTHH:mm:ssZ。表示时间范围开始时间的 UTC 时间戳。
</ParamField>

<ParamField path="path.granularity" type="str">
指标响应的粒度。
</ParamField>

<ParamField path="path.requested_metrics" type="List">
历史请求的指标。
</ParamField>

<ParamField path="path.engagement_fields" type="List or None" default="None">
要显示的参与度字段，以逗号分隔的列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetInsightsHistoricalResponse` - 响应数据

<div id="get_liking_users">
  ### `get_liking_users`
</div>

获取点赞该帖子的用户
根据帖子 ID 检索对该帖子点赞的用户列表。

#### 参数

<ParamField path="path.id" type="Any">
单个帖子 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
最大结果数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的用户字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetLikingUsersResponse`

<div id="get_quoted">
  ### `get_quoted`
</div>

获取引用帖子的帖子
根据指定帖子的 ID，返回引用该帖子的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
单个帖子 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
要返回的结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页面”的结果。
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
要排除的实体集合（例如 “replies” 或 “retweets”）。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
用逗号分隔的 Tweet 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
用逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
用逗号分隔的要展示的 Media 字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
用逗号分隔的要展示的 Poll 字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
用逗号分隔的要展示的 User 字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
用逗号分隔的要展示的 Place 字段列表。
</ParamField>

#### 返回

`IteratorGetQuotedResponse`

<div id="get_reposted_by">
  ### `get_reposted_by`
</div>

获取转发用户
根据帖子 id 检索转发指定帖子的用户列表。

#### 参数

<ParamField path="path.id" type="Any">
单个帖子 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
最大结果数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的 User 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要扩展的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的 Tweet 字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetRepostedByResponse`

<div id="get_reposts">
  ### `get_reposts`
</div>

获取转发列表
返回转发指定帖子（通过其 ID 标识）的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
单个帖子 id。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页结果。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要展示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要展示的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要展示的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要展示的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要展示的地点字段列表。
</ParamField>

#### 返回

`IteratorGetRepostsResponse`

<div id="hide_reply">
  ### `hide_reply`
</div>

隐藏回复
隐藏或取消隐藏经过身份验证用户所拥有会话中的某条回复。

#### 参数

<ParamField path="path.tweet_id" type="Any">
你要隐藏或取消隐藏的回复 ID。
</ParamField>

<ParamField path="body.body" type="HideReplyRequest">
请求正文
</ParamField>

<div id="search_all">
  ### `search_all`
</div>

搜索所有帖子
从完整存档中检索与搜索查询匹配的帖子。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="str">
一个用于匹配帖子的查询/规则/过滤器。请参阅 [https://t.co/rulelength](https://t.co/rulelength) 以确定最大查询长度。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子时所使用的最早 UTC 时间戳。从该时间点起开始返回帖子。时间戳精确到秒，且为包含关系（即 12:00:01 包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子时所使用的最新（最近）UTC 时间戳。只返回此时间点之前的帖子。时间戳精确到秒，且为不包含关系（即 12:00:01 不包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
只返回帖子 id 大于（即比其更新）指定 id 的结果。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
只返回帖子 id 小于（即比其更早）指定 id 的结果。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
单个请求可返回的最大搜索结果数。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一“页”的结果。与该参数一起使用的值直接取自 API 返回的响应，不应被修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”的结果。与该参数一起使用的值直接取自 API 返回的响应，不应被修改。
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
返回结果的排序方式。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要返回的 Tweet 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要返回的 Media 字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要返回的 Poll 字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要返回的 User 字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要返回的 Place 字段列表。
</ParamField>

#### 返回

`IteratorSearchAllResponse`

<div id="search_recent">
  ### `search_recent`
</div>

搜索最近的帖子
检索过去 7 天内符合搜索查询的帖子。

#### 参数

<ParamField path="path.query" type="str">
用于匹配帖子的单个查询 / 规则 / 过滤条件。请参阅 [https://t.co/rulelength](https://t.co/rulelength) 了解查询的最大长度。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子时所使用的最早 UTC 时间戳。时间戳为秒级精度，且为包含区间（即 12:00:01 包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子时所使用的最新（最近）UTC 时间戳。时间戳为秒级精度，且为不包含区间（即 12:00:01 不包含该分钟的第一秒）。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
返回帖子 ID 大于（即比其更新）指定 ID 的结果。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
返回帖子 ID 小于（即比其更早）指定 ID 的结果。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
单个请求可返回的搜索结果的最大数量。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。该参数使用的值直接来自 API 返回的响应，不应被修改。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。该参数使用的值直接来自 API 返回的响应，不应被修改。
</ParamField>

<ParamField path="path.sort_order" type="str or None" default="None">
返回结果的排序顺序。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的 Tweet 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要扩展的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
要显示的媒体字段的逗号分隔列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
要显示的投票字段的逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的用户字段的逗号分隔列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
要显示的地点字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorSearchRecentResponse`