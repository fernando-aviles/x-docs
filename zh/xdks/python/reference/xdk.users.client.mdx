---
title: "UsersClient"
sidebarTitle: "UsersClient"
---

<div id="usersclient">
  ## UsersClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于处理用户相关操作的 Client

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 参数

<ParamField path="path.client" type="Client"></ParamField>

<div id="block_dms">
  ### `block_dms`
</div>

屏蔽私信
根据指定用户的 id，为已认证用户屏蔽与该用户之间互相发送的私信。

#### 参数

<ParamField path="path.id" type="Any">
经过认证的用户请求屏蔽其私信的目标用户 id。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`BlockDmsResponse` - 响应数据

<div id="create_bookmark">
  ### `create_bookmark`
</div>

创建书签
将帖子添加到已通过身份验证的用户的书签中。

#### 参数

<ParamField path="path.id" type="Any">
要为其添加书签的已认证源用户的 ID。
</ParamField>

<ParamField path="body.body" type="CreateBookmarkRequest">
请求正文
</ParamField>

<div id="delete_bookmark">
  ### `delete_bookmark`
</div>

删除书签
通过其 ID，从已认证用户的书签中移除某个帖子。

#### 参数

<ParamField path="path.id" type="Any">
要移除其书签的已验证源用户的 id。
</ParamField>

<ParamField path="path.tweet_id" type="Any">
源用户要从书签中移除的帖子 id。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`DeleteBookmarkResponse` - 响应数据

<div id="follow_list">
  ### `follow_list`
</div>

关注列表
让已通过身份验证的用户根据其 ID 关注指定的列表。

#### 参数

<ParamField path="path.id" type="Any">
将要关注该列表的已认证源用户的 ID。
</ParamField>

<ParamField path="body.body" type="FollowListRequest">
请求正文
</ParamField>

<div id="follow_user">
  ### `follow_user`
</div>

关注用户
使已认证用户通过指定用户的 id 关注该用户。

#### 参数

<ParamField path="path.id" type="Any">
发起关注目标用户请求的已认证源用户的 ID。
</ParamField>

<ParamField path="body.body" type="FollowUserRequest">
请求体
</ParamField>

<div id="get_blocking">
  ### `get_blocking`
</div>

获取屏蔽列表
返回由指定用户 ID 屏蔽的用户列表。

#### 参数

<ParamField path="path.id" type="Any">
要为其返回结果的已认证源用户的 id。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页面”的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的用户字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要展开的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的推文字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetBlockingResponse`

<div id="get_bookmark_folders">
  ### `get_bookmark_folders`
</div>

获取书签文件夹列表
检索当前经过身份验证的用户创建的书签文件夹列表。

#### 参数

<ParamField path="path.id" type="Any">
要返回结果的已认证源用户的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetBookmarkFoldersResponse`

<div id="get_bookmarks">
  ### `get_bookmarks`
</div>

获取书签
获取当前经过身份验证的用户已加书签的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
要为其返回结果的已通过身份验证的源用户的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要显示的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要显示的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要显示的地点字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetBookmarksResponse`

<div id="get_bookmarks_by_folder_id">
  ### `get_bookmarks_by_folder_id`
</div>

按文件夹 ID 获取书签
为已通过身份验证的用户根据文件夹 ID 检索特定书签文件夹中的帖子。

#### 参数

<ParamField path="path.id" type="Any">
要为其返回结果的已通过身份验证的源用户 ID。
</ParamField>

<ParamField path="path.folder_id" type="Any">
已通过身份验证的用户要从中获取帖子所用的书签文件夹 ID。
</ParamField>

#### 返回

`GetBookmarksByFolderIdResponse` - 响应数据

<div id="get_by_id">
  ### `get_by_id`
</div>

通过 ID 获取用户  
根据指定用户的 ID 获取其详细信息。

#### 参数

<ParamField path="path.id" type="Any">
要查找的 User ID。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的 User 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetByIdResponse` - 响应数据

<div id="get_by_ids">
  ### `get_by_ids`
</div>

通过 id 获取多个用户
根据多个用户的 id 获取其详细信息。

#### 参数

<ParamField path="path.ids" type="List">
以逗号分隔的用户 ID 列表。最多可指定 100 个 ID。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的用户字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要展开的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的推文字段的逗号分隔列表。
</ParamField>

#### 返回

`GetByIdsResponse` - 响应数据

<div id="get_by_username">
  ### `get_by_username`
</div>

通过用户名获取 User
根据用户名检索指定 User 的详细信息。

#### 参数

<ParamField path="path.username" type="str">
用户名。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的 User 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

#### 返回

`GetByUsernameResponse` - 响应数据

<div id="get_by_usernames">
  ### `get_by_usernames`
</div>

根据用户名获取 Users
根据多个 Users 的用户名检索其详细信息。

#### 参数

<ParamField path="path.usernames" type="List">
以逗号分隔的用户名列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的 User 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的 Tweet 字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetByUsernamesResponse` - 响应数据

<div id="get_followed_lists">
  ### `get_followed_lists`
</div>

获取用户关注的列表
根据特定用户的 id，检索该用户所关注的列表。

#### 参数

<ParamField path="path.id" type="Any">
要查找的 User 的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果数量的最大上限。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页”的结果。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
要显示的、以逗号分隔的列表字段。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要展开的、以逗号分隔的字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的、以逗号分隔的 User 字段。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetFollowedListsResponse`

<div id="get_followers">
  ### `get_followers`
</div>

获取关注者
通过特定用户的 id，检索关注该用户的用户列表。

#### 参数

<ParamField path="path.id" type="Any">
要查询的 User 的 id。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页”的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要返回的 User 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要返回的推文字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetFollowersResponse`

<div id="get_following">
  ### `get_following`
</div>

获取关注用户
根据指定用户的 id，检索其所关注的用户列表。

#### 参数

<ParamField path="path.id" type="Any">
要查询的用户的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页”的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的用户字段列表，用于展示。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表，用于展示。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetFollowingResponse`

<div id="get_liked_posts">
  ### `get_liked_posts`
</div>

获取点赞的帖子
根据特定用户的 id 获取该用户点赞的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
要查询的用户 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要返回的推文字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要展开的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
要返回的媒体字段的逗号分隔列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
要返回的投票字段的逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要返回的用户字段的逗号分隔列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
要返回的地点字段的逗号分隔列表。
</ParamField>

#### 返回

`IteratorGetLikedPostsResponse`

<div id="get_list_memberships">
  ### `get_list_memberships`
</div>

获取列表成员关系
根据其 id 检索特定用户加入的列表集合。

#### 参数

<ParamField path="path.id" type="Any">
要查找的用户 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定的一“页”结果。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
以逗号分隔的要显示的列表字段。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的用户字段。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetListMembershipsResponse`

<div id="get_me">
  ### `get_me`
</div>

获取当前用户
返回已认证用户的详细信息。

#### 参数

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的用户字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要展开的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的推文字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetMeResponse` - 响应数据

<div id="get_mentions">
  ### `get_mentions`
</div>

获取提及列表
通过用户的 id 检索提及该用户的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
要查询的用户 ID。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
结果集中要包含的最小帖子 ID。如果同时指定了此参数和 start_time，此参数具有更高优先级。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
结果集中要包含的最大帖子 ID。如果同时指定了此参数和 end_time，此参数具有更高优先级。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子时所使用的最早 UTC 时间戳。如果同时指定了 since_id 参数，则 since_id 具有更高优先级。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子时所使用的最晚 UTC 时间戳。如果同时指定了 until_id 参数，则 until_id 具有更高优先级。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
逗号分隔的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
逗号分隔的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
逗号分隔的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
逗号分隔的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
逗号分隔的地点字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetMentionsResponse`

<div id="get_muting">
  ### `get_muting`
</div>

获取静音列表
返回经身份验证的用户所静音的用户列表。

#### 参数

<ParamField path="path.id" type="Any">
要为其返回结果的已通过身份验证的源用户的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的用户字段列表，以逗号分隔。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要展开的字段列表，以逗号分隔。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的推文字段列表，以逗号分隔。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetMutingResponse`

<div id="get_owned_lists">
  ### `get_owned_lists`
</div>

获取所拥有的列表
根据特定用户的 id，检索其所拥有的列表。

#### 参数

<ParamField path="path.id" type="Any">
要查询的 User 的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页面”的结果。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
以逗号分隔的要显示的列表（List）字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的用户（User）字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetOwnedListsResponse`

<div id="get_pinned_lists">
  ### `get_pinned_lists`
</div>

获取置顶的列表
返回经过身份验证用户所置顶的列表。

#### 参数

<ParamField path="path.id" type="Any">
需要返回结果的已通过身份验证的源用户的 id。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
要显示的列表字段，以逗号分隔。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要展开的字段，以逗号分隔。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的用户字段，以逗号分隔。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetPinnedListsResponse` - 响应数据

<div id="get_posts">
  ### `get_posts`
</div>

获取帖子
根据指定用户的 id，检索其创作的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
要查询的 User 的 ID。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
要包含在结果集中最小的帖子 ID。若同时指定了该参数和 start_time，则该参数具有更高优先级。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
要包含在结果集中最大的帖子 ID。若同时指定了该参数和 end_time，则该参数具有更高优先级。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果数量上限。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
要排除的实体集合（例如“replies”或“retweets”）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。要返回帖子时允许的最早 UTC 时间戳。如果同时指定了 since_id 参数，则 since_id 参数具有更高优先级。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。要返回帖子时允许的最晚 UTC 时间戳。如果同时指定了 until_id 参数，则 until_id 参数具有更高优先级。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要展示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要展示的 Media 字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要展示的 Poll 字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要展示的 User 字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要展示的 Place 字段列表。
</ParamField>

#### 返回

`IteratorGetPostsResponse`

<div id="get_reposts_of_me">
  ### `get_reposts_of_me`
</div>

获取转发我内容的帖子
检索转发已认证用户内容的帖子列表。

#### 参数

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
用于指定要返回的推文字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
用于指定要展开的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
用于指定要返回的 Media 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
用于指定要返回的 Poll 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
用于指定要返回的 User 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
用于指定要返回的 Place 字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetRepostsOfMeResponse`

<div id="get_timeline">
  ### `get_timeline`
</div>

获取时间线
获取经过身份验证用户时间线中按时间倒序排列的帖子列表。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
已通过认证的源用户的 ID，用于列出其按时间倒序排列的时间线帖子。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
结果集中要包含的最小帖子 ID。如果同时指定了该参数和 start_time，则该参数优先。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
结果集中要包含的最大帖子 ID。如果同时指定了该参数和 end_time，则该参数优先。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取结果的下一“页”。
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
要排除的实体集合（例如 “replies” 或 “retweets”）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。开始返回帖子时所使用的最早 UTC 时间戳。如果同时指定了 since_id 参数，则 since_id 优先。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子时所使用的最晚 UTC 时间戳。如果同时指定了 until_id 参数，则 until_id 优先。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要显示的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要显示的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要显示的地点字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetTimelineResponse`

<div id="like_post">
  ### `like_post`
</div>

点赞帖子
使已认证用户根据其 id 点赞指定的帖子。

#### 参数

<ParamField path="path.id" type="Any">
请求为该帖子点赞的已认证源用户的 ID。
</ParamField>

<ParamField path="body.body" type="LikePostRequest">
请求正文
</ParamField>

<div id="mute_user">
  ### `mute_user`
</div>

静音用户
使已通过身份验证的用户根据指定用户的 id 将该用户静音。

#### 参数

<ParamField path="path.id" type="Any">
发起将目标 User 静音请求的已认证源 User 的 id。
</ParamField>

<ParamField path="body.body" type="MuteUserRequest">
请求体
</ParamField>

<div id="pin_list">
  ### `pin_list`
</div>

固定列表
使已认证用户通过其 ID 固定指定的列表。

#### 参数

<ParamField path="path.id" type="Any">
用于固定该列表的已认证源用户的 id。
</ParamField>

<ParamField path="body.body" type="PinListRequest">
请求体
</ParamField>

<div id="repost_post">
  ### `repost_post`
</div>

转发帖子
使已通过身份验证的用户通过其 id 转发特定帖子。

#### 参数

<ParamField path="path.id" type="Any">
发起转发该帖子(Post)请求的已认证源用户的 ID。
</ParamField>

<ParamField path="body.body" type="RepostPostRequest">
请求正文
</ParamField>

<div id="search">
  ### `search`
</div>

搜索用户
检索与搜索查询匹配的用户列表。

#### 参数

<ParamField path="path.query" type="Any">
用于查询用户的查询字符串。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。与该参数一起使用的值直接取自 API 提供的响应，不应修改。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的用户字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorSearchResponse`

<div id="unblock_dms">
  ### `unblock_dms`
</div>

取消屏蔽私信
为已认证用户，根据特定用户的 id，取消对与该用户之间私信往来的屏蔽。

#### 参数

<ParamField path="path.id" type="Any">
由已通过认证的用户请求解除其私信（DM）屏蔽的目标用户的 ID。
</ParamField>

#### 返回

`UnblockDmsResponse` - 响应数据

<div id="unfollow_list">
  ### `unfollow_list`
</div>

取消关注列表
使经过认证的用户根据指定的 ID 取消关注特定列表。

#### 参数

<ParamField path="path.id" type="Any">
将要取消关注该列表的已认证源用户的 ID。
</ParamField>

<ParamField path="path.list_id" type="Any">
要取消关注的列表的 ID。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`UnfollowListResponse` - 响应数据

<div id="unfollow_user">
  ### `unfollow_user`
</div>

取消关注用户
使经过认证的用户通过其 id 取消关注指定用户。

#### 参数

<ParamField path="path.source_user_id" type="Any">
正在请求取消关注目标用户的已认证源用户的 ID。
</ParamField>

<ParamField path="path.target_user_id" type="Any">
源用户请求取消关注的目标用户的 ID。
</ParamField>

#### 返回

`UnfollowUserResponse` - 响应数据

<div id="unlike_post">
  ### `unlike_post`
</div>

取消点赞帖子
使已通过身份验证的用户根据其 id 取消对特定帖子的点赞。

#### 参数

<ParamField path="path.id" type="Any">
发起取消点赞请求的已通过身份验证的源用户的 ID。
</ParamField>

<ParamField path="path.tweet_id" type="Any">
用户请求取消点赞的帖子的 ID。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`UnlikePostResponse` - 响应数据

<div id="unmute_user">
  ### `unmute_user`
</div>

取消对用户的静音
使已认证用户通过目标用户的 id 取消对该特定用户的静音。

#### 参数

<ParamField path="path.source_user_id" type="Any">
发起取消对目标 User 静音请求的已认证源 User 的 ID。
</ParamField>

<ParamField path="path.target_user_id" type="Any">
源 User 请求取消静音的目标 User 的 ID。
</ParamField>

#### 返回

`UnmuteUserResponse` - 返回数据

<div id="unpin_list">
  ### `unpin_list`
</div>

取消固定列表
使已认证用户根据列表的 id 取消固定指定的列表。

#### 参数

<ParamField path="path.id" type="Any">
要返回结果的已通过身份验证的源用户的 ID。
</ParamField>

<ParamField path="path.list_id" type="Any">
要取消固定的列表的 ID。
</ParamField>

#### 返回

`UnpinListResponse` - 响应数据

<div id="unrepost_post">
  ### `unrepost_post`
</div>

取消转发帖子
使通过身份验证的用户根据指定的 id 取消转发某个特定帖子。

#### 参数

<ParamField path="path.id" type="Any">
发起转发该帖请求的已通过身份验证的源用户的 id。
</ParamField>

<ParamField path="path.source_tweet_id" type="Any">
用户请求取消转发的帖子的 id。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`UnrepostPostResponse` - 响应数据