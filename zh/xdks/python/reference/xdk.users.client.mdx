---
title: "UsersClient"
sidebarTitle: "UsersClient"
---

<div id="usersclient">
  ## UsersClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">继承自：object</Badge>

用于用户相关操作的客户端

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

#### 参数

<ParamField path="path.client" type="Client"></ParamField>

<div id="block_dms">
  ### `block_dms`
</div>

屏蔽私信
为已认证用户，根据特定用户的 id，屏蔽与该用户之间的私信收发。

#### 参数

<ParamField path="path.id" type="Any">
已通过身份验证的用户请求屏蔽其私信的目标用户的 id。
</ParamField>

#### 返回

`BlockDmsResponse` - 响应数据

<div id="create_bookmark">
  ### `create_bookmark`
</div>

创建书签
将帖子添加到已通过身份验证用户的书签中。

#### 参数

<ParamField path="path.id" type="Any">
要为其添加书签的已认证源用户的 ID。
</ParamField>

<ParamField path="body.body" type="CreateBookmarkRequest">
请求体
</ParamField>

<div id="delete_bookmark">
  ### `delete_bookmark`
</div>

删除书签
根据其 ID，从经过身份验证的用户的书签中移除一个帖子。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
要从其书签中移除帖子的已认证源用户的 ID。
</ParamField>

<ParamField path="path.tweet_id" type="Any">
源用户要从其书签中移除的帖子 ID。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`DeleteBookmarkResponse` - 响应数据

<div id="follow_list">
  ### `follow_list`
</div>

关注列表
使已认证用户关注具有指定 ID 的特定列表。

#### 参数

<ParamField path="path.id" type="Any">
将关注该 List 的已认证源用户的 id。
</ParamField>

<ParamField path="body.body" type="FollowListRequest">
请求正文
</ParamField>

<div id="follow_user">
  ### `follow_user`
</div>

关注用户
使已认证用户通过用户 id 关注指定用户。

#### 参数

<ParamField path="path.id" type="Any">
发起关注目标 User 请求的已认证源 User 的 ID。
</ParamField>

<ParamField path="body.body" type="FollowUserRequest">
请求体
</ParamField>

<div id="get_blocking">
  ### `get_blocking`
</div>

获取屏蔽用户
检索由指定用户 ID 所屏蔽的用户列表。

#### 参数

<ParamField path="path.id" type="Any">
要为其返回结果的已通过身份验证的源用户的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果数量的最大值。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页面”的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的用户字段列表，用于指定要返回的字段。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表，用于指定要返回的字段。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetBlockingResponse`

<div id="get_bookmark_folders">
  ### `get_bookmark_folders`
</div>

获取书签文件夹
检索经过身份验证的用户创建的书签文件夹列表。

#### 参数

<ParamField path="path.id" type="Any">
要返回结果的已认证源用户的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetBookmarkFoldersResponse`

<div id="get_bookmarks">
  ### `get_bookmarks`
</div>

获取书签列表
返回已通过身份验证的用户加了书签的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
要返回结果的已认证源用户的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要显示的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要显示的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要显示的地点字段列表。
</ParamField>

#### 返回

`IteratorGetBookmarksResponse`

<div id="get_bookmarks_by_folder_id">
  ### `get_bookmarks_by_folder_id`
</div>

根据文件夹 ID 获取书签
为已通过身份验证的用户通过文件夹的 ID 检索特定书签文件夹中的帖子。

#### 参数

<ParamField path="path.id" type="Any">
要返回结果的已通过身份验证的源用户的 ID。
</ParamField>

<ParamField path="path.folder_id" type="Any">
已通过身份验证的用户尝试为其获取帖子内容的书签文件夹的 ID。
</ParamField>

#### 返回

`GetBookmarksByFolderIdResponse` - 响应数据

<div id="get_by_id">
  ### `get_by_id`
</div>

通过 id 获取用户
根据指定用户的 id 获取其详细信息。

#### 参数

<ParamField path="path.id" type="Any">
要查询的用户 ID。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的用于显示的用户字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的用于显示的推文字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetByIdResponse` - 响应数据

<div id="get_by_ids">
  ### `get_by_ids`
</div>

通过 id 获取用户
根据多个用户的 id 检索其详细信息。

#### 参数

<ParamField path="path.ids" type="List">
以逗号分隔的用户 id 列表。最多可以指定 100 个 id。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的用户字段列表，用于指定要显示的字段。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的字段列表，用于指定要扩展的字段。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表，用于指定要显示的字段。
</ParamField>

#### 返回

`GetByIdsResponse` - 响应数据

<div id="get_by_username">
  ### `get_by_username`
</div>

根据用户名获取用户  
根据指定用户的用户名检索其详细信息。

#### 参数

<ParamField path="path.username" type="str">
用户名。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的 User 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetByUsernameResponse` - 响应数据

<div id="get_by_usernames">
  ### `get_by_usernames`
</div>

通过用户名获取多个 User
根据多个 User 的用户名检索其详细信息。

#### 参数

<ParamField path="path.usernames" type="List">
以逗号分隔的用户名列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的用于显示的用户字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的用于扩展的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的用于显示的推文字段列表。
</ParamField>

#### 返回

`GetByUsernamesResponse` - 响应数据

<div id="get_followed_lists">
  ### `get_followed_lists`
</div>

获取关注的列表
通过特定用户的 id，检索该用户关注的列表。

#### 参数

<ParamField path="path.id" type="Any">
要查询的用户 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定页的结果。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
要返回的 List 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要扩展的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要返回的 User 字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetFollowedListsResponse`

<div id="get_followers">
  ### `get_followers`
</div>

获取关注者
根据指定 User 的 id，检索关注该用户的 User 列表。

#### 参数

<ParamField path="path.id" type="Any">
要查询的用户 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页面”的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的用户字段列表，用于显示。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表，用于显示。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetFollowersResponse`

<div id="get_following">
  ### `get_following`
</div>

获取关注列表
根据指定用户的 id，检索该用户正在关注的用户列表。

#### 参数

<ParamField path="path.id" type="Any">
要查找的用户 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页”的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的用户字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示推文字段列表。
</ParamField>

#### 返回

`IteratorGetFollowingResponse`

<div id="get_liked_posts">
  ### `get_liked_posts`
</div>

获取点赞的帖子列表
通过特定用户的 id，检索该用户点赞过的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
要查询的用户 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的地点字段列表。
</ParamField>

#### 返回

`IteratorGetLikedPostsResponse`

<div id="get_list_memberships">
  ### `get_list_memberships`
</div>

获取列表成员关系
通过特定用户的 id，检索该用户所加入的列表。

#### 参数

<ParamField path="path.id" type="Any">
要查询的用户 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页”的结果。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
以逗号分隔的要返回的 List 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要返回的 User 字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetListMembershipsResponse`

<div id="get_me">
  ### `get_me`
</div>

获取当前用户
返回已认证用户的详细信息。

#### 参数

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的用户字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要扩展的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的推文字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetMeResponse` - 响应数据

<div id="get_mentions">
  ### `get_mentions`
</div>

获取提及列表
根据指定用户的 id，检索提及该用户的帖子列表。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.id" type="Any">
要查找的 User 的 ID。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
结果集中要包含的最小帖子 ID。如果同时指定了此参数和 start_time，则此参数具有更高优先级。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
结果集中要包含的最大帖子 ID。如果同时指定了此参数和 end_time，则此参数具有更高优先级。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页结果。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子时使用的最早 UTC 时间戳。如果同时指定了 since_id 参数，则 since_id 具有更高优先级。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子时使用的最晚 UTC 时间戳。如果同时指定了 until_id 参数，则 until_id 具有更高优先级。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的 Tweet 字段列表，用于展示。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的 Media 字段列表，用于展示。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的 Poll 字段列表，用于展示。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的 User 字段列表，用于展示。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的 Place 字段列表，用于展示。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetMentionsResponse`

<div id="get_muting">
  ### `get_muting`
</div>

获取静音列表
检索由已通过身份验证的用户静音的用户列表。

#### 参数

<ParamField path="path.id" type="Any">
要为其返回结果的已通过认证源用户的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的用户字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要扩展的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的推文字段的逗号分隔列表。
</ParamField>

#### 返回

`IteratorGetMutingResponse`

<div id="get_owned_lists">
  ### `get_owned_lists`
</div>

获取用户拥有的列表
根据指定用户的 id，检索其拥有的列表。

#### 参数

<ParamField path="path.id" type="Any">
要查询的 User 的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页”的结果。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
用逗号分隔的要显示的 List 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
用逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
用逗号分隔的要显示的 User 字段列表。
</ParamField>

#### 返回

`IteratorGetOwnedListsResponse`

<div id="get_pinned_lists">
  ### `get_pinned_lists`
</div>

获取已置顶的列表
检索经身份验证用户已置顶的列表。

#### 参数

<ParamField path="path.id" type="Any">
要为其返回结果的已通过认证源用户的 ID。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
要显示的 List 字段的以逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要展开的字段的以逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的 User 字段的以逗号分隔列表。
</ParamField>

#### 返回

`GetPinnedListsResponse` - 响应数据

<div id="get_posts">
  ### `get_posts`
</div>

获取帖子列表
根据指定用户的 id 检索该用户发布的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
要查询的 User 的 ID。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
结果集中要包含的最小帖子 ID。如果同时指定了该参数和 start_time，则该参数具有更高优先级。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
结果集中要包含的最大帖子 ID。如果同时指定了该参数和 end_time，则该参数具有更高优先级。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
用于获取下一页结果的参数。
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
要排除的实体集合（例如“replies”或“retweets”）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子时使用的最早 UTC 时间戳。如果也指定了 since_id 参数，则 since_id 具有更高优先级。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子时使用的最晚 UTC 时间戳。如果也指定了 until_id 参数，则 until_id 具有更高优先级。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要显示的 Media 字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要显示的 Poll 字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的 User 字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要显示的 Place 字段列表。
</ParamField>

#### 返回

`IteratorGetPostsResponse`

<div id="get_reposts_of_me">
  ### `get_reposts_of_me`
</div>

获取转发我内容的帖子
检索一个帖子列表，这些帖子转发了已通过身份验证用户的内容。

#### 参数

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”的结果。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表，用于显示。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的字段列表，用于扩展。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的媒体字段列表，用于显示。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的投票字段列表，用于显示。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的用户字段列表，用于显示。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的地点字段列表，用于显示。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetRepostsOfMeResponse`

<div id="get_timeline">
  ### `get_timeline`
</div>

获取时间线
检索已认证用户时间线上按时间倒序排列的帖子列表。

#### 参数

<ParamField path="path.id" type="Any">
用于列出其逆时间顺序时间线帖子列表的已认证源用户的 ID。
</ParamField>

<ParamField path="path.since_id" type="Any or None" default="None">
结果集中要包含的最小帖子 ID。如果同时指定了该参数和 start_time，则该参数优先。
</ParamField>

<ParamField path="path.until_id" type="Any or None" default="None">
结果集中要包含的最大帖子 ID。如果同时指定了该参数和 end_time，则该参数优先。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一页的结果。
</ParamField>

<ParamField path="path.exclude" type="List or None" default="None">
要排除的实体集合（例如“replies”或“retweets”）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子时的最早 UTC 时间戳。如果同时指定了 since_id 参数，则 since_id 优先。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子时的最晚 UTC 时间戳。如果同时指定了 until_id 参数，则 until_id 优先。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要显示的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要显示的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要显示的地点字段列表。
</ParamField>

#### 返回

`IteratorGetTimelineResponse`

<div id="like_post">
  ### `like_post`
</div>

点赞帖子
使已通过身份验证的用户按其 ID 点赞指定的帖子。

#### 参数

<ParamField path="path.id" type="Any">
请求点赞该帖子的经过身份验证的源用户的 id。
</ParamField>

<ParamField path="body.body" type="LikePostRequest">
请求正文
</ParamField>

<div id="mute_user">
  ### `mute_user`
</div>

静音用户
使已通过认证的用户根据指定用户的 ID 将其静音。

#### 参数

<ParamField path="path.id" type="Any">
请求将目标 User 静音的经过身份验证的源 User 的 id。
</ParamField>

<ParamField path="body.body" type="MuteUserRequest">
请求体
</ParamField>

<div id="pin_list">
  ### `pin_list`
</div>

将 List 固定
使经过认证的用户根据其 id 将指定的 List 固定。

#### 参数

<ParamField path="path.id" type="Any">
将固定该 List 的已认证源用户的 id。
</ParamField>

<ParamField path="body.body" type="PinListRequest">
请求正文
</ParamField>

<div id="repost_post">
  ### `repost_post`
</div>

转发帖子
使已认证用户根据帖子 ID 转发特定帖子。

#### 参数

<ParamField path="path.id" type="Any">
发起转发该帖子的已认证源用户的 ID。
</ParamField>

<ParamField path="body.body" type="RepostPostRequest">
请求体
</ParamField>

<div id="search">
  ### `search`
</div>

搜索用户
检索与搜索查询匹配的用户列表。

<div id="parameters">
  #### Parameters
</div>

<ParamField path="path.query" type="Any">
用于搜索用户的查询字符串。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.next_token" type="Any or None" default="None">
此参数用于获取下一页结果。该参数所使用的值直接来自 API 提供的响应，不应修改。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的 User 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

#### 返回

`IteratorSearchResponse`

<div id="unblock_dms">
  ### `unblock_dms`
</div>

取消屏蔽私信
为已认证用户，根据特定用户的 id，取消对与该用户之间私信往来的屏蔽。

#### 参数

<ParamField path="path.id" type="Any">
经过认证的请求用户希望解除私信屏蔽的目标 User 的 ID。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`UnblockDmsResponse` - 响应数据

<div id="unfollow_list">
  ### `unfollow_list`
</div>

取消关注 List
使已认证用户根据指定 List 的 ID 取消关注该 List。

#### 参数

<ParamField path="path.id" type="Any">
将取消关注该 List 的已认证源用户的 ID。
</ParamField>

<ParamField path="path.list_id" type="Any">
要取消关注的 List 的 ID。
</ParamField>

#### 返回

`UnfollowListResponse` - 响应数据

<div id="unfollow_user">
  ### `unfollow_user`
</div>

取消关注用户
使已认证用户通过指定用户的 id 取消关注该用户。

#### 参数

<ParamField path="path.source_user_id" type="Any">
发起取消关注请求的已认证源用户的 ID。
</ParamField>

<ParamField path="path.target_user_id" type="Any">
源用户请求取消关注的目标用户的 ID。
</ParamField>

#### 返回

`UnfollowUserResponse` - 响应数据

<div id="unlike_post">
  ### `unlike_post`
</div>

取消点赞帖子
使经过身份验证的用户根据指定的 ID 取消点赞某个帖子。

#### 参数

<ParamField path="path.id" type="Any">
发起取消点赞请求的已通过身份验证的源用户的 id。
</ParamField>

<ParamField path="path.tweet_id" type="Any">
用户请求取消点赞的帖子 id。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`UnlikePostResponse` - 响应数据

<div id="unmute_user">
  ### `unmute_user`
</div>

取消对用户的静音
使已认证用户根据目标用户的 id 取消对其的静音。

#### 参数

<ParamField path="path.source_user_id" type="Any">
请求对目标用户取消静音的已认证源用户的 ID。
</ParamField>

<ParamField path="path.target_user_id" type="Any">
源用户请求取消静音的目标用户的 ID。
</ParamField>

#### 返回

`UnmuteUserResponse` - 响应数据

<div id="unpin_list">
  ### `unpin_list`
</div>

取消置顶列表
让已认证用户根据其 ID 取消置顶指定的列表。

#### 参数

<ParamField path="path.id" type="Any">
要为其返回结果的已认证源用户的 ID。
</ParamField>

<ParamField path="path.list_id" type="Any">
要取消固定的列表的 ID。
</ParamField>

#### 返回

`UnpinListResponse` - 响应数据

<div id="unrepost_post">
  ### `unrepost_post`
</div>

取消转发帖子
使经过身份验证的用户取消转发指定 ID 的帖子。

#### 参数

<ParamField path="path.id" type="Any">
发起转发该帖请求的、已通过身份验证的源用户的 ID。
</ParamField>

<ParamField path="path.source_tweet_id" type="Any">
用户请求取消转发的帖子的 ID。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`UnrepostPostResponse` - 响应数据