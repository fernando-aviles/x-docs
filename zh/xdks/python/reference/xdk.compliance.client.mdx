---
title: "ComplianceClient"
sidebarTitle: "ComplianceClient"
---

<div id="complianceclient">
  ## ComplianceClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">继承自：object</Badge>

用于执行合规相关操作的客户端

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_jobs">
  ### `create_jobs`
</div>

创建合规作业
根据指定的作业类型创建一个新的合规作业。
body: 请求体
:returns: 响应数据
:rtype: CreateJobsResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="CreateJobsRequest"></ParamField>

<div id="get_jobs">
  ### `get_jobs`
</div>

获取合规任务
检索按任务类型和可选状态过滤的合规任务列表。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.type" type="str">
要列出的合规作业类型。
</ParamField>

<ParamField path="path.status" type="str or None" default="None">
要列出的合规作业状态。
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
要显示的以逗号分隔的 ComplianceJob 字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetJobsResponse` - 响应数据

<div id="get_jobs_by_id">
  ### `get_jobs_by_id`
</div>

通过 ID 获取合规任务（Compliance Job）
检索指定 ID 的合规任务（Compliance Job）的详细信息。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
要检索的 Compliance Job 的 ID。
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
要显示的 ComplianceJob 字段的逗号分隔列表。
</ParamField>

#### 返回

`GetJobsByIdResponse` - 响应数据