---
title: "ComplianceClient"
sidebarTitle: "ComplianceClient"
---

<div id="complianceclient">
  ## ComplianceClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于合规相关操作的客户端

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_jobs">
  ### `create_jobs`
</div>

创建合规作业
为指定的作业类型创建一个新的合规作业。
body: 请求正文
:returns: 响应数据
:rtype: CreateJobsResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="CreateJobsRequest"></ParamField>

<div id="get_jobs">
  ### `get_jobs`
</div>

获取合规任务
根据任务类型和可选状态筛选并返回合规任务列表。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.type" type="str">
要列出的 Compliance Job 的类型。
</ParamField>

<ParamField path="path.status" type="str or None" default="None">
要列出的 Compliance Job 的状态。
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
要显示的以逗号分隔的 ComplianceJob 字段列表。
</ParamField>

<div id="returns">
  #### 返回
</div>

`GetJobsResponse` - 响应数据

<div id="get_jobs_by_id">
  ### `get_jobs_by_id`
</div>

通过 ID 获取合规作业（Compliance Job）
根据其 ID 检索特定合规作业（Compliance Job）的详细信息。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
要检索的合规作业的 id。
</ParamField>

<ParamField path="path.compliance_job_fields" type="List or None" default="None">
以逗号分隔的要显示的 ComplianceJob 字段列表。
</ParamField>

<div id="returns">
  #### 返回
</div>

`GetJobsByIdResponse` - 响应数据