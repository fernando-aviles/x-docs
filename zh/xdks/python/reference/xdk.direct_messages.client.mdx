---
title: "DirectMessagesClient"
sidebarTitle: "DirectMessagesClient"
---

<div id="directmessagesclient">
  ## DirectMessagesClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于执行私信相关操作的客户端

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create_by_conversation_id">
  ### `create_by_conversation_id`
</div>

根据会话 id 创建私信
向指定会话（通过其 id）发送一条新的私信。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.dm_conversation_id" type="str">
DM 会话 ID。
</ParamField>

<ParamField path="body.body" type="CreateByConversationIdRequest">
请求正文
</ParamField>

<div id="create_by_participant_id">
  ### `create_by_participant_id`
</div>

通过参与者 id 创建私信消息
通过参与者的 id 向指定参与者发送一条新的私信消息。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.participant_id" type="Any">
将接收此私信（DM）的目标用户的 ID。
</ParamField>

<ParamField path="body.body" type="CreateByParticipantIdRequest">
请求体
</ParamField>

<div id="create_conversation">
  ### `create_conversation`
</div>

创建私信会话
与指定参与者发起一个新的私信会话。
body: 请求 body
:returns: 响应 data
:rtype: CreateConversationResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="CreateConversationRequest"></ParamField>

<div id="delete_events">
  ### `delete_events`
</div>

删除私信事件
如果经过认证的用户是该事件的拥有者，则根据其 id 删除指定的私信事件。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.event_id" type="Any">
待删除的私信事件的 id。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`DeleteEventsResponse` - 响应数据

<div id="get_events">
  ### `get_events`
</div>

获取私信事件
检索所有会话中最近的私信事件列表。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页面”的结果。
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
要在结果中包含的 event_types 集合。
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
要显示的、以逗号分隔的 DmEvent 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要扩展的、以逗号分隔的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
要显示的、以逗号分隔的 Media 字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的、以逗号分隔的 User 字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的、以逗号分隔的 Tweet 字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetEventsResponse`

<div id="get_events_by_conversation_id">
  ### `get_events_by_conversation_id`
</div>

获取某个私信会话中的私信事件  
检索特定私信会话的私信事件。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
DM 会话 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页”的结果。
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
要在结果中包含的 event_types 集合。
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
以逗号分隔的要显示的 DmEvent 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要显示的 Media 字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的 User 字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetEventsByConversationIdResponse`

<div id="get_events_by_id">
  ### `get_events_by_id`
</div>

通过 id 获取私信事件
根据其 id 获取指定私信事件的详细信息。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.event_id" type="Any">
DM 事件 id。
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
以逗号分隔的 DmEvent 字段列表，用于显示。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的 Media 字段列表，用于显示。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的 User 字段列表，用于显示。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表，用于显示。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetEventsByIdResponse` - 响应数据

<div id="get_events_by_participant_id">
  ### `get_events_by_participant_id`
</div>

获取某个私信会话的事件  
检索特定私信会话的事件。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.participant_id" type="Any">
一对一私信会话中参与用户的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页面”的结果。
</ParamField>

<ParamField path="path.event_types" type="List or None" default="None">
要在结果中包含的 event_types 集合。
</ParamField>

<ParamField path="path.dm_event_fields" type="List or None" default="None">
以逗号分隔的 DmEvent 字段列表，用于显示。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的 Media 字段列表，用于显示。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的 User 字段列表，用于显示。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表，用于显示。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetEventsByParticipantIdResponse`