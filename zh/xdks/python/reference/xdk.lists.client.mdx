---
title: "ListsClient"
sidebarTitle: "ListsClient"
---

<div id="listsclient">
  ## ListsClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于列表相关操作的客户端

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="add_member">
  ### `add_member`
</div>

向 List 添加成员
根据 List 的 id，将一个 User 添加到指定的 List 中。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
要为其添加成员的 List 的 ID。
</ParamField>

<ParamField path="body.body" type="AddMemberRequest">
请求体
</ParamField>

<div id="create">
  ### `create`
</div>

创建 List
为已认证用户创建一个新的 List。
body: 请求正文
:returns: 响应数据
:rtype: CreateResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

删除列表
根据其 id 删除一个由已通过认证的用户拥有的特定列表。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
要删除的列表的 ID。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`DeleteResponse` - 响应数据

<div id="get_by_id">
  ### `get_by_id`
</div>

通过 ID 获取 List
根据其 ID 检索特定 List 的详细信息。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
List 的 ID。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
用于显示的 List 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
用于扩展的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
用于显示的 User 字段的逗号分隔列表。
</ParamField>

#### 返回

`GetByIdResponse` - 响应数据

<div id="get_followers">
  ### `get_followers`
</div>

获取 List 关注者
根据 List 的 id，检索关注该特定 List 的用户列表。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
List 的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页”的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要返回的 User 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要返回的推文字段列表。
</ParamField>

#### 返回

`IteratorGetFollowersResponse`

<div id="get_members">
  ### `get_members`
</div>

获取 List 成员
通过指定 List 的 id，检索该 List 的成员用户列表。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
List 的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页面”的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要返回的 User 字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要返回的推文字段列表。
</ParamField>

#### 返回

`IteratorGetMembersResponse`

<div id="get_posts">
  ### `get_posts`
</div>

获取列表中的帖子
通过列表 ID 检索与特定列表关联的帖子。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
列表 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”的结果。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的地点字段列表。
</ParamField>

#### 返回

`IteratorGetPostsResponse`

<div id="remove_member_by_user_id">
  ### `remove_member_by_user_id`
</div>

移除列表成员
根据列表 id 和用户 id，将该用户从指定列表中移除。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
要从中移除成员的 List 的 id。
</ParamField>

<ParamField path="path.user_id" type="Any">
要从该 List 中移除的用户的 id。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`RemoveMemberByUserIdResponse` - 响应数据

<div id="update">
  ### `update`
</div>

更新 List
根据其 id，更新经过身份验证的用户所拥有的特定 List 的详细信息。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
要修改的 List 的 ID。
</ParamField>

<ParamField path="body.body" type="UpdateRequest">
请求体
</ParamField>