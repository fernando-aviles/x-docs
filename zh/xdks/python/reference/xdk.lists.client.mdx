---
title: "ListsClient"
sidebarTitle: "ListsClient"
---

<div id="listsclient">
  ## ListsClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于列表相关操作的客户端

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="add_member">
  ### `add_member`
</div>

添加列表成员
通过列表的 id，将某个用户添加到指定的列表中。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
要向其添加成员的列表的 ID。
</ParamField>

<ParamField path="body.body" type="AddMemberRequest">
请求体
</ParamField>

<div id="create">
  ### `create`
</div>

创建 List
为通过身份验证的用户创建一个新的 List。
body: 请求体
:returns: 响应的 data
:rtype: CreateResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

删除 List
根据其 id 删除经过身份验证的用户所拥有的指定 List。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
要删除的 List 的 id。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`DeleteResponse` - 响应数据

<div id="get_by_id">
  ### `get_by_id`
</div>

按 ID 获取 List
根据其 ID 获取特定 List 的详细信息。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
List 的 ID。
</ParamField>

<ParamField path="path.list_fields" type="List or None" default="None">
要显示的 List 字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要扩展的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的 User 字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetByIdResponse` - 响应数据

<div id="get_followers">
  ### `get_followers`
</div>

获取 List 的关注者
检索按其 id 指定的特定 List 的关注者 Users 列表。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
列表的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
最大结果数。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定“页面”的结果。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的用户字段，以逗号分隔的列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要扩展的字段，以逗号分隔的列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的推文字段，以逗号分隔的列表。
</ParamField>

#### 返回

`IteratorGetFollowersResponse`

<div id="get_members">
  ### `get_members`
</div>

获取 List 成员
通过指定 List 的 ID，检索该 List 中成员的 User 列表。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
列表的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取指定结果的“页”。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的用户字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要扩展的字段列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetMembersResponse`

<div id="get_posts">
  ### `get_posts`
</div>

获取列表中的帖子
根据列表的 ID 检索与该列表关联的帖子列表。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
List 的 ID。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="Any or None" default="None">
此参数用于获取下一“页”结果。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要返回的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要返回的 Media 字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要返回的 Poll 字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要返回的 User 字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要返回的 Place 字段列表。
</ParamField>

#### 返回

`IteratorGetPostsResponse`

<div id="remove_member_by_user_id">
  ### `remove_member_by_user_id`
</div>

移除列表成员
通过列表的 id 和用户的 id，将指定用户从该列表中移除。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
要从中移除成员的 List 的 ID。
</ParamField>

<ParamField path="path.user_id" type="Any">
要从该 List 中移除的 User 的 ID。
</ParamField>

#### 返回

`RemoveMemberByUserIdResponse` - 响应数据

<div id="update">
  ### `update`
</div>

更新 List
根据其 id 更新经过身份验证的用户所拥有的特定 List 的详细信息。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.id" type="Any">
要修改的列表的 ID。
</ParamField>

<ParamField path="body.body" type="UpdateRequest">
请求体
</ParamField>