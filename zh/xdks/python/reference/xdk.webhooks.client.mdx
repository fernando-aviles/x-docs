---
title: "WebhooksClient"
sidebarTitle: "WebhooksClient"
---

<div id="webhooksclient">
  ## WebhooksClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于处理 webhook 操作的客户端

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="create">
  ### `create`
</div>

创建 Webhook
创建一个新的 Webhook 配置。
body: 请求正文
:returns: 响应数据
:rtype: CreateResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="CreateRequest"></ParamField>

<div id="create_stream_link">
  ### `create_stream_link`
</div>

创建流链接
创建一个链接，用于将 FilteredStream 事件传递到指定的 webhook。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.webhook_id" type="Any">
要链接到你的 FilteredStream 规则集的 webhook ID。
</ParamField>

<ParamField path="path.tweet_fields" type="str or None" default="None">
要显示的推文字段列表，使用逗号分隔。
</ParamField>

<ParamField path="path.expansions" type="str or None" default="None">
要扩展的字段列表，使用逗号分隔。
</ParamField>

<ParamField path="path.media_fields" type="str or None" default="None">
要显示的媒体字段列表，使用逗号分隔。
</ParamField>

<ParamField path="path.poll_fields" type="str or None" default="None">
要显示的投票字段列表，使用逗号分隔。
</ParamField>

<ParamField path="path.user_fields" type="str or None" default="None">
要显示的用户字段列表，使用逗号分隔。
</ParamField>

<ParamField path="path.place_fields" type="str or None" default="None">
要显示的地点字段列表，使用逗号分隔。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`CreateStreamLinkResponse` - 响应数据

<div id="create_webhook_replay_job">
  ### `create_webhook_replay_job`
</div>

为 webhook 创建重放作业
创建一个重放作业，用于检索最长可追溯至过去 24 小时内所有已投递或尝试投递到该 webhook 的事件。
body: 请求体
:returns: 响应 data
:rtype: CreateWebhookReplayJobResponse

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="CreateWebhookReplayJobRequest"></ParamField>

<div id="delete">
  ### `delete`
</div>

删除 webhook
删除现有的 webhook 配置。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.webhook_id" type="Any">
要删除的 webhook 的 ID。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`DeleteResponse` - 响应数据

<div id="delete_stream_link">
  ### `delete_stream_link`
</div>

删除流链接
从 FilteredStream 事件中删除与指定 webhook 之间的链接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.webhook_id" type="Any">
要与你的 FilteredStream 规则集关联的 webhook ID。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`DeleteStreamLinkResponse` - 响应数据

<div id="get">
  ### `get`
</div>

获取 Webhook
获取与客户端应用关联的 Webhook 配置列表。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.webhook_config_fields" type="List or None" default="None">
以逗号分隔的 WebhookConfig 字段列表，用于指定要在响应中显示的字段。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetResponse` - 响应数据

<div id="get_stream_links">
  ### `get_stream_links`
</div>

获取流链接
获取与过滤流规则集关联的 webhook 链接列表。
:returns: 响应数据
:rtype: GetStreamLinksResponse

#### 返回

`GetStreamLinksResponse`

<div id="validate">
  ### `validate`
</div>

验证 webhook
为指定的 webhook 触发一次 CRC 校验。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.webhook_id" type="Any">
要检查的 webhook 的 ID。
</ParamField>

#### 返回

`ValidateResponse` - 响应数据