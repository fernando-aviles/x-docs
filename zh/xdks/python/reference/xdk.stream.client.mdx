---
title: "StreamClient"
sidebarTitle: "StreamClient"
---

此模块提供一个用于与 X API 流式端点交互的客户端。
实时流式操作会返回生成器，在数据到达时持续产出数据。
流式连接由客户端自动管理，并使用指数退避重试逻辑以提升健壮性。

<div id="streamclient">
  ## StreamClient
</div>

<Badge color="blue">类</Badge>

<Badge color="gray">基类：object</Badge>

用于流操作的流式 Client

<div id="constructors">
  ## 构造函数
</div>

<div id="__init__">
  ### `__init__`
</div>

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.client" type="Client"></ParamField>

<div id="get_rule_counts">
  ### `get_rule_counts`
</div>

获取过滤流规则数量
获取过滤流当前生效规则集中的规则数量。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.rules_count_fields" type="List or None" default="None">
要显示的 RulesCount 字段的逗号分隔列表。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`GetRuleCountsResponse` - 响应数据

<div id="get_rules">
  ### `get_rules`
</div>

获取流规则
检索过滤流当前生效的全部规则或其中一部分。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.ids" type="List or None" default="None">
以逗号分隔的规则 ID 列表。
</ParamField>

<ParamField path="path.max_results" type="int or None" default="None">
返回结果的最大数量。
</ParamField>

<ParamField path="path.pagination_token" type="str or None" default="None">
通过传入请求中返回的 `next_token` 值来设置该字段，用于在结果中进行分页。
</ParamField>

<div id="returns">
  #### 返回值
</div>

`IteratorGetRulesResponse`

<div id="labels_compliance">
  ### `labels_compliance`
</div>

帖子标签流（Streaming）
以流式方式传输应用于帖子的所有标签事件。
这是一个流式端点，会在数据可用时实时输出数据。
每个输出项表示来自该流的一个数据点。
连接由带有指数退避的重试逻辑自动管理。
如果流断开连接，SDK 会在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
请求的回填时长（分钟）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子标签的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子标签的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
用于自定义重试行为、超时和回调的可选 StreamConfig。
</ParamField>

<div id="likes_compliance">
  ### `likes_compliance`
</div>

流式传输点赞合规数据（Streaming）
为用户流式传输所有与点赞相关的合规数据。
这是一个流式端点，会在数据可用时实时输出。
每个输出项代表流中的一个独立数据点。
连接由带指数退避的重试逻辑自动管理。
如果流断开，SDK 会在无需客户端额外操作的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
请求回填的数据时长（分钟）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供 Likes 合规事件的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供 Likes 合规事件的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
可选的 StreamConfig，用于自定义重试行为、超时和回调。
</ParamField>

<div id="likes_firehose">
  ### `likes_firehose`
</div>

流式获取所有点赞（Streaming）
以实时方式流式获取所有公开点赞。
这是一个流式端点，会在数据可用时实时返回数据。
每个返回的元素代表流中的一个数据点。
连接由带指数退避重试逻辑的机制自动管理。
如果流连接中断，SDK 将在无需 Client 干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
分区号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
请求回填的分钟数。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。用于提供点赞数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。用于提供帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.like_with_tweet_author_fields" type="List or None" default="None">
要显示的 LikeWithTweetAuthor 字段的以逗号分隔的列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要扩展的字段的以逗号分隔的列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的 User 字段的以逗号分隔的列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的推文字段的以逗号分隔的列表。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
可选的 StreamConfig，用于自定义重试行为、超时和回调。
</ParamField>

<div id="likes_sample10">
  ### `likes_sample10`
</div>

抽样 Likes 流（流式传输）
以实时方式提供 10% 的公开 Likes 抽样数据流。
这是一个流式端点，会在数据可用时实时返回数据。
每个返回的条目代表流中的一个独立数据点。
连接由带指数退避的重试逻辑自动管理。
如果流断开，SDK 会在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
分区编号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
请求的回填时长（分钟）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供点赞数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.like_with_tweet_author_fields" type="List or None" default="None">
要显示的 LikeWithTweetAuthor 字段，用逗号分隔的列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要展开的字段，用逗号分隔的列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的 User 字段，用逗号分隔的列表。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的 Tweet 字段，用逗号分隔的列表。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
用于自定义重试行为、超时时间和回调的可选 StreamConfig。
</ParamField>

<div id="posts">
  ### `posts`
</div>

按规则过滤的帖子流（流式）

以实时方式流式传输与当前规则集匹配的帖子。
这是一个流式端点，会在数据可用时实时输出数据。
每个输出的条目代表流中的单个数据点。
连接由指数退避重试逻辑自动管理。
如果流断开，SDK 将在无需 Client 参与的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
请求回填的分钟数。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。开始提供帖子数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。结束提供帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的地点字段列表。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
可选的 StreamConfig，用于自定义重试行为、超时时间和回调。
</ParamField>

<div id="posts_compliance">
  ### `posts_compliance`
</div>

流式获取帖子合规数据（Streaming）
流式返回与帖子相关的所有合规数据。
这是一个流式端点，会在数据可用时实时输出数据。
每个输出的条目代表流中的一个数据点。
连接由带指数退避的重试逻辑自动管理。
如果流断开，SDK 会在无需开发者干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
分区编号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
请求回填的数据时长（分钟）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。开始提供帖子合规事件的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子合规事件的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
可选的 StreamConfig，用于自定义重试行为、超时时间和回调。
</ParamField>

<div id="posts_firehose">
  ### `posts_firehose`
</div>

流式获取所有帖子（Streaming）
以实时方式流式获取所有公开帖子。
这是一个流式端点，会在数据可用时实时输出数据。
每个输出的条目代表来自该流的一个数据点。
连接由带指数退避的重试逻辑自动管理。
如果流断开连接，SDK 会在无需 Client 干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
分区编号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
请求回填的分钟数。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
格式为 YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
格式为 YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表，用于指定要展示的字段。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的字段列表，用于指定要展开的字段。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的媒体字段列表，用于指定要展示的字段。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的投票字段列表，用于指定要展示的字段。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的用户字段列表，用于指定要展示的字段。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的地点字段列表，用于指定要展示的字段。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
可选的 StreamConfig，用于自定义重试行为、超时和回调。
</ParamField>

<div id="posts_firehose_en">
  ### `posts_firehose_en`
</div>

流式获取英文帖子（Streaming）
实时流式传输所有公开的英文帖子。
这是一个流式端点，会在数据可用时实时输出数据。
每个输出项代表流中的单个数据点。
连接由带有指数退避的重试逻辑自动管理。
如果流断开，SDK 将在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
分区号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
请求回填的分钟数。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。返回帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要显示的媒体字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要显示的投票字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的用户字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要显示的地点字段列表。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
可选的 StreamConfig，用于自定义重试行为、超时和回调。
</ParamField>

<div id="posts_firehose_ja">
  ### `posts_firehose_ja`
</div>

流式传输日语帖子（Streaming）
实时流式传输所有公开的日语帖子。
这是一个在数据可用时实时返回数据的流式端点。
每个返回的条目代表来自该流的一个数据点。
连接由带指数退避的重试逻辑自动管理。
如果流断开，SDK 会在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
分区编号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
请求回补的分钟数。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的要显示的推文字段列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的要展开的字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的要显示的 Media 字段列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的要显示的 Poll 字段列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的要显示的 User 字段列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的要显示的 Place 字段列表。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
可选的 StreamConfig，用于自定义重试行为、超时和回调。
</ParamField>

<div id="posts_firehose_ko">
  ### `posts_firehose_ko`
</div>

流式获取韩语帖子（Streaming）
实时流式获取所有公开的韩语帖子。
这是一个流式端点，在数据可用时会实时返回数据。
每个返回的条目代表该流中的一个数据点。
连接通过指数退避重试策略自动管理。
如果流断开，SDK 将在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
分区号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
请求回填的数据时长（分钟）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。提供帖子数据的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表，用于展示。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的待展开字段列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的 Media 字段列表，用于展示。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的 Poll 字段列表，用于展示。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的 User 字段列表，用于展示。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的 Place 字段列表，用于展示。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
可选的 StreamConfig，用于自定义重试行为、超时时间和回调。
</ParamField>

<div id="posts_firehose_pt">
  ### `posts_firehose_pt`
</div>

流式获取葡萄牙语帖子（Streaming）
实时流式返回所有公开的葡萄牙语帖子。
这是一个流式端点，会在数据可用时实时返回数据。
每个返回的条目代表流中的单个数据点。
连接由指数退避重试逻辑自动管理。
如果流断开，SDK 将在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
分区编号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
请求的回填时长（分钟）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。最早提供帖子内容的 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。最晚提供帖子内容的 UTC 时间戳。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的推文字段的逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要展开的字段的逗号分隔列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
要显示的媒体字段的逗号分隔列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
要显示的投票字段的逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的用户字段的逗号分隔列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
要显示的地点字段的逗号分隔列表。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
用于自定义重试行为、超时和回调的可选 StreamConfig。
</ParamField>

<div id="posts_sample">
  ### `posts_sample`
</div>

采样帖子流式传输（Streaming）
实时传输 1% 的公开帖子样本。
这是一个流式端点，在数据可用时立即返回数据。
每个返回项都代表流中的一个数据点。
连接由带有指数退避的重试逻辑自动管理。
如果流断开，SDK 会在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
请求回填的分钟数。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
以逗号分隔的推文字段列表，用于指定要返回的推文字段。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
以逗号分隔的字段列表，用于指定要展开的字段。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
以逗号分隔的 Media 字段列表，用于指定要返回的 Media 字段。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
以逗号分隔的 Poll 字段列表，用于指定要返回的 Poll 字段。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
以逗号分隔的 User 字段列表，用于指定要返回的 User 字段。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
以逗号分隔的 Place 字段列表，用于指定要返回的 Place 字段。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
可选的 StreamConfig，用于自定义重试行为、超时时间和回调。
</ParamField>

<div id="posts_sample10">
  ### `posts_sample10`
</div>

流式获取 10% 抽样帖子（Streaming）
实时流式返回 10% 抽样的公开帖子。
这是一个流式端点，会在数据可用时实时返回数据。
每个返回项表示来自该流的单个数据点。
连接的管理采用指数退避的重试逻辑自动完成。
如果流中断，SDK 会在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
分区编号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
请求回填的数据时长（分钟）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。将开始提供帖子内容的最早 UTC 时间戳。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。将停止提供帖子内容的最晚 UTC 时间戳。
</ParamField>

<ParamField path="path.tweet_fields" type="List or None" default="None">
要显示的推文字段，逗号分隔列表。
</ParamField>

<ParamField path="path.expansions" type="List or None" default="None">
要展开的字段，逗号分隔列表。
</ParamField>

<ParamField path="path.media_fields" type="List or None" default="None">
要显示的 Media 字段，逗号分隔列表。
</ParamField>

<ParamField path="path.poll_fields" type="List or None" default="None">
要显示的 Poll 字段，逗号分隔列表。
</ParamField>

<ParamField path="path.user_fields" type="List or None" default="None">
要显示的 User 字段，逗号分隔列表。
</ParamField>

<ParamField path="path.place_fields" type="List or None" default="None">
要显示的 Place 字段，逗号分隔列表。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
用于自定义重试行为、超时和回调的可选 StreamConfig。
</ParamField>

<div id="update_rules">
  ### `update_rules`
</div>

更新流的规则
在过滤流的当前规则集中添加或删除规则。

<div id="parameters">
  #### 参数
</div>

<ParamField path="body.body" type="UpdateRulesRequest">
请求正文
</ParamField>

<div id="users_compliance">
  ### `users_compliance`
</div>

流式获取用户合规数据（Streaming）
流式返回与用户相关的所有合规数据。
这是一个流式端点，会在数据可用时实时输出数据。
每条输出的数据代表该流中的一个数据点。
连接由指数退避重试逻辑自动管理。
如果流断开，SDK 会在无需客户端干预的情况下自动重新连接。

<div id="parameters">
  #### 参数
</div>

<ParamField path="path.partition" type="int">
分区号。
</ParamField>

<ParamField path="path.backfill_minutes" type="int or None" default="None">
请求的回填时长（分钟）。
</ParamField>

<ParamField path="path.start_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。将从该最早的 UTC 时间戳开始提供 User Compliance 事件。
</ParamField>

<ParamField path="path.end_time" type="str or None" default="None">
YYYY-MM-DDTHH:mm:ssZ。将提供截至该最晚 UTC 时间戳的 User Compliance 事件。
</ParamField>

<ParamField path="path.stream_config" type="StreamConfig">
用于自定义重试行为、超时和回调的可选 StreamConfig。
</ParamField>