---
title: 分页
sidebarTitle: 分页
description: 浏览大型结果集
keywords: ["分页", "分页令牌", "下一页令牌", "按页浏览结果", "游标分页"]
---

当 API 响应包含的结果多于一次可返回的数量时，请使用分页来检索所有分页数据。

***

<div id="how-pagination-works">
  ## 分页的工作原理
</div>

1. 使用 `max_results` 发起初始请求
2. 在 `meta` 对象中检查响应中是否包含 `next_token`
3. 如果存在，使用该令牌作为 `pagination_token` 再次发起请求
4. 重复此过程，直到不再返回 `next_token`

```bash
# 初始请求
curl "https://api.x.com/2/users/12345/tweets?max_results=100" \
  -H "Authorization: Bearer $TOKEN"

# 响应包含 next_token
# {"data": [...], "meta": {"next_token": "abc123", ...}}

# 下一页
curl "https://api.x.com/2/users/12345/tweets?max_results=100&pagination_token=abc123" \
  -H "Authorization: Bearer $TOKEN"
```

***

<div id="pagination-tokens">
  ## 分页令牌
</div>

| Token | Description |
|:------|:------------|
| `next_token` | 位于响应的 `meta` 对象中。用于获取下一页。 |
| `previous_token` | 位于响应的 `meta` 对象中。用于返回上一页。 |
| `pagination_token` | 请求参数。设置为 `next_token` 或 `previous_token` 的值。 |

***

<div id="response-structure">
  ## 响应结构
</div>

```json
{
  "data": [
    {"id": "1234", "text": "..."},
    {"id": "1235", "text": "..."}
  ],
  "meta": {
    "result_count": 100,
    "next_token": "7140w9gefhslx3",
    "previous_token": "77qp89slxjd"
  }
}
```

当没有更多结果时，`next_token` 会被省略：

```json
{
  "data": [...],
  "meta": {
    "result_count": 42,
    "previous_token": "77qp89abc"
  }
}
```

***

<div id="pagination-parameters">
  ## 分页参数
</div>

| 参数 | 描述 | 默认值 |
|:----------|:------------|:--------|
| `max_results` | 每页返回的结果数 | 取决于具体端点 |
| `pagination_token` | 上一个响应返回的令牌 | 无 |

请查看各端点的 API 参考，了解各自的 `max_results` 限制。

***

<div id="example-paginating-through-all-results">
  ## 示例：分页遍历所有结果
</div>

<Tabs>
  <Tab title="Python">
    ```python
    import requests

    def get_all_tweets(user_id, bearer_token):
        url = f"https://api.x.com/2/users/{user_id}/tweets"
        headers = {"Authorization": f"Bearer {bearer_token}"}
        params = {"max_results": 100}
        
        all_tweets = []
        
        while True:
            response = requests.get(url, headers=headers, params=params)
            data = response.json()
            
            if "data" in data:
                all_tweets.extend(data["data"])
            
            # 检查是否有下一页
            next_token = data.get("meta", {}).get("next_token")
            if not next_token:
                break
                
            params["pagination_token"] = next_token
        
        return all_tweets
    ```
  </Tab>

  <Tab title="JavaScript">
    ```javascript
    async function getAllTweets(userId, bearerToken) {
      const url = `https://api.x.com/2/users/${userId}/tweets`;
      const headers = { Authorization: `Bearer ${bearerToken}` };
      
      let allTweets = [];
      let paginationToken = null;
      
      do {
        const params = new URLSearchParams({ max_results: 100 });
        if (paginationToken) {
          params.set("pagination_token", paginationToken);
        }
        
        const response = await fetch(`${url}?${params}`, { headers });
        const data = await response.json();
        
        if (data.data) {
          allTweets.push(...data.data);
        }
        
        paginationToken = data.meta?.next_token;
      } while (paginationToken);
      
      return allTweets;
    }
    ```
  </Tab>
</Tabs>

***

<div id="best-practices">
  ## 最佳实践
</div>

<CardGroup cols={2}>
  <Card title="使用最大返回数量" icon="arrow-up-1-9">
    请求允许的最大 `max_results` 值，以减少 API 调用次数。
  </Card>

  <Card title="处理不完整页" icon="square-check">
    最后一页的结果数可能少于 `max_results`。
  </Card>

  <Card title="存储令牌" icon="database">
    如果需要稍后恢复分页，请保存 `next_token`。
  </Card>

  <Card title="不要通过分页轮询" icon="clock">
    对于新增数据，请使用 `since_id`，而不是反复进行分页。
  </Card>
</CardGroup>

***

<div id="result-ordering">
  ## 结果排序
</div>

结果按**时间倒序**返回：

* 第一页的第一个结果 = 最新
* 最后一页的最后一个结果 = 最早

此规则适用于单页内以及跨页的结果。

***

<div id="notes">
  ## 注意事项
</div>

* 分页 token 是不透明的字符串——不要尝试解析或修改它们
* token 可能在一段时间后失效
* 即使返回的结果少于 `max_results`，仍可能存在更多结果（继续请求，直到不再返回 `next_token`）
* 使用 [SDK](/zh/x-api/tools-and-libraries/sdks) 来自动处理分页

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="访问频率限制" icon="gauge-high" href="/zh/x-api/fundamentals/rate-limits">
    了解分页时的请求限制。
  </Card>

  <Card title="SDKs" icon="cube" href="/zh/x-api/tools-and-libraries/sdks">
    包含内置分页功能的库。
  </Card>
</CardGroup>