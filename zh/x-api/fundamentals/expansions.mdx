---
title: Expansions
sidebarTitle: Expansions
description: 在 API 响应中包含相关对象
keywords: ["expansions", "扩展对象", "对象扩展", "嵌套对象", "相关对象"]
---

Expansions 使你能够在单个 API 响应中包含相关对象。你无需发起多次请求，就可以在一次调用中同时获取帖子及其作者、媒体或其引用的帖子。

***

<div id="how-expansions-work">
  ## 扩展的工作原理
</div>

当你请求扩展时，API 会在响应的 `includes` 部分中返回完整的对象：

```bash
curl "https://api.x.com/2/tweets/1234567890?expansions=author_id" \
  -H "Authorization: Bearer $TOKEN"
```

响应：

```json
{
  "data": {
    "id": "1234567890",
    "text": "Hello world!",
    "author_id": "2244994945"
  },
  "includes": {
    "users": [{
      "id": "2244994945",
      "name": "X Developers",
      "username": "xdevelopers"
    }]
  }
}
```

`data` 中的 `author_id` 指向 `includes` 中的用户对象。

***

<div id="post-expansions">
  ## 帖子扩展
</div>

| Expansion | 返回内容 | 使用场景 |
|:----------|:--------|:---------|
| `author_id` | User 对象 | 获取帖子作者的详细信息 |
| `referenced_tweets.id` | 帖子对象 | 获取被引用/被回复的帖子 |
| `referenced_tweets.id.author_id` | User 对象 | 获取被引用/被回复帖子的作者 |
| `in_reply_to_user_id` | User 对象 | 获取被回复的用户 |
| `attachments.media_keys` | Media 对象 | 获取图片、视频、GIF |
| `attachments.poll_ids` | 投票对象 | 获取投票选项和票数 |
| `geo.place_id` | Place 对象 | 获取位置详情 |
| `entities.mentions.username` | User 对象 | 获取被提及的用户 |
| `edit_history_tweet_ids` | 帖子对象 | 获取已编辑帖子的历史版本 |

***

<div id="user-expansions">
  ## 用户扩展
</div>

| 扩展 | 返回 | 使用场景 |
|:----------|:--------|:---------|
| `pinned_tweet_id` | 帖子对象 | 获取用户的置顶帖子 |

***

<div id="space-expansions">
  ## Space 扩展
</div>

| 扩展 | 返回 | 使用场景 |
|:----------|:--------|:---------|
| `creator_id` | User 对象 | 获取 Space 创建者 |
| `host_ids` | User 对象 | 获取 Space 主持人 |
| `speaker_ids` | User 对象 | 获取 Space 发言者 |
| `invited_user_ids` | User 对象 | 获取受邀用户 |

***

<div id="dm-expansions">
  ## DM 扩展
</div>

| Expansion | 返回 | 用例 |
|:----------|:--------|:---------|
| `sender_id` | User 对象 | 获取消息发送者 |
| `participant_ids` | User 对象 | 获取会话参与者 |
| `attachments.media_keys` | Media 对象 | 获取附带媒体 |
| `referenced_tweets.id` | Post 对象 | 获取被引用的帖子 |

***

<div id="list-expansions">
  ## 列表扩展
</div>

| 扩展项 | 返回值 | 用例 |
|:----------|:--------|:---------|
| `owner_id` | 用户对象 | 获取列表所有者 |

***

<div id="combining-with-fields">
  ## 与字段结合使用
</div>

Expansions 会为每个对象返回默认字段。要获取额外字段，请将 Expansions 与字段参数结合使用：

```bash
curl "https://api.x.com/2/tweets/1234567890?\
expansions=author_id,attachments.media_keys&\
user.fields=description,public_metrics&\
media.fields=url,alt_text" \
  -H "Authorization: Bearer $TOKEN"
```

响应：

```json
{
  "data": {
    "id": "1234567890",
    "text": "Check out this image!",
    "author_id": "2244994945",
    "attachments": {
      "media_keys": ["3_1234567890"]
    }
  },
  "includes": {
    "users": [{
      "id": "2244994945",
      "name": "X Developers",
      "username": "xdevelopers",
      "description": "The voice of the X Developer Platform",
      "public_metrics": {
        "followers_count": 570842
      }
    }],
    "media": [{
      "media_key": "3_1234567890",
      "type": "photo",
      "url": "https://pbs.twimg.com/media/example.jpg",
      "alt_text": "Example image"
    }]
  }
}
```

***

<div id="multiple-expansions">
  ## 多个扩展
</div>

请求多个扩展时，将它们写成一个以逗号分隔的列表：

```bash
expansions=author_id,referenced_tweets.id,attachments.media_keys
```

***

<div id="common-patterns">
  ## 常见模式
</div>

<Tabs>
  <Tab title="完整帖子上下文">
    获取同时包含作者、媒体和被引用帖子的帖子：

    ```bash
    expansions=author_id,attachments.media_keys,referenced_tweets.id
    tweet.fields=created_at,public_metrics,conversation_id
    user.fields=username,name,profile_image_url
    media.fields=url,preview_image_url,type
    ```
  </Tab>

  <Tab title="会话线程">
    获取回复及其作者：

    ```bash
    expansions=author_id,in_reply_to_user_id,referenced_tweets.id
    tweet.fields=conversation_id,in_reply_to_user_id,created_at
    user.fields=username,name
    ```
  </Tab>

  <Tab title="带置顶帖的用户">
    获取包含其置顶帖的用户资料：

    ```bash
    expansions=pinned_tweet_id
    user.fields=description,public_metrics,verified
    tweet.fields=created_at,public_metrics
    ```
  </Tab>
</Tabs>

***

<div id="linking-data-and-includes">
  ## 关联 data 和 includes
</div>

`includes` 中的对象不包含位置信息。请使用 id 将它们关联起来：

```python
# Python 示例
response = api_call()
post = response["data"]
users = {u["id"]: u for u in response["includes"]["users"]}

# Get the author
author = users.get(post["author_id"])
print(f"{author['name']} said: {post['text']}")
```

```javascript
// JavaScript 示例
const { data: post, includes } = response;
const users = Object.fromEntries(
  includes.users.map(u => [u.id, u])
);

const author = users[post.author_id];
console.log(`${author.name} said: ${post.text}`);
```

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="字段" icon="list" href="/zh/x-api/fundamentals/fields">
    为每个对象请求特定字段。
  </Card>

  <Card title="数据字典" icon="book" href="/zh/x-api/fundamentals/data-dictionary">
    完整的对象架构定义。
  </Card>
</CardGroup>