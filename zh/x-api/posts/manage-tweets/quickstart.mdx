---
title: 快速入门
sidebarTitle: 快速入门
description: 使用 X API 创建和删除帖子
keywords: ["管理推文快速入门", "发布推文快速入门", "创建推文教程", "推文创建指南", "发布推文示例"]
---

import { Button } from '/snippets/zh/button.mdx';

本指南将引导你使用 X API 创建和删除帖子。

<Note>
  **前提条件**

  在开始之前，你需要：

  * 一个拥有已获批准 App 的 [开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 用户访问令牌（OAuth 1.0a 或 OAuth 2.0 PKCE）
</Note>

***

<div id="create-a-post">
  ## 创建一条帖子
</div>

<Steps>
  <Step title="准备请求">
    `POST` `/2/tweets` 端点需要一个 JSON 请求体，其中至少包含 `text` 或 `media` 字段：

    ```json
    {
      "text": "Hello from the X API!"
    }
    ```
  </Step>

  <Step title="发送请求">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/tweets" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"text": "Hello from the X API!"}'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # 创建帖子
      response = client.posts.create(text="Hello from the X API!")
      print(f"Created Post: {response.data.id}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // 创建帖子
      const response = await client.posts.create({ text: "Hello from the X API!" });
      console.log(`Created Post: ${response.data?.id}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="查看响应">
    成功的响应包含新帖子的 `id` 和 `text`：

    ```json
    {
      "data": {
        "id": "1445880548472328192",
        "text": "Hello from the X API!"
      }
    }
    ```
  </Step>
</Steps>

***

<div id="advanced-examples">
  ## 高级示例
</div>

<AccordionGroup>
  <Accordion title="回复帖子">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/tweets" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{
          "text": "This is a reply!",
          "reply": {
            "in_reply_to_tweet_id": "1234567890"
          }
        }'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # 创建一条回复
      response = client.posts.create(
          text="This is a reply!",
          reply={"in_reply_to_tweet_id": "1234567890"}
      )
      print(f"Created reply: {response.data.id}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // 创建一条回复
      const response = await client.posts.create({
        text: "This is a reply!",
        reply: { inReplyToTweetId: "1234567890" },
      });
      console.log(`Created reply: ${response.data?.id}`);
      ```
    </CodeGroup>
  </Accordion>

  <Accordion title="引用帖子">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/tweets" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{
          "text": "来看看这个！",
          "quote_tweet_id": "1234567890"
        }'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # 引用一条帖子
      response = client.posts.create(
          text="来看看这个！",
          quote_tweet_id="1234567890"
      )
      print(f"已创建的引用：{response.data.id}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // 引用一条帖子
      const response = await client.posts.create({
        text: "来看看这个！",
        quoteTweetId: "1234567890",
      });
      console.log(`已创建的引用：${response.data?.id}`);
      ```
    </CodeGroup>
  </Accordion>

  <Accordion title="包含媒体的帖子">
    首先，使用 [媒体上传端点](/zh/x-api/media/quickstart/media-upload-chunked) 上传媒体，然后引用 `media_id`：

    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/tweets" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{
          "text": "今日照片！",
          "media": {
            "media_ids": ["1234567890123456789"]
          }
        }'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # 带媒体的帖子
      response = client.posts.create(
          text="今日照片！",
          media={"media_ids": ["1234567890123456789"]}
      )
      print(f"已创建带媒体的帖子: {response.data.id}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // 带媒体的帖子
      const response = await client.posts.create({
        text: "今日照片！",
        media: { mediaIds: ["1234567890123456789"] },
      });
      console.log(`已创建带媒体的帖子: ${response.data?.id}`);
      ```
    </CodeGroup>
  </Accordion>

  <Accordion title="含投票的帖子">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/tweets" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{
          "text": "你最喜欢的颜色是什么？",
          "poll": {
            "options": ["红色", "蓝色", "绿色", "黄色"],
            "duration_minutes": 1440
          }
        }'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # 包含投票的帖子
      response = client.posts.create(
          text="你最喜欢的颜色是什么？",
          poll={"options": ["红色", "蓝色", "绿色", "黄色"], "duration_minutes": 1440}
      )
      print(f"已创建的投票：{response.data.id}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // 包含投票的帖子
      const response = await client.posts.create({
        text: "你最喜欢的颜色是什么？",
        poll: { options: ["红色", "蓝色", "绿色", "黄色"], durationMinutes: 1440 },
      });
      console.log(`已创建的投票：${response.data?.id}`);
      ```
    </CodeGroup>
  </Accordion>
</AccordionGroup>

***

<div id="delete-a-post">
  ## 删除帖子
</div>

<Steps>
  <Step title="获取帖子 ID">
    你需要想要删除的帖子的 ID。创建帖子时会返回该 ID。
  </Step>

  <Step title="发送 DELETE 请求">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X DELETE "https://api.x.com/2/tweets/1445880548472328192" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # 删除帖子
      response = client.posts.delete("1445880548472328192")
      print(f"Deleted: {response.data.deleted}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // 删除帖子
      const response = await client.posts.delete("1445880548472328192");
      console.log(`Deleted: ${response.data?.deleted}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="确认删除">
    ```json
    {
      "data": {
        "deleted": true
      }
    }
    ```
  </Step>
</Steps>

<Warning>
  你只能删除自己发布的帖子。
</Warning>

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="集成指南" icon="book" href="/zh/x-api/posts/manage-tweets/integrate">
    关键概念和最佳实践
  </Card>

  <Card title="媒体上传" icon="image" href="/zh/x-api/media/quickstart/media-upload-chunked">
    为帖子上传媒体内容
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/posts/create-post">
    完整的端点参考文档
  </Card>

  <Card title="示例代码" icon="github" href="https://github.com/xdevplatform/Twitter-API-v2-sample-code">
    可运行的代码示例
  </Card>
</CardGroup>