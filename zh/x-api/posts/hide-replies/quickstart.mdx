---
title: 快速入门
sidebarTitle: 快速入门
description: 隐藏和重新显示你帖子下的回复
keywords: ["隐藏回复快速入门", "隐藏回复教程", "审核回复指南"]
---

import { Button } from '/snippets/zh/button.mdx';

本指南将引导你完成在你发起的对话中隐藏和取消隐藏帖子回复的操作。

<Note>
  **先决条件**

  在开始之前，你需要：

  * 一个包含已获批准应用的[开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 用户访问令牌（OAuth 1.0a 或 OAuth 2.0 PKCE）
</Note>

***

<div id="hide-a-reply">
  ## 隐藏回复
</div>

<Steps>
  <Step title="查找该回复的帖子 ID" icon="message">
    获取你想要隐藏的回复的 ID。你只能隐藏由当前已认证用户发起的会话中的回复。

    ```
    https://x.com/user/status/1232720193182412800
                              └── 这是帖子 ID
    ```
  </Step>

  <Step title="发送隐藏请求" icon="eye-slash">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X PUT "https://api.x.com/2/tweets/1232720193182412800/hidden" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"hidden": true}'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # 隐藏回复
      response = client.posts.hide_reply("1232720193182412800", hidden=True)
      print(f"Hidden: {response.data.hidden}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // 隐藏回复
      const response = await client.posts.hideReply("1232720193182412800", {
        hidden: true,
      });
      console.log(`Hidden: ${response.data?.hidden}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="确认回复已被隐藏" icon="check">
    ```json
    {
      "data": {
        "hidden": true
      }
    }
    ```

    该回复现在已从主会话视图中隐藏。用户仍然可以通过点击“View hidden replies”来查看它。
  </Step>
</Steps>

***

<div id="unhide-a-reply">
  ## 取消隐藏回复
</div>

要使已隐藏的回复重新可见：

<CodeGroup dropdown>
  ```bash cURL
  curl -X PUT "https://api.x.com/2/tweets/1232720193182412800/hidden" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"hidden": false}'
  ```

  ```python Python SDK
  from xdk import Client
  from xdk.oauth1_auth import OAuth1

  oauth1 = OAuth1(
      api_key="YOUR_API_KEY",
      api_secret="YOUR_API_SECRET",
      access_token="YOUR_ACCESS_TOKEN",
      access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
  )

  client = Client(auth=oauth1)

  # Unhide a reply
  response = client.posts.hide_reply("1232720193182412800", hidden=False)
  print(f"Hidden: {response.data.hidden}")
  ```

  ```javascript JavaScript SDK
  import { Client, OAuth1 } from "@xdevplatform/xdk";

  const oauth1 = new OAuth1({
    apiKey: "YOUR_API_KEY",
    apiSecret: "YOUR_API_SECRET",
    accessToken: "YOUR_ACCESS_TOKEN",
    accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
  });

  const client = new Client({ oauth1 });

  // Unhide a reply
  const response = await client.posts.hideReply("1232720193182412800", {
    hidden: false,
  });
  console.log(`Hidden: ${response.data?.hidden}`);
  ```
</CodeGroup>

**响应：**

```json
{
  "data": {
    "hidden": false
  }
}
```

***

<div id="important-notes">
  ## 重要说明
</div>

<Note>
  * 你只能隐藏**你发起的**会话中的回复
  * 被隐藏的回复仍然可以通过“查看被隐藏的回复”查看
  * 当某条回复被隐藏时，其作者不会收到任何通知
</Note>

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="按主题管理" icon="tags" href="/zh/x-api/posts/hide-replies/integrate/manage-replies-by-topic">
    根据内容审核回复
  </Card>

  <Card title="实时审核" icon="bolt" href="/zh/x-api/posts/hide-replies/integrate/manage-replies-in-realtime">
    在回复出现时实时审核
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/posts/hide-reply">
    完整的端点说明文档
  </Card>
</CardGroup>