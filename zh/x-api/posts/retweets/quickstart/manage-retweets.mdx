---
title: 管理转推
sidebarTitle: 管理转推
description: 使用 X API 转推和取消转推
keywords: ["管理转推", "转推帖子", "取消转推", "转推快速入门", "转推教程", "转推指南"]
---

import { Button } from '/snippets/zh/button.mdx';

本指南将带你逐步完成如何使用 X API 进行转推以及撤销转推。

<Note>
  **前提条件**

  在开始之前，你需要：

  * 一个拥有已获批准 App 的[开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 用户访问令牌（OAuth 1.0a 或 OAuth 2.0 PKCE）
</Note>

***

<div id="retweet-a-post">
  ## 转发帖子
</div>

<Steps>
  <Step title="获取你的用户 ID">
    你需要已认证用户的 ID。你可以使用 [用户查询接口](/zh/x-api/users/lookup/introduction) 获取，或者从你的 Access Token 中找到（其中的数字部分就是你的用户 ID）。
  </Step>

  <Step title="获取帖子 ID">
    当你查看帖子时，可以在 URL 中找到帖子 ID：

    ```
    https://x.com/XDevelopers/status/1228393702244134912
                                    └── 这里是帖子 ID
    ```
  </Step>

  <Step title="发送转发请求">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/users/123456789/retweets" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"tweet_id": "1228393702244134912"}'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # 转发帖子
      response = client.posts.retweet(
          user_id="123456789",
          tweet_id="1228393702244134912"
      )

      print(f"Retweeted: {response.data.retweeted}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // 转发帖子
      const response = await client.posts.retweet("123456789", {
        tweetId: "1228393702244134912",
      });

      console.log(`Retweeted: ${response.data?.retweeted}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="查看响应">
    ```json
    {
      "data": {
        "retweeted": true
      }
    }
    ```
  </Step>
</Steps>

***

<div id="undo-a-retweet">
  ## 撤销转推
</div>

取消一条转推：

<CodeGroup dropdown>
  ```bash cURL
  curl -X DELETE "https://api.x.com/2/users/123456789/retweets/1228393702244134912" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client
  from xdk.oauth1_auth import OAuth1

  oauth1 = OAuth1(
      api_key="YOUR_API_KEY",
      api_secret="YOUR_API_SECRET",
      access_token="YOUR_ACCESS_TOKEN",
      access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
  )

  client = Client(auth=oauth1)

  # 撤销转推
  response = client.posts.unretweet(
      user_id="123456789",
      tweet_id="1228393702244134912"
  )

  print(f"Retweeted: {response.data.retweeted}")
  ```

  ```javascript JavaScript SDK
  import { Client, OAuth1 } from "@xdevplatform/xdk";

  const oauth1 = new OAuth1({
    apiKey: "YOUR_API_KEY",
    apiSecret: "YOUR_API_SECRET",
    accessToken: "YOUR_ACCESS_TOKEN",
    accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
  });

  const client = new Client({ oauth1 });

  // 撤销转推
  const response = await client.posts.unretweet("123456789", "1228393702244134912");

  console.log(`Retweeted: ${response.data?.retweeted}`);
  ```
</CodeGroup>

**响应：**

```json
{
  "data": {
    "retweeted": false
  }
}
```

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="转推查询" icon="retweet" href="/zh/x-api/posts/retweets/quickstart/retweets-lookup">
    获取转推了某条帖子的用户
  </Card>

  <Card title="引用帖子" icon="quote-left" href="/zh/x-api/posts/quote-tweets/quickstart">
    获取引用的帖子
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/posts/retweet-a-post">
    完整的端点文档
  </Card>
</CardGroup>