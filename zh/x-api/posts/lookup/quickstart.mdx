---
title: 快速开始
sidebarTitle: 快速开始
description: 在几分钟内发出你的首个帖子查询请求
keywords: ["帖子查询快速入门", "推文查询快速入门", "查询教程", "查询指南", "获取推文快速入门", "查询示例"]
---

import { Button } from "/snippets/zh/button.mdx"

本指南将引导你使用 X API v2 发起第一个帖子查询请求。

<Note>
  **前置条件**

  在开始之前，你需要：

  * 一个拥有已获批准应用的[开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 你的应用的 Bearer Token（可在开发者控制台的“Keys and tokens”下找到）
</Note>

***

<Steps>
  <Step title="查找帖子 ID">
    每条帖子都有唯一的 id。你可以在该帖子的 URL 中找到它：

    ```
    https://x.com/XDevelopers/status/1228393702244134912
                                    └── 这是帖子 ID
    ```
  </Step>

  <Step title="发起请求">
    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/tweets/1228393702244134912" \
        -H "Authorization: Bearer $BEARER_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # 根据 ID 获取单个帖子
      response = client.posts.get("1228393702244134912")
      print(response.data)
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      const response = await client.posts.get("1228393702244134912");
      console.log(response.data);
      ```
    </CodeGroup>
  </Step>

  <Step title="查看响应">
    默认响应会包含该帖子的 `id`、`text` 和 `edit_history_tweet_ids`：

    ```json
    {
      "data": {
        "id": "1228393702244134912",
        "text": "What did the developer write in their Valentine's card?\n\nwhile(true) {\n    I = Love(You);\n}",
        "edit_history_tweet_ids": ["1228393702244134912"]
      }
    }
    ```
  </Step>

  <Step title="请求更多字段">
    使用查询参数获取更多数据：

    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/tweets/1228393702244134912?\
      tweet.fields=created_at,public_metrics,author_id&\
      expansions=author_id&\
      user.fields=username,verified" \
        -H "Authorization: Bearer $BEARER_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # 获取包含额外字段和扩展的帖子
      response = client.posts.get(
          "1228393702244134912",
          tweet_fields=["created_at", "public_metrics", "author_id"],
          expansions=["author_id"],
          user_fields=["username", "verified"]
      )

      print(response.data)
      print(response.includes)  # 包含作者用户对象
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      const response = await client.posts.get("1228393702244134912", {
        tweetFields: ["created_at", "public_metrics", "author_id"],
        expansions: ["author_id"],
        userFields: ["username", "verified"],
      });

      console.log(response.data);
      console.log(response.includes); // 包含作者用户对象
      ```
    </CodeGroup>

    **响应：**

    ```json
    {
      "data": {
        "id": "1228393702244134912",
        "text": "What did the developer write in their Valentine's card?...",
        "created_at": "2020-02-14T19:00:55.000Z",
        "author_id": "2244994945",
        "public_metrics": {
          "retweet_count": 156,
          "reply_count": 23,
          "like_count": 892,
          "quote_count": 12
        },
        "edit_history_tweet_ids": ["1228393702244134912"]
      },
      "includes": {
        "users": [
          {
            "id": "2244994945",
            "username": "XDevelopers",
            "verified": true
          }
        ]
      }
    }
    ```
  </Step>

  <Step title="查询多个帖子">
    在单个请求中最多可获取 100 条帖子：

    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/tweets?\
      ids=1228393702244134912,1227640996038684673,1199786642791452673&\
      tweet.fields=created_at,author_id" \
        -H "Authorization: Bearer $BEARER_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # 通过 ID 获取多条帖子
      response = client.posts.get_posts(
          ids=["1228393702244134912", "1227640996038684673", "1199786642791452673"],
          tweet_fields=["created_at", "author_id"]
      )

      for post in response.data:
          print(f"{post.id}: {post.text[:50]}...")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      const response = await client.posts.getPosts({
        ids: ["1228393702244134912", "1227640996038684673", "1199786642791452673"],
        tweetFields: ["created_at", "author_id"],
      });

      response.data?.forEach((post) => {
        console.log(`${post.id}: ${post.text?.slice(0, 50)}...`);
      });
      ```
    </CodeGroup>
  </Step>
</Steps>

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="集成指南" icon="book" href="/zh/x-api/posts/lookup/integrate">
    了解身份验证、速率限制和最佳实践
  </Card>

  <Card title="字段和扩展" icon="sliders" href="/zh/x-api/fundamentals/fields">
    掌握字段与扩展体系
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/posts/get-post-by-id">
    查看所有可用参数
  </Card>

  <Card title="示例代码" icon="github" href="https://github.com/xdevplatform/Twitter-API-v2-sample-code">
    探索更多示例
  </Card>
</CardGroup>