---
title: 快速入门
sidebarTitle: 快速入门
description: 获取特定帖子的引用帖子
keywords: ["引用推文快速入门", "引用推文教程", "获取引用推文指南"]
---

import { Button } from '/snippets/zh/button.mdx';

本指南将引导你检索引用帖子（Quote Posts，指引用其他帖子的帖子）。

<Note>
  **先决条件**

  在开始之前，你需要：

  * 一个已获批准 App 的[开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 你的 App 的 Bearer 令牌
</Note>

***

<Steps>
  <Step title="查找帖子 ID" icon="message">
    获取你想要查看其引用帖的帖子 ID。你可以在帖子的 URL 中找到它：

    ```
    https://x.com/XDevelopers/status/1409931481552543749
                                    └── 这里是帖子 ID
    ```
  </Step>

  <Step title="请求引用帖子（Quote Posts）" icon="terminal">
    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/tweets/1409931481552543749/quote_tweets?\
      tweet.fields=created_at,public_metrics,author_id&\
      expansions=author_id&\
      user.fields=username,verified&\
      max_results=10" \
        -H "Authorization: Bearer $BEARER_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_BEARER_TOKEN")

      # 使用分页获取引用帖子（Quote Posts）
      for page in client.posts.get_quote_tweets(
          "1409931481552543749",
          tweet_fields=["created_at", "public_metrics", "author_id"],
          expansions=["author_id"],
          user_fields=["username", "verified"],
          max_results=10
      ):
          for post in page.data:
              print(f"{post.text[:50]}... - Likes: {post.public_metrics.like_count}")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

      // 使用分页获取引用帖子（Quote Posts）
      const paginator = client.posts.getQuoteTweets("1409931481552543749", {
        tweetFields: ["created_at", "public_metrics", "author_id"],
        expansions: ["author_id"],
        userFields: ["username", "verified"],
        maxResults: 10,
      });

      for await (const page of paginator) {
        page.data?.forEach((post) => {
          console.log(`${post.text?.slice(0, 50)}... - Likes: ${post.public_metrics?.like_count}`);
        });
      }
      ```
    </CodeGroup>
  </Step>

  <Step title="查看响应" icon="eye">
    ```json
    {
      "data": [
        {
          "id": "1495979553889697792",
          "text": "Great thread on the new API features! https://t.co/...",
          "author_id": "29757971",
          "created_at": "2022-02-22T04:31:34.000Z",
          "public_metrics": {
            "retweet_count": 5,
            "reply_count": 2,
            "like_count": 42,
            "quote_count": 1
          },
          "edit_history_tweet_ids": ["1495979553889697792"]
        }
      ],
      "includes": {
        "users": [
          {
            "id": "29757971",
            "username": "developer",
            "verified": false
          }
        ]
      },
      "meta": {
        "result_count": 1,
        "next_token": "avdjwk0udyx6"
      }
    }
    ```
  </Step>

  <Step title="对结果进行分页" icon="arrow-right">
    SDK 会自动处理分页。对于 cURL，请使用 `next_token` 来获取更多引用帖子（Quote Posts）：

    ```bash
    curl "https://api.x.com/2/tweets/1409931481552543749/quote_tweets?\
    max_results=10&\
    pagination_token=avdjwk0udyx6" \
      -H "Authorization: Bearer $BEARER_TOKEN"
    ```
  </Step>
</Steps>

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="转推" icon="retweet" href="/zh/x-api/posts/retweets/introduction">
    查询转推
  </Card>

  <Card title="帖子查询" icon="magnifying-glass" href="/zh/x-api/posts/lookup/introduction">
    按 id 查询帖子
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/posts/get-quoted-posts">
    完整端点文档
  </Card>
</CardGroup>