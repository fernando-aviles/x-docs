---
title: Full-Archive Search 快速入门
sidebarTitle: 全量归档搜索
description: 搜索自 2006 年以来的全部帖子归档
keywords: ["全量归档搜索", "归档搜索快速入门", "历史搜索", "全量归档教程", "归档搜索指南"]
---

import { Button } from '/snippets/zh/button.mdx';

本指南将引导你完成第一个全量归档搜索请求，从完整的 X 归档（可追溯至 2006 年 3 月）中查找帖子。

<Warning>
  使用全量归档搜索需要 [自助](/zh/x-api/getting-started/about-x-api) 或 [企业级](/zh/x-api/getting-started/about-x-api) 访问权限。[升级你的访问权限](https://developer.x.com/en/portal/products) 以使用此端点。
</Warning>

<Note>
  **先决条件**

  开始之前，你需要：

  * 一个[开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 你的 App 的 Bearer 令牌（可在开发者控制台的 “Keys and tokens” 下找到）
</Note>

***

<div id="step-1-build-a-query">
  ## 步骤 1：构建查询
</div>

全量归档搜索支持所有查询运算符。构建查询的方式与近期搜索相同：

```
from:XDevelopers lang:en
```

<Tip>
  全量归档搜索支持长度最多为 1,024 个字符的查询（企业版为 4,096 个字符）。
</Tip>

***

<div id="step-2-set-a-time-range">
  ## 步骤 2：设置时间范围
</div>

默认情况下，查询会返回最近 30 天内的帖子。使用 `start_time` 和 `end_time` 来搜索特定时间段：

| 参数 | 格式 | 示例 |
|:----------|:-------|:--------|
| `start_time` | ISO 8601 | `2020-01-01T00:00:00Z` |
| `end_time` | ISO 8601 | `2020-12-31T23:59:59Z` |

***

<div id="step-3-make-a-request">
  ## 步骤 3：发起请求
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/tweets/search/all?\
  query=from%3AXDevelopers&\
  start_time=2020-01-01T00%3A00%3A00Z&\
  end_time=2020-12-31T23%3A59%3A59Z&\
  max_results=100" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # 对完整归档进行分页搜索
  for page in client.posts.search_all(
      query="from:XDevelopers",
      start_time="2020-01-01T00:00:00Z",
      end_time="2020-12-31T23:59:59Z",
      max_results=100
  ):
      for post in page.data:
          print(post.text)
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // 对完整归档进行分页搜索
  const paginator = client.posts.searchAll({
    query: "from:XDevelopers",
    startTime: "2020-01-01T00:00:00Z",
    endTime: "2020-12-31T23:59:59Z",
    maxResults: 100,
  });

  for await (const page of paginator) {
    page.data?.forEach((post) => {
      console.log(post.text);
    });
  }
  ```
</CodeGroup>

***

<div id="step-4-review-the-response">
  ## 步骤 4：检查响应
</div>

```json
{
  "data": [
    {
      "id": "1271111223220809728",
      "text": "Tune in tonight and watch as @jessicagarson takes us through...",
      "edit_history_tweet_ids": ["1271111223220809728"]
    },
    {
      "id": "1270799243071062016",
      "text": "As we work towards building the new Twitter API...",
      "edit_history_tweet_ids": ["1270799243071062016"]
    }
  ],
  "meta": {
    "newest_id": "1271111223220809728",
    "oldest_id": "1270799243071062016",
    "result_count": 2
  }
}
```

<Note>
  在编辑功能推出之前（2022 年 9 月）所创建的帖子不会包含 `edit_history_tweet_ids`。
</Note>

***

<div id="step-5-add-fields-and-expansions">
  ## 步骤 5：添加字段和 expansions
</div>

使用查询参数请求更多数据：

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/tweets/search/all?\
  query=from%3AXDevelopers&\
  start_time=2020-01-01T00%3A00%3A00Z&\
  end_time=2020-12-31T23%3A59%3A59Z&\
  tweet.fields=created_at,public_metrics,author_id&\
  expansions=author_id&\
  user.fields=username,description&\
  max_results=100" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # 使用字段和 expansions 进行搜索
  for page in client.posts.search_all(
      query="from:XDevelopers",
      start_time="2020-01-01T00:00:00Z",
      end_time="2020-12-31T23:59:59Z",
      tweet_fields=["created_at", "public_metrics", "author_id"],
      expansions=["author_id"],
      user_fields=["username", "description"],
      max_results=100
  ):
      for post in page.data:
          print(f"{post.created_at}: {post.text[:50]}...")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // 使用字段和 expansions 进行搜索
  const paginator = client.posts.searchAll({
    query: "from:XDevelopers",
    startTime: "2020-01-01T00:00:00Z",
    endTime: "2020-12-31T23:59:59Z",
    tweetFields: ["created_at", "public_metrics", "author_id"],
    expansions: ["author_id"],
    userFields: ["username", "description"],
    maxResults: 100,
  });

  for await (const page of paginator) {
    page.data?.forEach((post) => {
      console.log(`${post.created_at}: ${post.text?.slice(0, 50)}...`);
    });
  }
  ```
</CodeGroup>

***

<div id="step-6-paginate-through-results">
  ## 步骤 6：分页遍历结果
</div>

SDK 会自动处理分页。对于 cURL，请使用响应中的 `next_token`：

```bash
curl "https://api.x.com/2/tweets/search/all?\
query=from%3AXDevelopers&\
max_results=500&\
next_token=b26v89c19zqg8o3fo7gesq314yb9l2l4ptqy" \
  -H "Authorization: Bearer $BEARER_TOKEN"
```

<Card title="分页指南" icon="arrow-right" href="/zh/x-api/posts/search/integrate/paginate">
  进一步了解如何处理大规模结果集
</Card>

***

<div id="key-differences-from-recent-search">
  ## 与最近搜索的主要差异
</div>

| 功能 | 最近搜索 | 全量存档搜索 |
|:--------|:--------------|:--------------------|
| 时间范围 | 最近 7 天 | 2006 年 3 月至今 |
| 访问要求 | 所有开发者 | 按使用付费，企业版 |
| 每个请求的最大结果数 | 100 | 500 |
| 查询长度 | 512 个字符 | 1,024 个字符 |
| 速率限制 | 450 / 15 分钟 | 300 / 15 分钟，1 / 秒 |
| 认证方式 | App-Only，User Context | 仅 App-Only |

***

<div id="common-parameters">
  ## 常用参数
</div>

| 参数 | 描述 | 默认值 |
|:----------|:------------|:--------|
| `query` | 搜索语句（必填） | — |
| `max_results` | 每页帖子数量（10-500） | 10 |
| `start_time` | 最早帖子时间戳 | 30 天前 |
| `end_time` | 最新帖子时间戳 | 现在 |
| `next_token` | 分页令牌 | — |
| `tweet.fields` | 附加帖子字段 | `id`, `text` |
| `expansions` | 要包含的关联对象 | — |

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="构建查询" icon="magnifying-glass" href="/zh/x-api/posts/search/integrate/build-a-query">
    掌握查询语法和运算符
  </Card>

  <Card title="运算符参考" icon="list-check" href="/zh/x-api/posts/search/integrate/operators">
    查看所有可用的运算符
  </Card>

  <Card title="分页指南" icon="arrow-right" href="/zh/x-api/posts/search/integrate/paginate">
    处理大型结果集
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/posts/full-archive-search">
    完整的端点文档
  </Card>
</CardGroup>