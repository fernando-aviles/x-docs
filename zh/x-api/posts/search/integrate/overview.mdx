---
title: 集成指南
sidebarTitle: 概览
description: 集成 Search Posts 端点到应用程序中的关键概念和最佳实践
keywords: ["搜索集成", "搜索概览", "搜索指南", "搜索实现", "帖子搜索集成"]
---

本指南介绍将 Search Posts 端点集成到应用程序中所需掌握的关键概念。

***

<div id="authentication">
  ## 身份验证
</div>

<div id="recent-search">
  ### 最近搜索
</div>

最近搜索支持多种身份验证方法：

| 方法 | 使用场景 |
|:-------|:---------|
| [OAuth 2.0 App-Only](/zh/resources/fundamentals/authentication#oauth-2-0) | 公开帖子数据 |
| [OAuth 2.0 Authorization Code with PKCE](/zh/resources/fundamentals/authentication#oauth-2-0-authorization-code-flow-with-pkce-2) | 私有指标 |
| [OAuth 1.0a User Context](/zh/resources/fundamentals/authentication) | 私有指标 |

<div id="full-archive-search">
  ### 全量存档搜索
</div>

全量存档搜索仅支持通过 [OAuth 2.0 App-Only](/zh/resources/fundamentals/authentication#oauth-2-0) 进行身份验证。

<Warning>
  由于全量存档搜索仅支持 App-Only 身份验证，因此无法获取私密指标（`non_public_metrics`、`organic_metrics`、`promoted_metrics`）。
</Warning>

***

<div id="building-queries">
  ## 构建查询语句
</div>

查询使用运算符来匹配帖子，可以通过布尔逻辑组合这些运算符：

```
(AI OR "machine learning") lang:en -is:retweet has:links
```

<div id="query-length-limits">
  ### 查询长度限制
</div>

| 访问权限级别 | 最近搜索 | 全量归档搜索 |
|:-------------|:--------------|:--------------------|
| 自助 | 512 个字符 | 1,024 个字符 |
| 企业级 | 4,096 个字符 | 4,096 个字符 |

<div id="operator-types">
  ### 运算符类型
</div>

| 类型 | 描述 | 示例 |
|:-----|:------------|:--------|
| **独立使用** | 可以单独使用 | `#python`, `from:user` |
| **需配合使用** | 必须与独立运算符一起使用 | `has:media`, `is:retweet` |

<Card title="构建查询" icon="magnifying-glass" href="/zh/x-api/posts/search/integrate/build-a-query">
  深入了解查询语法
</Card>

<Card title="运算符参考" icon="list-check" href="/zh/x-api/posts/search/integrate/operators">
  查看全部可用运算符
</Card>

***

<div id="fields-and-expansions">
  ## 字段和扩展
</div>

默认情况下，响应仅包含 `id`、`text` 和 `edit_history_tweet_ids`。使用参数来请求更多数据。

<div id="example-request">
  ### 示例请求
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/tweets/search/recent?\
  query=python&\
  tweet.fields=created_at,public_metrics,author_id&\
  expansions=author_id,attachments.media_keys&\
  user.fields=username,verified&\
  media.fields=url,type" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # 搜索最新的帖子
  for page in client.posts.search_recent(
      query="python",
      tweet_fields=["created_at", "public_metrics", "author_id"],
      expansions=["author_id", "attachments.media_keys"],
      user_fields=["username", "verified"],
      media_fields=["url", "type"],
      max_results=100
  ):
      for post in page.data:
          print(f"{post.text} - Likes: {post.public_metrics.like_count}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // 搜索最新的帖子
  const paginator = client.posts.searchRecent("python", {
    tweetFields: ["created_at", "public_metrics", "author_id"],
    expansions: ["author_id", "attachments.media_keys"],
    userFields: ["username", "verified"],
    mediaFields: ["url", "type"],
    maxResults: 100,
  });

  for await (const page of paginator) {
    page.data?.forEach((post) => {
      console.log(`${post.text} - Likes: ${post.public_metrics?.like_count}`);
    });
  }
  ```
</CodeGroup>

<div id="available-expansions">
  ### 可用扩展
</div>

| 扩展项 | 返回内容 |
|:----------|:--------|
| `author_id` | 作者的用户对象 |
| `attachments.media_keys` | 附带的媒体对象 |
| `attachments.poll_ids` | 附带的投票对象 |
| `referenced_tweets.id` | 被引用或被回复的帖子 |
| `geo.place_id` | 地点对象 |
| `entities.mentions.username` | 被提及的用户对象 |

<Card title="字段和扩展指南" icon="sliders" href="/zh/x-api/fundamentals/fields">
  进一步了解如何自定义响应
</Card>

***

<div id="pagination">
  ## 分页
</div>

搜索端点以分页形式返回结果。使用响应中的 `next_token` 来获取更多结果页。

<div id="how-it-works">
  ### 工作原理
</div>

1. 使用 `max_results` 发起初始请求
2. 检查 `meta` 对象中的 `next_token`
3. 在后续请求中包含 `next_token`
4. 重复上述步骤，直到不再返回 `next_token`

<div id="example">
  ### 示例
</div>

<CodeGroup dropdown>
  ```bash cURL
  # 第一次请求
  curl "https://api.x.com/2/tweets/search/recent?query=python&max_results=100" \
    -H "Authorization: Bearer $BEARER_TOKEN"

  # 使用分页令牌的后续请求
  curl "https://api.x.com/2/tweets/search/recent?query=python&max_results=100&next_token=NEXT_TOKEN" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # SDK 会自动处理分页
  all_posts = []

  for page in client.posts.search_recent(query="python", max_results=100):
      if page.data:
          all_posts.extend(page.data)

  print(f"找到 {len(all_posts)} 条帖子")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  async function getAllResults(query) {
    const allPosts = [];

    // SDK 会自动处理分页
    const paginator = client.posts.searchRecent(query, { maxResults: 100 });

    for await (const page of paginator) {
      if (page.data) {
        allPosts.push(...page.data);
      }
    }

    return allPosts;
  }

  // 用法
  const posts = await getAllResults("python");
  console.log(`找到 ${posts.length} 条帖子`);
  ```
</CodeGroup>

<Card title="分页指南" icon="arrow-right" href="/zh/x-api/posts/search/integrate/paginate">
  了解更多分页相关内容
</Card>

***

<div id="post-edits">
  ## 帖子编辑
</div>

帖子在发布后 30 分钟内最多可编辑 5 次。搜索端点始终返回帖子最新的版本。

<div id="considerations">
  ### 注意事项
</div>

* `edit_history_tweet_ids` 包含所有帖子 ID（从最早到最新）
* 在 30 分钟编辑窗口结束后获取的帖子即为最终版本
* 在近实时场景中，最近发布的帖子可能仍会被编辑

<Card title="帖子编辑基础知识" icon="clock-rotate-left" href="/zh/x-api/fundamentals/edit-posts">
  深入了解帖子编辑
</Card>

***

<div id="best-practices">
  ## 最佳实践
</div>

<CardGroup cols={2}>
  <Card title="从具体条件入手" icon="crosshairs">
    使用多个运算符来缩小结果范围并减少噪声。
  </Card>

  <Card title="迭代测试" icon="flask">
    先从范围较宽的查询开始，然后根据结果逐步优化。
  </Card>

  <Card title="处理分页" icon="arrow-right">
    为大型结果集实现合理的分页机制。
  </Card>

  <Card title="缓存结果" icon="database">
    在本地存储结果以避免重复请求。
  </Card>
</CardGroup>

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="构建查询" icon="magnifying-glass" href="/zh/x-api/posts/search/integrate/build-a-query">
    掌握查询语法
  </Card>

  <Card title="运算符参考" icon="list-check" href="/zh/x-api/posts/search/integrate/operators">
    所有可用运算符
  </Card>

  <Card title="分页" icon="arrow-right" href="/zh/x-api/posts/search/integrate/paginate">
    处理大型结果集
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/posts/recent-search">
    完整端点文档
  </Card>
</CardGroup>