---
title: 管理书签
sidebarTitle: 管理书签
description: 使用 X API 添加和删除书签
keywords: ["管理书签快速入门", "书签快速入门", "书签教程", "书签指南", "书签快速入门"]
---

import { Button } from '/snippets/zh/button.mdx';

本指南将引导你使用 X API 添加和删除书签。

<Note>
  **前提条件**

  在开始之前，你需要：

  * 一个拥有已获批准应用（App）的[开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 一个权限范围为 `bookmark.write` 的用户访问令牌（User Access Token，OAuth 2.0 PKCE）
</Note>

***

<div id="add-a-bookmark">
  ## 添加书签
</div>

<Steps>
  <Step title="获取你的用户 ID">
    你需要经过身份验证的用户 ID。你可以通过 `/2/users/me` 端点或 [用户查询端点](/zh/x-api/users/lookup/introduction) 获取。
  </Step>

  <Step title="获取帖子 ID">
    在查看帖子时，从 URL 中找到帖子 ID：

    ```
    https://x.com/XDevelopers/status/1460323737035677698
                                    └── 这是帖子 ID
    ```
  </Step>

  <Step title="添加书签">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/users/2244994945/bookmarks" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"tweet_id": "1460323737035677698"}'
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

      # 添加书签
      response = client.bookmarks.create(
          user_id="2244994945",
          tweet_id="1460323737035677698"
      )

      print(f"Bookmarked: {response.data.bookmarked}")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

      // 添加书签
      const response = await client.bookmarks.create("2244994945", {
        tweetId: "1460323737035677698",
      });

      console.log(`Bookmarked: ${response.data?.bookmarked}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="查看响应">
    ```json
    {
      "data": {
        "bookmarked": true
      }
    }
    ```
  </Step>
</Steps>

***

<div id="remove-a-bookmark">
  ## 移除书签
</div>

从书签中删除一条帖子：

<CodeGroup dropdown>
  ```bash cURL
  curl -X DELETE "https://api.x.com/2/users/2244994945/bookmarks/1460323737035677698" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

  # 移除书签
  response = client.bookmarks.delete(
      user_id="2244994945",
      tweet_id="1460323737035677698"
  )

  print(f"Bookmarked: {response.data.bookmarked}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

  // 移除书签
  const response = await client.bookmarks.delete("2244994945", "1460323737035677698");

  console.log(`Bookmarked: ${response.data?.bookmarked}`);
  ```
</CodeGroup>

**响应：**

```json
{
  "data": {
    "bookmarked": false
  }
}
```

***

<div id="required-scopes">
  ## 必需的权限范围
</div>

在使用 OAuth 2.0 PKCE 时，你的访问令牌必须包含以下权限范围：

| Scope | 描述 |
|:------|:------------|
| `bookmark.write` | 添加或移除书签 |
| `tweet.read` | 读取帖子数据 |
| `users.read` | 读取用户数据 |

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="书签查询" icon="bookmark" href="/zh/x-api/posts/bookmarks/quickstart/bookmarks-lookup">
    获取你已添加书签的帖子
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/posts/add-post-to-bookmarks">
    完整的端点文档
  </Card>
</CardGroup>