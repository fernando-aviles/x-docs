---
title: 书签查找
sidebarTitle: 书签查找
description: 获取你已加书签的帖子
keywords: ["书签查找快速入门", "书签查找快速上手", "书签查找教程", "书签查找指南"]
---

import { Button } from '/snippets/zh/button.mdx';

本指南将引导你使用 X API 获取你已添加书签的帖子。

<Note>
  **前提条件**

  在开始之前，你需要：

  * 一个已获批准的 App 所关联的[开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 具有 `bookmark.read` 作用域的用户访问令牌（OAuth 2.0 PKCE）
</Note>

***

<div id="get-your-bookmarks">
  ## 获取你的书签
</div>

<Steps>
  <Step title="获取你的用户 ID">
    你需要当前已认证用户的 ID。你可以使用 `/2/users/me` 端点，或者通过 [用户查询端点](/zh/x-api/users/lookup/introduction) 获取。
  </Step>

  <Step title="请求你的书签">
    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/users/2244994945/bookmarks?\
      tweet.fields=created_at,public_metrics,author_id&\
      max_results=10" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

      # 通过分页方式获取已添加到书签的帖子
      for page in client.bookmarks.get(
          "2244994945",
          tweet_fields=["created_at", "public_metrics", "author_id"],
          max_results=10
      ):
          for post in page.data:
              print(f"{post.text[:50]}... - Likes: {post.public_metrics.like_count}")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

      // 通过分页方式获取已添加到书签的帖子
      const paginator = client.bookmarks.get("2244994945", {
        tweetFields: ["created_at", "public_metrics", "author_id"],
        maxResults: 10,
      });

      for await (const page of paginator) {
        page.data?.forEach((post) => {
          console.log(`${post.text?.slice(0, 50)}... - Likes: ${post.public_metrics?.like_count}`);
        });
      }
      ```
    </CodeGroup>
  </Step>

  <Step title="查看响应">
    ```json
    {
      "data": [
        {
          "id": "1501258597237342208",
          "text": "Have you built a project using the X API you'd like to share with the community? We'd love to hear from you!",
          "created_at": "2024-01-15T10:30:00.000Z",
          "author_id": "2244994945",
          "public_metrics": {
            "retweet_count": 15,
            "reply_count": 8,
            "like_count": 89,
            "quote_count": 3
          }
        },
        {
          "id": "1501258542258348032",
          "text": "This is just one way developer innovation helps make X a better place...",
          "created_at": "2024-01-15T09:15:00.000Z",
          "author_id": "2244994945",
          "public_metrics": {
            "retweet_count": 22,
            "reply_count": 5,
            "like_count": 156,
            "quote_count": 7
          }
        }
      ],
      "meta": {
        "result_count": 2,
        "next_token": "7140dibdnow9c7btw4539n0vybdnx19ylpayqf16fjt4l"
      }
    }
    ```
  </Step>
</Steps>

***

<div id="include-author-information">
  ## 包含作者信息
</div>

使用扩展来获取帖子作者的相关数据：

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/2244994945/bookmarks?\
  tweet.fields=created_at,author_id&\
  expansions=author_id&\
  user.fields=username,verified" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

  # 获取包含作者信息的书签
  for page in client.bookmarks.get(
      "2244994945",
      tweet_fields=["created_at", "author_id"],
      expansions=["author_id"],
      user_fields=["username", "verified"]
  ):
      for post in page.data:
          print(f"{post.text[:50]}...")
      # 作者信息位于 page.includes.users 中
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

  // 获取包含作者信息的书签
  const paginator = client.bookmarks.get("2244994945", {
    tweetFields: ["created_at", "author_id"],
    expansions: ["author_id"],
    userFields: ["username", "verified"],
  });

  for await (const page of paginator) {
    page.data?.forEach((post) => {
      console.log(`${post.text?.slice(0, 50)}...`);
    });
    // 作者信息位于 page.includes?.users 中
  }
  ```
</CodeGroup>

***

<div id="required-scopes">
  ## 必需的授权范围
</div>

当使用 OAuth 2.0 PKCE 时，你的 access token 必须包含以下 scope：

| Scope | 说明 |
|:------|:------------|
| `bookmark.read` | 读取书签 |
| `tweet.read` | 读取帖子数据 |
| `users.read` | 读取用户数据（用于扩展） |

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="管理书签" icon="bookmark" href="/zh/x-api/posts/bookmarks/quickstart/manage-bookmarks">
    添加和删除书签
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/posts/get-users-bookmarks">
    完整端点说明
  </Card>
</CardGroup>