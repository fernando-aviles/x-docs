---
title: 书签查询
sidebarTitle: 书签查询
description: 获取你已加书签的帖子
keywords: ["bookmarks lookup 快速入门", "bookmark lookup 快速入门", "bookmark lookup 教程", "bookmark lookup 指南"]
---

import { Button } from '/snippets/zh/button.mdx';

本指南将引导你使用 X API 获取你已添加为书签的帖子。

<Note>
  **先决条件**

  在开始之前，你需要：

  * 一个拥有已获批准 App 的[开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 一个包含 `bookmark.read` scope 权限的用户访问令牌（OAuth 2.0 PKCE）
</Note>

***

<div id="get-your-bookmarks">
  ## 获取你的书签
</div>

<Steps>
  <Step title="获取你的用户 ID">
    你需要已认证用户的 ID（当前用户的 ID）。你可以通过 `/2/users/me` 端点获取，或者使用 [用户查询端点](/zh/x-api/users/lookup/introduction)。
  </Step>

  <Step title="请求你的书签">
    <CodeGroup dropdown>
      ```bash cURL
      curl "https://api.x.com/2/users/2244994945/bookmarks?\
      tweet.fields=created_at,public_metrics,author_id&\
      max_results=10" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN"
      ```

      ```python Python SDK
      from xdk import Client

      client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

      # 通过分页获取已加书签的帖子
      for page in client.bookmarks.get(
          "2244994945",
          tweet_fields=["created_at", "public_metrics", "author_id"],
          max_results=10
      ):
          for post in page.data:
              print(f"{post.text[:50]}... - Likes: {post.public_metrics.like_count}")
      ```

      ```javascript JavaScript SDK
      import { Client } from "@xdevplatform/xdk";

      const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

      // 通过分页获取已加书签的帖子
      const paginator = client.bookmarks.get("2244994945", {
        tweetFields: ["created_at", "public_metrics", "author_id"],
        maxResults: 10,
      });

      for await (const page of paginator) {
        page.data?.forEach((post) => {
          console.log(`${post.text?.slice(0, 50)}... - Likes: ${post.public_metrics?.like_count}`);
        });
      }
      ```
    </CodeGroup>
  </Step>

  <Step title="查看响应">
    ```json
    {
      "data": [
        {
          "id": "1501258597237342208",
          "text": "你是否构建了使用 X API 的项目，并希望与社区分享？我们非常期待听到你的分享！",
          "created_at": "2024-01-15T10:30:00.000Z",
          "author_id": "2244994945",
          "public_metrics": {
            "retweet_count": 15,
            "reply_count": 8,
            "like_count": 89,
            "quote_count": 3
          }
        },
        {
          "id": "1501258542258348032",
          "text": "这只是开发者创新帮助让 X 变得更好的众多方式之一……",
          "created_at": "2024-01-15T09:15:00.000Z",
          "author_id": "2244994945",
          "public_metrics": {
            "retweet_count": 22,
            "reply_count": 5,
            "like_count": 156,
            "quote_count": 7
          }
        }
      ],
      "meta": {
        "result_count": 2,
        "next_token": "7140dibdnow9c7btw4539n0vybdnx19ylpayqf16fjt4l"
      }
    }
    ```
  </Step>
</Steps>

***

<div id="include-author-information">
  ## 包含作者信息
</div>

使用 expansions 参数来获取帖子作者的信息：

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/2244994945/bookmarks?\
  tweet.fields=created_at,author_id&\
  expansions=author_id&\
  user.fields=username,verified" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

  # 获取包含作者信息的书签
  for page in client.bookmarks.get(
      "2244994945",
      tweet_fields=["created_at", "author_id"],
      expansions=["author_id"],
      user_fields=["username", "verified"]
  ):
      for post in page.data:
          print(f"{post.text[:50]}...")
      # 作者信息位于 page.includes.users 中
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

  // 获取包含作者信息的书签
  const paginator = client.bookmarks.get("2244994945", {
    tweetFields: ["created_at", "author_id"],
    expansions: ["author_id"],
    userFields: ["username", "verified"],
  });

  for await (const page of paginator) {
    page.data?.forEach((post) => {
      console.log(`${post.text?.slice(0, 50)}...`);
    });
    // 作者信息位于 page.includes?.users 中
  }
  ```
</CodeGroup>

***

<div id="required-scopes">
  ## 所需权限范围
</div>

在使用 OAuth 2.0 PKCE 时，你的访问令牌必须包含以下权限范围：

| Scope | Description |
|:------|:------------|
| `bookmark.read` | 读取书签 |
| `tweet.read` | 读取帖子数据 |
| `users.read` | 读取用户数据（用于 expansions） |

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="管理书签" icon="bookmark" href="/zh/x-api/posts/bookmarks/quickstart/manage-bookmarks">
    添加和删除书签
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/posts/get-users-bookmarks">
    完整端点文档
  </Card>
</CardGroup>