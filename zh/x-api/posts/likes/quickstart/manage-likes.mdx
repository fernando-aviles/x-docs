---
title: 管理点赞
sidebarTitle: 管理点赞
description: 使用 X API 对帖子点赞和取消点赞
keywords: ["管理点赞", "点赞帖子", "取消点赞", "点赞快速开始", "点赞教程", "点赞指南"]
---

import { Button } from '/snippets/zh/button.mdx';

本指南将引导你使用 X API 对帖子进行点赞和取消点赞。

<Note>
  **先决条件**

  在开始之前，你需要：

  * 一个已获批准的 App 的[开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 用户访问令牌（OAuth 1.0a 或 OAuth 2.0 PKCE）
</Note>

***

<div id="like-a-post">
  ## 为帖子点赞
</div>

<Steps>
  <Step title="获取你的用户 ID">
    你需要已认证用户的 ID。你可以使用 [用户查找端点](/zh/x-api/users/lookup/introduction) 或从 Access Token 中查看（数字部分就是你的用户 ID）。
  </Step>

  <Step title="获取帖子 ID">
    在查看帖子时，在 URL 中找到帖子 ID：

    ```
    https://x.com/XDevelopers/status/1228393702244134912
                                    └── 这是帖子 ID
    ```
  </Step>

  <Step title="发送点赞请求">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/users/123456789/likes" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"tweet_id": "1228393702244134912"}'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # 为帖子点赞
      response = client.posts.like(
          user_id="123456789",
          tweet_id="1228393702244134912"
      )

      print(f"Liked: {response.data.liked}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // 为帖子点赞
      const response = await client.posts.like("123456789", {
        tweetId: "1228393702244134912",
      });

      console.log(`Liked: ${response.data?.liked}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="查看响应">
    ```json
    {
      "data": {
        "liked": true
      }
    }
    ```
  </Step>
</Steps>

***

<div id="unlike-a-post">
  ## 取消对帖子的点赞
</div>

取消对某条帖子的点赞：

<CodeGroup dropdown>
  ```bash cURL
  curl -X DELETE "https://api.x.com/2/users/123456789/likes/1228393702244134912" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client
  from xdk.oauth1_auth import OAuth1

  oauth1 = OAuth1(
      api_key="YOUR_API_KEY",
      api_secret="YOUR_API_SECRET",
      access_token="YOUR_ACCESS_TOKEN",
      access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
  )

  client = Client(auth=oauth1)

  # 取消对帖子的点赞
  response = client.posts.unlike(
      user_id="123456789",
      tweet_id="1228393702244134912"
  )

  print(f"Liked: {response.data.liked}")
  ```

  ```javascript JavaScript SDK
  import { Client, OAuth1 } from "@xdevplatform/xdk";

  const oauth1 = new OAuth1({
    apiKey: "YOUR_API_KEY",
    apiSecret: "YOUR_API_SECRET",
    accessToken: "YOUR_ACCESS_TOKEN",
    accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
  });

  const client = new Client({ oauth1 });

  // 取消对帖子的点赞
  const response = await client.posts.unlike("123456789", "1228393702244134912");

  console.log(`Liked: ${response.data?.liked}`);
  ```
</CodeGroup>

**响应：**

```json
{
  "data": {
    "liked": false
  }
}
```

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="点赞查询" icon="heart" href="/zh/x-api/posts/likes/quickstart/likes-lookup">
    获取对该帖子点赞的用户
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/posts/like-a-post">
    完整的端点文档
  </Card>
</CardGroup>