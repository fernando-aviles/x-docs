---
title: 已认证用户快速入门
sidebarTitle: 已认证用户
description: 获取当前已认证用户的资料
keywords: ["authenticated user", "me endpoint", "current user", "authenticated lookup"]
---

import { Button } from '/snippets/zh/button.mdx';

本指南将指导你通过 `/me` 端点获取当前已通过认证的用户个人资料。

<Note>
  **先决条件**

  在开始之前，你需要：

  * 一个拥有已获批准应用的[开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 用户访问令牌（OAuth 1.0a 或 OAuth 2.0 PKCE）
</Note>

***

<div id="get-the-authenticated-user">
  ## 获取已认证用户
</div>

使用用户访问令牌向 `/me` 端点发起请求：

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/me?\
  user.fields=created_at,description,verified,public_metrics,profile_image_url" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

  # 获取已认证用户
  response = client.users.get_me(
      user_fields=["created_at", "description", "verified", "public_metrics", "profile_image_url"]
  )

  print(f"Username: {response.data.username}")
  print(f"ID: {response.data.id}")
  print(f"Followers: {response.data.public_metrics.followers_count}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

  // 获取已认证用户
  const response = await client.users.getMe({
    userFields: ["created_at", "description", "verified", "public_metrics", "profile_image_url"],
  });

  console.log(`Username: ${response.data?.username}`);
  console.log(`ID: ${response.data?.id}`);
  console.log(`Followers: ${response.data?.public_metrics?.followers_count}`);
  ```
</CodeGroup>

***

<div id="response">
  ## 响应
</div>

```json
{
  "data": {
    "id": "2244994945",
    "name": "X Developers",
    "username": "XDevelopers",
    "created_at": "2013-12-14T04:35:55.000Z",
    "description": "The voice of the X developer community",
    "verified": true,
    "profile_image_url": "https://pbs.twimg.com/profile_images/...",
    "public_metrics": {
      "followers_count": 583423,
      "following_count": 2048,
      "tweet_count": 14052,
      "listed_count": 1672
    }
  }
}
```

***

<div id="use-case">
  ## 使用场景
</div>

在以下情况下，`/me` 端点至关重要：

* **验证身份** — 确认用户已正确完成身份验证
* **获取用户 ID** — 为其他 API 调用获取已认证用户的 ID
* **个性化体验** — 在你的应用中展示用户的个人资料
* **代表用户发起请求** — 明确你正在代表哪位用户发起请求

***

<div id="include-pinned-post">
  ## 包含置顶帖子
</div>

请求返回用户的置顶帖子：

<CodeGroup dropdown>
  ```bash cURL
  curl "https://api.x.com/2/users/me?\
  user.fields=pinned_tweet_id&\
  expansions=pinned_tweet_id&\
  tweet.fields=created_at,text" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

  # 获取带有置顶帖子的已认证用户
  response = client.users.get_me(
      user_fields=["pinned_tweet_id"],
      expansions=["pinned_tweet_id"],
      tweet_fields=["created_at", "text"]
  )

  print(f"Username: {response.data.username}")
  # 置顶帖子在 response.includes.tweets 中
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

  // 获取带有置顶帖子的已认证用户
  const response = await client.users.getMe({
    userFields: ["pinned_tweet_id"],
    expansions: ["pinned_tweet_id"],
    tweetFields: ["created_at", "text"],
  });

  console.log(`Username: ${response.data?.username}`);
  // 置顶帖子在 response.includes?.tweets 中
  ```
</CodeGroup>

<div id="response-with-expansion">
  ### 包含扩展的响应
</div>

```json
{
  "data": {
    "id": "2244994945",
    "name": "X Developers",
    "username": "XDevelopers",
    "pinned_tweet_id": "1234567890"
  },
  "includes": {
    "tweets": [
      {
        "id": "1234567890",
        "text": "Welcome to my profile!",
        "created_at": "2024-01-01T00:00:00.000Z"
      }
    ]
  }
}
```

***

<div id="available-fields">
  ## 可用字段
</div>

| 字段 | 描述 |
|:------|:------------|
| `created_at` | 账户创建日期 |
| `description` | 用户简介 |
| `profile_image_url` | 头像 URL 地址 |
| `verified` | 认证状态 |
| `public_metrics` | 粉丝数/关注数 |
| `location` | 用户自定义的所在地 |
| `url` | 用户的网站 |
| `protected` | 账户保护状态 |
| `pinned_tweet_id` | 置顶帖子 ID |

***

<div id="authentication-requirement">
  ## 身份验证要求
</div>

<Warning>
  `/me` 端点必须使用用户上下文身份验证。不支持仅应用（Bearer Token）身份验证。
</Warning>

请使用以下任一身份验证方式：

* [OAuth 1.0a 用户上下文](/zh/resources/fundamentals/authentication)
* [OAuth 2.0 Authorization Code with PKCE](/zh/resources/fundamentals/authentication#oauth-2-0-authorization-code-flow-with-pkce-2)

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="用户查找" icon="users" href="/zh/x-api/users/lookup/quickstart/user-lookup">
    查找其他用户
  </Card>

  <Card title="集成指南" icon="book" href="/zh/x-api/users/lookup/integrate">
    关键概念和最佳实践
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/users/authenticated-user-lookup">
    完整的端点文档
  </Card>
</CardGroup>