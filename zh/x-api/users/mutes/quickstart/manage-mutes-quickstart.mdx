---
title: 管理静音
sidebarTitle: 管理静音
description: 使用 X API 将用户静音和取消静音
keywords: ["管理静音快速入门", "将用户静音快速入门", "管理静音教程", "静音指南", "静音快速入门"]
---

import { Button } from '/snippets/zh/button.mdx';

本指南将引导你使用 X API 对用户进行静音和取消静音操作。

<Note>
  **先决条件**

  在开始之前，你需要：

  * 一个包含已获批准 App 的[开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 用户访问令牌（OAuth 1.0a 或 OAuth 2.0 PKCE）
</Note>

***

<div id="mute-a-user">
  ## 静音用户
</div>

<Steps>
  <Step title="获取你的用户 ID">
    你需要你这个已通过身份验证的用户的 ID。你可以使用 [用户查询端点](/zh/x-api/users/lookup/introduction) 来获取它，或者从你的 Access Token 中获取（其中的数字部分就是你的用户 ID）。
  </Step>

  <Step title="获取目标用户 ID">
    使用 [用户查询端点](/zh/x-api/users/lookup/introduction) 找到你想要静音的帐户对应的用户 ID。
  </Step>

  <Step title="发送静音请求">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/users/123456789/muting" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"target_user_id": "9876543210"}'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # 静音用户
      response = client.users.mute(
          source_user_id="123456789",
          target_user_id="9876543210"
      )

      print(f"Muting: {response.data.muting}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // 静音用户
      const response = await client.users.mute("123456789", {
        targetUserId: "9876543210",
      });

      console.log(`Muting: ${response.data?.muting}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="查看响应">
    ```json
    {
      "data": {
        "muting": true
      }
    }
    ```
  </Step>
</Steps>

***

<div id="unmute-a-user">
  ## 取消对用户的静音
</div>

移除对某个用户的静音状态：

<CodeGroup dropdown>
  ```bash cURL
  curl -X DELETE "https://api.x.com/2/users/123456789/muting/9876543210" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client
  from xdk.oauth1_auth import OAuth1

  oauth1 = OAuth1(
      api_key="YOUR_API_KEY",
      api_secret="YOUR_API_SECRET",
      access_token="YOUR_ACCESS_TOKEN",
      access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
  )

  client = Client(auth=oauth1)

  # 取消对用户的静音
  response = client.users.unmute(
      source_user_id="123456789",
      target_user_id="9876543210"
  )

  print(f"Muting: {response.data.muting}")
  ```

  ```javascript JavaScript SDK
  import { Client, OAuth1 } from "@xdevplatform/xdk";

  const oauth1 = new OAuth1({
    apiKey: "YOUR_API_KEY",
    apiSecret: "YOUR_API_SECRET",
    accessToken: "YOUR_ACCESS_TOKEN",
    accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
  });

  const client = new Client({ oauth1 });

  // 取消对用户的静音
  const response = await client.users.unmute("123456789", "9876543210");

  console.log(`Muting: ${response.data?.muting}`);
  ```
</CodeGroup>

**响应：**

```json
{
  "data": {
    "muting": false
  }
}
```

***

<div id="mute-vs-block">
  ## 静音与拉黑
</div>

| 功能 | 静音 | 拉黑 |
|:--------|:-----|:------|
| 你能看到对方的帖子 | 否 | 否 |
| 对方能看到你的帖子 | 是 | 否 |
| 对方可以关注你 | 是 | 否 |
| 对方可以向你发送私信 | 是 | 否 |
| 对方会知道吗 | 否 | 是 |

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="静音查询" icon="volume-xmark" href="/zh/x-api/users/mutes/quickstart/mutes-lookup">
    获取你已静音的用户列表
  </Card>

  <Card title="屏蔽" icon="ban" href="/zh/x-api/users/blocks/quickstart">
    改为屏蔽这些用户
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/users/mute-user-by-user-id">
    完整的端点文档
  </Card>
</CardGroup>