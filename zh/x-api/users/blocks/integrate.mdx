---
title: 集成指南
sidebarTitle: 集成指南
description: 集成 blocks 端点的关键概念和最佳实践
keywords: ["blocks 集成", "屏蔽用户集成", "blocks 指南"]
---

import { Button } from '/snippets/zh/button.mdx';

本指南介绍将 blocks 端点集成到你的应用程序所需的关键概念。

***

<div id="authentication">
  ## 身份验证
</div>

Blocks 相关端点需要用户身份验证：

| Method | Description |
|:-------|:------------|
| [OAuth 2.0 Authorization Code with PKCE](/zh/resources/fundamentals/authentication#oauth-2-0-authorization-code-flow-with-pkce-2) | 推荐用于新应用程序 |
| [OAuth 1.0a User Context](/zh/resources/fundamentals/authentication) | 用于兼容旧版集成 |

<Warning>
  不支持仅应用（App-Only）身份验证。必须代表用户进行身份验证。
</Warning>

<div id="required-scopes-oauth-20">
  ### 必需 Scope（OAuth 2.0）
</div>

| Scope | 用途 |
|:------|:-------------|
| `block.read` | 获取被屏蔽的账户 |
| `block.write` | 屏蔽和取消屏蔽账户 |
| `users.read` | 与 block 相关 scopes 一同使用时为必需 |

***

<div id="endpoints-overview">
  ## 端点概览
</div>

| 方法 | 端点 | 描述 |
|:-------|:---------|:------------|
| GET | `/2/users/:id/blocking` | 获取被屏蔽账号列表 |
| POST | `/2/users/:id/blocking` | 屏蔽账号 |
| DELETE | `/2/users/:source_user_id/blocking/:target_user_id` | 取消屏蔽账号 |

***

<div id="fields-and-expansions">
  ## 字段与扩展
</div>

<div id="default-response">
  ### 默认响应
</div>

```json
{
  "data": [
    {
      "id": "1234567890",
      "name": "Example User",
      "username": "example"
    }
  ]
}
```

<div id="available-fields">
  ### 可用字段
</div>

<Accordion title="user.fields">
  | 字段 | 描述 |
  |:------|:------------|
  | `created_at` | 账户创建日期 |
  | `description` | 用户简介 |
  | `profile_image_url` | 头像 URL |
  | `public_metrics` | 粉丝数/关注数 |
  | `verified` | 认证状态 |
</Accordion>

<Accordion title="expansions">
  | 扩展 | 描述 |
  |:----------|:------------|
  | `pinned_tweet_id` | 用户置顶帖子 |
</Accordion>

***

<div id="what-happens-when-you-block">
  ## 屏蔽后会发生什么
</div>

<CardGroup cols={2}>
  <Card title="对方将无法" icon="xmark">
    * 查看你的帖子（除非对方未登录账号）
    * 关注你
    * 向你发送私信
    * 将你添加到列表
    * 在照片中标记你
  </Card>

  <Card title="你将无法" icon="xmark">
    * 查看对方的帖子
    * 关注对方
    * 向对方发送私信
  </Card>
</CardGroup>

<Note>
  当你屏蔽关注你的用户时，他们会自动取消关注你。
</Note>

***

<div id="pagination">
  ## 分页
</div>

对于屏蔽列表较长的用户，结果会以分页形式返回：

<CodeGroup dropdown>
  ```bash cURL
  # 首次请求
  curl "https://api.x.com/2/users/123/blocking?max_results=100" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"

  # 使用分页令牌的后续请求
  curl "https://api.x.com/2/users/123/blocking?max_results=100&pagination_token=NEXT_TOKEN" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  # 使用 OAuth 2.0 用户访问令牌
  client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

  # SDK 会自动处理分页
  all_blocked = []

  for page in client.users.get_blocking(user_id="123", max_results=100):
      if page.data:
          all_blocked.extend(page.data)

  print(f"Blocked {len(all_blocked)} users")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

  async function getAllBlockedUsers(userId) {
    const allBlocked = [];

    // SDK 会自动处理分页
    const paginator = client.users.getBlocking(userId, { maxResults: 100 });

    for await (const page of paginator) {
      if (page.data) {
        allBlocked.push(...page.data);
      }
    }

    return allBlocked;
  }

  // 使用示例
  const blocked = await getAllBlockedUsers("123");
  console.log(`Blocked ${blocked.length} users`);
  ```
</CodeGroup>

***

<div id="error-handling">
  ## 错误处理
</div>

| 状态码 | 错误 | 解决方案 |
|:-------|:------|:---------|
| 400 | 无效请求 | 检查用户 ID 的格式 |
| 401 | 未授权 | 验证访问令牌是否有效 |
| 403 | 禁止访问 | 检查 scope 和权限设置 |
| 404 | 未找到 | 用户不存在 |
| 429 | 请求过多 | 等待一段时间后重试 |

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="快速开始" icon="rocket" href="/zh/x-api/users/blocks/quickstart">
    发送你的第一个拉黑请求
  </Card>

  <Card title="静音" icon="volume-xmark" href="/zh/x-api/users/mutes/introduction">
    将用户静音而不是拉黑
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/users/get-blocking">
    完整端点说明
  </Card>

  <Card title="示例代码" icon="github" href="https://github.com/xdevplatform/Twitter-API-v2-sample-code">
    可运行的代码示例
  </Card>
</CardGroup>