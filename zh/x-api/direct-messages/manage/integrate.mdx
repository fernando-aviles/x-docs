---
title: 集成指南
sidebarTitle: 集成指南
description: 发送私信的关键概念和最佳实践
keywords: ["管理私信集成", "私信管理集成", "发送私信集成"]
---

import { Button } from '/snippets/zh/button.mdx';

本指南将介绍把管理私信的端点集成到你的应用程序中所需掌握的关键概念。

***

<div id="authentication">
  ## 身份验证
</div>

DM 端点需要用户身份验证：

| 方法 | 说明 |
|:-------|:------------|
| [OAuth 2.0 Authorization Code with PKCE](/zh/resources/fundamentals/authentication#oauth-2-0-authorization-code-flow-with-pkce-2) | 推荐使用 |
| [OAuth 1.0a User Context](/zh/resources/fundamentals/authentication) | 旧版兼容支持 |

<Warning>
  不支持 App-only 身份验证。所有私信均为私密内容。
</Warning>

<div id="required-scopes-oauth-20">
  ### 所需 scope（OAuth 2.0）
</div>

| Scope | 用途 |
|:------|:-------------|
| `dm.write` | 发送和删除消息 |
| `dm.read` | 必须与 dm.write 一起使用 |
| `tweet.read` | 使用与 dm 相关的 scope 时必须包含 |
| `users.read` | 使用与 dm 相关的 scope 时必须包含 |

***

<div id="endpoints-overview">
  ## 端点概览
</div>

| Method | Endpoint | Description |
|:-------|:---------|:------------|
| POST | `/2/dm_conversations/with/:participant_id/messages` | 发送一对一消息 |
| POST | `/2/dm_conversations` | 创建群组会话 |
| POST | `/2/dm_conversations/:dm_conversation_id/messages` | 向会话中添加消息 |
| DELETE | `/2/dm_events/:event_id` | 删除消息 |

***

<div id="sending-messages">
  ## 发送消息
</div>

<div id="one-to-one-message">
  ### 一对一消息
</div>

向特定用户发送一条消息。如果当前还没有会话，则会新建一个会话：

<CodeGroup dropdown>
  ```bash cURL
  curl -X POST "https://api.x.com/2/dm_conversations/with/9876543210/messages" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"text": "Hello!"}'
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

  # 发送一对一私信
  response = client.dm_conversations.create_message(
      participant_id="9876543210",
      text="Hello!"
  )
  print(response.data)
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

  // 发送一对一私信
  const response = await client.dmConversations.createMessage({
    participantId: "9876543210",
    text: "Hello!",
  });
  console.log(response.data);
  ```
</CodeGroup>

<div id="group-conversation">
  ### 群组会话
</div>

创建一个新的群组会话，并发送第一条消息：

<CodeGroup dropdown>
  ```bash cURL
  curl -X POST "https://api.x.com/2/dm_conversations" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
    -H "Content-Type: application/json" \
    -d '{
      "conversation_type": "Group",
      "participant_ids": ["944480690", "906948460078698496"],
      "message": {"text": "Welcome to our group!"}
    }'
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

  # 创建一个群组会话
  response = client.dm_conversations.create(
      conversation_type="Group",
      participant_ids=["944480690", "906948460078698496"],
      message={"text": "Welcome to our group!"}
  )
  print(response.data)
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

  // 创建一个群组会话
  const response = await client.dmConversations.create({
    conversationType: "Group",
    participantIds: ["944480690", "906948460078698496"],
    message: { text: "Welcome to our group!" },
  });
  console.log(response.data);
  ```
</CodeGroup>

<Note>
  `conversation_type` 字段必须设置为 `"Group"`（区分大小写）。
</Note>

<div id="add-to-existing-conversation">
  ### 添加到现有会话
</div>

向你参与的任意会话发送一条消息：

<CodeGroup dropdown>
  ```bash cURL
  curl -X POST "https://api.x.com/2/dm_conversations/1582103724607971328/messages" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"text": "Another message"}'
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

  # 向现有会话添加消息
  response = client.dm_conversations.add_message(
      dm_conversation_id="1582103724607971328",
      text="Another message"
  )
  print(response.data)
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

  // 向现有会话添加消息
  const response = await client.dmConversations.addMessage({
    dmConversationId: "1582103724607971328",
    text: "Another message",
  });
  console.log(response.data);
  ```
</CodeGroup>

***

<div id="media-attachments">
  ## 媒体附件
</div>

每条消息最多可附加一条媒体内容（照片、视频或 GIF）。

<Steps>
  <Step title="上传媒体">
    使用 [Media Upload endpoint](/zh/x-api/media/quickstart/media-upload-chunked) 上传文件并获取一个 `media_id`。
  </Step>

  <Step title="在消息中添加">
    ```json
    {
      "text": "Check out this image!",
      "attachments": [{"media_id": "1583157113245011970"}]
    }
    ```
  </Step>
</Steps>

<Note>
  * 媒体必须由已认证用户上传
  * 媒体在上传后 24 小时内可用
  * 每条消息仅支持一个附件
</Note>

***

<div id="sharing-posts">
  ## 分享帖子
</div>

在消息文本中添加该帖子的 URL，即可在消息中包含它：

```json
{
  "text": "Have you seen this? https://x.com/XDevelopers/status/1580559079470145536"
}
```

响应中会包含一个 `referenced_tweets` 字段，其中包含帖子 ID。

***

<div id="message-requirements">
  ## 消息要求
</div>

| 字段 | 是否必需 | 说明 |
|:------|:---------|:------|
| `text` | 是* | 如果没有附件则必填 |
| `attachments` | 是* | 如果没有文本则必填 |

*必须至少提供 `text` 或 `attachments` 其中之一。

***

<div id="id-compatibility-with-v11">
  ## 与 v1.1 的 ID 兼容性
</div>

会话和事件 ID 在 v1.1 与 v2 端点之间是共享的。这使得混合式工作流成为可能：

* 使用 v2 创建消息
* 使用 v1.1 删除消息（v2 中尚不可用）
* 从 x.com URL 中引用会话 ID

***

<div id="error-handling">
  ## 错误处理
</div>

| 状态 | 错误 | 解决方案 |
|:-------|:------|:---------|
| 400 | 无效请求 | 检查请求体格式 |
| 401 | 未经授权 | 校验访问令牌 |
| 403 | 无权访问 | 检查 scope（权限范围）和用户权限 |
| 429 | 请求过多 | 等待后重试 |

<div id="common-issues">
  ### 常见问题
</div>

<AccordionGroup>
  <Accordion title="无法发送给该用户">
    收件人的私信设置可能不允许接收来自陌生用户的消息，或者该用户可能已经屏蔽了你。
  </Accordion>

  <Accordion title="媒体附件失败">
    请确保媒体由同一已认证用户上传，且上传时间在 24 小时之内。
  </Accordion>

  <Accordion title="群组创建失败">
    请确认所有参与者 ID 均有效，并且这些用户允许群组私信邀请。
  </Accordion>
</AccordionGroup>

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="快速入门" icon="rocket" href="/zh/x-api/direct-messages/manage/quickstart">
    发送你的第一条私信
  </Card>

  <Card title="DM 查询" icon="inbox" href="/zh/x-api/direct-messages/lookup/introduction">
    检索私信会话
  </Card>

  <Card title="媒体上传" icon="image" href="/zh/x-api/media/quickstart/media-upload-chunked">
    上传用于附件的媒体
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/direct-messages/create-dm-message-by-participant-id">
    完整端点说明文档
  </Card>
</CardGroup>