---
title: 迁移指南
sidebarTitle: 迁移指南
keywords: ["管理私信功能迁移", "私信管理功能迁移", "v1.1 到 v2 管理私信", "私信迁移指南", "迁移私信管理功能"]
---

import { Button } from '/snippets/zh/button.mdx';

<div id="comparing-v11-and-v2-manage-direct-message-endpoints">
  ## 比较 v1.1 和 v2 管理私信端点
</div>

Direct Messages 端点的 v1.1 和 v2 版本都提供了创建私信的方法。本指南旨在帮助你理解两者之间的差异，并为迁移到 v2 提供参考信息。 

这两个版本之间的一个主要区别是：v1.1 只支持一对一会话，而 v2 引入了对群组会话的支持。其中一个结果是 v1.1 只支持“消息已创建”事件，而 v2 还支持与参与者加入和离开会话相关的事件。实际上，v2 的一个根本性更新，就是将 `dm_conversations` 建立为一个核心 API 对象。   

在 v1.1 中，有两个用于管理私信的端点：

* POST direct&#95;messages/events/new - 创建一条一对一私信。此 v1.1 端点只能创建一对一消息，不支持群组消息。  

* DELETE direct&#95;messages/events/destroy - 从认证用户的视图中删除一条一对一消息。 

在 v2 中，有三个用于创建私信的 POST 方法： 

* **POST /2/dm&#95;conversations/with/:participant&#95;id/messages** - 创建一条一对一私信。此方法会将消息添加到现有的一对一会话中，或创建一个新会话。`:participant_id` 路径参数是接收消息账号的用户 ID。 

* **POST /2/dm&#95;conversations** - 创建一个新的群组会话并向其中添加一条私信。此类请求需要提供会话参与者列表。请注意，你可以使用相同的参与者列表创建多个会话。这些请求将始终返回新的会话 ID。 

* **POST /2/dm&#95;conversations/:dm&#95;conversation&#95;id/messages** - 创建一条私信并将其添加到现有会话中。`:dm_conversation_id` 路径参数是要向其添加消息的会话 ID。 

一个重要细节是，会话和事件 ID 在 X 平台的 v1.1 和 v2 版本之间是共享的。这意味着两个版本可以一起使用。例如，私信 v1.1 端点提供了返回单个事件和删除事件的方法，而这些方法在 v2 中尚不可用。由于 ID 在 v1.1 和 v2 之间是通用的，你可以基于 v2 提供的 ID 发起 v1.1 请求，或者根据 X 应用中会话 URL 上显示的会话 ID 发起请求。

下表比较了 v1.1 和 v2 管理私信端点的一些基本方面。此处所列的 X API v2 特性适用于所有私信查询端点。

|     |     |     |
| :--- | :--- | :--- |
| **Description** | **Standard v1.1** | **X API v2** |
| Host domain | https://api.x.com | https://api.x.com |
| Endpoint root path | [/1.1/direct&#95;messages](https://api.x.com/1.1/direct_messages) | [/2/dm&#95;conversations](https://api.x.com/2/users/:id/dm_conversations)<br /><br />Direct Messages 会话被引入为一个基础性的 API 对象。   <br /><br />这些端点会检索 MessageCreate、ParticipantsJoin 和 ParticipantLeave 事件。 |
| HTTP methods supported | POST | POST |
| Supports Group Direct Messages |     | ✔   |
| Event types supported | message&#95;create | MessageCreate, ParticipantsJoin, ParticipantsLeave |
| [Authentication](/zh/resources/fundamentals/authentication) | OAuth 1.0a User Context | OAuth 1.0a User Context<br /><br />OAuth 2 User Context（作用域：dm.read、dm.write） |
| Requires the use of credentials from a [developer App](/zh/resources/fundamentals/authentication) associated with a X API v2 [Project](/zh/resources/fundamentals/developer-apps) |     | ✔   |
| Default request [rate limits](/zh/x-api/fundamentals/rate-limits)*  <br />*All requests require user tokens | 每个用户每 24 小时 1000 次请求  <br />每个 app 每 24 小时 15000 次请求 | 每个用户每 15 分钟 200 次请求<br /><br />每个用户每 24 小时 1000 次请求<br /><br />每个 app 每 24 小时 15000 次请求<br /><br />这些速率限制在所有 `dm_conversations` POST 端点之间共享。 |

下表将 v2 POST 方法与 v1.1 版本进行了比较。请注意，这些 v2 能力通过支持群组会话扩展了可用功能。 

**创建一个新的“一对一”私信**

Path: POST /2/dm&#95;conversations/with/:participant&#95;id/messages

|     |     |     |
| :--- | :--- | :--- |
| **描述** | **Standard v1.1** | **X API v2** |
| 端点路径 | POST direct&#95;messages/events/new (message&#95;create) | POST /2/dm&#95;conversations/with/:participant&#95;id/messages |
| 默认请求 [速率限制](/zh/x-api/fundamentals/rate-limits) | 每位用户每 24 小时 1000 次请求  <br />每个应用每 24 小时 15000 次请求 | 每位用户每 15 分钟 200 次请求<br /><br />每位用户每 24 小时 1000 次请求<br /><br />每个应用每 24 小时 15000 次请求<br /><br />这些速率限制由所有 dm&#95;conversations POST 端点共享。 |
| 是否支持群组私信 |     | ✔   |

**创建新的群组私信会话并向其中添加一条消息**

路径：POST /2/dm&#95;conversations

|     |     |     |
| :--- | :--- | :--- |
| **描述** | **Standard v1.1** | **X API v2** |
| 端点路径 | 不支持 | POST /2/dm&#95;conversations |
| 默认请求 [速率限制](/zh/x-api/fundamentals/rate-limits) |     | 每位用户每 15 分钟 200 次请求<br /><br />每位用户每 24 小时 1000 次请求<br /><br />每个应用每 24 小时 15000 次请求<br /><br />这些速率限制由所有 dm&#95;conversations POST 端点共享。 |
| 是否支持群组私信 |     | ✔   |

**通过 ID 向现有会话添加一条私信**

路径：POST /2/dm&#95;conversations/:dm&#95;conversation&#95;id/messages

|     |     |     |
| :--- | :--- | :--- |
| **描述** | **Standard v1.1** | **X API v2** |
| 端点路径 | 不支持 | POST /2/dm&#95;conversations/:dm&#95;conversation&#95;id/messages |
| 默认请求 [速率限制](/zh/x-api/fundamentals/rate-limits) |     | 每位用户每 15 分钟 200 次请求<br /><br />每位用户每 24 小时 1000 次请求<br /><br />每个应用每 24 小时 15000 次请求<br /><br />这些速率限制由所有 dm&#95;conversations POST 端点共享。 |
| 是否支持群组私信 |     | ✔   |