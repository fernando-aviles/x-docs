---
title: 集成指南
sidebarTitle: 集成指南
keywords: ["置顶列表集成", "固定列表集成", "置顶列表指南", "置顶列表设置", "列表置顶集成"]
---

import { Button } from '/snippets/zh/button.mdx';

本页介绍用于集成置顶列表端点的工具和关键概念。

***

<div id="helpful-tools">
  ## 实用工具
</div>

在我们介绍一些有助于你集成该端点的关键概念之前，建议你先熟悉：

<div id="postman">
  ### Postman
</div>

Postman 是一个非常适合用来测试 API 端点的工具。每个 Postman 请求都会包含所有路径参数和请求体参数，帮助你快速了解你可以使用的内容。要进一步了解我们的 Postman 集合，请访问我们的[《使用 Postman》](/zh/tutorials/postman-getting-started)页面。

<div id="code-samples">
  ### 代码示例
</div>

想用你熟悉的编程语言，通过一些代码来开始使用这个 endpoint 吗？我们在 [GitHub 页面](https://github.com/xdevplatform/Twitter-API-v2-sample-code) 上提供了多种代码示例，你可以将它们作为起点来使用。

<div id="third-party-libraries">
  ### 第三方库
</div>

利用我们社区中的[第三方库](/zh/x-api/tools-and-libraries/overview)来帮助你快速上手。你可以通过查看相应的版本标签，找到兼容 v2 端点的库。

***

<div id="key-concepts">
  ## 核心概念
</div>

<div id="authentication">
  ### 身份验证
</div>

所有 X API v2 端点都要求你使用一组凭证（也称为密钥和令牌）对请求进行身份验证。你可以使用 OAuth 1.0a 用户上下文来对发送到此端点的请求进行身份验证。

使用 [OAuth 1.0a 用户上下文](/zh/resources/fundamentals/authentication)，这意味着你必须使用一组 API Key 和用户访问令牌才能发起一次成功的请求。访问令牌必须与代表其发起请求的用户关联。如果你想为另一位用户生成一组访问令牌，则该用户必须通过 [三方 OAuth 授权流程](https://developer.x.com/resources/fundamentals/authentication/obtaining-user-access-tokens)授权你的 App。

请注意，OAuth 1.0a 相对较难使用。如果你对这种身份验证方法不熟悉，我们建议你使用某个[库](/zh/x-api/tools-and-libraries/overview)，或使用 Postman 等工具。

<div id="developer-console-projects-and-developer-apps">
  ### 开发者控制台、Project 和开发者 App
</div>

要获取一组可用于 X API v2 端点的身份验证凭证，你必须先[注册开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)，在该账号下设置一个 [Project](/zh/resources/fundamentals/developer-apps)，并在该 Project 中创建一个 [开发者 App](/zh/resources/fundamentals/developer-apps)。然后，你可以在你的开发者 App 中找到对应的 keys 和 tokens。

<div id="rate-limits">
  ### 速率限制
</div>

每天都有成千上万的开发者向 X API 发起请求。为了管理如此庞大的请求量，我们在每个端点上设置了[速率限制](/zh/x-api/fundamentals/rate-limits)，限制你代表你的 App 或代表已认证用户所能发起的请求次数。

这些端点的速率限制是按用户级别计算的，这意味着你代表其发起请求的已认证用户，在任何开发者 App 中调用该端点的次数都有上限。

下表展示了每个端点对应的速率限制。

| Endpoint | HTTP method | Rate limit |
| :--- | :--- | :--- |
| /2/users/:id/pinned&#95;lists | POST | 每 15 分钟 50 次请求 |
| /2/users/:id/pinned&#95;lists/:list&#95;id | DELETE | 每 15 分钟 50 次请求 |
| /2/users/:id/pinned&#95;lists | GET | 每 15 分钟 15 次请求 |

<div id="fields-and-expansions">
  ### 字段和 expansions
</div>

X API v2 的 GET 端点允许用户使用一组称为 `fields` 和 `expansions` 的工具，精确选择希望从 API 返回哪些数据。`expansions` 参数允许你展开在载荷中被引用的对象。例如，查询置顶列表时可以获取如下 [expansions](/zh/x-api/fundamentals/expansions)：

* `owner_id`

`fields` 参数允许你精确选择希望在不同数据对象中接收哪些[字段](/zh/x-api/fundamentals/fields)。此端点主要返回用户对象。默认情况下，列表对象会返回 `id` 和 `name` 字段。若要接收额外字段，例如 `list.created_at` 或 `list.description`，必须通过 `fields` 参数显式请求这些字段。

我们在 [X API v2 数据字典](/zh/x-api/fundamentals/data-dictionary)中新增了一篇关于如何结合使用 [fields 和 expansions](/zh/x-api/fundamentals/data-dictionary#how-to-use-fields-and-expansions) 的指南。

下表展示了该查找端点可用的字段和 expansions：

| Endpoint | Fields | Expansions |
| :--- | :--- | :--- |
| /2/users/:id/pinned&#95;lists | `list.fields`, `user.fields` | `owner_id` |