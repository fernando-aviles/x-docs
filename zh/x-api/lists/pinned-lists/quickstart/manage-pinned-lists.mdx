---
title: 管理固定列表
sidebarTitle: 管理固定列表
description: 固定和取消固定列表
keywords: ["管理固定列表快速入门", "固定列表快速入门", "取消固定列表快速入门", "固定列表管理"]
---

import { Button } from '/snippets/zh/button.mdx';

本指南将逐步介绍如何固定和取消固定列表。

<Note>
  **前提条件**

  在开始之前，你需要：

  * 一个包含已获批准 App 的[开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 用户访问令牌（OAuth 1.0a 或 OAuth 2.0 PKCE）
</Note>

***

<div id="pin-a-list">
  ## 置顶列表
</div>

<Steps>
  <Step title="获取你的用户 ID 和列表 ID">
    你需要已认证用户的用户 ID，以及你想置顶的列表的 ID。查看列表时，可以在该列表页面的 URL 中找到列表 ID。
  </Step>

  <Step title="置顶该列表">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/users/2244994945/pinned_lists" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"list_id": "1454155907651158017"}'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # 置顶列表
      response = client.lists.pin(
          user_id="2244994945",
          list_id="1454155907651158017"
      )

      print(f"Pinned: {response.data.pinned}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // 置顶列表
      const response = await client.lists.pin("2244994945", {
        listId: "1454155907651158017",
      });

      console.log(`Pinned: ${response.data?.pinned}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="查看响应">
    ```json
    {
      "data": {
        "pinned": true
      }
    }
    ```
  </Step>
</Steps>

***

<div id="unpin-a-list">
  ## 取消置顶列表
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl -X DELETE "https://api.x.com/2/users/2244994945/pinned_lists/1454155907651158017" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client
  from xdk.oauth1_auth import OAuth1

  oauth1 = OAuth1(
      api_key="YOUR_API_KEY",
      api_secret="YOUR_API_SECRET",
      access_token="YOUR_ACCESS_TOKEN",
      access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
  )

  client = Client(auth=oauth1)

  # 取消置顶列表
  response = client.lists.unpin(
      user_id="2244994945",
      list_id="1454155907651158017"
  )

  print(f"Pinned: {response.data.pinned}")
  ```

  ```javascript JavaScript SDK
  import { Client, OAuth1 } from "@xdevplatform/xdk";

  const oauth1 = new OAuth1({
    apiKey: "YOUR_API_KEY",
    apiSecret: "YOUR_API_SECRET",
    accessToken: "YOUR_ACCESS_TOKEN",
    accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
  });

  const client = new Client({ oauth1 });

  // 取消置顶列表
  const response = await client.lists.unpin("2244994945", "1454155907651158017");

  console.log(`Pinned: ${response.data?.pinned}`);
  ```
</CodeGroup>

**响应：**

```json
{
  "data": {
    "pinned": false
  }
}
```

***

<div id="important-notes">
  ## 重要说明
</div>

<Note>
  * 你只能置顶你关注或拥有的列表
  * 置顶的列表会显示在 X 应用中你的列表顶部
  * 你可以置顶的列表数量有限
</Note>

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="置顶列表查询" icon="thumbtack" href="/zh/x-api/lists/pinned-lists/quickstart/pinned-list-lookup">
    获取你的置顶列表
  </Card>

  <Card title="列表查询" icon="list" href="/zh/x-api/lists/list-lookup/quickstart">
    获取列表详情
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/lists/pin-list">
    完整端点文档
  </Card>
</CardGroup>