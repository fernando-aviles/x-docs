---
title: 置顶列表概览
sidebarTitle: 概览
description: 开始使用置顶列表相关端点
keywords: ["置顶列表概览", "置顶列表快速入门", "列表置顶概览", "列表置顶概览"]
---

import { Button } from '/snippets/zh/button.mdx';

固定列表（Pinned Lists）端点可用于查询用户已置顶的列表，并管理哪些列表被置顶。

<Note>
  **先决条件**

  在开始之前，你需要：

  * 一个拥有已批准 App 的[开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 用户访问令牌（OAuth 1.0a 或 OAuth 2.0 PKCE）
</Note>

***

<div id="available-endpoints">
  ## 可用端点
</div>

<CardGroup cols={2}>
  <Card title="查看置顶列表" icon="thumbtack" href="/zh/x-api/lists/pinned-lists/quickstart/pinned-list-lookup">
    获取你的置顶列表
  </Card>

  <Card title="管理置顶列表" icon="pin" href="/zh/x-api/lists/pinned-lists/quickstart/manage-pinned-lists">
    置顶或取消置顶列表
  </Card>
</CardGroup>

***

<div id="authentication">
  ## 认证
</div>

| 操作 | 认证方式 |
|:----------|:---------------|
| 查询置顶列表 | OAuth 1.0a 或 OAuth 2.0 PKCE |
| 置顶/取消置顶列表 | OAuth 1.0a 或 OAuth 2.0 PKCE |

<Warning>
  查询和管理操作都需要用户上下文身份验证。不支持仅应用（Bearer Token）身份验证。
</Warning>

***

<div id="quick-example">
  ## 快速示例
</div>

<CodeGroup dropdown>
  ```bash cURL
  # 获取置顶列表
  curl "https://api.x.com/2/users/2244994945/pinned_lists" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_USER_ACCESS_TOKEN")

  # 获取置顶列表
  response = client.lists.get_pinned("2244994945")

  for lst in response.data:
      print(f"{lst.name}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ accessToken: "YOUR_USER_ACCESS_TOKEN" });

  // 获取置顶列表
  const response = await client.lists.getPinned("2244994945");

  response.data?.forEach((lst) => {
    console.log(lst.name);
  });
  ```
</CodeGroup>

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="列表查询" icon="list" href="/zh/x-api/lists/list-lookup/quickstart">
    获取列表详情
  </Card>

  <Card title="管理列表" icon="pen" href="/zh/x-api/lists/manage-lists/quickstart">
    创建和更新列表
  </Card>
</CardGroup>