---
title: 管理列表成员
sidebarTitle: 管理列表成员
description: 在列表中添加或移除成员
keywords: ["管理列表成员快速入门", "添加列表成员快速入门", "移除列表成员快速入门", "列表成员管理"]
---

import { Button } from '/snippets/zh/button.mdx';

本指南将指导你如何向 List 列表添加和移除成员。

<Note>
  **先决条件**

  在开始之前，你需要：

  * 一个拥有已获批准 App 的[开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 用户访问令牌（OAuth 1.0a 或 OAuth 2.0 PKCE）
  * 一个你拥有的 List 列表
</Note>

***

<div id="add-a-member-to-a-list">
  ## 向 List 添加成员
</div>

<Steps>
  <Step title="获取 List ID 和用户 ID">
    你需要获取你的 List 的 ID，以及你想添加的那位用户的用户 ID。可以使用 [user lookup endpoint](/zh/x-api/users/lookup/introduction) 查找用户 ID。
  </Step>

  <Step title="添加成员">
    <CodeGroup dropdown>
      ```bash cURL
      curl -X POST "https://api.x.com/2/lists/1441162269824405510/members" \
        -H "Authorization: Bearer $USER_ACCESS_TOKEN" \
        -H "Content-Type: application/json" \
        -d '{"user_id": "2244994945"}'
      ```

      ```python Python SDK
      from xdk import Client
      from xdk.oauth1_auth import OAuth1

      oauth1 = OAuth1(
          api_key="YOUR_API_KEY",
          api_secret="YOUR_API_SECRET",
          access_token="YOUR_ACCESS_TOKEN",
          access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
      )

      client = Client(auth=oauth1)

      # 向 List 添加成员
      response = client.lists.add_member(
          list_id="1441162269824405510",
          user_id="2244994945"
      )

      print(f"Is member: {response.data.is_member}")
      ```

      ```javascript JavaScript SDK
      import { Client, OAuth1 } from "@xdevplatform/xdk";

      const oauth1 = new OAuth1({
        apiKey: "YOUR_API_KEY",
        apiSecret: "YOUR_API_SECRET",
        accessToken: "YOUR_ACCESS_TOKEN",
        accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
      });

      const client = new Client({ oauth1 });

      // 向 List 添加成员
      const response = await client.lists.addMember("1441162269824405510", {
        userId: "2244994945",
      });

      console.log(`Is member: ${response.data?.is_member}`);
      ```
    </CodeGroup>
  </Step>

  <Step title="查看响应结果">
    ```json
    {
      "data": {
        "is_member": true
      }
    }
    ```
  </Step>
</Steps>

***

<div id="remove-a-member-from-a-list">
  ## 从列表中移除成员
</div>

<CodeGroup dropdown>
  ```bash cURL
  curl -X DELETE "https://api.x.com/2/lists/1441162269824405510/members/2244994945" \
    -H "Authorization: Bearer $USER_ACCESS_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client
  from xdk.oauth1_auth import OAuth1

  oauth1 = OAuth1(
      api_key="YOUR_API_KEY",
      api_secret="YOUR_API_SECRET",
      access_token="YOUR_ACCESS_TOKEN",
      access_token_secret="YOUR_ACCESS_TOKEN_SECRET"
  )

  client = Client(auth=oauth1)

  # 从列表中移除成员
  response = client.lists.remove_member(
      list_id="1441162269824405510",
      user_id="2244994945"
  )

  print(f"Is member: {response.data.is_member}")
  ```

  ```javascript JavaScript SDK
  import { Client, OAuth1 } from "@xdevplatform/xdk";

  const oauth1 = new OAuth1({
    apiKey: "YOUR_API_KEY",
    apiSecret: "YOUR_API_SECRET",
    accessToken: "YOUR_ACCESS_TOKEN",
    accessTokenSecret: "YOUR_ACCESS_TOKEN_SECRET",
  });

  const client = new Client({ oauth1 });

  // 从列表中移除成员
  const response = await client.lists.removeMember(
    "1441162269824405510",
    "2244994945"
  );

  console.log(`Is member: ${response.data?.is_member}`);
  ```
</CodeGroup>

**响应：**

```json
{
  "data": {
    "is_member": false
  }
}
```

***

<div id="important-notes">
  ## 重要说明
</div>

<Note>
  * 你只能管理自己拥有的列表中的成员
  * 将用户添加到列表不需要对方的许可
  * 用户可以看到自己被添加到的公开列表
</Note>

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="列表成员查询" icon="users" href="/zh/x-api/lists/list-members/quickstart/list-members-lookup">
    获取列表成员
  </Card>

  <Card title="管理列表" icon="pen" href="/zh/x-api/lists/manage-lists/quickstart">
    创建和更新列表
  </Card>

  <Card title="API 参考" icon="code" href="/zh/x-api/lists/add-list-member">
    完整端点说明
  </Card>
</CardGroup>