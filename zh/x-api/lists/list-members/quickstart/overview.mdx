---
title: 列表成员概览
sidebarTitle: 概览
description: 列表成员端点快速入门
keywords: ["列表成员快速入门", "列表成员概览", "列表成员教程", "列表成员关系快速入门"]
---

import { Button } from '/snippets/zh/button.mdx';

List 成员端点允许你查找某个 List 的成员并管理成员资格。

<Note>
  **先决条件**

  在开始之前，你需要：

  * 一个已批准 App 的[开发者账号](https://developer.x.com/en/portal/petition/essential/basic-info)
  * 你的 App 的 Bearer Token（用于查找）
  * 用户访问令牌（User Access Token，用于管理成员）
</Note>

***

<div id="available-endpoints">
  ## 可用端点
</div>

<CardGroup cols={2}>
  <Card title="查询列表成员" icon="users" href="/zh/x-api/lists/list-members/quickstart/list-members-lookup">
    获取列表成员
  </Card>

  <Card title="管理列表成员" icon="user-plus" href="/zh/x-api/lists/list-members/quickstart/manage-list-members">
    添加或移除成员
  </Card>
</CardGroup>

***

<div id="authentication">
  ## 身份验证
</div>

| 操作 | 身份验证方式 |
|:----------|:---------------|
| 查找成员 | Bearer Token、OAuth 1.0a 或 OAuth 2.0 PKCE |
| 添加或移除成员 | OAuth 1.0a 或 OAuth 2.0 PKCE |

***

<div id="quick-example">
  ## 快速示例
</div>

<CodeGroup dropdown>
  ```bash cURL
  # 获取列表成员
  curl "https://api.x.com/2/lists/84839422/members" \
    -H "Authorization: Bearer $BEARER_TOKEN"
  ```

  ```python Python SDK
  from xdk import Client

  client = Client(bearer_token="YOUR_BEARER_TOKEN")

  # 获取列表成员
  for page in client.lists.get_members("84839422"):
      for user in page.data:
          print(f"{user.username}")
  ```

  ```javascript JavaScript SDK
  import { Client } from "@xdevplatform/xdk";

  const client = new Client({ bearerToken: "YOUR_BEARER_TOKEN" });

  // 获取列表成员
  const paginator = client.lists.getMembers("84839422");

  for await (const page of paginator) {
    page.data?.forEach((user) => {
      console.log(user.username);
    });
  }
  ```
</CodeGroup>

***

<div id="next-steps">
  ## 后续步骤
</div>

<CardGroup cols={2}>
  <Card title="列表查找" icon="list" href="/zh/x-api/lists/list-lookup/quickstart">
    获取列表详情
  </Card>

  <Card title="列表中的帖子" icon="message" href="/zh/x-api/lists/list-tweets/quickstart">
    获取列表中的帖子
  </Card>
</CardGroup>